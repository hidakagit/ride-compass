# レビュー基準改善提案（2026-08-18）

**対応状況（2026-08-18追記）**: 提案2〜7はユーザー承認どおり反映済み
（complexity.md / overall.md / consistency.md / principles.md / ui.md）。
提案1は**ユーザー指示により修正のうえ反映**: 「特定ファイル（page.tsx）個別の閾値管理は
微妙。肥大化傾向のファイルを汎用的に監視したい」との判断を受け、page.tsx個別閾値の新設は
取り下げ、代わりに complexity.md へ**「規模ウォッチ」節（実装ファイル行数の横断実測・
前回比つきの規模ウォッチ表・発火条件〔前回比+15% / 1,000行新規超過 / 上位5件新規登場〕→
KEEP/抽出提案/個別閾値のいずれかへ必ず分類）**を新設した。既存のMapView.tsx個別閾値は
経過措置として維持（T123完了時の次閾値設定後も、以降の新規個別閾値は原則作らず
規模ウォッチで判断する）。context.mdの現状化（8軸表記の除去・レシピ付き軸と
照合フィクスチャ機構の追記・Keep List代表例のポインタ化）は提案どおり反映。
overall.md の変更コスト表参照も提案2に合わせ「直近のcomplexityレビュー結果」へ追従させた。

## 分析サマリ

- **対象期間**: 2026-08-16〜2026-08-18。history/ 6件（2026-08-16_all / 2026-08-16_improve /
  2026-08-17_all / 2026-08-17_consistency / 2026-08-18_overall / 2026-08-18_complexity）＋
  docs/直下の基盤構築以前レビュー6件＋improvement-plan.mdのT89〜T124の起票・対応実績。
- **主な傾向**:
  1. **原則化・機械化した項目は機能している**: タイル世代の対上げは2026-08-17_allでF-1
     （2回目の漏れ）を指摘後、T117（v9系変更）・T119（v10）ではいずれも同一コミットで
     対上げされた。閾値付きKEEPの「次の閾値必須」ルール（2026-08-16 improve 提案2）も、
     2026-08-18のMapView閾値発火を正しく検出→T123起票へ繋がった。
  2. **新しい再発パターンは「双子実装の片側だけ修正」**: T120（service糊のログ集計）・
     T121-a（Overrideモデルの閾値順序検証）で同型ミスが2週間に2回発生。既存の
     「手動同期ペア」観点（検知テストの有無を見る）ではカバーできない
     （双子は同期「値」ではなく同期「構造」のため）。構造的解消はT122/T123で起票済みだが、
     完了までの暫定チェックが基準に無い。
  3. **レビュー外で発覚したUI不具合が2件**: T112（情報アイコンがスマホのタップで開かない）・
     T118（基準値ピッカーがモバイル幅で画面外へ溢れ、本番実機スクショで発覚）。いずれも
     研究UIパネルのモバイル実機確認が既存ui.mdの観点（主要導線中心）から漏れていた。
  4. **並行セッション干渉が形を変えて3回**: T119のT番号衝突（2回のマージ）・T118の
     検証ポート奪取・2026-08-18レビュー中の作業ツリー変化（レビュー実施中にgit statusが
     5→9ファイルへ増加）。レビュー実施手順側の防御が未明文化。
  5. **context.mdの陳腐化が再発**: 「現在8軸」（実際は9軸、T119）・MapView閾値の記述
     （発火済みなのに「再設定済み」のまま）。2026-08-16 improve 提案3（数値を書かない）の
     適用漏れ箇所が残っていた。

## 変更提案

### 提案1: context.mdの現状化（陳腐化数値の除去＋レシピ付き軸・同期機構の追記）

- 対象ファイル: `review/context.md` の「重要なデータフロー」4項・「正準定義と同期機構」・
  「意図的な設計判断」
- 種別: 修正（陳腐化の除去＋現状の追記）
- 根拠:
  - 50行目「現在8軸（標高・風・…・事故密度）」→ T119以降9軸。軸数・列挙の直書きは
    2026-08-16 improve 提案3で除去した「書いた瞬間から陳腐化する値」と同じ構造要因。
  - 79-80行目のKeep List代表例「T91で新閾値『MapView.tsx 1,800行 or
    STATIC_OVERLAY_LAYERS 10種』を再設定」→ 2026-08-18に行数側が発火（実測1,908行、
    history/2026-08-18_complexity.md F-1）。T123完了条件で次閾値の再設定が予定されている。
  - 「正準定義と同期機構」節に、T107/T119で確立した最大級の同期機構
    （domain純関数⇔MapLibre expression⇔生成フィクスチャ照合）が未記載。
- 変更案:
  ```diff
   4. **評価軸の追加は1本道**: 取込（import_profile.yaml / ALLOWED_WAY_TAGS）→ domain純関数 →
      共通合成 → route_preference.yaml → AttributeRepository＋ファサード対称委譲 →
      フロントはカタログ編集のみ。エンジンファイルに軸固有の知識を書かない。
  -   現在8軸（標高・風・路面・停止密度・交差点密度・trafficStress・自転車インフラ・事故密度）。
  +   **軸の数・一覧はarchitecture.md §7を正として都度参照する（本ファイルに書かない）。**
  +   交通ストレス・安全度は「レシピ付き軸」（判定レシピをYAML/リクエスト上書き可能な形へ
  +   外出しし、タイルへは材料タグのみ焼き込み、最終値はフロントのMapLibre expressionが計算）
  +   であり、追加経路が通常軸の1本道より大きい（変更コスト表G'参照。共通基盤はT122/T123で
  +   整備中）。

   （「正準定義と同期機構」節へ追記）
  +- レシピ付き軸のPython⇔MapLibre expression二重実装は、export_openapi.pyが書き出す
  +  生成フィクスチャ（traffic-stress/safety-test-cases.json・*-recipe.json）と
  +  照合テストで同期を担保する（同期バグはこの照合が無い「糊」でのみ発生した実績:
  +  T120・T121-a）。

   （「意図的な設計判断」の代表例を更新）
  -- `page.tsx` / `MapView.tsx` の分割見送り（閾値監視つき。当初の閾値は決めておいた2点
  -  ともT47で消化済み、T91で新閾値「MapView.tsx 1,800行 or STATIC_OVERLAY_LAYERS 10種」を再設定）
  +- `page.tsx` / `MapView.tsx` の分割見送り（閾値監視つき。現在有効な閾値・発火状況は
  +  直近のcomplexityレビュー（history/）とimprovement-plan.mdの該当タスク（T91→T123）を
  +  正として参照する。page.tsxにも閾値を新設: 「useState+useStoredState合計40件 or
  +  1,300行到達」でグルーピングを判断（history/2026-08-18_complexity.md F-3）
  ```
- 影響: context.mdから陳腐化しうる値がさらに減り、レシピ付き軸という新概念が
  レビュー前提に載る。page.tsx閾値の新設はKeep Listの実質的変更のため、
  この提案の承認をもって閾値が有効になる点に注意。

### 提案2: complexity.mdの変更コスト表を「直近レビュー結果との比較」へ変更し、レシピ系シナリオを追加

- 対象ファイル: `review/complexity.md` の「### 変更コストの計測」
- 種別: 修正＋追加
- 根拠: 現行は「complexity-review-2026-08-16.md の変更コスト表（A〜G）と比較」と
  固定ファイル参照だが、2026-08-18_complexity.mdで実測に基づく新行（B'段階数変更=19ファイル
  〔T117〕・G'レシピ付き軸追加=64ファイル・+3,677行〔T119〕・Hレシピ項目追加=12〜15箇所）を
  追加した。比較先を固定したままだと、次回レビューが最新の実測を無視して古い表と比較する。
  2026-08-16 improve 提案4（外部データソース追加ケース）と同じ拡張パターン。
- 変更案:
  ```diff
   ### 変更コストの計測
   代表的な変更シナリオ（評価軸追加・道路属性追加・エンジン追加・レイヤー追加）、
   および**外部静的データソース追加**（…T50・T51が実例）について
  +さらに**レシピ付き評価軸の追加**（G'、T119実例: 64ファイル）・**レシピの段階数変更**
  +（B'、T117実例: 19ファイル）・**レシピ項目の追加**（H）について
   「編集が必要なファイル数・箇所数」を実際に数え、
  -complexity-review-2026-08-16.md の変更コスト表（A〜G）と比較して悪化/改善を報告する。
  +**直近のcomplexityレビュー結果（history/の最新complexity。初回のみ
  +docs/complexity-review-2026-08-16.md）の変更コスト表と比較**して悪化/改善を報告する。
  +T122/T123（レシピ共通基盤）完了後の最初のレビューでは、G'/Hの削減効果
  +（見込み: 64→40ファイル前後・12〜15→7〜9箇所）を実測で検証する。
   評価軸追加の1本道（context.md）から外れた箇所数の増加は最優先の指摘対象。
  ```
- 影響: 変更コストの基準線が常に最新実測へ追従する。T122/T123の効果検証が
  次回レビューの明示的な宿題になる。

### 提案3: consistency.mdへ「双子実装の対称性チェック」を追加（T122/T123完了までの暫定・削除条件付き）

- 対象ファイル: `review/consistency.md` の「### 設計 ↔ 実装」末尾
- 種別: 追加（削除条件付き）
- 根拠: T120（`get_traffic_stress_breakdown`のログ集計修正が双子
  `get_safety_breakdown`へ未反映）・T121-a（`TrafficStressRecipeOverride`の閾値順序検証が
  双子`SafetyRecipeOverride`へ未反映）と、「同じ構造を対で持つ実装の片側だけ直す」ミスが
  2回発生（格上げ条件「同型2回以上」成立）。既存の「手動同期ペアの検知テスト網羅」観点は
  値の同期を見る項目で、構造の対称性は対象外だった。
- 変更案:
  ```diff
   ### 設計 ↔ 実装
   （既存項目の末尾へ追加）
  +- **双子実装の対称性**: 交通ストレス⇔安全度のように「同じ構造を対で持つ」実装群
  +  （domain breakdown・*Override APIモデル・service糊・expression・ポップアップ等）の
  +  片側だけを変更したコミットが対象範囲にある場合、対になる側へ同じ変更が必要か・
  +  反映済みかを確認する（T120・T121-aで「片側だけ修正」が2回発生した実績）。
  +  **削除条件: T122・T123（レシピ共通基盤）完了で双子の糊が共通化されたら、
  +  本項目は削除する**（構造的解消が正であり、チェックリストでの恒久運用はしない）。
  ```
- 影響: T122/T123完了までの橋渡しとして同期ミスの3回目を防ぐ。削除条件を最初から
  書くことで基準ファイルの恒久肥大を避ける。

### 提案4: consistency.mdへ「MVT焼き込み値変更時のタイル世代対上げ確認」を明文化

- 対象ファイル: `review/consistency.md` の「### 設計 ↔ 実装」（「ドメインルールと実装の乖離」
  項目の直後）
- 種別: 追加
- 根拠: 2026-08-17_all「レビュー基準への示唆」で提案済み（T70・T93の2回発生で格上げ条件成立）
  だが、まだ基準へ未反映だった。その後T117/T119では正しく対上げされており再発はないものの、
  レシピ項目・材料タグの増減（T122のshoulder撤去は**プロパティ削除＝非互換**）が控えるため、
  正式反映しておく価値が高い。
- 変更案:
  ```diff
  +- タイル世代の対上げ: MVT焼き込み値（CASE式・材料タグ・domain純関数）を変更した
  +  コミットが、対応するタイル世代定数（`ROAD_SURFACE_TILE_VERSION`等）と生成物
  +  （region-tile-config.json）を同一コミットで上げているか（T70・T93で対上げ漏れが
  +  2回発生。プロパティ削除を伴う変更はv9と同じ非互換のため、frontend先行デプロイの
  +  注意書きの有無も確認する）
  ```
- 影響: consistencyレビューのチェック1項目増。T122（shoulder撤去）の検証に直接効く。

### 提案5: principles.mdへ「並行セッション下のレビュー実施手順」を追加

- 対象ファイル: `review/principles.md` の「## 共通実行手順」手順3
- 種別: 追加
- 根拠: 並行セッション起因の混乱が形を変えて3回発生した:
  (1) T119のT番号2重衝突（2回のマージ・改番）、(2) T118の検証ポート奪取
  （誤ったビルドを検証）、(3) 2026-08-18レビュー実施中に作業ツリーが変化
  （git statusが5→9ファイルへ増加、T121を別セッションが進行中だった。
  history/2026-08-18_overall.md冒頭の注記参照）。レビューのEvidenceが動く対象を
  指すと再現不能な指摘になる。
- 変更案:
  ```diff
   3. 対象範囲を決める（引数があればそれに従う。…）
  +   **開始時に対象コミット（HEAD）を記録し、Evidenceはコミット済みHEADへ固定する。**
  +   並行セッションがレビュー中に作業ツリーを変更しうる（2026-08-18に実測。作業ツリーの
  +   進行中変更は「参考」として区別して記す）。**起票案にはT番号を仮置きしない**
  +   （並行セッションとの番号衝突がT119で2回発生した実績。「起票案」とだけ書き、
  +   番号はユーザーが起票する時点で確定する）。
  ```
- 影響: レビューの再現性が上がる。実施手順が1段増えるが、`git rev-parse HEAD`の記録のみで
  コストはほぼゼロ。

### 提案6: ui.mdへ「新設・変更パネルのモバイル実機確認（研究UI含む）」を明示

- 対象ファイル: `review/ui.md` の「### モバイル/デスクトップ」および
  「### 研究用UIと一般ユーザー向けUIの区別」
- 種別: 修正（既存観点の強化）
- 根拠: T112（情報アイコンのtitleツールチップがスマホのタップで開かない）・T118
  （T117で5段階化した基準値ピッカーがモバイル幅で画面外へ溢れ、**本番の実機スクショで
  ユーザーが発見**）と、研究UIパネルのモバイル起因不具合がレビュー外で2件発覚した。
  現行ui.mdのモバイル観点は「主要導線（ボトムシート・タブバー・地図操作）」中心で、
  新設パネルの収まり・タップ操作が対象として明示されていない。また「研究UIは洗練度は
  低い基準でよい」の記述が、操作可能性（タップで開く・画面内に収まる）まで
  低基準と誤読されうる。
- 変更案:
  ```diff
   ### モバイル/デスクトップ
   - 390px幅での主要導線（ボトムシート・タブバー・地図操作の共存）
  +- **対象範囲に新設・変更されたパネル・フォームがある場合（研究UI含む）、390px幅での
  +  収まり（横スクロール・画面外への溢れ）とタップ操作（ホバー/title依存UIが
  +  タップで機能するか）を必ず実機確認する**（T112・T118の2件がレビュー外で
  +  ユーザー/本番から発覚した実績）
   - デスクトップ/モバイルでの機能差が意図通りか

   ### 研究用UIと一般ユーザー向けUIの区別
  -- 研究UIも操作可能性は評価するが、洗練度は一般UIより低い基準でよい
  +- 研究UIも操作可能性（タップで開く・入力できる・画面内に収まる）は一般UIと同基準で
  +  評価する。低い基準でよいのは見た目の洗練度のみ
  ```
- 影響: UIレビューの実機確認対象が増える（パネル単位）。研究UIのモバイル不具合を
  レビュー段階で拾えるようになる。

### 提案7: overall.mdの「撤去済み機構への言及」観点を削除（トリガー成立）

- 対象ファイル: `review/overall.md` の「### 新旧設計の混在・残骸」3つ目の箇条書き
- 種別: 削除
- 根拠: 2026-08-16 improve の削除候補で「該当なしが2回続いたら再検討」とトリガーを
  設定済み。その後2026-08-17_all（「撤去済み機構への言及残骸なし✔」）・
  2026-08-18_overall（「新規混入なし✔」）と**2回連続で該当なし**となりトリガー成立。
  撤去（T22、2026-08-16）から2ヶ月近く経過し、直前の箇条書き
  「旧実装・デッドコード・未使用エクスポート」が一般形として同じ内容をカバーする。
- 変更案:
  ```diff
   ### 新旧設計の混在・残骸
   - 「並行追加→設定で切替→旧削除」の3段階（設計原則9）の途中にあるものに、…
   - 旧実装・デッドコード・未使用エクスポート・テストのみが参照するコード
  -- 撤去済み機構（Overpassフォールバック・ORS extras等）への言及がdocs・コメントに残っていないか
  ```
- 影響: チェック1項目減（基準の軽量化）。将来別の大型撤去（例: T122のshoulder撤去）が
  起きた場合は、一般形の「旧実装・デッドコード」観点で検出できる。

## 削除候補（価値の低い観点）

- 提案7として正式提案（撤去済み機構への言及チェック）。
- それ以外に「毎回該当なし」が続く観点は現時点でなし。overall/complexityの分担
  （2026-08-16改定）は今回も重複指摘ゼロで機能しており変更不要。

## 見送り（検討したが提案しないもの、理由つき）

- **閾値付きKEEP「起票後未消化なら毎回P2再指摘」ルール**（2026-08-17_allの示唆）:
  T91は消化済み・今回の発火は即日T123起票と、現行運用（次閾値必須ルール）だけで
  回っている。未消化放置が再発したら再提案する。
- **レシピ外出し機構・共有プリミティブ経由の軸追加をKeep Listへ明文化**:
  T122/T123で構造自体が変わる最中のため、完了後の最初のcomplexityレビューで
  確定形をKeep Listへ反映する方が正確（トリガー: T122・T123完了）。
- **all.mdのPhase構成変更**: 今回はallを実施しておらず（overall+complexity同時実施は
  分担どおり機能）、判断材料なし。
- **T124（measure_axis_stats.py）のレビューへの組み込み**（例: complexityレビューで
  毎回実行して相関・丸め損失を監視する案）: 計測スクリプト自体が未実装。T124完了後、
  「レビューで毎回回す価値があるか」をコスト（dev DB必須・実行時間）と併せて再検討する。
- **並行セッションのポート分離手順の基準化**: T118で実害があったが、レビュー基盤の
  管轄（review/*.md）ではなく開発環境運用の問題。`.claude/launch.json`・メモリ
  （Parallel Worktree Resource Contention）側で管理する。
