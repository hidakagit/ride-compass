# 統合レビュー（2026-09-10、第6回）

- 実施日: 2026-09-10
- 対象コミット（HEAD固定）: `87f7333e`（`git fetch origin`実施、origin/masterと一致）
- 比較ベースライン: `3749bdcf`（前回統合レビュー`2026-09-08_all.md`の対象コミット）
- 対象範囲: **95コミット / 412ファイル・17,533追加・4,297削除**（うち実装238ファイル・8,704追加）
- 実施方式: 専用worktree（`87f7333e`をdetached checkout）。overall（Phase 3）・consistency（Phase 5）は
  ドメイン単位で**11シャード**へ分割し、1シャード1Agentへ両レンズの確認観点を渡して並行実施。
  complexity（Phase 4）・ui（Phase 6）は単独実施。
  **Agentの指摘のうちP1以上は本体でコードを再確認して確定させた**（誤りが1件、過大評価が2件見つかり訂正した。Phase 7参照）。
- 本レビュー固有の実測: この環境にはPostgreSQLしか無かったため**PostGIS拡張を導入し、CIと同じ手順
  （`bootstrap_ci_db.py`→`export_openapi.py`）を再現**して生成物ドリフトを実測した。

## Executive Summary

前回レビューの指摘131件を束ねた**27本のタスク（T659〜T685）が本範囲内で全件完了**しており、
負債返済の実行力は前回から一段上がっている。規模ウォッチの次閾値も`size_watch.json`へ書き戻され
（T658）、2回連続で「鳴りっぱなし」だった安全弁が機能を回復した。
`review_checks.py docs`の機械チェックは全項目0件、backend 1,733 passed・ruffクリーン、
eslint 0 problems である。

その一方で、本レビューの中心的な発見は**「置いたはずのガードが実際には鳴っていない」が
4つの異なる形で同時に起きていた**ことである。

1. CIのAPI契約ドリフト検知が、ジョブ既定の作業ディレクトリを継承した結果
   **pathspecがどこにもマッチせず常にexit 0**になっていた（実測で確認）。
2. その空振りの陰で、静的軸カタログが**本番の軸集合と実際に乖離**していた
   （PostGISを立ててCIと同じ手順を再現し、224行追加/105行削除の差分を再現）。
3. 逆に、正しく鳴っているドリフト検知テストは**5コミット連続で無視され、masterのCIが赤のまま**だった。
4. 蛇行軸では、バッチ登録漏れ・カバレッジ診断の母集団取り違え・キャッシュ経路の材料復元漏れが
   **同じ方向に3つ重なり、互いの検知を打ち消していた**。

これに加えて、本範囲で新設された「利用者に**絶対の事実**を見せる」機能群
（T686 分布プレビュー・T687 得点の隣の生値・T690 最短距離ルート）が、いずれも
**その主張が成り立たない入力への手当てを欠いている**。とくにT690の最短ルートは
0次ハードフィルタ（自転車通行禁止・自動車専用道路・利用者が指定した勾配上限）を
**すべて迂回した経路を、先頭固定で必ず1本返す**——本レビューで唯一、利用者を直接危険へ導きうる欠陥である。

**過去5回のレビューでP0は0件だったが、今回2件を立てた。** 深刻度は修正の大きさではなく
状態の深刻さで付けている（1件は1行の修正で直る）。

## Phase 1: 全体把握

### 対象範囲
- 対象コミット（HEAD固定）: `87f7333e`（`git fetch origin`実施、origin/masterと一致）
- 比較ベースライン: `3749bdcf`（前回統合レビュー`2026-09-08_all.md`の対象コミット）
- 差分: **95コミット / 412ファイル・17,533追加・4,297削除**（うち実装238ファイル・8,704追加）
- 実施方式: 専用worktree `scratchpad/rc-review-87f7333`（`87f7333e`をdetached checkout）。
  overall（Phase 3）・consistency（Phase 5）はドメイン単位で**11シャード**へ分割し、
  1シャード1Agentへ両レンズの確認観点を渡して並行実施。complexity（Phase 4）・ui（Phase 6）は単独実施。
  Agentの指摘のうちP1以上は**本体でコードを再確認して確定**させた。

### 変更の中心（コミット数）
T655（停止密度の作り直し）15、T687（生値表示）7、T692（way単位の材料）5、T691（蛇行軸）4、
T654（自転車の停止要因）4、T657（列挙・既定値の生成物集約）3。

前回レビュー（第5回）の指摘131件を27本のタスク（T659〜T685）へ起票し、**その全27本が本範囲内で完了**している。
残る未完了はT654（PBF再取込待ち）・T655・T688・T689の4本。改善計画全体では完了453本 / 未完了28本。

### 本範囲の性格
差分の中身は大きく3系統に分かれる。
1. **前回レビュー指摘の消化**（T659〜T685の27本）。残骸撤去・ruff導入・docs現状化・
   テスト空白埋め・CSS重複解消など、負債返済が中心。
2. **新しい材料・軸の追加**（T654 停止要因、T655 停止密度の作り直し、T691/T692 蛇行軸）。
   新raw層テーブル`way_geometry`（migration 0036）と`road_edges.curvature_deg_per_km`（0035）を伴う。
3. **「軸単体で判断できるようにする」機能群**（T686 分布プレビュー、T687 得点の隣に生値、
   T690 目的地モードの最短距離ルート）。いずれも**新しい種類の値を利用者へ見せる**変更。

**指摘の重心は3にある。** 1（負債返済）は網羅的に片付いており、2（材料追加）は1本道が概ね機能している。
一方3は、いずれも「これは絶対の事実です」という強い主張を伴う表示であるにもかかわらず、
その主張が成り立たない条件の検討が追いついていない（Phase 7 テーマB参照）。

### 過去レビュー指摘の解消状況（Regression確認）
- 前回P1×8件のうち、本範囲で起票された27本により**全件が対応済み**（起票状況で確認）。
- 前回complexityのG7-6（`timeoutMs=15000`がfrontend 7ファイルへ直書き）は**解消**
  （`lib/apiTimeouts.ts`が5定数の単一ソースになった、T679）。
- 前回complexityが提案した次閾値4件は**`size_watch.json`へ全件反映済み**（T658）。
  2回連続で「鳴りっぱなし」だった安全弁が機能を回復した。
- `python scripts/review_checks.py docs`の機械チェックは**全項目0件**（記載漏れ・死んだ参照・
  記載粒度違反・状態行不一致・未定義CSSトークン）。

## Phase 2: 定量メトリクス（対象 87f7333e、前回 3749bdcf）

| 指標 | 今回（87f7333e） | 前回（3749bdcf） |
|---|---|---|
| 実装本体（行） | backend/app 28,535・frontend/src 23,591・計 **52,126** | 49,310 |
| テスト（行） | backend 34,175・frontend 19,111・計 53,286 | 49,521 |
| 実装:テスト比 | 1 : 1.02 | 1 : 1.00 |
| ファイル数 | 実装 backend 144・frontend 149／テスト backend 145・frontend 127 | 実装 135/134・テスト 139/117 |
| コミット数（churn起点2026-09-03） | 519 | 465 |
| backend pytest（-m "not postgis"） | **1,733 passed** | 1,727 |
| backend ruff | **All checks passed** | — |
| frontend vitest | **1,142 passed / 2 failed（1,144）** | 1,063 passed |
| tsc --noEmit（next typegen後） | 0エラー | 0エラー |
| eslint | 0 problems | 0 problems |
| e2e（Playwright smoke） | 2 passed | — |
| npm audit | **critical 2 / high 3** / moderate 0 / low 0 | critical 0 / high 0 |

churn上位: `docs/improvement-plan.md` 240、`frontend/src/app/page.tsx` 49、
`frontend/src/components/Map/MapView.tsx` 43、`docs/tasks/T567.md` 39、
`docs/modules/backend/routing-engine.md` 35。

### 対象範囲
95コミット / 412ファイル・17,533追加・4,297削除（うち実装 238ファイル・8,704追加）。


## Phase 4: 複雑度平衡性レビュー

### 規模ウォッチ表（実測、`87f7333e`。前回=`3749bdcf`）

| ファイル | 今回 | 前回 | 増分 | 閾値 | 発火 |
|---|---:|---:|---:|---:|---|
| `components/Map/MapView.tsx` | 3,545 | 3,530 | +15 | 3,700 | — |
| `infrastructure/road_graph_repository.py` | 2,699 | 2,297 | **+402** | 2,800 | ○ +18% |
| `docs/architecture.md` | 2,682 | 2,718 | −36 | 3,000 | — |
| `app/page.tsx` | 2,159 | 2,198 | −39 | 2,400 | — |
| `services/road_graph_engine.py` | 2,089 | 1,981 | +108 | 2,200 | — |
| `components/AxisStudio/AxisComposer.tsx` | 1,793 | 1,741 | +52 | 1,750 | ○ 閾値超過 |
| `components/MapOverlayControls/MapOverlayControls.tsx` | 1,175 | 1,165 | +10 | — | — |
| `domain/material_catalog.py` | 1,003 | 890 | +113 | — | ○ 1,000行超過 |
| `domain/routing.py` | 826 | 812 | +14 | — | — |
| `domain/evaluation.py` | 822 | 1,255 | **−433** | — | — |
| `domain/axis_definitions.py` | 768 | 718 | +50 | — | — |
| `services/graph_service.py` | 582 | 568 | +14 | — | — |
| `components/Map/mapLayers.ts` | 579 | 606 | −27 | — | ○ 上位に新規登場 |
| `components/Map/icons.tsx` | 578 | 610 | −32 | — | — |

**前回提案の次閾値は`size_watch.json`へ全件反映済み**（T658で実施）。その結果、前回2回連続で
「鳴りっぱなし」だった4ファイルのうち`road_graph_engine.py`・`MapView.tsx`・`page.tsx`は
発火が止まった。安全弁が機能を回復している。

### コピペ検出（jscpd、min-lines=5 / min-tokens=60）

**ファイルを跨ぐクローン 14件・136行（重複率 0.14%）**。前回 13件・106行（0.12%）でほぼ横ばい。
新規で最大のものは20行:
`batch/precompute_way_attribute_counts.py:62` ⇔ `batch/precompute_way_curvature.py:47`。

### 写経の監視（jscpdが原理的に検出できない型）

- 前回G7-6（`timeoutMs=15000`がfrontend 7ファイルへ直書き）は**解消**。
  `frontend/src/lib/apiTimeouts.ts`が新設され、5種のタイムアウト定数の単一ソースになっている（T679）。
- `result`/`error_type`/`status`/`miss` … backend 11〜13ファイル。`log_external_call`へfieldsを
  積む定型でdocs/logging.mdが要求する書き方そのもの。**前回同様、問題としない**。

### 変更コスト表（実測）

| Case | 内容 | 今回 | 前回比 |
|---|---|---|---|
| G | 評価軸追加（既存プリミティブ範囲内） | axis_admin API 1回・コード変更0 | **変化なし** |
| I | 動的材料（`dedicated_way_value_layer`）追加 | 3件目の追加は無し（wind/gradientの2件のまま） | T672で汎用化を最後まで通したため、次の追加コストは前回想定より**小さい** |
| **新規: 一次属性＋材料＋軸の同時追加** | T691+T692（蛇行 curvature、新raw層テーブル`way_geometry`付き） | **実装18ファイル**（backend/app 15＋migration 2＋fixture 1）＋生成物3。frontend手書き **0…のはずが実際は2箇所必要だった**（下記F-1） | 前回のT624（外部静的データソース追加、実装19ファイル）と同水準 |


### 規模ウォッチ発火4件の分類（complexity.md「規模ウォッチ」節2に従い(a)(b)(c)へ）

- **`infrastructure/road_graph_repository.py`（2,699行、+402＝+18%）: (a) 理由つきKEEP**。
  増分はT691/T692（curvature・way_geometry）とT655（poi_counts_by_kind）のSQLで、すべて既存の
  5サブリポジトリ（`DerivedGraphRepository`・`RawOsmRepository`・`RoadSurfaceTileQuery`・
  `AttributeRepository`）の担当範囲内に収まっている。**新しい変更理由は持ち込まれていない。**
  個別閾値2,800は未超過のため据え置く。
- **`components/AxisStudio/AxisComposer.tsx`（1,793行、閾値1,750を43行超過）: (b) 抽出・分割の提案**。
  判断根拠はPhase 7-2 矛盾2を参照（純増がフォーム項目の増減ではなくpayload往復変換由来のため、
  T355のKEEPの前提が今回の増分には当てはまらない）。抽出案は`BreakpointCurveEditor`（136行）と
  `axisDraft.ts`（265行）。**閾値の引き上げは提案しない。**
- **`domain/material_catalog.py`（1,003行、1,000行の新規超過）: (c) 閾値付きKEEPの設定を提案**。
  `MaterialSpec`宣言32件＋小さな抽出器ファクトリで構成される宣言的カタログで、
  成長は材料数に比例する（T673が回復した「材料追加の1本道」の単一正本そのもの）。
  分割は正本を割ることになるため行わない。**次閾値提案: 1,400行**
  （現在32材料・1,003行から、材料が1.5倍になるまでを吸収できる値）。
- **`components/Map/mapLayers.ts`（579行、上位に新規登場）: (a) 理由つきKEEP**。
  実際には**−27行の減少**で、上位入りは他ファイルが縮んだことによる相対的なもの。対応不要。

## Phase 6: UI/UXレビュー（**モック環境での実機確認、スコープを明記して限定**）

### 実施できたこと・できなかったこと（先に明記する）

この環境には**データ投入済みのPostGISが無く**（PostGIS拡張は本レビューで導入したが、
関東7都県のOSMデータは無い）、実バックエンドを起動できなかった。そのため
`ui.md`が求める「dev環境を起動して実機確認」は**そのままの形では実施できていない**。

代わりに、リポジトリのE2Eスモークが**モックバックエンド（`e2e/fixtures.ts`）で動く構成**である
ことを利用し、`npm run start:standalone`（本番Dockerfileと同じエントリポイント）で
実ブラウザ（Chromium）を起動して操作した。**実ブラウザによる描画・操作は本物**だが、
**地図タイル・ルート候補・軸の値はすべてモック**である。したがって
「候補の評価値の妥当性」「地図レイヤーの色分け」「空タイル・カバレッジ外の見え方」は
今回**未検証**であり、下の所見はUIの構造・導線・レスポンシブに限られる。

### 実施した操作

- E2Eスモーク（`e2e/smoke.spec.ts`）: **2 passed**（ルート生成→候補一覧の表示／地図レイヤーのON/OFF切替）。
- デスクトップ幅1440×900: 初回表示 → 距離20km入力 → ルート生成 → 候補タブ2件表示 → 候補2へ切替。
- モバイル幅390×844: 初回表示 → 「ルート設定」シート展開 → 距離入力 → 生成 → 「ルート結果」→「地図の見え方」。
- 地図レイヤー: 「道路」グループ展開 → 「道路種別」チップの`aria-pressed`反転を確認。

### 所見

- **主要導線は破綻なく通る。** デスクトップは左サイドバーに「ルート設定 →（生成）→ ルート結果 →
  地図の見え方」が上から順に並び、初見でも「距離を決めて生成する」までは迷わない。
  モバイルは下部ナビ3つ（ルート設定／ルート結果／地図の見え方）とボトムシートで、
  デスクトップのサイドバーと同じ順序・同じ語彙が保たれている。**用語の衝突は見つからなかった。**
- **[P3] 候補の評価軸パネルで、否定文と一覧が同時に出る。** 「このルートで表示できる評価軸データが
  ありません」という文の**直下に**、9軸の一覧（各行に「データなし」バッジ）が並ぶ。
  文は「無い」と言い、一覧は「ある（が値が無い）」と示しており、初見では矛盾して読める。
  ただし**これはモックデータで全軸が欠損しているために出た状態**で、実データでは
  この否定文自体が出ない可能性が高い。実機確認が取れるまで**起票は保留**とし、
  実データで再現するかを次回のUIレビューで確認する。
- **[参考] `npm run start:standalone`が本番Dockerfileと同じ構成で起動することを確認**できた。
  これはE2Eの設計（`playwright.config.ts`のコメント）が意図したとおりに機能している。
- 前回（2026-09-03）のP3「生成ボタンラベルのデスクトップ/モバイル表記揺れ」は、
  今回モバイル側がボトムシート内に「ルート生成」ボタンを持つ形になっており**揺れは確認されなかった**
  （ただし対象範囲が異なるため解消と断定はしない）。

### 次回への申し送り

**UIレンズは、データ投入済みのdev環境が使える回にまとめて実施した方がよい。**
今回のようなモック環境では、UIレビューの中核である「空状態（候補0件・データ未整備地域・
カバレッジ外の空タイル）で、壊れているのかデータが無いのか操作が足りないのかが伝わるか」が
そもそも検証できない。今回の限定的な確認をもって「UIに問題なし」とは読まないこと。

## Phase 7: 矛盾と判断（相互確認）

### 7-1. 重複指摘の統合（根本原因側へ寄せた5件）

| 統合後のFinding | 統合元 | 根本原因側へ寄せた理由 |
|---|---|---|
| **I-1 検知の安全網が二重に落ちている**（P0） | F-A（CIドリフト検知の空振り）＋F-C（masterが5回連続で赤） | 症状は「ガードが鳴らない」と「鳴っているのに無視されている」で正反対だが、根は同じ——**機械的検知を置いたこと自体を「守られている」と読み替え、それが実際に効いているかを確認していない**。別々のFindingにすると、片方を直しても同じ型が残る |
| **I-3 静的軸カタログの陳腐化**（P1） | F-B＋S5(export_openapi:103)＋S10(evaluationAxes:17)＋F-I(raw_value_unit欠落)＋S10(PREFERENCE_AXIS_DESCRIPTIONS) | 5つとも「静的フォールバックが実行時APIの劣化コピーになっている」の別の側面。個別に直すと、次に軸が増えたとき同じ形で再発する |
| **I-4 蛇行軸の配線欠け**（P1） | S5(refresh_derived未登録)＋S4(material_coverage母集団)＋S1(EdgeMaterialTable.get())＋S4(OFFSET分割) | 4箇所が**同じ方向へ間違っており、互いの検知を打ち消している**。どれか1つでも正しければ気づけた——連鎖として1件に統合しないと重大さが伝わらない |
| **I-5 生値（raw value）の精度が仕様を満たさない**（P1） | S1/S2(route.py:205)＋S2/S9(axisRawValue.ts)＋S9(axisRawValue.test.ts) | 症状は backend の丸めと frontend の表示精度に分かれるが、原因は1つ——**0〜100の難易度用に作られた集約関数を、スケールの違う物理量へ流用した** |
| **I-6 負値軸の分布プレビューが嘘をつく**（P1） | S3/S5(axis_preview_service)＋S8(scoreDistribution)＋S8(zero_share)＋S3(docstring) | backend が潰れた分布を返し、frontend がそれを検証せず描き、**警告文が事実と正反対の助言を出す**という一本の因果。片側だけ直すと残りが矛盾する |

### 7-2. 矛盾の解消（レンズ間で判断が割れた3件）

**矛盾1: 一次属性の手書き対応表——「backendへ移設」か「削除」か**
- overall（自分の初期判断）: 軸は`chip_label`等をbackendの軸データへ移して手書き辞書を撤去済み。一次属性だけ取り残されているので**同じ移設をすべき**。
- S7: `PRIMARY_ATTRIBUTE_CHIP_LABELS`は**本番の消費者がゼロ**で、docstringが主張する用途（材料一覧の薄字ラベル）は実在しない。**削除**が正しい。
- **判断: S7を採る（削除）。** 本体で消費者をgrep網羅し確認した（参照は自身のテストとコメントのみ）。判断原則9「将来のための過剰設計を避ける」に照らせば、使われていない表をbackendへ移設するのは**負債を保存したうえで移設コストも払う**最悪の選択。
  ただしS7の表現「手書き3表すべてがbackendへ移せば消せる」は行き過ぎで、`PRIMARY_ATTRIBUTE_LAYER_IDS`は`page.tsx:39`経由で実利用があり、値域`MapLayerId`はフロント固有の概念でbackendは知らない。`PRIMARY_ATTRIBUTES_WITHOUT_LAYER`がテスト専用なのは**設計どおり**（「漏れ」と「意図的にレイヤー無し」の区別を明示する集合）。→ **削除は略名表のみ、残り2表は維持**。

**矛盾2: `AxisComposer.tsx`——「閾値引き上げ」か「分割」か**
- context.md Keep List: churnが継続中のためすぐには分割せず監視のみ先行する（T355、閾値付きKEEP）。
- S8: 今回の純増（+52行）は**フォーム項目の増減ではなくpayload往復変換**由来で、KEEPの前提（分割しても直後に別理由で書き換わる）が今回の増分には当てはまらない。**(b)分割提案**。
- **判断: S8を採る（分割）。** 判断原則6「責務分割の基準はコードの類似性ではなく同じ理由で変更されるか」。S8は領域ごとの行数と変更理由を実測で表にしており、`BreakpointCurveEditor`（136行、propsで閉じている）と Draft⇔payload変換群（265行、DOM非依存）は`breakpointTools.ts`・`scoreDistribution.ts`という**既存の前例と同じ粒度**。新しい方針の導入ではなく既存方針の適用にあたるため、複雑度を増やさない。
  ただし**優先度はP3に置く**——現状1,793行は閾値超過43行にすぎず、I-1〜I-6を差し置いて着手する性質ではない。

**矛盾3: `MapView.tsx`の`ensure*`関数——「実害が無いからKEEP」か「横展開して直す」か**
- complexity的な見方: `makeEnsureAttributeLineLayer`の色式はモジュール定数なので、再適用しなくても現在は壊れない。直すと分岐が増える。
- S6＋principles.md判断原則16: 「この汎用機構は今は動的な値に依存しないから実害なし」という判断は**T587で一度下してユーザー指摘により撤回した実績がある**。1箇所を直したら同じパターンの他箇所へ横展開する。
- **判断: 横展開して直す（判断原則16を優先）。** ただしT587が4関数を直した際に`makeEnsureAttributeLineLayer`だけ残った経緯を踏まえると、個別に直すより**「ensure関数は必ず既存レイヤーへも設定を再適用する」という形をファクトリ側で強制する**（各関数が個別にearly returnを書けない構造にする）方が根本的。P2として起票し、`ensureDynamicWeatherLayer`のfilter・icon-size未再適用（S6 P2）と同じタスクへ束ねる。

### 7-3. トレードオフの評価（現フェーズ＝研究優先・個人開発・低利用規模を前提に）

| トレードオフ | 天秤の結論 |
|---|---|
| **maplibre-gl 6系（major）への更新 vs 破壊的変更の確認コスト** | **`next`のみ先に上げ、maplibre-glは分離した独立タスクにする。** nextは16.3.0→16.3.4のminorで低リスク、maplibre-glは5→6のmajorで地図UI全体の再確認が要る。XSSの到達経路（OSMタグ由来の値がポップアップへ出る）は実在するが、現フェーズは**一般公開前・低利用規模**で、攻撃者が特定利用者を狙う動機が薄い。一括で止めるより分離して確実に片方を進める方が全体の安全性が上がる |
| **`axis_preview_service`の負値対応 vs 「負の生値を持つ軸は3本だけ」** | **直す。** 3本のうち`openness`・`bicycle_infra_quality`は**公開済み**で、しかも軸スタジオは「折れ点の妥当性を実データで判断する」ための道具。**較正が最も難しい軸で嘘の分布と正反対の助言を出す**のは、道具としての価値を負にする。研究優先フェーズだからこそ優先度が高い |
| **`stream_id_chunks`の長時間トランザクション（S5 P2） vs サーバーサイドカーソル化の利得（T660の実測: peak 55.9MB→1.1MB）** | **現状維持（KEEP）＋観測を足す。** T660は本番500万Edgeへの外挿で約1.5GB→一定という実測根拠を持つ（判断原則14）。idle in transactionによるVACUUM阻害は理屈としては正しいが、**本番で観測された事実ではない**。ロールバックではなく、バッチ実行中の`pg_stat_activity`の確認手順をdocsへ足すのが釣り合う |
| **`ComparisonPanel`へ生値を出す（S9 P3） vs 表の情報密度** | **出す。** 研究モードの実験比較は本プロジェクトの主目的そのもので、`scoring.yaml`（相対）と`route_preference.yaml`（絶対）の役割境界（context.md）に照らすと、**実験間で比較できるのは絶対値の側だけ**。得点だけを並べた比較表は、リクエスト間比較不可という設計判断と矛盾している |
| **`docs/modules`のdead symbol 10件（S11） vs 修正コスト** | **直すが優先度はP3。** 実害は「探しても見つからない」という理解コストのみ。ただし**機械チェックが識別子を見ていない**（`find_dead_file_refs`はバッククォート付きファイル名のみ）ことは仕組みの穴なので、検知の拡張（I-1と同じタスク群）へ入れる方が費用対効果が高い |

### 7-4. 相殺の確認（あるRecommendationが別のKEEPを壊さないか）

- **I-3（静的カタログを再生成する）は、S10が指摘するとおり`evaluationAxes.test.ts:79-82`を赤にする。**
  再生成すると`PREFERENCE_AXES`へ`axis_abdaded0be20`・`curvature`が入り、`PREFERENCE_AXIS_DESCRIPTIONS`にキーが無いため`description===""`でテストが落ちる。
  → **I-3の実施順序は「①`export_openapi.py`へ`description`・`raw_value_unit`の書き出しを足す → ②`PREFERENCE_AXIS_DESCRIPTIONS`を撤去し生成物由来へ寄せる → ③生成物を再生成」でなければならない。**
  ②を飛ばすと「原則2（新しい軸の追加にfrontendのコード変更を要しない）を破らないと赤が消せない」状態になる。
- **I-2（最短ルートのハードフィルタ迂回を直す）は、`test_select_shortest_distance_route_ignores_axis_cost`を赤にする**（S3が指摘）。
  当該テストは`highway="trunk"`を「軸コストが高い」と誤解したまま、フィルタ迂回の結果を期待値として固定している。
  → 修正時は**テストの意図を書き直す**（`trunk`ではなく実際に軸コストだけが高いEdgeを使う）。テストが赤くなることを「修正が間違い」と読まないよう、起票時に明記する。
- **I-1（CIのpathspecを直す）を先にやると、その瞬間にCIがI-3で赤になる。**
  → **I-1とI-3は同じ作業単位にする**（ガードを直す＝ガードが検知するものを同時に直す）。順序は I-3の①②③ → I-1 の順。
- Keep Listへの影響: `MapView.tsx`の分割見送り・Repositoryファサードのフラット委譲・PBF取込のasyncpg COPY直行は、本レビューのどのRecommendationとも衝突しない。
  **ただしKeep Listの「wind_scoreのエンジン間の意味差」は、`wind_score`が実装から消滅しているため項目自体が無効**（context.mdは他2項目をT462で前提消滅と注記済みだが、この項目は取り残されている）。→ `/review:improve`での更新候補として記録する。

## Phase 8: 最終統合判断

### 実施順序の提案（improvement-plan.md「進め方の原則」＝検証の自動化→境界の固定→内部の再配置）

**第0段（今すぐ、他のすべてに先行する）— 安全網の回復**
1. `primaryAttributes.ts`へ`curvature`を登録し**masterを緑に戻す**（略名表の削除まで一気にやらず、まず緑にする）
2. `ci.yml`の`git diff --exit-code`へ`working-directory: .`を付ける — **ただし単独では赤になるので3とセット**
3. I-3の①②③（`export_openapi.py`へ`description`/`raw_value_unit`追加 → `PREFERENCE_AXIS_DESCRIPTIONS`撤去 → 生成物再生成）
4. ガードが**実際に効くこと**を確認する（生成物を意図的にdirtyにしてCIが落ちることを1回見る）

**第1段 — 利用者に届いている誤り**
5. I-2 最短ルートのハードフィルタ迂回（＋テストの意図の書き直し）
6. I-6 負値軸の分布プレビュー（backendの階級生成＋frontendの値域検証）
7. I-5 生値の精度（difficulty用の丸めを生値へ流用しない）
8. I-4 蛇行軸の配線（`refresh_derived`登録＋カバレッジの母集団＋`EdgeMaterialTable.get()`）

**第2段 — 検知の空白を埋める**
9. `axis_admin`のバリデータ非対称（`priority_overrides`・`CategoricalShape`）
10. 低ズーム案内の軸レイヤー配線（S6 P1）
11. `refresh_derived`の`_STAGES`と`app/batch/precompute_*.py`の突き合わせテスト
12. `NARRATIVE_PATTERN`へ「〜だった頃は」型を追加、`find_undefined_css_tokens`を`.tsx`へ拡張、`find_dead_file_refs`を識別子へ拡張
13. 依存更新（`next` 16.3.4）／`maplibre-gl` 6系は独立タスク

**第3段 — 内部の再配置（急がない）**
14. `AxisComposer.tsx`の分割（`BreakpointCurveEditor`＋`axisDraft.ts`）
15. `ensure*`関数の再適用強制、precompute骨格の共通化、CSS重複の解消、docs乖離の一括是正

### 「全体として最もバランスが良い状態」

このコードベースは、**同じ設計を2度書かない**ための機構（宣言的カタログ5系統、生成物からの片側import、ドリフト検知テスト、規模ウォッチ、経緯コメントの機械検知）を非常に高い密度で持っており、前回レビューの指摘131件を27本のタスクで完走し切ったことからも、負債返済の実行力は疑いようがない。**壊れているのは仕組みの設計ではなく、仕組みが生きていることの確認**である。今回見つかった最も重いものは「ガードが空振りしていた」「ガードが鳴っているのに5コミット進めた」「診断が評価とは別のテーブルを見ていた」「テストが欠陥を仕様として固定していた」の4つで、いずれも**新しい仕組みを足すのではなく、既にある仕組みが本当に鳴るかを1回確かめれば防げた**。したがって最もバランスの良い状態とは、機構をさらに増やした状態ではなく、**各ガードについて「意図的に壊したときに落ちること」を一度確認し、その確認自体を仕組みへ組み込んだ状態**である。あわせて、T686/T687/T690で新設した「利用者に絶対の事実を見せる」機能群には、**その主張が成り立たない入力（負の生値・桁の小さい生値・通行不可の道）を先に列挙してから実装する**という規律を足したい——この3機能はいずれも実装は正しく動いており、欠けているのは適用範囲の見極めだけである。

## Findings（統合後、優先度順）

Phase 3〜6の生の指摘は後段の「Phase別の詳細」に全件残してある。ここは
Phase 7で重複統合・矛盾解消を経た**統合後のFinding**のみを載せる。

### [P0] I-1 検知の安全網が二重に落ちている（ガードが鳴らない／鳴っているのに無視されている）

- Problem: 機械的検知を置いたこと自体が「守られている」と読み替えられており、
  ガードが実際に効いているかが一度も確認されていない。2つの形で同時に顕在化している。
- Evidence:
  - **(a) ガードが鳴らない**: `.github/workflows/ci.yml:125`
    `- run: git diff --exit-code -- frontend/src/types/generated/` が、ジョブ既定
    `defaults.run.working-directory: backend`（同`:64-66`）を継承して`backend/`で実行される。
    pathspecは`backend/frontend/src/types/generated/`へ解決され、**マッチ0件＝差分なしとして常にexit 0**。
    直前2ステップ（`npm ci`・`npm run generate:api`）は明示的に`working-directory: frontend`を
    指定しているのに、この検証ステップだけ指定が無い。
    実測: 生成物をdirtyにした状態で `cd <repo>/backend && git diff --exit-code -- frontend/src/types/generated/`
    → **exit 0**、`cd <repo> && …` → exit 1。
    実CI（run 34431803012、`266c065b`）でも`backend`ジョブ step 14 は success。
  - **(b) 鳴っているのに無視されている**: `frontend`ジョブの`npm test`が
    `src/components/Map/primaryAttributes.test.ts`の2件で失敗し続け、
    **run 855〜859の5回連続でmasterのCIがfailure**（直近の成功はrun 854 = `9e01a68d`）。
    原因はT691（`45ffd36b`）が一次属性`curvature`をカタログへ追加した際、
    `PRIMARY_ATTRIBUTE_CHIP_LABELS`（`primaryAttributes.ts:44`）と
    `PRIMARY_ATTRIBUTE_LAYER_IDS`/`PRIMARY_ATTRIBUTES_WITHOUT_LAYER`（同`:70`/`:93`）への登録漏れ。
    ローカル単独実行でも再現（2 failed / 5 passed）。**ドリフト検知テストは設計どおり機能している。**
- Root Cause: (a)はT350（`13794155`）で独立していた`api-contract`ジョブを`backend`ジョブへ統合した際、
  旧ジョブは**ステップごとに**`working-directory`を書いており検証ステップだけ無指定＝リポジトリルート
  だったが、統合先はジョブ既定で`backend`を持つため、無指定の意味が変わった。
  (b)は赤を前提に作業を進める運用。
- Impact: consistency.mdが「同種の手動同期ペアで2回以上の再発が観測された場合は、手順の再徹底ではなく
  **機械的検知の追加を既定の第一候補にする**」と定め実際に収束させた当の検知が無言で機能停止し、
  同時に、生きている検知は無視されていた。**生成物ドリフトを止める仕組みが1つも残っていない状態が2日続いた。**
- Recommendation: Phase 8「第0段」の順序で実施する（I-1単独でCIを直すとI-3で赤になるため、
  **I-3の①②③を先に済ませてからガードを直す**）。あわせて「意図的に壊したときに落ちること」を
  1回確認する手順をガードの導入条件にする。
- Scope: S（ci.yml 1行＋対応表2エントリ）／確認手順の仕組み化はM
- Confidence: **High**（両方とも実測で再現）

### [P0] I-2 目的地モードの「最短」ルートが0次ハードフィルタをすべて迂回する

- Problem: `select_shortest_distance_route`が前向き木・後ろ向き木のコスト配列に
  `context.statics.edge_length_m`（純粋な実距離）をコスト・距離の**両方**へ渡すため、
  0次ハードフィルタで除外されたEdgeを通る経路を返す。しかもその経路は先頭固定で必ず1本出る。
- Evidence:
  - `services/road_graph_engine.py:1265`・`:1270` — 両ツリーとも`edge_length_m`を渡す。
  - 対比: 軸重み経路は`:1093`が`context.legs[0].cost_list`、`:1134`が`inbound.cost_list`を渡す。
    このコスト配列は`:299`で`cost_array = np.where(self._hard_filter_excluded, np.inf, cost_array)`と
    inf化されており、**通行可否はこのコスト配列だけが表現している**。
  - `domain/hard_filters.py:117-145` `compute_hard_filter_excluded`は
    motorway / trunk / `no_bicycle`（OSM `bicycle=no`）に加え、
    **利用者が指定する`max_average_grade_percent`も同じ配列へ畳み込む**。
  - 既定値は`DEFAULT_HARD_FILTERS = {"no_bicycle", "motorway", "trunk"}`（同`:35`）で、
    `hard_filters=None`（＝利用者が何も触らない状態）でも全フィルタが有効。
  - `services/route_generator.py:481-497` — 「距離だけで選んだ最短経路を基準線として**必ず1本含める**」
    （条件分岐なし）。同`:511` `candidates.sort(key=lambda c: not c.is_shortest_distance)`で先頭固定。
  - `_build_segment_details`が読む`leg.difficulty_array`はinf化**前**の配列のため、
    区間表示も通常の難易度を示し、除外対象の道を通っている手掛かりが1つも出ない。
- Impact: 起点〜目的地間に国道trunkや自動車専用道路が走る一般的なケースで、
  **自転車通行禁止・自動車専用の区間を含む経路が「最短」として先頭に提示される**。
  利用者が「◯%以上の坂を避ける」を明示的に設定しても、最短ルートだけは無視する。
  本レビューで唯一、利用者を直接危険へ導きうる欠陥。
- Recommendation: 距離木にも`_hard_filter_excluded`によるinf化を適用する
  （`np.where(excluded, np.inf, edge_length_m)`をコスト配列として渡し、距離配列は生のままにする）。
  **同時に`test_select_shortest_distance_route_ignores_axis_cost`（`test_road_graph_engine.py:1460-1479`）を
  書き直す**——このテストは最短側Edgeを`highway="trunk"`にして「軸コストを高くし」とコメントしながら、
  実際には0次フィルタで通行不可になっている結果を期待値として固定しており、**修正すると赤くなる**。
  赤を「修正が間違い」と読まないよう、起票時に明記する。
- Scope: S（エンジン1箇所）＋S（テストの意図の書き直し）
- Confidence: **High**（コスト配列の対比・フィルタの内訳・先頭固定をすべて実測）

### [P1] I-3 静的軸カタログが本番の軸集合と乖離し、再生成すると設計原則違反のテストが赤になる

- Evidence（実測）:
  - `backend/fixtures/axis_definitions_snapshot.json`（revision 64、本番DBのダンプ）の公開軸は10本
    （gradient / wind / surface_q / car_stress / accident / night / bicycle_infra_quality / openness /
    **axis_abdaded0be20**［停止密度］/ **curvature**［蛇行］）。
  - `frontend/src/types/generated/axis-catalog.json`は9軸で、**旧`stop_density`を持ち上記2軸を欠く**。
  - PostGISを立て`bootstrap_ci_db.py`（15件投入）→`export_openapi.py`を実行すると、
    `axis-catalog.json`に**224行追加 / 105行削除**の差分（`git diff --stat`）。
  - 更新履歴: スナップショットは`772a9189`→`581586c9`→`509f0210`と3回更新されたが、
    `axis-catalog.json`の最終更新は`45ffd36b`で止まっている。
  - `axes[]`のキーは18個で、実行時APIが返す**`raw_value_unit`・`description`を含まない**
    （`export_openapi.py:186-232`の書き出しリストに無い。`map_value_unit`は書き出している）。
  - `lib/evaluationAxes.ts:53-64`の`PREFERENCE_AXIS_DESCRIPTIONS`は軸id→説明文の手書き辞書で、
    `evaluationAxes.test.ts:79-82`が「全軸が説明文を持つ」ことを強制している。
- Impact: 実行時APIの取得前・失敗時に使われる静的フォールバック
  （`useAxisCatalog.ts: FALLBACK_CATALOG`）の6系統（`DEFAULT_ROUTE_PREFERENCE`・`PREFERENCE_AXES`・
  `RAMP_AXES`・`AXIS_LABELS`・`SECONDARY_AXES`・`ROUTE_STYLE_MODES`）が揃って
  「存在しない軸を含み、公開済みの2軸を欠く」状態。旧`stop_density`のramp
  （`thresholds=[1,2,4]`）が地図レンズに現れるが、現行の`axis_abdaded0be20`は材料も折れ点も帯ラベルも
  別物（`display_thresholds_override=[2,4,7,12]`）のため色分けが現在の評価と一致しない。
  T687の生値表示もフォールバック中は`rawValueUnit`が得られず**無言で消える**。
- **影響の限定（過大評価を避けるための確認）**: `page.tsx:1352-1357`は`axisCatalog.loaded`が偽なら
  `route_preference`自体を省略しbackendの既定値へ委ねる自己防衛を持つ（実測）。
  **重みキーの不一致による422は起きない。** 影響は表示層に限られる。
  それでもP1に置くのは、ドリフトそのものより**それを止めるはずのCIが空振りしていた**（I-1）ことが本体だから。
- Recommendation（**順序が重要**）:
  ① `export_openapi.py`の書き出しへ`description`・`raw_value_unit`を足す
  ② `PREFERENCE_AXIS_DESCRIPTIONS`を撤去し生成物由来へ寄せる
  ③ 生成物を再生成してコミット → ④ I-1のCI修正
  **②を飛ばすと「生成物を正しく更新するとテストが赤になり、赤を消すには軸ごとの説明文を
  frontendへ手書きする」という、design-principles.md構造仕様2（新しい軸の追加にfrontendの
  コード変更を要しない）を強制的に破らせる状態になる。**
- Scope: M
- Confidence: **High**（生成物の差分・キー集合をすべて実測）

### [P1] I-4 蛇行軸（curvature）の配線が3箇所で同じ方向に欠け、互いの検知を打ち消している

- Evidence（すべて実測）:
  1. `batch/refresh_derived.py:57-66`の`_STAGES`は④〜⑪の8段で、**`precompute_edge_curvature`を含まない**。
     一方`migrations/0035_add_road_edges_curvature.sql:5`は「適用後は…の実行が必須」と書く。
     派生データ再構築の**単一エントリポイント**が、必須と宣言されたバッチを呼ばない。
  2. ルート評価が読むのは`road_edges.curvature_deg_per_km`。1により、`build_road_graph`がsplit時に
     埋める新規Edge以外はNULLのまま残り、蛇行軸（`default_weight=0.1`、本番で公開済み）は
     それらの区間で欠損扱いになり重み再正規化で薄まって**無警告のまま評価から抜ける**。
  3. `infrastructure/material_coverage.py:206-216`の`curvature_deg_per_km`は`way_geometry`のみを
     `missing_condition`にしている。コメント自身が「Edge単位（road_edges、**ルート評価が読む**）と
     Way単位の両方にある」と認めたうえでWay側を選んでいる。
     → `precompute_way_curvature`だけ流せば**カバレッジ画面は「欠損0%」と報告する**一方、評価側は算出不能。
  4. （さらに）`domain/attributes.py:416-431`の`EdgeMaterialTable.get()`が
     `curvature_deg_per_km`を`EdgeMaterialBundle`へ渡していない（`:269`/`:313`/`:385`/`:478-479`は保持・使用している）。
     `test_attributes.py:270`の往復テストは全fixtureが`None`のため検出できずgreen。
- Impact: 「軸を公開したのに効いていない」が、**ログにもカバレッジ診断にもテストにも現れない**。
  T691完了コミット（`509f0210`）の「本番でルートを生成し全候補・全区間へcurvatureが載ることを確認した」は
  `build_road_graph`が新規に埋めたEdgeを含むタイルでの確認であり、既存Edgeの多いタイルでは静かに欠損する。
- Recommendation: ①`_STAGES`へ追加＋**`_STAGES`と`app/batch/precompute_*.py`のファイル一覧を
  突き合わせるドリフト検知テスト**（現`test_refresh_derived.py`は8段を手書き列挙するだけで次も再発する）
  ②カバレッジを評価が読む`road_edges`側で数える ③`get()`へフィールドを渡し、
  **bundleの全フィールドが往復で保存されることを型から機械的に確認する**形にする。
- Scope: M
- Confidence: **High**

### [P1] I-5 生値（raw value）の精度が、T687が掲げた仕様を満たしていない

- Evidence: `domain/route.py:205`の`merge_axis_raw_values`が、0〜100のdifficulty専用集約
  `distance_weighted_difficulty`（`domain/difficulty.py:84`で`round(total, 1)`）を流用している。
  区間単位（`road_graph_engine.py:1577`）では未丸めのfloatだが、
  `aggregate_segments_into_bins`と`route_generator.py:575`の2段で小数1桁へ丸められる。
  公開軸`accident`の単位は`件/(km・年)`で、`material_catalog.py:385-389`の代表点は
  **少ない=0.02 / 普通=0.1 / 多い=0.3**。→ 実データの典型値0.03〜0.15は「0.0」か「0.1」にしかならず、
  0.05未満はすべて0.0。frontend側`RouteAxisProfile/axisRawValue.ts:34-38`は絶対値1未満で
  `toFixed(2)`を使う前提のため「0.00件/(km・年)」と表示され、**事故ゼロの道と区別できない**。
  単位が`/km`で終わらないため実数の併記（`totalUnitFor`）も出ない。
  テストは`test_route.py:292`以降が0.5以上の値しか使わず、frontendの`axisRawValue.test.ts:30`は
  `0.08`という**実運用では到達しない入力**の期待値を固定しており、双方とも検出できない。
- Impact: design-principles.md構造仕様11「目盛りに依存しない絶対の事実である生値」が
  事故密度軸で成立していない。T687の目的そのものが達成されていない。
- Recommendation: 生値の集約はdifficulty用の丸めから分離し、**桁数を材料の`unit`と代表点のスケールから
  導く**（または丸めずに配信して表示側で決める）。frontendのテストは配信される精度で書き直す。
- Scope: M
- Confidence: High

### [P1] I-6 負の生値を持つ軸で、分布プレビューが嘘の分布と事実と正反対の助言を出す

- Evidence: `services/axis_preview_service.py:128-141`
  `upper = quantiles["p99"] * 1.2 if quantiles["p99"] > 0 else max(upper, 1.0)` →
  `i = min(HISTOGRAM_BINS-1, int(value / width))` → `buckets[max(0, i)]`、
  `zero_share = sum(m for m, v in ordered if v <= 0) / total_m`。
  負値のみの分布は`upper`が1.0へ倒れ、全サンプルが階級0へクランプされ`zero_share=1.0`になる。
  負の生値を持つ公開軸は`openness`（`trees_percent`×-1 + `built_percent`×-1、生値約-160〜0）・
  `bicycle_infra_quality`（terms全て負、-4〜0）・`night`の3本で、**前2本は公開済み**。
  frontend `scoreDistribution.ts:56-70`は`bins`の値域を一切検証せず階級中央値へ折れ点を当てるため、
  `openness`は「100点 100.0%」と描画され、`distributionWarnings`（同`:81-90`）が
  **「延長の100%が満点に張り付きます。上限を高くしないと道の差が出ません。」**を出す。
  実際のp50=-79は約31点で、助言に従うと軸が壊れる。
  しかも同一パネル内の`DistributionPreview.tsx:52-57`は正しい分位（p10=-144…）を表示しており、
  **画面上で自己矛盾した2つの数字が並ぶ**。
- Impact: T686が「折れ点の妥当性を実データで判断できるようにする」ために作った道具が、
  **較正が最も難しい負値軸で道具としての価値を負にしている**。研究優先の現フェーズでは影響が大きい。
- Recommendation: backendは階級の下限を`min(0, データ下端)`から取る。
  frontendは`quantiles.p10 < bins[0][0]`（ヒストグラムがデータ下端を覆っていない）を検知して
  帯・警告を出さず「この設定では分布を表示できません」へ倒す。
  `zero_share`は語（「ゼロ」）と定義（`v <= 0`）のどちらを正とするか決める。
  **`axis_preview_service`にはbackendテストが1件も無い**ので、同時に置く。
- Scope: M
- Confidence: High

### [P1] I-7 軸スタジオのGUI操作で全ルート生成が500になりうる（バリデータの非対称）

- Evidence: `api/routers/axis_admin.py:206-229`の
  `_check_dynamic_and_static_materials_are_not_mixed`は`self.shape.terms`しか見ず、
  `if not isinstance(self.shape, BreakpointLinearShape): return self`で`CategoricalShape`を素通しする。
  一方、動的軸と判定されるかは`_axes_depending_on_materials`（`domain/axis_definitions.py:537`）が
  `definition.materials`を根拠にし、その`materials`プロパティ（同`:268-283`）は
  **`priority_overrides`が参照する材料も含む**と明記している。
  `evaluate_dynamic_axis_arrays`（`domain/dynamic_materials.py:122-137`）が渡す辞書は
  「静的軸スコア＋動的材料」だけで静的材料の配列を含まないため、
  `evaluate_axis_array`の`values = materials[override.material]`（`axis_definitions.py:762`）がKeyError。
- Impact: 「terms=[wind_drag_ratio]、priority_overrides=[{material:"motor_vehicle_no", …}]」の軸を
  GUIから保存でき、以後`/api/routes/generate`が**全リクエスト500**になる。
  バリデータのdocstring自身が「GUI操作だけで全ルート生成が落ちる」ことを防ぐ目的だと述べている。
  `test_axis_admin_routes.py:189`もterms経由の混在しか検証していない。
- Recommendation: 判定を`self.shape.terms`ではなく`AxisDefinition.materials`と同じ導出
  （terms＋priority_overrides、shape種別を問わない）へ揃える。
- Scope: S
- Confidence: **High**（本体でコード確認）

### [P1] I-8 「表示範囲が広すぎます」の案内が、軸レイヤーに対して一度も出ない

- Evidence: `mapLayers.ts:1970-1975` `isRoadSurfaceGroupVisible`は
  `roadSurfaceSharedLayerIds.some((id) => visibility[id])`。
  第2引数`buildRoadSurfaceSharedLayerIds`（同`:559-579`）は
  `...dedicatedAxes.map(dedicatedWayValueMapLayerId)`と`...rampAxes.map(axisMapLayerId)`を明示的に含める。
  しかし`MapView.tsx`の3箇所の呼び出し（`:2643-2652`・`:2950-2958`・`:3446-3455`）は
  第1引数へ`{roadType, roadSurface, designation, tunnel, oneway}`の5キーしか渡さず、
  軸レイヤーIDは常に`undefined`＝falsyで**一度も数えられない**。effectの依存配列にも無い。
  MapViewは`axisVisibility`・`dedicatedWayValueVisibility`をpropで受け取っている（`:2212`/`:2170`）が、
  これらは**軸idキー**、`roadSurfaceSharedLayerIds`は**レイヤーid**（`axis:<axisId>`）で、
  単に展開しても噛み合わない——配線が最後まで通っていない。
- Impact: 「路面/道路の種類」をOFFのままレンズで風やcar_stressだけをONにし、
  `ROAD_TILE_MIN_ZOOM`未満まで引くと、道路が一本も塗られないのに案内が出ず
  「データが無い地域」と区別できない。`MapLayersPanel.tsx:251`はフル一覧を使う側なので状態ドット抑制も効かない。
  **`MapView.tsx:1966-1969`のコメントは「新規公開ramp軸を低ズームでONにしても案内が出ないままになるため
  実行時リストを渡す」と目的を明記し、`MapView.dataStatus.test.ts:144-157`は`{"axis:car_stress": true}`を
  直接渡して「修正済み」を主張しているが、本番配線がそのキーを一度も供給しないため
  純関数テストが通っても機能が成立していない**（同`:3432-3433`のコメントは逆に「ramp軸は対象外（既知の制約）」と
  書いており、コメント同士も矛盾）。
- Recommendation: 呼び出し側で軸id→レイヤーidへ写像したうえで`axisVisibility`・
  `dedicatedWayValueVisibility`を合流させる。純関数テストだけでなく**配線を通したテスト**を置く。
- Scope: S〜M
- Confidence: **High**（本体でコード確認）

### [P1] I-9 依存の既知脆弱性が critical 2 / high 3（前回は全0件）

| 深刻度 | パッケージ | 現在 | 内容 | 修正版 |
|---|---|---|---|---|
| critical | `next` | 16.3.0（直接依存） | Windows RCE（CVSS 9）／Image Optimization APIのAVIF経由RCE | 16.3.4（**minor**） |
| critical | `maplibre-gl` | ^5.24.0（直接依存） | XSS Sanitizer Bypass in `DOM.sanitize()`（CVSS 10） | 6.9.0（**major**） |
| high | `sharp` / `js-yaml` / `@redocly/openapi-core` | 推移依存 | libheif由来ほか | いずれも修正あり |

- 到達可能性の評価（過大評価を避けるための確認）:
  - Next.jsのWindows向けRCEは**該当しない**（`frontend/Dockerfile`は`node:20-slim`、Linuxコンテナ）。
  - AVIF経由のRCEは`next/image`の利用が`src`配下に**0件**、`next.config.ts`に`images`設定なし、
    `public/`にAVIFなし。攻撃面は小さいが`/_next/image`自体はstandaloneサーバに存在する。
  - **maplibre-glのXSSは実際に効きうる**: 地図ポップアップが表示する値はOSMタグ由来＝
    **第三者が編集できるデータ**。これが本項の実質的な主眼。
- Recommendation: `next`を16.3.4へ（minorなので低リスク）。`maplibre-gl`は6系がmajorのため
  **独立したタスクへ分離**する（Phase 7-3のトレードオフ参照）。
- Scope: next=S / maplibre-gl=M
- Confidence: High

### [P1] I-10 軸カタログ取得失敗時、利用者の重み設定が無言で捨てられる

- Evidence: `hooks/useAxisCatalog.ts:214-229` — 実行時フェッチはマウント時1回きりで、失敗しても再試行せず
  `loaded=false`のまま固定（`catch`が共有ストアを書き換えない）。
  `CATALOG_API_TIMEOUT_MS=10000`に対しbackendのコールドスタートが10秒を超えるとrejectする。
  `RouteSettingsPanel`はフォールバック軸で通常どおり重みスライダーを描画しユーザーは編集できるが、
  生成時は`page.tsx:1356-1358`の`weightOverrideEnabled && axisCatalog.loaded`が偽になり
  `route_preference`が省略され、backendの既定重みで探索される。
  **画面には失敗の表示も「既定値で生成しました」の表示も一切出ない。**
  隣の`lensAxisId`（`:1347`）には同じガードが無く、存在しない軸idがそのまま送られ
  backendの`AXIS_DEFINITIONS.get(lens_axis_id)`がNoneを返して黙って無視する（422にはならない）。
- Impact: **評価モデルの研究が主目的の現フェーズでは、実験結果を取り違える形で効く。**
  `useAxisCatalog.ts:51-57`のdocstringは「`loaded`で未確定状態を区別しなければならない」と明記しており、
  実装がその契約を片側でしか満たしていない。
- Recommendation: カタログ取得失敗を利用者に見せる（重みパネルを無効化するか、明示的な再試行導線を出す）。
  `lensAxisId`にも同じガードを掛ける。
- Scope: S〜M
- Confidence: High

### P2（61件）・P3（80件）

件数が多いため、統合後の分類のみをここに示し、個票は後段「Phase別の詳細」に全件残す。

**P2の内訳（分類ごとの件数）**
- 契約不一致（コメント・docstring・型が述べる前提が実装で強制されていない）: 18件
- docs乖離（`architecture.md`・`docs/modules/*`・`design-principles.md`の記述と実装のずれ）: 14件
- 重複（CSS・共通骨格の写経・正準定義の値コピー）: 11件
- テスト欠落／意味のないテスト（実運用で到達しない入力を固定、恒真に近い照合）: 9件
- スケール（OFFSET分割・デバウンス非対称・イベントループ占有・キャッシュ世代の手動同期ペア）: 6件
- 残骸（削除済み機構を語る記述・宙に浮いたコメント・使われない定数）: 3件

**P3の内訳**
- 死んだ参照（改名・削除済みの識別子/ファイル名を指す記述）: 21件
  （うち`docs/modules`全15ファイル横断のdead symbol 10件は1エントリに束ねてある）
- 個数・組み合わせを語る表現の陳腐化（「5値」「4本」「7軸/8軸」「six/five materials」「6段」等）: 12件
- コメントと実装の不一致（到達不能な分岐の説明・逆の主張）: 15件
- UI一貫性（未定義CSSトークン・配色ルール違反・語彙の食い違い）: 11件
- 残骸・重複テスト・その他: 21件

**とくに拾っておきたいP2/P3**
- `page.tsx:62`が、**同じコミット範囲で新設された規約**（`docs/frontend-design-system.md:100-103`
  「別コンポーネントの`*.module.css`を直接importして借りない」）に唯一違反したまま、
  「問題なく使える」と正当化するコメント付きで残っている（P2）。
- `docs/design-principles.md:52`（**設計原則の唯一の正本**）が、実装に存在しない`wind_score`を
  「やむを得ない場合は必須とする」方式の実例として名指ししている（P2、grep 0件で確認）。
  同`:70`の`DEDICATED_WAY_VALUE_LAYER_IDS`（T672で撤去）も同様。
  **あわせて`.claude/commands/review/context.md`のKeep Listにも「wind_scoreのエンジン間の意味差」が
  意図的な設計判断として残っている**（他2項目はT462で前提消滅と注記済みだが、これは取り残し）。
- `services/regionApi.ts:159-160`・`lib/axisMaterialsCatalog.ts:9-10`に**新規混入した経緯コメント**が、
  `review_checks.py`の`NARRATIVE_PATTERN`に「〜だった頃は」「〜ころは」という時制表現が
  無いため素通りしている（P3）。**T656が「経緯コメント検知の穴を塞ぐ」を実施した直後の差分で、
  同じ穴から2件入っている。**
- `docs/frontend-design-system.md:65`は「存在しないトークン名は機械的に弾く」と無条件に書くが、
  `find_undefined_css_tokens`は`.css`のみを対象にしており、`.tsx`内の`var(--…)`参照31件は
  検査されない（P3）。フォールバック付き参照（`var(--shadow-md, …)`・`var(--font-size-xs, …)`・
  `var(--color-warning-soft, …)`）も検知対象外で、**参照3箇所・定義0件・実効値2種**という
  状態が実際に発生している。

## KEEP（変更しない方がよい設計。理由つき）

- **`MapView.tsx`の分割見送り**（3,545行、閾値3,700）。5系統が同居するが、いずれもMapLibre `map`という
  単一の可変資源への配線で、純ロジックは既に`axisLayers.ts`・`dedicatedWayValueLayer.ts`・
  `valueScale.ts`・`useLayerDataStatus.ts`へ分離済み。**Keep Listの判断を覆す新しい根拠は無い。**
- **`road_graph_engine.py`の3アルゴリズム同居**（2,089行、閾値2,200）。前回の判断（`_RoadGraphContext`
  19フィールド・`_LegCostComposer`・`select_diverse_by_overlap`・迂回率学習を共有しており、
  切り出すと内部状態の公開API化コストが利得を上回る）を覆す材料は無い。
  今回の`select_shortest_distance_route`追加も66行で、木の作り方だけが違う。
- **`road_graph_repository.py`の5サブリポジトリ＋ファサード構成**（2,699行、+402、閾値2,800）。
  増分はcurvature/way_geometry/poi_countsのSQLで、すべて既存のサブリポジトリの担当範囲内。
  Repositoryファサードのフラット委譲契約は正式なインターフェースとして維持する。
- **`domain/material_catalog.py`が1,000行を超えたこと**（1,003行）。`MaterialSpec`宣言32件＋
  小さな抽出器ファクトリで構成される**宣言的カタログ**で、成長は材料数に比例する。
  T673が回復した「材料追加の1本道」の単一正本そのもの。分割は正本を割ることになる。
- **`stream_id_chunks`のサーバーサイドカーソル方式**（Phase 7-3参照）。T660の実測根拠つき（判断原則14）。
- **`PRIMARY_ATTRIBUTES_WITHOUT_LAYER`がテスト専用であること**。「キーが無い＝漏れ」と
  「意図的にレイヤー無し」を区別する明示集合という設計意図どおり（Phase 7-2 矛盾1参照）。
- **highway 3スコープの別定義**・**`/api/routes/preview`の残置**・**PBF取込のasyncpg COPY直行**
  （context.md Keep List、今回も再指摘しない）。

## REMOVE

- `PRIMARY_ATTRIBUTE_CHIP_LABELS`（`primaryAttributes.ts:44`）と対応するテスト。本番の消費者ゼロ（実測）。
- `frontend/public/{file,globe,next,vercel,window}.svg`（Next.jsテンプレート由来、参照0件）。
- `PREFERENCE_AXIS_DESCRIPTIONS`（`lib/evaluationAxes.ts:53`）。生成物へ寄せる（I-3の②）。
- `EvaluationService`のクラス形式（状態を持たない素通し委譲、本体からの呼び出し元ゼロ）。
- `FIXED_LAYER_VISIBILITY_DEFAULTS`（`page.tsx:193`。`DEFAULT_LAYER_VISIBILITY`と完全に同値）。
- `RouteSettingsPanel.test.tsx`の`catalogResponse`第2・第3引数（8箇所すべての呼び出しが渡していない）。
- 削除済み機構を語るコメント群（風の面塗り・評価軸グループ・観測/推定グループ・`renderMapColorToggle`・
  `renderGroupLegendToggle`・`use_cache`・`_LRUCache`ほか）。

## SIMPLIFY

- precompute系バッチのドライバ骨格（5本に同じ形。`_common.py`へ`run_chunked_precompute`を1本足す。
  ただし`precompute_way_landcover.py`は固有の関心事を持つため無理に同じ器へ入れない）。
- `axisPreviewApi.ts`の`getJson`を`lib/fetchJson.ts`へ寄せる（`x-request-id`が欠けている）。
- `healthApi.ts:6`の`AbortSignal.timeout(5000)`を`STATUS_API_TIMEOUT_MS`へ。
- MapOverlayControlsの`.visibilityInfoButton`を`composes: roundIconButton`へ、
  `.visibilityCheckbox`系を共有`ui/Checkbox`へ。
- `riskMap.ts`と`precipitationNowcast.ts`が同じ`rasrf/targetTimes.json`を二重取得している箇所。

## REFACTOR

- `AxisComposer.tsx`の抽出（`BreakpointCurveEditor` 136行＋`axisDraft.ts` 265行 → 1,400行前後へ）。
  Phase 7-2 矛盾2の判断に基づく。**優先度はP3**（閾値超過43行にすぎない）。
- `ensure*`関数の「既存レイヤーへも設定を再適用する」をファクトリ側で強制する形へ
  （個別関数がearly returnを書けない構造にする）。Phase 7-2 矛盾3の判断。
- 生値の集約をdifficulty用の丸めから分離する（I-5）。
- `secondaryAxes.ts`の絞り込み（地図チップの都合）と`PREFERENCE_AXES`（重み一覧の母集合）の分離。
  現状は`show_map_icon=false`にした公開軸が重み一覧からも消え、`wind`が手書きで1件復活させてある。

## EXTEND

- `axis_preview_service`のbackendテスト（現在1件も無い）。
- `refresh_derived.py`の`_STAGES`と`app/batch/precompute_*.py`の突き合わせテスト。
- `raw_axis_ids`を検証するテスト（`backend/tests/`全体で参照0件）。
- `weatherCode.ts`⇔`domain/weather.py: derive_weather_code`の突き合わせテスト
  （コメントが「10値だけ」と契約を宣言しているが検証が無い）。
- `review_checks.py`の検知拡張3件: `NARRATIVE_PATTERN`へ時制表現（「〜だった頃は」「〜ころは」）、
  `find_undefined_css_tokens`を`.tsx`へ、`find_dead_file_refs`を識別子へ。
- 新設2エンドポイント（`preview-distribution`・`material-catalog/{id}/distribution`）の
  401／503／404テスト（既存エンドポイントは全件が対で持っている）。

## DEFER（トリガー条件つき）

- `maplibre-gl` 6系（major）への更新 — **トリガー: 一般公開の意思決定、または`next`更新の完了後に
  独立タスクとして着手できる余力ができた時点**。XSSの到達経路（OSMタグ→ポップアップ）は実在するが、
  現フェーズは一般公開前・低利用規模。
- `stream_id_chunks`の長時間トランザクション対策 — **トリガー: 本番で`idle in transaction`起因の
  テーブル肥大化・`idle_in_transaction_session_timeout`による中断が実際に観測された時点**。
  現時点ではT660の実測根拠が上回る（判断原則14）。
- `NumberField`/`SliderNumberField`の`components/ui/`への共有化 — **トリガー: 3箇所目の
  「制御された数値入力が入力途中を食う」問題が出た時点**。現在は`RideConditionBar`が別解を採っており、
  UXが異なるため統合の是非が決まらない。
- `T629`（ボタン系CSSの共有コンポーネント化） — 既存のトリガー（MapLayersPanel再編）が未発火のため据え置き。

## Regression / Previous Findings

- **前回（2026-09-08）の指摘131件を束ねた27本（T659〜T685）は全件完了**。抜き取り5本
  （T662・T672・T675・T681・T690）の完了条件はいずれも成立を確認した（S11）。
  T670（docs/modules全15ファイルの現状化）も抜き取り3件が是正済み。
- 前回complexity **G7-6**（`timeoutMs=15000`がfrontend 7ファイルへ直書き）: **解消**
  （`lib/apiTimeouts.ts`が5定数の単一ソース。残る直書きは`healthApi.ts:6`の1件のみ）。
- 前回complexityが提案した次閾値4件（road_graph_engine 2,200 / MapView 3,700 / page.tsx 2,400 /
  AxisComposer 1,750）: **`size_watch.json`へ全件反映済み**（T658）。
  結果、2回連続で鳴りっぱなしだった4ファイルのうち3ファイルで発火が止まった。
- **再発**: 「新規追加した対応表・列挙が既存の同期ペアへ追従していない」型が、
  T656（経緯コメント検知の穴を塞ぐ）・T674（導出漏れを塞ぐ）の直後に**別の穴から再発**している
  （`NARRATIVE_PATTERN`の時制表現・`find_undefined_css_tokens`の`.tsx`・`find_dead_file_refs`の識別子）。
  consistency.mdの方針（再発する手動同期ペアには機械的検知を足す）は正しく機能しているが、
  **検知器自身の適用範囲が検証されていない**という一段上の穴が残っている（I-1と同根）。
- **新規に発生した事象**: masterのCI赤（本範囲内、`4dd1cdfa`以降）／依存の脆弱性 critical 2・high 3
  （前回0件、上流の新規advisoryによる）。

## スコアサマリ

| 指標 | 値 |
|---|---|
| P0件数 / P1件数 / P2件数 / P3件数 | **2 / 8 / 61 / 80** |
| 総合スコア（100点満点） | `100 - (2×20 + 8×10 + 61×3 + 80×1)` = -283 → **0**（下限） |
| 前回同種レビューからの差分 | **±0**（前回も下限0で張り付き。内訳はP0 0→2・P1 8→8・P2 55→61・P3 68→80） |
| REMOVE/SIMPLIFY/REFACTOR件数 | REMOVE 7 / SIMPLIFY 5 / REFACTOR 4 = **16** |
| DEFER件数（トリガー未到達） | **4** |

**この指標について**: 前回（2026-09-08）が既に指摘したとおり、対象が実装の大半に及ぶ全量レビューでは
計算式が固定のため機械的に下限0へ張り付き、**スコアが状態を弁別しなくなっている**。
今回はP0が0→2になった一方でスコアは±0で、変化がまったく表現されていない。
経時比較には内訳（とくにP0/P1）を見る必要がある。この計算式自体の見直しは
`/review:improve`の対象として記録する。

## Overall Judgment

**負債返済の実行力は前回から明確に向上している。** 前回の指摘131件を27本のタスクへ束ねて全件完走し、
規模ウォッチの閾値も書き戻され、機械チェックは全項目0件、backend 1,733テストとruff・eslintはクリーン。
仕組みの設計そのものに構造的な問題は見当たらない。

**問題は仕組みの設計ではなく、仕組みが生きていることの確認にある。** 今回の重い指摘は
「ガードが空振りしていた」「ガードが鳴っているのに5コミット進めた」「診断が評価とは別のテーブルを
見ていた」「テストが欠陥を仕様として固定していた」の4つで、いずれも**新しい仕組みを足すのではなく、
既にある仕組みが本当に鳴るかを一度確かめれば防げた**ものである。
CLAUDE.mdとレビュー基盤は「機械的検知を第一候補にする」と正しく定めているが、
**検知器自身の適用範囲を検証する段がどこにも無い**——これが本レビューの最大の発見である。

加えて、本範囲で新設された「利用者に絶対の事実を見せる」3機能（T686・T687・T690）は、
実装はいずれも正しく動いているが、**その主張が成り立たない入力（負の生値・桁の小さい生値・
通行不可の道）への手当てを欠いている**。とくにT690の最短ルートは、利用者を法的に通行禁止の道へ
先頭固定で案内しうるため、本レビューで唯一「利用者に直接の危険が及ぶ」欠陥として扱った。

優先順位は明確である。**第0段（masterを緑に戻す＋CIのガードを直す＋生成物を再生成する）を
他のすべてに先行させ、次に利用者に届いている誤り4件（I-2・I-6・I-5・I-4）を片付ける。**
残りのP2・P3は量こそ多いが、その大半は「削除・改名した事実を語る記述の残存」と
「個数を語る表現の陳腐化」という2つの型に収まっており、まとめて機械的に処理できる性質のものである。

## improvement-plan.md への起票案（P0/P1、**ユーザー承認後に起票**）

番号は仮置きしない（並行セッションとの衝突を避けるため、起票時点で確定する）。

| # | タイトル | 規模 | 優先 | トリガー |
|---|---|---|---|---|
| 1 | **CIの生成物ドリフト検知を回復し、静的軸カタログを再生成する**（I-1＋I-3。`export_openapi.py`へ`description`/`raw_value_unit`追加→`PREFERENCE_AXIS_DESCRIPTIONS`撤去→再生成→`ci.yml`のpathspec修正→**意図的に壊して落ちることを確認**） | M | **最優先** | 即時 |
| 2 | **masterのCIを緑に戻す**（`primaryAttributes.ts`へ`curvature`登録。略名表の削除は#3へ分離） | S | **最優先** | 即時 |
| 3 | 一次属性の手書き対応表を整理する（`PRIMARY_ATTRIBUTE_CHIP_LABELS`と対応テストを削除。残り2表は維持） | S | 高 | #2の後 |
| 4 | **目的地モードの最短ルートが0次ハードフィルタを迂回する問題を直す**（I-2。`test_select_shortest_distance_route_ignores_axis_cost`の意図の書き直しを含む） | S〜M | **最優先** | 即時 |
| 5 | 負の生値を持つ軸で分布プレビューが嘘の分布と正反対の助言を出す問題を直す（I-6。backendの階級生成＋frontendの値域検証＋`axis_preview_service`のテスト新設） | M | 高 | 即時 |
| 6 | 生値の精度をdifficulty用の丸めから分離する（I-5。frontendの表示精度・テストの入力も揃える） | M | 高 | 即時 |
| 7 | 蛇行軸の配線を最後まで通す（I-4。`refresh_derived`登録＋`_STAGES`ドリフト検知テスト＋カバレッジの母集団＋`EdgeMaterialTable.get()`＋往復の型検証） | M | 高 | 即時 |
| 8 | 軸スタジオのバリデータ非対称を是正する（I-7。`priority_overrides`・`CategoricalShape`） | S | 高 | 即時 |
| 9 | 低ズーム案内を軸レイヤーへ配線する（I-8。純関数テストではなく配線を通したテストを置く） | S〜M | 高 | 即時 |
| 10 | `next`を16.3.4へ更新する（I-9。critical 1件の解消） | S | 高 | 即時 |
| 11 | 軸カタログ取得失敗を利用者に見せる（I-10。`lensAxisId`のガードを含む） | S〜M | 高 | 即時 |
| 12 | `review_checks.py`の検知範囲を広げる（`NARRATIVE_PATTERN`へ時制表現／`find_undefined_css_tokens`を`.tsx`へ／`find_dead_file_refs`を識別子へ） | M | 中 | #1の後 |
| 13 | CIが検証するNodeと本番のNodeを揃える（CI 22 / Dockerfile 20。`package.json`へ`engines`を書く） | S | 中 | 即時 |
| — | P2 61件・P3 80件 | — | — | 型ごとに束ねて別途起票（下記） |

**P2/P3の束ね方の提案**（前回のT659〜T685と同じ粒度）:
- A. docs乖離の一括是正（`architecture.md`・`design-principles.md`・`docs/modules`全15ファイル。dead symbol 10件・個数表現12件を含む）— 規模M
- B. 削除・改名された事実を語るコメントの一掃（`useDynamicWayValues` 6箇所・評価軸/観測/推定グループ・風の面塗り・`renderMapColorToggle`ほか）— 規模M
- C. 契約不一致の是正（コメント・docstring・型が述べる前提を実装へ合わせる、または記述を実態へ直す 18件）— 規模M
- D. テストの空白と偽陽性の是正（実運用で到達しない入力を固定したテスト・恒真に近い照合・新設エンドポイントの401/503テスト 9件）— 規模M
- E. UI/CSSの重複とトークンの是正（未定義トークン3種・配色ルール違反・チェックボックスの二重実装・コントラスト不足）— 規模M
- F. `ensure*`関数の再適用強制と、precompute骨格・fetch骨格の共通化取り残し — 規模M
- G. `AxisComposer.tsx`の抽出（`BreakpointCurveEditor`＋`axisDraft.ts`）— 規模M、優先度低

## `/review:improve` への申し送り（レビュー基準側の更新候補、本レビューでは変更しない）

1. **`context.md`のKeep List「wind_scoreのエンジン間の意味差」は前提が消滅している**
   （`wind_score`は実装に存在しない。grep 0件）。他2項目と同様にT462での前提消滅を注記すべき。
2. **スコア計算式が全量レビューで機械的に下限0へ張り付き、状態を弁別しなくなっている**
   （今回P0が0→2になったのにスコアは±0）。前回も同じ現象を記録している。
3. **「ガードを置いた」ことと「ガードが効いている」ことを区別する確認段が、どのレビュー観点にも無い。**
   consistency.mdは「機械的検知の追加を既定の第一候補にする」と定めるが、
   その検知器自身の適用範囲を確かめる観点を足すべき（本レビューのI-1が実例）。

---

# Phase 3・5 の詳細（シャード別の生の指摘、全件）

overall（Phase 3）とconsistency（Phase 5）はドメイン単位で11シャードへ分割し、
1シャード1Agentへ両レンズの確認観点を渡して実施した（principles.md「共通実行手順」4d）。
以下は各シャードの報告を、レンズごとの節構造を保ったまま収録したもの。
**Phase 7で重複統合・矛盾解消を経る前の生の指摘**であり、統合後の結論は上の「Findings」を正とする。


## シャード S1: backend/app/domain 前半

### overall
- evaluation.py:484 P2 責務混在/述語の二重定義 — 「どの軸が生値を持つか」の判定（表示都合のraw_value_unit＝材料のunit/additive依存）がStaticEdgeScoreMatrixの列構成を決めており、同じ述語が空タイル分岐(356-361)と通常分岐(483-487)に別式で2重定義。material_catalogでunitを1語足すだけでraw_axis_idsが変わるがtile_score_matrix_cacheの世代更新トリガー一覧に該当せず_SCORE_MATRIX_REVISIONが据え置き→combine_static_edge_score_matrices(790)のnp.concatenateが列数不一致でValueError→/api/routes/generate 500、または列ずれで別軸の生値を無警告表示。
- dynamic_way_values.py:92 P3 概念重複 — 「軸の元になる物理量の単位」がmap_value_unit（地図凡例、abs＋単項）とaxis_display.raw_value_unit（ルート結果、重み付き和）の2箇所に別ルールで存在。axis_catalog.py:165が同一エントリで両方配信。材料のunit変更時に片方だけ変わり地図凡例とルート結果で単位が食い違う。

### consistency
- attributes.py:423 P2 契約不一致/テスト欠落 — EdgeMaterialTable.get()がEdgeMaterialBundle再構築時にcurvature_deg_per_kmを渡しておらず常にNone。SearchMaterials.materialsのdocstring(514-515)が明言する「いずれの型も.get(edge_id)で同じ意味論のbundleを返す」契約が破れている。test_attributes.py:270の往復テストは全fixtureがNoneのため検出できずgreen。
- **axis_admin.py:219 P1 契約不一致（ガードの非対称）** — 動的材料と静的材料の混在を禁じるバリデータがself.shape.termsしか見ない。動的軸判定(_axes_depending_on_materials, axis_definitions.py:537)はterms＋priority_overridesを根拠にし、CategoricalShapeは早期returnで素通し。GUIから「terms=[wind_drag_ratio]、priority_overrides=[{material:"motor_vehicle_no",...}]」の軸を作ると422にならず保存され、以後evaluate_dynamic_axis_arrays(dynamic_materials.py:134-137)が静的材料配列を含まない辞書を渡しaxis_definitions.py:762がKeyError→/api/routes/generateが全リクエスト500。test_axis_admin_routes.py:189もterms経由の混在しか検証していない。
- axis_display.py:240 P2 契約不一致 — raw_value_unitが「単位一致・重みが正・2項以上ならadditive」の3条件のみで重みの大きさを見ない。生値はΣ(材料値×weight)なのでweight≠1だと材料の単位のスケールでなくなる。本番スナップショットの公開軸axis_abdaded0be20（signal×1.0＋level_crossing×1.5＋stop×0.3＋crossing×0.0）が「回/km」を返し「3.4 回/km」と表示するが実際の回数ではない。design-principles.md構造仕様11「目盛りに依存しない絶対の事実である生値」が崩れ、test_axis_display.py:737がこの挙動を期待値として固定。
- route.py:205 P2 契約不一致 — merge_axis_raw_valuesが生値集約にdistance_weighted_difficulty（difficulty.py:84でround(total,1)）を流用。0〜100スケール向けの丸め幅が桁の小さい生値には粗すぎる。公開軸accidentの単位は件/(km・年)で代表点は0.02/0.1/0.3。2段の丸めで0.05未満は全て0.0となり「事故ゼロの道」と「少ない道」が同じ表示。T687の目的が成立しない。test_route.py:292以降は0.5以上の値しか使わず検出できない。
- test_evaluation.py:789 P2 テスト欠落（双子実装の非対称） — 本番障害T536（空タイル混在でnp.concatenateが列数不一致）の回帰テストがaxis_scoresしか検証せず、同構造の並行配列axis_raw_values/raw_axis_ids（evaluation.py:692-693/741-746/787-790/813-814）を一切検証していない。backend/tests/全体でraw_axis_idsのヒット0件。空タイル分岐と通常分岐の生値フィルタ条件は現在同値だが片方だけ変更してもテストは全green、T536と同一の壊れ方が再発。
- docs/modules/backend/routing-engine.md:478 P3 docs乖離 — 「EdgeMaterialTableはEdgeMaterialBundleと1対1のビュー」「get()は必要なEdgeだけその場で組み立てる」と記すがget()はcurvature_deg_per_kmを復元せず1対1でない。
- docs/modules/backend/evaluation-scoring.md:19 P3 docs乖離 — 「domainの5ファイルは変更理由で分けてある」と書くが直上の対象ファイル表(14行目)は8ファイルを列挙。docs/modules/README.md「表にメトリックを書かない」の趣旨とも不整合。

### 所見なし項目
- 担当ファイルの追加行に経緯コメントの新規混入なし
- domain層へのI/O漏れなし
- AXIS_DEFINITIONSのPython literal撤去とDB単一正本、軸参照の再帰解決・REQUEST_DYNAMIC_MATERIAL_IDS起点の動的軸自動導出は軸idのハードコードを持たない汎用実装として保たれている


## シャード S2: backend/app/domain 後半

### overall
- route.py:205-209 P2 責務の再利用による契約の混線 — 【S1と重複】merge_axis_raw_valuesが0〜100のdifficulty専用集約distance_weighted_difficulty（difficulty.py:84でround(total,1)）を再利用し任意スケールの生値が小数第1位へ丸められる。accident軸の生値（材料accident_count_per_km_year、breakpoints=[[0.0,0.0],[0.5,100.0]]＝有効域0〜0.5件/(km・年)、reference_pointsも0.02/0.1/0.3）はroad_graph_engine.py:1577で未丸めのfloat→aggregate_segments_into_bins／route_generator.py:575のmerge_axis_raw_valuesを通る時点で0.0/0.1/…の6段階へ潰れる。実データの典型値0.03〜0.15は「0.00」か「0.10」にしかならず、T687の狙いが事故密度軸で機能しない。
- graph.py:96,144,150,198-199 P3 残骸/重複（読み手のいないフィールドの二重ロード） — curvature_deg_per_kmがEdgeLike／LeanEdge／__reduce__のpickle列として探索グラフ全体に載っているが、edge.curvature_deg_per_kmを読むコードが存在しない（評価側は別経路のEdgeMaterialBundleから読む）。road_graph_repository.py:1369が探索用トポロジクエリでSELECTし:1086が全LeanEdgeへ載せ、graph_material_cacheのpickleにも列として乗るが、消費者はattributes.py:167/479だけ。同じ列を:2380で改めてbundle側にもロードしており、タイルあたり数万〜十数万Edgeぶんの無駄な転送・pickleが常時発生。書き込み経路（build_road_graph→save_graph :1230）には必要なので削れるのは読み出し側。
- scripts/export_openapi.py:51 P3 取り残された消費者（T673のファイル分割） — 0次フィルタの正準がdomain/hard_filters.pyへ切り出された後もHARD_FILTER_HIGHWAY_TYPESだけapp.domain.evaluation経由の再エクスポートでimport（直後の:52のDEFAULT_HARD_FILTERSはhard_filtersから直接）。evaluation.py:58-62のimportが将来整理された時点で生成物スクリプトが壊れる。
- tile_score_matrix_cache.py:61-70 P3 docs乖離（キャッシュ世代の手動トリガー一覧） — _SCORE_MATRIX_REVISIONを上げるべきトリガー一覧に、T673でdomain/hard_filters.pyへ移ったHARD_FILTER_HIGHWAY_TYPESの増減が明示されていない。フィルタを1件足してもhighway_filter_flagsはdictのキーが増えるだけでSCORE_MATRIX_COLUMN_SIGNATURE（列名のSHA-1）は変わらず、ディスクキャッシュ済みタイルは新キーを持たないまま復元されcompute_hard_filter_excluded（hard_filters.py:135）が新フィルタを黙って適用しない。訪問済みbboxだけ新フィルタが効かないという局所的で気づきにくい症状。

### consistency
- **graph.py:46 P2 契約不一致（コメントが主張する挙動と実装が逆）** — 新規追加コメント「Noneは『測れない』（頂点3点未満・距離0）で0ではない」が実装（geo.py: curvature_deg_per_km）と逆。頂点2点はNoneではなく**0**を返す。geo.py:73-76は「頂点2点の折れ線は直線であり、曲がりが無いという測定結果として0を返す」と明記し、test_precompute_edge_curvature.py: test_two_point_edge_is_zero_on_both_sidesが「片方だけがNoneだと再splitのたびに『まっすぐな道』と『材料欠損』が入れ替わる」として0を固定（routing-engine.md:760-761も同じ）。graph.py側の説明だけが「2点＝欠損」と読め、これを根拠に「修正」すると欠損と0の取り違えを実際に作り込む。
- frontend/src/components/RouteAxisProfile/axisRawValue.ts:34-38 P2 契約不一致（表示精度と配信精度） — formatNumberが絶対値1未満の生値をtoFixed(2)で出す前提だが、backendが返せるaxis_raw_valuesは小数1桁まで。事故密度軸で「0.00件/(km・年)」となり0.04でも0.001でも同じ文字列。さらにtotal = rawValue * distanceKm（:28）も丸め後の値を掛けるためtotal<0.5の分岐（:30）に落ちて実数の併記も消える。両側のどちらが正かがコード上で決まっていない。
- test_route.py:292-310 P2 テスト欠落 — merge_axis_raw_values／aggregate_segments_into_binsの生値テストが全て0.5以上の値（0.5・2.5・4.0）で、丸め粒度が問題になる小さい生値のケースを1件も持たない。test_route_generator.py:460-477も4.0/0.0/1.0のみ。
- material_catalog.py:104-107 P3 契約不一致（コメントと実データの矛盾） — tile_property_direction_dependentの説明が「現行MATERIAL_CATALOGに該当する材料は無い…将来の型的な安全弁」と述べるが、同ファイル内のgradient_percent(:549)とwind_drag_ratio(:569)が既にTrueを設定。axis_display.py側docstring(:9-27)は正しくgradientがTrueである前提で書かれており食い違っている。
- route.py:179-184 P3 docs乖離（「両フィールド」という消えた前提の残存） — _merge_axis_value_dictのdocstringが共有者を2つと書き「両フィールドと同じ規約」と締めるが、現在の呼び出し元はmerge_axis_raw_values・merge_material_valuesを含む4つ。difficulty系2つの都合（0-100・小数1桁丸め）だけを見て変更すると生値・材料値の集約が同時に壊れる（実際にその丸めが上記P2の原因）。
- traffic.py:125-134 P3 docs乖離（件数の言及が実体と不一致） — POI_COUNT_KINDSの説明が「次の3点で異なる」だが直後の箇条書きは5点。T654で車止め・減速構造を足した際に件数が追従していない。
- test_traffic.py:4-40 P3 テスト欠落 — TestClassifyStopPoiがbarrier/traffic_calming（T654の新kind）を1件も直接検証せず、_BARRIER_STOP_VALUESの「意図的に外した値」（kerb・toll_booth・entrance・fence等）も分類器側では固定されていない。_BARRIER_STOP_VALUESへkerbを足してもプロファイル側さえ変えなければどのテストも赤くならない一方、_POI_COUNT_KIND_EXPR経由で停止密度が実データで跳ね上がる。
- test_precompute_edge_curvature.py:5-6 P3 経緯コメント（**新規追加分**） — 新規テストのモジュールdocstringに「実際にST_Azimuthをgeometryのまま呼んで平面計算になり…15%ずれた（docs/tasks/T691.md）」という事故の顛末（docs/comments.md「書いてはいけないコメント」の形）。review_checks.py docsの機械的検出はbackend/app・frontend/srcのみが対象のためtests配下は素通りする。

### 確認結果（指摘ではない）
- domain層からのI/O漏れ無し（external importはastral・numpy・scipy・rustworkx・pydantic・標準ライブラリのみ、app.services/infrastructure/apiへの依存ゼロ）。
- routing.pyの本レンジ差分はfind_missing_lazy_graph_edge_idへのalso_required_in追加のみ（+11/-9行。「+260行超」は前回レビュー時点の値で本レンジには該当せず）。
- 材料カタログの正準性: _SURFACE_VALUE_LABELSのキー集合はdomain/road.pyのGOOD/BAD_OSM_SURFACE_TAGSと完全一致。MATERIAL_CATALOGの全tile_propertyが_ROAD_SURFACE_TILE_MVT_SQLに実在。
- SQL CASE式↔domain純関数の突き合わせは新属性にも追従済み（TestPoiCountKinds・test_precompute_edge_curvature.py・TestProfileMatchesClassifier）。


## シャード S3: backend/app/services

### 前回指摘の解消確認
1-2 逆回りレグ割当・2-4 score_matrix未検証・3-6 prepare引数順・6-21 cost_by_edge_id誤参照・8-11 関数内import・9-6 JST定数5箇所は**すべて解消**。
road_graph_engine.py は2,089行で個別閾値2,200行の範囲内。select_shortest_distance_routeはselect_via_nodesと木の作り方だけが違う66行の追加で、前回のKEEP判断（3アルゴリズム同居・シームは_LegCostComposer）を覆す新根拠は無い。

### overall
- **road_graph_engine.py:1264-1273 P1(→統合でP0) 契約不一致/一本道からの逸脱** — select_shortest_distance_routeが前向き木・後ろ向き木のコスト配列にcontext.statics.edge_length_m（純粋な実距離）をそのまま渡すため、0次ハードフィルタ（motorway/trunk/no_bicycle・max_average_grade_percent）で除外されたEdgeを通る経路を返す。軸重み経路（context.legs[0].cost_list）は_build_search_graphのcost_array = np.where(self._hard_filter_excluded, np.inf, cost_array)（:299）でinf化されており、通行可否はこのコスト配列だけが表現している。既定（hard_filters=None→DEFAULT_HARD_FILTERS={"no_bicycle","motorway","trunk"}）で目的地モード生成すると「最短」タブ（is_shortest_distance=Trueで先頭固定、route_generator.py:511）が自転車通行禁止・自動車専用の区間を含む経路になる。_build_segment_detailsが読むleg.difficulty_arrayはinf化前のため区間表示も通常の難易度を示し、手掛かりが1つも出ない。
- **axis_preview_service.py:131-141 P1 契約不一致** — _distributionのヒストグラムが下限0固定で負の生値を先頭階級へ潰す。upper = p99*1.2 if p99>0 else max(upper,1.0)（:131）が負値のみの分布でupper=1.0になり、i=min(BINS-1,int(value/width))→buckets[max(0,i)]（:135-136）が負のiを0へクランプ。負の生値を持つ公開軸はbicycle_infra_quality・openness・nightの3軸。openness軸は実際の生値が概ね-200〜0（分位はp10=-156,p50=-99,p99=-12を正しく返す）にもかかわらずbinsが[(0.0,0.0167,1.0)]の1本だけになり、フロントのscoreBandsが中央値≈0.008を折れ点へ当てて得点100へ写し「100点:100.0%」と表示、distributionWarningsが「延長の100%が満点に張り付きます」という**事実と正反対の助言**を出す。zero_share（:141、v<=0を数える）も1.0＝「全区間がゼロ」に。
- route_generator.py:511-512 P2 契約不一致 — candidates.sort(key=lambda c: not c.is_shortest_distance)直後にcandidates[:max_routes]で切るため、max_routes=1（routes.py:164はge=1で許可）の目的地モードでは返る唯一の候補が常に距離最短ルートになり軸の重み設定が一切反映されない。docstringは基準線が答えを置き換える挙動を述べていない。テストはmax_routes=2のみ。
- evaluation_service.py:19-31 P3 残骸 — __init__（self._preference保持）が削除されクラスは状態を持たなくなった。evaluate_graphはcompute_edge_costs_bulkへ素通し委譲するだけで、本体からの呼び出し元ゼロ（testsとbenchmarksのみ）。クラスである根拠が失われている。

### consistency
- **test_road_graph_engine.py:1460-1479 P1 テストが欠陥を仕様として固定** — test_select_shortest_distance_route_ignores_axis_costが最短側Edgeをhighway="trunk"にして shortest.data == ["e-0-out","e-0-in"] をアサート。コメントは「trunkにして軸コストを高くし」と述べるがtrunkは0次ハードフィルタ対象で既定ではinf＝通行不可。ranked[0]が遠回りを選ぶのは「避けた」のではなく「通れない」ため。上記overall #1のフィルタ迂回をそのまま期待値として固定している。routing-engine.md:365-372も0次フィルタの扱いに触れていない。修正しようとするとこのテストが赤くなる。
- axis_preview_service.py:1-181 P2 テスト欠落 — 新規181行のモジュールに対応するbackendテストが1件も無い（axis_preview・value_distribution・_distributionのいずれもヒットなし）。分位算出(:120-127)・階級化(:132-140)・_raw_valueのrequired/preprocess分岐(:88-107)はいずれも純関数で未検証。POST /api/axis-admin/preview-distributionとGET /api/material-catalog/{id}の計算本体。
- route_generator.py:494-498 P2 契約不一致 — candidates[shortest_index].model_copy(...)がevaluate_loopsの戻り値と引数tracedの件数・順序1対1対応に依存するが、LoopRoutingEngine Protocolのdocstring（:35-37）は位置対応を契約として明示していない。エンジンが候補を1件でも落とすとshortest_indexが別候補を指し、件数が減るとIndexErrorで500。select_loop_turnaroundsは「失敗した候補はスキップされる」と明記されており非対称。
- axis_preview_service.py:45-51 P3 docs乖離 — ValueDistributionのdocstringがbinsを「(下限,上限,割合)」と述べるが実装(:137-140)は常にi*width＝0起点の下限しか生成しない。zero_shareという名前・フロント表示「ゼロ○%」に対し実装はv<=0で負値もゼロとして数える。
- route_generator.py:87-88 P3 残骸 — JST定数がdomain/time_zone.pyへ移設された際、説明コメントだけが主語の無い状態でturnaround_pool_sizeの前に浮いている。
- region_service.py:90 P3 残骸 — 同型。MVT_CONTENT_TYPEがtile_serving.pyへ移設された際コメントだけ残存。記述（road_surface・poiの2つに限る）は現在の事実とも合わない。定数移設のたびに同じ形の残骸が2箇所同時に出ている。


## シャード S4: backend/app/infrastructure

### overall
- road_graph_repository.py:894-912/2481-2487 P2 スケール（T660パターンの取り残し） — recompute_edge_curvatureだけがLIMIT/OFFSETページングで、T660が確立したstream_id_chunks（サーバーサイドカーソル）を使っていない。コメントは「edge_idの範囲で分割」と述べるが実装はOFFSET。本番500万行・CHUNK_SIZE=200,000で25チャンク、WHERE distance_m>0がヒープ参照を要求するため累計約6,500万行（実データの約13倍）を走査。他5本のprecomputeは全てstream_id_chunksに揃っておりこの1本だけが規模とともに悪化。バッチ実行中にsave_graphが新規Edgeを挿入するとウィンドウがずれ一部Edgeが未更新のまま残る。
- jma_tile_interpolation.py:119-160 P2 スケール/ログ方針 — 新設のcrop_and_upscale_mvtがリクエスト経路（jma_tile.py:62のasync def _interpolated_tile内で同期呼び出し）でフィーチャーごとのshapely intersection＋MVT decode/encodeを行うがasyncio.to_threadへ逃がしておらず所要時間の計測も無い。docs/logging.md「CPUバウンドの重い処理も外部APIと同様に所要時間を計測対象にする（過去にイベントループ停止の原因になった実績がある）」に反する。/api/debug/statsにもINFOサマリにも痕跡が残らない。
- graph_material_cache.py:58,66 / tile_score_matrix_cache.py:79,87 P2 キャッシュ方針（手動同期ペア） — pickle列構成の署名とディスクキャッシュのパスへ埋まる世代文字列が独立した2つの手動更新値。ドリフト検知テストは署名の一致しか検証できない。署名だけ更新して版を上げ忘れるとテストはgreenになり、デプロイ後に旧世代pickle（同じパス）が復元され最後の列が欠けたままAttributeError。docs/caching.mdが禁じる形そのもので、tile_score_matrix_cache.py:95で材料世代との複合にした対応が署名側へ適用されていない。
- material_coverage.py:190-200 P2 契約不一致（消費者取り残し） — poi_{kind}_per_kmのカバレッジが_EDGE_ATTRIBUTE_COUNTS_PRESENT_SQL（行の有無のみ）を使いpoi_counts IS NULL（未集計）を「値あり」と数える。migration 0034がこの前提を明示的に廃止している。0034適用済み・precompute未再実行の状態でGET /api/admin/material-catalog/coverageを開くと5材料すべてが欠損0%と表示される一方、get_edge_materials_batch（:2392）はNoneへ落とすため実際は停止要因の全軸が算出不能。同ファイルのgradient_percent（:175-179）はWHERE average_grade IS NOT NULLと正しく書かれており片側だけ流儀が崩れている。
- material_coverage.py:206-216 P2 契約不一致（母集団の取り違え） — curvature_deg_per_kmのカバレッジがway_geometryのみを見るが、ルート探索・ルート結果が実際に読むのはroad_edges.curvature_deg_per_km（別テーブル・別バッチ）。trees_percentとの類推は成立しない（trees_percentは保存場所が1つ）。precompute_way_curvature.pyだけ実行しprecompute_edge_curvature.pyを実行しないと、カバレッジ画面は欠損0%を報告する一方ルート探索側は全EdgeでNULL＝軸が算出不能。
- road_graph_repository.py:2185-2199/2618-2619 P2 レイヤー/契約（型の無い層またぎ） — sample_way_rowsがSQLAlchemyの生Rowをlist[Any]のままinfrastructureの外へ返し、axis_preview_service.py:61-83が列別名で直接属性アクセス。リポジトリの契約が「SQLがたまたまSELECTしている別名の集合」になっており型検査も突き合わせテストも効かない。他の読み出し（get_way_attribute_counts等）は全てドメイン型へ詰め替えており、ここだけ流儀が崩れている。
- road_graph_repository.py:2607-2610 P3 フラット委譲の対称性 — AttributeRepository.get_poi_counts_by_kindが持つ第3引数on_edge_tolerance_mをファサードが受け取らず既定値で固定して委譲（cluster_eps_mだけ通す）。調整したい呼び出し元はファサードを迂回するしかない。
- graph_material_cache.py:40-48 P3 docs乖離 — 「refresh_derived.pyはPBF再取込を除く下記バッチ一式を1コマンドで実行する」の直下にprecompute_edge_curvature.pyを列挙しているが、refresh_derived.py:58-65のSTEPSは8段でedge_curvatureを含まない。手順書どおり実行した運用者が「蛇行も埋まった」と誤解する。

### consistency
- road_graph_models.py:189-191/244-246 P2 契約不一致（実装・docsと逆のことを述べるコメント） — poi_countsの新規コメントが「値を足し合わせるとstop_countに一致する」と不変条件を宣言するが、実装・設計書はいずれも一致しないと明言。stop_countは_STOP_POI_COUNTS_SQL（線から一定距離内の点の総数）、poi_countsは_POI_COUNTS_BY_KIND_SQL（構成ノード限定＋始点除外＋40mクラスタリング）と別意味論。get_poi_counts_by_kindのdocstring（:2124-2126）は「したがって両者の合計は一致しない」、static-road-attributes.md:148は「**数え方はstop_countとは異なり、合計も一致しない。**」と明記。T655段階2時点では成り立った記述が段階3で成立しなくなったまま残存。
- road_graph_repository.py:2336-2337 P2 削除された事実を語る記述の残存 — get_edge_materials_batchのdocstringが「poi_countsはNOT NULL DEFAULT '{}'のため行があれば必ず辞書」と述べるが、migration 0034がまさにそのNOT NULLとDEFAULTを落とし既存の'{}'をNULLへ書き換えている。実装(:2392)もORM定義(nullable=True)もNULL前提で、docstringだけが廃止済みの制約を語る。上のmaterial_coverageの欠陥もこの同じ誤った前提から派生している可能性が高い。
- axis_preview_service.py / road_graph_repository.py:2185 P2 テスト欠落 — axis_preview_serviceとsample_way_rows/_SAMPLE_WAY_MATERIALS_SQLを参照するテストがリポジトリ全体に1件も無い。分布プレビュー（T686）の機能一式が無検証。他の同種の読み出しは全てPostGIS統合テストを持っており、この1本だけが抜けている。
- road_graph_repository.py:705-712 P3 宙に浮いたコメント — 「区間インスペクタ（開放度軸）…8列全て返す」は_WAY_LANDCOVER_BY_OSM_WAY_ID_SQL(:746)の説明だが、その直後へ_SAMPLE_WAY_MATERIALS_SQL(:713)を空行なしで割り込ませたため無関係な定義の説明として読める位置に取り残されている。
- test_road_graph_repository.py:1309 P3 個数表現の陳腐化 — test_..._combines_all_five_materials_correctlyの"five"が実態と合わない（EdgeMaterialBundleは8フィールド）。
- tile_score_matrix_cache.py:61-67 P3 双子実装の非対称 — 「この値を手動で上げること」のバッチ列挙にprecompute_edge_curvature.pyが無い。対になるgraph_material_cache.py:44-48には追加済み。
- road_graph_repository.py:918-941 P3 「未計算」と「算出不能」の区別の穴 — _RECOMPUTE_WAY_CURVATURE_SQLがWHERE ... AND w.geom IS NOT NULLのためgeomがNULLのwayには行が一切作られず、WayGeometryRowのdocstring(:301)と本SQLのコメント(:917)が主張する2状態の契約がgeom NULLのwayで成立しない。T688と同型の欠陥を新設way_geometryが再導入している。
- docs/modules/backend/static-road-attributes.md:17 P3 個数表現の陳腐化 — 「precompute系4本が共有する」が実際は5本。
- test_msm_freshness.py:10 P3 正準定義の迂回 — from app.infrastructure.msm_client import JSTのまま。T680で正準定義はdomain/time_zone.pyへ移り、msm_client.py:26は再エクスポートを持つだけ。

### 確認結果（指摘ではない）
- **PostGIS統合テストはCIで実際に実行されている**（ci.yml:70のpostgis/postgis:16-3.4サービス＋TEST_DATABASE_URL）。conftest.py:133のskipはローカル未接続時のみ。新規SQLにも実DB検証テストがあり、test_precompute_way_curvature.pyはSQLとdomain/geo.py: curvature_deg_per_kmの一致を許容差付きで固定（SQL⇔domain純関数の突き合わせ）。
- **タイル世代の対上げは正しく行われている**: ROAD_SURFACE_TILE_VERSION 18→23、POI_TILE_VERSION 3→4、region-tile-config.jsonも同一範囲で再生成済み。
- **migration（0033〜0036）とモデル定義の整合**は列の型・NULL可否とも一致。ただしconftest.pyのroad_graph_engineはcreate_allのみでapply_pending_migrationsを通さないため、この整合を機械的に検証しているのはtest_migrate.py（DB接続必須）だけ。
- _bulk_upsertのif not rows: return追加と_bulk_chunk_rows（列数からパラメータ上限を導出）は構造的な解消として妥当。
- _SAMPLE_WAY_MATERIALS_SQLのTABLESAMPLE SYSTEM、axis_preview_serviceのTTLCache利用はdocs/caching.mdの方針に沿っている。


## シャード S5: backend/app/api + batch + migrations + scripts

### overall
- batch/refresh_derived.py:57-66 P2 一本道からの逸脱/残骸 — 「派生データ再構築の単一エントリポイント」を名乗るが、新設のprecompute_edge_curvature.pyだけが_STAGESに登録されておらずどの再構築経路からも呼ばれない。python -m app.batch.refresh_derivedを実行してもroad_edges.curvature_deg_per_kmは埋まらず、build_road_graphがsplit時に埋める新規Edgeだけが値を持ち、is_split_up_to_dateが真でスキップされたタイルの既存EdgeはNULLのまま。蛇行軸（default_weight=0.1、公開済み）はそれらの区間で欠損扱いになり重み再正規化で薄まって無警告のまま評価から抜ける（T74・T101・T242と同型）。_STAGESとapp/batch/のファイル一覧を突き合わせるドリフト検知テストも無い（test_refresh_derived.pyは8段を手書き列挙するのみ）。
- batch/_common.py:116-133 P2 スケール — 新設のstream_id_chunksが読み取り専用セッション（サーバサイドカーソル）をバッチ全体の実行時間ぶん開いたままにする。docstringは「書き込みは別セッションで」とだけ述べ、読み取り側トランザクションの長時間保持に触れていない。precompute_elevation_attributes.py（chunkごとにGSI標高APIへ外部HTTP、初回バックフィルで数時間規模）が本diffでこの方式へ変わったため、カーソルを持つトランザクションが数時間idle in transactionのまま残りxminが進まずVACUUMが死行を回収できない。idle_in_transaction_session_timeoutを設定した環境では途中で切られasync forが例外で落ちる。precompute_way_landcover.pyも同型。
- api/dependencies.py:268-276 P3 非対称/スケール — 新設のget_road_graph_repositoryがget_session_factory()（command_timeout=20、タイル配信と共有）を使うが、唯一の利用者2件はいずれも全表走査寄りの管理APIで、同種の管理API（get_material_coverage_service等）は同じ理由で明示的に180秒のget_route_generation_session_factory()を選んでいる。全国展開でosm_raw_waysが数倍になると20秒超でQueryCanceledが未捕捉の500になる。同じ20秒プールをタイル配信と共有しているため重いクエリがタイル配信のコネクションを奪う。
- scripts/export_openapi.py:293 P3 重複（正準定義の複製） — "keys": sorted({"no_bicycle", *HARD_FILTER_HIGHWAY_TYPES})が正準集合DEFAULT_HARD_FILTERS（同ファイル:52でimport済み）を使わず"no_bicycle"を文字列リテラルで再構成。将来highway種別に由来しない4つ目のハードフィルタをDEFAULT_HARD_FILTERSへ足すとkeysにだけ載らず、HardFilterOverride._check_filter_keysの完全一致検証でhard_filtersを送る全ルート生成が422になる——このコメント自身が警告している事故を生成側の複製で再生産している。
- services/axis_preview_service.py:54-57 P3 スケール — _load_sampleのモジュール共有TTLCache（maxsize=1）に排他が無く、キャッシュミス時に同時到着したリクエストがそれぞれ抽選クエリを実行する。呼び出し口にタイル系のような同時実行semaphoreもレート制限も無い。AxisComposerが材料一覧ぶん/distributionを叩くため、初回に材料数ぶんのTABLESAMPLEクエリが同時に走りプール上限15に対して数十本が並ぶ。

### consistency
- **scripts/export_openapi.py:103 / frontend/src/types/generated/axis-catalog.json P1 生成物ドリフト（本体で再現確認済み）** — backend/fixtures/axis_definitions_snapshot.json（revision 64、公開軸10）とaxis-catalog.json（9軸）の軸集合が食い違う。スナップショットはstop_densityを削除しaxis_abdaded0be20（停止密度）へ差し替え、curvature（蛇行、default_weight=0.1、is_published=true）を追加しているが、静的カタログは旧stop_densityのままでcurvature・axis_abdaded0be20を一切含まない。実行時APIの取得完了前・失敗時に使われる静的フォールバック（useAxisCatalog.ts: FALLBACK_CATALOG／evaluationAxes.ts: DEFAULT_ROUTE_PREFERENCE）が存在しないstop_densityを提示し「蛇行」軸を欠いた状態でrampAxes・axisLabels・routeStyleModesを構成する。経緯: 509f0210がスナップショットのみ更新、axis-catalog.jsonの最終更新は45ffd36bで停止。
- docs/architecture.md:1274-1282 P2 docs乖離 — 7章「軸/axis_id/既定値」表（軸一覧の正と宣言している表）が追従していない。①curvature行が無い、②停止密度のaxis_idをstop_densityと書くが実DB/スナップショットではaxis_abdaded0be20、③自転車インフラを「正規化フラグ材料4件」と書くが実際は5件（shared_pedestrian_path追加済み、axis_admin.py:270-271は同diffで「boolean材料5件」へ更新済み）。CLAUDE.mdの「規模M以上でドメイン概念を新設するタスクは完了条件へarchitecture.md追従を既定で含める」に該当する更新漏れ。
- docs/batch-pipeline-dependencies.md:24-60 P2 docs乖離 — 依存DAGの正である本ファイルがprecompute_edge_curvature.pyを全く扱っていない（①〜⑪の番号にも表にも無い）。冒頭の「配下10バッチ」という件数も合っていない（番号付きだけで11本）。手順どおり実行した運用者はedge curvatureのバックフィルを実行しない。
- api/routers/material_catalog.py:151-172 P2 契約不一致（兄弟の一部にしか無いガード） — 新設のget_material_distributionだけDBAPIError→503変換を持たない。同ファイルのget_material_coverage(:217-223)は明示的に503へ変換し、axis_admin.pyも全CRUDを_guard_db_errorsで包み、双子のpreview_axis_distribution(axis_admin.py:500)も通している。migration 0036未適用の環境で材料分布を開くとLEFT JOIN way_geometryがUndefinedTableErrorを投げ、原因の当たりが付かない素の500になる。同じ原因に対して2つの異なる見え方をする。
- api/routers/axis_admin.py:485-503 P2 契約不一致 — 【S3と重複】ValueDistributionResponse.binsは「(下限,上限,割合)」と宣言するが生成側は階級を常に0起点で刻み負の生値をmax(0,i)で0番目へ押し込む。bicycle_infra_quality・opennessはいずれも生値が負で、分布プレビューを開くとp99が負のためupperが1.0へ倒れ全サンプルがbins[0]=(0.0,0.0167,1.0)に集まりzero_share=1.0になる。「0点が100%」の警告と1本だけの棒が出て実際の値域（-200〜0）はどこにも現れない。T686が「折れ点の妥当性を実データで判断できるようにする」ために作った機能が、較正が最も難しい負値軸で嘘の分布を返す。
- api/routers/material_catalog.py:32 P3 削除・統合された事実を語る残存表現 — 「上の2エンドポイントと異なり認可を要求する理由は…」がT661の移設後も残存。上に並ぶのは今や3エンドポイントで、うち2つ（/values・/distribution）は同じくBasic認証必須のため「異なり」という対比自体が成立しない。次に材料カタログへエンドポイントを足す担当が「values相当は認可不要でよい」と読み取り、T661で塞いだ穴を再び作る。
- api/routers/routes.py:73 P3 docs乖離（コード内参照先の陳腐化） — RoutePreferenceがdomain/route_preference.pyへ、DEFAULT_HARD_FILTERS・is_edge_allowedがdomain/hard_filters.pyへ移動しimportも書き換わったが、docstring/コメント4箇所（:73/:85/:108/:152）が旧位置domain/evaluation.pyを指したまま。:149のcompute_cost_from_axis_scoresだけは今も正しいため、どれが正しい参照か読み手には区別できない。
- api/routers/axis_admin.py:210 P3 docs乖離（**新規追加行**） — 本diffで新設したvalidatorのdocstringが「evaluate_dynamic_axis_arrays（domain/evaluation.py）」と書くが、この関数はdomain/dynamic_materials.py:122にある。新規に書き加えた行自体が誤った参照先を指している。
- tests/test_axis_admin_routes.py P2 テスト欠落 — 新設2エンドポイント（POST /api/admin/axis-definitions/preview-distribution、GET /api/admin/material-catalog/{id}/distribution）のテストが1件も無い。同ファイルの既存エンドポイントは全件について「認可なしは401」「DBエラーは503」を対で持ち、test_material_catalog_routes.pyは本diffで/valuesの401・誤資格情報テストを新設している。T661が「認可の非対称」を是正した直後に同じファイルへ非対称のまま新エンドポイントが2本増えている状態を検知する手段が無い。
- batch/precompute_edge_curvature.py:30 P3 契約不一致（コメントと実装） — 【S4と重複】コメントが「edge_idの範囲で分割する」と述べ road_graph_repository.py:895も同様だが、実装（:53のrange(0,edge_count,CHUNK_SIZE)とORDER BY edge_id LIMIT/OFFSET）はキーセット分割ではなくOFFSET分割。

### 確認結果（指摘ではない）
- migration 0033〜0036はいずれもDDLのみでaxis_definitions/axis_registry_metaへの行データ投入は無くルールに適合。
- load_axis_definitions_snapshotの呼び出しはbootstrap_fresh_db.py/bootstrap_ci_db.pyの2本のみ。import_pbf.py等の稼働中DB向けバッチからは呼ばれていない。
- タイル世代の対上げ（ROAD_SURFACE_TILE_VERSION 18→23、POI_TILE_VERSION 3→4、TILE_MATERIALS_CACHE_VERSION 5→9、region-tile-config.json）は同一コミットで揃っている。
- review_checks.py docs --since 3749bdcf は違反なし、新規追加行に経緯コメントの混入なし（backend/app配下）。


## シャード S6: frontend/src/components/Map/MapView.tsx とテスト群

### overall
- MapView.tsx:2358-2361/3315-3357/3418-3422/2362-2365 P2 局所最適の連鎖/スケール — T607でdedicatedWayValueLoading（フェッチ進行中フラグ）をbuildStaticOverlayLayersの入力へ加えた結果、パン・ズーム毎のフェッチ状態変化がstaticOverlayLayers配列の参照を毎回作り直し、色式とは無関係な「表示ON/OFF一括適用」「setStaticOverlayFilters（全オーバーレイへsetFilter）」「interactiveLayerIds再構築」まで巻き込んで再実行させる。レンズに風を選んでパンすると、useDedicatedWayValuesが500msデバウンス後にloading=true→falseと2回遷移し、elevation/ramp軸/designation/tunnel/oneway/専用配信軸/gradientFill/accidents/stopPoi/supplyPoiの全レイヤーへensure()+setLayoutProperty、およびsetFilter（MapLibreは各タイルの地物評価をやり直す）がパン1回につき2巡走る。
- MapView.tsx:1068-1207 P2 ensure関数の構造的欠陥（T587の横展開不足） — ensureDynamicWeatherLayerが再適用しているのはpaintプロパティだけで、minValueToShow由来のfilter（:1113-1117/:1168-1170/:1200-1204）とgridMarkのicon-size式（minScale/maxScale/maxValueForFullScaleを内包、:1153-1159）はaddLayer時の値で固定されたまま。将来DYNAMIC_WEATHER_RENDERERSのspecを実行時値から組み立てると、色は追随するのに間引き条件とアイコンのサイズ曲線だけが固定される。T587と同一の失敗形。
- MapView.tsx:1567-1574 P3 ensure関数の構造的欠陥（T587の横展開漏れ） — T587が修正した4関数と同型のファクトリでありながら、designation/tunnel/onewayを作るmakeEnsureAttributeLineLayerだけが`if (map.getLayer(layerId)) return;`のまま色式・不透明度式を再適用しない。判断原則16の横展開対象がこの1関数だけ残っている。
- MapView.tsx:1900 P3 正準定義の複製 — ramp軸レイヤーの除外判定がlayer.key.startsWith("axis:")という文字列リテラルで、axisLayers.ts:244-247のAxisMapLayerId/axisMapLayerId()という正準定義をコピー（mapLayers.tsにisAxisStudioLayerという既存の判定関数もある）。
- MapView.tsx:2371-2374 P3 重複 — buildRoadSurfaceSharedLayerIds(rampAxes, dedicatedAxes)をMapView.tsx(:2371)とpage.tsx(:846-849)が同じ入力から独立に2回計算している。

### consistency
- **MapView.tsx:2643-2652/2950-2958/3446-3455 P1 契約不一致（本体で検証済み）** — 「表示範囲が広すぎます」判定 isRoadSurfaceGroupVisible(visibility, roadSurfaceSharedLayerIds) の第2引数にはramp軸・専用way値配信軸のレイヤーIDが含まれる（mapLayers.ts:559-579が...dedicatedAxes.map(dedicatedWayValueMapLayerId)と...rampAxes.map(axisMapLayerId)を明示的に含める）のに、3箇所すべての呼び出しが第1引数へ{roadType, roadSurface, designation, tunnel, oneway}の5キーしか渡さない。判定本体はroadSurfaceSharedLayerIds.some((id) => visibility[id])（mapLayers.ts:1974）なので、軸レイヤーIDは常にundefined＝falsyで一度も数えられない。effectの依存配列にもこれらが無い。
  【本体での確認】MapViewはaxisVisibility・dedicatedWayValueVisibility（いずれもRecord<string,boolean>、**軸id**キー）をpropで受け取っている（:2212/:2170）が、roadSurfaceSharedLayerIdsは**レイヤーid**（`axis:<axisId>`）を持つため、単に展開しても噛み合わない。配線が最後まで通っていない。
  失敗シナリオ: 「路面/道路の種類」をOFFにしたままレンズで風やcar_stressだけをONにし、ROAD_TILE_MIN_ZOOM未満まで引くと、道路が一本も塗られないのに案内が一切出ず「データが無い地域」と区別できない。MapLayersPanel.tsx:251は`regionZoomTooWide && roadSurfaceSharedLayerIds.includes(layerId)`でフル一覧を使う側なので状態ドット抑制も効かない。MapView.tsx:1966-1969のコメントは「新規公開ramp軸を低ズームでONにしても案内が出ないままになるため実行時リストを渡す」と目的を明記し、MapView.dataStatus.test.ts:144-157/178-194は{"axis:car_stress": true}を直接渡して「修正済み」を主張するが、本番配線がそのキーを一度も供給しないため純関数テストが通っても機能が成立していない。さらにMapView.tsx:3432-3433のコメントは「ramp軸はこのチェックの対象外（既知の制約）」と逆のことを書いており、コメント同士も矛盾している。
- MapView.tsx:321/326-335 P2 契約不一致/ドリフト検知テスト欠落 — MapLibreがJSON文字列化するオブジェクト型フィールドの手書き列挙ROUTE_SEGMENT_OBJECT_PROPERTY_KEYSにaxis_raw_valuesが入っていない。RouteSegmentDetailは生成型（api.d.ts:1850-1852）でこのフィールドをオブジェクトとして持ち、segmentsToFeatureCollection(:337-360)はspreadで無条件にpropertiesへ載せる。T687の生値表示を区間クリック側（page.tsx:1682-1714）へ広げた瞬間、JSON文字列のまま渡りObject.entries()が1文字ずつのエントリを返してformatMaterialValueの.toFixed()がTypeError。同ファイル:319-320のコメントが「material_valuesの追加漏れで実際に実行時エラーが起きた」と記録している事故の完全な再演。MapView.segments.test.ts:137-147のフィクスチャはaxis_raw_values: {}を文字列化せずに渡すためギャップを覆い隠している。
- MapView.tsx:1429-1430/3367-3369 P2 削除された事実を語る記述の残存 — 「風penalty gridFillはDYNAMIC_WEATHER_RENDERERS汎用機構へ乗っている／統合されているため専用effectを持たない」というコメントが2箇所に残るが、風の面塗り（windPenalty.ts・penaltyFillソース）は2026-09-05に撤去済みでDYNAMIC_WEATHER_RENDERERS.windVectorはarrow（gridMark）のみ（:937-955）。:1429の相互参照先（:1463-1466）にも風の記述は無く参照自体が空振り。「勾配gridFillだけが汎用機構に乗らない例外」という現在の設計判断を、存在しない対比対象を前提に誤読させる。
- MapView.tsx:223/1382 P2 死んだ参照（docs乖離を含む） — T672でhooks/useDynamicWayValues.tsはuseDedicatedWayValues.tsへ改名され実ファイルは存在しないのに旧名への参照が残る。同じ死んだ参照はgradientGridFill.ts:41・dedicatedWayValueLayer.ts:10・useDebouncedValue.ts:11・useAxisValueDistribution.ts:46・useMaterialDistribution.ts:25、およびdocs/architecture.md:2489-2490にもある。architecture.mdはファイル名だけでなくシグネチャ（現行は軸の配列を受け取る1インスタンス）も旧仕様のまま。直前コミット74c007ce（T671「ソースコメントの参照先を是正する」）がこの是正を目的としながら取りこぼしている。
- MapView.tsx:1064-1067 P3 コメントが主張する契約と実装の不一致 — 「spec.*.colorExpression等は呼び出し元が軸スタジオ由来の動的なしきい値から組み立てることがある」と書かれているが、呼び出し元2箇所（:2622/:3410）とも常にモジュール定数DYNAMIC_WEATHER_RENDERERS[id]をそのまま渡すだけで動的経路は現存しない。T671が同じ行を書き換えながら文の主張自体は是正されていない。
- MapView.layerOps.test.ts:318-335 P3 残骸/テストが実装の実形状を守っていない — ensureDynamicWeatherLayerの再適用テストが撤去済みの風の面塗りのソースキーpenaltyFillという実在しないspec形状をas anyで作って検証している。raster/gridMark/vectorの再適用、およびfilter・icon-sizeの非再適用は一切カバーされていない。「T587の回帰テストがある」と見なされているためこれらを誤って削除・改変してもCIが緑のまま通る。
- docs/modules/frontend/dynamic-weather-layers.md P3 docs乖離（関数名ドリフト） — 「MapView.tsx内にensureGradientFillLayer/applyGradientFillGeojsonという独立実装を持つ」と記すが実体はファクトリ化されたmakeEnsureGradientFillLayer(display, loading)（MapView.tsx:1437）でensureGradientFillLayerという識別子は存在しない。
- docs/modules/frontend/static-map-layers.md P3 docs乖離（新規依存の未記載） — 路面ポップアップの路面状態ラベルが手書き辞書から生成物参照（material-catalog.jsonのvalue_labels、MapView.tsx:100/:2081-2090）へ変わったが、対象ファイル表・本文に生成物依存の記載が無い。
- docs/modules/frontend/static-map-layers.md P3 docs乖離（記述の破損） — 「出所の異なる2つのPartial<Record<MapLayerId, LayerDataStatus>>をマージしたものであるの2系統:」という編集途中の壊れた一文が残っている。
- MapView.routes.test.ts:291-304 P3 テスト名と検証内容の不一致 — 名前の前半が「再度trueで呼ぶとvisibleへ戻す」だが本体は3回目もfalseで呼びnoneのままを検証している。

### 規模判定
MapView.tsx 3,545行はsize_watch.jsonの個別KEEP閾値3,700未満のため**行数自体は指摘対象外**。
「複数の変更理由の同居」の観点では静的レイヤーの生存管理・動的気象の汎用描画機構・ルート/区間描画・
マーカー/クリック配線・データ取得状態の5系統が同居するが、いずれもMapLibre `map`インスタンスという
単一の可変資源への配線であり、純ロジックは既にaxisLayers.ts/dedicatedWayValueLayer.ts/valueScale.ts/
useLayerDataStatus.ts等へ分離済み。**分割を促す新しい根拠は無し（Keep List維持）**。


## シャード S7: frontend/src/components/Map（MapView.tsx以外）

### overall
- **primaryAttributes.ts:44 P2 残骸/未使用エクスポート（本体で検証済み）** — PRIMARY_ATTRIBUTE_CHIP_LABELS（18件の手書き対訳表）に**本番の消費者が1つも無い**。参照はprimaryAttributes.ts:44（定義）・primaryAttributes.test.ts:9/19/26（テスト）・secondaryAxes.ts:36（コメント）のみ。docstringは「材料一覧表示で薄字ラベルとして使う」と述べるが、MapOverlayControls/MapLayersPanelのいずれもこの辞書を引かない。「一次属性を1件足すたびに、誰も読まない表を4文字以下・一意という制約付きで更新しないとCIが落ちる」だけの負債。
  【本体での確認】`PRIMARY_ATTRIBUTE_LABELS`（正式名、生成物から導出）はaxisInspectorPopup.ts:11が実利用。`primaryAttributeIdsToLayerIds`はpage.tsx:39が実利用のため`PRIMARY_ATTRIBUTE_LAYER_IDS`は生きている。`PRIMARY_ATTRIBUTES_WITHOUT_LAYER`がテスト専用なのは「漏れ」と「意図的にレイヤー無し」を区別するという設計意図どおりで正しい。**死んでいるのは略名表だけ**。
- primaryAttributes.ts:44,70,93 P2 抽象化不足/1本道の非対称 — 軸については「フロント固有の表示情報」をbackendの軸データ（chip_label/icon_id/panel_hint/show_map_icon）へ移して手書き辞書を撤去した（secondaryAxes.ts:72-81が明記）のに、一次属性だけ同じ移設がされていない。axis-catalog.jsonのprimary_attributes[]はattr_id/label/sharedの3フィールドのみ（実データ確認）。
- **secondaryAxes.ts:67 P2 残骸/契約不一致（宣言したが一度も書き込まれないフィールド）** — 本範囲でSecondaryAxisSummary.rawValueUnitを新設したが、唯一の生成関数secondaryAxesFromCatalogAxes(:110-130)がこのフィールドを設定せず、CatalogAxis（axisLayers.ts:107-174）にもraw_value_unitが無い。SECONDARY_AXESの全要素でrawValueUnit===undefinedになり、lib/evaluationAxes.ts:86（axis.rawValueUnit ?? null）は常にnullを返す。useAxisCatalogのフェッチ完了前・失敗時はRouteAxisProfile.tsx:108のformatAxisRawValueが単位を得られず、T687の「得点の隣の生値」が無言で消える。型の上では値が入るように見えるため配線漏れが読者から見えない。
- secondaryAxes.ts:116 P2 責務混在/局所最適の連鎖 — 「地図チップに出すか」の都合で絞り込んだSECONDARY_AXES（display !== null && category !== "動的" && show_map_icon !== false）が、そのまま重み一覧PREFERENCE_AXES（lib/evaluationAxes.ts:73-99）の母集合になっている。軸スタジオでshow_map_icon=falseにした公開軸はフォールバック経路の重み一覧からも消える。既にwindはcategory==="動的"で除外されるためevaluationAxes.ts:89-98で手書きの1件として復活させてあり、同じ回避策が軸が増えるたびに必要になる構造。地図UIの設定が評価設定パネルの表示可否を決めるという因果は説明可能性を欠く。
- routeStyleModes.ts:36 P3 正準定義の複製 — 新設したLENS_NEUTRAL_COLOR = "#64748b"のdocstringが「候補線の非選択色と同じ」と契約を宣言するが実体（MapView.tsx:417のリテラル）をimportしていない。同じ値はroadFilterAxes.ts:84（COLOR_SLATE）・RouteAxisProfile.tsx:38（FALLBACK_DOT_COLOR）にも直書き。
- staticAttributeLayers.ts:298 P3 UI一貫性（自ファイルの配色ルール違反） — 新規追加した停止要因POIカテゴリbarrierにオレンジ#ea580cを割り当てているが、同ファイル:39-46が「停止要因POIの種別は評価配色（緑・アンバー・オレンジ・赤の系統）を避けた中立色を使う」と定めている。AXIS_RAMP_COLORS[2]（#fb8c00）と同系のため「車止め・ゲート」が危険側の色に読める。traffic_calmingの#0d9488（:299）は補給休憩POIの「給水」（:322）と同一色で、両レイヤーは同じregion-poi-tiles/source-layer stop_poiをbaseFilterだけで分けており同時ONにできるため、同じ画面に同色の点が別の意味で並ぶ。
- riskMap.ts:100 P3 重複（同一リソースの二重フェッチ） — fetchLinearRainbandFramesとprecipitationNowcast.ts:90 fetchRasrfFramesが同じrasrf/targetTimes.jsonをそれぞれ独立に取得（コメント:42-44で「見る行が違うためフェッチは別々に行う」と明記）。fetchJmaTargetTimesに重複排除が無く、降水チップONのたびにJMAプロキシへ同一URLの往復が2回発生。「1回のfetchで4種共通」（:86-87）という同ファイル内の設計方針と非対称。

### consistency
- **primaryAttributes.ts:44,70,93 P0 テスト失敗/ドリフト検知の未追従（CI赤）** — 45ffd36b(T691)がaxis-catalog.jsonのprimary_attributes[]へcurvatureを追加したが3表がいずれも追随しておらずドリフト検知テストが2件失敗。PRIMARY_ATTRIBUTES(:31)は生成物から19件を読む。landcover（T624）は両方に追加済みなので漏れているのはcurvatureのみ。
- mapLayers.ts:324 P2 契約不一致（UI文言と実装カタログの乖離） — stopPoiレイヤーのdescription(:324)とpanelHint(:325-327)が「信号・横断歩道・一時停止・踏切」の4種を手書き列挙したままで、STOP_POI_CATEGORIES（staticAttributeLayers.ts:292-300）へ追加された「車止め・ゲート」「ハンプ・狭さく」を含まない。実際に地図へ描かれ凡例にも並ぶ7カテゴリのうち2つを説明しない。宣言的カタログと説明文の二重管理でドリフト検知テストも無い。
- **docs/design-principles.md:70 P2 docs乖離（削除済みシンボルへの死んだ参照）** — 構造仕様9が「ビルド時にしか導出できない定数（DEDICATED_WAY_VALUE_LAYER_IDS等）」を正の例として名指しするが、この定数は本範囲（T672）でmapLayers.tsから削除済み、後継はaxisLayers.ts:291 DEDICATED_WAY_VALUE_AXES。**設計原則の唯一の正本が存在しないシンボルを例示している。** 同じ死んだ参照がdocs/modules/backend/axis-studio.md:236・docs/architecture.md:2572にも残る。
- docs/modules/frontend/dynamic-weather-layers.md:265 P2 docs乖離 — 「tileBaseUrl()は…呼び出し時に評価する関数（riskTargetTimesUrl()等）として持つ」と書くが、riskTargetTimesUrl/rasrfTargetTimesUrl/targetTimesN1〜N3Urlは本範囲ですべて削除され、jmaNowcastFrames.ts:120 JMA_TARGET_TIMES_PATHS＋:130 jmaTargetTimesUrl(id)の1箇所へ集約された。新しい正準がどのdocsにも記載されず、次に要素を足す人が旧パターンを再導入する。同節:249のensureGradientFillLayerも実体はmakeEnsureGradientFillLayer。
- docs/modules/frontend/static-map-layers.md:16 P2 docs乖離 — 対象ファイル表がMap/primaryAttributes.tsを「一次属性⇄二次軸の双方向導出」と記すが、逆引き（axisMaterials/attrConsumers）はT308で撤去済みで、現在残るのはprimaryAttributeIdsToLayerIds（片方向）だけ。「双方向」が削除された機能の存在を前提にした表現として残っている。
- staticAttributeLayers.ts:302 P3 契約記述の陳腐化（個数を語る記述） — 「osm_raw_pois.kindは取込時にclassify_stop_poiで5値のいずれかへ分類済み」とあるが、traffic.py:31-40 StopPoiKindは現在7値でpoi-kinds.jsonも8キー。同ファイル:39の列挙も5種のまま。同:1-17のモジュール冒頭はT292で撤去済みのcar_stress専用レイヤーを現役として説明したまま。
- gradientGridFill.ts:41 P3 参照先の誤り（改名済みモジュール） — 【S6と重複】useDynamicWayValuesでgrepすると6箇所ヒットするのに実ファイルは存在しない（dedicatedWayValueLayer.ts:10・MapView.tsx:223,1382・useDebouncedValue.ts:11・useAxisValueDistribution.ts:46・useMaterialDistribution.ts:25）。改名の全消費者チェック（CLAUDE.md 機械的チェック2）がコメント本文に及んでいない。
- ui/statusDot.module.css:10 P3 語彙の不整合（新規ファイルが撤去済みの表現を再導入） — 本範囲で新設したこのファイルに「（設計原則8）」が残り、frontend/src全体で番号付き「設計原則N」を含む唯一の箇所。同じ範囲の別コミット（T671）が6ファイルから「設計原則2/8/12」を平文へ置換している最中に、移設元（LayerChip.module.css）の文言をそのままコピーして持ち込んだ。設計原則8はdocs/design-principles.mdの現行番号体系に存在しない。
- mapLayers.ts:23 P3 コメントが主張する挙動と実データの不一致 — 「bicycle_infra_qualityは地図レイヤーを持たない[show_map_icon=false]」とあるが、axis-catalog.jsonの同軸はshow_map_icon: trueかつdisplay.kind: "ramp"でbuildMapLayersがaxis:bicycle_infra_qualityレイヤーを生成しレンズから描画される。同じ誤りがprimaryAttributes.ts:86-88にもある。
- recipeControls.tsx:83 P3 コメントが主張する挙動と実装の不一致 — FieldLabelの説明が「開閉状態もこのコンポーネント自身が持つ」と述べるが開閉stateはInfoPopover.tsx:42へ移設済み。:96-98の記述もInfoPopover.tsx:48の現仕様と食い違う。InfoPopoverのtriggerAriaLabel「動詞を含めない主語」契約にテストが無い（現時点の全6呼び出しは名詞形で正しい）。
- MapOverlayControls.test.tsx:318 P3 コメントの陳腐化 — 「isAxisStudioLayer、dataNature==="composite"またはid==="windAxis"」というID集合による旧判定のコメントが残る。実装はmapLayers.ts:149-155で記述子のaxisStudioLayerフラグ判定へ変わり、同ファイル:378のテストデータ自体は正しく更新済み。次に専用way値配信軸を増やす人が「idがwindAxisかどうか」で分岐するコードを再導入しうる（T672がまさに解消した構造）。

### 一次属性・軸・材料を1件足したときにフロント側で手で書き足す必要がある対応表（網羅）
| 追加対象 | 表 | 場所 | ドリフト検知 | 導出できない理由 |
|---|---|---|---|---|
| 一次属性 | PRIMARY_ATTRIBUTE_CHIP_LABELS | primaryAttributes.ts:44 | あり（**現在失敗中**） | 4文字以下の略名はMapOverlayControls.module.css: .iconChipのチップ幅制約に由来。**ただし本番消費者ゼロで導出以前に不要の疑い** |
| 一次属性 | PRIMARY_ATTRIBUTE_LAYER_IDS | primaryAttributes.ts:70 | あり（**現在失敗中**） | 値域MapLayerIdがmapLayers.ts:36-75のフロント固有概念で、backendは自分のレイヤーIDを知らない |
| 一次属性 | PRIMARY_ATTRIBUTES_WITHOUT_LAYER | primaryAttributes.ts:93 | あり（**現在失敗中**） | 「キーが無い＝漏れ」と「意図的にレイヤー無し」を区別する明示集合 |
| 停止/補給POI種別 | STOP_POI_CATEGORIES・SUPPLY_POI_CATEGORIES | staticAttributeLayers.ts:292,318 | あり（poi-kinds.json照合） | 色とラベルはUI固有。**ただしmapLayers.ts:324の説明文だけ同期対象外で今回ずれた** |
| OSM surface/highwayタグ | SURFACE_GROUPS・HIGHWAY_GROUPS | roadFilterAxes.ts:122,172 | surfaceのみ。**highwayは検知なし** | 表示グルーピングは評価と別粒度（意図的判断） |
| 軸のアイコン形状 | AXIS_ICON_PALETTE | axisIconPalette.tsx:40 | 不要（未知idはAxisRampIconへ安全に倒れる） | SVGコンポーネントはコードのため |
| 軸の説明文 | PREFERENCE_AXIS_DESCRIPTIONS | lib/evaluationAxes.ts:53 | **なし** | 静的フォールバック専用。欠けても""になるだけで無言劣化 |
| 軸そのもの | — | — | — | **手書き不要**（mapLayers.test.ts:47-61が3件目の専用way値配信軸の自動追従を検証。原則1・13は保たれている） |

### 確認結果（指摘ではない）
宣言的カタログ5系統への集約は本範囲で崩れていない。新設のFIXED_LENS_LABELS（routeStyleModes.ts:188）は
NONE_MODE.label/DIFFICULTY_MODE.labelからの導出でRecordリテラルの対訳表の新設に当たらない。
GRADIENT_AXIS_ID（gradientGridFill.ts:18）も軸id直書きの散在を1箇所へ集約する方向の変更で原則違反ではない。


## シャード S8: frontend/src/components/AxisStudio
※今回範囲の追加行に経緯コメントの混入は無い（diffの追加行をgrepで確認済み）。

### overall
- **scoreDistribution.ts:56-70 P1 契約不一致/誤情報** — scoreBandsがbinsの値域を一切検証せず階級中央値へ折れ点を当てるため、生値が負になる軸ではbackendの潰れた1階級をそのまま「全量が特定の得点帯」と描き、distributionWarningsが断定的な誤った助言を出す。実データ検証: openness（terms全てweight -1、生値-160〜0）はquantiles p10=-144…p99=-2、bins=[(0.0,0.0167,1.0)]のみ。フロントはこの1本を中央値0.00835としてscoreForValueへ渡し（:60）、折れ点[[-100,0],[-80,30],[-20,100]]の右端クランプで100点→「100点 100.0%」を描画。さらにdistributionWarnings（:81-90）が「延長の100%が満点に張り付きます。上限を高くしないと道の差が出ません。」を出す。**実際のp50=-79は約31点であり、助言に従って上限を上げると軸が壊れる。** bicycle_infra_quality（生値-4〜0）も同じ。しかも同一パネル内の「材料の合成値: p10=-144…」（DistributionPreview.tsx:52-57）が負値を出しており、画面上で自己矛盾した2つの数字が並ぶ。フロント側は`quantiles.p10 < bins[0][0]`を見れば検知でき、その場合は帯・警告を出さず「この設定では分布を表示できません」に倒すべき。
- DistributionPreview.tsx:36-40 P2 契約不一致（空状態の欠落） — sample_ways=0（Way単位で値を持たない材料の軸）を「分布あり」として扱い、全帯0.0%のバーと「0本（0km）」という無意味なパネルを出す。way_scalar_materialsはルート文脈が要る材料（gradient_percent・wind_drag_ratio）を常にNoneで返す（axis_inspector.py:120-122）ため、公開軸のgradient・windを編集で開くと発生。分岐は`!distribution`（null）でしか空状態にならない。backend側docsはこの状況の存在を認めているのにUI側に説明が無い。
- AxisComposer.tsx:690-698 P2 無駄なI/O・ログ汚染 — 「かけあわせ評価」（他軸参照）でも分布取得を有効化するが、DistributionPreviewはshapeKind === "breakpoint_linear"分岐の内側（:1227/:1330）にしか無く応答は必ず捨てられる。terms編集のたびにPOST /api/admin/axis-definitions/preview-distributionが飛び（初回はWay抽選で1秒前後）、backendは全Wayでmaterials.get("car_stress")→None→required欠損→0件を返す。さらにMaterialRangeHint（:1174）はtermOptionsが軸一覧の場合もterm.material（=axis_id）でGET /api/admin/material-catalog/{id}/distributionを呼び、is_known_materialが偽で404。request_log.py: _access_levelはstatus_code >= 400をWARNINGにするため、**かけあわせ軸を開くたび軸の数だけWARNINGが本番ログに残る**。
- scoreDistribution.ts:8-15 P2 重複（正準定義の複製） — OpenAPI生成型ValueDistributionResponse（api.d.ts:1988、bins: [number,number,number][]まで同形）が存在するのにレスポンス形を手書きinterfaceで複製（MaterialDistributionもaxisPreviewApi.ts:14で手書き）。他の管理APIクライアントは@/types/route経由の生成型を使う。**生成物ドリフト検知の外に出る**ため、backendが型を変えても静的にもCIにも引っかからず実行時にundefinedを読むまで気づけない。
- services/axisPreviewApi.ts:20-34 P2 重複（骨格の写経） — lib/fetchJson.tsが集約している「fetch→通信エラーcatch→ok確認→detail解析→debugLog」の骨格を独自のgetJsonで作り直しており、fetchJsonが持つ開始ログ・x-request-id・タイムアウト種別の判別・parse失敗の文言分岐を欠く。fetchJson.ts:4-12は「骨格を各クライアントへ写経すると片方だけ改良された非対称が静かに生まれる」と明記している。分布APIが失敗したとき、x-request-id（backendログとの突き合わせキー）とタイムアウト/通信エラーの区別がこの経路だけ残らない。

### consistency
- **AxisStudio.tsx:104-108 P2 契約不一致（stale closure）/テスト欠落** — 「調整する」からの保存が**成功して再公開された後**にも、closeComposer()が古いrepublishAxisIdを見て「下書きのまま残った」という誤った警告を必ず出す。handleSaveとcloseComposerは同一レンダーのスコープで定義され、どちらも当該レンダーのrepublishAxisIdを捕捉する。:105のsetRepublishAxisId(null)は捕捉済みのローカル束縛を書き換えないため:108のcloseComposer()内の判定（:84）は依然として非nullとなり、noticeに「「調整する」で下書きへ戻したまま編集を終えました。この軸は一般ユーザーには表示されません。」が設定される。運用者は再公開成功直後に「本番から軸が消えた」と読む通知をstyles.errorText（エラーの見た目、:204）で受け取り無駄な復旧作業へ向かう。AxisStudio.adjustPublished.test.tsxは「開く」「中断する」の2本のみで保存成功パスを踏んでいない。
- AxisStudio.tsx:117-120 P2 契約不一致（失敗時の縮退） — reload()が失敗してもhandleAdjustPublishedのcatchは発火しない（reloadが内部でcatchしてlistErrorにするため）ので、DB上は非公開化済みなのに一覧は公開済みのままとなり「調整する」が黙って「表示だけ編集」（制限モード）に化ける。ユーザーは材料・折れ点を編集できないまま「更新する」を押し、再公開はされるが目的だった調整は一切行われていない。失敗の説明は上部のlistErrorにしか出ない。
- docs/modules/frontend/axis-studio.md:289-291 P2 docs乖離 — 今回の書き換えで導入した説明が実在しないシンボル名2つ（EDITED_PAYLOAD_KEYS・_AllPayloadKeysCovered）を指す。実装にあるのは型エイリアスEditedPayloadKey（AxisComposer.tsx:382）と_PayloadKeyCoverage（同:420）で、「EDITED_PAYLOAD_KEYSが持ち」という記述はランタイムの配列の存在を示唆する点でも誤り。
- docs/modules/frontend/axis-studio.md:255-258 P2 docs乖離 — 「ポップオーバーのCSS自体もrecipeControls.module.cssを流用し二重定義しない」が実装と一致しない（同じ誤りが:217にも）。今回AxisComposer.tsx:22-23・MaterialCoveragePanel.tsx:7-8は@/components/ui/infoButton.module.css・floatingPopover.module.cssを直接importする形へ移った。同じ段落の直前2文は今回更新されているのにこの1文だけ残っている。
- **AxisComposer.test.tsx:24-28 P2 テスト方針違反（実HTTP）** — AxisComposerを描画する5つのテストファイルのいずれも@/services/axisPreviewApiをモックしておらず、T686で追加された2つのフックが実fetchを発火する。ファイル冒頭が自ら掲げる「実HTTPは呼ばない」方針と矛盾。useAxisValueDistributionはコンポーネント冒頭（:694）で無条件に呼ばれ既定draftはbreakpoint_linearなのでenabled=true。MAP_FETCH_DEBOUNCE_MS=500を超えるテストでfetch("/admin/api/axis-definitions/preview-distribution")がhappy-domの既定オリジンへ実際に投げられ、AbortSignal.timeout(60000)のタイマーも張られる。モックしているのはAxisStudio.adjustPublished.test.tsx:20-23のみ。
- docs/modules/frontend/axis-studio.md:35 P3 docs乖離 — 鮮度パネルの説明が対象テーブルを3つと列挙したままでway_landcoverが抜けている（backendのGENERATION_FRESHNESS_SPECSは4テーブル）。:233-236の「algorithm_versionはedge/wayのみ」も古い。今回DerivedDataFreshnessPanel.tsx:51-57の説明文は「テーブルを列挙せずbackendのspecに従う」形へ書き換えられたのにdocs側は3テーブル列挙のまま。
- MaterialRangeHint.tsx:12-19 P3 docs乖離/UI密度 — docsは「材料選択行の下に出す1行表示」と述べるが、実装は.termRow（display:flex; flex-wrap:wrap）の子としてⓘボタンと係数スライダーの**間**にインライン配置（AxisComposer.tsx:1174-1177）。.quantilesはmargin:0の<p>で、狭幅では「select / ⓘ / 実データ: p50=… / スライダー / 数値 / 必須 / ⓘ / 削除」が不規則に折り返す。
- MaterialCoveragePanel.tsx:42-43 P3 契約不一致（コメントの主張が実装より強い） — 追加コメントは「Recordで全semanticsを要求する（…黙って消える）」と経路を塞いだと読めるが、missing_semanticsがnullの経路は今も塞がっていない。生成型はnull許容で、:155のfilterはnullをどのグループにも入れないため、excluded_reason===nullかつmissing_semantics===nullの材料は表にも「集計対象外」のdetailsにも現れず消える。現状はbackend側の暗黙の対で成立しているだけ。
- MaterialRangeHint.tsx:23 P3 契約不一致（語と定義のずれ） — 「ゼロX%」と表示しているがbackendのzero_shareはv <= 0の延長割合。負値を取りうる数値材料を配線した時点で下り勾配の道が「ゼロ」として数えられる。
- AxisComposer.test.tsx:360-388 P3 重複テスト — 今回追加した:389「素通しフィールドがすべて…」が:360のpriority_overrides単体テストを完全に包含する。
- AxisStudio.adjustPublished.test.tsx:1 P3 テスト（理由なき環境固定） — `// @vitest-environment jsdom`が理由の説明なしに置かれている。唯一の先例AxisStudio.test.tsx:1-3は「happy-domがwindow.confirmを持たないため」と理由を明記。本ファイルはwindow.confirmを使わず理由も無い。vitest.config.mtsのコメント（T329）はjsdom→happy-domで約35%短縮と記録。

### 規模判定: (b) 抽出・分割を提案する
閾値1,750に対し今回1,793行（前回1,741、+52、超過43）。増分の内訳は①PASSTHROUGH_PAYLOAD_KEYS＋
カバレッジ型ブロックの新設（+約65行）、②分布プレビューの配線（+約17行）、③InfoPopover共通化による削減（-約12行）。

| 領域 | 行数 | 変更理由 |
|---|---:|---|
| import/ShapeKindカード定義/情報アイコン薄ラッパ | 110 (1-110) | UI語彙 |
| NumberField・SliderNumberField | 78 (112-189) | 数値入力の入力体験 |
| toBreakpointX・niceTicks | 18 (191-208) | 折れ点の座標変換 |
| BreakpointCurveEditor（SVG・ドラッグ・キー操作） | 136 (210-345) | 図の描画・入力デバイス対応 |
| generateAxisId | 16 (347-362) | id採番 |
| Draft型・payloadカバレッジ型・変換群 | 265 (364-628) | **backendのpayloadスキーマ変更** |
| コンポーネント本体 | 1,136 (658-1793) | フォーム項目の増減 |

少なくとも4つの独立した変更理由が同居し、**今回の純増は「フォーム項目の増減」ではなく「payload往復変換」由来**。
同一ディレクトリには既に「DOM非依存の純ロジックは別ファイルへ」（breakpointTools.ts・scoreDistribution.ts）
「独立したパネルは別コンポーネントへ」（DistributionPreview.tsx・MaterialRangeHint.tsx）という前例があり、
抽出は新方針の導入ではなく既存方針の適用。

提案:
1. `BreakpointCurveEditor`（136行）→ 別ファイル。propsだけで閉じておりAxisComposerのstateへ触れていない。
2. Draft⇔payload変換群とPASSTHROUGH_PAYLOAD_KEYS/カバレッジ型（265行）→ `axisDraft.ts`。
   DOM非依存の純関数群で、コンポーネントを起動せず直接テストできる副次効果もある。
3. （任意）NumberField/SliderNumberField（78行）→ ui/寄りの共有部品。ただしUXが
   RideConditionBar.tsx:150-162の別解と異なるため統合は要検討で、分割の必須要件には含めない。

1と2で約400行が外へ出て1,400行前後になり閾値1,750に十分な余裕が生まれる。
**閾値引き上げを推さない理由**: 超過分が「フォームUIの必然的な成長」ではなく別レイヤー由来であり、
churn継続を理由とした前回のKEEP（T355）の前提——分割しても直後に別理由で書き換わる——が今回の増分には当てはまらない。


## シャード S9: frontend/src/components（Map・AxisStudio以外、42ファイル・8,150行）

### overall
- **RouteAxisProfile/axisRawValue.ts:34-38 P1 契約不一致/表示精度** — formatNumberは絶対値1未満でtoFixed(2)を使うが、backendはaxis_raw_valuesをdistance_weighted_difficulty（difficulty.py:84のround(total,1)）経由で集約するため小数1桁でしか配信されず、2桁目は常に0、0.05未満はすべて0.0へ潰れる。公開軸accident（raw_value_unit="件/(km・年)"、代表値は少ない=0.02/普通=0.1/多い=0.3）で実際の事故密度0.04（32kmで約1.3件）のルートを開くと「0.00件/(km・年)」と表示され、単位が/kmで終わらないため実数の併記（totalUnitFor）も出ず、事故ゼロのルートと完全に区別できない。**得点側と同じ丸め関数を使い回したことが原因。**
- RouteAxisProfile.module.css:82-89 P2 重複/UI不整合/コントラスト — 「未使用」バッジのスタイルがLensControl.module.css:155-162とほぼバイト単位で重複し、本差分でRouteAxisProfile側だけfont-size 0.68→0.65rem・padding変更で食い違った。両方が参照する--color-warning-softはglobals.cssに定義が無く（フォールバックのrgba直値が2ファイルへ複製）、--color-warning-strong(#b45309)は:rootのみでダークの@mediaに再定義が無い。ダークテーマでパネル面(#2a2a2d)上のコントラスト比約2.85:1（AA 4.5:1未満、0.65remの小文字）。**フォールバック付き参照のためT675の未定義トークン検知をすり抜けている。**
- MapOverlayControls.module.css:523-541 P2 重複 — .visibilityInfoButtonがui/roundIconButton.module.cssと同じ宣言をサイズ違いで手書き。同ファイルの.expandToggle(:352)はcomposes済み。
- MapOverlayControls.module.css:490-545 P2 重複/コメントと実装の不一致 — .visibilityCheckbox系が共有ui/Checkbox/Checkbox.tsxと同一の見た目仕様をCSSで再実装。正当化コメント「MapOverlayControls全体がネイティブフォームコントロールを使わずbutton+aria-pressedで統一」は、同ファイルのMapOverlayControls.tsx:269が既に共有Checkbox（Radix）を使っているため成立していない。同じパネル内に2種類のチェックボックスが並ぶ。
- LensControl.tsx:155-162 P3 UI重複/語彙不統一 — <input type="checkbox">を直接使う。リポジトリ全体でネイティブcheckboxを残す唯一の箇所（他5ファイルはui/Checkbox）。
- RideConditionBar.module.css:28 P3 未定義トークン — var(--shadow-md, ...)の--shadow-mdはglobals.cssに定義が無い（定義は--shadow-floatのみ）。フォールバック付きのため検知対象外で残った。出発時刻ポップオーバーだけ他の浮きパネルと異なる影。
- LensControl.module.css:119 P3 未定義トークン/値の不一致 — var(--font-size-xs, 0.72rem)は未定義で、同トークン名がAxisStudioの2ファイルでは0.75remという別のフォールバック値。**参照3箇所・定義0件・実効値2種。**
- RouteSettingsPanel.tsx:301 P2 既定値の二重定義 — 本差分でDEFAULT_HARD_FILTERS(:41-46)を生成物route-generate-config.jsonから導出する形に直したのに、hardFilterCustomized(:301)と表示側on={hardFilters[key] ?? true}(:427)・トグル(:429)は既定値をtrue固定でハードコードしたまま。backendが既定OFFのフィルタを1つ追加すると、何も操作していないのに「変更あり」バッジが出てDisclosureが既定で開く。「backendが正」と宣言したコメント(:27-30)と食い違う。
- BackendLogsPanel.tsx:48-59 P2 契約不一致/例外未捕捉 — 追加された.catch()はPromiseの拒否しか捕まえないが、Clipboard APIは[SecureContext]のため非セキュアコンテキストではnavigator.clipboard自体がundefinedになり同期TypeErrorを投げcatchに入らない。**コメントが対策したと述べる当のケースでボタンが無反応になる。** またcatchの書き込み先はerror stateで:117が「取得失敗: {error}」として描画するため、権限拒否時も「ログ取得が失敗した」と読める。
- ComparisonPanel.tsx:78-88 P3 新フィールドの消費者漏れ — T687でRouteCandidate.axis_raw_valuesが追加されRouteAxisProfileは表示するようになったが、研究モードの実験比較表はaxis_difficulties（0-100の得点）だけを表示し、自身のコメント(:70「個別軸の生値行」)とdocs:218も得点行を「生値行」と呼び続けている。テストfixtureにはaxis_raw_values: {}が足されただけでコンポーネントは読んでいない。**実験間で軸の効き方を比べる唯一の面が相対評価だけになっている。**
- WeatherPanel/weatherCode.ts:109-116 P3 UI語彙の重複と食い違い — CATEGORY_LABEL/ICON_BY_CATEGORY/get*DisplayがamedasWeatherIcon.ts:32-58とほぼ同一なうえ、同じ"clear"カテゴリ・同じSunIconに対してラベルが「快晴」と「晴れ」で食い違う。ヘッダー（アメダス実測）が「晴れ」、真下の「今日の見通し」（MSM予報）が同じアイコンで「快晴」と出る。
- RouteSettingsPanel.tsx:41 P3 責務配置の非対称 — 生成物由来の定数DEFAULT_HARD_FILTERSをUIコンポーネントからexportし、page.tsx・lib/hardFilterSync.tsが消費。対になるroute_preference側の既定値はlib/evaluationAxes.tsにあり同期ロジックもlib/に揃っている。

### consistency
- docs/modules/frontend/route-settings-and-results.md:136-137 P2 docs乖離 — 「呼び出し側（page.tsx）はaxesをルート設定の重み>0の軸のみへ絞り込んで渡す」と書かれているが、page.tsx:1718はaxes={axisCatalog.axes}（公開軸すべて）。同ドキュメントの直後の記述とも矛盾する。この記述を信じて絞り込むと「未使用」バッジが構造的に一度も表示されなくなり、design-principles.md UI仕様「消さずに薄くする」が破れる。
- docs/architecture.md:649 P2 docs乖離 — RouteSettingsPanelの説明が「軸ごとのチェックボックス＋重みスライダー」「各軸の行末尾に『この条件で地図を色分け』トグル（renderMapColorToggle）」と現状として書くが、renderMapColorToggleは実装に存在せず（レンズの入口はLensControlのみ）、個別重みスライダーも撤去済み（調整手段は帯グラフのドラッグ・矢印キーのみ）。同:2419-2421の「ルート確定後: パラメータ指定UIは終了する」も、page.tsx:2015-2017がTravelBearingControlをhasDetailに関係なく常時描画している事実と矛盾。
- MapOverlayControls.tsx:1016 P3 死んだ参照 — closeGroupLegendのコメントが「上のrenderGroupLegendToggle」を指すが存在せずrenderVisibilitySettings(:910)へ改名済み。MapOverlayControls.module.css:129も同じ旧名。
- MapOverlayControls.tsx:1071 P3 削除済み概念の残存参照 — 「評価軸グループと同じ理由（上のコメント参照）」が:1071/:1092/:1094に3回出るが、評価軸グループはT418で撤去済みで参照先の「上のコメント」自体が存在しない。「観測グループ」（:542/:547/:825/:827）「推定グループ」（:177/:759/:829/:919）も、現在は道路/環境/スポットの3グループしか無い分類の旧名で語り続けている。
- MapLayersPanel.tsx:305-306 P3 コメントと実装の不一致（**本差分の追加行**） — 「それ以外は標準構成（panelHint＋OFF案内＋絞り込み軸）で足りる」と書くが、renderStandardSectionBody(:273-281)はpanelHintを一切描画しない（panelHintを描くのはMapOverlayControls.tsx:1116のみ）。
- **RouteAxisProfile/axisRawValue.test.ts:30 P2 実質的に意味のないテスト/テスト欠落** — formatAxisRawValue(0.08, "回/km", null)→"0.08回/km"等は、backendが小数1桁へ丸めて配信するため**実運用で到達しない入力**に対する期待値を固定している。逆に「配信される精度で何が表示されるか」（0.04→0.0→"0.00"）を押さえたテストは無い。2桁分岐が実データでは常に末尾0を出す問題が、テスト全greenのため検知されない。
- RouteSettingsPanel.test.tsx:26-30 P3 デッドコード（テストfixture）/死んだ参照 — catalogResponseの第2・第3引数はファイル内8箇所すべての呼び出しが第1引数のみを渡すため一度も使われていない。追加理由として:21/:45のコメントが挙げるrenderMapColorToggleは実装から消えている。
- MapOverlayControls.tsx:918-921 P3 コメントと実装の不一致 — items.layerIdについて「推定グループの専用レイヤーを持たない軸（勾配・舗装質・夜間）はundefinedのまま渡す」とあるが、唯一の呼び出し元(:1110-1117)は常にlayerId: member.idを渡す。layerId?のoptional性・toggleHidden(:712)のガードは到達不能な分岐。挙げられている軸は既に地図UIから撤去済み。
- WeatherPanel/weatherCode.ts:73-77 P3 手動同期ペアのドリフト検知欠落 — 本差分の追加コメントが「backendが実際に返すのは0/1/2/3・61/63/65・71/73/75の10値だけ（domain/weather.py: derive_weather_code）」と具体的な値集合を契約として宣言するが、突き合わせテストが無い（weatherCode.test.tsが存在しない）。同じ二重実装でもWindBearingSlider.test.ts⇔backend/tests/test_geo.pyのcompass_labelは両側に境界値表を持つ。
- docs/modules/frontend/route-settings-and-results.md:218 P3 docs乖離（語彙衝突） — ComparisonPanelの説明がaxis_difficulties（得点）から作る行を「個別軸の生値行」と呼ぶが、同ドキュメント:137以降でT687が導入した「生値」はaxis_raw_values（物理量）を指す。**同一ファイル内で「生値」が2つの異なる値を指している。**

### 確認結果（指摘ではない）
T675の未定義トークン是正はフォールバック無し参照については本シャード内で違反0件。
T676のcomposes集約は新規6ファイルすべてdocs/modules/frontend/page-composition.md:22に記載済みで、
z-indexスケールもdocs/frontend-design-system.md:37-55に現状文書として反映されている。
T629は`.claude`のトリガー（MapLayersPanel再編）が未発火のため未着手のままで問題なし。
ソースコード追加行への経緯コメント混入は本シャードでは0件。


## シャード S10: frontend/src/{hooks,services,lib,types}（56ファイル）

### overall
- **lib/evaluationAxes.ts:17 P1 正準定義の複製/生成物の陳腐化** — 【F-B・S5と同一根本原因】静的生成物axis-catalog.jsonが本番DB（コミット済みスナップショット revision 64）と乖離し、DEFAULT_ROUTE_PREFERENCE・PREFERENCE_AXES・RAMP_AXES・AXIS_LABELS・SECONDARY_AXES・ROUTE_STYLE_MODESという**6系統のフォールバックが揃って「存在しない軸を含み、公開済みの2軸を欠く」**。静的側のpreference_defaultsは9キー{gradient .15, wind .26, surface_q .19, stop_density .2, car_stress .2, accident .08, night 0, bicycle_infra_quality .15, openness 0}。スナップショット側の公開軸は10件でstop_densityはaxis_abdaded0be20（重み.2）へ置換済み・curvature（重み.1）が追加済み（772a9189／509f0210。どちらも生成物を再生成していない）。フォールバック中は旧stop_densityのramp（tile_inputs=stop_per_km×1.0 + intersection_per_km×0.3, thresholds=[1,2,4]）が地図レンズ／推定指標チップに現れるが、現行のaxis_abdaded0be20は材料も折れ点も帯ラベルも全く別物（poi_signal_per_km×1.0 + poi_level_crossing_per_km×1.5 + poi_stop_per_km×0.3, display_thresholds_override=[2,4,7,12], 距離換算ラベル付き）のため、色分けが現在の評価と一致しない。
- **lib/evaluationAxes.ts:53-64 P1 設計原則違反（構造仕様2・3）** — PREFERENCE_AXIS_DESCRIPTIONSが軸id→説明文の手書き辞書として残り、しかもevaluationAxes.test.ts:79-82が「全軸が説明文を持つ」ことを強制しているため、軸スタジオでGUI作成した軸を追加するとfrontendのコード変更が必須になる。**上のドリフトを直してaxis-catalog.jsonを再生成すると、PREFERENCE_AXESにaxis_abdaded0be20・curvatureが入り、いずれもキーが無いためdescription===""となりテストが即座に落ちる**——「生成物を正しく更新するとテストが赤になり、赤を消すには軸ごとの説明文をfrontendへ手書きする」という、原則2を強制的に破らせる構造。説明文自体はbackendがAxisCatalogEntry.descriptionとして配信しており（useAxisCatalog.ts:126の実行時経路は正しくそれを使う）、フォールバック側だけがコピーを持っている。
- hooks/useAxisCatalog.ts:214-229 P2 契約不一致/サイレント劣化 — 軸カタログの実行時フェッチはマウント時1回きりで、失敗しても再試行せずloaded=falseのまま固定される。CATALOG_API_TIMEOUT_MS=10000に対しbackendのコールドスタートが10秒を超えるとrejectし、catch(:224-228)は共有ストアを書き換えないためloadedは永久にfalse。RouteSettingsPanelはフォールバック軸で通常どおり重みスライダーを描画しユーザーは編集できるが、生成時はroute_preferenceが省略されbackendの既定重みで探索される。**画面には失敗の表示も「既定値で生成しました」の表示も一切出ない。評価モデルの研究が主目的の現フェーズでは、実験結果を取り違える形で効く。**
- app/page.tsx:1347 P2 兄弟フィールド間のバリデーション非対称 — すぐ隣のroutePreference(:1356-1358)はaxisCatalog.loadedで未確定状態を除外しているのに、同じ未検証カタログ由来のlensAxisIdにはガードが無く、存在しない軸idがそのままリクエストへ載る。backend側はroad_graph_engine.py:1657のAXIS_DEFINITIONS.get(lens_axis_id)がNoneを返して黙って無視する（422にはならない）ため、地図が「選んだ軸で塗られない」状態になるが手掛かりが残らない。useAxisCatalog.ts:51-57のdocstringは「loadedで未確定状態を区別しなければならない」と明記しており、実装がその契約を片側でしか満たしていない。
- services/healthApi.ts:6 P2 正準定義の複製（取り残された消費者） — lib/apiTimeouts.ts（T676が新設）が「7ファイル・14箇所の直書きを全部これへ寄せた」と述べるが、AbortSignal.timeout(5000)が1箇所残っている。値も用途もSTATUS_API_TIMEOUT_MSと完全一致。CLAUDE.md機械的チェック2の実施漏れ。
- **hooks/useDedicatedWayValues.ts:120,174 P2 スケールしないパターン（デバウンス非対称）** — 走行方位・想定速度はドラッグ連続発火を理由にデバウンスしているのに、同じくスライダーで連続変化する`at`（出発時刻）だけ生値のまま依存配列へ入っており、時刻スライダーの操作1回ごとに可視タイル全件の再フェッチが走る。RideConditionBar.tsx:108-116のDynamicLayerTimeSliderはselectイベント（コマを跨いだ瞬間）ごとに発火するため、15時間ぶんを端から端までドラッグすると数十回発火。レンズ=風のときrequestKey(:70-79)が変わりtilesCoveringViewportが返すタイル数ぶんのGETが新規発行される。seqガード(:151)は結果を捨てるだけでリクエスト自体は全部backendへ届く。**:27-29のコメントが「そのまま依存配列へ入れるとドラッグ1回で可視タイル数×連続イベント数ぶんのfetchが発生してしまう」と書いている当の問題が、`at`についてだけ未対処。**
- services/axisPreviewApi.ts:151-169 P3 重複（共通骨格の写経） — 【S8と重複】T679がlib/fetchJson.tsへ集約した骨格を新設getJsonが独自に持ち直し、x-request-idのログだけが欠けている。docs/tasks/T676.md:121-123が「本タスクでは骨格の統合は触っていない」と保留を記録しているが、**docs/improvement-plan.mdへ独立した`- [ ]`行として起票されていない**（CLAUDE.md機械的チェック6）ため/task:next等の機械抽出から永久に漏れる。
- hooks/useDynamicWeatherLayers.ts:355-358 P3 契約不一致（クロージャでの時刻固定） — linearRainbandVisibleがuseMemoの中でnew Date()を評価しながら依存にdynamicLayerTargetTimeしか持たないため実時間が進んでも再評価されない。「今後3時間以内に大雨のおそれ」の3時間窓判定が、共有時刻を最後に触った瞬間のnowで固定される。画面を4時間放置すると窓を外れた予測マップが残る。**判断原則16の対象パターン（クロージャでの値固定）。**

### consistency
- hooks/useAxisValueDistribution.ts:46 P2 死んだ参照 — 本差分内で削除済みのファイルhooks/useDynamicWayValues.tsを参照するコメントが、**同じ差分内で新規追加されたファイル**に書かれている（useAxisValueDistribution.ts:46・useMaterialDistribution.ts:25はT686で新設、useDebouncedValue.ts:11は既存）。担当外だがgradientGridFill.ts:41・MapView.tsx:223,1382・dedicatedWayValueLayer.ts:10にも同じ取り残し。
- docs/architecture.md:2488-2490 P2 docs乖離（死んだ参照） — 「フェッチ本体はservices/regionApi.ts: fetchDynamicWayValues(materialId, ...)・状態管理はhooks/useDynamicWayValues.ts: useDynamicWayValues(materialId, ...)」と書くが、実装はfetchDynamicWayValues(axisId, ...)（regionApi.ts:142-150）とuseDedicatedWayValues(axes, ...)。**ファイル名・関数名・第1引数の意味（材料id→軸id、単数→配列）がすべて変わっている。**
- hooks/useLocation.ts:33,49 P3 死んだ参照 — useWeatherConditions.tsのfetchWeatherForという参照先が同じ差分内のT679で撤去されて存在しない。コミット順は74c007ce（T671、参照先を「是正」）→a353e5e3（T679、fetchWeatherForごと置換）。**是正したばかりの参照が直後に死んだ。** fetchWeatherForはリポジトリ全体でコメント内にしか存在しない（useLocation.ts:33,49・SystemStatusPanel.tsx:67・page.test.tsx:317,404,572）。
- **services/regionApi.ts:159-160 P3 経緯コメントの新規混入＋機械的検知の穴** — 本差分の追加行に「独自実装だった頃はここだけrequestIdを残さず、失敗時にサーバーログと突き合わせできなかった」という経緯記述。lib/axisMaterialsCatalog.ts:9-10にも同型（「手書きで持っていたころは、APIが落ちているときだけ選択肢が古いという気づく機会の無いドリフトが実際に発生していた」）。**review_checks.py:83-87のNARRATIVE_PATTERNは「以前は|従来は|旧…|改善計画T[0-9]+|T[0-9]{3,4}で|指摘を受け|…」で、「〜だった頃は」「〜ころは」という時制表現を含んでいないため両方とも素通りする。T656が「経緯コメント検知の穴を塞ぐ」を実施した直後の差分で、同じ穴から2件混入している。**
- **lib/evaluationAxes.test.ts:21-25 P2 テスト欠落（手動同期ペアのドリフト検知が実質不在）** — 「PREFERENCE_AXESの軸集合はpreference_defaults（生成物）と一致する」というドリフト検知テストは、比較する両辺がどちらも同じaxis-catalog.jsonから導出されているため**恒真に近く、生成物そのものの陳腐化を検知できない**。生成物とリポジトリ内の実DB像（backend/fixtures/axis_definitions_snapshot.json）を突き合わせるテストはリポジトリ内に1件も存在しない（`grep -rl axis_definitions_snapshot frontend/src`は0件）。唯一の防波堤がCIのgit diff --exit-codeだけで、それが通っていない今の状態を検知する二重チェックが無い。同ファイル:61-65の「地図と同じ並び順・同じラベルで並ぶ」テストも、PREFERENCE_AXESがSECONDARY_AXES.map(...)そのものである以上、実装をなぞるだけで失敗しえない。
- components/Map/secondaryAxes.ts:67-69 P3 契約不一致（宣言されるが誰も設定しないフィールド） — 【S7と重複、F-Iで統合】T687で追加されたrawValueUnitを唯一の生成関数secondaryAxesFromCatalogAxes(:110-131)が一切セットせず、上流のuseAxisCatalog.ts: toCatalogAxis(:76-114)もraw_value_unitをCatalogAxisへ写していない。**export_openapi.py:196-227はraw_value_unitをそもそもaxis-catalog.jsonへ書き出していないため、生成物を再生成しても値は入らない。**「単位が定まる軸だけが持つ」という型ドキュメントが、実装上は「どの軸も持たない」。
- services/regionApi.test.ts:140,161,176 P3 削除・改名された事実を語る周辺表現の残存 — T672が第1引数をmaterialId→axisIdへ改めたのに、テストのタイトルが旧語彙のまま（「material_id・z/x/y・bearing_degを含むURLへGETし」「material_idが変わればパスも変わる（改善計画T423、材料id駆動のエンドポイント統一）」）。実際のパスは/api/region/dynamic-way-values/{axis_id}/でテスト本体は"wind"/"gradient"という軸idを渡している。
- hooks/useAxisCatalog.test.ts:134,202 P3 陳腐化した個数表現 — テスト名・コメントが静的フォールバックを「既存7軸」「静的フォールバックの7軸」と述べるが、現在のaxis-catalog.jsonは9軸。テスト本体はlength > 0しか見ないため何軸になっても永久に緑。
- hooks/usePolledFetch.ts:13-16,45,63-66 P3 契約不一致（docstringと実装のずれ） — hasFetchedのdocstringは「enabled: falseの間はfalseのまま」と述べるが、実装は一度trueになった後enabledがfalseへ戻ってもリセットしない。レイヤーをONにして取得完了→OFFにすると、deriveFetchLayerStatusがこれを「取りに行った結果、値が無かった」として扱うため、レイヤーを消しただけの状態が「データなし」と表示されうる。

### 取り残しの確認結果（重点指定分）
- **T667（localStorage保護）**: 取り残しなし。window.localStorageを触るのはlib/safeStorage.ts・hooks/useStoredState.ts:59,78・app/page.tsx:585,597,599の3系統だけでいずれもtry/catch内。
- **T662（生成条件のdirty判定）**: 取り残しなし。stableStringifyによりキー順差の誤検知も塞がれ、generationRequest.test.tsが両方向を固定している。
- **T679（fetch骨格・ポーリング・Popover）**: regionApi.ts・debugAdminApi.ts・axisAdminApi.ts・routeApi.tsは全てlib/fetchJson.tsの骨格へ寄っており、useWeatherConditionsの連番ガードはuseLocationFetchへ、useWeatherGridの粗格子はusePolledFetchへ集約済み。**取り残しはaxisPreviewApi.ts（骨格）とhealthApi.ts（タイムアウト定数）の2本。**
- **lib/apiTimeouts.tsの5定数**: 全てに実消費者があり、ブラウザ側クライアントとroute handlerが同じ定数を共有できている。直書きの残存はhealthApi.ts:6の1件のみ（jmaTileProtocol.ts:58はMapLibreプロトコル側のabortControllerで別途中断される設計のため対象外）。


## シャード S11: frontend/src/app + docs/

### overall
- **page.tsx:62 P2 設計原則違反/重複** — 同一コミット範囲でdocs/frontend-design-system.mdへ新設された「別コンポーネントの*.module.cssを直接importして借りない」規約に、page.tsx自身が唯一違反したまま残り、しかも「問題なく使える」と正当化するコメントが付いている。:62が@/components/MapLayersPanel/MapLayersPanel.module.cssをimportし:1523でlayerPanelStyles.groupを使う。規約（frontend-design-system.md:100-103）は「CSS Modulesは存在しないクラス名にundefinedを返すため、貸し手側の改名が無スタイルのまま本番へ出る（tscもlintも止めない）」と述べる。components/ui/*を借りる他2ファイルは規約が認める共有先で、違反はこの1件のみ。
- global-error.tsx:28 P3 双子実装の非対称 — error.tsxは本diffで#666→var(--color-muted)へトークン化されたが、対のglobal-error.tsxは#666のまま。global-errorはglobals.cssを読まないためトークン化できないという事情がありうるが、その理由がどちらのファイルにも書かれていない。
- page.tsx:193-226 P3 重複/残骸 — FIXED_LAYER_VISIBILITY_DEFAULTSとDEFAULT_LAYER_VISIBILITYが本diffで完全に同値（{...FIXED_...}）になったのに2定数のまま残り、前者は後者の定義でしか使われない。
- app/admin/api/material-distribution/[materialId]/route.ts P3 テスト欠落/非対称 — 本diffで追加された2本のRoute Handlerだけがテストを持たず、既存7本は全てroute.test.tsを持つ。material-distributionはencodeURIComponent＋DISTRIBUTION_API_TIMEOUT_MSという検証対象を持ち（兄弟のmaterial-values/[materialId]/route.test.tsはまさにこの2点を固定）、パス組み立てを壊してもテストは緑のまま。

### consistency
- **page.tsx:866 P2 契約不一致（コメントと実装が逆）** — 「軸スタジオ由来のレイヤーだけは1つだけ選べる状態を保つ」というコメントが、同じdiffで排他ロジックごと削除されたhandleLayerToggleの直上に残り、しかも**本diffの追加行として書き換えられている**（windAxis・gradientAxis→専用way値配信軸）。現行のhandleLayerToggleはsetLayerVisibility(prev => ({...prev, [id]: on}))のみで排他判定を持たない（対応テスト2件も本diffで削除済み）。docs/modules/frontend/static-map-layers.md:157にも同じ主張が残っており（こちらも本diffで文言だけ更新）、コードとdocsの両方が消えた機構を語っている。
- page.tsx:840 P3 契約不一致 — mapLayersの宣言位置の根拠を「handleLayerToggle（直下）が排他ドメイン判定のためmapLayers全体を参照するため」と説明するが、現行のhandleLayerToggleはmapLayersを一切参照せず依存配列からも外れている。
- docs/modules/frontend/page-composition.md:144 P2 docs乖離 — useStoredStateのreloadKeyの説明例が「layerVisibilityは2段階復元」のままだが、本diffでdeserializeは常に{...DEFAULT_LAYER_VISIBILITY}だけを走査する形へ変わっている。page.tsx側のコメント（:546-548）は「キー集合自体はカタログに依存しない」と正しく書いており、**docsと実装とコード内コメントが三者で食い違う**。
- globals.css:106 P3 個数表現の不一致 — 本diffで新設したz-indexスケールのコメントが「画面全体で重なり合う層はここに列挙した6段しか無い」と述べるが、直後のトークンは7つ（20/25/30/45/46/50/60）。docs/frontend-design-system.md側は「7段」と正しく書いており、どちらが正か読み手が判断できない。
- docs/modules/backend/elevation.md:86 P2 docs乖離（削除済み機構の記述） — 「本バッチが読むgeometryはuse_cache=FalseでRedis cache-asideを迂回する」が残るが、use_cache引数はコードに存在せず、edge geometryのRedis cache-aside自体がT652で撤去済み。同じ主張が:119の図中にも二重に残る。
- docs/modules/frontend/route-settings-and-results.md:225 P2 記載粒度違反＋dead symbol — 「旧・風スコア/舗装率の固定行（RouteCandidate.wind_score/road_score直接参照）は材料値の生値行へ置き換えた」という一文が、README「記載粒度」が禁じる経緯記述であり、かつwind_score/road_scoreはbackend・frontend双方に1件も存在しない。**review_checks.py docsの記載粒度チェックは「違反なし」を返すため、この形の言い換えは機械では止まらない。**
- **docs/design-principles.md:52 P2 docs乖離（正本が存在しない機構を必須と規定）（本体で検証済み）** — 設計原則の唯一の正本が、構造仕様5で「やむを得ない場合はwind_score方式（docs明記＋識別フィールド＋両側の型コメント）を必須とする」と規定しているが、`wind_score`は`backend/app`・`frontend/src`のどこにも存在しない（grep 0件で確認）。同名フィールドの意味差を作らざるを得ない場面で参照すべき実例が消えている。構造仕様9のDEDICATED_WAY_VALUE_LAYER_IDS（T672で撤去）も同様。
  【本体での追加確認】`.claude/commands/review/context.md`のKeep Listにも「wind_scoreのエンジン間の意味差（engineフィールドで識別する管理された不整合）」が**意図的な設計判断として残っている**。context.mdは他2項目をT462で前提消滅と注記済みだが、この項目は取り残されている。
- **docs/modules/frontend/dynamic-weather-layers.md:162 P2 dead symbol/個数表現の不一致** — 「データ取得状態」節が中核として説明する純粋関数dynamicWeatherStatus(loading, error, hasPayload)は存在せず、実装はmapLayers.tsのderiveFetchLayerStatus(loading, error, hasPayload, hasFetched)（第4引数はT674で追加）。同節は:161で「3つのチップ付き動的気象レイヤー」と述べた直後、:170で同じ集合を「この9レイヤー」と数える。hasFetchedを渡し忘れた新レイヤーが「未取得」を「データなし」と断定して静かに誤表示する（まさにT674が直した欠陥）。
- **docs/modules 全15ファイルの dead symbol 10件 P3（横断）** — 実装に存在しない識別子を名指しする箇所が10件残っている。**機械チェック「docs/modules の死んだ参照」は`find_dead_file_refs`がバッククォート付き"ファイル名"のみを見るため、識別子はすべて素通りする**（本体でコード確認）。一覧:
  `backend/axis-studio.md:236` DEDICATED_WAY_VALUE_LAYER_IDS（T672で撤去）／
  `backend/elevation.md:65` graph_material_cache.py: _LRUCache（現在はcachetools.LRUCache）／
  `backend/elevation.md:86,119` use_cache／
  `backend/routing-engine.md:603` RouteScoreComponent（domain/route.pyに無し）／
  `backend/weather-dynamic-layers.md:301`・`frontend/dynamic-weather-layers.md:147` WIND_VARIABLES（実体はmsm_client.py: FORECAST_VARIABLES）／
  `frontend/axis-studio.md:289,291` EDITED_PAYLOAD_KEYS・_AllPayloadKeysCovered／
  `frontend/dynamic-weather-layers.md:157,162` dynamicWeatherStatus／
  `:249` ensureGradientFillLayer（実体はmakeEnsureGradientFillLayer）／
  `:265` riskTargetTimesUrl（実体はjmaTargetTimesUrl）。
- docs/modules/frontend/static-map-layers.md:63 P3 docs乖離（シグネチャ） — buildStaticOverlayLayers(axisOverlayLayers)と1引数で図示するが実装は4引数。すぐ下の:75は本diffで更新されており、同じ図の中で新旧が混在する。
- docs/architecture.md:1205 P3 個数表現の不一致 — 7章の見出しが「静的道路属性と**7軸**評価モデル」、配下の小見出しが「**8軸**の一覧と重み」（:2025も引用）で同一ドキュメント内で矛盾し、実データの公開軸は10本。
- docs/frontend-design-system.md:65 P3 契約の過大主張 — 「存在しないトークン名は機械的に弾く……pre-commitでブロックする」と無条件に書くが、review_checks.py: find_undefined_css_tokensは`if not f.endswith(".css"): continue`で.tsxを対象外にしている。.tsx内のvar(--…)参照は31件あり（本diffがerror.tsx:35へ追加したvar(--color-muted)も含む）どれも検査されない。現時点で未定義参照は0件だが、T675が塞いだはずの穴が同じ症状で再発しうる。

### 完了マーク済みタスクの抜き取り検証（5本）
| タスク | 判定 |
|---|---|
| T662（dirty判定を単一ソースから導出） | **成立**。buildGenerateRequest/generationConditionsKeyが同一入力を通り、除外はlens_axis_id＋max_routesのみ。旧実装で比較対象外だったstart_timeが新たに比較へ入るが、dynamicLayerTargetTimeはuseState(() => new Date())で自走しないため誤検知は生じない |
| T672（dedicated_way_value_layer軸の汎用化） | **成立**。showWindAxis/showGradientAxis/useDynamicWayValuesの軸別インスタンス化が消え、dedicatedFetchAxes＋useDedicatedWayValues＋dedicatedWayValueVisibilityの汎用Mapへ統合。残る軸id名指しはGRADIENT_AXIS_ID 1箇所（docs記載どおりの意図的例外） |
| T675（未定義CSSトークン検知） | **実装は成立**（--color-text系0件、docsチェック0件）。ただし検査範囲が.css限定である点がdocsに書かれていない（上記P3） |
| T681（設定の保存有無の統一） | **成立**。HARD_FILTERS_STORAGE_KEY定数＋useStoredState＋deserialize内syncHardFilterKeys、lib/hardFilterSync.tsと.test.tsも存在。useStoredBooleanStateへの集約でboolean 5箇所の写経も解消済み |
| T690（最短距離ルートを既定で出す） | **成立**（機能の存在として）。route_generator.py:497,511がis_shortest_distanceを立てて先頭固定、生成型・types/route.ts経由でfrontendへ到達、lib/routeTabLabel.ts＋page.tsx:1651-1661＋page.test.tsxの回帰テストまで揃う。**ただしその最短ルート自体が0次ハードフィルタを迂回している（F-K参照）** |
補足: T670（docs/modules全15ファイルの現状化）は対象13件のうち6-4/6-13/6-17を抜き取り確認し、いずれも是正済み。
ただしT670のスコープ外として上記のdead symbol 10件が残っている。


---

# 本体（レビュアー自身）が実測・自己検証したFindings

Agentの指摘のうちP1以上をコードで再確認した結果と、本体が独自に実測した項目。
統合後の結論は上の「Findings」を正とする。


### F-A【P0】CIのAPI契約ドリフト検知が、作業ディレクトリの継承により常に素通りしている
- Problem: `.github/workflows/ci.yml`の`backend`ジョブ最終ステップ
  `- run: git diff --exit-code -- frontend/src/types/generated/` が、ジョブ既定の
  `defaults.run.working-directory: backend`（ci.yml:64-66）を継承して`backend/`で実行される。
  pathspecは`backend/frontend/src/types/generated/`へ解決され、**マッチ0件＝差分なしとして常にexit 0**になる。
  直前2ステップ（`npm ci`・`npm run generate:api`）は明示的に`working-directory: frontend`を
  指定しているのに、この検証ステップだけ指定が無い。
- Evidence（実測）:
  - `.github/workflows/ci.yml:62-66`（job既定）、同`:125`（検証ステップ、working-directory指定なし）
  - 本レビューでの実測: 生成物を意図的にdirtyにした状態で
    `cd <repo>/backend && git diff --exit-code -- frontend/src/types/generated/` → **exit 0**、
    `cd <repo> && git diff --exit-code -- frontend/src/types/generated/` → exit 1。
  - master HEAD直前の実CI（run 34431803012、commit `266c065b`）で`backend`ジョブは
    conclusion=success、step 14「Run git diff --exit-code -- frontend/src/types/generated/」も
    success。**同時点で実際には下記F-Bのドリフトが存在していた。**
- Root Cause: T350（コミット`13794155`）で独立していた`api-contract`ジョブを`backend`ジョブへ統合した際、
  旧ジョブは**ステップごとに**`working-directory`を書いており検証ステップだけ無指定＝リポジトリルート
  だったが、統合先の`backend`ジョブは**ジョブ既定**で`backend`を持つため、無指定が
  「ルート」ではなく「backend」を意味するようになった。
- Impact: consistency.mdが「同種の手動同期ペアで2回以上の再発が観測された場合は、
  手順の再徹底ではなく機械的検知の追加を既定の第一候補にする」と定め、実際に収束させた
  当の検知が、**無言で機能停止していた**。生成物ドリフトを止める仕組みが1つも残っていない。
- Recommendation: 検証ステップへ`working-directory: .`（またはジョブ既定を外し各ステップへ明示）を付ける。
  あわせて「ガード自体が効いていること」を担保するため、意図的にdirtyにしたときに落ちることを
  確認する手順（またはガードの自己テスト）を持たせる。
- Scope: S（ci.yml 1行）。ただし修正すると下記F-Bが顕在化するためF-Bとセットで実施する。
- Confidence: **High**（実測で再現）

### F-B【P1】静的軸カタログ（axis-catalog.json）が本番の軸集合と乖離している
- Problem: `backend/fixtures/axis_definitions_snapshot.json`（revision 64、本番DBのダンプ）は
  公開軸10本（gradient / wind / surface_q / car_stress / accident / night /
  bicycle_infra_quality / openness / **axis_abdaded0be20**［停止密度］/ **curvature**［蛇行］）を持つが、
  `frontend/src/types/generated/axis-catalog.json`は9軸で、**旧`stop_density`を持ち
  `axis_abdaded0be20`・`curvature`を欠く**。
- Evidence（実測）: 本レビューでCIと同じ手順を再現した。
  PostgreSQL 16 + PostGIS を立て `scripts/bootstrap_ci_db.py`（スナップショット15件を投入）→
  `scripts/export_openapi.py` を実行した結果、`axis-catalog.json`に
  **224行追加 / 105行削除**の差分が出た（`git diff --stat`）。
  更新履歴: スナップショットは`772a9189`→`581586c9`→`509f0210`と3回更新されたが、
  `axis-catalog.json`の最終更新は`45ffd36b`で止まっている。
- Impact: このファイルは実行時API（`GET /api/axis-catalog`）の**取得前・失敗時の静的フォールバック**
  （`hooks/useAxisCatalog.ts: FALLBACK_CATALOG`・`lib/evaluationAxes.ts: DEFAULT_ROUTE_PREFERENCE`）。
  存在しない`stop_density`を提示し、公開済みの「蛇行」軸を欠いた状態で
  rampAxes・axisLabels・routeStyleModesが構成される。
- **影響の限定（過大評価を避けるための確認）**: `page.tsx:1352-1357`は
  「軸カタログ未取得のままキー整合を行うと静的フォールバックに合わせて書き換えてしまう」ため
  `axisCatalog.loaded`が偽なら`route_preference`自体を省略しbackendの既定値へ委ねる、という
  自己防衛を持つ（実測）。したがって**重みキーの不一致による422は起きない**。
  影響はフェッチ完了前・失敗時の**表示層**（地図レイヤー・ラベル・レンズ選択肢・重みパネル）に限られる。
  それでも本項をP1に置くのは、ドリフトそのものより**それを止めるはずのCIが空振りしていた**
  （F-A）ことが本体だからである。
- Recommendation: F-Aを直したうえで生成物を再生成しコミットする。
  「スナップショット更新時は生成物も再生成する」ペアはF-Aの検知が回復すれば機械的に守られる。
- Scope: S（再生成とコミット）
- Confidence: **High**（実測で再現）

### F-C【P1】masterのCIが5回連続で赤のまま、その上に5コミットが積まれている
- Problem: `frontend`ジョブの`npm test`が`src/components/Map/primaryAttributes.test.ts`の
  2件で失敗し続けている。原因は、T691（`45ffd36b`）が一次属性`curvature`（蛇行）を
  カタログへ追加した際、フロントの手書き対応表
  `PRIMARY_ATTRIBUTE_CHIP_LABELS`（`primaryAttributes.ts:44`）と
  `PRIMARY_ATTRIBUTE_LAYER_IDS`/`PRIMARY_ATTRIBUTES_WITHOUT_LAYER`（同:70/:93）への
  登録が漏れたこと。**ドリフト検知テストは設計どおり機能して赤を出している**。
- Evidence（実測）:
  - ローカル単独実行で再現: `npx vitest run src/components/Map/primaryAttributes.test.ts`
    → `AssertionError: curvatureの略名が無い` / `curvatureがどちらの対応表にも無い`（2 failed / 5 passed）
  - 実CI: run 859（`266c065b`）・858・857・856・855 が5回連続 conclusion=failure。
    失敗しているのは全て`frontend`ジョブの`npm test`のみで、`backend`・`e2e`はsuccess。
  - 直近の成功は run 854（`9e01a68d`）。
- Impact: 赤を前提に作業が進む状態が2日続いており、この間に入った別の回帰を検知できない。
- Recommendation: 2つの対応表へ`curvature`を登録する。
  あわせて、この2表が**一次属性を1つ増やすたびに手で書き足す必要がある**構造自体を
  （生成物からの導出で消せないか）検討する——F-Dを参照。
- Scope: S（登録）／構造の見直しはM
- Confidence: **High**（実測で再現）

### F-D【P2】「評価軸追加の1本道」に、一次属性を増やすときだけフロント手書きが残る
- Problem: 評価軸・材料の追加はコード変更0（axis_admin API 1回）で通る一方、
  **一次属性**を1つ増やすと`primaryAttributes.ts`の手書き対応表への追記が必須になる。
  T691/T692はこれを踏まずにpushされ、F-Cの赤になった。
- Evidence（本体で消費者を網羅確認）: `frontend/src/components/Map/primaryAttributes.ts`
  | 表 | 本番の消費者 | 判定 |
  |---|---|---|
  | `PRIMARY_ATTRIBUTES`（:31） | 生成物`axis-catalog.json: primary_attributes[]`から片側import | 手書きではない |
  | `PRIMARY_ATTRIBUTE_LABELS`（:36） | `axisInspectorPopup.ts:11` | 生成物から導出。手書きではない |
  | `PRIMARY_ATTRIBUTE_CHIP_LABELS`（:44） | **ゼロ**（参照は自身のテストと`secondaryAxes.ts:36`のコメントのみ） | **死んでいる** |
  | `PRIMARY_ATTRIBUTE_LAYER_IDS`（:70） | `primaryAttributeIdsToLayerIds`経由で`page.tsx:39` | 生きている |
  | `PRIMARY_ATTRIBUTES_WITHOUT_LAYER`（:93） | テストのみ | **これは設計どおり**（「漏れ」と「意図的にレイヤー無し」を区別する明示集合） |
- Inference: 手書き3表のうち、**真に不要なのは略名表だけ**。
  `PRIMARY_ATTRIBUTE_CHIP_LABELS`のdocstringは「材料一覧表示で薄字ラベルとして使うため
  全一次属性ぶんを持つ」と述べるが、その消費者は存在しない——
  **表の存在理由がdocstringの中にしか無い状態**で、一次属性を足すたびに
  「4文字以下・一意」という制約付きの更新をCIが強制している。
- Recommendation:
  1. `PRIMARY_ATTRIBUTE_CHIP_LABELS`と対応するテスト（`primaryAttributes.test.ts:17-29`）を**削除する**。
     これでF-Cの2件の失敗のうち1件が構造的に消え、以後の一次属性追加の手間も減る。
  2. 残る`PRIMARY_ATTRIBUTE_LAYER_IDS`/`PRIMARY_ATTRIBUTES_WITHOUT_LAYER`は
     **フロント固有の概念（`MapLayerId`）を値域に持つため生成物からは導出できない**。
     こちらは維持し、`curvature`を`PRIMARY_ATTRIBUTES_WITHOUT_LAYER`へ追加する
     （蛇行は材料のtile_property設定でramp軸の地図レンズを自動で得ており、
     一次属性としての専用レイヤーは持たない）。
  - **注**: レビュー中、当初は「略名をbackendの一次属性カタログへ移して片側importにする」案を
    検討したが、消費者ゼロの確認により「移設ではなく削除」が正しいと判断を改めた。
- Scope: S
- Confidence: High（消費者をgrepで網羅確認）

### F-E【P2】precompute系バッチのドライバ骨格が5本へ写されている
- Problem: `_common.py`が`batch_session_factory`/`count_targets`/`stream_id_chunks`の
  プリミティブを提供している一方、その上のドライバ
  （対象件数ログ → dry-run早期return → 0件WARNING → `total_chunks`算出 →
  `stream_id_chunks`ループ → チャンク進捗INFO）が5本のバッチへ同じ形で書かれている。
- Evidence: `precompute_edge_attribute_counts.py:67-92`・`precompute_elevation_attributes.py:63-84`・
  `precompute_way_attribute_counts.py:66-94`・`precompute_way_curvature.py:50-68`・
  `precompute_way_landcover.py:167-205` の各`run()`。jscpdもこのうち1組を
  20行のクローンとして検出（`precompute_way_attribute_counts.py:62` ⇔ `precompute_way_curvature.py:47`、
  今回範囲で最大の新規クローン）。
- Inference: 「0件のときWARNINGを出して0を返す」「進捗を何行ごとにINFOで出す」は
  同じ理由で変わる決まりごとで、5箇所に分かれている。
- Recommendation: `_common.py`へ`run_chunked_precompute(stmt, chunk_handler, *, label, dry_run)`を
  1本足し、各バッチはチャンク処理の中身だけを渡す形にする。**ただし
  `precompute_way_landcover.py`は範囲外判定・部分被覆の集計という固有の関心事を
  ループ内に持つため、無理に同じ器へ入れない**（4本＋例外1本でよい）。
- Scope: M
- Confidence: High

### F-F【P1】依存の既知脆弱性が critical 2 / high 3（前回は全0件）
- Evidence（実測、`npm audit`）:
  | 深刻度 | パッケージ | 現在 | 内容 | 修正版 |
  |---|---|---|---|---|
  | critical | `next` | 16.3.0（直接依存） | Unauthenticated RCE on **windows-hosted** servers（CVSS 9）／Image Optimization APIのAVIF経由RCE | 16.3.4（**semver minor**） |
  | critical | `maplibre-gl` | ^5.24.0（直接依存） | XSS Sanitizer Bypass in `DOM.sanitize()`（CVSS 10） | 6.9.0（**semver major**） |
  | high | `sharp` / `js-yaml` / `@redocly/openapi-core` | 推移依存 | libheif由来ほか | いずれも修正あり |
- 到達可能性の評価（過大評価しないための確認）:
  - Next.jsのWindows向けRCEは**該当しない**（`frontend/Dockerfile`は`node:20-slim`、Linuxコンテナ）。
  - AVIF経由のImage Optimization RCEは、`next/image`の利用が**src配下に0件**、
    `next.config.ts`に`images`設定なし、`public/`にAVIFなし。直接の攻撃面は小さいが
    `/_next/image`エンドポイント自体はstandaloneサーバに存在するため「無い」とは言い切れない。
  - **maplibre-glのXSSは実際に効きうる**: 地図ポップアップ（`buildRoadSurfacePopupHtml`）が
    表示する値はOSMタグ由来＝**第三者が編集できるデータ**で、`DOM.sanitize()`のバイパスは
    そのまま任意スクリプト実行につながる。これが本項の実質的な主眼。
- Recommendation: `next`は16.3.4へ（minorなので低リスク）。`maplibre-gl`は6系がmajorのため
  破壊的変更の確認が要る——**先に`next`だけ上げて分離**し、maplibre-glは独立したタスクにする。
- Scope: next=S / maplibre-gl=M
- Confidence: High（audit実測、到達可能性はコードで確認）

### F-G【P2】CIが検証するNodeと本番が動かすNodeが違う
- Evidence: `.github/workflows/ci.yml:98,140,162` はいずれも`node-version: "22"`
  （コメントも「dev環境と同じNode 22系に固定する」）。一方
  `frontend/Dockerfile:1,8` は`FROM node:20-slim`（build・runの両方）。
  `package.json`に`engines`の指定は**無い**ため、この差を機械的に止めるものが1つも無い。
- Impact: lint・型検査・vitest・e2eのすべてがNode 22で通っているが、本番が動くのはNode 20。
  ランタイム差に起因する不具合（Node 22で挙動が変わったAPIに依存した実装等）はCIを素通りする。
- Recommendation: Dockerfileを`node:22-slim`へ揃えるか、CIを20へ落とすか、いずれかへ一本化する
  （揃える方向を選ぶなら`package.json`へ`engines.node`を書き、根拠を1箇所にする）。
- Scope: S
- Confidence: High

### F-H【P3】Next.jsテンプレート由来の未使用アセットが5件残っている
- Evidence: `frontend/public/{file,globe,next,vercel,window}.svg` の5件は
  `frontend/src`・`frontend/e2e`のどこからも参照が0件（grep実測）。
  Dockerfileが`COPY --from=build /app/public ./public`で本番イメージへ入れている。
- Recommendation: 削除する（T677「フロントの残骸を撤去する」の取り残し）。
- Scope: S
- Confidence: High

### 本体で追加確認した事実（Agent指摘の裏取り）
- **最短距離ルートが迂回するフィルタの範囲**: `compute_hard_filter_excluded`
  （`domain/hard_filters.py:117-145`）は motorway / trunk / no_bicycle に加えて
  **`max_average_grade_percent`（利用者が指定する勾配上限）も同じ配列へ畳み込む**。
  `select_shortest_distance_route`はこの配列を一切参照しないため、
  「◯%以上の坂を避ける」設定も最短ルートには効かない。
- **`select_via_nodes`との対比**: `road_graph_engine.py:1093`は
  `context.legs[0].cost_list`（`:299`でinf化済み）を渡し、`:1134`も`inbound.cost_list`を渡す。
  一方`:1265`・`:1270`は前向き・後ろ向きとも`edge_length_m`をコスト・距離の両方に渡す。
  **通行可否を表現する唯一の場所を、この1メソッドだけが通っていない。**
- **`refresh_derived.py`の`_STAGES`**: ④〜⑪の8段で、`precompute_edge_curvature`は含まれない
  （実測）。migration `0035_add_road_edges_curvature.sql:5`は「適用後は
  app/batch/precompute_edge_curvature.py の実行が必須」と書くが、単一エントリポイントは実行しない。
- **`EdgeMaterialTable.get()`の材料復元漏れ**: `attributes.py:269/313/385/478-479`は
  `curvature_deg_per_km`を保持・`to_legacy_dicts`で使用する一方、
  `get()`（`:416-431`）は`EdgeMaterialBundle(...)`へ渡していない（実測）。
- **停止密度軸の生値の単位**: 本番スナップショット（revision 64）の公開軸`axis_abdaded0be20`は
  `poi_signal_per_km×1.0 + poi_level_crossing_per_km×1.5 + poi_stop_per_km×0.3 +
  poi_crossing_per_km×0.0`。`raw_value_unit`（`axis_display.py:215-247`）は
  weight<0を弾きweight==0を飛ばすだけで**重みの大きさを見ない**ため「回/km」を返す。
  重み付き和は1kmあたりの実際の回数ではない（実測で構成を確認）。
- **分布ヒストグラムの負値クランプ**: `axis_preview_service.py:128-141`で
  `upper = p99*1.2 if p99>0 else max(upper,1.0)` → `int(value/width)` → `buckets[max(0,i)]`、
  `zero_share`は`v <= 0`。負値のみの分布は1本の階級へ潰れ`zero_share=1.0`になる（実測）。
- **生値フィルタ述語の二重定義の現状**: `evaluation.py:356-361`（空タイル分岐）は
  `raw_value_unit(...) is not None and not (materials & REQUEST_DYNAMIC_MATERIAL_IDS)`、
  `:483-487`（通常分岐）はそこへ`axis_raw_value_array(...) is not None`が加わる。
  `axis_raw_value_array`がNoneを返すのは非`BreakpointLinearShape`のときのみで、
  `raw_value_unit`も同じ条件でNoneを返すため、**現時点で両者の集合は一致しており実害は無い**。
  ただし述語が2箇所へ別の形で書かれ、`raw_axis_ids`を検証するテストが
  `backend/tests/`全体で**0件**（実測）のため、片側だけの変更を止めるものが無い。
  ——**「現在は壊れていないが、構造として壊れうる」ものとして扱う**（判断原則16）。

### F-I【P2】静的フォールバックカタログが、実行時APIと同じ情報を持たない（F-Bと同根）
- Problem: 実行時API（`GET /api/axis-catalog`）と静的生成物（`axis-catalog.json`）は
  「同じ形の入力を同じ変換関数で処理できるようにする」（`export_openapi.py:202-207`の宣言）
  はずだが、実際にはフィールドが揃っていない。
- Evidence（実測）:
  - 実行時経路は`hooks/useAxisCatalog.ts:133`で`rawValueUnit: entry.raw_value_unit ?? null`を設定する。
  - 静的生成物`axis-catalog.json`の`axes[]`のキーは
    `axis_id / category / chip_label / dedicated_way_value_layer / display /
    display_band_labels_override / display_thresholds_override / dynamic_way_value_needs_* /
    icon_id / label / map_value_kind / map_value_unit / panel_hint / primary_attribute_ids /
    shape / show_map_icon` の18個で、**`raw_value_unit`を含まない**（実測）。
  - `export_openapi.py:186-232`の書き出しリストにも`raw_value_unit`が無い一方、
    `map_value_unit`は書き出している——**同じ「単位」でも片方だけが載っている**。
- Impact: 取得完了前・失敗時のフォールバック経路では
  `RouteAxisProfile.tsx:108`の`formatAxisRawValue(..., axis.rawValueUnit, ...)`が単位を得られず、
  T687で追加した「得点の隣の生値」が**無言で消える**（エラーにならないため気づけない）。
  F-B（静的カタログの軸集合が古い）と合わせると、静的フォールバックは
  「古い軸集合」かつ「フィールドが欠けた」二重に劣化した状態にある。
- Recommendation: `export_openapi.py`の書き出しへ`raw_value_unit`を足す。
  より根本的には、実行時APIのレスポンスモデルと静的生成物のフィールド集合が
  一致していることを**照合テストで機械的に固定する**（現在はどちらも人手の列挙）。
- Scope: S（フィールド追加）／照合テストの新設はM
- Confidence: High（生成物のキーを実測）

### F-J【P1】蛇行軸（curvature）の配線が3箇所で同じ方向に欠けており、相互に検知を打ち消している
本レビューで最も重要な**連鎖**。個々は他シャードの指摘だが、繋ぐと1件の重い欠陥になる。

1. **バッチが実行されない**: `refresh_derived.py:57-66`の`_STAGES`は④〜⑪の8段で、
   `precompute_edge_curvature`を**含まない**（実測）。
   一方`migrations/0035_add_road_edges_curvature.sql:5`は
   「適用後は app/batch/precompute_edge_curvature.py の実行が必須」と書く。
   派生データ再構築の**単一エントリポイント**を名乗るスクリプトが、必須と宣言された
   バッチを呼ばない。
2. **評価が読む場所が埋まらない**: ルート評価が読むのは`road_edges.curvature_deg_per_km`
   （`get_edge_materials_batch`→`EdgeMaterialBundle.curvature_deg_per_km`）。1により、
   `build_road_graph`がsplit時に埋める新規Edge以外はNULLのまま残る。
   蛇行軸（`default_weight=0.1`、本番で公開済み）はそれらの区間で欠損扱いになり、
   重み再正規化で薄まって**無警告のまま評価から抜ける**。
3. **診断が別のテーブルを見ている**: `material_coverage.py:206-216`の
   `curvature_deg_per_km`は`way_geometry`のみを`missing_condition`にしている。
   コメント自身が「Edge単位（road_edges、**ルート評価が読む**）とWay単位の両方にある」と
   認めたうえでWay側を選んでいる（実測）。
   → `precompute_way_curvature`だけ流せば**カバレッジ画面は「欠損0%」と報告する**一方、
   評価側は全Edgeで算出不能。
4. （さらに）**キャッシュ経路でも落ちる**: `attributes.py:416-431`の
   `EdgeMaterialTable.get()`が`curvature_deg_per_km`を`EdgeMaterialBundle`へ渡していない（実測）。

- Impact: 「軸を公開したのに効いていない」が、**ログにもカバレッジ診断にもテストにも現れない**。
  T691完了コミット（`509f0210`）は「本番でルートを生成し、全候補・全区間の
  axis_difficulties/axis_raw_valuesへcurvatureが載ることを確認した」と記録しているが、
  それは`build_road_graph`が新規に埋めたEdgeを含むタイルでの確認であり、
  1〜3が揃うと**既存Edgeの多いタイルでは静かに欠損する**。
- Recommendation:
  1. `refresh_derived.py`の`_STAGES`へ`precompute_edge_curvature`を追加する。
     あわせて**`_STAGES`と`app/batch/precompute_*.py`のファイル一覧を突き合わせる
     ドリフト検知テスト**を置く（現在の`test_refresh_derived.py`は8段を手書き列挙するだけで、
     次に増えるバッチでも同じ漏れが再発する）。
  2. `material_coverage`の`curvature_deg_per_km`を、評価が実際に読む
     `road_edges.curvature_deg_per_km`側で数えるよう直す（Way側も見たいなら2エントリにする）。
  3. `EdgeMaterialTable.get()`へ`curvature_deg_per_km`を渡す。
     `attributes.py`の往復テスト（`test_attributes.py:270`）は全fixtureが`None`のため
     この種の漏れを検出できない——**bundleの全フィールドが往復で保存されることを
     型から機械的に確認する**形にする。
- Scope: M
- Confidence: **High**（3点すべて実測）
