# 定量メトリクス計測（2026-08-31）

## 対象

- 対象コミット: `efd34ac`（origin/masterと一致）
- 初回計測のため前回差分なし

## 計測結果

### 1. 規模（cloc）

**重要な注意（初回計測で発覚）**: 最初の計測コマンドは`.claude/worktrees/`配下の
残存worktree（登録済み15件＋git未登録の破損ディレクトリ2件、計17件。うち大半が
backend/frontendのほぼ丸ごとの複製）を除外し忘れ、実装規模を約2.6倍に水増しして
計測してしまった（964ファイル/174,969行 → 正しくは538ファイル/57,047行）。以後の
計測コマンドは必ず`--exclude-dir`へ`worktrees`を含める（本ファイルのコマンド例は
修正済み）。

```bash
npx --yes cloc . \
  --exclude-dir=node_modules,.venv,venv,.next,dist,build,.git,coverage,.pytest_cache,__pycache__,.turbo,htmlcov,worktrees \
  --exclude-ext=json,lock,svg,png,jpg,jpeg,ico,pbf,mbtiles,parquet
```

| 区分 | ファイル数 | 実装行数(comment/blank除く) |
|---|---:|---:|
| 全体（docs/配下Markdown含む） | 916 | 99,420 |
| 実装コードのみ（Markdown除く） | 538 | 57,047 |
| うちTypeScript | 218 | 26,669 |
| うちPython | 247 | 26,961 |

### 実装本体 vs テストの内訳

| 区分 | ファイル数 | 行数 |
|---|---:|---:|
| backend/app（実装本体） | 114 | 10,264 |
| backend/scripts・benchmarks | 23 | 1,508 |
| backend/tests | 110 | 15,189 |
| frontend/src 実装本体（テスト・generated除く） | 112 | 13,128 |
| frontend/src テスト（.test./.spec.） | 98 | 11,664 |
| frontend/src/types/generated（自動生成、OpenAPI由来） | 7 | 6,652 |

**所見（Evidence、判断はoverall/complexityへ）**: テストコード（backend 15,189行＋
frontend 11,664行＝26,853行）が、純粋な実装本体（backend 11,772行＋frontend
13,128行＝24,900行）とほぼ同量（テスト/実装比率 約1.08）。docs/testing.mdの
テスト方針（レート制限境界値テスト・PostGIS統合テスト・T331での一括320件追加等）と
整合する規模。

### 2. 変更頻度（churn）

初回計測のため対象期間なし（次回計測時、本ファイルの日付を起点に計測する）。

### 3. テスト規模

```
backend: 1,177/1,409 tests collected（232件はpostgis marker除外、pytest -q -m "not postgis" --collect-only）
frontend: 98 test files / 872 tests passed（npx vitest run、実行結果は参考値。本項目は収集件数を主とする方針だが初回のため実測を残す）
```

### 4. 静的検査

```
frontend tsc --noEmit: エラー0件
frontend eslint: 警告・エラー0件
```

backend: lintツール未導入のため計測対象外（DEFER）。

### 5. 依存関係

```
frontend npm audit: critical 0 / high 0 / moderate 0 / low 0 / info 0（脆弱性0件）
```

backend: pip-audit未導入のため計測対象外（DEFER）。

## 要確認フラグ

初回計測のため閾値比較なし。次回計測から前回比+15%等の発火条件を適用する。

## 副産物としての対応

- 発見した残存worktree17件（ローカル）・関連ブランチ19件（うちmaster未マージ1件を
  除く18件）を削除。リモートの`claude/*`ブランチ11件のうち9件（master取り込み済み・
  オープンPRなし）を削除、2件（`claude/code-review-9cgl9h`・
  `claude/reverent-cannon-a1a880`）は未マージのため保持（内容はユーザーへ別途報告）。
  詳細はユーザーとの対話ログ（本ファイルには経緯を転記しない、docs/modules/README.md
  「記載粒度」節の禁止パターンに準じる）。
- 依存関係の脆弱性0件・静的検査エラー0件は良好な状態。次回計測での悪化有無を追跡する。
