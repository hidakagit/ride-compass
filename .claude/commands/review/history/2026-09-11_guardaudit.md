# ガード実効性監査・指摘の出自分析（2026-09-11）

- 実施日: 2026-09-11
- 対象コミット: `9d8fead5`
- 実施方式: 専用worktree（`/tmp/claude-0/rc-audit`、detached checkout）。本体の作業ツリーは変更しない。
- 動機: 「最近レビュー指摘が多い。構造的な問題がないか」という問い。
  `/review:all`のトリガーは未該当（前回から1日・4,561行）のため、周期レビューとは別に実施した。

## なぜこれを調べたか（数字の出発点）

| 回 | 対象範囲 | 指摘数 |
|---|---|---:|
| 第5回 (2026-09-08) | 431コミット / 実装268ファイル・52,012行（実装の83%＝実質全量） | 131件 |
| 第6回 (2026-09-10) | 95コミット / 実装238ファイル・8,704追加 | 151件 |

**範囲が1/4以下になったのに件数が増えている。** `scores.md`は4回連続で件数増を
「検出網羅性の向上」で説明しているが、第5回で既に実質全量に達しているため第6回には
この説明が効かない。総合スコアも2回連続0で、`scores.md`自身が
「計算式が固定のため全量レビューではスコアが状態を弁別しない」と書いている。

## 調査1: ガードの実効性監査（ミューテーション試験）

`review_checks.py`の検知器へ、わざと違反を1件ずつ入れて本当に落ちるかを確かめた。

- **pre-commit経路（`docs --staged`）: 11検知器すべてPASS。** 検知器そのものは健全。
- **CI経路（`docs --since`、`docs-consistency.yml`が走らせるもの）: 2件が機能していなかった。**

| 検知器 | CI経路（是正前） | 実態 |
|---|---|---|
| `find_redis_skeleton_violations` | **MISS** | `--staged`分岐にしか無く、CIには検査項目自体が存在しない |
| `find_undeclared_dead_refs` | **WARN** | 検知はするが「参考」扱いでexit codeに入らない（混入させてもexit 0） |

`.git/hooks/pre-commit`は各clone・各コンテナで手動インストールする前提で、
`docs-consistency.yml`自身が「未インストールのセッション・コンテナでは発火しない」ため
CIを常時の安全網に置くと書いている。その安全網に空いた穴だった。

文書側は実態より広く主張していた:

- CLAUDE.md（キャッシュ方針）: 「骨格を写経しない。**pre-commitとCIが機械的に検出する**」
- `docs/tasks/T723.md`: 「**新しい乖離の混入はpre-commitとCIで止まり**」

これは第5回レビュー（`2026-09-08_all.md:208`）が既に
「『pre-commit/CIが機械的にブロックする』という前提が成立していない」と指摘した型の残り。

### 是正（本監査で実施）

1. `DETECTOR_ENFORCEMENT`表を新設し、「どの検知器をどの経路でexit codeへ含めるか」の
   正本を1箇所にした。`staged`/`since`/`full`の3経路を明示する。
2. `cmd_docs`が実行時に**配線の自己申告**を行う（`unwired_detectors`）。表へ足しただけ・
   分岐から外しただけでは件数0のまま素通りするため、表と配線のずれ自体を違反として扱う。
3. `scripts/tests/test_review_checks.py`へ5件追加（35→40件）。中心は
   **pre-commit経路とCI経路の強制集合が一致すること**——今回の穴の一般形。
   許可リストが存在しないファイルを指していないか（＝検知器が静かに緩んでいないか）も固定した。
4. `redis_skeleton`のCI経路を通した結果、既存の実違反12件（`jma_amedas_service.py`）が
   初めて可視化された。内容を確認したところ、全国約1,300観測所を`pipeline`で
   Hashへ一括読み書きしており`get_json`/`set_json`では表現できない。
   `docs/caching.md`「自前で骨格を書いてよい例外」の「mget/pipelineによる一括読み書き」に
   当たるため許可リストへ理由付きで追加した（fail-openと失敗の記録は満たしている）。
5. 文書側の主張は、弱めるのではなく**実態を主張に合わせる**形で解消した。

### 是正後の再試験

同じ違反をコミット済みにしてCI経路で再試験し、**9件すべてPASS**（exit 1）を確認した。

## 調査2: 指摘の出自分類

第6回レビュー本文が`file:line`で指す箇所43件を、対象コミット`87f7333e`で`git blame`し、
その行を作ったコミットで分類した。

| 出自 | 件数 | 比率 |
|---|---:|---:|
| (b) 範囲外の既存コード | 21 | 48.8% |
| 解決不能（削除済み・行ずれ） | 11 | 25.6% |
| **(c) 前回レビュー対応（T659〜T685）が作った** | **9** | **20.9%** |
| (a) 本範囲の新規実装 | 2 | 4.7% |

解決できた32件でみると**(c)が28%**。内訳はT676(4件)・T675・T673・T659・T679・T672。

**限界**: 151件のうち`file:line`が書かれた43件だけが対象で、行番号付きの指摘は
機械的・docs系へ偏る。全数調査ではなくサンプルである。

同型の実例が本監査の直前にもある。T712（第6回P2/P3束ねF、2026-09-10）が
`ensureLayerFromSpec`を新設して「既存レイヤーへもspecを丸ごと再適用する」を全ensure関数へ
強制した結果、spec外から太さを決めていた`applySecondaryAxisCasingStyles`が巻き戻され、
ramp軸の線が常に下敷き幅で描かれるユーザー可視の不具合になった（`9d8fead5`で是正）。

CLAUDE.md「毎タスク完了時の即時チェック」2は共有パターンの変更時に
「他に取り残された消費者がいないか」を求めるが、**「既存の消費者が新しい契約と両立するか」**
という観点は含んでいない。

## 調査3: 型ごとの再発率

タスク515本を型で分類し、月別の推移を見た。

| 型 | 本数 | 8月 → 9月 | 是正の手段 |
|---|---:|---|---|
| docs/コメントと実装のずれ | 30 | 13 → **17** ↑ | 主に手で一掃 |
| 検知器・安全網の穴 | 28 | 17 → 10 ↓ | — |
| 写経・重複 | 20 | 6 → **13** ↑ | 主に手で一掃 |
| 軸の1本道からの逸脱 | 14 | 8 → 3 ↓ | 構造（汎用機構化） |
| テストの空白・偽陽性 | 12 | 6 → 3 ↓ | — |

**減った型と増えた型が手段できれいに分かれる。** 「軸の1本道」は検知器ではなく
構造そのもので再発しえなくした型で、確実に減っている。増えている2型は手で一掃するしかなく、
しかも第6回の最大カテゴリ（契約不一致18・コメントと実装の不一致15・個数表現の陳腐化12）は
**原理的に機械検知できない**——読んで理解しないと判定できない。

## 結論

1. **散文に、コードから導ける事実を書いている**（第6回P2/P3 141件のうち83件＝59%の供給源）。
   検知器を足しても捕まらない型で、書くのをやめる以外に減らす手がない。→ [T724](../../../docs/tasks/T724.md)段階3。
2. **一括修正が回帰を作っている**（locatableな指摘の28%）。共有パターンの契約を変えるとき、
   既存の消費者が新しい契約と両立するかを見る観点が運用に無い。
3. **ガードが自分の穴から漏れ続ける**。28本のタスクを費やしてなお本監査で2件発見した。
   検知器は鳴るのに配線が抜けている、が共通形（本監査で是正済み）。

## 起票案（ユーザー承認待ち）

- CLAUDE.md「毎タスク完了時の即時チェック」2へ、「既存の消費者が新しい契約と両立するか」を
  加える（規模S）。現在の文面は「取り残された消費者がいないか」だけで、T712→`9d8fead5`の
  型を捕まえられない。
- 周期レビューへガードのミューテーション試験を組み込むか、`review_checks.py`のサブコマンドへ
  昇格させるかの判断（規模S〜M）。本監査のスクリプトは使い捨てで、次回は再び手で書くことになる。
