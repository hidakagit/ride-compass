# レビュー基準改善提案（2026-09-19）

## 分析サマリ

- **対象期間**: 前回の `/review:improve`（2026-08-27）以降。この間に `/review:all` が**8回**（09-03・09-08・09-10・09-11・09-13・09-15・09-16・09-19）、`/review:ui` が1回（09-12）、ガード実効性監査が1回（09-11）実施されている。
- **対象**: `history/` の全結果ファイル、`docs/` の基盤構築以前のレビュー6本、`docs/improvement-plan.md`（完了687件・未完了36件）、現在の `review/*.md`（計1,627行）。
- **主な傾向**:
  1. **P0/P1の解消率は高い**（第10回のP0 2件・P1 4件は全件完了、第11回のP0は0件）。**基準の問題はP0/P1ではなくP2/P3の側にある**——束ね起票（T819〜T826・T903〜T908）に入った分は解消する一方、束ねから漏れた個票は回を跨いで残る（`is_fastest` の契約コメント3回連続、`LAYER_ICONS` 2回、`icons.tsx` の未使用エクスポート2回）。
  2. **基準ファイル自身が、基準が他者へ課しているルールを守れていない**。後述の提案1・2はどちらも「削除条件が成立したのに消していない」「前提が消滅したのに名指しし続けている」で、`complexity.md` が禁じている「安全弁を一度きりにする」と同じ形である。しかもそれを守らせるはずの検知器 `review_doc_dead_refs` は、記法の穴（第11回 F-1）で**0件を返し続けている**。
  3. **7回連続で総合スコアが下限0に張り付き、7回とも結果側に「弁別しない／内訳を見ること」と書かれている**（`scores.md` を `grep -c` で実測: 7）。指標が指標として機能していない。
  4. **Phase 6（UI）の実施可否が3回にわたって環境要因で崩れた**（09-10/09-11はdev DB不在、09-19はネットワーク遮断）。09-11の結果自身が「実施可否の判定と代替手段を手順へ明記する」と勧告しているが、`ui.md` は T684 以降更新されていない。
  5. **レビューが見逃し、ユーザー指摘で発覚した欠陥に共通の型がある**——「配信も描画も正しく動いているが、**出ている値・色が実態と合わない**」（T902・T914・T915・T916・T931）。この期間だけで5件。現在のどのレビュー観点もこれを対象にしていない。

---

## 変更提案

### 提案1: `consistency.md` の「双子実装の対称性」を、名指しの対を外して既存項目へ統合する

- **対象ファイル**: `review/consistency.md`「設計 ↔ 実装」節の末尾項目
- **種別**: 削除＋統合
- **根拠**（すべて本体で確認）:
  - この項目は自ら削除条件を書いている——「**削除条件: T122・T123（レシピ共通基盤）完了で双子の糊が共通化されたら、本項目は削除する**（構造的解消が正であり、チェックリストでの恒久運用はしない）」。
  - `docs/improvement-plan-archive/2026-08-17.md:1104,1158` に `### - [x] T122. …（2026-08-18完了）` `### - [x] T123. …（2026-08-18完了）`。**削除条件は1か月以上前・8回のレビューを跨いで成立している。**
  - さらに**項目が名指しする対そのものが消滅している**。「交通ストレス⇔安全度」の安全度軸は `backend/fixtures/axis_definitions_snapshot.json` に `"safety"` が **0件**、実装に残る「安全度」の綴りは2件（いずれも撤去の記録）。T292（2026-08-25）でレシピ機構自体が撤去済み。
  - **ただし「対になる実装の片側だけ変更した」という型は今も指摘を生んでいる**（本体で確認）——`2026-09-08_all.md:233`（`wind_way_service.py` ⇔ `gradient_way_service.py` の定数名が非対称）、`2026-09-10_all.md:824`（テストの非対称）・`:898`（バッチ列挙の非対称）・`:1137`（`error.tsx` ⇔ `global-error.tsx` のトークン化漏れ）。**この4件はいずれも「交通ストレス⇔安全度」とは無関係**で、項目が名指しする対からではなく型から出ている。
- **変更案**: 名指しの対・削除条件・T120/T121-aの経緯を含む6行を削除し、既存の「**共有フィールド・パターンの全消費者チェック**」項目へ1行を足して統合する。
  > （既存項目の末尾へ）対で持つ実装（`wind_way_service.py` ⇔ `gradient_way_service.py`、`error.tsx` ⇔ `global-error.tsx` 等）の片側だけを変えたコミットが対象範囲にあるときも、同じ要領で対になる側を確認する。
  経緯（T120・T121-aで片側だけ修正が2回発生した実績と、T122/T123での構造的解消）は `_history.md` へ1行で移す。
- **影響**: 検出力は落ちない——**生きている型は統合先へ残り、死んだ対の名指しだけが消える**。得られるのは、基準ファイル自身が「削除条件が成立したら消す」を実行した最初の例になること。`complexity.md` が他者へ課している「閾値を発火させたまま次を設定しないと安全弁が一度きりで機能しなくなる」というルールを、基準の側が8回守れていなかったという矛盾が解ける。

### 提案2: `overall.md`「このプロジェクト固有のチェック」の2項目を削除する

- **対象ファイル**: `review/overall.md`「このプロジェクト固有のチェック」（:63-64）
- **種別**: 削除
- **根拠**（本体で実測）:
  - 「エンジン間で同名フィールドの意味が食い違っていないか（管理された例外は**wind_score**のみ）」——`grep -rn "wind_score" backend/app frontend/src` = **0件**、`routing_engine` = **0件**。T462（2026-08-31）でopenrouteserviceエンジンを撤去し `routing_engine` 設定ごと無くなったため、「エンジン間」という前提が存在しない。
  - 「**scoring**（相対）/ `route_preference`（絶対）の役割境界が崩れていないか」——`total_score`（相対スコア）は撤去済みで、残る綴りは `frontend/src/app/admin/page.test.tsx:10` の「total_score撤去に伴い…」というコメント1件のみ。`context.md`「評価は絶対評価1系統」が正。
  - **この陳腐化は2026-09-10のレビューで一度見つかっている**（`2026-09-10_all.md:273,591,795`）。当時の指摘は `docs/design-principles.md:52` と `context.md` のKeep Listを名指しし、**その2つは是正された**（本体で確認: 現在どちらも `wind_score` 0件）。しかし**同じ前提を持つ `overall.md:63` は当時のgrepの対象に入らず、9回分のレビューを跨いで残った**。
  - これは CLAUDE.md「修正の原則」の**範囲は性質から導く**（「直す対象を『症状が出た場所』で決めず、その欠陥を成立させている性質で決め、当てはまる箇所を機械的に列挙してから直す」）に対する、レビュー基盤自身での違反例である。
  - **それを機械的に止めるはずの `review_doc_dead_refs` は0件を返している**。本体で実証: `identifier_exists("wind_score", corpus)` は `False` だが、overall.md は `wind_score` をバッククォート無しで書いているため `DOC_IDENT_RE` が抽出しない（同じ行にバッククォートを付けて渡すと検知器は鳴る）。**T743 が「レビュー手順書の陳腐化は全レビューへ伝播する」として作った検知器が、まさにその対象を見逃している。**
- **変更案**: 上記2項目を削除し、3項目目（Keep List の再指摘禁止）だけを残す。節の見出しは残す。
- **影響**: この2項目由来の新規Findingは9回のレビューで0件のため検出力は落ちない。**全レビューが最初に読むファイルから「今も2つのエンジンがある」「相対スコアがある」という誤った前提が消える。** なお見逃しの根治は第11回 F-1（記法の穴）の起票側で行うが、**本提案はF-1を待たない**——待つ間も全レビューが誤った前提を読み続けるため。

### 提案3: `principles.md` のスコアサマリで、総合スコアを主指標から降ろす

- **対象ファイル**: `review/principles.md`「スコアサマリ」
- **種別**: 修正
- **根拠**:
  - `100 - (P0×20 + P1×10 + P2×3 + P3×1)`（下限0）は、**7回連続で0に張り付いている**（09-08以降すべて）。`scores.md` の「前回差分」欄には7回とも「下限0で張り付き／計算式が固定のため全量レビューではスコアが状態を弁別しない——**内訳を見ること**」と書かれている（`grep -c` で7）。
  - つまり**運用は既に内訳へ移っており、形式だけが総合スコアを主指標として残している**。第11回はP0が6回ぶりに0件という大きな変化があったのに、総合スコアは前回と同じ0で、表からはそれが読めない。
  - 計算式を固定した当初の目的（主観採点を避ける）は、件数の内訳でそのまま達成される。
- **変更案**: 表の行順を入れ替え、先頭を「P0 / P1 件数と前回比」、2行目を「`category` 先頭語の内訳と前回比」（提案5で機械化）、総合スコアは**継続性のために計算し続けるが3行目以降へ置き、「下限に張り付いている回は弁別力を持たない」と1行注記する**。`scores.md` の列構成は変えない（過去行を書き換えないという運用を守るため）。
- **影響**: 過去との比較可能性は維持される（式も列も変えない）。失われるものは「1つの数字で語れる」という見かけだけで、それは既に7回機能していない。得られるのは、**「P0が0件になった」という実際に起きた改善が表の先頭に出る**こと。

### 提案4: `ui.md` の実施方法を、既定＝`frontend/e2e/fixtures.ts`（backendモック）へ組み替える

- **対象ファイル**: `review/ui.md`「実施方法（必須）」
- **種別**: 修正
- **根拠**:
  - 現在の手順は「**実際にdev環境を起動し**、Playwright（Chromium）で一連の操作を実行して確認する」＋「実機確認は本体の開発環境（`frontend/`）で行う」と、**実DB前提の1本しか書いていない**。
  - これが3回崩れている。09-10・09-11は「この実行環境にPostgreSQLが起動しておらず `.env` も無い。PostGIS＋関東のPBF投入が前提のため実機確認が成立しない」（09-11 結果:11-13）。09-19は本番URLへのアクセスがポリシーで拒否。09-11 の結果は「**Phase 6（UI）が2回連続で満足に実施できていない。…実施可否の判定と代替手段を手順へ明記する**」と自ら勧告している（:577-578）が、`ui.md` は `a93f7b0f`（T684）以降更新されていない。
  - 一方、**代替手段は既にリポジトリにある**——`frontend/e2e/fixtures.ts` はCIのE2Eスモークが使うbackendモックで、`playwright.config.ts` が実バックエンド・実外部APIに依存しないことを明記している。既定の現在地・30km周回は開発DBのデータ範囲外で即失敗するという実測もある。
  - `ui.md` の中に `e2e` / `fixtures` の綴りは **0件**（本体で `grep`）。
- **変更案**: 「実施方法」を3段に書き分ける。
  1. **既定**: `frontend/e2e/fixtures.ts` のモックで、変更のあった画面＋主要導線1周をデスクトップ幅と390px幅で確認する。
  2. **実データが要る所見だけ追加で**: カバレッジ・実際の値の妥当性・タイル配信は本番または実DBで見る。**見られなかった場合は結果ファイルへ「実機未確認」と明記しConfidenceを下げる**（省略してよい、を明文化する）。
  3. **どちらも不可のとき**: コード精読で代替し、その旨を冒頭に書く。
- **影響**: 「必ず実機」という要求を降ろすため、実データ由来の所見（タイルの空白・カバレッジ外）は取りこぼしやすくなる。**ただし現状は3回中3回で丸ごと取りこぼしているので、下がるのではなく上がる。** 得られるのは、UIフェーズが環境に左右されず毎回成立すること。

### 提案5: `category` 先頭語の集計を機械化する（`review_checks.py` のサブコマンド）

- **対象ファイル**: `review/principles.md`「共通実行手順」4d-2＋`scripts/review_checks.py`
- **種別**: 追加（**ただし基準への観点追加ではなく、既にある基準の機械化**）
- **根拠**:
  - 4d-2 は「**回をまたいで数えられるようにするため**」語彙を固定し、「トレンドは `category` の先頭語で数える」と定めている（2026-09-16 に `3a2ed0b6` で導入）。
  - しかし**導入から2回、どちらの結果ファイルにも語彙別の集計が無い**。09-16の結果に `doc-drift` の件数集計は無く、09-19は本体が手で数えた（P2で16/60）。4d-2 が引用する「直近3回で3割前後に留まる」という数値も `documentation.md` 側の記述で、レビュー結果から再現できない。
  - **語彙を固定する作業は終わっているのに、固定した目的が達成されていない**。これは観点の不足ではなく、集計という機械作業に手順が割り当てられていないだけである。
- **変更案**: `review_checks.py` へ `categories` サブコマンド（仮）を足し、`history/*_all_shards.md` の `category:` 行の先頭語を数えて回ごとの表を出す。`principles.md` 4d-2 には「集計は `review_checks.py categories` が出す。手で数えない」の1行だけを残し、比率の記述（「直近3回で3割前後」）は出力へ譲る。**`documentation.md` 側の数値も同じ出力を指すようにする**（数え上げを文章に残さない、というプロジェクト自身の方針と揃う）。
- **影響**: 追加コストは小さく、`_all_shards.md` の書式（`category:` で始まる行）は既に2回とも守られている。失われるものは無い。得られるのは、「`doc-drift` が減っているのか」という**この基盤が最も繰り返し問うている問いに、実測で答えられる**ようになること。

### 提案6: `ui.md` へ「表示されている値の妥当性」観点を追加する（機械化の検討を条件に）

- **対象ファイル**: `review/ui.md`「確認観点」
- **種別**: 追加
- **根拠**:
  - この期間にユーザー指摘で発覚した欠陥のうち5件が同じ型である——**「配信も描画も正しく動いているが、出ている値・色が実態と合わない」**。
    - T931「幹線道路に実地ではあり得ない勾配が付く」（本文: 「ユーザー指摘: レンズで15%以上と出た道路は、実地では…15%など絶対に無い幹線道路である」）
    - T914「標高の面レイヤが、勾配の無い所まで塗りつぶす」（本文: 「ユーザー指摘『標高図とか常時色付きのものを見ると前より違和感ある』」）
    - T902「土地被覆レイヤーが、都心では一律のグレーにしかならない」（本文: 「**配信も描画も正しく動いている**」）
    - T915「地図は連続補間、凡例は離散の色見本で、帯の中ほどの色が一致しない」
    - T916「起伏の陰影が、関東平野の傾きでは出ていても気づけない」
  - 現在の `ui.md` の観点は「初見ユーザーの理解可能性」「一貫性・用語」「地図とUIの関係（**判読できるか**）」で、**値が正しいかは対象外**。`overall`/`consistency` も「実装と設計・テストの一致」を見るため、実装どおりに動いた結果が実態と合わないことは射程外。5件とも**レビューの後にユーザーが見つけている**。
- **変更案**: 「地図とUIの関係」節へ1項目を追加する。
  > - **出ている値の妥当性**: 既知の場所を1つ選び、レンズ・面レイヤーが出している数値・色が実地と整合するか突き合わせる（例: 平坦な幹線道路の勾配、市街地の土地被覆）。「正しく配信され正しく描画された、もっともらしいが間違った値」はこの突き合わせでしか出ない。
  >   **ただし、同じ型を2回見つけたら観点を増やさず材料側の妥当範囲宣言（`MAX_PLAUSIBLE_AVERAGE_GRADE_PERCENT` が先例）へ移す**ことを既定とする。
- **影響**: 観点が1つ増える。**増やすこと自体は improve.md の方針（チェックリストより機械化）に反する**ため、変更案の中に「2回で機械化へ移す」という自己終了条件を埋め込んでいる。T931 が既に `MAX_PLAUSIBLE_AVERAGE_GRADE_PERCENT` という形で個別に機械化しており、一般化（`MaterialSpec` への妥当範囲宣言）の芽はある。**この観点は、その一般化が入るまでの橋渡しとして置く。**

### 提案7: `consistency.md` の機械化済み4項目を圧縮し、経緯を `_history.md` へ移す

- **対象ファイル**: `review/consistency.md`「設計 ↔ 実装」節
- **種別**: 修正（統合・圧縮）
- **根拠**:
  - 「ソースコードの経緯コメント再発」「docs/modules の記載漏れ・死んだ参照」「docs/modules のフォーマット準拠」「improvement-plan.md のチェック状態」の4項目は、**すべて `review_checks.py` の検知器が機械的に出す**ようになっている（`source_narrative`・`undocumented_files`/`dead_file_refs`/`dead_identifier_refs`・`narrative`・`plan_vs_tasks`）。
  - にもかかわらず、この4項目だけで `consistency.md`（183行）の**40行超**を占めており、その大半は「なぜこの検知が要るか」の経緯（T566の2,844件、T517の `BackendLogsPanel.tsx`、`dynamic-way-values.md` の `wind_service.py` 死んだ参照、T180・T185・T218の4回再発）である。
  - CLAUDE.md は「**何をどの経路で強制しているかの正本は `review_checks.py` の `DETECTOR_ENFORCEMENT` であり、その中身をこのファイルの手順として書き写さない——写した側だけが古くなる**」と定めている。`consistency.md` は `DETECTOR_ENFORCEMENT` の表そのものは写していないが、**同じ理由が当てはまる分量の説明を持っている**。
  - 実際に古くなっている例がある——「新規ファイルの記載漏れは `review_checks.py` の `undocumented_files` がpre-commit・CIで止めるが、その検知器が入る前から残っている取り残しと…」という書き分けは、`undocumented_files` が全件0件を返すようになった現在ほとんど意味を持たない。
- **変更案**: 4項目をそれぞれ1〜2行へ圧縮する（例: 「**docs/modules の記載漏れ・死んだ参照**: `review_checks.py docs` の出力を読み、**判断だけを行う**（何を検知するかは検知器が正）」）。経緯（4回再発・2,844件・具体のファイル名）は `_history.md` へ移す。`_history.md` は既にこの用途のために存在し、`README` も「経緯は `_history.md` へ集約し正式版には手順のみ」と定めている。
- **影響**: `consistency.md` が183行→約145行になる。失われうるのは「なぜこの検知が要るか」を読む導線だが、`_history.md` へのリンクで保たれる。得られるのは、**検知器が変わったときに書き換えるべき場所が1つ（検知器）に寄ること**と、レビュー実行時に毎回読む量が減ること。提案5・6で増える分を、ここで相殺する。

---

## 削除候補（価値の低い観点）

- **提案2で削除を提案した2項目**（エンジン間の同名フィールド、scoring/route_preference の境界）。9回のレビューで新規Findingは0件。
- **提案1の「交通ストレス⇔安全度」という名指し**（型そのものは統合先へ残すため、削除するのは名指しと削除条件だけ）。
- **`consistency.md`「タイル世代」項目**（「見るのは**プロパティ削除を伴う変更でfrontendを先にデプロイしているか**だけ」）: 削除は**提案しない**。T848 で世代が実行時組み立てへ移った直後の期であり、この1点だけは機械化されていない（第11回でも該当なしだったが、判定コストがほぼゼロで、外すと非互換デプロイ順の事故が無検査になる）。**次回、T848後2期連続で該当なしなら削除候補へ**。
- **`metrics.md`「5. 依存関係」の `pip-audit` DEFER**: 削除は提案しない。トリガー（backendへ `pip-audit` が導入された時点）が明示されており、DEFERの扱いとして正しい形になっている。

---

## context.md の陳腐化確認（手順10）

`review_doc_dead_refs` は0件だが、それは提案2で示した記法の穴によるもので、**保証にならない**。人手で確認した結果:

- **陳腐化していない**: レイヤー構造・正準定義・`/api/routes/preview` の残置（`routes.py:62` に実在）・研究モードの2層（`lib/researchMode.ts` 実在）・「軸の数・一覧は書かず生成物を都度参照する」方針・「閾値の正本は `size_watch.json`」。
- **更新を要する2件**（提案には含めず、次の編集時に併せて直すことを推奨）:
  1. **`AxisComposer.tsx` のKEEP記述**——「T270新設[474行]から3日で1,123行[+137%]…churnが継続中のためすぐには分割せず**監視のみ先行する運用**」とあるが、T924で**1,424行→327行**になった。この運用が**実際に機能した唯一の完了例**であり、経緯としてその結末まで書いておく価値がある（`complexity.md` が「閾値を発火させたまま次を設定しない」を戒めている、その反例として）。
  2. **`road_graph_repository.py` のKEEP記述**——T357で設定した個別閾値3,100行を、第11回で**3,236行**が超過した。context.md は「現在有効な閾値の正本は `size_watch.json`」と正しく書いているので記述自体は嘘ではないが、**次の閾値を決める判断が未了のまま**である。これは `complexity.md`「出力」節が「閾値を発火させたまま次の閾値を設定しないと、安全弁が一度きりで機能しなくなる」と定めている状態そのもの。
- **見出しの日付**: 「## 現在の開発フェーズと今後想定される主要な変更（2026-08-16時点の要約…）」は、中身が更新されているのに見出しの日付が古い。実害は小さいので今回は提案しない。

---

## 見送り（検討したが提案しないもの、理由つき）

- **`all.md` のPhase構成の変更**: Phase 3（overall）とPhase 5（consistency）のシャード共有は、11シャード・2波で問題なく回った。Phase 4（complexity）・Phase 6（UI）の単独実施も規約どおり機能している。**変える理由が観測されていない**ため見送る。
- **P2/P3の束ね起票を手順として必須化する**: 3回連続で実効（T819〜T826・T903〜T908・本回の提案）だが、**束ねから漏れた個票が回を跨いで残る**という実害が出ている（`is_fastest` 3回・`LAYER_ICONS` 2回・`icons.tsx` 2回）。手順を足すより、**束ねの母集団を `_all_shards.md` の全 `category:` 行から導出する**（提案5の集計の副産物）方が筋がよい。提案5が入ってから再検討する。
- **`review_checks.py` の記法の穴の是正を基準側へ書く**: 第11回 F-1 として既に起票案がある。**基準へ「バッククォートを付けて書くこと」と書くのは、検知器の穴を書き手の規律で埋める形**で、このプロジェクトが一貫して避けてきた方向。見送る。
- **`history/metrics.md` への転記手順の修正**: 第11回で「2026-09-03 以降7回、`/review:all` が一度も追記していない」ことを記録した。**本来ここで提案すべきだが、`metrics.md`（`/review:metrics`）と `all.md`（Phase 2）のどちらが転記の責任を持つかは、`/review:metrics` を単独で回す運用が今後あるかに依存する**。第11回時点でその判断材料が無いため、**次回の improve で、`/review:metrics` の単独実行がこの3週間に1度も無かった事実（`history/` に `*_metrics.md` が2026-09-03以降ゼロ）を根拠に「Phase 2 が転記まで行う」へ寄せることを提案する**。いま決めると、単独実行の側を根拠なく切り捨てることになる。
- **`ui.md` の「390px幅の実機確認」要求の緩和**: 提案4で既定をモックへ移すと、390px幅の確認はむしろ**やりやすくなる**（`resize_window` 相当がモックでも効く）ため、緩和は不要。
- **基準ファイル全体の統廃合**: 計1,627行のうち `_history.md` が287行を占め、正式版は1,340行。提案1・2・7で約50行減る。**これ以上の統廃合は、減らすこと自体が目的化する**ため見送る。

---

## 反映について

**このファイルは提案であり、`review/*.md` は1行も編集していない。** 承認された提案のみ、承認後の指示で反映する。

提案の依存関係: 提案5（集計の機械化）→ 「束ねの母集団を導出する」という次回の検討の前提になる。提案7（圧縮）は提案1（削除）と同じコミットで行うと `consistency.md` の差分が読みやすい。提案2は第11回 F-1（記法の穴）の起票と独立に実施できる（F-1 を待つと、その間も全レビューが誤った前提を読み続ける）。
