# レビュー基準改善提案（2026-08-27）

- 実施日: 2026-08-27
- 対象コミット（HEAD固定）: `e24bfd2`
- 前回improve: [2026-08-18_improve.md](2026-08-18_improve.md)

**対応状況（2026-08-27追記）**: 提案1〜7すべてユーザー承認・反映済み。
context.md（提案1・3）・complexity.md（提案2・7）・consistency.md（提案4）・
principles.md（提案5）・history/README.md（提案6）へ反映済み。削除候補・見送りは変更なし
（見送りの各項目は本文中に記載済みの理由のまま据え置き）。

## 分析サマリ

- **対象期間**: 2026-08-18〜2026-08-27（前回improveが対象にしなかった`2026-08-18_all.md`を含む）。
- **対象レビュー**: history/の11ファイル（2026-08-18_all・2026-08-19_all・
  2026-08-19_consistency_2・2026-08-22_all・2026-08-23_all・2026-08-23_all_2・
  2026-08-23_consistency・2026-08-24_overall・2026-08-25_overall・2026-08-25_ui・
  2026-08-27_all）。`all`系は内部にoverall/complexity/consistency/uiの4観点を含むため、
  実質的なレビュー実施回数はこれより多い。加えてdocs/tasks/のT130〜T382の中から
  レビュー起因タスクの対応実績（T196・T247・T286・T292・T298・T356等）を突き合わせた。
- **主な傾向**:
  1. **「architecture.md未追従」が全期間で最頻出の指摘**（2026-08-16→17→18(3回目)→
     19(4回目)→22(5回目)→24→25→27と、ほぼ毎回言及される）。都度「更新する」という
     Recommendationで個別対応されてきたが、07-27時点でも「肥大化トリガー発火・未着手」
     として残っており、チェックリスト運用だけでは構造的に解決していない。
  2. **「手動同期ペアのドリフト」（OpenAPI生成物）も同型の再発を繰り返した**
     （T180・T185・T218の3回でCLAUDE.mdへルール昇格→ルール昇格の**翌日**に4回目が
     発生→その後「機械化」されてようやく収束、と履歴に明記されている）。書かれた
     ルールの強化だけでは止まらず、機械的検知の強化で初めて止まった実例。
  3. 一方で**「双子実装の対称性」「タイル世代の対上げ」「モバイル横溢れ」は原則化・
     基準追加後に目に見えて再発が減った**——原則化・機械化が効くパターンと効かない
     パターンの違いが、今回の対象期間でより明確になった。
  4. `docs/improvement-plan.md`は本セッション中（2026-08-27）に肥大化（9,687行）を
     理由として`docs/tasks/Txxx.md`への分割を実施したが、`review/context.md`の
     「タスク・レビュー履歴の基盤」節はこの新構造に未追従（今回検出）。
  5. `review/context.md`には検証可能な事実誤り（既定エンジンの記述）が見つかった——
     「陳腐化しうる数値を書かない」という2026-08-16改善だけではカバーできない、
     「質的な現状記述」の陳腐化という新しいパターン。
  6. `history/`蓄積が24ファイル・約6,900行に達し、本レビューの実施自体に3並列の
     サブエージェント委任を要した。`history/README.md`が自ら定めた
     「蓄積が増えたらサマリ化を提案する」トリガーが明確に成立している。
  7. T292（推定軸の内部軸/公開軸階層化、規模XL）により、旧「レシピ付き評価軸」機構
     （専用Pythonレシピ・Override API・4パネルUI）が丸ごと撤去され、
     `complexity.md`の変更コスト表が前提とする G'/B'/H という特殊シナリオ区分が
     構造的に消滅した。基準ファイル側の追従が必要。
  8. 本日（2026-08-27）新設したMapOverlayControlsのページ送りボタン機能で、
     同一機能領域に対し1日で4件の実機修正（T374・T376・T380・T381）が発生した。
     いずれもPlaywrightヘッドレス実行では本質的に再現しにくい種類の不具合
     （長押しのネイティブコールアウト、タップ的中判定、flexboxクロス軸の意味の
     取り違え）で、レビュー未実施のうちにユーザーの実機操作で発覚した。

## 変更提案

### 提案1: context.mdの「タスク・レビュー履歴の基盤」節を、本日のdocs/tasks/分割後の構造へ更新

- 対象ファイル: `review/context.md` の「## タスク・レビュー履歴の基盤」節
- 種別: 修正
- 根拠: 本セッション中（2026-08-27）に`docs/improvement-plan.md`（9,687行）を
  タスク単位ファイル（`docs/tasks/Txxx.md`）＋一覧インデックスへ分割した
  （CLAUDE.md「作業ツリーの安全」節にも採番衝突対策として明文化済み）。しかし
  `context.md`の該当節は旧構造（「完了済みタスクの実施記録…は`docs/improvement-plan-archive/`
  へ退避済み」）のみを記述しており、新設の`docs/tasks/`とその使い分け
  （進行中/未完了の背景・方針・実装メモは`docs/tasks/Txxx.md`、日付ごとの完了記録は
  従来どおり`docs/improvement-plan-archive/`）が反映されていない。レビュー実施者が
  「対象範囲でチェック済み/完了マークが付いたタスクを1つずつ列挙する」
  （consistency.md）際、どこを読めば良いかの案内が古いままになる。
- 変更案:
  ```diff
   - **タスク登録**: docs/improvement-plan.md。T番号＋チェックボックス＋
     規模（S=1時間以内/M=半日/L=1日以上）＋トリガー条件。**現在のT番号・進行中/未着手一覧は
  -  improvement-plan.mdの末尾（オープンなタスクを含む節のみが残る）を都度参照する
  -  （本ファイルには具体的な番号・件数を書かない。書いた瞬間から陳腐化するため）。**
  -  完了済みタスクの実施記録・完了タスク一覧（旧「記録」テーブル）は2026-08-19の棚卸し整理で
  -  docs/improvement-plan-archive/（日付ごとのファイル＋README.md索引）へ退避済み。
  +  improvement-plan.mdの一覧（インデックス）を都度参照する（本ファイルには具体的な
  +  番号・件数を書かない。書いた瞬間から陳腐化するため）。**2026-08-27、タスク単位の
  +  ファイル分割を実施済み**: improvement-plan.md本体はチェックボックス付きリンクの
  +  一覧のみを持ち、各タスクの背景・対応方針・実装メモ・検証結果は
  +  `docs/tasks/Txxx.md`（未完了・完了を問わず1タスク=1ファイル）にある。
  +  日付ごとの完了記録アーカイブ（`docs/improvement-plan-archive/`、2026-08-19棚卸で
  +  新設）は本分割より前の運用で、両者の使い分けは`docs/improvement-plan-archive/README.md`
  +  の注記を参照。
     レビュー指摘はimprovement-plan.mdへ起票する（起票はユーザー承認後）。1タスク=1コミット、
     挙動変更はテスト先行。
  ```
- 影響: レビュー実施者（人間・別セッションのエージェント問わず）が現在の一次情報の
  在り処を正しく辿れるようになる。反映しないまま次回レビューを迎えると、
  「completedマーク済みタスクを1つずつ列挙して確認する」consistency.mdの手順自体が
  古い前提のまま実行され、`docs/tasks/`が調査対象から漏れるリスクがある。

### 提案2: complexity.mdの「規模ウォッチ」対象へ`docs/architecture.md`を追加する

- 対象ファイル: `review/complexity.md` の「### 規模ウォッチ（肥大化の汎用監視）」
- 種別: 修正（対象範囲の拡大）
- 根拠: `docs/architecture.md`は「コード変更と同一コミットで更新」というCLAUDE.mdの
  ルールにもかかわらず、対象期間中ほぼ毎回（2026-08-18・19・22・24・25・27）
  未追従・矛盾記述が指摘される最頻出パターンだった。2026-08-25以降は指摘の主眼が
  「未追従」から「肥大化そのもの」（2,146行、+23%）へ移り、`docs/tasks/T286.md`に
  記録の通り2026-08-27に2回目の`decisions/`への抽出（T8が1回目）で1,929行へ縮小した
  ばかりである（現在1,973行）。同じ抽出運用が2回発生している以上、これはもう
  「たまに起きる特別対応」ではなく規模ウォッチで横断的に追うべき対象である。
  現行の規模ウォッチは「実装ファイル（テスト・生成物・CSS・migrationを除く）」を
  対象と定義しており、docsファイルは暗黙に対象外になっている。
- 変更案:
  ```diff
   ### 規模ウォッチ（肥大化の汎用監視）
   特定ファイルを名指しで監視するのではなく、毎回同じ手順で横断計測する:
   1. 実装ファイル（テスト・生成物・CSS・migrationを除く）の行数を実測し、
      backend / frontend 各上位5件程度を**規模ウォッチ表**（ファイル・今回行数・前回行数・増分）
      として出力へ含める。前回値は history/ の前回complexityレビューの同表から取る
      （初回は 2026-08-18_complexity.md 記載の実測値を初期値としてよい）。
  +  **`docs/architecture.md`も同じ表に加える**（T8・T286の2回、decisions/への抽出で
  +  対応した実績があり、単発ではなく繰り返す成長パターンと判断する。前回値は
  +  T286完了時点の1,929行を初期値とする）。
   2. **発火条件（いずれか）**: 前回同種レビュー比 +15%以上の成長／1,000行の新規超過／
      上位5件への新規登場。発火したファイルは行数だけで問題化せず
      「複数の変更理由が同居しているか」で精査し、必ず次のいずれかへ分類する:
      (a) 理由つきKEEP、(b) 抽出・分割の提案、(c) 閾値付きKEEPの個別設定（Keep Listへの提案）。
  +  `docs/architecture.md`が発火した場合の(b)は「decisions/への経緯記述の抽出」
  +  （T8・T286と同じ手法）を既定の選択肢とする。
  ```
- 影響: 規模ウォッチ表にdocsファイルが1行増える程度のコストで、「未追従の指摘→
  更新のRecommendation」という毎回同じで効果の薄いサイクルに、「肥大化そのものが
  更新を妨げている」という根本原因への対処（構造的な分割）を明示的な選択肢として
  組み込める。

### 提案3: context.mdの事実誤り（既定エンジン）を訂正し、「質的な現状記述」も陳腐化しうる旨を明記する

- 対象ファイル: `review/context.md` の「## 現在の開発フェーズと今後想定される主要な変更」
- 種別: 修正
- 根拠: 125行目「既定エンジンはopenrouteservice（評価情報の見える化・精査を優先）」は
  事実として誤り。`docs/tasks/T247.md`（2026-08-23完了）により`routing_engine`の既定値は
  `road_graph`へ切り替わっている。2026-08-16improveの提案3は「陳腐化する**具体的な数値**を
  書かない」ことに対応したが、今回見つかったのは数値ではなく**質的な現状（どちらが既定か）
  の断定**であり、同じ根本原因（要約ファイルが一次情報の変化に追従しない）の別の現れである。
  この記述はoverallレビューの「エンジン間で同名フィールドの意味が食い違っていないか」等の
  判断材料になりうるため、誤ったままだと判断を誤らせるリスクがある。
- 変更案:
  ```diff
   ## 現在の開発フェーズと今後想定される主要な変更（2026-08-16時点の要約。正は improvement-plan.md）
   
  -- 既定エンジンはopenrouteservice（評価情報の見える化・精査を優先）。RoadGraphエンジンの
  -  探索品質・性能改善は将来拡張として継続。
  +- 既定エンジンは`backend/app/config.py`の`routing_engine`設定値を都度確認する
  +  （2026-08-23のT247で`road_graph`へ切替済み。以後も切替が起こりうるため、本ファイルには
  +  固定の既定値を書かない）。openrouteserviceは非既定でも引き続き選択可能な並存エンジン。
   - 進行中/未着手タスクの一覧はimprovement-plan.mdを都度参照する（本ファイルには転記しない）。
  ```
- 影響: レビュー時に「どちらが既定か」を誤認するリスクを除去する。加えて、
  「数値だけでなく質的な断定も陳腐化しうる」という教訓をimprove手順の step10
  （context.mdの陳腐化確認）で今後意識的に探す対象に加える効果がある。

### 提案4: consistency.mdの手動同期ペア系チェックへ「2回目以降の再発は機械的検知を既定の推奨にする」旨を明記

- 対象ファイル: `review/consistency.md` の「### 設計 ↔ 実装」（API仕様と実装の乖離の項目）
- 種別: 修正
- 根拠: OpenAPI生成物ドリフトは、T180・T185・T218の3回でCLAUDE.mdへルール昇格
  （「同一コミットで再生成しdiffクリーンを確認する」という**書かれた手順**）されたが、
  ルール昇格の**翌日**（history/2026-08-23_all.mdの実施日基準）に4回目が発生した
  （history/2026-08-24_overall.md F-1、`AxisDefinitionResponse.description`未反映）。
  その後history/2026-08-25_overall.mdは同項目を「改善済み・機械化」と記録しており、
  以後の対象期間（2026-08-27まで）で同型の再発は確認されていない。**書かれた手順の
  強化だけでは止まらず、機械的検知の強化で初めて止まった**という実例が、この期間で
  最も明確な形で残った。一方「双子実装の対称性」「タイル世代対上げ」は原則化
  （チェックリスト化）だけで再発が大きく減っており、両者の違い（前者は毎回人手の
  再生成手順が必要＝機械化が容易、後者は判断が必要な設計変更＝チェックリストで
  注意を促す方が適切）を基準文面に残す価値がある。
- 変更案:
  ```diff
   - API仕様と実装の乖離: openapi.json / `types/generated/api.d.ts` が最新か
     （生成→`git diff --exit-code` 相当の確認。CIのapi-contractジョブと同じ観点）
  +  。**同型のドリフトが3回以上再発した実績がある**（T180・T185・T218→CLAUDE.mdへ
  +  ルール昇格→昇格の翌日に4回目が発生、history/2026-08-24_overall.md F-1）。
  +  書かれた手順・CLAUDE.mdへの格上げだけでは再発を防げなかった一方、その後の
  +  機械的検知の強化で収束した（history/2026-08-25_overall.md）。**同種の手動同期
  +  ペア（生成→diff確認という定型作業を要するもの）で2回以上の再発が観測された
  +  場合は、Recommendationとして「手順の再徹底」ではなく「機械的検知の追加
  +  （pre-commit/CI/テストでの強制）」を既定の第一候補にする。**
  ```
- 影響: 今後同種のドリフト（例: 風格子定数・タイル世代等の他の手動同期ペア）で
  2回以上の再発が観測された際に、レビューのRecommendationが「またルールを書く」で
  終わらず「機械化する」方向へ最初から誘導される。

### 提案5: principles.mdの共通実行手順へ「引用するhistory/ファイルの実在確認」を追加

- 対象ファイル: `review/principles.md` の「## 共通実行手順」手順2
- 種別: 追加
- 根拠: `docs/improvement-plan.md`のT349エントリと`docs/architecture.md:1131`が、
  一度も保存されなかった`history/2026-08-26_complexity.md`を根拠として引用していたことが
  2026-08-27の統合レビュー（複雑度エージェント・全体把握の2経路で独立に）発覚した
  （`docs/tasks/T356.md`として事後記録・対応済み）。principles.mdの共通実行手順5は
  「結果を`history/YYYY-MM-DD_<review-type>.md`として保存する」と定めているが、
  「保存されたはずの結果が実際に存在するか」を確認する手順がどこにもなく、
  保存漏れが最終成果物（タスク・設計書）へ引用された後まで発覚しなかった。
- 変更案:
  ```diff
   2. 過去レビューを確認する: [history/](history/) の同種レビュー（直近1〜2件）と、
      基盤構築以前のレビュー（docs/*-review-*.md、context.md に一覧あり）。
      再発チェックと「解消済み」の明示に使う。
  +   **docs/やimprovement-plan.md（docs/tasks/含む）がhistory/の特定ファイルを
  +   根拠として引用している場合、そのファイルが実際にhistory/へ保存されているかを
  +   確認する（`history/2026-08-26_complexity.md`が2箇所から引用されながら一度も
  +   保存されていなかった実績がある。docs/tasks/T356.md参照）。存在しない場合は
  +   P3として指摘し、遡及復元できなければ引用元へ「未保存」の注記を提案する。**
  ```
- 影響: レビュー実施のたびに軽い存在確認（`ls`/`git log`1回）が増えるだけで、
  「検証不能な引用」がドキュメントに残り続けるリスクを構造的に潰せる。

### 提案6: history/の蓄積に対するサマリ化・アーカイブ運用を開始する

- 対象ファイル: `review/history/README.md` の「## 運用ルール」
- 種別: 追加（運用開始。基準ファイル自体の記述はほぼ変更不要）
- 根拠: `history/README.md`は既に「蓄積が増えて参照が重くなったら、`/review:improve`で
  『古い履歴のサマリ化』を提案する（現時点では件数が少ないため何もしない）」と
  自ら明記している。2026-08-16時点は1ファイルだったが、本レビュー時点で
  `history/`は24ファイル・約6,900行に達し、**この`/review:improve`自体の実施に
  3並列のサブエージェント委任を要した**（本ファイル冒頭の分析サマリ参照）。
  トリガーは明確に成立している。本セッション中に`docs/improvement-plan.md`
  （9,687行）を`docs/tasks/Txxx.md`＋一覧インデックスへ分割した前例が、
  そのまま応用できる形になっている。
- 変更案（history/README.mdへの追記案）:
  ```diff
   ## 運用ルール
   
   - 履歴ファイルは追記型。過去の判断を書き換えない（誤りだった場合は削除せず
     「訂正」節を追記する。docs/ui-review-2026-08-16.md の訂正1・2が先例）
   - レビュー実行時は、直近の同種レビュー1〜2件を必ず読み、
     Regression / Previous Findings セクションで対応関係を明示する
  -- 蓄積が増えて参照が重くなったら、/review:improve で「古い履歴のサマリ化」を提案する
  -  （現時点では件数が少ないため何もしない）
  +- **2026-08-27、トリガー成立**: `history/`が24ファイル・約6,900行に達し、
  +  `/review:improve`自体の実施に複数サブエージェントへの分割委任を要するようになった。
  +  同種レビューの各`all`実施結果に完全に包含される個別レビュー
  +  （例: `_all.md`と同日・同範囲の`_overall.md`等、重複が明らかなもの）や、
  +  3世代以上前の`all`は、`docs/improvement-plan-archive/`と同じ発想で
  +  日付範囲ごとの要約ファイルへ圧縮することを検討する。直近2〜3世代の`all`と
  +  各観点の最新1件はフルテキストのまま残す（Regression追跡に必要なため）。
  +  実施は本提案がユーザー承認された後、別タスクとして着手する（改善提案自体は
  +  コードを書かないため、要約作業は`/review:improve`の範囲外の実装タスクとする）。
  ```
- 影響: 次回以降の`/review:improve`・各レビューの「直近1〜2件を読む」手順が
  軽くなる。一方でサマリ化の実施自体は本提案の範囲外（別タスクとして着手）とし、
  本提案はトリガー成立の記録と方針の合意に留める。

### 提案7: complexity.mdの変更コスト表からG'/B'/H（レシピ系シナリオ）を撤去する

- 対象ファイル: `review/complexity.md` の「### 変更コストの計測」
- 種別: 削除
- 根拠: G'（レシピ付き評価軸の追加）・B'（レシピの段階数変更）・H（レシピ項目の追加）は
  いずれも「専用Pythonレシピ＋Override API＋専用パネル」という旧設計（`CarStressRecipe`・
  `RoadSuitabilityRecipe`等）を前提にした変更コストの特殊区分だった。T292
  （2026-08-25完了、規模XL）はこの機構自体を撤去し、「内部軸→公開軸の宣言的組み立て」へ
  一本化した。history/2026-08-27_all.mdは「complexity C-1（評価軸追加コストの二重化）は
  構造的解消（T292でG'/B'/H区分自体消滅）」と明記している。もはや存在しない変更経路の
  コスト区分を基準ファイルに残しておくと、次回の複雑度レビューが実在しないシナリオを
  探して時間を使うリスクがある。
- 変更案:
  ```diff
   ### 変更コストの計測
   代表的な変更シナリオ（評価軸追加・道路属性追加・エンジン追加・レイヤー追加）、
   および**外部静的データソース追加**（新規raw層テーブル＋取込バッチ＋Edgeへの事前マッチング
   バッチ（線データの場合）＋domain定数＋Repository対称メソッド＋タイル配信/プロパティ拡張＋
  -フロントカタログ、という6点セット。T50・T51が実例）、さらに**レシピ付き評価軸の追加**
  -（G'、T119実例: 64ファイル・+3,677行）・**レシピの段階数変更**
  -（B'、T117実例: 19ファイル）・**レシピ項目の追加**（H、12〜15箇所）について
  +フロントカタログ、という6点セット。T50・T51が実例）について
   「編集が必要なファイル数・箇所数」を実際に数え、
   **直近のcomplexityレビュー結果（history/ の最新complexity。初回のみ
   docs/complexity-review-2026-08-16.md）の変更コスト表**と比較して悪化/改善を報告する。
  -T122/T123（レシピ共通基盤）完了後の最初のレビューでは、G'/Hの削減効果
  -（見込み: 64→40ファイル前後・12〜15→7〜9箇所）を実測で検証する。
  +T292（内部軸/公開軸の階層化、2026-08-25完了）により旧レシピ機構は撤去済みのため、
  +以後の「評価軸追加」コストは基本のCase（評価軸追加）1本で測る。新設された
  +内部軸/公開軸の階層構造に特有の変更コストパターンが今後見つかった場合は、
  +その時点で新しいCase名を割り当てる（G'/B'/Hの再利用はしない）。
   評価軸追加の1本道（context.md）から外れた箇所数の増加は最優先の指摘対象。
  ```
- 影響: 基準ファイルの記述が実装と一致した状態に戻り、次回のcomplexityレビューが
  実在しないコスト区分の検証に時間を割かずに済む。過去の実測値（G'=64ファイル等）は
  history/内の該当レビュー（2026-08-18_complexity.md等）にそのまま残るため、
  歴史的record自体は失われない。

## 削除候補（価値の低い観点）

- **consistency.mdの「双子実装の対称性」チェックの具体例（交通ストレス⇔安全度）**:
  T292でこの具体的なペア自体が実装から消滅した（history/2026-08-19_consistency_2.mdは
  「具体的ペア消滅後も一般的チェック観点として引き続き有効」と判断済み）。今回も
  この判断を追認し、**削除は提案しない**が、次回改訂の機会があれば例示を
  「交通ストレス⇔安全度」から一般形（「同一計算を担う実装が複数箇所にある場合の
  対称的変更」）へ差し替えることを推奨する（今回は文面変更のコストに見合う緊急性が
  ないため見送り、提案本体には含めない）。
- **complexity.mdの「外部静的データソース追加」変更コストケース**: 2026-08-16改善で
  追加されたが、対象期間（2026-08-18〜2026-08-27）で新規の外部静的データソース追加が
  なく、再利用実績を確認できなかった。「毎回該当なし」の基準（2回以上）にはまだ
  達していないため、削除は提案しない。

## 見送り（検討したが提案しないもの、理由つき）

- **ui.mdへ「タッチ操作固有の癖」（長押しコールアウト・タップ的中判定・
  flexboxクロス軸の意味）の正式なチェック項目追加**: 本日（2026-08-27）新設した
  MapOverlayControlsのページ送りボタンで、同一機能領域に1日で4件の実機修正
  （T374・T376・T380・T381）が発生し、いずれもPlaywrightヘッドレス実行では
  本質的に再現しにくい種類の不具合だった。正式な観点追加を検討する価値はあるが、
  (1) 4件は「同じ指摘の再発」ではなく異なる根本原因を持つ別々の不具合であり、
  「2回以上の同型再発」という格上げ条件に厳密には合致しない、(2) この機能領域は
  今回のレビュー対象コミット（2026-08-27_all.mdの対象`f7b8f5e`）より後に作られたため
  正式なUIレビューを一度も受けていない。次に同種の不具合（別の新設タッチコントロールで
  実機発覚のバグ）が発生したら、そこで初めて「2回目」として正式提案する方が
  根拠として強い。ただし記録として、この4件のパターン
  （長押し時のuser-select/-webkit-touch-callout抑止漏れ・align-selfの軸依存性・
  タップ対象サイズ不足）は次回のui.md改訂時の有力候補として申し送る。
- **all.mdのPhase構成・実行順序の変更**: 対象期間中`all`は5回実施され
  （2026-08-18・19・22・23×2・27）、Phase 2〜5の省力化ルール（直近の同種レビュー結果を
  再利用する）が実際に複数回機能していることを確認した（例: 2026-08-18_allが
  2026-08-18_overall/complexityの続きとして差分のみ実施）。順序自体に問題を示す
  証拠は見つからなかった。
- **overall/complexityの分担再定義**: 対象期間中、両者の重複指摘は観測されなかった
  （Phase6での統合はいずれも「異なる症状・同一根本原因」であり、同一症状の重複ではない）。
  現行の分担定義は機能しているため変更しない。
- **context.mdの「意図的な設計判断」節のさらなるポインタ化**: 既に
  AxisComposer.tsx・road_graph_repository.py/engine.pyの3項目が「現在有効な閾値は
  直近のcomplexityレビューを参照」という形でポインタ化済み。今回新たに追加すべき
  固定値の書き込みは見つからなかった。
