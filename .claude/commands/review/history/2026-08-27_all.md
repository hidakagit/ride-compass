# 統合レビュー（2026-08-27・第8回）

- 実施日: 2026-08-27
- レビュー種別: all（overall/complexity/consistency/uiを段階実施→Phase6相互確認→Phase7統合判断）
- **対象コミット（HEAD固定）: `f7b8f5e`**（コミットメッセージ「T350: axis_definitionsを全14軸DB専有化しAXIS_DEFINITIONSのPython literalを撤去」）。`git fetch`実施済み、ローカルmaster＝origin/master先端で一致（差分ゼロ）を確認済み。
- 対象範囲: 前回統合レビュー（第7回、`e686949`）以降**222コミット・329ファイル・+40,747/-13,312行**。前回overallレビュー（2026-08-25、`8cea9ee`）以降は142コミット・258ファイル。前回ui/consistency個別レビューはそれぞれ2026-08-25（軸スタジオ限定）・2026-08-23（`e686949`）。
- 実施場所: Phase2〜4（overall/complexity/consistency）は専用worktree（`.claude/worktrees/review-all-20260827`、detached HEAD固定）で3体のサブエージェントへ分担実行（対象範囲が非常に大きい[222コミット]ため、本体コンテキストの圧迫を避ける目的で並行委任した。各エージェントにはworktree限定・コード/docs変更禁止を明示し、開始時にHEAD一致を確認させた）。Phase5（UI）はdevサーバーを本体作業ツリーで起動しPlaywright（自前スクリプト、Claude Browserペインは不使用——ユーザー既定方針）で実機確認した後、停止済み。
- テスト実測: consistencyサブエージェントがDB未接続のためbackend全件実行はできず（Confidence Medium、詳細後述）。overall/complexityサブエージェントはコードレビュー中心（大規模差分のため全件実行は割愛、grep・行数実測・個別ファイル読解で裏取り）。
- **コードは変更していない（全Phaseとも読み取り専用調査）。**

## Phase 1: 全体把握

222コミットの主要テーマ（新しい順）:

1. **T350（XL、直近・最重要）**: `domain/axis_definitions.py`の`AXIS_DEFINITIONS`という360行のPython literal（14軸分の評価軸定義）を完全撤去し、`axis_definitions`DBテーブルを唯一の正本にした。T348（Python正本＋自動生成SQL）→T349（フォールバック撤去・fail-fast化）→T350（Python literal完全撤去）という3段階の設計反復を経て確定。
2. **T336〜T348（材料正規化）**: 評価軸の「材料(material)」を正規化+線形結合で統一。MATERIAL_CATALOG宣言駆動化（T280）、bicycle_infra材料の完全削除（T347、独立公開軸bicycle_infra_qualityへ置換）。
3. **T299〜T332（フロント大規模UI再設計）**: Tailwind+Radix基盤新設（T299）、開発者タブ廃止・モバイルタブ3分割再編（T300）、軸スタジオを4ステップウィザードへ再設計・用語平易化（T332、前々回UIレビューF-1〜F-5への対応を含む）、route_preference.yaml撤廃（T316）。
4. **T305〜T321**: 軸スタジオの継続的UX改善、既存7軸ハードコーディングの複数回の全面監査。
5. **T295〜T298**: 前々回overallレビュー（2026-08-25、第9回）の起票4件、全件対応済み。

## Phase 2: 全体最適レビュー（overall・サブエージェント実施）

### Executive Summary

前回（2026-08-25第9回）が最重要提言としたF-1「DB化された軸定義の半端な鮮度検知の不在」は、推奨（軽量な起動時突き合わせ）を上回る形で構造的に解消された。T295→T349→T350という段階を経て「フォールバック先が物理的に存在しない」構造に到達し、対症療法から構造的解決へ転換した。F-2〜F-4も本期間中に全件解消。局所最適の連鎖・新旧設計の混在といった構造的問題は本レビュー範囲では確認されなかった。

### Findings

**[P3] O-1. architecture.md肥大化のトリガー（T286）が発火済みだが未着手**
- Evidence: `docs/architecture.md`は1,741行→**2,146行**（+23%）。T286のトリガー「次の大きな設計転換時」はT347〜T350（XL級）で該当したが、T286自体は`- [ ]`のまま未着手。
- Recommendation: 着手判断をユーザーに促す（起票済みのため新規起票は不要）。
- Scope: M（既起票） / Confidence: High

### KEEP（overall）
- T350のfail-fast設計（`AxisDefinitionSyncError`、main.pyのlifespanで捕捉しない）
- 削除禁止ガード（`_CODE_COUPLED_AXIS_IDS`）と対症療法の自覚（T352への導線がコメントに明記）
- `realistic_axis_fixtures.py`（テスト専用固定フィクスチャ、DB値非検証の境界を明示）
- T299のTailwind/CSS Modules併存（使い分け基準が`docs/frontend-design-system.md`に明文化）
- T347のbicycle_infra削除（残骸なし）

### REMOVE / SIMPLIFY / REFACTOR / EXTEND / DEFER
該当なし。

### Regression / Previous Findings（前回2026-08-25第9回F-1〜F-4）

| 前回指摘 | 評価 |
|---|---|
| F-1（P2、DB化軸定義の鮮度検知不在） | **完全解消（推奨を上回る対応）**。T295→T349→T350で「フォールバック先が物理的に存在しない」構造に到達。デプロイパイプラインへのmigration適用ステップ追加（T349）でT294の運用面の根本原因にも対処済み |
| F-2（P3、軸id・材料id名前空間衝突ガード不在） | **解消済み**（T296） |
| F-3（P3、car_stressランプ表示の未登録highway緑色） | **解消済み**（T297） |
| F-4（P3、T292削除物の残骸コメント） | **解消済み**（T298）。`recipe_then_breakpoint_linear`は「削除すべき残骸」ではなく「目論見書テンプレート4種の線引きに紐づく意図的なKEEP」と実装時に方針転換 |
| 規模ウォッチ（MapView.tsx/page.tsx） | 横ばい〜微増、両者とも閾値内 |
| architecture.md肥大 | 悪化継続、トリガー発火（O-1） |

---

## Phase 3: 複雑度平衡性レビュー（complexity・サブエージェント実施）

### Executive Summary

前回（第7回）が「トレードオフとして受け入れる」としたC-1（評価軸追加コストの二重化）は、単なる受け入れで終わらず3段階の設計反復（T221 Stage B/C→T292→T348→T349→T350）で構造的に解消された。特にT292による専用レシピ機構の全廃は、変更コスト表の複数区分（G'/B'/H）を一挙に消滅させる稀に見る単純化。一方、この激動の副産物として3ファイル（`AxisComposer.tsx`・`road_graph_repository.py`・`road_graph_engine.py`）が閾値監視の外側で成長している。

### 規模ウォッチ表

**Backend**

| ファイル | 今回 | 前回 | 増分 | 発火 |
|---|---|---|---|---|
| `infrastructure/road_graph_repository.py` | 2,432 | ~2,030 | +19.8% | ○ |
| `services/road_graph_engine.py` | 842 | 675 | +24.7% | ○ 新規上位登場 |
| `domain/material_catalog.py` | 746 | 0（新設） | 新規 | ○ |
| `domain/evaluation.py` | 733 | ~760 | -3.6% | 未発火 |
| `domain/axis_definitions.py` | 630 | 0（新設） | 新規 | ○ |

参考: `domain/difficulty.py`は~300行→95行（-68%、T221 Stage B/Cで責務移動、問題なし）。

**Frontend**

| ファイル | 今回 | 直近実測(T284,08-24) | 増分 | 発火 |
|---|---|---|---|---|
| `components/AxisStudio/AxisComposer.tsx` | 1,123 | 474 | **+137%** | ○ 急成長 |
| `components/Map/MapView.tsx` | 2,496 | 2,489（閾値2,800） | +0.3% | 未発火 |
| `app/page.tsx` | 1,792 | 1,773（閾値1,900） | +1.1% | 未発火 |

page.tsxのuseState等は36件→**13件**（T299以降のフック抽出が奏功、T284が懸念した閾値到達リスクは顕在化せず）。

### 変更コスト表（今回 vs 前回）

| Case | シナリオ | 前回 | 今回 | 評価 |
|---|---|---|---|---|
| G | 評価軸追加（既存4shape種） | スカラー2箇所＋配列3箇所 | **migration SQL 1本のみ** | 大幅改善（C-1解消） |
| G' | レシピ付き軸追加 | 64ファイル・+3,677行(T119) | **区分自体が消滅**（T292） | 区分解消 |
| B' | レシピ段階数変更 | 19ファイル(T117) | **区分自体が消滅** | 区分解消 |
| H | レシピ項目追加 | 12〜15箇所 | **区分自体が消滅** | 区分解消 |
| （新規）表示専用の一次属性追加 | — | — | backend1箇所＋タイル世代＋frontend約8ファイル | 変化なし（T285トリガー待ち） |

### Findings

**[P2] C-1（今回番号: F-1）. `AxisComposer.tsx`が3日間で474→1123行（+137%）へ急成長、閾値未登録**
- Evidence: `fc7bd5a`（2026-08-24新設、474行）→現在1,123行。`renderShapeParamsStep`（658-1001行、343行）が単一関数内で5つのshape種別ごとに異なるUIを分岐（別々の変更理由が同居）。
- Recommendation: (a)shape種別ごとのサブコンポーネント分割、または(b)閾値付きKEEP登録。churnが継続中のため、まず(b)で監視下に置き、churn沈静化後に(a)を判断することを推奨。
- Scope: S（登録）〜M（分割） / Confidence: High

**訂正（T355着手時、2026-08-27）**: 上記Evidenceの「5つのshape種別」は誤り。
`frontend/src/components/AxisStudio/AxisComposer.tsx`の`ShapeKind`型・
`renderShapeParamsStep`の分岐条件を実装で確認したところ、実際は4種
（breakpoint_linear/recipe_then_breakpoint_linear/categorical/flag_sum）のみで、
`priority_condition`への言及・分岐は存在しない（`grep -n "priority_condition"`は
0件）。行数急成長・閾値未登録というFinding本体の指摘とRecommendationは変わらないが、
種別数の記述のみ4種へ訂正する。改善計画T355の閾値提案も「5つ目のshape種到達」へ
修正済み。

**[P3] F-2. `road_graph_repository.py`・`road_graph_engine.py`が揃って+15%超成長、個別閾値が未登録のまま2回連続の「最大ファイル」指摘に**
- Evidence: repository 2,030→2,432行（+19.8%）、engine 675→842行（+24.7%）。責務別4クラス＋ファサード構造・単一エンジンクラスの境界はいずれも維持（構造は健全）。
- Recommendation: 両ファイルへ閾値付きKEEPを新規登録（repository 2,800行、engine 1,100行を提案）。現時点で分割は不要。
- Scope: S / Confidence: High

**[P3] F-3. `history/2026-08-26_complexity.md`が改善計画T349の根拠として引用されているが、history/に一度も保存されていない**
- Evidence: `docs/improvement-plan.md:7667`（T349）がこのファイルをF-1・P0の根拠として引用しているが、`git log --all --diff-filter=A`で全ブランチ検索してもこのファイルは一度も存在しない。
- Impact: 実害は限定的（T349自体は独立の実測根拠[DB読み込み44ms]も持つため結論は揺るがない）が、principles.md標準手順5「結果はhistory/へ保存する」が守られなかった一例。
- Recommendation: 当時のセッション記録が残っていれば遡及コミット、残っていなければimprovement-plan.mdのT349該当箇所へ「レビュー結果ファイルは未保存」と注記。
- Scope: S / Confidence: High（不在の事実）

### KEEP（complexity）
- T221 Stage B/C（軸定義のレジストリ駆動評価、shape種別ベースの汎用関数化。C-1を構造的に解消）
- T292（car_stressレシピ機構全廃・内部軸階層への統合。G'/B'/H区分自体を消滅させた）
- T350削除禁止ガード（3行の最小実装、対症療法の自覚とT352への導線を明記）
- T348→T349→T350の3段階の設計反復そのもの（「テストが遅くなる」という当初根拠を自ら44ms実測で再検証・撤回した点が事実と推測を分ける姿勢と一致）
- T335（ST_IntersectionのgridSize指定。複数の仮説を実測で棄却し誤修正を1度revertした記録も残る実測駆動の模範例）
- T299（Tailwind限定移行、使い分け基準の文書化）
- page.tsxのstate分散（useAxisCatalog等へのフック抽出、36→13件）

### Keep List更新案
- 新規追加提案: T221 Stage B/C以降の軸評価汎用化、T292のレシピ機構全廃、T299のTailwind限定移行
- 新規閾値提案: `AxisComposer.tsx`（1,400行 or 5つ目のshape種到達）、`road_graph_repository.py`（2,800行）、`road_graph_engine.py`（1,100行）
- 既存閾値（page.tsx 1,900行・MapView.tsx 2,800行）は据え置き（今回未発火）

### 前回指摘の解消状況
C-1「評価軸追加の1本道がベクトル化で二重化」→ **解消済み（上位互換の解決）**。T221 Stage B/Cで解消し、さらにT292でG'/B'/H区分自体が消滅するという、前回指摘の範囲を超えた進展。

---

## Phase 4: 設計・実装・テスト整合性レビュー（consistency・サブエージェント実施）

### Executive Summary

T350の完了報告9項目は**すべてコードで裏付けが取れた**（1項目[backend全1287件green]のみworktree環境の制約でConfidence Medium）。XL規模の一括変更としては非常に精度の高い完了報告。T347（bicycle_infra削除）・T316（route_preference.yaml撤廃）も残骸なし。一方、**T332（軸スタジオのウィザード化）でarchitecture.mdの追従漏れを新規発見**（過去5回以上再発している同型パターン）。

### T350完了報告9項目の検証結果

| # | 主張 | 検証結果 |
|---|---|---|
| 1 | AXIS_DEFINITIONSをPython literalから`{}`へ | **一致** |
| 2 | `generate_axis_migration_sql.py`削除 | **一致** |
| 3 | `axis_registry_service.py`差分ログ撤去 | **一致** |
| 4 | `test_migrate.py`が構造検証のみ | **一致** |
| 5 | CLAUDE.md・architecture.md更新 | **一致**（ただしT332は別途未追従、F-1参照） |
| 6 | `realistic_axis_fixtures.py`が8ファイルで使用 | **一致** |
| 7 | CI `api-contract`へpostgresコンテナ＋bootstrap_ci_db.py追加 | **一致** |
| 8 | 削除禁止ガード実装 | **一致**（テスト網羅は一部、F-2参照） |
| 9 | backend全1287件green | 未実行検証（DB/venv未整備）、静的関数数カウント(1256)は矛盾なし。**Confidence Medium** |

OpenAPI生成物ドリフト: T350コミット単体で`api.d.ts`・`openapi.json`のみ変更（11行差分）、`axis-catalog.json`は無変更を確認——完了報告の主張と整合。

### Findings

**[P2] F-1. architecture.mdのAxisComposer.tsx記述がT332（ウィザード化）に追従していない**
- Evidence: `docs/architecture.md:526`は変更前の姿（単一フォーム、「テンプレート選択」という表現）のまま。`AxisComposer.tsx:331-332`は`STEPS = ["basic","shape_kind","shape_params","display_publish"]`という明確な4ステップウィザード。`architecture.md`内`grep -n "ウィザード\|T332"`は0件。
- Impact: architecture.mdを一次情報とする後続作業が実際のUI構造を誤認するリスク。
- Root Cause: CLAUDE.mdの「規模M以上でAPI・ドメイン概念・レイヤー種を新設するタスクはarchitecture.md追従を完了条件に含める」ルールが、既存フロントコンポーネントの構造再設計（規模L、UI導線の抜本変更）には明確に運用されていなかった可能性。
- Recommendation: `docs/architecture.md:526`をウィザード構成・カード選択・ステップ単位検証が分かる内容へ更新。
- Scope: S / Confidence: High

**[P3] F-2. T350の削除禁止ガード（car_stress/night/wind/gradient）のテストがwind・gradientをカバーしていない**
- Evidence: `_CODE_COUPLED_AXIS_IDS`は4axis_id対象だが、`test_axis_registry_service.py`は`car_stress`・`night`のみ検証。
- Impact: 低（4件とも同じ1行の分岐ロジックを通るため、構造上ほぼ保証される）。ただし将来の分岐ロジック変更時にwind/gradientの回帰だけ検知漏れになるリスク。
- Recommendation: parametrize化またはwind・gradientの回帰テストを追加。
- Scope: S / Confidence: High

### T347（bicycle_infra完全削除）・T316（route_preference.yaml撤廃）確認結果
両タスクとも残骸なし、KEEP。bicycle_infraの残存grep結果は全て履歴コメントまたは別概念（`bicycle_infra_dedicated`・`bicycle_infra_quality`）。

### KEEP（consistency）
- T350のPython literal撤去とDB一本化（実装・テスト・docsの三者が高精度で一致）
- T347のbicycle_infra完全削除、T316のroute_preference.yaml撤廃

### Regression / Previous Findings
- 前回`2026-08-23_consistency.md` F-1・F-2 → 解消済み（T233/T234、再発なし）
- `2026-08-23_all_2.md` Phase4 F-1（性能目標未達） → **T248（2026-08-24）で対応済み**
- `2026-08-23_all_2.md` Phase4 F-2（`.env`未作成環境での起動失敗） → 未対応のまま（前回も「対応不要〜任意」の低優先度判定、新たな悪化ではない）
- architecture.md未追従5回以上の類型 → **F-1として新規に同型再発を検出**（T332）

---

## Phase 5: UI/UXレビュー（ui・本エージェント自身が実機確認）

### 実施方法と範囲

対象範囲が非常に広い（222コミット）ため、**変更が最も大きく・直近のUIレビュー（2026-08-25、軸スタジオ限定）からの差分が最大の画面**に絞って実機確認した（絞ったことを明記）:
1. メインのルート生成フロー一巡（初回表示→ルート生成→空状態表示→モバイルタブ切替）、デスクトップ1280px・モバイル390px両方
2. 軸スタジオ（`/admin`）の新規作成ウィザード（T332で全面再設計）、デスクトップ・モバイル両方
3. 前々回UIレビュー（2026-08-25）のF-1〜F-5がT332後も解消されたままか（コード確認＋一部実機確認）

dev環境（backend:8000・frontend:3010、ローカルPostgreSQL稼働）を起動し、Playwright（Chromium、自前スクリプト。ユーザー既定方針によりClaude Browserペインは不使用）で確認後、確認終了時にサーバーを停止した。

### 実機で確認できたこと（KEEP）

- **メインフローの空状態表示**: 距離30km/20km/15kmいずれも「条件に合うルート候補が見つかりませんでした。距離を変えて試してください。」という明確なメッセージが表示され、「壊れているのか操作が足りないのか」が伝わる（原因はdev DBのroad_graphタイル取込範囲外——後述のRegression参照、UIの問題ではない）。デスクトップ・モバイルとも一貫。コンソールエラーなし。
- **モバイルタブ再編（T300）**: 「ルート設定」「ルート結果」「地図の見え方」の3タブがボトムシートで機能し、390px幅で横スクロール・レイアウト崩れなし。タブ切替後も空状態メッセージが一貫して表示される。
- **軸スタジオウィザードのテンプレート選択（ステップ2/4）**: 前々回UIレビューF-2が指摘した「区分線形補間」「線形結合」等の数式的語彙が**画面から消え**、「はい/いいえ、または種類ごとに点数を決める」「数値の大きさに応じて点数を変える」「複数の要素の有無を数えて減点・加点する」「他の軸を重みで足し合わせて点数を変える（nX + mYのように）（上級者向け）」という利用者視点の文言＋具体例（「例: 一方通行かどうか、自転車専用道かどうか」等）へ全面置換されている。実機確認（デスクトップ1280px・モバイル390px、スクリーンショット`12_desktop/mobile_wizard_step2.png`）で確認、コンソールエラーなし。技術用語（区分線形補間等）は主要文言としては露出せず、F-2の推奨を上回る対応（括弧書き併記ではなく完全な言い換え）。
- **軸一覧の材料ラベル解決**: 「自転車インフラ」軸のサマリが「車ストレス内部軸: 自転車インフラ補正」という解決済みラベルで表示され、生のsnake_case識別子は露出していない（前々回F-3の解消を実機で再確認）。
- **公開済み軸の削除ボタン**: 無効化＋説明ツールチップ（「公開済み軸は削除できません（先に「非公開に戻す」を押してください）」）付きで表示され、誤操作を防ぐ設計。

### コードレベルで確認したこと（実機クリックでの再現は後述の理由で未達、Confidence: High（コード）/ 実機操作Confidence: 未検証）

- **内部軸削除時の依存関係警告（T323、前々回F-1訂正後の推奨に対応）**: `AxisStudio.tsx:127-134`に、削除対象の軸が他の軸から材料として参照されている場合「この軸は次の軸から材料として参照されています: {names}\n削除すると、それらの軸が正しく評価できなくなります（評価対象外になります）。\n本当に削除しますか？」という`window.confirm`ダイアログを表示するコードを確認した。これは前々回レビューの訂正1（「一律拒否ではなく、削除前に参照元とその影響を明示する」）の推奨と文言レベルで一致する。ただし本レビューの実機クリック検証は、テストスクリプトのセレクタが同一ページ内の別軸（公開済み軸、無効化ボタン）を誤って選択してしまい30秒でタイムアウトしたため未達（**製品側の不具合ではなくテストスクリプト側の選定ミス**、再現条件を変えれば追試可能）。
- **折れ点(breakpoints)のスコア向き説明（前々回F-5）**: `AxisComposer.tsx:779`に「スコアは0(最も走りやすい)〜100(最も走りにくい)で入力します。値が大きいほど走りにくくしたければ右肩上がりに、走りやすくしたければ右肩下がりに折れ点を設定してください。」という説明文を確認（779・855・930行の3箇所、flag_sum欄にも同旨あり）。F-5の推奨どおり実装済み。
- **カテゴリ値テンプレートの多値対応表記（前々回F-4）**: T332のウィザード全面書き換えにより、F-4が指摘した「真偽2値」という古い`<option>`文言自体が新UIから消滅（ドロップダウン→カード選択への転換で該当箇所ごと置換）。F-4は個別修正ではなくT332に吸収される形で解消。

### Findings
該当なし（前々回UIレビューF-1〜F-5はいずれも解消済みで新規の劣化なし。新規に発見したUI問題はゼロ）。

### 実機確認できなかった範囲（明記）
- ルート生成の候補比較・区間色分け・区間インスペクタ（この session のdev DBでは30km/20km/15kmいずれもroad_graphタイル未カバレッジで候補0件となり到達できなかった。既知事象T318[起点(35.75,139.74)でdistance_km=25/30が候補0件、20kmはヒットする場合がある]と同系統だが、今回は20kmでも同じログ`Road Graphタイル取込範囲外`が出ており、T318記録時点より悪化・変動している可能性がある。ただしT318は「深追いせず、本番debug_modeでの再検証待ち」として既に保留判断済みであり、本レビューでは新規指摘とせずRegressionで扱う）。
- 軸スタジオウィザードのステップ3/4（材料選択の実操作・折れ点の実際の入力UI操作）は、上記の理由で実機クリックまでは至らず、コード確認に留まる。
- ダークモード（対象範囲内でのダークモード関連変更なしのため省略）。

### Regression / Previous Findings

| 前々回UIレビュー(2026-08-25)指摘 | 評価 |
|---|---|
| F-1（P0、内部軸削除の依存関係不可視性） | **解消済み（コード確認High、実機クリックは今回未達）**。T323実装のconfirm()ダイアログが訂正1の推奨と一致する文言で存在 |
| F-2（P1、変換テンプレートの数式語彙） | **解消済み、推奨を上回る対応**。実機確認済み（デスクトップ・モバイル） |
| F-3（P1、car_stressサマリの生識別子漏れ） | **解消済み**。実機確認済み |
| F-4（P2、カテゴリ値テンプレートの文言不整合） | **解消済み（T332に吸収される形で該当箇所ごと消滅）** |
| F-5（P2、breakpointsのスコア向き説明欠如） | **解消済み**。コード確認済み |

T318（distance_km=25/30で候補0件、既知・保留中） → **今回のdev環境では15km/20km/30kmいずれでも再現**（`Road Graphタイル取込範囲外`ログ、bbox実測値付き）。新規指摘とはしないが、T318の保留判断（「本番debug_modeでの再検証待ち」）を継続する上での参考データとして記録する。

---

## Phase 6: 相互確認・矛盾と判断

**矛盾は検出されなかった**。overall（構造・残骸の存在検出）・complexity（複雑さの配置適否・変更コスト）・consistency（設計/実装/テストの整合）・ui（実機での理解可能性）の4レンズは、いずれも同じ結論——**T348→T349→T350・T292という一連のXL級転換が、前回までの負債（C-1・前回F-1〜F-4等）を構造的に解消しながら実行された、過去最高水準の期間**——を異なる角度から裏付けており、相殺すべきトレードオフも見当たらなかった。

**重複統合1**: complexity F-3（`history/2026-08-26_complexity.md`の不在）は、本レビューのPhase 1調査中に本エージェント自身も独立に同一の事実（`docs/improvement-plan.md:7667`・`docs/architecture.md:1131`の2箇所から引用されているが、全ブランチのgit履歴に一度も存在しない）を発見していた。2つの独立した経路（サブエージェントの複雑度レビュー・本エージェントの一次調査）が同じ結論に達したため、Confidenceを最高水準（High、独立して2回確認）として統合Findingへ採用する。優先度はcomplexity側の判定（P3）を踏襲する——実害は限定的（T349の結論自体は別の実測根拠でも支持されている）ため。

**重複統合2**: overall O-1（architecture.md肥大化トリガー発火）とconsistency F-1（architecture.mdのT332追従漏れ）は、根本原因が異なる別事象（前者はファイル全体の肥大化という「量」の問題、後者は特定セクションの陳腐化という「質」の問題）だが、いずれも「architecture.mdの保守が変更速度に追いついていない」という同じ構造的傾向の別の症状である。個別のFindingとして維持しつつ、Phase 7でこの傾向を横断的に指摘する。

**トレードオフの評価**: complexity F-1（AxisComposer.tsxの分割 vs 閾値登録）は、サブエージェント自身が「churn継続中は分割よりも閾値登録を優先」という判断を既に下しており、本レビューもこれを支持する（研究フェーズ・個人開発という現在の開発フェーズを踏まえ、まだ変化し続けているコードを今すぐ固定化する構造変更を急ぐ必要はない）。

**相殺の確認**: 統合されたFindingsのRecommendationはいずれも既存のKEEP判断を壊さない（architecture.md更新はdocsのみ、閾値登録は監視の追加のみ、テスト追加は既存実装への追記のみ）。

---

## Phase 7: 最終統合判断

### 統合Findings（優先度順）

1. **[P2] 統合-1. architecture.mdのAxisComposer.tsx記述がT332（ウィザード化）に追従していない**（consistency F-1）: `docs/architecture.md:526`をウィザード構成・カード選択・ステップ単位検証が分かる内容へ更新する。
2. **[P2] 統合-2. `AxisComposer.tsx`が3日間で+137%へ急成長、閾値未登録**（complexity F-1）: 閾値付きKEEPを新規登録（1,400行 or 5つ目のshape種到達）。分割はchurn沈静化後に判断。
3. **[P3] 統合-3. レビュー基盤の欠落——`history/2026-08-26_complexity.md`が2箇所から引用されているのに未保存**（complexity F-3、本エージェントも独立に確認）: 遡及コミットまたはimprovement-plan.mdへの注記。
4. **[P3] 統合-4. `road_graph_repository.py`・`road_graph_engine.py`が揃って+15%超成長、閾値未登録**（complexity F-2）: 両ファイルへ閾値付きKEEPを新規登録（2,800行/1,100行）。
5. **[P3] 統合-5. T350削除禁止ガードのテストがwind・gradientをカバーしていない**（consistency F-2）: parametrize化またはテスト追加。
6. **[P3] 統合-6. architecture.md肥大化のトリガー（T286）が発火済みだが未着手**（overall O-1）: 着手判断をユーザーへ促す（起票済み、新規起票不要）。

**横断的傾向（Phase 6重複統合2の帰結）**: 統合-1・統合-6はいずれも「architecture.mdの保守が変更速度に追いついていない」という同根の傾向であり、統合-3（レビュー履歴の欠落）も広義には同じ「記録の鮮度管理」というカテゴリに属する。個々の実害は軽微だが、3件同時に検出されたことは、このプロジェクトが繰り返し自認してきた「ドキュメント同期漏れ」パターン（CLAUDE.md冒頭に明記済み）が、コード側の設計原則遵守が向上した現在も、記録・文書の領域では依然として残存していることを示す。

### 実施順序案

```text
Phase A（記録の整合、即時可能、いずれもS規模）
  ├─ 統合-1: architecture.md AxisComposer.tsx節の更新
  ├─ 統合-3: history/2026-08-26_complexity.mdの不在について注記または遡及復元
  ├─ 統合-5: wind/gradient向け削除禁止ガードのテスト追加
  └─ 統合-6: T286着手判断をユーザーへ確認（対応そのものは既起票）

Phase B（境界の固定、閾値登録のみ、いずれもS規模）
  ├─ 統合-2: AxisComposer.tsxへ閾値付きKEEP登録
  └─ 統合-4: road_graph_repository.py・road_graph_engine.pyへ閾値付きKEEP登録

Phase C（判断待ち、内部の再配置、トリガー到達後）
  └─ AxisComposer.tsxの分割（renderShapeParamsStepのサブコンポーネント化）は
     統合-2の閾値到達 or churn沈静化のいずれか早い方を待つ
```

### スコア（統合Findingsベース、算定基準は2026-08-23_all_2.md・2026-08-25_overall.mdと同一: P1×15/P2×5/P3×1）

- 今回の指摘負債: P2×2 + P3×4 = 10+4 = **14** → 健全度 **86/100**
- 観点別: overall **99**（P3×1）/ complexity **89**（P2×1+P3×2）/ consistency **94**（P2×1+P3×1）/ ui **100**（新規指摘ゼロ、前々回5件全解消）

| 回 | 実施日 | 対象範囲 | 統合Findings | 負債 | 健全度 |
|---|---|---|---|---|---|
| 第1回 | 2026-08-16 | 基盤構築後初回 | P1×1 P2×3 P3×2 | 32 | 68 |
| 第2回 | 2026-08-17 | 〜T105ほか | P1×1 P2×2 P3×1 | 26 | 74 |
| 第3回 | 2026-08-18 | 〜T130ほか | P1×1 P2×3 P3×1 | 31 | 69 |
| 第4回 | 2026-08-19 | 21+コミット | P1×2 P2×5 P3×1 | 56 | 44 |
| 第5回 | 2026-08-22 | 108コミット | P1×2 P2×4 P3×1 | 51 | 49 |
| 第6回 | 2026-08-23 | 27コミット | P1×2 P2×2 P3×1 | 41 | 59 |
| 第7回 | 2026-08-23 | 30コミット | P2×2 P3×1 | 11 | 89 |
| **第8回** | **2026-08-27** | **222コミット** | **P2×2 P3×4** | **14** | **86** |

（参考: 第7回〜第8回の間に単発の`review:overall`（2026-08-25第9回、86/100）・`review:ui`（2026-08-25、軸スタジオ限定、P0×1 P1×2 P2×2で起票→全件同日実装完了）が実施されている。これらは`review:all`のトレンドとは別集計だが、本表の連続性の参考として記す。）

**P0/P1は本回もゼロ**（2回連続）。222コミットという過去最大規模の対象範囲にもかかわらず健全度86は前回89に近い高水準を維持しており、要因は(a) T292・T350という2件のXL級構造転換がいずれも高精度な自己検証（実測・突き合わせ・完了報告の裏取り）付きで実行されたこと、(b) 前回・前々回レビューの指摘（C-1、F-1〜F-4×2セット）が新規根拠なしの再指摘ゼロで全件解消されたこと、(c) 残る指摘6件がいずれもコード品質ではなく「記録・監視の登録漏れ」という、実装の正しさとは独立した運用面の軽微事項に限定されたこと。

### 全体として最もバランスが良い状態

この222コミットの期間は、「複数管理はやめてシンプルにして」というユーザーの一貫した要望に対し、いったん妥協案（T348第三案）を採用→実測で前提が崩れたことを自ら認めて差し戻し（T349）→最終的にフル版へ到達（T350）という、意思決定の往復も含めて全て記録に残しながら、評価軸システムの二重管理を構造的に解消しきった期間だった。同時にT292でレシピ機構という並行サブシステムを丸ごと削除し、複雑度平衡性レビューが指摘した変更コスト表の3区分（G'/B'/H）を消滅させている。これらはいずれも「複雑さゼロを目指さない」という原則に照らしても正しい単純化——不要だった複雑さ（二重管理・専用レシピという並行系統）を削り、必要な複雑さ（domain層の評価ロジック・fail-fast設計）は維持している。

UI面でも、前々回UIレビューが指摘した5件（内部軸削除の不可視性・専門用語・生識別子漏れ・文言不整合・スコア向き説明欠如）が、個別修正の寄せ集めではなくT332という1つの一貫したウィザード再設計に統合されて解消されており、「軸スタジオの利用者は事実上開発者自身」という現フェーズの制約下で妥当な優先順位判断だったと評価できる。

残る課題はコード・設計の健全性ではなく、**記録の鮮度管理**という一点に収束している——architecture.mdの特定セクション未追従（統合-1）、architecture.md全体の肥大化トリガー未着手（統合-6）、そしてレビュー結果自体がhistory/へ保存されなかった一例（統合-3）。これらはいずれもS規模で即座に対応可能であり、次に大きな設計転換が入る前（architecture.mdが2,000行を超えた今、T286のトリガーは既に発火済み）に手当てするのが最もバランスの良い次の一手である。新機能の追加や軸スタジオのさらなる拡張は、これらの記録整合を待つ必要はなく並行して進めてよい。

---

## 起票案（P2/P3のみ、ユーザー承認後にimprovement-plan.mdへ。T番号は起票時に確定）

**対応状況（2026-08-27追記、ユーザー承認により全件起票済み）**:

- 起票案A→**T354**〔P2・S〕: `docs/architecture.md`のAxisComposer.tsx記述をT332後のウィザード構成へ更新。
- 起票案B→**T355**〔P2・S(登録)〜M(分割)〕: `AxisComposer.tsx`へ閾値付きKEEPを登録（1,400行 or 5つ目のshape種到達）。churn沈静化後にshape種別ごとのサブコンポーネント分割を検討。
- 起票案C→**T356**〔P3・S〕: `history/2026-08-26_complexity.md`の不在について、遡及コミットまたはimprovement-plan.md T349エントリへの注記。
- 起票案D→**T357**〔P3・S〕: `road_graph_repository.py`（2,800行）・`road_graph_engine.py`（1,100行）へ閾値付きKEEPを登録。
- 起票案E→**T358**〔P3・S〕: T350削除禁止ガードのテストをwind・gradientまで拡張（parametrize化）。
- 起票案F→**T286の既存エントリへ状況記録を追記**（新規タスクは起票せず、トリガー発火の事実のみ記録。着手はユーザー承認後）。

---

## 後記

- 本レビューは対象範囲が非常に大きかった（222コミット・329ファイル）ため、Phase2〜4を3体のサブエージェントへ並行委任し、本エージェントはPhase1の全体把握・Phase5の実機UI確認・Phase6〜7の統合判断に専念する構成を取った。各サブエージェントには専用worktree・コード変更禁止・HEAD一致確認を徹底させ、結果はいずれも具体的なファイルパス・行番号付きのEvidenceを伴っていた。
- レビュー実施中、devサーバー（backend:8000・frontend:3010）を一時起動してPlaywright実機確認を行い、確認終了後に停止済み。作業ツリーへの変更は一切行っていない。
