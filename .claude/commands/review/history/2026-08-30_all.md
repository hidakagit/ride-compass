# 統合レビュー（2026-08-30・第9回）

- 実施日: 2026-08-30
- レビュー種別: all（overall/complexity/consistency/uiを段階実施→Phase6相互確認→Phase7統合判断）
- **対象コミット（HEAD固定）: `a5d5d51`**（コミットメッセージ「T418更新: T414完了により実装済みのWindBearingSlider/windPenalty配線を踏まえた補足」）。`git fetch`実施済み、ローカルmaster＝origin/master先端で一致（差分ゼロ）を確認済み。
- **UIレンズ（Phase5）のみ対象コミットが1つ先の`470bf32`**（「T414実装のクリーンアップ: 配色式・8方位ラベルの重複解消とコンパススライダーのデバウンス追加」）。独立worktreeで並行実施していたため生じたズレ。`git merge-base --is-ancestor a5d5d51 470bf32`で祖先関係を確認済み、`git diff --stat a5d5d51..470bf32`は8ファイル・+86/-58行でいずれも風（T414）関連ファイルのみ（`wind_way_service.py`・`page.tsx`・`windAxisLayer.ts`・`windPenalty.ts`・`WindBearingSlider.tsx/test.ts`・`useDynamicWeatherLayers.ts`・`useWindAxisPenalties.ts`）。他3レンズの対象範囲（`f7b8f5e..a5d5d51`）を実質的に超えない差分であり、UIレンズの実施内容に影響しないと判断した。
- 対象範囲: 前回統合レビュー（第8回、`f7b8f5e`）以降**173コミット・464ファイル・+36,387/-12,791行**。前回all実施（2026-08-27）から本レビューまで12日間・約70タスクが未レビューのまま経過しており、`.claude/commands/review/README.md`「定期的なレビュー」節の新設トリガー（7日経過 or 20タスク完了、いずれか早い方／分割元タスク完了直後）に複数の観点から抵触していたことが実施の背景。
- 実施場所: Phase2〜4（overall/complexity/consistency）はこのレビュー用worktree（`.claude/worktrees/agent-ad4b1b44ba1495c64`）で3体のサブエージェントへ分担実行。Phase5（UI）は環境構築の競合を避けるため別の独立git worktreeを持つ4体目のサブエージェントへ委任し、Playwright（Chromium、headless、自前スクリプト。Claude Browserペインは不使用——ユーザー既定方針）で実機確認した。
- **サブエージェント結果の授受経路について**: 4体のサブエージェントの完了報告は、本エージェントへの直接到達ではなく、本セッションの上位（起動元）を経由して中継される形になった（`SendMessage`仕様上、末端subagentからの通知が起動元へ向かう経路がある模様）。中継内容をそのまま採用せず、本エージェント自身が一次情報（実ファイルの`Read`・`Grep`・`git log`・`wc -l`等）で以下を独立に裏取りしてから採用した: (1) `backend/pytest.ini`の内容とマーカー未登録、`backend/tests/`配下11ファイルの`xdist_group(name="postgis")`使用実態・`@pytest.mark.postgis`不在（consistency F-2の核）、(2) `docs/architecture.md`の該当行（1444行・2298-2301行）の実際の文言、(3) `MapView.tsx`(2,992行)・`AxisComposer.tsx`(1,345行)・`MapOverlayControls.tsx`(1,490行)・`page.tsx`(1,726行)・`icons.tsx`(644行)の実測行数、(4) `470bf32`の実在・親子関係・差分内容、(5) `history/2026-08-26_complexity.md`の不在とT356の実際の解決方法（ファイル復元ではなく2箇所への注記追加）。いずれも中継内容と一致した。
- **コードは変更していない（全Phaseとも読み取り専用調査）。**

## Phase 1: 全体把握

173コミットの主要テーマ（新しい順）:

1. **T400（規模L・分割元、直近・最重要）**: 「地図×評価軸の連動モデル再設計」をT401〜T406へ分割し全完了（2026-08-30）。RouteScorerのdistance/difficulty2指標化（T401）、`Route.axis_difficulties`新設とBottomSheetプロファイル表示（T402）、区間クリック内訳の小型チャート（T403）、`display_override`廃止（T404）、動的材料の二重表現（T405）、パネル構成を「道路/評価軸/環境/スポット」4チップ・3排他ドメインへ再編（T406）。
2. **T405→T414の自己修正**: T405（風のway_id→wind_penalty配信、Redis経由）は実装後に「道路自身のOSM格納方向を使う」という前提が誤りだったと判明し、T414で「動的材料の状態別表現契約」を材料非依存の一般契約へ確立し作り直した。旧実装の残骸なく撤去済み（Phase4で検証済み）。
3. **T417撤回→T418/T419/T421/T422/T423（本レビュー時点で未完了のまま並行進行中）**: 評価軸チップの展開方向変更（T417）は実装前に撤回され、「評価軸チップを地図UIから撤去しルート設定/結果パネルへ統合する」方針（T418）へ差し替え。T419（レイアウト見直し）・T421（結果パネルの評価軸カタログ駆動化）・T422（環境グループのプレースホルダー）・T423（勾配の評価軸実装、動的材料契約の2例目）はいずれも起票済み・未着手。
4. **T407/T408/T410/T412/T416/T420**: 気象庁の動的タイル系レイヤー（降水短時間予報rasrf・キキクル・線状降水帯予測マップ調査）の追加、バックエンド経由化（プロキシ+キャッシュ）、既定ON化。
5. **T396/T397**: 軸スタジオの評価軸合成ロジックを4テンプレートから2プリミティブ+合成へ再設計、UIを4カードから3カード（なめらか/ぴったり/かけあわせ）へ再設計。
6. **T395**: 道路交通センサスデータ活用可否調査（最終判断: 見送りを推奨）。
7. **T398/T399**: 風グリッドキャッシュのSQLite→Redis一本化、標高データ永続化調査（結論: 既に完了済み・追加設計不要）。
8. **T351**: 派生データの系譜追跡列（`source_*_import_run_id`・`algorithm_version`）新設。
9. **CLAUDE.md自体の階層化**（0c9f5e2・3ba3d1a・1fa573e）: 「ドキュメント階層」節の新設（何をCLAUDE.md本体・docs/・レビュー基盤・タスクファイルのどこへ書くかの基準）、`docs/workflow.md`新設、レビュー定期実施の閾値明文化（本レビューの実施根拠そのもの）。

過去レビュー（`history/2026-08-27_all.md`ほか）が繰り返し指摘してきた「architecture.mdの記録鮮度管理」という横断的傾向は本期間も継続しており、Phase 6で詳述する。

---

## Phase 2: 全体最適レビュー（overall・サブエージェント実施）

- 対象コミット: `a5d5d51` / 前回基準: `f7b8f5e`

### Executive Summary

本期間（173コミット）の主軸はT400「地図×評価軸の連動モデル再設計」で、T401〜T406へ分割し全完了した。特筆すべきはT405→T414の自己修正——実機フィードバックを受けてT405の設計前提（道路自身のOSM格納方向を使う）が誤りだったと判明し、T414で契約を材料非依存へ作り直した。旧実装は残骸なく撤去済み。2026-08-30新設の「分割元タスク完了ゲート」がT422・T423を自己検出。architecture.mdはT406・T412・T414について追従済みだが、T286（2,148→1,929行）以降再び2,545行（+32%）まで肥大化しトリガー再発火。

### Findings

**[P3] O-1. architecture.mdの肥大化トリガーがT286完了後に再発火**
- Evidence: 現在2,545行（T286完了直後1,929行から+616行、+32%）。
- Recommendation: 次の閑散期にT286と同型の「経緯記述をdecisions/へ追い出す第3弾」を検討。緊急性は低い。
- Scope: M / Confidence: High

### KEEP

T405→T414の自己修正、T351（汎用テーブル新設見送り＋CHUNK_SIZE調整の実測裏取り）、T404のdisplay_override廃止（3段階の意図的併存＋削除条件明記）、windAxisのid特例分岐（T411トリガー射程内）、T400の分割元タスク完了ゲート自己検出、T393/T394（網羅的grep＋実測裏取り）、T401のRouteScorer単純化。

### REMOVE / SIMPLIFY / REFACTOR / EXTEND

該当なし。

### DEFER

本レンズ固有の新規DEFERなし。T411・T409は既存起票のまま。

### Regression / Previous Findings

前回O-1（P3、T286で解消）→本期間で再発火、新規O-1として再掲。RouteList.tsx/ComparisonPanel.tsxのレガシーフィールドべた書きを独自に発見しかけたが、T421として既に起票済みと確認、新規Finding化せず。

### スコアサマリ

| 指標 | 値 |
|---|---|
| P0件数 / P1件数 / P2件数 / P3件数 | 0 / 0 / 0 / 1 |
| 総合スコア（100点満点） | 99 |
| 前回同種レビューからの差分 | 初回記録（新算出式のため比較不可） |
| REMOVE/SIMPLIFY/REFACTOR件数 | 0 |
| DEFER件数（トリガー未到達） | 0（本レンズ固有分。既存T411/T409は集計対象外） |

### Overall Judgment

T400という単一L規模タスクを6分割し全完了、前回（222コミット・86/100）に匹敵する規律。T405→T414の自己修正、分割元タスク完了ゲートの自己検出実績が肯定的。残る課題はarchitecture.mdの記録鮮度管理（P3）のみ。

---

## Phase 3: 複雑度平衡性レビュー（complexity・サブエージェント実施）

- 対象コミット: `a5d5d51` / 前回基準: `f7b8f5e`

### Executive Summary

T400系列（T401〜T414・T421〜T423）は複雑さの削減が明確に上回った期間: AxisShapeは4テンプレート→2プリミティブ(T396)、AxisComposer UIは4カード→3カード(T397)、評価軸追加コストはAPI呼び出し1回・コード変更ゼロへ短縮。T414は道路自身のway単位クエリ・per-way Redisキーを撤去しタイル単位の単純設計へ差し替え、複雑さが正しく削減された好例。一方MapView.tsxが今回初めて閾値超過（行数2,800→2,992、DYNAMIC_WEATHER_RENDERERS種類数5→8）。docs/architecture.mdはT286後も2,146→2,545行(+18.6%)と3度目の再肥大化、内部2箇所で軸スタジオのカード構成記述が食い違う実害も確認。AxisComposer.tsxは2回連続+15%成長トリガー発火だが個別閾値(1,400行)未到達。P0/P1は今回もゼロ。

### 規模ウォッチ表（抜粋、本エージェントが実測値を裏取り済み）

Backend上位5件は前回と同一顔ぶれ、いずれも未発火。

Frontend:

| ファイル | 今回 | 前回 | 増分 | 発火 |
|---|---|---|---|---|
| `components/Map/MapView.tsx` | **2,992**（実測済み） | 2,496 | +19.9% | ○ 閾値2,800行・種類数条件とも超過 |
| `app/page.tsx` | **1,726**（実測済み） | 1,792 | -3.7% | 未発火（ただしuseState等13→27件、+108%） |
| `components/MapOverlayControls/MapOverlayControls.tsx` | **1,490**（実測済み） | 1,141 | +30.6% | ○ 上位3位定着 |
| `components/AxisStudio/AxisComposer.tsx` | **1,345**（実測済み） | 1,123 | +19.8% | ○ 2回連続、個別閾値1,400行未到達 |
| `components/Map/icons.tsx` | **644**（実測済み） | 497 | +29.6% | 上位5件へ新規登場 |

`docs/architecture.md`: 2,146→**2,545行**（実測済み、+18.6%）。

### 変更コスト表

評価軸追加（既存プリミティブ範囲内）: axis_admin API呼び出し1回のみ・コード変更ゼロ、さらに改善。新規計測: JMA動的レイヤー追加（T410実績、12ファイル+738/-12行、中程度・低リスク）、外部ラスタタイルのバックエンド経由化（T412実績、初回5ファイル新規+消費側7箇所URL置換、basemap_clientパターン複製で判断コスト低）。

### Findings

**[P2] F-1. MapView.tsxの閾値二重超過、次閾値未設定**
- Evidence: 2,496→2,992行（個別閾値2,800行）。DYNAMIC_WEATHER_RENDERERSは2→8件（個別閾値「5件目」も超過）。
- Root Cause: 増分の大半（8件中6件）はraster専用の単純な宣言的成長、`ensureWindPenaltyFillLayer`のようなbespoke ensure/apply関数の増加とが閾値上で混在。
- Recommendation: (a)raster専用エントリはノーカウント化、(b)種類数条件を「gridFill等の描画ロジックを持つレンダラー or bespoke関数の件数」へ再定義（現在実質3件相当）、(c)行数閾値2,800→3,200行へ引き上げ。
- Scope: S(Keep List更新のみ) / Confidence: High

**[P2] F-2. docs/architecture.mdが3度目の再肥大化、内部2箇所（§735とT396/T397経緯節1514-1553行）で軸スタジオのカード構成記述が矛盾**
- Evidence: 2,146→2,545行（+18.6%）。§735「4種のテンプレート...変わらず」vs 経緯節「3カード・2プリミティブへの再設計」で矛盾。T8・T286に続き3回目の同型再発、5回以上の再発類型の6件目。
- Recommendation: (1)§735をT396/T397後の構成へ更新（consistencyレビューF-1と根本原因重複、Phase 6で統合済み）、(2)T8/T286と同じ手法で経緯記述をdocs/decisions/へ抽出するタスクを起票。次閾値「3,000行到達」または「次の規模L以上の設計転換完了時」。
- Scope: S(1のみ)〜M(2まで) / Confidence: High

**[P3] F-3. AxisComposer.tsx 2回連続+15%成長トリガー、個別閾値未到達**
- Recommendation: 既存の閾値付きKEEP（1,400行 or 5つ目のshape種到達）を維持、監視継続。
- Scope: S / Confidence: High

**[P3] F-4. MapOverlayControls.tsx +30.6%成長、上位3位定着**
- Evidence: T406とT414（windAxisチップ）が主因。評価軸グループが`expandDirection="flatRight"`のまま残存、T417撤回→T418（評価軸チップの地図UI撤去）へ差し替えられた移行途中状態とコードで確認。
- Recommendation: 閾値付きKEEPの新規登録はT418完了後まで意図的に見送る（DEFER）。
- Scope: S / Confidence: High

**[P3] F-5. icons.tsx +29.6%成長、上位5件へ新規登場**
- Evidence: T410（キキクル4種）のアイコン追加が主因。Impact低。対応不要、記録のみ。
- Scope: - / Confidence: High

**[P3] F-6. display_overrideの後方互換フォールバック（T404）、削除タスク（T409）未着手のまま**
- 意図的な新旧併存として管理されている（設計原則8準拠、削除条件はT409に明記済み）。対応不要。
- Scope: - / Confidence: High

### KEEP

T396/T397(4→2プリミティブ・4→3カード)、T414(バックエンド簡略化)、T412(basemap_clientパターン踏襲)、T351(汎用テーブル新設見送り)、T422/T411(トリガー付きDEFER)、T404のdisplay_override後方互換フォールバック。

### Keep List更新案

新規閾値提案: MapView.tsx（行数3,200行への引き上げ＋種類数条件の再定義）。既存のAxisComposer.tsx（1,400行）・road_graph_repository.py（2,800行）・road_graph_engine.py（1,100行）は据え置き（今回未発火）。

### REMOVE / SIMPLIFY

該当なし。

### REFACTOR

F-1: DYNAMIC_WEATHER_RENDERERSのカウント方式再定義（Keep List更新のみ）。

### EXTEND

該当なし。

### DEFER

F-4（MapOverlayControls.tsx閾値登録、T418完了がトリガー）、F-3（AxisComposer.tsx分割、既存トリガーのまま）、F-6（display_override削除、T409着手判断がトリガー）。

### Regression / Previous Findings

前回complexity F-1（AxisComposer.tsx急成長）→登録完了（T355）だが継続監視中（本レビューF-3）。F-2（road_graph_repository/engine成長）→解消済み（T357）。F-3（history/2026-08-26_complexity.md不在）→本エージェントが直接確認したところ、ファイル自体は依然として存在しないが、T356（完了・2026-08-27）は「復元ではなく2箇所への注記追加」という設計どおりの対応で完了しており、追加対応は不要（Phase 6で詳述）。統合-1（architecture.md未追従）→解消済み（T354）だが本レビューF-2として新しい観点で再発。統合-6（architecture.md肥大化）→未解消のまま悪化継続。

自己監査の良好事例: RouteList.tsx/ComparisonPanel.tsxのレガシーフィールドべた書きは、CLAUDE.mdの新チェックにより実装セッション自身が発見しT421として起票済み、新規Finding化せず。

### スコアサマリ

| 指標 | 値 |
|---|---|
| P0件数 / P1件数 / P2件数 / P3件数 | 0 / 0 / 2 / 4 |
| 総合スコア（100点満点） | 90 |
| 前回同種レビューからの差分 | 初回記録（新算出式のため比較不可） |
| REMOVE/SIMPLIFY/REFACTOR件数 | 0 / 0 / 1 |
| DEFER件数（トリガー未到達） | 3 |

### Overall Judgment

コード構造の健全性は高水準維持。T396/T397/T414はいずれも複雑さを削減する方向の設計反復。今回の指摘は全て「登録・監視の運用漏れ」に集中しコードを触らず対応可能（F-1/F-3/F-4/F-5）。唯一構造対応が要るのはF-2（architecture.md抽出、S〜M規模）。

---

## Phase 4: 設計・実装・テスト整合性レビュー（consistency・サブエージェント実施）

- 対象コミット: `a5d5d51` / 前回基準: `f7b8f5e`

### Executive Summary

対象範囲(`f7b8f5e..a5d5d51`、173コミット・464ファイル・+36,387/-12,791行、T400番台)は、前回統合レビュー(2026-08-27第8回)が指摘した6件の起票(T354〜T358)が全件解消済みの状態からスタートしている。今回最大の関心事だったT405→T414の作り直しは、(a) T405完了報告は当時の実装と正確に一致しており「歴史的記録」として自己訂正済み、(b) T414後の実装は訂正後の契約(状態機械・材料非依存設計)を満たし、テスト(backend 1456件・frontend 807件)で裏付けが取れる、(c) コード上の残骸はゼロ(`get_way_bearings_in_tile`等の旧識別子はコメント・docsの履歴参照としてのみ残存)——という高精度な結果だった。OpenAPI生成物ドリフトも検出されず、MVTタイル世代の対上げ(T367のv16→v17)も同一コミットで正しく実施されている。T400の完了後再監査(分割元タスク完了ゲート)も機能しており、未起票フォローアップを自ら2件発見・起票する様子を確認できた。

一方、新規に2件のarchitecture.md記述ドリフトを発見した(うち1件はT413当日実装分の反映漏れという「同日中の追従漏れ」で、単なる遅延ではなく積極的に誤った記述が残る事例)。また、CLAUDE.md・docs/architecture.mdが説明する`pytest -m "not postgis"`というテストスキップ機構が、実際にはpytest markerとして存在せず完全な無操作(no-op)であることを実行確認で発見した(実際のスキップは`tests/conftest.py`等9ファイルのfixtureレベルの`try/except pytest.skip()`によるもので、影響は限定的だが説明の機序が事実と異なる)。**この主張は本統合レビューの取りまとめエージェント自身も、`backend/pytest.ini`の直接読み取りと`backend/tests/`配下11ファイルへの`grep`により独立に再確認済み**（`pytest.ini`に`markers`セクションなし、全PostGIS統合テストは`@pytest.mark.postgis`ではなく`pytest.mark.xdist_group(name="postgis")`を使用、`@pytest.mark.postgis`は0件）。

### T405→T414の検証結果

| 観点 | 検証結果 |
|---|---|
| (a) T405完了報告と実装の一致 | 一致。`docs/tasks/T405.md`は実装当時の内容を正確に記録し、冒頭に自己訂正がある。虚偽の完了報告ではない |
| (b) T414実装が契約を満たすか | 満たす。`backend/app/services/wind_way_service.py`の`get_way_wind_penalties`は`bearing_deg`引数を受け取りタイル単位で1回計算。`wind_way_penalty_cache.py`のキーは`(z,x,y,時刻バケット,向きバケット)`へ縮小。backend 1456件・frontend 807件のテストが実行時に全通過 |
| (c) T405時点の残骸 | 残骸なし。生存参照はコメント・docsの履歴的言及のみ。旧テスト`test_get_way_bearings_in_tile_matches_domain_bearing_between`は実際に削除され`test_get_way_ids_in_tile_*`系5件へ置き換わっている(`backend/tests/test_road_graph_repository.py:1932-1973`) |

### テスト実行結果

- backend: `pytest -q -m "not postgis"` → 1456 passed(DB接続込み)
- frontend: `npx tsc --noEmit` → クリーン(LayoutProps既知事象1件のみ)
- frontend: `npx eslint .` → クリーン(0件)
- frontend: `npx vitest run --pool=threads` → 90ファイル・807件すべて通過
- OpenAPI生成物: 差分ゼロ

### Findings

**[P2] F-1. architecture.mdがMapLayersPanel(サイドバー)の最上位グルーピングについて、T413で既に覆された記述を2箇所に残している**
- Evidence: `docs/architecture.md:2299-2301`（取りまとめエージェントが再確認: 実際の行番号は2298-2301）「サイドバー...は独立した設計判断として引き続き`MapLayerDataNature`(観測/推定/動的)の2見出しを使う」。`docs/architecture.md:2477-2482`「サイドバー...の最上位グルーピングはT406のスコープ外として意図的に変更していない...チップ側とサイドバー側で最上位の語彙が異なる状態が残っている(意図的な積み残し)」。実装: `frontend/src/components/MapLayersPanel/MapLayersPanel.tsx:7,480`が`MAP_OVERLAY_GROUP_ORDER`(4分類)を使用。`docs/tasks/T413.md`(状態: 完了・2026-08-30)に「サイドバーもチップと完全に同じ`mapOverlayGroupFor`を単一ソースとして使う方式へ統一した」と明記
- Impact: architecture.mdを一次情報とする後続作業が既に廃止された2区分方式を前提に実装するリスク
- Root Cause: T413はT406の積み残しを埋める独立タスクとして同日中に起票・実装されたが、architecture.md更新はT406時点の記述のまま据え置かれた
- Recommendation: `docs/architecture.md:2298-2301`・`2477-2482`をT413後の実装に合わせて書き換える（complexity F-2の§735修正と根本原因を共有、Phase 6で統合）
- Scope: S / Confidence: High

**[P2] F-2. CLAUDE.md・architecture.mdが説明する`pytest -m "not postgis"`は実際にはpytest markerとして存在せず完全な無操作(no-op)**
- Evidence: `CLAUDE.md:60,114`・`docs/architecture.md:1444`が`pytest -m "not postgis"`でのスキップを明記。実測: `-m "not postgis"`と無指定は同じ1456件を収集(deselect 0件)、`-m postgis`は1456 deselected(0件選択)。`grep -rln "mark.postgis" backend/tests`→0件。実際のスキップ機構は`conftest.py:74-80`等9ファイルのfixtureレベル`try/except pytest.skip()`
- 取りまとめエージェントによる独立再確認: `backend/pytest.ini`は`[pytest]`セクションに`asyncio_mode`・`asyncio_default_fixture_loop_scope`のみを持ち`markers`定義なし。`backend/tests/`配下11ファイル全件が`postgis`ではなく`xdist_group(name="postgis")`を使用していることを確認済み
- Impact: 実害は限定的(9ファイル全て同じ安全パターン)だが、将来この慣習に従わない新規テストが追加された場合CI等で予期しないハード失敗を招くリスク
- Recommendation: (a) `pytest.ini`へ`markers = postgis: ...`を登録し実際に機能させる、または(b) CLAUDE.md/architecture.mdの記述を実態に合わせて訂正。principles.mdの「同種ドリフト2回以上再発なら機械的検知優先」原則に照らし(a)推奨
- Scope: S / Confidence: High

**[P3] F-3. consistency.md自身の「双子実装の対称性」チェック項目が削除条件成立(T122・T123完了、2026-08-18)から12日間未削除**
- Recommendation: `/review:improve`経由で削除提案（レビュー基準ファイル自体の変更のため、improvement-plan.mdへの直接起票ではなく`/review:improve`の提案→承認フローに乗せる）
- Scope: S / Confidence: High

### KEEP

T400の分割元タスク完了ゲート自己再監査(T422・T423を機械的grepで検出)、T414の設計訂正プロセス(材料非依存の一般契約へ抽象化)、T367のMVTタイル世代対上げ、T358(削除禁止ガードのwind/gradientテスト拡張)、T356(history/未保存問題の注記)、OpenAPI生成物の同期精度。

### REMOVE

consistency.mdの「双子実装の対称性」チェック項目(F-3、`/review:improve`経由)。

### SIMPLIFY / REFACTOR / EXTEND

該当なし。

### DEFER

該当なし(T409/T411/T422/T423は既存起票のまま)。

### Regression / Previous Findings

前回6件(統合-1〜6のうち1〜5)全解消済み。統合-6(architecture.md肥大化)は据え置き。新規同型再発: F-1はT332→T354、T406積み残し→T413記述漏れに続く3回目の同型検出。

**取りまとめエージェントによる補足**: 「統合-3（history/2026-08-26_complexity.md不在）が1〜5に含まれ解消済み」という本記述と、complexityレンズ自身のRegression節「F-3は未解消のまま(実害限定的)」は、一見矛盾するがそうではない。`docs/tasks/T356.md`を直接確認したところ、T356（完了・2026-08-27）は「遡及復元できる一次記録が残っていなかったため、`docs/improvement-plan.md`のT349該当箇所と`docs/architecture.md:1131`の2箇所へ『結果ファイルは未保存・全ブランチのgit履歴に存在しない』という趣旨の注記を追加する」という設計どおりの対応で完了している。つまり「T356というタスクは完了済み（consistencyの主張）」と「`history/2026-08-26_complexity.md`というファイル自体は依然として存在しない（complexityの主張）」はどちらも事実であり、対応方針の違いではなく着眼点の違いによる表現差。追加対応は不要（Phase 6で再掲）。

### スコアサマリ

| 指標 | 値 |
|---|---|
| P0件数 / P1件数 / P2件数 / P3件数 | 0 / 0 / 2 / 1 |
| 総合スコア（100点満点） | 93 |
| 前回同種レビューからの差分 | 初回記録（新算出式のため比較不可） |
| REMOVE/SIMPLIFY/REFACTOR件数 | 1 / 0 / 0 |
| DEFER件数（トリガー未到達） | 0 |

### Overall Judgment

T405→T414の作り直しは完了報告・実装・テストの三者が高精度で一致、コード残骸ゼロ。OpenAPI・MVTタイル世代も完全クリーン。architecture.mdの記述遅れは3回目の同型検出で今回は「同日中に覆った判断を意図的な現状維持と誤って断定」という質の悪化あり。pytest -m not postgisの発見は文書の記述を検証せず信じることの危険性を示す実例（本レビュー自身の運営においても同じ教訓が当てはまった。冒頭「サブエージェント結果の授受経路について」参照）。両者P2止まりでP0/P1は0件。

---

## Phase 5: UI/UXレビュー（ui・サブエージェント実施、独立worktree）

- **対象コミット: `470bf32`**（他3レンズの`a5d5d51`より1コミット進む、T414クリーンアップのみ。冒頭の対象範囲差の説明を参照）
- 実施場所: 専用worktree、Playwright(headless Chromium)実機確認、デスクトップ1280×800・モバイル390×844両方

### Executive Summary

T406（4チップ・3排他ドメイン）とT413（サイドバー語彙統一）を実機クリック検証、いずれも設計どおり正しく動作。T406の排他ドメイン制御——道路ON→評価軸ON で道路が自動OFF（aria-pressed実測）、環境ONは評価軸に無影響、スポットONは環境に無影響——を確認。T413のサイドバー4見出し統一（T413完了報告は実機未確認だった）を本レビューが補完・確認。T417は実装前撤回のため実機上も変化なし、矛盾なし。T418〜T423は未着手のままドキュメント記載どおりの状態（評価軸チップ地図上残存等）、既知のため新規指摘化せず。新規P0/P1指摘ゼロ、コンソールエラーはアメダス502のみ。

dev環境が並行セッションとPostgreSQL/Redisを共有しており道路タイル読み取り・Redisアクセスでタイムアウト多発、ルート生成成功確認・完全なタイル描画確認ができなかった（既知の環境制約T318類似、新規UI不具合として扱わない）。

### Findings

該当なし（P0〜P3すべて新規指摘なし）。

### KEEP

- T406の排他ドメイン制御（`10_axis_chip_expanded.png`〜`43_spot_on_env_unaffected.png`、aria-pressed実測、Confidence: High）
- T413のサイドバー4見出し統一（body innerText抽出で確認、Confidence: High）
- T417撤回の後始末（矛盾なし、Confidence: High）
- モバイル390px幅BottomSheet3タブとも横スクロールなし（scrollWidth実測、Confidence: High）
- キキクル既定ON(T420)は平常時ほぼ透明で情報過多にならないことを実機確認（Confidence: Medium、実際に危険度発生時は未確認）

### REMOVE / SIMPLIFY / REFACTOR / EXTEND / DEFER

該当なし。

### Regression / Previous Findings

T318（road_graphタイル未カバレッジで候補0件）今回も再現したが環境要因との切り分け未了、Confidence: Medium、新規指摘化せず。T406.md「積み残し」節（サイドバー語彙不整合）はT413で解消済みと確認。

### 実機確認できなかった範囲

ルート生成成功確認（T318 or 環境要因）、アメダス502（Redis接続タイムアウト0.2秒設定、環境固有の可能性大、Confidence: Low。0.2秒という値が並行セッション共有DBという運用実態と整合しない可能性は次回complexity/consistencyで検討価値ありとメモ）、モバイルでの読み込み失敗バナーと評価軸チップの視覚的重なり（pointer-events:noneでブロックなし、Finding化せず）、軸スタジオ深掘り（対象範囲外、Confidence: Low）、ダークモード（対象範囲外）。

### スコアサマリ

| 指標 | 値 |
|---|---|
| P0件数 / P1件数 / P2件数 / P3件数 | 0 / 0 / 0 / 0 |
| 総合スコア（100点満点） | 100 |
| 前回同種レビューからの差分 | 初回記録（新算出式のため比較不可） |
| REMOVE/SIMPLIFY/REFACTOR件数 | 0 |
| DEFER件数（トリガー未到達） | 0 |

### Overall Judgment

T406・T413はいずれも実機検証で設計どおり機能、非自明な境界条件（排他ドメイン）をDOM状態遷移で裏取りできた意義は大きい。最大の注意点はUI不具合ではなくdev環境のDB/Redis共有によるタイムアウトで、確認できなかった範囲を透明性をもって記録。次回、より単独稼働に近い環境でのルート生成成功パス確認を推奨（新規タスク起票は不要）。

---

## Phase 6: 相互確認・矛盾と判断

**設計トレードオフの矛盾は検出されなかった**（前回2026-08-27と同様）。4レンズとも同じ結論——T400→T401〜T406・T405→T414という一連の転換が、前回までの負債を解消しながら規律よく実行された期間——を異なる角度から裏付けている。

**重複統合1（最重要）: architecture.mdの3症状を1つの根本原因へ統合**

overall O-1（肥大化トリガー再発火、量の問題、P3）・complexity F-2（3度目の再肥大化＋§735の内部矛盾、P2）・consistency F-1（MapLayerDataNature記述がT413で覆されたまま残存、P2）の3件は、いずれも「architecture.mdの保守が変更速度に追いついていない」という同じ根本原因の別の症状である。前回（2026-08-27）Phase 6が既に同じ傾向を「横断的傾向」として指摘しており、3回目の検出にあたる。

判断: 根本原因側（architecture.mdの記録鮮度管理プロセスの欠如）へ寄せて1つの統合Findingとするが、具体的な修正箇所は3箇所（§735のテンプレート数記述、2298-2301行・2477-2482行のMapLayerDataNature記述、経緯記述のdecisions/への抽出）に分かれ、それぞれ独立に編集可能なため、Recommendationは3項目立てで残す。優先度は構成要素のうち高い方（P2）を採用する——2/3が実装済みUIと矛盾する具体的な誤情報（quality issue）であり、量の増加（quantity issue）より実害が大きいと判断した。

**重複統合2: history/2026-08-26_complexity.mdの「解消」表現の食い違い**

consistencyレンズの「前回6件のうち1〜5全解消済み」という記述と、complexityレンズ自身の「F-3(history/2026-08-26_complexity.md不在)は未解消のまま」という記述は、字面上は矛盾して見える。取りまとめエージェントが`docs/tasks/T356.md`を直接確認した結果、両者はどちらも正しい——**T356というタスク**（対応方針: 復元できなければ注記で整合させる、という設計）は完了しているが、**`history/2026-08-26_complexity.md`というファイル自体**は復元されておらず存在しない、という2つの異なる対象を指しているに過ぎない。T356の対応方針自体が最初から「ファイル復元ではなく注記」を選択肢として明記していたため、ファイル不在は設計どおりの結果であり、新たな対応は不要。両レンズのRegression記述はそのまま残し、本節で解釈を補足するに留める（新規Finding化しない）。

**重複統合3: MapOverlayControls.tsxの成長とT417/T418移行**

complexity F-4（MapOverlayControls.tsx +30.6%成長、T418完了まで閾値登録DEFER）とui Executive Summary（「T418〜T423は未着手のままドキュメント記載どおりの状態」）は、同じ「T417撤回→T418移行途中」という状態を異なる角度（複雑度・UI動作）から確認しており、矛盾はなく相互に補強し合う。complexityのDEFER判断（T418完了まで閾値登録を待つ）を支持する。

**トレードオフの評価**: 現在の開発フェーズ（研究優先・個人開発・低利用規模）を踏まえ、今回発見された指摘はいずれも「登録・監視の運用漏れ」または「ドキュメント記述の追従漏れ」に集中しており、アーキテクチャ上の本質的なトレードオフ判断を要する項目はなかった。

**相殺の確認**: 統合されたFindingsのRecommendationはいずれも既存のKEEP判断を壊さない（architecture.md更新はdocsのみ、閾値登録・カウント方式再定義は監視の追加のみ、pytest marker修正は既存テストの実行結果を変えない）。

---

## Phase 7: 最終統合判断

### 統合Findings（優先度順）

1. **[P2] 統合-1. architecture.mdの記録鮮度管理が3症状で破綻**（overall O-1 + complexity F-2 + consistency F-1、Phase 6重複統合1）:
   - (a) `docs/architecture.md:2298-2301`・`2477-2482`をT413後の実装（サイドバーもチップと同じ4分類`mapOverlayGroupFor`を使用）に合わせて書き換える
   - (b) `docs/architecture.md`§735の変換テンプレート記述をT396/T397後（2プリミティブ・3カード）へ更新する
   - (c) T8・T286と同型の「経緯記述をdecisions/へ追い出す第3弾」を次の閑散期に検討する（新規閾値: 3,000行到達 or 次の規模L以上の設計転換完了時）
2. **[P2] 統合-2. `pytest -m "not postgis"`が実際にはpytestマーカー未登録により完全な無操作（no-op）**（consistency F-2、取りまとめエージェントが独立に再確認済み）: `pytest.ini`へ`markers = postgis: ...`を登録し実際に機能させる（推奨）、またはCLAUDE.md/architecture.mdの記述を実態（fixtureレベルのtry/except skip）に合わせて訂正する。
3. **[P2] 統合-3. MapView.tsxの規模ウォッチ閾値が二重超過、次閾値未設定**（complexity F-1）: raster専用エントリのノーカウント化、種類数条件の再定義、行数閾値2,800→3,200行への引き上げをKeep Listへ登録する。
4. **[P3] 統合-4. AxisComposer.tsx 2回連続急成長、既存閾値のまま監視継続**（complexity F-3）: 対応不要、既存の閾値付きKEEP（1,400行 or 5つ目のshape種）を維持。
5. **[P3] 統合-5. MapOverlayControls.tsx急成長、T418完了までDEFER**（complexity F-4）: 閾値登録をT418完了まで意図的に見送る。
6. **[P3] 統合-6. icons.tsx急成長、対応不要**（complexity F-5）: 記録のみ。
7. **[P3] 統合-7. display_override後方互換フォールバック、T409着手判断待ち**（complexity F-6）: 意図的な新旧併存として管理済み、対応不要。
8. **[P3] 統合-8. consistency.md自身の「双子実装の対称性」チェック項目、削除条件成立から12日未削除**（consistency F-3）: `/review:improve`経由で削除提案（improvement-plan.mdへの直接起票ではない）。

**Regression総括**: 前回（2026-08-27第8回）の統合-1〜6は、統合-6（architecture.md肥大化）を除く5件が解消済みだった。しかし統合-6自体は今回も未解消のまま悪化し、新たに統合-1（本レビュー）として「量」に加え「質」（2件の具体的な誤記述）の面でも再発した。これで3回連続の同型検出であり、単発の閑散期対応では解決しないプロセス上の課題であることを示唆する。

### 実施順序案

```text
Phase A（記録の整合、即時可能、いずれもS規模）
  ├─ 統合-1(a): architecture.md 2298-2301行・2477-2482行の書き換え
  ├─ 統合-1(b): architecture.md §735の書き換え
  ├─ 統合-2: pytest.iniへのpostgisマーカー登録 or 記述訂正
  └─ 統合-8: consistency.md「双子実装の対称性」項目の削除（/review:improve経由）

Phase B（境界の固定、閾値登録のみ、いずれもS規模）
  └─ 統合-3: MapView.tsxのKeep List更新（カウント方式再定義＋閾値引き上げ）

Phase C（判断待ち、内部の再配置、トリガー到達後）
  ├─ 統合-1(c): architecture.md経緯記述のdecisions/への抽出（次の閑散期）
  └─ 統合-5: MapOverlayControls.tsxの閾値登録（T418完了後）
```

### スコアサマリ（統合Findingsベース、算出式: `100 - (P0×20 + P1×10 + P2×3 + P3×1)`）

| 指標 | 値 |
|---|---|
| P0件数 / P1件数 / P2件数 / P3件数 | 0 / 0 / 3 / 5 |
| 総合スコア（100点満点） | **86** |
| 前回同種レビューからの差分 | 初回記録（principles.md 2026-08-30改訂の新算出式による最初の`all`スコアのため、旧算出式の前回86/100とは単純比較不可） |
| REMOVE/SIMPLIFY/REFACTOR件数 | 1 / 0 / 1 |
| DEFER件数（トリガー未到達） | 3（統合-5、および既存起票のT411・T409・T422/T423） |

観点別（各レンズ自身のスコアサマリより）: overall **99**（P3×1）/ complexity **90**（P2×2+P3×4）/ consistency **93**（P2×2+P3×1）/ ui **100**（新規指摘ゼロ）。

### 「全体として最もバランスが良い状態」

T400という規模L・分割元タスクを6つの独立タスクへ分割し全完了、さらにT405→T414という設計前提の自己訂正を残骸ゼロで成し遂げた本期間は、複雑さの削減（4テンプレート→2プリミティブ、4カード→3カード、per-way計算→タイル単位計算）がコード量の増加を上回る、規律あるXL級の設計反復として評価できる。P0/P1が4レンズ通じてゼロという結果は、研究優先・個人開発フェーズの現在の開発速度に対して品質管理が追いついていることを示す。残る課題は一貫して「登録・監視の運用漏れ」（MapView.tsx等の閾値未更新）と「ドキュメントの記録鮮度」（architecture.mdの3度目の乖離、pytest markerの説明不一致）の2種に集約されており、いずれもコードを変更せずS〜M規模の記述修正で解消できる。今回新たに得られた教訓は、ドキュメントの記述（`pytest -m "not postgis"`が機能するという説明）を検証なしに信じることの危険性が、レビュー基盤の運営そのもの（本セッションが中継内容を一次情報で裏取りする必要に迫られた経緯）でも実例として再現されたことであり、「事実と推測を分ける」というprinciples.mdの最重要原則の重みを改めて裏付けた。

## 起票案（P0/P1/P2は improvement-plan.md への起票案として提示。起票はユーザー承認後）

| 優先度 | 起票案 | 規模目安 | 備考 |
|---|---|---|---|
| P2 | 統合-1: architecture.mdの記述整合（サイドバー4分類・§735テンプレート数の書き換え） | S | 2箇所の書き換えのみ |
| P2 | 統合-1(c): architecture.md経緯記述のdecisions/への抽出（第3弾） | M | 次の閑散期にT8/T286と同型で実施 |
| P2 | 統合-2: pytest.iniへのpostgisマーカー登録、または記述訂正 | S | markers登録を推奨（機械的検知の方が記述訂正より再発防止効果が高い） |
| P2 | 統合-3: MapView.tsxのKeep List更新（カウント方式再定義＋閾値3,200行への引き上げ） | S | コード変更なし、監視基準の更新のみ |
| P3（参考） | 統合-8: consistency.md「双子実装の対称性」項目の削除 | S | improvement-plan.mdではなく`/review:improve`の提案フローへ |

統合-4/5/6/7（P3、対応不要 or 既存DEFERのまま）は起票不要。
