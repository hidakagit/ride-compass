# 統合レビュー（2026-09-03、第4回）— **中間保存（Phase 1・2完了、Phase 3〜6は未完）**

> **この記録は中間保存である。** 2026-09-03のセッションが、Phase 3（overall）の
> 7シャードのうち1シャード（O1）完了・Phase 4（complexity）の規模ウォッチ実測のみ完了・
> Phase 5（consistency）8シャードすべて未完・Phase 6（UI）未実施の時点で、APIのセッション
> 上限（rate limit、11:10 JSTにリセット）により中断したため、完了済みの結果と再開に必要な
> 情報を先に保存する（all.md「Phase 3〜6 共通の省力化ルール」に従う）。再開時は末尾の
> 「再開手順」から続け、全Phase完了時に本ファイルを統合する。

- 実施日: 2026-09-03（中断時刻 約06:50 JST）
- 対象コミット（HEAD固定）: `89c94cd`（`git fetch origin master`実施、origin/master先端と一致確認済み。
  T557起票コミット）
- 比較ベースライン: `cab1441`（前回統合レビュー`2026-08-31_all.md`の対象コミット）。
  一時ブランチ`review-overall-baseline`・`review-consistency-baseline`をこのSHAに作成済み
  （**再開時もこれを使う。全Phase完了後に`git branch -D`で削除する**）
- 実施方式: 専用worktree `.claude/worktrees/review-all-2026-09-03`（`89c94cd`にdetach、
  **再開時もこのworktreeを使う**。`frontend/node_modules`は無い[ジャンクション作成に失敗]
  ため、frontendのtsc/eslint/vitest計測は本体チェックアウト[同一コミット・クリーン]で
  実施した）。overall・consistencyはドメイン単位でシャーディング（下記）し、Explore Agent
  （読み取り専用）へ委任した。
- **コードは変更していない（全フェーズ読み取り専用）。**

対象範囲: `cab1441..89c94cd`の207コミット・380ファイル（docs/tasks 103・
frontend/src/components 79・backend/tests 45・backend/app/infrastructure 22・
backend/app/domain 15・frontend/src/app 14・docs/modules 14・.claude 13・
backend/app/services 10 他）。

---

## Phase 1: 全体把握（完了）

前回統合レビュー（`2026-08-31_all.md`、第3回）以降の主な変更:

- **T531 周回生成のフロンティア方式転換（規模L、2026-09-03完了・本番反映）**: 8方位固定→
  一対全最短経路木（scipy）で折返し点を軸重み順に選び、retraceペナルティ付き復路A\*で周回にする。
  `domain/routing.py`（+旧SparseRoadGraph撤去）・`services/road_graph_engine.py`・
  `route_generator.py`・`infrastructure/search_graph_cache.py`・API `max_routes`・frontend
  候補件数入力/タブ順位番号。直後に`/code-review`（`2026-09-03_codereview.md`）を実施し
  T557（上位10件＋P3相当10件）として起票済み。派生: T551〜T556。
- **T522派生の性能改修群（T536〜T540・T546、いずれも完了・本番反映）**: タイル単位静的スコア
  行列＋ベクトルコスト合成（T536）、探索グラフ・索引のタイル集合キャッシュ（T537）、
  タイル材料キャッシュのディスク永続化（T538）、split事前バッチ（T539）、列指向
  `EdgeMaterialTable`（T546）。新設infrastructure 5ファイル。
- **ルート結果UIの再構成（T518・T545・T548・T549・T550、完了）**: `RouteList`・`WeightPanel`・
  `routeSegmentChartPopup`撤去、`RouteAxisProfile`/`AxisContributionBar`統合、
  `total_score`（`scoring.yaml`/`RouteScorer`）撤去、`supports_route_coloring`撤去、
  寄与度のbackend算出（`axis_contributions`）、区間詳細のボトムシート統合。
- **研究モードの再配置（T519・T517）**: `HeaderMenu`新設、`BackendLogsPanel`＋admin debug logs
  プロキシ新設。
- **JMAタイル予熱・Redis（`jma_tile_prewarm_service.py`・`jma_tile_redis_cache.py`）**、
  `simple_api_client.py`新設、migration 0028〜0030（軸定義テーブルのDDL）。
- レビュー基盤: metrics種別新設（T453系）、pre-commit（OpenAPI契約・docs/modules経緯記述検査）。
- **直前の`/code-review`（2026-09-03、T531対象）で既に起票済み（T557）の指摘**は本レビューで
  重複起票しない。前回統合レビューのP1×2（T464同型のRedis fail-open統一、
  `windBoundaries`/`gradientBoundaries`の汎用化=T468）は完了マーク済み（実態確認は
  Phase 5で行う予定だった。T468は`grep windBoundaries|gradientBoundaries`が本番コードで
  12件ヒットするため、**完了マークと実装の乖離を疑う**——再開時にconsistency C5/C7で確認する）。

過去レビューの参照: `2026-08-31_all.md`・`2026-08-31_complexity.md`・`2026-08-31_overall.md`・
`2026-08-30_ui.md`・`2026-09-01_codereview.md`・`2026-09-03_codereview.md`。

---

## Phase 2: 定量メトリクス計測（完了）

詳細と計測コマンドは[2026-09-03_metrics.md](2026-09-03_metrics.md)（`history/metrics.md`へ各1行追記済み）。

| 項目 | 今回（`89c94cd`） | 前回（2026-08-31） | 差分 |
|---|---:|---:|---|
| 総ファイル数 / 総行数（cloc SUM） | 1,020 / 110,428 | 916 / 99,420 | +104 / +11.1% |
| 実装コードのみ（Markdown除く） | 61,480 | 57,047 | +7.8% |
| 実装本体（テスト除く: backend/app 11,277＋frontend impl 16,907） | 28,184 | 24,900 | +13.2% |
| テスト（backend/tests 16,217＋frontend tests 12,541） | 28,758 | 26,853 | +7.1% |
| churn（2026-08-31以降） | 207コミット / 383ファイル | 初回記録 | — |
| backend pytest収集（`not postgis`） | 1,344（postgis 238件除外） | 1,177 | +167 |
| frontend vitest件数 | 942 | 872 | +70 |
| tsc / eslint | 0エラー / 0エラー・警告1（`coverage/`生成物） | 0 / 0 | — |
| npm audit（critical/high/moderate/low） | 0/0/0/0 | 0/0/0/0 | — |

churn上位10: `docs/improvement-plan.md`（123）・`page.tsx`（23）・
`route-settings-and-results.md`（22）・`MapView.tsx`（19）・`road_graph_engine.py`（16）・
`page-composition.md`（14）・`routing-engine.md`（14）・`RouteSettingsPanel.module.css`（13）・
`architecture.md`（13）・`test_road_graph_engine.py`（13）。

**要確認フラグ**: 実装本体+13.2%（閾値+15%未満、未発火）。churnは初回記録のため新顔判定不可。
静的検査・依存関係とも未発火。

---

## Phase 3: 全体最適レビュー（**未完: 7シャード中O1のみ完了**）

シャード構成（再開時に同じ分割で続ける）:

| シャード | 対象 | 状態 |
|---|---|---|
| O1 | backend/app/domain（15ファイル） | **完了**（下記） |
| O2 | backend/app/services（10ファイル、`road_graph_engine.py`含む） | 未実施（rate limitで中断） |
| O3 | backend/app/infrastructure（22ファイル、新設5含む） | 未実施 |
| O4 | backend/app/api・batch・main/config・migrations 0028〜0030・scripts・deploy-backend.yml・pre-commit | 未実施 |
| O5 | frontend/src/components/Map（`MapView.tsx`3,633行含む）＋MapColorLegend | 未実施 |
| O6 | frontend/src/components（Map以外: AxisComposer・HeaderMenu・BackendLogsPanel・RouteAxisProfile・RouteForm・RouteSettingsPanel・TravelBearingControl等） | 未実施 |
| O7 | frontend/src/app（`page.tsx`2,143行・admin API routes）・hooks・lib・services | 未実施 |

### O1（backend/app/domain）の結果

- **[P1] `SearchMaterials.materials`のunion型（`dict[str, EdgeMaterialBundle] | EdgeMaterialTable`、
  `attributes.py:389`）に対し、消費側`road_graph_engine.py:190/259/1418`は全て`dict[...]`と
  注釈**。`EdgeMaterialTable`は`get`/`__getitem__`/`values`/`__len__`のみでdict互換
  （`items`/`keys`/`__iter__`/`__contains__`無し）。本番のタイルキャッシュ経路は
  `EdgeMaterialTable`、テストの多くとuncached経路は素のdictを流すため、「テストが通る型」と
  「本番が使う型」が分岐したまま恒久化している（将来`.items()`を足すと本番だけ500）。
- **[P2] スカラー評価経路一式**（`is_edge_allowed`・`compute_edge_axis_scores`・
  `_resolve_static_edge_materials`・`compute_edge_cost`・`compute_edge_costs_bulk`＋
  `EvaluationService`）が本番実行経路から外れ「回帰テストオラクル」として残置されているが
  削除条件が無い。0次フィルタ・材料解決が2実装並存（T343で実障害の前科あり）。
- **[P2] 「材料id→値」辞書組み立てが3箇所に手書き重複**（`evaluation.py:201-215`インスペクタ・
  `:470-483`スカラー版・`MATERIAL_CATALOG`extractor）で、カバーする材料集合が食い違う
  （`bridge`/`surface`/`smoothness`/`tracktype`等7材料がインスペクタ辞書に無く、軸スタジオで
  それらを使う軸は区間インスペクタで常に「データなし」になる）。評価軸追加の1本道に隠れた
  必須ステップが残っている。
- **[P2] `RouteSegmentDetail`/`RouteCandidate`に軸固有の一次材料フィールド**
  （`gradient_percent`/`wind_penalty`/`road_surface_good`、`wind_score`/`road_score`）が
  汎用dict化（T309/T431/T550）の途中で取り残され、エンジン側の軸固有集計関数
  （`_aggregate_road_score`/`_aggregate_wind_score`）も残る。削除条件・完了条件なし。
- **[P2] `compute_edge_costs_bulk`のレガシーシグネチャ維持のため、タイル経路が
  `EdgeMaterialTable`→個別辞書への逆変換**（`evaluation.py:1005-1039`）を負い、しかも
  `isinstance`両分岐に二重実装（局所最適の連鎖の典型）。
- **[P2] `dynamic_way_values.py:52-61`の`material_id`が実際には`axis_id`**で、
  `DYNAMIC_MATERIAL_EVALUATORS`（真の材料id）と同じ語彙で別の名前空間を指す。
- [P3] `_evaluate_axes_bulk`が軸が参照しない材料も含む`MATERIAL_CATALOG`全26材料ぶんの配列を
  タイルごとに確保（`evaluation.py:700-708`）。
- [P3] `axis_definitions.py:310-313`のコメントが「migration SQLで軸を追加する」という
  禁止済み運用を指示したまま（同ファイル冒頭docstringと矛盾）。
- KEEP候補: domain層の純粋性（I/O参照0件）、`_neumaier_accumulate`＋`round1_array`のビット一致、
  `tile_score_matrix_cache.sync_disk_cache_with_axis_revision`の2経路無効化。

---

## Phase 4: 複雑度平衡性レビュー（**部分完了: 規模ウォッチ実測のみ**）

### 規模ウォッチ表（実測、`89c94cd`。前回=`2026-08-31_complexity.md`の`0958e13`）

Backend:

| ファイル | 今回 | 前回 | 増分 | 発火 |
|---|---:|---:|---|---|
| `infrastructure/road_graph_repository.py` | 2,284 | 2,652 | **−13.9%** | 未発火（個別閾値2,800、T357） |
| `services/road_graph_engine.py` | 1,449 | 874 | **+65.8%** | **○ 個別閾値1,100行を超過**（T531で折返し選定・復路探索・hydrate統合が加わった。精査は再開時） |
| `domain/evaluation.py` | 1,177 | 744 | **+58.2%** | **○ +15%超**（T536の静的スコア行列・ベクトル合成・レガシー変換[O1 P2参照]） |
| `domain/material_catalog.py` | 814 | 815 | ±0 | 未発火 |
| `domain/axis_definitions.py` | 752 | 681 | +10.4% | 未発火 |
| `services/graph_service.py` | 610 | （圏外） | — | 上位6件目へ新規登場（キャッシュ階層T536〜T538） |
| `domain/routing.py` | 552 | （圏外） | — | 新規登場（T531の一対全木・CSR・間引き） |

Frontend:

| ファイル | 今回 | 前回 | 増分 | 発火 |
|---|---:|---:|---|---|
| `components/Map/MapView.tsx` | 3,633 | 3,114 | **+16.7%** | **○ T430で更新した閾値3,200行を超過** |
| `app/page.tsx` | 2,143 | 1,827 | **+17.3%** | **○ 閾値1,900行を超過**（T518/T545/T548/T550/T531） |
| `components/AxisStudio/AxisComposer.tsx` | 1,568 | 1,381 | +13.5% | **○ 個別閾値1,400行を超過**（T355） |
| `components/MapOverlayControls/MapOverlayControls.tsx` | 1,149 | 1,139 | +0.9% | 未発火 |
| `components/Map/mapLayers.ts` | 687 | 666 | +3.2% | 未発火 |

`docs/architecture.md`: 2,699行（前回2,740、−1.5%、閾値3,000未発火）。

**再開時の精査対象**: 発火5件（`road_graph_engine.py`・`evaluation.py`・`MapView.tsx`・
`page.tsx`・`AxisComposer.tsx`）を「複数の変更理由が同居しているか」で(a)理由つきKEEP／
(b)抽出・分割提案／(c)閾値付きKEEPの個別設定へ分類し、次閾値を必ず明示する。変更コスト表
（Case G: 評価軸追加は`axisId === "surface"`[静的路面フィルタ]以外のハードコード0件を再確認済み、
Case I: 動的材料。T468完了マークに対し`windBoundaries`/`gradientBoundaries`が本番コードに
12件残る点を要確認）と前回Keep List照合は未実施。

---

## Phase 5: 設計・実装・テスト整合性レビュー（**未実施: 8シャードすべて中断**）

シャード構成（再開時に同じ分割で続ける。各シャードは対応テスト・設計書を含む）:

| シャード | 対象 |
|---|---|
| C1 | backend/app/domain＋tests（test_evaluation・test_routing・geo_fixtures等）＋evaluation-scoring.md |
| C2 | backend/app/services＋tests（test_road_graph_engine・test_route_generator・benchmarks）＋routing-engine.md・logging.md |
| C3 | backend/app/infrastructure＋tests（新設5キャッシュのテスト）＋migrations 0028〜0030＋cross-cutting-infrastructure.md |
| C4 | backend/app/api・batch・main/config・scripts・deploy-backend.yml＋tests＋**完了タスク（T472〜T557）の照合表**・分割元タスクの未起票チェック |
| C5 | frontend/src/components/Map＋tests＋map-axis-coloring.md・static-map-layers.md（**T468の実態確認**） |
| C6 | frontend/src/components（Map以外）＋tests＋route-settings-and-results.md・developer-research-tools.md |
| C7 | frontend/src/app・hooks・lib・services・types/generated＋tests＋e2e（**smoke.spec.tsのタブラベル`北方向 — 20.3 km`が現行の`1. 北方向 20.3 km`と整合するか**） |
| C8 | docs横断: 死んだ参照（全docs/modulesのバッククォート付きファイル名の実在確認）・新規ファイルの記載漏れ（`git diff --diff-filter=A`）・記載粒度違反・architecture.mdの完了タスク反映・improvement-plan.mdと`docs/tasks/*.md`「状態」行の照合・history/参照の実在 |

---

## Phase 6: UI/UXレビュー（**未実施**）

Playwright headlessスクリプトを作成済み（scratchpad `ui_review_2026-09-03.js`。デスクトップ
1400×900と390×844モバイルで、初回表示→距離20km生成→タブ→2番目候補→地図クリック→
横スクロール有無・console/5xxを記録）。devサーバー（backend :8000、frontend :3010、
`.claude/launch.json`）は起動済みだった。再開時は同スクリプトを`frontend/`で
`NODE_PATH=$(pwd)/node_modules node <script>`として実行し、所見を確定前に再検証する。

---

## Phase 7・8（未実施）

---

## 再開手順

1. `git fetch origin master`でHEADが進んでいないか確認する。進んでいれば、対象コミットを
   `89c94cd`に固定したまま続けるか（worktreeはそのまま使える）、新HEADで取り直すかを判断する
   （差分が小さければ`89c94cd`固定で続け、Regression節に「以降のコミットは対象外」と書く）。
2. worktree `.claude/worktrees/review-all-2026-09-03`と一時ブランチ2本が残っていることを
   `git worktree list`・`git branch`で確認する（無ければ`git worktree add --detach <path> 89c94cd`・
   `git branch -f review-overall-baseline cab1441`で再作成）。
3. Phase 3のO2〜O7、Phase 5のC1〜C8をExplore Agent（読み取り専用、`run_in_background`）へ
   同じシャード分割で委任する（プロンプトの観点は`overall.md`/`consistency.md`の該当節。
   一度に16件起動するとrate limitに当たりやすいため、**4〜6件ずつ**に分けて起動する）。
4. Phase 4は規模ウォッチ発火5件の精査・変更コスト表・Keep List照合を本体で行う。
5. Phase 6はスクリプトを実行し、所見を再検証したうえで確定する。
6. Phase 7（矛盾・重複の統合）→Phase 8（優先度順・実施順序・バランス状態）→本ファイルを
   統合版へ書き換え、`scores.md`へ各レンズ＋allの行を追記し、一時ブランチとworktreeを削除する。
