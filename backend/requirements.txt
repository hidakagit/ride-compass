fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-xdist==3.8.0
pytest-cov==6.0.0
pydantic-settings==2.7.1
pyyaml==6.0.3
mapbox-vector-tile==2.2.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
geoalchemy2==0.16.0
shapely==2.0.6
astral==3.2
cachetools==7.1.7
# タイル材料・スコア行列のディスク永続キャッシュ（infrastructure/tile_persistent_cache.py）。
# 容量上限・退避・複数プロセスからの共有をライブラリ側が持つ。
diskcache==5.6.3
# JMAタイルの奇数ズーム補間（infrastructure/jma_tile_interpolation.py）で親タイルを
# 切り出して拡大するために使う。土地被覆ラスタタイル（infrastructure/landcover_raster.py）の
# PNG書き出しも使う。
pillow==12.3.0
# 土地被覆ラスタ（GeoTIFF）の読み取り・再投影。バッチ（precompute_way_landcover.py）と
# 地図タイル配信（infrastructure/landcover_raster.py）の両方が使うため本体側に置く。
# wheelがGDAL/PROJを同梱しており、本番VM（ARM64）でもビルドなしで入る。
rasterio==1.5.1
# 気象庁MSMの前処理済みデータ（Open-Meteoが公開する.om形式）を読む公式リーダー。
# infrastructure/msm_client.pyが使う。
omfiles==1.2.0
numpy==2.5.2
# 経路探索の本体（domain/routing.py）をJITコンパイルする。到達時刻をラベルとして持ち回る
# 探索は、コストが辺の静的な属性であることを前提にしたライブラリ（rustworkx・scipy）の
# コールバックでは表せず、自前で書く必要がある。aarch64のwheelが提供されており本番VM
# （Oracle Cloud ARM）でもそのまま入る。
numba==0.67.0
redis==5.2.1
apscheduler==3.11.0
