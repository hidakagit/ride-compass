# 並行実行（司令塔）の規約

複数のタスクを、複数のエージェントへ振り分けて同時に進めるときの取り決め。親セッション
（司令塔）が何を決め、エージェントが何を守り、1回回すたびに何を測って本規約を直すかを
ここに集約する。**1タスクを1セッションで進めるときには適用しない**（CLAUDE.mdだけで足りる）。

本規約は実測で直していく前提で書かれている。直し方は末尾の「改善の回し方」節。

## 司令塔の役割

司令塔の成果物は**管理されている状態**であって、振り出した本数ではない。管理されている
状態とは、いつ誰に聞かれても、全エージェントについて「何をしていて・いつ始めて・見込みに
対してどこにいて・異常があるか」を事実で答えられ、異常には手を打っている状態をいう。
そのために司令塔は**計画（上限と門）→ 実行（振り出し）→ 確認（時間で回す）→ 是正**を
回す。振り出しだけを繰り返すのは司令塔ではない。

司令塔は**振り分け・監査・ユーザー判断の集約**を持ち、実装はしない。例外は、会話の文脈
そのものが材料になる作業（その回の振り返りの実測値を書き戻す等）だけ。**実行中に来た
新しい依頼も、司令塔が自分で片付けずに状態の表のキューへ積み、上限の範囲で振り出す**——
司令塔が実装に回っている間、全体を見ている者がいなくなる。

- 振り分けと監査を同じ人が持つのは、エージェントの完了報告を**主張として受け取り、
  事実で確かめる**役が要るからである。報告は作業者の自己申告であり、誤りを含む
  （実例は「完了時の監査」節）。
- ユーザーへの質問は、エージェントから上がった保留をまとめて1回で出す。エージェントに
  直接ユーザーへ聞かせない——並行する複数の問いが、互いの前提を知らないまま届くため。

## 振り出す前

### 対象の導出

`docs/improvement-plan.md`の未完了行のうち、トリガーの無いものと発火済みのものが候補。
次に当たるものは振り出さず、一覧にしてユーザーへ示す:

- **別セッション・ユーザー自身が進めているもの**。`git worktree list`・リモートの作業
  ブランチ・`ListAgents`（他のローカルセッション）で確かめる。作業ツリーが在るだけでは
  進行中と言えない（HEADと未コミット変更の更新時刻を見る）。ユーザーが「別途やる」と
  言ったものと、それに充てられた派生タスクも含む。
- **画面の設計をユーザーと詰めてから作るもの**（新機能のUI・利用者に見える情報量を
  増やすもの）。設計原則（`docs/architecture/design-principles.md`）のUI原則は、
  実装前にユーザーと合意することを前提にしている。
- **ユーザーの判断・外部アカウントの操作が要るもの**（残すか消すかを決めるタスク・
  クラウドの後始末等）。
- **判断の重い総点検**（規模Lで、対象の母集団を決めること自体が作業の中心のもの）。

### ユーザーが手動で進めているタスク

ユーザーが別セッションで手動で進めるタスクの番号を伝えてきたら、それを「手動中」として
扱い、**影響のあるタスクを振り出しの候補から外す**。

1. 手動中のタスクの`Txxx.md`から、触る範囲（ファイル・モジュール・文書）と、段階・派生
   として充てられたタスクを取る。
2. 影響のあるタスクを機械的に列挙する: 手動中のタスクを本文・トリガーで参照している
   タスク、充てられた派生タスク、同じファイル・モジュールを触るタスク。
3. **既に稼働中のエージェントの担当と重なるもの**は、重なりの中身をユーザーへ示し、
   止めるか続けるかを確かめる（続けると、後からpushする側——多くは手動のセッション——が
   rebaseの衝突を払う）。
4. 手動中の番号と除外したタスクは、その回の記録（`Txxx.md`）に書く。会話だけに置くと、
   司令塔のセッションが切れたときに失われる。
5. 手動中のタスクが終わったと伝えられたら、除外を解いて候補へ戻す。

ユーザーが**これから**手動で始める大きなタスクについては、始める前に済ませておくべき
タスク（前提）を司令塔が洗い出して先に消化し、済んだら開始してよいことを知らせる。
手動のセッションが後から衝突を払うより、先に片付けておく方が全体の手戻りが少ない。

- **前提に入れるもの**: そのタスクが組み替える範囲（ファイル・モジュール）を触り、かつ
  そのタスクに吸収されないもの——利用者に届いている誤りの修正・安全網（そのタスクの前に
  あるべきテスト）・前段の後始末など。**入れないもの**: そのタスクの段階として充てられて
  いるもの（そのタスクがやる）と、ユーザー判断待ちで止まっているもの（判断が出るまで
  前提にすると永久に開始できない——一覧に「判断待ち」として別に示す）。
- 前提の一覧・根拠・残りを、その回の記録へ書く。完了の監査のたびに残りを更新する。
- **残りが0になったら、会話とプッシュ通知の両方で「開始してよい」と知らせる**。
  ユーザーはその場にいないことがある。

### キューの組み方

- **テーマ単位のキューにし、同じファイルを触るタスクは同じエージェントへ直列に積む**。
  並行する2本が同じファイルを触ると、後からpushする側がrebaseの衝突解消を払う。
  触るファイルはタスクエントリの背景・方針から見積もる（外れても致命的ではないが、
  大きな書き換え同士を並べない）。
- キューの中は**1タスク=1コミットで止まらず進む**。1タスクが保留で止まっても次へ進む。
- 振り出すエージェント同士に、互いが何を触っているかを指示文で伝える（衝突したときに
  相手の変更を尊重して統合する判断材料になる）。

### 上限と門

- **同時に動かす本数の上限は、`/orchestrate`の起動時に引数で受け取る**（例:
  `/orchestrate --concurrent 3`）。省略時は**3本**。根拠: 重い処理はロックで1本ずつしか
  走らないため、それを超えるエージェントは列に並ぶだけで、7〜8本でCPUが常時100%・
  ロック待ち最大16〜19分になった（[T1049](../records/tasks/T1049.md)第1回）。上限は
  **手元の作業ツリーで動いている本数**で数える（リモート指定でも手元で動くことがある）。
- 司令塔は上限を超えない。さらに次のどれかに当たる間は、上限に空きがあっても振り出さない
  （**門**）:
  - 完了したのに監査していないエージェントがいる（監査を先にする）
  - 見込み時間を超えた・一定時間コミットが無いエージェントがいる（是正を先にする）
  - ロック待ちが大きい、またはCPUが飽和している（`lockrun.py --report`・プロセス別CPU）
- 振り出すときに**見込み時間**を決め、状態の表に書く。見込みはタスクの規模の札
  （S=1時間 / M=半日 / L=1日以上）を起点にする。
- **門を開けるのは確認の結果であって、依頼の勢いではない**。「どんどん進めて」と言われても、
  門が閉じていれば振り出さず、閉じている理由を返す。

### 状態の表

`<gitの共通ディレクトリ>/orchestration/board.json`（全作業ツリーから読める・リポジトリには
入らない）を、エージェントの状態の**唯一の正本**にする。持つもの: 回の名前・上限・確認間隔・
手動中のタスク、エージェントごとの名前・担当キュー・実際の実行場所・状態（稼働／停止指示／
停止済み／完了・監査待ち／監査済み）・開始時刻・見込み時間・最後の記録。

司令塔は振り出し・停止・報告の受領・監査・定期確認のたびに更新する。会話にだけ状態を
置くと、司令塔のセッションが切れたとき・会話が要約されたときに失われる。
`/orchestrate:status`は、この表とリポジトリの事実（作業ツリーの最新コミット・タスクの
`状態:`・台帳の行）を突き合わせて出す。

### ローカルかリモートか

**本番（VMへのSSH・本番DB）・この開発機そのもの・手元のテストDBが要るものだけローカル**で
動かし、ほかはリモート（`isolation: "remote"`）で動かす。**ただし、リモート指定が実際に
リモートで動くとは限らない**——第1回では、リモート指定の全エージェントが手元に作業ツリーを
作り、手元で`npm ci`まで走らせていた。**最初の1本を振り出したら、手元に作業ツリーが
できていないか・手元のロックを取っていないかを確かめてから、2本目以降を足す**。

- リモートはエージェントごとに別の機械で、依存の導入・型検査・テストを取り合わない。
  Linuxなので開発機固有の遅さ（MSYSのプロセス起動・ウイルス対策の走査）を受けない。
- リモートからは本番へ触れない。本番での計測・データ移行が要る段は、リモートでコードを
  終えてローカルへ引き継ぐ（エージェントに「ユーザー確認事項（ローカル実行が必要）」として
  報告させる）。
- リモートは開発機のロック（下記）を共有しない。リモート同士・リモートとローカルの
  pushは、rebaseのやり直しで吸収する。

### 起動のしかた

- ローカルは`isolation: "worktree"`。**作業ツリーを同時に作ると、検証が時間切れになって
  起動が失敗する**——1本ずつ起動する。失敗して残った作業ツリーは中身を確かめてから
  片付ける（自動で作られた`.claude/settings.local.json`以外の変更が無いこと）。
- 指示文には、担当キュー・各タスクの注意点・並行する他エージェントの担当範囲を書き、
  共通ルールは本規約の「エージェント共通ルール」節を**読ませる**（貼り付けない）。
  リモートもリポジトリを読めるので同じでよい。

## エージェント共通ルール

司令塔から振り出されたエージェントは、着手前にこの節と`CLAUDE.md`の全文を読み、厳守する。

### 前提

- 各タスクの正本は`docs/records/tasks/Txxx.md`。完了条件を満たしたかで完了を判断する
  （「実装した」ではなく「効いている」）。
- 対象モジュールの`docs/modules/*.md`を手を入れる前に精読し、乖離は同じコミットで直す。
- ユーザーへ見せる文言・コミットメッセージ・最終報告はすべて日本語。
- 作業するのは自分に割り当てられた作業ツリーの中だけ。本体のチェックアウトと、他の
  セッション・エージェントの作業ツリーは触らない。自分が変更したのではない差分を
  見つけても戻さない。
- 最初に`git config core.hooksPath .githooks`が効いているかを確かめる（リモートでは
  設定されていないことがある。`--no-verify`は禁止）。
- `git add -A`・`git add .`は禁止。明示パスでステージし、コミット前に
  `git diff --cached --stat`を見る。
- 使い捨てのファイル（調査スクリプト・スクリーンショット・一時spec）はリポジトリに
  置かない（`frontend/e2e/`へ置いたspecはCIが拾う）。

### 重い処理は1本ずつ（ローカルのみ）

同じ開発機の上で複数のエージェントが走るため、重い処理は`scripts/lockrun.py`を通す:

```
python scripts/lockrun.py <ロック名> -- '<bashコマンド文字列>'
```

| ロック名 | 通すもの |
|---|---|
| `npm-ci` | `cd frontend && npm ci`（作業ツリーには`node_modules`が無い。ジャンクションで共有しない） |
| `frontend-heavy` | `tsc --noEmit`・`next typegen`・`vitest run <ファイル>`・`playwright test` |
| `backend-db` | backendの`pytest`（共有の`ridecompass_test`を掴むため） |
| `git-push` | fetch→rebase→pushの一続き（下記） |

backendのPythonは作業ツリーに`.venv`が無いため、本体のチェックアウトの
`backend/.venv`を使う。

### 本番

- 本番DBの読み取りの計測（`backend/scripts/run_probe.py`経由）は確認なしでよい。
- 本番への書き込み・削除・バッチ実行・常設設定の変更はしない。コード側を終え、必要な
  操作を最終報告の「ユーザー確認事項」に書く。
- `backend/**`をpushすると本番へ自動デプロイされる。既存DB行を新コードが読めなくなる
  変更は、本番データ移行が先（CLAUDE.md「コミット時の同期ルール」）——その場合は
  pushせずに報告する。

### 判断に迷ったら

ユーザーにしか決められない判断（本番から外れたものを残すか・利用者に見える情報量を
増やすか・要件の推定が要るもの）は、その項目だけ保留してタスクエントリに「保留: 何が・
なぜ・選択肢」を書き、残りを進める。保留があるタスクは`状態: 未完了`のまま台帳の行も
残す。新しいタスクは`scripts/new_task.py`で起票する。範囲外で見つけたものは、直さずに
最終報告へ書く（司令塔が担当のエージェントへ回す）。

### 完了とpush

- 完了の作法: `Txxx.md`の`状態:`を`完了`にして実施内容・検証結果を書き、
  `docs/improvement-plan.md`からその行を消す。本文に未実施の段階・派生が残るなら別Txxxへ起票。
- コミットメッセージには、実行したコマンドと観測値、実装の増減（実装・テスト・文書を
  分けて行数）を書く。
- **masterへは直接pushしない。** 1タスク（段階を切るなら1段階）を終えるごとに、最新の
  origin/masterへrebaseしてから、**自分の作業ブランチ**`orch/<エージェント名>`へpushし、
  司令塔へ「監査待ち」と報告する（コミットsha・検証のコマンドと観測値を添える）。masterへ
  進めるのは、監査を通したあとの司令塔である——backendはmasterへのpushで本番へ自動デプロイ
  されるため、監査を後にすると、監査前のものが本番に出る。

  ```
  git fetch origin master && git rebase origin/master && git push -f origin "$(git rev-parse HEAD)":refs/heads/orch/<エージェント名>
  ```

  作業ブランチはそのエージェント専用なので`-f`でよい。監査を待たずに次のタスクへ進んでよい
  （次のタスクのコミットは同じブランチに積む。司令塔は監査したコミットまでをmasterへ進める）。
- 司令塔から「差し戻し」が来たら、指摘を直して同じブランチへ積み直す。
- `docs/improvement-plan.md`は各エージェントが隣接する行を消すため衝突しやすい。
  rebaseでは**両方の削除を残す**（相手が消した行を戻さない）。

### 最終報告

キューを終えたら、タスクごとに: 状態（完了／一部保留／未着手）とコミットsha、何を直したか、
検証に使ったコマンドと結果、実装の増減、新規起票、ユーザー確認事項、見積もりとのずれ。

## 管理のループ（時間で回す）

**確認を、エージェントからの完了通知だけに頼らない。** 通知は終わったときにしか来ない
ので、止まっている・遅れているエージェントは何も知らせてこない——**何も起きていないことは、
時間で見に行かないと検知できない**。

- 振り出した時点で、定期確認を設定する（Claude Codeの`CronCreate`で20分ごと等。
  セッション限りで7日で切れる）。確認の中身を定期実行のプロンプトに書いておく。
- 1回の確認で見るもの（どれも軽いコマンドで取れるものだけ。飽和した機械に重い確認を
  足さない）:
  - 状態の表の稼働中エージェントごとに、作業ツリーの最新コミット時刻・未コミット件数・
    開始からの経過と見込み時間
  - 完了通知が来たのに監査していないもの
  - ロックの保持者と待ち（`python scripts/lockrun.py --report --since <回の開始>`）
  - CPUの空き
  - 共有設定が変わっていないか（`core.hooksPath`が相対の`.githooks`のままか——全作業
    ツリーで共有されるため、どのエージェントからでも書き換わる。第1回で実際に絶対パスへ
    戻っていた）
- 異常が無ければ表の最終確認時刻だけ更新する。異常があれば下の是正の段階へ進む。

### 異常の是正

進捗が無い（見込み時間を超えた、または一定時間コミットが無いのに未コミットの変更が
積み上がっている）エージェントには、次の順で手を打つ。

1. **問い合わせる**（`SendMessage`）: いま何に時間を取られているか・詰まっている点・
   あとどれだけか。
2. **途中の成果をコミットさせる**: 作業ツリーにだけある変更は、停止・事故で失われる。
   段階を切れるなら段階としてpushさせる。
3. **分割する**: 見積もりと実態が大きくずれていれば（第1回では規模Sの見立てのタスクが、
   撤去の連鎖と本番の欠陥の発見で数時間に膨らんだ）、残りを別タスクへ起票させて、
   終わっている段だけ閉じる。
4. **一時停止する**: 資源の逼迫が原因なら、上限を下げて止める（下の「止める手段の段階」）。

- 完了はエージェントの通知で知る。完了の確認のためにポーリングしない（上の定期確認は
  「何も起きていない」ことを見るためのもので、完了の検知には使わない）。

### 止める手段の段階

止める手段は、**止めたい原因（司令塔の注意・機械の応答・エージェントの協力）に頼らない**
ものを段階で持つ。前の段が効かなければ次へ進む。第1回で止められたのは、司令塔のセッションが
生きていて、機械が応答し、エージェントが指示を読めたからで、どれかが欠ければ段1は効かない。

| 段 | 手段 | 頼るもの |
|---|---|---|
| 1 | 停止の指示（`SendMessage`: 実行中のコマンドが終わったら、途中の成果をWIPコミットしてpushせずに報告して終える） | エージェントの協力。長いコマンドの途中では読まれない |
| 2 | 強制停止（`TaskStop`）。作業ツリーの変更はディスクに残る | 司令塔のセッション |
| 3 | **停止ファイル**: `<gitの共通ディレクトリ>/orchestration/STOP`を置く。置かれている間、`scripts/lockrun.py`は重い処理とpushを始めず、pre-pushフックはpushを拒否する | ファイル1つ。司令塔が不在でも、ユーザーがエクスプローラーで空ファイルを作るだけで効く |
| 4 | ユーザーの操作: アプリで各セッションを止める。それでも止まらなければOSでプロセスを止める（下記） | ユーザー |
| 5 | 被害の回復: masterへ出た誤りはrevertをpushする（backendは自動デプロイされるので、revertも自動で本番へ出る）。デプロイ自体を止めるならGitHubの画面でワークフローを無効にする。本番DBは取り直せない管理データのバックアップ（[T972](../records/tasks/T972.md)）から戻す | 手順とバックアップ |

段3は、エージェントが指示に従わなくても、**masterと本番へ何も出ていかなくする**ことが目的で、
作業そのものを止めるものではない（重い処理を新しく始めないので、負荷は下がっていく）。
解除はファイルを消すだけ。

段4でOSのプロセスを止めるとき（PowerShell。作業ツリーの下で動いているものだけを対象にし、
本体のチェックアウトや他のアプリを巻き込まない）:

```powershell
Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -like '*\.claude\worktrees\agent-*' } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

止めたあとの作業ツリーは消さない（未コミットの変更が残っている）。中身を確かめてから、
再開するか片付けるかを決める。
- エージェントが範囲外で見つけたものは、担当のエージェントへ`SendMessage`で回す
  （担当がいなければ起票）。
- 規約・手順を途中で変えたら、稼働中の全エージェントへ伝える。本規約の該当節を直し、
  指示文には「該当節を読み直せ」と書く。
- CIが赤くなったら、原因のコミットを**ログで**特定してから扱う。エージェントの
  「別のコミットのせい」は主張であり、直後のコミットで緑に戻るなら並列実行の不安定な
  テストを疑う（不安定なテストも、赤を本物と見分けられなくするので起票する）。

## 完了時の監査（masterへの門）

「監査待ち」の報告が来たら、次のタスクを振り出すより先に監査する。**監査がmasterへの門**で、
通したものだけを司令塔がmasterへ進める。

- 作業ブランチ`orch/<エージェント名>`の、前回masterへ進めた位置から報告のコミットまでを見る。
- 通したら、`git-push`ロックの中でorigin/masterへrebaseし（衝突したら自分で解かず、
  エージェントへ差し戻す）、masterへpushする。pre-pushの門もここで通る。
- 通さなければ、何が足りないかを添えてエージェントへ差し戻す。

### 監査の目的と範囲

監査は**「このコミットをmasterへ進めてよいか」を決める門**である。確かめるのは、
報告どおりのものが・報告どおりの証拠つきで・安全にmasterへ入る形になっているか。

**監査がやらないこと**: 設計のやり直し・コードの書き方の好みの指摘（`/code-review`の持ち物）、
フルスイートの実行（CIの持ち物）。監査で気づいた範囲外の改善点は、差し戻しの理由にせず
起票する。

### 監査の項目と合格の基準

1件の監査で、次をすべて見る。**止め（ブロック）**の項目が1つでも満たされなければ差し戻す。

| 項目 | 合格の基準 | 区分 |
|---|---|---|
| 1. 範囲 | `git diff --stat <前回masterへ進めた位置>..<報告のsha>`のファイルが、そのタスクの範囲に収まる。無関係なファイル・`.claude/settings.local.json`・使い捨てのspec/スクリプト・生成物の取り残しが無い | 止め |
| 2. 記録の整合 | `Txxx.md`の`状態:`と台帳の行が一致する（完了なら行が無い、未完了なら残る）。未完了なら「保留: 何が・なぜ・選択肢」がある。本文に残った未実施の段階・派生は、別Txxxとして台帳にある | 止め |
| 3. 完了条件 | `Txxx.md`の完了条件の各項目に、満たしたことを示す記述（何で確かめたか）がある。満たせない項目は保留として書かれている | 止め |
| 4. 検証の証拠 | コミットメッセージに、実行したコマンドと観測値がある。検知器・ガード・テストを足したなら、わざと壊した入力で落ちることの確認がある。緩和を入れたなら、外した件数と割合がある | 止め |
| 5. 主張の抜き取り検証 | 報告の中の事実の主張のうち、**結論が最も強く依存しているもの**を少なくとも1つ、司令塔が自分で確かめる（コード・公式資料・DB・CIのログ）。誤っていれば、その主張に依存する結論も疑う | 止め |
| 6. 本番への影響 | `backend/**`を含むなら、既存のDB行を新コードが読めるか・マイグレーションが要るか・本番での操作が要るかが書かれていて、要る操作は実施されずに「ユーザー確認事項」になっている。秘匿情報を含まない | 止め |
| 7. 同期ルール | CLAUDE.md「コミット時の同期ルール」の対象（API・Pydantic・レジストリ・domain定数・焼き込み値）を変えたなら、生成物が同じコミットで再生成されている。対象モジュールの`docs/modules/*.md`が実装に追従している | 止め |
| 8. 報告の正確さ | 報告の数字・sha・ブランチ名が事実と合う。誤りは止めにはしないが、記録に残す（「回ごとに残す記録」の「監査で見つかった誤った主張」） | 記録 |
| 9. 見積もりとのずれ | 規模・所要が見立てと大きくずれていれば、その理由が書かれている | 記録 |

### 監査の結果

- **通す**: `git-push`ロックの中でorigin/masterへrebaseし、masterへpushする。pre-pushの門も
  ここで通る。push後のCIを定期確認で見て、赤ければ原因をログで特定する。
- **差し戻す**: 満たしていない項目と、何があれば通すかを具体的に書いて、担当のエージェントへ
  返す。司令塔が自分で直さない（直すと、監査した者と書いた者が同じになる）。
- **保留する**: ユーザーの判断がmasterへ入れる前に要るもの（本番のデータを読めなくする変更等）は、
  判断が出るまで進めない。

### 監査の記録

状態の表の各エージェントに、報告を受けた時刻・監査を終えた時刻・結果・止めた項目・見つけた
誤りを書く。監査の待ち時間（報告から結果まで）は「回ごとに残す記録」の測るものに入れる——
門を置いた以上、監査の遅れはそのまま全体の処理量の低下になる。

保留になった判断は、まとめてユーザーへ出す。答えが出たら、そのタスクを担当した
エージェントへ`SendMessage`で返す（会話の文脈を持っているので、新しく起動するより速い）。

## 環境

- **開発機が飽和しているときは計測しない**。性能の比較は、条件をそろえて交互に複数回
  取った中央値か、本番で取る。
- **遅いときは環境を先に疑う**。プロセス1つの起動に秒単位かかるなら、コードではなく
  ウイルス対策の走査・CPUの飽和を測る（`Get-Counter`でプロセス別のCPU使用率）。
  実例: 第三者製のウイルス対策が並行実行中にCPUの2〜4割を使い、`git status`が5倍遅く
  なっていた（[T1049](../records/tasks/T1049.md)）。
- `core.hooksPath`は相対パス（`.githooks`）にする。絶対パスだと全作業ツリーが本体の
  チェックアウトにあるフックを走らせ、作業ツリーで直したフックが効かない。

## 改善の回し方

本規約は、1回の並行実行（司令塔が振り出してから全エージェントが終わるまで）ごとに実測で
直す。**実測の裏付けが無い変更は入れない**（CLAUDE.md「修正の原則」と同じ基準）。

### 回ごとに残す記録

1回ごとに`scripts/new_task.py`で記録用のタスクを起こし（または既存の改善タスクへ追記し）、
次を書く。数字は取り方も一緒に書く。

| 測るもの | 取り方 | 何を見るためか |
|---|---|---|
| 振り出したタスク・完了・保留の数 | 台帳と各エージェントの最終報告 | 振り出しの精度（保留が多いなら「振り出さない」基準が緩い） |
| ロックごとの待ち時間・保持時間・失敗回数 | `python scripts/lockrun.py --report --since <開始時刻>` | どの資源が詰まったか（待ちが大きいロックは分割・リモート化の候補） |
| `git-push`ロックの失敗回数 | 同上の「失敗」列 | rebaseの衝突・フックの拒否の頻度（キューの組み方の良し悪し） |
| 監査で見つかった誤った主張 | 監査のたびに司令塔が控える | 監査の項目に何を足すか |
| 監査の待ち時間 | 状態の表の報告時刻と監査終了時刻の差 | 監査が門として全体を詰まらせていないか |
| ユーザー判断の待ち時間 | 保留が上がってから答えが返るまで | 保留の出し方（まとめ方・粒度） |
| CIの赤とその原因 | CIの履歴とログ | 不安定なテスト・並行pushによる破損の有無 |

### 直し方

1. 回の終わりに振り返り（`/task:retro`）を行い、本規約への変更案を出す。
2. 変更案ごとに、上の記録のどの数字が根拠かを書く。根拠が無いものは次の回で測る項目に
   回し、本規約へは入れない。
3. 当てたら、次の回で同じ数字が動いたかを見る。動かなければ変更を取り消すか、別の
   原因を探す。
