# T449. architecture.mdが軸スタジオのshape kind数について自己矛盾している（4種 vs 2プリミティブ）

状態: 完了（2026-08-31）

- 背景: `統合レビュー第2回`（`.claude/commands/review/history/2026-08-30_all_2.md`P2-5）の
  指摘。`docs/architecture.md:754`が軸スタジオの「4種のshape kind」（T332時代の記述）と
  書いたままで、同ファイル1531-1563行のT396時代の書き直し（2プリミティブ+合成という
  正しい設計への言及）と自己矛盾している。
- 対応方針: 754行目の古い記述を、T396以降の実装（2プリミティブ設計）に合わせて訂正する。
- Confidence: High（docsシャードが両箇所を直接読み比較）。
- 規模S。

## 実施記録（2026-08-31完了）

- **対応**: `docs/architecture.md`754行目付近（AxisComposer.tsxの説明）のうち、T332時点の
  「4種のテンプレート（categorical/breakpoint_linear/flag_sum/
  recipe_then_breakpoint_linear）」「内部で保持する変換テンプレート自体（4種のshape
  kind）は変わらず」という記述を、T396/T397後の実態（保存時の`kind`は常に
  `breakpoint_linear`/`categorical`の2プリミティブへ正規化、選択カードは「なめらか
  評価」「ぴったり評価」「かけあわせ評価」の3枚）へ書き換えた。1524行目以降の
  「shapeの2プリミティブ化」節（詳細な設計経緯）は変更せず、754行目からその節への
  参照を追加する形で整合させた。
- **横展開チェック**: `flag_sum`・`recipe_then_breakpoint_linear`・「4種のshape kind」
  等の文言をgrepし、他に古いまま残っている記述が無いことを確認した（他の言及箇所は
  いずれも「旧」を明示した正しい歴史的経緯の記述だった）。
- **副次的な完了**: この修正は[T427](T427.md)の指摘2（同じ箇所を別の統合レビュー第9回が
  検出したもの）とも同一のため、T427自体も本タスクの修正で完了とした
  （T427は指摘1[T448で解消確認]・指摘2[本タスクで解消]の両方が済んだため）。
