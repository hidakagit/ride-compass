# T720. 停止密度の飽和解消を本番の軸インスペクタで確認する 規模S

状態: 完了（2026-09-12）（2026-09-11起票、[T655](T655.md)の完了条件から分離）

## 背景

[T655](T655.md)の完了条件のうち「停止密度の難易度が全wayで100に張り付いていないこと
（本番の軸インスペクタで確認）」だけが、本番環境の操作を伴うため未確認のまま残っている。
他の3条件（Contextのフィールド削減・種別別の保持・横断歩道の重み0）はコードとテストで
確認済み。

[T655](T655.md)が飽和を確認した測定は**本番**（東京都心のwayを無作為抽出して全件100.0、
タイル6枚555kmで延長の92.3%が飽和点超え）だったのに対し、直し方の効果を測ったのは
**開発DB**（3,000 Edge・191.9km）である。開発DBでは「4回/km以上が占める延長」が41.2%→17.0%
（信号のみなら8.7%）へ下がったが、同じ改善が本番の密度でも起きているかは測っていない。

## 保留することで何が起きうるか

- 停止密度が本番でも飽和したままなら、「ストップアンドゴーが少ないルート」は依然として
  作れない。軸の重みをいくら上げても候補道路がほぼ同点のためルートが変わらず、
  **症状はエラーではなく「重みが効かない」という無言の形**で出る。
- [T719](T719.md)の判断1（`intersection_count_per_km`を軸として復活させるか）が、
  停止側の本番分布を入力にできないまま止まる。

## 確認手順

計測部分は`backend/scripts/measure_axis_saturation.py`として用意済み
（[T719](T719.md)と同じ2026-09-11。軸スタジオの分布プレビューと同じ母集団・同じ材料の
組み立てを使い、違いは折れ点を通した**後**の難易度を見る点）。軸インスペクタで手作業で
無作為抽出する代わりに、延長で重み付けた分位点と上端・下端の割合が一度に出る。

1. 本番のbackendが読むDBに対して実行する:

   ```
   python scripts/measure_axis_saturation.py
   ```

   公開軸すべてを一覧する。停止密度だけ見るなら`--axis <軸id>`、サンプルを広げるなら
   `--sample-percent`。

2. 停止密度の「上端」（難易度95以上が占める延長の割合）が大半を占めていないことを見る。
   [T655](T655.md)の起票時点では合成値`stop + 0.3×intersection`で延長の92.3%が飽和点超え
   だった。開発DBでの直し後の実測では「4回/km以上」が41.2%→17.0%へ下がっている。

3. 張り付いている場合は折れ点
   （現在`[(0.0,0.0),(0.5,20.0),(1.5,50.0),(3.0,75.0),(5.0,100.0)]`）を実測の分位点から
   引き直す。折れ点の調整は軸スタジオ経由（unpublish→PUT→republish、CLAUDE.md
   「コミット時の同期ルール」）で行い、[deployment-sync.md](../deployment-sync.md)の
   完了条件に従う。

## 完了条件

- 本番のDBに対して`measure_axis_saturation.py`を実行し、停止密度の上端の割合を
  本タスクへ記録すること
- 張り付いていた場合は折れ点を引き直し、引き直した値と引き直し後の実測を記録すること

## なぜ自動化して終わりにしないか

この計測をCIや起動時チェックへ組み込むことは**しない**。飽和は「壊れている」とは限らず、
本当に全区間が難しい軸（都心の停止密度など）もありうる。閾値で機械的に落とすと、
正しい軸を落とすか、通すために閾値を意味の無い値まで緩めるかのどちらかになる。
判断は人が見て行い、機械は数字を出すところまでにする。

## 実測（2026-09-12、本番DB）

`measure_axis_saturation.py`を本番のDATABASE_URLに対して実行した。停止密度の軸idは
本番では`axis_abdaded0be20`。折れ点は`[(0.0,0.0),(0.5,20.0),(1.5,50.0),(3.0,75.0),(5.0,100.0)]`、
`terms`は信号1.0・踏切1.5・一時停止0.3・横断歩道0.0。

| 母集団 | way | 延長 | p10 | p50 | p90 | p99 | 上端(≧95) | 下端(≦5) |
|---|---|---|---|---|---|---|---|---|
| 全域（TABLESAMPLE 2%） | 20,000本 | 3,786km | 0.0 | 0.0 | 60.6 | 100.0 | 3.9% | 82.0% |
| 都心（千代田〜新宿・文京） | 8,360本 | 1,142km | 0.0 | 67.6 | 100.0 | 100.0 | 34.5% | 39.0% |
| 23区西部（世田谷・杉並） | 18,674本 | 2,530km | 0.0 | 0.0 | 100.0 | 100.0 | 12.3% | 64.5% |
| 多摩（立川・八王子） | 19,015本 | 2,833km | 0.0 | 0.0 | 99.8 | 100.0 | 11.4% | 73.6% |

都心のbboxは`35.65,139.72,35.71,139.80`。[T655](T655.md)が飽和を観測したのも都心のため、
比較はこの行で行う。**上端は92.3%（[T655](T655.md)起票時）から34.5%へ下がり、同じ地域で
p25=0.0・p50=67.6・p75=100.0と値が散るようになった**。飽和は解消している。

## 折れ点を引き直さない判断

都心では延長の34.5%がなお上端（≧95、生値で約4.8回/km以上）に載り、生値のp99は27回/kmまで
伸びるため、上端の内側は互いに区別できない。それでも折れ点は引き直さない——この軸の役割は
「停止の少ない道を選ばせる」ことで、そのために要るのは**下側の分解能**である。都心でも
延長の39.0%が下端に、p50が67.6にあり、避ける先の候補は十分に残っている。上端の内側で
「多い」と「極端に多い」を区別できても、どちらも避ける対象である以上ルートは変わらない。

引き直しが要るのは、下端の割合が小さくなって「選べる道が無い」状態になったときで、
その兆候はこのスクリプトの下端の列に出る。

## 併せて分かったこと

- **全域の平均だけでは地域差が見えない**。同じ軸の上端が全域3.9%に対し都心34.5%で、
  約9倍違う。全域だけを見て「飽和していない」と結論すると、実際に走る市街地の状態を
  見落とす。スクリプトへ`--bbox`を足し、地域を絞って測れるようにした。
- **舗装質が全区間で材料欠損**（全域・都心とも）。Way単位の材料解決が`surface`を
  tags jsonbから読もうとしているが、`surface`は専用列で入っていない。区間インスペクタで
  舗装質が常に「データなし」になる実害があり、[T753](T753.md)へ分離した。
- 勾配・風はWay単位では算出できない材料（ルート文脈が要る）のため、このスクリプトの
  母集団では原理的に測れない。

## 関連

- [T655](T655.md): 停止密度の作り直し本体（完了条件の出所）
- [T719](T719.md): 旧集計の撤去（本タスクの結果を判断の入力にする）
- [T753](T753.md): Way単位の材料解決が舗装タグを拾えていない（本計測中に発覚）
