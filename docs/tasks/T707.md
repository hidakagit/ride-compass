# T707. docs乖離の一括是正（architecture.md・design-principles.md・docs/modules全15ファイル） 規模M

状態: 完了（2026-09-10起票、統合レビュー第6回 P2/P3の束ねA。対象26件／2026-09-10完了）

## 背景

`scripts/review_checks.py docs`は全項目0件だが、**機械では拾えない型**の乖離が26件残っている。
内訳は「実装に存在しない識別子を名指し（21件）」と「個数・組み合わせを語る表現の陳腐化（12件、一部重複）」。

## 対象（実装に存在しない識別子）

`find_dead_file_refs`はバッククォート付き**ファイル名**しか見ないため識別子は素通りする（[T705](T705.md)）。

| 場所 | 記述 | 実体 |
|---|---|---|
| `docs/design-principles.md:52` | 構造仕様5が「やむを得ない場合は**wind_score方式**を必須とする」 | `wind_score`は`backend/app`・`frontend/src`に**存在しない**（grep 0件、T462で消滅） |
| `docs/design-principles.md:70` | 構造仕様9が`DEDICATED_WAY_VALUE_LAYER_IDS`を正の例に | T672で撤去、後継は`axisLayers.ts:291 DEDICATED_WAY_VALUE_AXES` |
| `docs/modules/backend/axis-studio.md:236` | 同上 | 同上 |
| `docs/architecture.md:2572` | 同上 | 同上 |
| `docs/modules/backend/elevation.md:65` | `graph_material_cache.py: _LRUCache` | `cachetools.LRUCache` |
| `docs/modules/backend/elevation.md:86,119` | 「`use_cache=False`でRedis cache-asideを迂回する」 | `use_cache`引数は無く、edge geometryのRedis cache-aside自体がT652で撤去済み |
| `docs/modules/backend/routing-engine.md:603` | `RouteScoreComponent` | `domain/route.py`に無し |
| `docs/modules/backend/weather-dynamic-layers.md:301`／`frontend/dynamic-weather-layers.md:147` | `WIND_VARIABLES` | `msm_client.py: FORECAST_VARIABLES` |
| `docs/modules/frontend/axis-studio.md:289,291` | `EDITED_PAYLOAD_KEYS`・`_AllPayloadKeysCovered` | 型`EditedPayloadKey`（`AxisComposer.tsx:382`）・`_PayloadKeyCoverage`（同`:420`） |
| `docs/modules/frontend/dynamic-weather-layers.md:157,162` | `dynamicWeatherStatus(loading, error, hasPayload)` | `mapLayers.ts: deriveFetchLayerStatus(..., hasFetched)`（第4引数はT674で追加） |
| `docs/modules/frontend/dynamic-weather-layers.md:249` | `ensureGradientFillLayer` | `makeEnsureGradientFillLayer`（`MapView.tsx:1437`） |
| `docs/modules/frontend/dynamic-weather-layers.md:265` | `riskTargetTimesUrl()`等5関数 | すべて削除、`jmaNowcastFrames.ts:120 JMA_TARGET_TIMES_PATHS`＋`:130 jmaTargetTimesUrl(id)`へ集約 |
| `docs/architecture.md:2488-2490` | `useDynamicWayValues.ts: useDynamicWayValues(materialId, ...)` | `useDedicatedWayValues.ts: useDedicatedWayValues(axes, ...)`（**ファイル名・関数名・第1引数の意味がすべて変わっている**） |
| `docs/architecture.md:649` | `renderMapColorToggle`・「軸ごとのチェックボックス＋重みスライダー」 | 存在しない（レンズの入口は`LensControl`のみ、重みスライダーも撤去済みで帯グラフのドラッグ・矢印キー） |
| `docs/modules/frontend/route-settings-and-results.md:225` | `RouteCandidate.wind_score`/`road_score` | backend・frontend双方に0件 |

## 対象（個数・組み合わせを語る表現）

| 場所 | 記述 | 実態 |
|---|---|---|
| `docs/architecture.md:1205` | 7章の見出し「静的道路属性と**7軸**評価モデル」 | 配下の小見出しは「**8軸**の一覧と重み」（`:2025`も引用）、実データの公開軸は**10本** |
| `docs/architecture.md:1274-1282` | 7章の軸表 | `curvature`の行が無い／停止密度の`axis_id`が`stop_density`（実体は`axis_abdaded0be20`）／自転車インフラが「正規化フラグ材料4件」（実際は5件、`axis_admin.py:270-271`は同diffで5件へ更新済み） |
| `docs/batch-pipeline-dependencies.md:24-60` | 「配下10バッチ」の依存DAG | `precompute_edge_curvature.py`を一切扱っていない（番号付きだけで11本） |
| `docs/modules/backend/static-road-attributes.md:17` | 「precompute系**4本**が共有する」 | 5本 |
| `docs/modules/backend/evaluation-scoring.md:19` | 「domainの**5ファイル**は変更理由で分けてある」 | 直上の対象ファイル表は8ファイルを列挙 |
| `docs/modules/frontend/dynamic-weather-layers.md:161,170` | 「**3つ**のチップ付き動的気象レイヤー」→「この**9レイヤー**」 | 同じ集合を9行違いで別の数で数えている |
| `frontend/src/app/globals.css:106` | 「ここに列挙した**6段**しか無い」 | 直後のトークンは7つ（`docs/frontend-design-system.md`側は「7段」と正しい） |
| `docs/modules/frontend/axis-studio.md:35,233-236` | 鮮度パネルの対象テーブル3つ | `GENERATION_FRESHNESS_SPECS`は4テーブル（`way_landcover`が抜け） |

## その他のdocs乖離

- `docs/modules/frontend/route-settings-and-results.md:136-137`「呼び出し側は`axes`を重み>0の軸のみへ
  絞り込んで渡す」→ `page.tsx:1718`は`axes={axisCatalog.axes}`（公開軸すべて）。
  同ドキュメント直後の記述とも矛盾し、信じて絞り込むと「未使用」バッジが構造的に出なくなる。
- `docs/modules/frontend/route-settings-and-results.md:218` — ComparisonPanelの得点行を「生値行」と呼ぶ。
  同ファイル`:137`以降でT687が導入した「生値」は`axis_raw_values`を指す（**同一ファイル内で語が衝突**）。
  この一文は`docs/modules/README.md`「記載粒度」が禁じる経緯記述（「〜へ置き換えた」）でもある。
- `docs/modules/frontend/page-composition.md:144` — `reloadKey`の説明が「2段階復元」のままだが、
  本範囲で`deserialize`は常に`{...DEFAULT_LAYER_VISIBILITY}`だけを走査する形へ変わった
  （`page.tsx:546-548`のコメントは正しく書かれており**docsと実装とコード内コメントが三者で食い違う**）。
- `docs/modules/frontend/static-map-layers.md:16,63` — `primaryAttributes.ts`を「双方向導出」
  （逆引きはT308で撤去済み）、`buildStaticOverlayLayers`を1引数で図示（実装は4引数）。
- `docs/modules/frontend/static-map-layers.md` — 「出所の異なる2つの…であるの2系統:」という
  編集途中の壊れた一文。路面ポップアップの`value_labels`生成物依存も未記載。
- `docs/modules/frontend/axis-studio.md:255-258,217` — ポップオーバーCSSの共有先が
  `recipeControls.module.css`のままだが、実装は`@/components/ui/`を直接import。
- `docs/architecture.md:2419-2421`「ルート確定後: パラメータ指定UIは終了する」→
  `page.tsx:2015-2017`は`TravelBearingControl`を`hasDetail`に関係なく常時描画。
- `docs/frontend-design-system.md:65` の契約の過大主張は[T705](T705.md)で扱う。

## 実施上の注意

`docs/modules/README.md`「記載粒度」が禁じる経緯記述を**新たに書き加えない**こと
（CLAUDE.md「毎タスク完了時の即時チェック」3）。個数を語る表現は数字を更新するのではなく、
[T671](T671.md)が決めた方針どおり**個数を語らない表現へ書き換える**。

## 実施内容（2026-09-10）

対象26件のうち、`docs/batch-pipeline-dependencies.md`の1件（`precompute_edge_curvature.py`
が依存DAGに無い）は[T699](T699.md)で解消済みだったため、残り25件を実施した。

### 実装に存在しない識別子

| ファイル | 変更 |
|---|---|
| `design-principles.md` 構造仕様5 | `wind_score`方式の名指しを、方式そのものの記述（docs明記＋レスポンスの識別フィールド＋両側の型コメントの3点）へ |
| `design-principles.md` 構造仕様9 / `modules/backend/axis-studio.md` / `architecture.md` | `DEDICATED_WAY_VALUE_LAYER_IDS`→`axisLayers.ts: DEDICATED_WAY_VALUE_AXES`（3箇所） |
| `modules/backend/elevation.md` | `graph_material_cache.py: _LRUCache`→`cachetools.LRUCache`／`use_cache=False`でRedisを迂回（引数・cache-aside自体が無い）→DBへ直接問い合わせる（2箇所） |
| `modules/backend/routing-engine.md` | `RouteScoreComponent`の列挙を削除 |
| `modules/backend/weather-dynamic-layers.md` / `modules/frontend/dynamic-weather-layers.md` | `WIND_VARIABLES`→`msm_client.py: FORECAST_VARIABLES`（実際の変数も気温・雲量を含む） |
| `modules/frontend/axis-studio.md` | `EDITED_PAYLOAD_KEYS`→型`EditedPayloadKey`、`_AllPayloadKeysCovered`→`_PayloadKeyCoverage` |
| `modules/frontend/dynamic-weather-layers.md` | `dynamicWeatherStatus(loading, error, hasPayload)`→`mapLayers.ts: deriveFetchLayerStatus(..., hasFetched)`（第4引数の役割も追記）／`ensureGradientFillLayer`→`makeEnsureGradientFillLayer`／`riskTargetTimesUrl()`→`jmaTargetTimesUrl(id)`＋`JMA_TARGET_TIMES_PATHS` |
| `architecture.md` | `useDynamicWayValues.ts: useDynamicWayValues(materialId, ...)`→`useDedicatedWayValues.ts: useDedicatedWayValues(axes, ...)`（第1引数の意味の違いも明記）／`useWindAxisPenalties.ts`→`useDedicatedWayValues.ts`／`renderMapColorToggle`と「軸ごとのチェックボックス＋重みスライダー」→レンズは`LensControl`のみ・重みは帯グラフのドラッグ／`RouteSettingsPanel.tsx: mapColorLayerIdFor`の名指しを削除 |
| `modules/frontend/route-settings-and-results.md` | `RouteCandidate.wind_score`/`road_score`への言及を削除 |

### 個数・組み合わせを語る表現

[T671](T671.md)の方針どおり、数字を更新するのではなく**個数を語らない表現へ**書き換えた。

- `architecture.md`: 7章見出し「7軸評価モデル」→「評価軸モデル」、「8軸の一覧と重み」→
  「評価軸の算出元と重み」、`overall_difficulty`の説明の「8軸すべて」→「公開軸すべて」、
  `evaluation.py`の「7軸の重み」→「axis_idキーの重み辞書」、`useAxisCatalog.ts`の
  「既存7軸の静的フォールバック」→「ビルド時点の公開軸」、区間インスペクタの「6軸」・
  「全8軸重み」→軸を数えない表現。
- 7章の軸表は**軸の一覧そのものを載せるのをやめ**、「軸ごとの算出元と背景」（DBの行だけ
  からは辿れない情報）に絞った。軸・`axis_id`・既定重みは`axis_definitions`テーブルが
  唯一の正本で、GUIから増減するため表に写すと必ず陳腐化する（実際`curvature`・`openness`の
  行が欠け、停止密度の`axis_id`が実体[自動採番]と違っていた）。`axis_id`がGUI作成軸では
  軸名と一致しない旨だけを注記した。
- `modules/backend/static-road-attributes.md`「precompute系4本」→「precompute系バッチ」。
- `modules/backend/evaluation-scoring.md`「domainの5ファイル」→「domainのファイル」。
- `modules/frontend/dynamic-weather-layers.md`「3つのチップ付き…」「この9レイヤー」→
  `DYNAMIC_WEATHER_LAYER_IDS`／「動的気象レイヤー」（同じ集合を別の数で数えていた）。
- `frontend/src/app/globals.css`「ここに列挙した6段しか無い」→「ここに列挙したものしか無い」、
  対になる`docs/frontend-design-system.md`の「7段のトークン」→「`--z-*`トークン」。
- `modules/frontend/axis-studio.md`の鮮度パネルは対象テーブル名の列挙をやめ、
  backendの`GENERATION_FRESHNESS_SPECS`が決める旨へ（`way_landcover`が抜けていた）。

### その他のdocs乖離

- `route-settings-and-results.md`: `RouteAxisProfile`へ渡す`axes`は公開軸すべて（重み0の軸を
  落とすと「未使用」バッジが構造的に出せない）／ComparisonPanelの「材料値の生値行」・
  「個別軸の生値行」は同ファイル内でT687の「生値」（`axis_raw_values`）と語が衝突するため
  「材料値の行」「軸ごとの難易度の行」へ／生値の書式例を実装の出力（`0.8回/km`）へ。
- `page-composition.md`: `reloadKey`の例を、キー集合がカタログに依存しない
  `layerVisibility`の実際の理由（移行後の値を2回目の復元が読み直せるよう揃える）と、
  2段階復元が本当に意味を持つ`lens`の両方が分かる形へ。
- `static-map-layers.md`: `primaryAttributes.ts`の「双方向導出」→一方向（逆引きは撤去済み）／
  `buildStaticOverlayLayers`の図示を実際の4引数へ／編集途中で壊れていた一文を修復／
  路面ポップアップの`smoothness`値ラベルが生成物経由でbackendから来ることを追記。
- `modules/frontend/axis-studio.md`: ポップオーバーCSSの共有先を実装どおり
  `@/components/ui/`（`infoButton.module.css`・`floatingPopover.module.css`）へ（2箇所）。
- `architecture.md`: 「ルート確定後: パラメータ指定UIは終了する」から、常時描画されている
  走行方位コントロールと存在しない`renderMapColorToggle`への言及を外した。

`docs/frontend-design-system.md:65`の契約の過大主張は予告どおり[T705](T705.md)へ残す。

## 検証結果

- 置き換え後の識別子（`DEDICATED_WAY_VALUE_AXES`・`FORECAST_VARIABLES`・
  `deriveFetchLayerStatus`・`makeEnsureGradientFillLayer`・`jmaTargetTimesUrl`・
  `JMA_TARGET_TIMES_PATHS`・`EditedPayloadKey`・`_PayloadKeyCoverage`・
  `useDedicatedWayValues`・`fetchDynamicWayValues`・`GENERATION_FRESHNESS_SPECS`・
  `buildRoadSurfacePopupHtml`）が実装に存在することを1件ずつgrepで確認した。
- 軸の実データ（`backend/fixtures/axis_definitions_snapshot.json`）と突き合わせ、
  7章の表の欠落（`curvature`・`openness`）・`axis_id`の相違・材料件数の相違を確認した。
- `python scripts/review_checks.py docs` 違反なし。`globals.css`を触ったため
  frontendの`tsc --noEmit`・`eslint src`・`vitest run src/app/page.test.tsx`も実行しクリーン。
