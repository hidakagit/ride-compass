# T684. レビュー基盤を是正する（実機確認の場所・記載漏れ）

規模S。

状態: 完了（2026-09-09）

## 背景

統合レビュー第5回（[2026-09-08_all.md](../../.claude/commands/review/history/2026-09-08_all.md)）
が検出した指摘のうち、下表の2件を1つの起票単位として扱う。

実機確認をレビュー用worktreeで行おうとすると`npm ci`（3〜8分・約1GB）が必要になる。
**実機確認は本体の開発環境で行い、worktreeはコード精読に限る**旨をui.mdへ明記する。

## 対象の指摘

| # | Sev | 対象 | 内容 |
|---|---|---|---|
| 12-3 | P3 | `.claude/commands/review/ui.md` | 実機確認をレビュー用worktreeで行おうとすると、`node_modules`ジャンクションをTurbopackが拒否し、`npm ci`（3〜8分・約1GB）が必要になる。**実機確認は本体の開発環境で行う**旨を明記していないため、この回り道が再発しうる（本レビューで実際に発生） |
| 12-4 | P3 | `frontend/src/components/Map/secondaryAxes.ts` | `docs/modules`のどの対象ファイル表にも無い。属すべきは`static-map-layers.md`（推定指標チップグループの軸一覧生成が同モジュールの責務） |

各項目のEvidence・failure_scenario・Recommendationはレビュー結果の全件表を参照する。

## 実施内容（2026-09-09）

| # | 対応 |
|---|---|
| 12-3 | `ui.md`「実施方法（必須）」へ、**実機確認は本体の開発環境で行う**（コードの精読はworktreeでよいが、dev環境の起動だけは本体へ戻る）ことを明記した。原則側だけを読んで回り道しないよう、`principles.md`の共通実行手順3（レビューは専用worktreeで実施する）にも例外として1行添えている。経緯は`_history.md`へ集約した（レビュー基盤の正式版は手順のみを書く規約） |
| 12-4 | **対応不要**。レビュー対象コミット`3749bdcf`より後の`ec23948d`（T656、同日）で`Map/secondaryAxes.ts`が`static-map-layers.md`の対象ファイル表へ既に追記されていた |

## 検証

`ui.md`・`principles.md`・`_history.md`のみの変更でコード変更を含まない。
`review_checks.py docs --staged`（pre-commit）で違反なし。
