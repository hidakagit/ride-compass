# T427. architecture.mdの記述整合（サイドバー4分類・軸スタジオテンプレート数の書き換え）

状態: 未完了（指摘1は解消済み、指摘2が残る。2026-08-30起票・統合レビュー`/review:all`第9回で発見）

- 背景: [統合レビュー2026-08-30](../../.claude/commands/review/history/2026-08-30_all.md)の
  consistency/complexity両レンズが独立に発見。詳細はhistory参照。
- 指摘1（consistency F-1）: `docs/architecture.md:2299-2301`・`2477-2482`が「サイドバーは
  T406のスコープ外として観測/推定の2区分のまま」と記述しているが、[T413](T413.md)で
  既にサイドバーも`mapOverlayGroupFor`による4分類（現在は[T418](T418.md)完了により
  3分類）へ統一済み。同日中に覆った設計判断を「意図的な現状維持」として誤って断定している。
  → **[T448](T448.md)（2026-08-31）で確認: T413/T418とは無関係な後続の変更（架空・
  モジュール設計書整備等でarchitecture.mdが広く書き換わった過程）で、この誤った記述
  自体が既に本文から消え、現在は`docs/architecture.md`2347行目付近で「サイドバー...も
  改善計画T413で同じ`mapOverlayGroupFor`を単一ソースとして使うよう統一済み」と正しく
  記述されている（grep確認済み）。追加の書き換え作業は不要、Finding 1は解消済みとして
  クローズしてよい。**
- 指摘2（complexity F-2）: `docs/architecture.md:735`と`1514-1553行目`（T396/T397完了時の
  経緯節）で、軸スタジオのテンプレート/カード構成の記述が矛盾している（前者は「4種の
  テンプレート...変わらず」、後者は「3カード・2プリミティブへの再設計」）。
  → **未解決のまま残っている（2026-08-31、T448確認時点でも該当箇所は現存）**。
  現在の行番号は754行目付近（「内部で保持する変換テンプレート自体（4種のshape kind）は
  変わらず」）と1524行目以降（「shapeの2プリミティブ化（改善計画T396）」「T397で3カードへ
  再設計」）——architecture.mdの他箇所の加筆により当初の行番号からはずれているが、
  矛盾する記述自体は解消されていない。
- 対応方針: 指摘2（テンプレート/カード構成の矛盾）を実装（T396/T397後の状態、shape kindは
  2プリミティブ[breakpoint_linear/categorical]・UIは3カード）に合わせて書き換える。
  指摘1は対応不要（解消済み）。
