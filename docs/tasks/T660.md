# T660. precompute系4本の全量ロードをkeyset paginationへ

規模M。

状態: 未着手（2026-09-08起票）

## 背景

統合レビュー第5回（[2026-09-08_all.md](../../.claude/commands/review/history/2026-09-08_all.md)）
が検出した指摘のうち、下表の1件を1つの起票単位として扱う。

**2026-09-04に本番で発生済み**——VMメモリ枯渇でSSH/HTTPともに6時間14分応答不能、
強制電源サイクルで復旧（`docs/disaster-recovery.md:120-140`）。記録された対策は
swapfile追加・`--memory`指定という運用回避のみで、全件ロードの構造は4本すべてに現存する。
実施タイミングはdisaster recovery手順の再実行が必要になりうるため運用状況と合わせて判断する。

## 対象の指摘

| # | Sev | 対象 | 内容 |
|---|---|---|---|
| 2-1 | P1 | `precompute_edge_attribute_counts.py:58-60` / `precompute_elevation_attributes.py:49-63` / `precompute_way_attribute_counts.py:62-67` / `precompute_way_landcover.py:107-122` | 4本すべてが「チャンク分割する前に対象IDを`result.all()`で全件取得しPythonの`list`へ入れる」同一パターン。`road_edges`は本番約500万件で`edge_id`は`String`型。2本は加えて`ORDER BY geom`でPG側にも500万行のソートを強いる。**2026-09- |

各項目のEvidence・failure_scenario・Recommendationはレビュー結果の全件表を参照する。

## 着手時に決めること

- 上記のうちどれを実施し、どれを見送るか（見送る場合は理由を本ファイルへ残す）
- 実施順序（レビューのPhase 8は「検証の自動化→境界の固定→実害のある修正→
  現状文書の是正→内部の再配置」を提案している）
