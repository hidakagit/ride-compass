# T425. ゼロベース網羅レビューのP1指摘13件の対応検討 規模M

状態: 未着手（起票のみ、2026-08-30・ゼロベース網羅レビューで発見）

- 背景: [T424](T424.md)と同じレビュー（[docs/zero-base-review-2026-08-30.md](../zero-base-review-2026-08-30.md)）で
  検出したP1（重要）指摘13件。個別のfile:line・failure_scenarioは同ドキュメントの該当節を参照——
  ここでは重複記載せず一覧のみをひも付ける。着手時は各項目を個別タスクへ切り出すか、
  まとめて対応するかを都度判断すること。
- **注意（2026-08-30追記）**: 本レビューはコミット470bf32時点のコードを対象にした。直後に
  [T418](T418.md)（評価軸チップを地図UIから撤去しルート設定パネルへ統合、コミット
  0ee439b/735f116）が着地している。上記13件のうち`backend/**`・`MapView.tsx`・
  `jmaNowcastFrames.ts`・`useDynamicWeatherLayers.ts`はT418の変更対象外のため影響なし。
  12・13番目（`AxisComposer.tsx`のbreakpoints昇順チェック・循環参照検出）はT418でも
  同ファイルへ軽微な変更（4行）が入っているため、着手前にfile:lineを現状のコードで
  再確認すること（ロジック自体が撤去された形跡はない）。

## 一覧（doc内の節番号・file・1行要約）

1. §2 `backend/app/infrastructure/elevation_client.py:141-144` — DEMタイルの一時通信エラーが永続キャッシュされ標高Noneが固定化する
2. §2 `road_edge_geometry_cache.py`/`road_graph_tile_cache.py`/`wind_forecast_cache.py`/`wind_way_penalty_cache.py` — Redis cache-asideモジュール4本が`log_external_call`を使わず個別実装（CLAUDE.mdログ方針違反）
3. §3 `frontend/src/components/Map/MapView.tsx:2139` — 「変わらないデータを更新」ボタンでT414のwindAxisPenalties/windPenaltyGeojsonが再適用されない
4. §3 `frontend/src/components/Map/MapView.tsx:1546` — windPenaltyFillレイヤーがinteractiveLayerIdsから除外漏れ、クリックで誤った路面ポップアップ
5. §4 `frontend/src/components/Map/jmaNowcastFrames.ts:51-57` — 実況フレーム0件時のフォールバックが逆で、降水/雷/竜巻ナウキャストが空になる
6. §5 `backend/app/api/routers/weather.py`ほか12箇所 — レート制限429処理ブロックの重複（`_tile_validation.py`の前例があるのに未適用）
7. §5 `backend/app/api/routers/axis_admin.py:209-217` — `priority_overrides[*].material`が未検証で保存でき0次条件が無警告で不発動
8. §7 `backend/app/services/jma_amedas_service.py:148-157` — `refresh_all_stations()`の全失敗経路にWARNINGが無い
9. §7 `backend/app/services/region_service.py:269-278` — 高頻度WARNINGが抑制ヘルパー未経由（docs/logging.md違反、graph_service.py等にも横展開）
10. §8 `frontend/src/services/*Api.ts`（7ファイル） — `API_BASE_URL`フォールバックの手書き複製（`backendInternalUrl.ts`の前例と同型）
11. §8 `frontend/src/hooks/useDynamicWeatherLayers.ts:237-283` — キキクル・線状降水帯予測マップのフェッチ失敗が`dynamicLayerError`へ反映されない
12. §9 `frontend/src/components/AxisStudio/AxisComposer.tsx:519-557` — 折れ点(breakpoints)のx昇順チェックが無くbackend前提を静かに破れる
13. §9 `frontend/src/components/AxisStudio/AxisComposer.tsx:510-512` — 軸のかけあわせ参照で多段の循環参照を保存前に検出しない

- 対応方針（未検討）: 3・4（MapView.tsx、T414自身の回帰）と7・12・13（軸スタジオの入力検証）は
  関連性が高くまとめて着手しやすい。着手前にユーザーへ優先順位を確認すること。
