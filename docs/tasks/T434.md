# T434. ルート結果の色分けモードを評価で有効にした軸だけに動的に絞り込む 規模S

状態: 着手中（2026-08-30）

- 背景: T433で`DEFAULT_ROUTE_STYLE_MODE_ID`のハードコードは解消したが、ユーザーから
  「評価で有効にした軸だけが動的に出て欲しい。勾配は常にあるからハードコードでいいという
  話ではない」と直接指摘を受けた。詳細は追記予定。
