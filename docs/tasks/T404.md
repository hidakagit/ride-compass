# T404. display_override廃止（JS変換ロジック拡張＋軸スタジオの色分けしきい値編集）

状態: 未着手（[T400](T400.md)節6からの分割）

- 背景: 詳細はT400.md「6. display_overrideの廃止方針」参照。`derive_ramp_inputs`とフロントの
  `buildAxisRampValueExpression`を拡張し、`preprocess="abs"`対応・軸参照の再帰解決・実行時
  スケール変換の定数化に対応する。軸スタジオのGUIへ「色分けのしきい値だけを自分で調整する」
  項目を追加し、バックエンド専用の`display_override`（生JSON）を廃止する。
