# T571: 派生データ鮮度台帳（生データ更新時刻 vs 派生computed_atの機械比較、T281段階3）

状態: 未着手（2026-09-04起票、[T281](T281.md)段階3を独立タスクとして分離）

## 背景

[T281](T281.md)（派生データ鮮度の段階対応）の段階1（依存DAG文書化）・段階2
（統合エントリポイント`refresh_derived.py`）は完了したが、段階3（鮮度台帳）は
「同クラス障害の再発、またはT127全国展開の意思決定」というトリガー待ちのまま。
T281自体は段階1・2の完了を理由に完了扱い（`[x]`）にしたため、`/task:next`の機械抽出
（`docs/improvement-plan.md`の`- [ ]`行のみを対象）からは外れる。トリガー成立時に
確実に候補として拾われるよう、段階3を独立タスクへ分離した。

## 対応方針（T281段階3の記述を引き継ぎ）

*_import_runs系を一般化した鮮度台帳で「生データ更新時刻 vs 派生computed_at」を機械比較
可能にする。[T351](T351.md)が鮮度台帳の材料となる列（`source_*_import_run_id`・
`algorithm_version`、`docs/batch-pipeline-dependencies.md`冒頭の追記参照）を先行して
用意済みのため、着手時はこの列を読むだけで構築でき、新たな系譜追跡機構をゼロから設計
する必要はない。T242残課題（標高バックフィル定期実行）のスケジューリング基盤新設と
関連するため、着手時に統合を検討する。

トリガー: 同クラス障害（T74・T101・T242と同種、派生データ欠損によるサイレントな評価軸
除外）の再発、またはT127（全国展開）の意思決定。
