# T551: 目的地ルートを上位N件（via-node方式の代替経路）へ拡張する

状態: 未着手（2026-09-03起票、[T531](T531.md)推敲中のユーザー提案。T531の共通基盤
[一対全Dijkstra木・len積算・経路復元・多様性間引き]完成後に着手）

- 背景: 目的地ルート（`generate_via_waypoints`、destination指定）は現在1件のみ生成する。
  周回のN件化（T531）と同じ部品で、起点からの前向き木＋目的地からの後ろ向き木による
  via-node方式の代替経路をN件返せるようにする（詳細は追記予定）。
