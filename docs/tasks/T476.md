# T476. MapOverlayControls.test.tsxへWidthSwatch統合・ref安定化の回帰テストを追加

状態: 完了（2026-08-31）

- 背景: T471の対応方針項目3・4（いずれも「実害のあるバグ」と明記）に対応する回帰テストが
  `MapOverlayControls.test.tsx`に追加されていない。同じT471内の他3項目
  （SystemStatusPanel・RouteSettingsPanel・WindBearingSlider）はいずれも回帰テストを
  伴っており、扱いが不揃い（統合レビュー第3回 `history/2026-08-31_all.md` P2、Shard E）。
- 対応方針:
  1. `renderLegendSwatch`のWidthSwatch統合（`MapOverlayControls.tsx:230-235`）:
     `entry.width`を持つ`legendDetails`エントリを渡し、`WidthSwatch`相当の要素
     （`DISPLAY_SCALE=1.8`倍の高さ等）が正しく描画されることを確認するテストを追加。
  2. `usePagedOverflow`内`registerViewport`/`registerTrack`の`useCallback`化
     （`MapOverlayControls.tsx:292-338`）: 親の再レンダーを挟んでも`chipRow`の展開/収納や
     ページ送りボタンの表示が壊れないこと（間接的にobserver安定性を担保する確認）を
     検証するテストを追加。
- 規模: S〜M（テストケース2〜3件追加、実装変更は不要）
- 実施結果（2026-08-31）:
  1. `renderLegendSwatch`のWidthSwatch統合: `entry.width`ありは実寸太さバー
     （`aria-hidden="true"`のspan、`height`が`width * DISPLAY_SCALE`(=1.8)と一致し
     `detailSwatchDot`クラスを持たない）、`entry.width`無しは従来どおり色ドット
     （`detailSwatchDot`クラス、`aria-hidden`なし）で描画されることを確認する1テストを追加。
  2. `usePagedOverflow`の`registerViewport`/`registerTrack`のuseCallback安定化:
     jsdom/happy-domがResizeObserver未実装のため最小モック
     （`useElementHeightCssVar.test.ts`と同じ方式）でコンストラクタ・`disconnect`呼び出しを
     直接観測し、親が新しい配列参照（内容は同じ）でレンダーしてもResizeObserverが
     再構築されないこと、かつ再レンダーを挟んでもグループの展開/収納が機能し続けることを
     確認する1テストを追加。
  3. `npx vitest run src/components/MapOverlayControls/MapOverlayControls.test.tsx --pool=threads`
     （35件全green）・`npx tsc --noEmit`とも通過を確認。
