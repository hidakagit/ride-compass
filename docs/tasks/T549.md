# T549: `supports_route_coloring`フラグの全面撤去（全公開軸を自動的にルート地図色分け対象へ）

状態: 完了（2026-09-03）

## 背景

ユーザーから「自転車インフラを重み設定したのに地図の色分けで選べない」という指摘を受け
調査したところ、`bicycle_infra_quality`軸は`supports_route_coloring=False`（地図の
色分け対象外）だった。`backend/app/domain/axis_definitions.py`の該当docstring
（改善計画T440の訂正記録）を確認したところ、以前は`gradient`のみこの機構の対象外という
非対称な設計だったが、「軸スタジオと同期していない」という指摘を受けて訂正され、
**現在はこの機構（`axis_difficulties[axis_id]`を汎用の3段階色分けとして使う仕組み）の
対象外になる軸は技術的に存在しない**ことが判明した。つまり`supports_route_coloring=False`の
軸（car_stress・night・accident・bicycle_infra_quality・stop_density）は、技術的制約では
なく「まだ誰も軸スタジオでtrueにしていないだけ」。

ユーザーへこの経緯を説明したところ、「全部色付けできるなら除外できる設定を残す意味ない」
という判断に至った。地図の色分けチップ列は既にroute_preferenceの重み>0でフィルタ済み
（`filterRouteStyleModesByPreference`）のため、実際に使っている軸だけに絞り込む役割は
既にそちらが担っており、`supports_route_coloring`という追加の手動フラグを二重に持つ意味が
薄いという判断。ユーザー承認済み（「問題ない。バックグラウンドで進めて」）。

## 対応方針

`supports_route_coloring`フィールドをシステム全体（DBカラム含む）から撤去し、
`routeStyleModesFromCatalogAxes`が公開軸すべてを無条件でルート色分けモードの候補にする
（実際の表示絞り込みはroute_preferenceの重みのみが担う）よう変更する。

## 実装メモ

隔離git worktree（`.claude/worktrees/agent-ac9e969a281c044e6`、
ブランチ`worktree-agent-ac9e969a281c044e6`）で実施。

- **backend**: `domain/axis_definitions.py`から`AxisDefinition.supports_route_coloring`
  フィールドとdocstringを削除（`dedicated_way_value_layer`のdocstringは別概念である旨を
  維持しつつ、削除したフィールド名への参照のみ書き換えた——`dedicated_way_value_layer`
  フィールド自体は撤去対象外のため無変更）。`api/routers/axis_admin.py`の
  `AxisDefinitionPayload`・`api/routers/axis_catalog.py`の`AxisCatalogEntry`から
  フィールドを削除（`to_definition()`/`_to_response()`は`model_dump()`経由のため
  追随のための追加変更は不要だった）。`infrastructure/axis_definition_models.py`の
  SQLAlchemyカラム定義、`axis_definition_repository.py`のSELECT/INSERT/UPDATE 3箇所を削除。
  `services/axis_registry_service.py`は直接の参照は無くコメントのみ更新。
- **DBスキーマ**: 新規migration
  `backend/migrations/0030_axis_definitions_drop_route_coloring.sql`
  （`ALTER TABLE axis_definitions DROP COLUMN IF EXISTS supports_route_coloring`）を追加。
  既存の`0023_axis_definitions_time_scope_route_coloring.sql`
  （supports_route_coloringを追加した側）・`0027_axis_definitions_dedicated_way_value_layer.sql`
  は運用どおり書き換えていない（本文中に旧フィールド名への言及が残るが、過去の
  migrationは経緯記録として書き換えない方針のため許容）。`backend/fixtures/
  axis_definitions_snapshot.json`から13軸ぶんの`supports_route_coloring`キーを
  スクリプトで一括削除（`dump_axis_definitions_snapshot.py`と同じ
  `json.dumps(..., ensure_ascii=False, indent=2, sort_keys=True)`形式を再現し、
  diffが該当13行の削除のみになることを確認）。
- **frontend**: `routeStyleModes.ts: routeStyleModesFromCatalogAxes`の
  `axes.filter((axis) => axis.supports_route_coloring)`を`axes.map(...)`（全軸対象）へ
  変更。`CatalogAxis`型（`axisLayers.ts`）・`useAxisCatalog.ts`の変換関数・
  `AxisComposer.tsx`のDraft型/emptyDraft/draftFromExisting/保存payloadから
  フィールドを削除（`AxisComposer.tsx`はこのフィールドの編集欄を元々持たず
  pass-through保持のみだったため、GUIフォーム自体の変更は無い）。
  `RouteAxisProfile.tsx`・`page.tsx`・`MapView.tsx`のコメント中の言及を実態
  （全公開軸が対象、絞り込みはroute_preferenceの重みのみ）に更新。ロジック自体は
  想定どおり無変更で済んだ。
- **OpenAPI生成物**: `backend/scripts/export_openapi.py`（dev DB接続、axes=14を確認）→
  `npm run generate:api`を実行し、`frontend/src/types/generated/{openapi.json,
  axis-catalog.json, api.d.ts}`の3ファイルが該当キーの削除のみ（他の生成物
  [surface-tags.json等]は無変更）であることを確認した。
- **並行セッションとの分離**: T547（AxisComposer.tsx等を本体ツリーで編集中）とは
  隔離worktreeのため無衝突。`AxisComposer.tsx`はworktree作成時点のコピーから
  始まるためT547の未コミット変更は含まれないが、本タスクは
  `supports_route_coloring`関連の削除のみに集中し、数値入力まわりの実装パターンは
  変更していない。

## 検証結果

- backend: `pytest -q -m "not postgis"`（1348 passed, 238 deselected）全緑。
  `pytest tests/test_migrate.py`（ridecompass_test DBへの実接続、まっさらなDBへ
  全migration適用→スナップショット読み込みの一連の流れ）も8件全緑——新規migration
  0030が正しく適用されることを確認済み。
- frontend: `npx vitest run`（102 files, 947 tests）全緑。`npx tsc --noEmit`
  エラー無し（`frontend/src/app/layout.tsx`の`LayoutProps`型エラーは、この
  worktreeで一度も`next dev`/`next build`を実行しておらず`.next/types`が
  存在しなかったことが原因の環境要因——`npx next typegen`で解消し、本タスクの
  変更とは無関係と確認済み）。変更した全`.ts`/`.tsx`ファイルへの`npx eslint`も
  エラー無し。
- `routeStyleModes.test.ts`（タスク一覧に明記されていなかったが、
  `routeStyleModesFromCatalogAxes`の挙動を直接検証するテストのため今回の変更で
  実際に失敗した）を、公開8軸すべて（gradient・wind・surface_q・stop_density・
  car_stress・accident・night・bicycle_infra_quality）が無条件でモード化される
  新しい期待値へ更新した。
- `git diff --exit-code -- frontend/src/types/generated/`は本タスクの変更を
  含めた状態では非ゼロ（意図した差分そのもの）だが、差分内容が
  `supports_route_coloring`キーの削除のみであることを目視確認済み。
