# T964. PostGIS統合テストが、並行セッション間で同じDBを共有している 規模S

状態: 未着手（2026-09-19起票、T950の完了検証で4件が偽の失敗を出したことから）

## 背景

`backend/tests/conftest.py`の`TEST_DATABASE_URL`が既定で単一のDBを指すため、同じ作業ツリー
群で動く複数のセッションが同じ行を同時に書き換える。落ちるのはテストの中身と無関係な
ファイルで、単独実行では通る。
