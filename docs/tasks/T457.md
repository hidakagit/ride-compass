# T457. T425項目3・4（風のredraw未再適用・interactiveLayerIds除外漏れ）と同じバグがgradientにも複製されている

状態: 起票

- 背景: `複雑度平衡性レビュー(正しい設計で全体実施)`
  （`.claude/commands/review/history/2026-08-31_complexity.md`F-1）の指摘。
  [T425](T425.md)項目3・4（`frontend/src/components/Map/MapView.tsx`の「変わらないデータを
  更新」ボタンで風のfeature-state再適用が漏れる／`windPenaltyFill`が`interactiveLayerIds`から
  除外され誤ったポップアップが出る）と同じ2件のバグが、T423（勾配軸の実装）で
  「風の実装を見ながら」踏襲する形でそのまま複製され、gradientにも存在することが判明した
  （`docs/tasks/T423.md`「意図的にスコープ外とした点」に実装者自身が明記）。
- 経緯（2026-08-31）: 本来T425（wind分のみのスコープ）へ追記する形で起票する予定だったが、
  T425がちょうど別セッションで実装完了直前の状態にあり、追記によってスコープが割り込み
  手戻り・再テストを引き起こしてしまったため、ユーザー指示により独立タスクとして切り出した。
  T425側からはこの追記を削除済み。
- 対応方針: T425（wind分）の修正が完了した後、同じ修正パターンをgradientへも適用する。
  3責務（redraw再適用・feature-stateクリア・interactiveLayerIds所属）を
  `dedicated_way_value_layer`フラグ駆動で一般化するかどうかは別軸の判断で、
  `context.md`のKeep Listへ「3件目の動的材料を追加する判断をした時点」をトリガーとした
  DEFER項目として記録済み（本タスクの対応範囲外、個別バグ修正のみで足りる）。
- Confidence: High（`docs/tasks/T423.md`の一次記録で裏付け）。
- 規模S。
