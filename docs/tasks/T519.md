# T519. 研究モードを一般利用者向けのヘッダーメニューへ再配置し、/admin依存を解消する 規模S〜M

状態: 未完了

- 背景: `researchEnabled`フラグ（localStorage、サーバー検証なし）の唯一の意図された
  有効化UIは`/admin`（Basic認証保護下）にあるが、フラグ自体は`/admin`を経由せず
  一般公開トップページから直接書き換え可能であることが判明。ユーザーとの協議の結果、
  研究モードのpage.tsx側機能（比較タブ・実験スロット記録・地図重ね描き）は一般利用者
  向けに正式公開する方針とした。詳細は追記予定。
