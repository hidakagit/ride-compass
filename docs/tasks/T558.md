# T558: 周回ルートの進行方向矢印（T293）が区間色分け線に隠れて見えない問題の修正

状態: 着手中（2026-09-03起票、ユーザー報告「ルート生成の結果、向きの矢印が描画されていない」）

## 背景

T293で追加した選択中候補の進行方向矢印（`route-arrow-halo`/`route-arrow`）が実機で見えない。
Playwright診断で、矢印は配置されているが白い縁取り層が1つも配置されず、紺色の矢印が
上に重なる区間色分け線（`route-detail-segments-line`、既定の勾配モードで紺系）に同色で沈んでいると判明。
