# T474. モジュール設計書・architecture.md・タスクファイルのドキュメントドリフト一括修正

状態: 完了（2026-08-31）

- 背景: 統合レビュー第3回（`history/2026-08-31_all.md`）で、複数タスク（T460・T462・T464・
  T466・T467・T469・T471等）が同一ディレクトリへ連続して手を入れた結果、対応する
  ドキュメント側が更新から取り残されるパターンがP2として9件検出された（Shard A・B・F・G・H）。
  いずれも実害は軽微（誤った記述を信じた場合の手戻りリスク）だが、CLAUDE.mdの
  「既存モジュールへの修正・追加に着手する際は対象docsを精読し、乖離を同一コミットで
  修正する」ルールに反する状態が現に生じている。
- 対応方針（各箇所を該当ファイルへ反映）:
  1. `docs/modules/backend/cross-cutting-infrastructure.md`: `GET /api/debug/db-status`の
     認可要否を「不要」→「必要（`require_admin_basic_auth`、T467で追加）」へ修正（Shard B）。
  2. 同ファイル: lifespan図へシャットダウン段（scheduler停止→httpクライアントclose、T464）を
     追加し、未処理例外時もX-Request-IDを返す旨を1文追加（Shard B）。
  3. `docs/modules/backend/dynamic-way-values.md`: `get_dynamic_way_value_service`の
     `_build`の説明を、旧if/else分岐から`_DYNAMIC_WAY_VALUE_SERVICE_FACTORIES`
     （T460の登録テーブル方式）へ更新（Shard B・Shard G重複発見）。
  4. `docs/modules/backend/axis-studio.md`: `AxisDefinitionRepository.acquire_write_lock()`
     （T469のadvisory lockによるTOCTOU対策）を追記（Shard B）。
  5. `docs/architecture.md`: §1技術選定表（L20-25、`routing_engine`設定・
     `OpenRouteServiceEngine`/`RoutingService`/`WindService`/`ElevationService`への
     生きた言及）・§9「現状の要点」（L2730-2733）・ファイルツリーのgeo.py注釈（L560、
     `sample_indices`等の削除済み関数）を、T462（openrouteserviceエンジン完全撤去）後の
     road_graph単一構成へ書き換える（Shard A・Shard C1重複発見）。
  6. `docs/architecture.md`: T471の生成物`route-generate-config.json`（`MAX_ROUTE_DISTANCE_KM`
     の片側import）を「正準定義と同期機構」節へ追記（Shard G）。
  7. `.claude/commands/review/context.md`: 「意図的な設計判断」の「エンジン切替の併存」
     「DI工場が使わない側エンジンの軽量依存も毎回構築」の2項目を、T462後の単一エンジン化を
     踏まえて削除するか根拠を明記し直す（Shard A。`/review:improve`相当の手続きで検討）。
  8. `docs/tasks/T444.md`・`docs/tasks/T457.md`: 「`context.md`のKeep Listへ...記録済み」
     という誤記述を、「`history/2026-08-31_complexity.md`のDEFER節にトリガー付きで記録済み
     （`context.md`本体への反映は未承認）」へ訂正する（Shard D・Shard H重複発見、
     `T465.md`が既に自己検出していた指摘の正式起票）。
  9. `docs/tasks/T431.md`: 冒頭「状態:」ヘッダを「未着手（起票のみ...）」から
     「完了（2026-08-30）」へ訂正する（本文の実施記録・improvement-plan.mdのチェック状態
     との整合、Shard H）。
  10. `docs/modules/frontend/dynamic-weather-layers.md`: 対象ファイル表へ新設
      `usePolledFetch.ts`（T470、5箇所のフェッチ骨格を担う中核部品）を追記する（Shard F）。
- 規模: M（複数ファイルのdocs修正のみ、コード変更なし）
- 実施結果（2026-08-31）: 項目1・2・4〜10は本文どおりdocs修正を実施。項目3
  （`dynamic-way-values.md`のif/else分岐→`_DYNAMIC_WAY_VALUE_SERVICE_FACTORIES`更新）は
  着手時点で既に正しい記述へ更新済みだったため対応不要と確認（別タスクの過程で
  先行して直っていた）。コード変更は無し。
