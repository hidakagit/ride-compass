# T970. データの置き場所と形を決め直す 規模L

状態: 設計中（2026-09-20起票）。[T969](T969.md)点検0を対話で実施した結果の設計と決定を、
ここへ積む。実装は未着手。

## 背景

何をDBへ持つかの基準がどこにも無く、置き場所が「そのとき動いていた運用状態」で決まって
きた。生データ・派生データ・管理データ・運用の記録のうち、基準があるのはキャッシュだけ
だった（[docs/caching.md](../caching.md)）。

β版は関東全域、正式版は全国へ広げる。現状の形のまま全国化すると147GiBのボリュームに対し
約139GB（95%）で入らない。

## 決定1: 外部ソースの生データをDBへ置く条件

次を**すべて**満たすものだけ、生データをDBへ持つ。

1. **手動で取り直さなければ最新が反映されない**（目安: 1年以上陳腐化しない）
2. **母集団を自分で確定でき、その量が容量に見合う**
3. **手元の別の生データから作れるものを、二重に持たない**

判定順は2→3→1ではなく、どれか1つでも落ちれば持たない。

| 対象 | 判定 |
|---|---|
| OSM・事故統計・指定路線・標高（DEM）・土地被覆（LULC） | DBへ生データを置く |
| **基礎地図（OpenFreeMapのベクタタイル）** | **条件1と条件3で落ちる**（[T976](T976.md)） |
| 気象（雨雲・アメダス・警報・MSM・洪水・暑さ指数） | 条件1で落ちる。キャッシュのまま |
| GSI色別標高図・標高タイル（dem_png） | 条件3で落ちる。自前のDEMから描く |

**訂正（2026-09-20）**: 基礎地図を当初この表で「DBへ置く」側に入れていたが、誤りだった。
条件に当てて確かめていない。実際には2つ落ちる。

- 条件1: OpenFreeMapは[公式ページ](https://openfreemap.org/)で週次のプラネット配布を
  明記しており、タイルは週次で焼き直される。1年どころか数週間で陳腐化する
- 条件3: OpenFreeMapのデータ源はOSM（公式ページに明記）で、そのOSMを我々は別に持っている。
  道路網を二重に持っている

条件3は同じ表の`dem_png`を落とすのに使っている基準であり、基礎地図にだけ適用していなかった。
容量の影響は関東のz14タイル15,065枚で約8.8GB（開発DB実測614KB/枚からの換算）。対応は
[T976](T976.md)。

Redisは現状維持（外部への負荷を肩代わりするものだけ）。利用を広げない。制約が厳しくなる
とき、またはβ版を終えるときに実状と合うか再精査する。

### 絞り込み（母集団の宣言）

取込プロファイルをOSM専用から全ソース共通へ広げ、runごとに丸ごと記録する。**全国化は
bboxと行条件の指定を変えて取り直すだけ**にする。

- OSMの行の絞り込みは現状維持（通行しうるhighway種別＋自転車可のfootway/path）
- **OSMのタグの絞り込みは廃止**（許可リストをやめ、全タグを持つ）
- ラスタのズームは**元データの分解能から導く**。UIの都合で決めない
  （DEM5A=5mメッシュ→z15、LULC=10m画素→z14）。
  配信元がそれ以上を持たないことは実測で確認済み（z16以降は404）

## 決定2: 生データの共通フォーマット

```sql
CREATE TABLE source_runs (
    run_id      bigserial PRIMARY KEY,
    source      text        NOT NULL,
    status      text        NOT NULL,
    started_at  timestamptz NOT NULL,
    finished_at timestamptz,
    origin      jsonb       NOT NULL,   -- 取得元URL・ファイル名・配信元のタイムスタンプ
    profile     jsonb       NOT NULL,   -- そのとき適用した絞り込みの宣言を丸ごと
    counts      jsonb       NOT NULL
);

CREATE TABLE source_features (
    source      text     NOT NULL,
    natural_key text     NOT NULL,
    run_id      bigint   NOT NULL REFERENCES source_runs (run_id),
    geom        geometry(Geometry, 4326) NOT NULL,
    attrs       jsonb    NOT NULL DEFAULT '{}',
    payload     bytea,
    PRIMARY KEY (source, natural_key)
) PARTITION BY LIST (source);
```

- `attrs`は「解釈できる属性の袋」、`payload`は「解釈の要る配列実体」（ラスタ画素・
  OSMの参照ノードid列・タイル本体）
- ラスタは**タイル1枚＝1行**。これで点・線・面が同じ骨格に乗る
- パーティションにするのは、**生データの置き換えを`DETACH`＋`ATTACH`にする**ため。
  これが「生データを差し替えたら下流が洗い替わる」の入口になる

**事故統計の属性は袋（`attrs`）で持つ**。実列にすると4.4倍安いが（開発DBで実測:
袋345MB／実列78MB、303,455行換算）、ソースごとにDDLが要り「同一フォーマット」が成立
しなくなる。差は全国で2.8GBで、見込み総量に対して2.1%。

## 決定3: road_edges の形

**この節は決定11が差し替えた（2026-09-20）。** 「区間1本＝1行、有向グラフはメモリ上で
作る」は誤りで、有向の構造をDBへ戻した。以下のDDLと容量の試算は採らない。判断の経緯は
記録として残す。

```sql
CREATE TABLE road_edges (
    osm_way_id    bigint   NOT NULL REFERENCES osm_raw_ways (osm_way_id) ON DELETE CASCADE,
    segment_index smallint NOT NULL,
    from_node_id  bigint   NOT NULL REFERENCES osm_raw_nodes (osm_node_id),
    to_node_id    bigint   NOT NULL REFERENCES osm_raw_nodes (osm_node_id),
    geom          geometry(LineString, 4326) NOT NULL,
    distance_m    real     NOT NULL,
    bearing_deg   real     NOT NULL,
    source_run_id bigint   NOT NULL REFERENCES source_runs (run_id),
    PRIMARY KEY (osm_way_id, segment_index)
);
-- GiST索引は持たない（空間の絞り込みは osm_raw_ways 側で行う）
```

変更点と根拠:

- **区間1本＝1行**（逆向きの行をやめる）。逆向きペアはジオメトリの完全な逆順で、距離も
  同じ（標本975件すべて）。向きが持つ情報は方位（+180°）・勾配（符号反転）・通行可否
  （`osm_raw_ways.direction`）で導ける。有向グラフは`build_road_graph`がメモリ上で作る
  実行時の構成物であり、表が両向きを持つ必要はない
- **識別子を複合自然キーへ**。`edge_id`は`way-{数値}-seg{数値}-{fwd|bwd}`という文字列で
  23.1バイト、ノードは`osm-node-{数値}`で20.0バイト。文字列を組み立てているのは
  `domain/graph.py`の3行だけで、**解析している箇所はbackend・frontendとも0件**
  （不透明なトークンとして扱われている）。ノードが`osm_raw_nodes`への本物のFKになる
- **`highway`を持たない**。`osm_raw_ways.highway`の複製
- **`updated_at`を`source_run_id`へ**。現在の`road_edges`は系譜列を1つも持たず、OSMを
  取り込み直しても古いと判定できない
- **GiST索引を持たない**。空間の絞り込みは親wayのGiSTで行い、`osm_way_id`（新主キーの
  先頭列）で区間を引く

GiSTを外す判断の実測（本番、3地点×z12〜16の15タイル。過剰取得＝way経由で引いたときの
行数の比）:

| z | 過剰取得 | 所要時間 |
|---|---|---|
| 12（グラフ読み込みの粒度） | 1.00〜1.03倍 | 同等（1件だけ+19%、他は速い側） |
| 14〜16（路面タイルの粒度） | 1.08〜1.37倍（絶対数は数十行） | 同等かway経由が速い |

wayのGiSTは1,333,000件でroad_edgesの5,067,000件より小さく、索引が小さいぶん有利に働く。
**バッチ4本（POI割り当て・事故割り当て・土地被覆リング・標高）の実行計画は未確認**で、
実装時に確かめる。

### 効き（実測からの試算）

| | 現状 | 新形 |
|---|---|---|
| 行数 | 5,065,850 | 2,611,210 |
| heap | 2,362 MB | 約 1,060 MB |
| 索引計 | 1,355 MB | 約 220 MB |
| **合計** | **3,719 MB** | **約 1,280 MB（−66%）** |
| **全国換算** | **38 GB** | **約 13 GB** |

## 決定4: 第1層（生データだけを入力にする派生）の形

### 粒度の整理

| 層 | 点 | 線（粗） | 線（細） | 面 |
|---|---|---|---|---|
| 生データ | osm_node / accident | osm_way / designation | — | dem / lulc |
| 派生 | `node_materials` | `way_materials` | `edge_materials` ＋ `road_edges` | **持たない** |

**wayは面ではなく粗い線**で、区間と同じものの粗さ違いである。面の派生（`tile_materials`）は
不要——面の生データを読む出口は「タイル表示（生ラスタから直接描く）」と「材料（線ごとの
リングで集計）」の2つだけで、どちらも面の中間結果を必要としない。

### 表の構造がバッチの写しになっていた

way単位の計算済み値が4表・4バッチに分かれ、`osm_raw_ways`を4回走査し、主キー索引を4つ
（196MB）持ち、系譜を4セット×1,333,039行持っていた。内容が違うのではなく、書いた
バッチが違うだけである。3本は`run_chunked_precompute`＋`select(OsmRawWayRow.osm_way_id)`＋
`CHUNK_SIZE=5_000`という同一の骨格で、同じ母集団を走っていた。

**粒度ごとに1表にする。**way単位の材料が増えても列が1本増えるだけで、表もバッチも
増えない（構造仕様8「1本道の追加点」）。

### 点データの歪み

点を持つ表が5つあり、母集団が重なっていた（実測）:

- `osm_raw_pois`の75%（317,465件）は`osm_raw_nodes`にもある同じOSMノード
- `road_nodes`（1,871,229）は`osm_raw_nodes`の完全な部分集合
- `osm_raw_pois.kind`は取込時の分類器の出力＝**生データの表に派生が混ざっている**

タグを全保持すると決めたので、分類は取込から外して派生側へ移せる。生データは
`source_features(source='osm_node')`へ統合する。

### 「交差点」の定義が2つあった

| | 数え方 | 使う側 |
|---|---|---|
| `raw_intersection_nodes` | `osm_raw_ways.node_ids`の隣接頂点を数える | way粒度の`intersection_count`（地図） |
| `road_nodes.degree` | `COUNT(DISTINCT 隣接グラフ頂点)` | 区間粒度の`intersection_count`（評価） |

実測: 後者は前者の厳密な部分集合で、30,399件が食い違う（うち26,512件は
`road_nodes.degree = 2`）。食い違うノードから3,000件を抜いて測ると、**接続する区間は
平均5.82本（両向きなので約2.9本）なのに、異なる隣接グラフ頂点はちょうど2.00**——
2本の枝が同じ次の交差点へ向かうと`DISTINCT`で潰れる。

つまり**同じ「交差点数」という材料が、地図と評価で違う値になっている**。評価側は26,512
箇所を数え落とす。根本は、グラフの位相としての次数（探索が使う）と、そこに集まる道の
本数（材料が使う）という別のものに同じ列を使っていること。

**`branch_count`（集まる道の本数）1つへ統一**し、way粒度・区間粒度の両方がこれを使う。
グラフの位相としての次数は保存しない（探索はメモリ上でグラフを組むときに数える）。

### way粒度を残す理由

路面タイルは z12〜15 で、区間粒度へ切り替わるのが z14。**way粒度が使われるのは z12・z13
の2段だけ**。区間から集約して作れるかを本番で測った（way数はA/Bで完全一致し、同じ母集団を
作れることは確認済み）:

| 地点 | z | 現行 | 区間から集約 |
|---|---|---|---|
| 都心 | 12 | 33.1 ms | **1,276.3 ms** |
| 都心 | 13 | 10.9 ms | 27.7 ms |
| 郊外 | 12 | 22.2 ms | 62.9 ms |
| 山間 | 12 | 8.7 ms | 10.0 ms |

z13は問題ないが、z12の都心が重い。本番の路面タイル生成は z12 で1.23秒なので約2倍になる。
**way粒度は「z12のタイルのため」に残す**——これが唯一の理由で、他の理由は無い。
（都心z12だけ非線形に遅く、プランが切り替わっている可能性がある。1件だけの観測。）

`divided`と`designation`はそもそも区間版が無く、道1本の性質なのでway粒度が正しい。

### 区間の材料が欠けたときのway値へのフォールバックは廃止する

`edge_landcover`の充足率は 2,399,882 / 2,611,210 区間 = **91.9%**。残り8%はway値
（道全体の平均）を受け取り、隣の区間と違う定義の値になる。way粒度を残すこととは別問題で、
**区間側を100%埋めてフォールバックの経路自体を無くす**。

### 定義

```sql
CREATE TABLE node_materials (
    osm_node_id         bigint   PRIMARY KEY REFERENCES osm_raw_nodes (osm_node_id),
    kind                text,                             -- 分類器の出力（今の osm_raw_pois.kind）
    branch_count        smallint NOT NULL DEFAULT 0,      -- そこに集まる道の本数
    has_traffic_signals boolean  NOT NULL DEFAULT false,  -- 半径内に信号があるか
    max_highway_rank    smallint NOT NULL DEFAULT 0,
    source_run_id       bigint   NOT NULL REFERENCES source_runs (run_id)
);
-- geom は持たない（source_features から引く）。GiSTも持たない——本番の索引統計で
-- idx_road_nodes_geom は 5,055,354 回使われているが、タイル配信でもルート生成でも
-- 1回も増えず、バッチ側の履歴と判明した（利用者の経路では使われていない）。

CREATE TABLE way_materials (
    osm_way_id         bigint   PRIMARY KEY REFERENCES osm_raw_ways (osm_way_id) ON DELETE CASCADE,
    accident_count     real     NOT NULL,
    intersection_count smallint NOT NULL,
    poi_signal         smallint NOT NULL,   -- POI_COUNT_KINDS の宣言から生成する固定列
    poi_crossing       smallint NOT NULL,   -- （jsonbの袋をやめる）
    poi_stop           smallint NOT NULL,
    poi_level_crossing smallint NOT NULL,
    poi_barrier        smallint NOT NULL,
    lc_valid_pixels    integer  NOT NULL,
    lc_water real NOT NULL, lc_trees real NOT NULL, lc_flooded_veg real NOT NULL,
    lc_crops real NOT NULL, lc_built real NOT NULL, lc_bare        real NOT NULL,
    lc_snow_ice real NOT NULL, lc_rangeland real NOT NULL,
    divided            boolean  NOT NULL,
    designation_emergency real,             -- N10 のマッチ率。無指定はNULL
    designation_logistics real,             -- N12
    source_run_id      bigint   NOT NULL REFERENCES source_runs (run_id)
);
-- length_m は osm_raw_ways.geom から ST_Length で導く（持たない）
-- matched_route_designation_ids は読み手が0件のため落とす
```

廃止: `raw_intersection_nodes`（`branch_count >= 3`で引く）・`way_attribute_counts`・
`way_landcover`・`way_divided_carriageway`・`designation_attributes`・`road_nodes`。

### 第1層の容量

| | 現状 | 新形 |
|---|---|---|
| `road_edges` | 3,719 MB | 約 1,280 MB |
| `road_nodes` → `node_materials` | 732 MB | 約 120 MB |
| way単位4表 → `way_materials` | 969 MB | 約 180 MB |
| `raw_intersection_nodes` | 190 MB | 0 |
| **計** | **5,610 MB** | **約 1,580 MB（−72%）** |
| **全国換算** | **58 GB** | **約 16 GB** |

## 決定5: 第2層（区間粒度の派生）を`edge_materials`へ統合する

**この節のDDLは決定11が差し替えた（2026-09-20）。** `edge_materials`は独立した表を
やめ、`road_segments`へ統合した。統合の根拠（1対1をバッチのコードが守っていた）は
決定11にある。

3表を1つにする。`road_edges`とはキーを共有するが表は分ける——形状・トポロジと材料は
更新のきっかけが違う（形状はwayの再分割時、材料は事故やLULCの更新時）。

### 向きの重複が第2層へ波及していた（実測）

| | 行数 | fwd | bwd | キー |
|---|---|---|---|---|
| `edge_attribute_counts` | 5,065,850 | 2,610,982 | 2,454,868 | `edge_id`（向き込み） |
| `elevation_attributes` | 5,065,850 | 2,610,982 | 2,454,868 | `edge_id`（向き込み） |
| `edge_landcover` | 2,610,177 | — | — | `(osm_way_id, node_lo, node_hi)`＝向き非依存 |

`edge_attribute_counts`が持つのは事故数・交差点数・POI数で、**向きで変わらない値**を2倍
持っている。`elevation_attributes`は向きで変わるが導ける——標本783ペアのうち773件（98.7%）
で`average_grade`が符号反転、5件が同値（平坦）。列の対応は機械的
（start↔end、gain↔loss、average_gradeは符号反転、max↔minは入れ替えて符号反転）。

`edge_landcover`は既に向き非依存のキーを自前で合成していた。路面タイルのSQLが
`LEAST/GREATEST`で同じことを実行時にやっているのと合わせ、**同じ回避が2箇所で独立に
発生している**。主キーを`(osm_way_id, segment_index)`にすれば両方消える。

### 定義

```sql
CREATE TABLE edge_materials (
    osm_way_id    bigint   NOT NULL,
    segment_index smallint NOT NULL,

    -- 未計算はNULL（バッチごとに埋まる時期が違うためNOT NULLにできない）
    accident_count     real,
    intersection_count smallint,
    poi_signal smallint, poi_crossing smallint, poi_stop smallint,
    poi_level_crossing smallint, poi_barrier smallint,

    -- 順方向の値だけ持つ。逆向きは読み出し時に入れ替え・符号反転で導く
    start_elevation_m real, end_elevation_m  real,
    elevation_gain_m  real, elevation_loss_m real,
    average_grade     real, max_grade        real, min_grade real,

    lc_valid_pixels integer,
    lc_water real, lc_trees real, lc_flooded_veg real, lc_crops real,
    lc_built real, lc_bare  real, lc_snow_ice    real, lc_rangeland real,

    source_run_id bigint NOT NULL REFERENCES source_runs (run_id),

    PRIMARY KEY (osm_way_id, segment_index),
    FOREIGN KEY (osm_way_id, segment_index)
        REFERENCES road_edges (osm_way_id, segment_index) ON DELETE CASCADE
);
```

行は`road_edges`と同時に作り（全列NULL）、各バッチは自分の列を`UPDATE`する。

**副産物**: 完成度の判定が一様になる。今は表ごとに違う述語を持ち
（`NOT EXISTS(...)`・`degree = 0`・`max_highway_rank = 0 AND EXISTS(...)`）、
「未計算と本当に0を区別できない」という注記が付いている。NULLで表せば
「その列がNULLの行数」で足りる。

**精度**: `elevation_attributes`は7列すべて`float8`だが、標高をcm精度で持つのに`real`で
足りる。`accident_count`も`float8`から`real`へ。

**訂正**: 先に`edge_landcover`の充足率を91.9%と報告したが、`reltuples`（プランナ用の
推定値）を実数として使った誤り。`count(*)`では 2,610,177 / 2,611,210 = **99.96%**
（欠けは1,033区間）。フォールバックが効く範囲は小さいが、経路自体を無くす方針は変えない。

### 容量

| | 現状 | 新形 |
|---|---|---|
| 3表の本体 | 2,117 MB | |
| 主キー索引 3本 | 874 MB | 1本 約 90 MB |
| **計** | **2,991 MB** | **約 515 MB（−83%）** |
| **全国換算** | **31 GB** | **約 5 GB** |

## 決定6: 生データの差し替えは3段で完結させる

### 今は2段で、温めるのが利用者になっている

```
1. 生データを入れる
2. 派生を再計算して世代を上げる   ← ここで終わり
```

`derived_data_meta.revision`は**システム全体で1つのカウンタ**で、どのバッチが成功しても
同じ値が上がる。これを鍵に含むキャッシュ（路面/POI/事故タイル・探索材料・スコア行列）が
一斉に無効になり、次に来た利用者が冷パスを踏む。

冷パスの実測（[T546](T546.md)・[T522](T522.md)）:

| | 実測 |
|---|---|
| 探索材料のDB冷パス | 39〜55秒 |
| 12タイル・254,052区間の真の冷パス | 34,969 ms（2.9秒/タイル） |
| way単位の勾配 | 809 ms/エントリ |

標高だけ差し替えても、土地被覆タイル・事故タイル・停止要因POIタイルという**無関係な
キャッシュまで捨てる**。

### 決めること

**(a) キャッシュの鍵をソース別の世代で作る。**`source_runs`をソースごとに持つので、
そのキャッシュが実際に読んだソースのrunだけを鍵に含められる。事故タイルはDEMの
差し替えで無効にならない。

**(b) 手順を3段にする。**

```
1. 生データを新しいrunで入れる
2. 派生（node/way/edge_materials）を再計算する
3. キャッシュを作り直してから世代を切り替える   ← 新設
```

3が[T546](T546.md)の「E. バッチ側で事前生成」（同タスクで**未起票**のまま残っていた段）に
当たる。事前生成の所要は、関東のz12タイル1,020枚（`road_graph_tiles`の実数）×2.9秒 =
**約50分**、全国で約8.6時間。派生の再計算と合わせても夜間バッチの枠に収まり、利用者に
数十秒を払わせる理由が無い。

正規化して表をまたぐようにした以上、ソースを洗い替えたときの影響範囲が広がるのは
避けられない。キャッシュ層を分離して事前生成を持つのは、その正当な対価として受け入れる。

## 決定7: 外部から取得した値の置き場をそろえる（気象層）

### 分かれ方に基準が無かった

6つとも外部ソースで、どれも外部への負荷を肩代わりしているのに、3つが共有の置き場、
3つがプロセス内にあった。`docs/caching.md`の基準（「Redisへ置くのは外部への負荷を
肩代わりするものだけ」）ではこの分かれ方を説明できない。しかも同文書は「外部フェッチを
肩代わりしているキャッシュをプロセス内に置くと、上流へのリクエストがワーカー数に比例して
増える」と書いている。

`flood`（洪水予報）と`jma_warning`（警報）は**全国分を1リクエストで取る電文**で、
`jma_amedas`とまったく同じ性質なのに、プロセス内にあった。

### 「いつ取るか」と「どこへ置くか」は別の軸

混同すると、WBGTのように「リクエスト駆動が正しいが、置き場は共有であるべき」ものを
扱えない。WBGTの実態:

| | 取得単位 | TTL |
|---|---|---|
| 地点マスタ（CSV） | 全国約840地点を1リクエスト | 24時間 |
| 予測値（`getForecastData`） | **地点（forecast_no）ごと** | 1時間 |

サイト側が「自動化ツールからの高頻度アクセスは控えて」と明記しているため、全840地点の
先読みは逆効果（840リクエスト/時間）。**取り方はリクエスト駆動のまま**が正しい。一方
置き場を共有にしないと、ワーカーが増えたぶん同じ地点を別々に取りに行く。

### 基準

> **外部から取得した値は、共有の置き場（Redis）へ置く。**プロセス内へ置いてよいのは、
> プロセス寿命で消えることに意味がある否定キャッシュだけ。**大きくてファイルとして扱う
> もの（MSMの`.om`）はディスク。**
>
> **いつ取りに行くか**は置き場と独立に決める。外部の1リクエストが多数の利用者ぶんを
> 賄えるならスケジューラが定期取得し、リクエスト経路は読むだけにする。利用者ごと・
> 地点ごとに取るものだけリクエスト駆動にしてよい。

| ソース | いつ取るか | どこへ置くか |
|---|---|---|
| jma_tile（雨雲・キキクル） | スケジューラが先読み | Redis（現状維持） |
| jma_amedas（実測） | スケジューラ | Redis（現状維持） |
| jma_warning（警報） | 全国1回なのでスケジューラへ寄せる | **Redisへ移す** |
| flood（洪水予報） | 全国1回なのでスケジューラへ寄せる | **Redisへ移す** |
| wbgt 地点マスタ | 全国1回・24時間TTL | **Redisへ移す** |
| wbgt 予測値 | リクエスト時のまま（地点ごと・自粛要請） | **Redisへ移す** |
| msm（予報） | スケジューラ | ファイル（本番56MB。`.om`はパスで読む形式） |
| GSIタイルの404（整備区域外） | リクエスト時 | プロセス内（**意図的にプロセス寿命へ縛っている**——整備区域が広がったとき再起動で再取得の機会が来るように） |

容量の懸念は無い。本番Redisは 1.92MB / 2GB（0.1%）しか使っておらず、移すのは小さな
JSON/CSVだけ。「Redisの利用を積極的に広げない」方針とも矛盾しない——新しい種類の用途を
足すのではなく、**既にRedisにあるべきものを揃えるだけ**で、ルートの材料や派生データを
Redisへ持つ話ではない。

### 気象をDBへ持つ話ではない

条件1（手動で取り直さなければ最新が反映されない）で落ちる。ただし**過去の気象は時間が
経てば「1年以上固定」になる**——今は捨てている。「昨年の同じ時期はどうだったか」を
使いたくなったらDB側の話になるが、今の出口にその要求は無いので対象外。

## 第3層（材料→軸スコア）の扱い

3つのキャッシュのうち`graph_material_cache`は、**DBの表をタイル単位でpickleしたもの**で、
存在理由は「DBの読み出しが遅い」（冷パス2.9秒/タイル）ことだけ。その遅さの原因が
第1・2層で直そうとしているものそのものである（5表のLEFT JOINが1つに、結合キーが23B→10B、
走査する行が半分）。**キャッシュが要らなくなるかは直してから測る**。今は判断しない。

`tile_score_matrix_cache`の存在理由は速度ではない（材料があれば行列は25ms）。軸定義を
編集したときに材料まで捨てずに済ませる**無効化の粒度**の分離で、これは正当。変えない。

`dynamic_way_value_cache`は鍵に時刻・向き・速度のバケットを含む動的材料の配信で、
静的な材料とは別物。混ぜて論じない。

## 決定8: migrationの仕組みを捨て、スキーマ変更は「測ってGAPを埋める」手順にする

### 積み上げた履歴と本番のスキーマは一致していない（実測、2026-09-20）

ORMの`Base.metadata`が宣言する形と、本番の実スキーマを突き合わせた。

```
本番の表 24 / ORMが宣言する表 22
■ 本番にあってORMに無い表: derived_data_meta, schema_migrations
■ 6表で、本番にあってORMが宣言していない外部キーが計8本
```

実物（`pg_get_constraintdef`）:

```
edge_attribute_counts_source_accident_import_run_id_fkey
  FOREIGN KEY (source_accident_import_run_id) REFERENCES accident_import_runs(id) ON DELETE SET NULL
```

対するORM側は `source_accident_import_run_id: Mapped[int | None] = mapped_column(Integer,
nullable=True)` で、`ForeignKey`の宣言が無い。同種が`way_attribute_counts`・
`edge_landcover`・`way_landcover`・`way_divided_carriageway`・`designation_attributes`にもある。

**`create_tables()`で新規環境を作ると、本番より外部キーが8本少ないDBができる。**
migrationが足したものをモデルが知らないまま、誰も突き合わせていなかった。

既存の検知器「ORMの列宣言とmigrationのDDLの食い違い（全件）」は**0件と報告している**。
名前のとおり列しか見ておらず制約を見ない、かつ**比較相手がソース同士**で本番ではない。

### migrationでないとできないことは無い

- **過去の断面へ戻す運用は想定されていない**（`migrate.py`が「downgradeは持たない最小構成」と明記）
- **意図の記録**は設計書で足りる（実行可能である必要が無い）
- **失敗時の復旧**はバックアップで足りる
- **1回ずつ本番を進める**ことは使い捨てのパッチで足りる
- 残るのは「止めずに、既存データを保ったまま進める」だけ。βは作り直しで約1日（実測からの
  外挿: `import_pbf`は東京都心41,555wayで547.9秒→関東32倍で約5時間、`way_landcover`全件
  63.5分、`edge_landcover`約1.5時間、`elevation_attributes`は6時間超でOOMした実績）。
  本番利用がほぼ無い今は止められる

### 何を守るために作られたか（記録を確認した）

[docs/decisions/pre-static-attributes-gate.md](../decisions/pre-static-attributes-gate.md)
決定3（2026-08-16）:

> `create_tables`に冪等ALTER×6・インデックス追加/削除・バックフィルUPDATEが**起動時関数
> として蓄積**している。（略）**起動時に全歴史を再実行する方式は保守コストが線形に悪化する**。

**守ろうとしたのは「起動のたびに全歴史のALTERを再実行するのをやめること」で、これは
達成されている。**「積み上げた履歴が正しいことの保証」は当初から目的に入っていない。

そして選択肢は A（最小migration機構）／B（Alembicフル）／C（現状維持＝`create_tables`への
追記）の3つで、**3つとも「起動時関数 vs migration方式」の枠の中**だった。「変更のたびに
使い捨てのSQLを1回当てる」「作り直す」は検討されていない。問題が「どのmigration方式か」
として立てられたため、**「そもそも仕組みが要るか」は一度も問われなかった**。当時は
Supabase・都心25km圏・容量400MB制約で、作り直しは今より安かった。

### 決定

**捨てる。**

| 捨てるもの | |
|---|---|
| `backend/migrations/*.sql` | 47本 |
| `backend/app/infrastructure/migrate.py` | 105行 |
| `schema_migrations` テーブル | |

**今回の再設計はmigrationで積まず、新しいスキーマを作って生データから再構築する。**

新しい手順:

```
スキーマを変えるとき
  1. 本番の実スキーマを測る
  2. あるべき姿とのGAPを出す
  3. GAPを埋めるパッチを1回当てる（使い捨て。記録は docs/tasks/Txxx.md へ）
  4. もう一度測って、GAPが0になったことを確認する      ← ここが「保証する者」
```

**4が今は存在しない。**だから8本ずれたまま誰も気づかなかった。

### 捨てることで消える副作用

| | |
|---|---|
| `/health` の `pending_migrations` | 概念自体が無くなる |
| 3つの取込バッチの `apply_pending_migrations(engine)` | 取込はスキーマが既に正しい前提になる |
| `tests/test_migrate.py` のブートストラップテスト | 「`create_tables()`→生データ→派生」の検証へ置き換わる |
| CLAUDE.mdの「既存DBの行データを新しいコードが読めなくなる変更」の3択手順 | 前提が変わるので書き直し |

### 捨てる前にやること

1. **47本のSQLコメントに散っている判断をDB設計書へ移す**（なぜこの制約か、なぜこの既定値か）。
   捨ててから書くと失われる
2. **GAP測定を検知器にする**。既存の「ORMの列宣言とmigrationのDDLの食い違い」は比較相手を
   ソースから**本番**へ変え、列だけでなく制約（FK・NOT NULL・UNIQUE・CHECK）も見る
3. **あるべき姿の宣言を、機械可読な形で1か所に置く**（下記「決定9」）

### この形は今日だけで5つ見つかった

作った時点では正しく、前提が変わった後も問われずに残っているもの:

- `raw_intersection_nodes`（road_edgesが遅延構築だった頃の産物。presplitが全域を分割する今も残る）
- `road_nodes.degree`（ランタイムで使われない、バッチの中間結果）
- `edge_id`の向き込み文字列（読む側が`LEAST/GREATEST`で毎回剥がしている）
- `ALLOWED_WAY_TAGS`（容量制約の産物。tagsは実測17MBで容量に寄与していない）
- migrationの仕組み（起動時関数の肥大化を止めるためのもの。目的は達成済み）

**選択肢の立て方に前提が埋まっていると、その前提だけが問われないまま結果が残る。**
決定を記録するときは、**選択肢が共有している前提を明示する**（上のADRなら「migrationの
仕組みを持つ」が3案に共通の前提だった）。

## 決定9: 正本は本番DB。リポジトリにあるのは宣言とバックアップ

「スキーマの正本をORMモデルに置くか`schema.sql`に置くか」という問いは、**どちらも正本
ではない**ので成り立たなかった。

```
正本             : 本番DB
あるべき姿の宣言 : ORMモデル（機械可読・1か所）
意図・判断の記録 : DB設計書（人が読む。実行も比較もしない）
一致の保証       : GAP検知器（本番 vs ORM宣言）← 今は無い
本番を失ったとき : ORM宣言から create_tables() ＋ 生データ ＋ 管理データのバックアップ
```

軸定義と同じ構造である。

| | 正本 | リポジトリのもの | 一致を保証するもの |
|---|---|---|---|
| 軸定義 | 本番DB | スナップショット＝**バックアップ** | 無い（[T972](T972.md)） |
| スキーマ | 本番DB | ORMモデル＝**あるべき姿の宣言** | 無い（実測で外部キーが8本ずれていた） |

**「正本が2つに割れる」という懸念は、両方を正本だと思っていたから出たもの**だった。
片方が正本で片方が宣言なら割れない。ずれるだけで、それは測れば分かる。

残る要求は1つ。**あるべき姿は機械可読な形で1か所に書かれていること**——GAP検知器が読んで
本番と突き合わせられなければ、宣言としての役目を果たさない。ORMモデルで足りる（今日の
GAP測定がそれで動いた）。`PARTITION BY LIST`が表せなければ同じ場所に補助の宣言を足す。
正本の話ではない。

## 決定10: 運用の記録の行き先と、「利用者の設定」の追加

### 運用の記録

| 今 | 行き先 |
|---|---|
| `osm_import_runs` / `accident_import_runs` / `designation_import_runs` | **`source_runs`へ統合**（決定2） |
| `road_graph_tiles` | **`source_runs.profile`＋カバレッジへ一般化**（下記） |
| `derived_data_meta`（グローバルな世代1行） | **ソース別の世代へ置換**（決定6） |
| `schema_migrations` | **廃止**（決定8） |

`road_graph_tiles`（1,020行）は「その範囲の生データを取り込んだか」を表すカバレッジ記録で、
タイル配信の可否判定と`presplit_road_graph`の母集団に使われており**現役**。ただし2点ずれて
いる。

- docstringが「**Overpass**への問い合わせを完了したこと」と書いているが、Overpassは
  [T462](T462.md)で撤去済み。実際に書いているのは`import_pbf`
- 名前が`road_graph_tiles`だが、中身は**生データの取込範囲**でグラフとは関係ない

そして**カバレッジを記録しているのはOSMだけ**。事故・指定路線は「関東7都県」という範囲を
持つのに、引ける形で持っていない。`source_runs.profile`に絞り込みが丸ごと入ることで揃う。

### 「利用者の設定」を分類へ足す

frontendの`localStorage`（色分けモード・レイヤーのON/OFF・レンズ・モバイルシートの高さ）は
**利用者ごとの設定**で、サーバー側のどの分類にも属さない（サーバーは知らないし知る必要も
ない）。UI原則「利用者が意図して決めた値は覚える」の実装。分類として独立させる。

### 不揮発なものの全体像（これで漏れが無い）

| 分類 | 置き場 | 決定 |
|---|---|---|
| 外部ソースの生データ | DB（`source_features`） | 決定1・2 |
| 派生データ | DB（粒度ごとに1表） | 決定4・5 |
| 管理データ（値） | DB（API経由でのみ変更） | 決定3 |
| 管理データ（式） | コード | 決定3 |
| 管理データ（外部の対応表） | 表ごとに振り分け | [T971](T971.md) |
| 取込の範囲の宣言 | コード（プロファイル） | 決定1 |
| 運用の記録 | DB（`source_runs`ほか） | 決定10 |
| キャッシュ | Redis / ディスク / プロセス内 | 決定7 |
| 生成物 | ファイル（backend→frontend の片側流れ） | 構造仕様9（変更なし） |
| 利用者の設定 | ブラウザ（localStorage） | 決定10 |
| あるべき姿の宣言 | リポジトリ（ORMモデル） | 決定9 |
| 取り直せないもののバックアップ | 未定 | [T972](T972.md) |

## 決定11: 区間と有向枝の形（決定3・決定5を差し替える、2026-09-20）

決定3の「区間1本＝1行、有向グラフはメモリ上で作る」は**誤りだった**。`road_edges`が
FROM/TOを持つ構造を捨て、一方通行の表現をアプリへ逃がしていた。対話で全面的に見直し、
以下を承認した。決定3のDDLと決定5のDDLは、この節が置き換える。

### 形

生データ層は`source_features`（`source`でLISTパーティション）のまま。区間は実表1本で、
**位相・形状・材料をすべて持つ**（決定5の`edge_materials`は統合して廃止）。**有向枝は
ビュー**で、区間から向きごとに1〜2行を導く。

```
source_features(osm_way)  node_ids bigint[] / geom / attrs
        ↓ 分割（Pythonバッチ。位置範囲 start_ord..end_ord を決める）
road_segments  位置範囲・形状・距離・方位・材料          ← 実表
        ↓ ビュー（向きごとに1〜2行、方位と標高を読み替え）
road_edges                                              ← 実体を持たない
```

### 分割が正しいことをDBで担保する

| 担保したいこと | 宣言 |
|---|---|
| 区間同士が重ならない | `EXCLUDE USING gist (osm_way_id WITH =, int4range(start_ord, end_ord) WITH &&)` |
| 前の区間の終わり＝次の始まり | `(osm_way_id, prev_index, start_ord)` の自己参照FK。`prev_index`は`NULLIF(segment_index - 1, -1)`の生成列で、先頭はNULLになりMATCH SIMPLEで素通りする |
| 先頭が1から始まる | `CHECK (segment_index > 0 OR start_ord = 1)` |
| 形状が位置範囲と辻褄が合う | `CHECK (ST_NumPoints(geom) = end_ord - start_ord + 1)` |
| 実在するwayを指す | `feature_id`（生成列）への、複合でないFK |
| 成功した取込から作られた | `source_run_success`へのFK |

開発DBで7ケースを実行して確かめた（すべてロールバック）。先頭の投入は通り、飛んだ区間・
重なる区間・1始まりでない先頭・途中の区間の削除はすべて弾かれ、環状を中間で切った2本は
通った。`btree_gist`拡張が要る。

### 宣言できないまま残るもの

- 最後の区間がwayの終端で終わること（wayの`array_length(node_ids, 1)`が要る＝他表参照。
  CHECKも生成列も他表を参照できない）
- 形状がwayのその区間と実際に一致すること（同上。頂点数の一致までは行内で縛れる）
- `distance_m`が`geom`の長さと一致すること（`ST_Length(::geography)`がIMMUTABLEでない）
- 派生層が生データ層を覆っていること（FKは向きが逆。**制約ではなく台帳の仕事**）

最後の1つは`derived_data_freshness.py`へ「生データの母数に対する行の欠け」を足して対応する。
いまの台帳は鮮度（`source_run_id`）と完成度（値のNULL件数）しか見ておらず、行そのものが
無いケースはどちらにも現れない。

### 判断の根拠（すべて本番実測、2026-09-20）

**有向枝を実表にせずビューにする。** 同じ列・同じ索引で作り直して比べると、有向にする
代償は行の倍増ぶんである。

| | 行数 | 本体 | 索引込み |
|---|---|---|---|
| 無向の区間 | 2,610,982 | 512 MB | 694 MB |
| 有向の枝 | 5,065,850 | 996 MB | 1,350 MB |

決定3が挙げた3,719 MBとの差は、`edge_id varchar`の主キー（370 MB）・端点の索引2本
（378 MB）・`osm_way_id`の索引（122 MB）・肥大を含んでおり、向きの代償ではなかった。

ルート探索はz12タイル単位でトポロジを読み、メモリLRU→ディスクpickleへ寄せる
（`graph_service._get_or_build_tile_materials`）。DBを叩くのはタイルが冷えているときだけ。
30km四方はz12タイル16枚で、**全部冷えている最悪の1回**が次のとおり。

| | 16枚合計 | 行数 |
|---|---|---|
| ビューから有向を読む | 348.0 ms | 484,143 |
| 区間表を読みPythonで展開 | 158.9 ms | 271,913 |

**形状は列で持つ。** 1表・geom列・GiSTで、z12タイル3枚を読む。

| | 3枚合計 | 容量 |
|---|---|---|
| 形状を列で持つ | 38.1 ms | 574 MB |
| 読むときにwayの頂点から組む | 1,649.7 ms | 229 MB |

345 MBで43倍。持つ。

**端点は持たない。** `node_ids[start_ord]`で引く。本番52,112行で124.7 ms 対 123.5 ms
（差1%）。同じ「複製か導出か」で形状と答えが割れるのは、費用が2桁違うため。

**逆向きの方位は`(bearing + 180) mod 360`で導く。** 本番2,454,642対の実測で、保存されて
いる両向きの差が180度からずれる量は中央値0.000228度・p99 0.002286度。`reverse_bearing_deg`
列は撤去する。

**分割はPythonのまま。** 同じ規則をSQLで書いて開発DBで突き合わせたところ、区間23,322本・
`(way, 区間)`の食い違い0件・端点の食い違い0件で**完全に一致した**。値は長さが平均0.192%
（測地線のぶんSQLが正確）、方位が平均0.074度ずれる。所要はSQLが493 ms、Pythonが125 ms
（読み込み52 msを含む）で、**SQLが約4倍遅い**。Python側の仕事が整数配列を歩くだけで元々
安く、SQL側は頂点を行に展開して並べ替える必要があるため。分割の正しさは表に張る宣言的
制約が担保するので、INSERTの実行者は担保の根拠にならない。移さない。

### 通行の向き

`way_materials.direction`を**`allowed_orientation`へ改名**する。保持しているのは「wayの
ノード列の順に進めるか、逆に進めるか、両方か」だけで、同じ表にある`bearing_deg`（方位）
とは別物である。値は`both`/`forward`/`backward`のまま。

**way単位のまま置く。** `oneway`はwayに付くタグで1本のway内で一定なので、区間へ持たせても
情報が増えない。読み出しは既に`way_materials`へ結合しているので追加費用がない。決定4で
検討した区間側の`traversable`列は廃止する。

ビューの結合は`INNER JOIN`。行が無いときに`COALESCE(..., 'both')`で既定値を作るのは、
DBが持っていない事実を捏造することになる。

### 環状の区間

始点と終点が同じ区間が本番に**1,685本**あり、そのうち**1,572本は両向きの方位がどちらも
0.000度**になっていた（最長1,411 m）。閉じた線に始点→終点の方位は定義できないため。
ロータリーや環状の生活道路として正しく実在する道なので取り込む。

分割の規則に1つ足す: **始点と終点が同じ区間は、中間の位置でもう1回切る**。`A → A`が
`A → M → A`になり、両方とも端点が異なるので方位が定義できる。環状のwayに限らず、同じ
ノードを2度通るwayでも同じ形の区間ができるので、「始点＝終点の区間」全般に当てはめる。

### 距離0の区間

本番2,611,210本のうち距離0は**2本**。1本は`LINESTRING EMPTY`（本物の欠陥）、もう1本は
実在する4.8 cmの区間が`round(_length_m(points), 1)`で0へ丸められたもの。短い区間自体は
正当で、1 m未満が1,782本あり`trunk`が22本・`primary`が49本含まれる。

**丸めを撤去したうえで**`CHECK (distance_m > 0)`と
`CHECK (NOT ST_IsEmpty(geom) AND ST_NumPoints(geom) >= 2)`を張る。

### 生データ層に足すもの

`node_ids bigint[]`を親に足す。`payload`を配列型にはできない——ラスタの画素とベクタタイルの
本体が同じ袋に入っており、1つの配列型に収まらないため、osm_way専用の列を別に持つ。子
パーティションだけに列を足すことはできない（「パーティションに列は追加できません」）ので、
親に付けて他ソースはNULLにし、osm_wayの約束を子のCHECKで縛る。

ソースごとの約束は子パーティションの冒頭へ書く。**書いてよいのは提供元の出典と原文だけ**で、
そう決めた経緯は書かない。

```sql
-- OpenStreetMap
-- 使用許諾: ODbL   https://www.openstreetmap.org/copyright
-- 「OpenStreetMap とその協力者をクレジットするという条件のもと、当方のデータは自由に複写し
--   配布し送信し利用を認めています。当方のデータを改変もしくは翻案した場合は、元データと
--   同じライセンスの適用によって配布が認められます。」
--
-- 仕様: https://wiki.openstreetmap.org/wiki/Elements
-- 「A way is an ordered list of between 1 (!) and 2,000 nodes that define a polyline.」
-- 「the way's first and last node will be the same. This is called a "closed way".」
-- 「Note that closed ways occasionally represent loops, such as roundabouts on highways,
--   rather than solid areas.」
-- 「An element cannot have two tags with the same key: the keys must be unique.」
--   → タグをJSONオブジェクト（attrs）で持ってよい根拠
CREATE TABLE source_features_osm_way PARTITION OF source_features FOR VALUES IN ('osm_way');
CREATE UNIQUE INDEX ON source_features_osm_way (feature_id);
ALTER TABLE source_features_osm_way
  ADD CHECK (GeometryType(geom) = 'LINESTRING'),
  ADD CHECK (node_ids IS NOT NULL AND array_length(node_ids, 1) >= 2),
  -- wayのノード列とジオメトリの頂点は1対1。取込は座標の欠けたwayを丸ごと落とすため
  -- 成り立つ（`osm_pbf.py`）。頂点だけ抜くと、区間の位置範囲が指す頂点がずれる。
  ADD CHECK (array_length(node_ids, 1) = ST_NumPoints(geom));

-- OpenStreetMap（node）
-- 「Each node comprises at least an id number and a pair of coordinates.」
--   https://wiki.openstreetmap.org/wiki/Elements
CREATE TABLE source_features_osm_node PARTITION OF source_features FOR VALUES IN ('osm_node');
CREATE UNIQUE INDEX ON source_features_osm_node (feature_id);
ALTER TABLE source_features_osm_node
  ADD CHECK (GeometryType(geom) = 'POINT');

-- 国土地理院 標高タイル（DEM）
-- 使用許諾: 公共データ利用規約（第1.0版）PDL1.0
--   https://www.gsi.go.jp/kikakuchousei/kikakuchousei40182.html
-- 「コンテンツを編集・加工等して利用する場合は、上記出典とは別に、編集・加工等を行ったことを
--   記載してください。」→ 出典表記の要件（T974）
--
-- 仕様: https://maps.gsi.go.jp/development/demtile.html
-- 「標高値が存在しない画素には「e」の文字が格納されている。」
--   → payloadのNODATA。導出側の標高がNULLになりうる根拠
-- 「標高データは小数点第二位までデータとして入っている（単位はm）。」
--   → payloadは int32・0.01m単位（scale=100）。int16では富士山（3,776m）が入らない
--
-- 仕様: https://maps.gsi.go.jp/development/hyokochi.html
-- 「元となる標高モデルデータ標高点の値は、地表面の測定値に基づいているため、構造物（建物、
--   高架橋等）の高さを反映したものではありません。」
--   → 橋・トンネルの区間に平均勾配を持たせない根拠（domain/attributes.py）
-- 「地図上で水部になっている地域では、標高値がデータに入っていない場合や、正確な値が
--   入っていない場合があります」
-- 最大ズームレベル: DEM5A=15 / DEM5B=15 / DEM5C=15 / DEM10B=14
--   → 製品ごとに取得するズームが違う（T978）
CREATE TABLE source_features_dem PARTITION OF source_features FOR VALUES IN ('dem');
ALTER TABLE source_features_dem
  ADD CHECK (payload IS NOT NULL),
  ADD CHECK (attrs->>'dtype' = 'int32_le' AND (attrs->>'scale')::int = 100);

-- 土地被覆（Impact Observatory / Esri / Microsoft, Sentinel-2 10m Annual LULC）
-- 使用許諾: CC BY 4.0   https://registry.opendata.aws/io-lulc/
--
-- クラス定義（機械可読の正本。file:values）:
--   https://planetarycomputer.microsoft.com/api/stac/v1/collections/io-lulc-annual-v02
--   0=No Data / 1=Water / 2=Trees / 4=Flooded vegetation / 5=Crops
--   7=Built area / 8=Bare ground / 9=Snow/ice / 10=Clouds / 11=Rangeland
--   → 9クラス（Cloudsを含む）とNo Data。payloadはこの生の値をそのまま持つ
CREATE TABLE source_features_lulc PARTITION OF source_features FOR VALUES IN ('lulc');
ALTER TABLE source_features_lulc
  ADD CHECK (payload IS NOT NULL),
  ADD CHECK (attrs->>'dtype' = 'uint8');

-- 交通事故統計（警察庁 本票）
-- 使用許諾: 公共データ利用規約（第1.0版）PDL1.0   https://www.npa.go.jp/rules/index.html
-- 「コンテンツを編集・加工等して利用する場合は、上記出典とは別に、編集・加工等を行ったことを
--   記載してください。」→ 出典表記の要件（T974）
--
-- 仕様: ファイル定義書・各種コード表
--   https://www.npa.go.jp/publications/statistics/koutsuu/opendata/index_opendata.html
-- 全68列が「固定長・文字列」。数値に見える列も文字列で、ゼロ詰めされる
-- 「死者数」桁数3・「交通事故により24時間以内に亡くなった者の人数をいう。」
--   → 値は '000' 形式。重み付けが数える対象は24時間以内の死亡に限られる
-- 「本票番号」「都道府県警察署等ごとの一連番号をいう。」
--   → 年をまたぐと振り直される。natural_keyに年を含める根拠
-- 「当事者種別」51=「軽車両－自転車」/ 52=「軽車両－駆動補助機付自転車」
-- 「地点　緯度（北緯）」桁数9・「経度（東経）」桁数10・「緯度は世界測地系（度分秒）とする。」
--   → 桁の内訳は定義書に無い。9桁/10桁から DD(D)+MM+SSsss と解釈している
-- 配布は暦年単位（ページの表記は「２０２４年（令和６年）」、ファイルは「本票_01-12月」）
CREATE TABLE source_features_accident PARTITION OF source_features FOR VALUES IN ('accident');
ALTER TABLE source_features_accident
  ADD CHECK (GeometryType(geom) = 'POINT');
```

子パーティションと、そこへ張るソースごとのCHECK・一意索引は取込の側が作る。

### 成功した取込へのFK

派生行が成功したrunを指していることを、**派生側に列を足さずに**担保する。成功した取込
だけが載る薄い表をソースでパーティションし、その子をFKで指す。

派生表は既にある`source_run_id`（8バイト）でこの子を指すだけなので、**追加の容量は0**。
`source`と`status`を生成列で持つ案（派生1,500万行に対し約330 MB）は採らない。この形なら
`status`を短く符号化する必要もないので`'succeeded'`のまま置く。

`_close_run`が成功時にこの1行を足す。忘れると派生が1行もINSERTできなくなるので、忘れた
ことが即座に分かる。

### マテリアライズドビューは採れない

分割をマテリアライズドビューにする案を検討したが、開発DBで試して不可と分かった。

| | |
|---|---|
| 一意索引・GiST索引 | 張れる |
| CHECK・EXCLUDE | **張れない**（`ADD CONSTRAINT`できない） |
| マテビューを参照するFK | **張れない**（テーブルではない） |
| マテビューから他表へのFK | **張れない** |
| パーティションにする | **張れない** |

普通のビューも参照先にできない。T970が入れようとしている宣言的制約をすべて失う。

### 承認したDDL

```sql
CREATE TABLE source_runs (
    run_id      bigserial   PRIMARY KEY,
    source      text        NOT NULL,
    status      text        NOT NULL,
    started_at  timestamptz NOT NULL,
    finished_at timestamptz,
    origin      jsonb       NOT NULL,
    profile     jsonb       NOT NULL,
    counts      jsonb       NOT NULL,
    CHECK (status IN ('running', 'succeeded', 'failed')),
    CHECK ((status = 'running') = (finished_at IS NULL))
);

CREATE TABLE source_run_success (
    source text   NOT NULL,
    run_id bigint NOT NULL REFERENCES source_runs (run_id),
    PRIMARY KEY (source, run_id)
) PARTITION BY LIST (source);

CREATE TABLE source_features (
    source      text   NOT NULL,
    natural_key text   NOT NULL,
    feature_id  bigint GENERATED ALWAYS AS
                (CASE WHEN natural_key ~ '^[0-9]+$' THEN natural_key::bigint END) STORED,
    run_id      bigint NOT NULL REFERENCES source_runs (run_id),
    geom        geometry(Geometry, 4326) NOT NULL,
    attrs       jsonb  NOT NULL DEFAULT '{}',
    payload     bytea,
    node_ids    bigint[],
    PRIMARY KEY (source, natural_key)
) PARTITION BY LIST (source);

CREATE EXTENSION IF NOT EXISTS btree_gist;

CREATE TABLE road_segments (
    osm_way_id    bigint   NOT NULL,
    segment_index smallint NOT NULL,
    start_ord     smallint NOT NULL,
    end_ord       smallint NOT NULL,
    prev_index    smallint GENERATED ALWAYS AS (NULLIF(segment_index - 1, -1)) STORED,

    geom        geometry(LineString, 4326) NOT NULL,
    distance_m  real NOT NULL,
    bearing_deg real NOT NULL,

    accident_count      real,     intersection_count smallint,
    poi_signal          smallint, poi_crossing       smallint,
    poi_stop            smallint, poi_level_crossing smallint,
    poi_barrier         smallint,

    -- 向きで変わる列は、**列名から反転が導ける名前**を付けること。有向枝のビューは
    -- 対応表を持たず、列名から読み替えを組み立てる（`reversed_material_expression`）。
    --   対になる語を入れ替える: start_ ↔ end_ ／ _gain_ ↔ _loss_ ／ max_ ↔ min_
    --   `_grade` で終わる量は符号を反転する
    -- この規則から外れた名前を付けると、その列は「向きで変わらない」と判定され、逆向きの
    -- 枝が順方向の値を返す。エラーにはならない。
    -- 例: 上り下りを uphill_m / downhill_m と名付けると反転しない。gain/loss を使う。
    start_elevation_m   real, end_elevation_m  real,
    elevation_gain_m    real, elevation_loss_m real,
    average_grade       real, max_grade real, min_grade real,
    lc_valid_pixels     integer,
    lc_water real, lc_trees real, lc_flooded_veg real, lc_crops real,
    lc_built real, lc_bare  real, lc_snow_ice   real, lc_rangeland real,

    source_run_id bigint NOT NULL,

    PRIMARY KEY (osm_way_id, segment_index),
    UNIQUE (osm_way_id, segment_index, end_ord),
    FOREIGN KEY (osm_way_id)    REFERENCES source_features_osm_way    (feature_id),
    FOREIGN KEY (source_run_id) REFERENCES source_run_success_osm_way (run_id),
    FOREIGN KEY (osm_way_id, prev_index, start_ord)
        REFERENCES road_segments (osm_way_id, segment_index, end_ord),

    CHECK (start_ord >= 1 AND end_ord > start_ord),
    CHECK (segment_index > 0 OR start_ord = 1),
    EXCLUDE USING gist (osm_way_id WITH =, int4range(start_ord, end_ord) WITH &&),

    CHECK (NOT ST_IsEmpty(geom) AND ST_NumPoints(geom) >= 2),
    CHECK (ST_NumPoints(geom) = end_ord - start_ord + 1),
    CHECK (distance_m  > 0),
    CHECK (bearing_deg >= 0 AND bearing_deg < 360),
    CHECK (accident_count     IS NULL OR accident_count     >= 0),
    CHECK (intersection_count IS NULL OR intersection_count >= 0),
    CHECK (poi_signal         IS NULL OR poi_signal         >= 0),
    CHECK (poi_crossing       IS NULL OR poi_crossing       >= 0),
    CHECK (poi_stop           IS NULL OR poi_stop           >= 0),
    CHECK (poi_level_crossing IS NULL OR poi_level_crossing >= 0),
    CHECK (poi_barrier        IS NULL OR poi_barrier        >= 0),
    CHECK (elevation_gain_m   IS NULL OR elevation_gain_m   >= 0),
    CHECK (elevation_loss_m   IS NULL OR elevation_loss_m   >= 0),
    CHECK (average_grade IS NULL OR average_grade BETWEEN -40 AND 40),
    CHECK (max_grade     IS NULL OR max_grade     BETWEEN -40 AND 40),
    CHECK (min_grade     IS NULL OR min_grade     BETWEEN -40 AND 40),
    CHECK (min_grade IS NULL OR max_grade IS NULL OR min_grade <= max_grade),
    CHECK (lc_valid_pixels IS NULL OR lc_valid_pixels >= 0),
    CHECK (lc_valid_pixels IS NULL OR lc_valid_pixels = 0 OR
           abs(coalesce(lc_water, 0) + coalesce(lc_trees, 0) + coalesce(lc_flooded_veg, 0)
             + coalesce(lc_crops, 0) + coalesce(lc_built, 0) + coalesce(lc_bare, 0)
             + coalesce(lc_snow_ice, 0) + coalesce(lc_rangeland, 0) - 100) < 0.5)
);
CREATE INDEX ON road_segments USING gist (geom);

CREATE VIEW road_edges AS
SELECT s.osm_way_id, s.segment_index, d.forward,
       w.node_ids[CASE WHEN d.forward THEN s.start_ord ELSE s.end_ord   END] AS from_node_id,
       w.node_ids[CASE WHEN d.forward THEN s.end_ord   ELSE s.start_ord END] AS to_node_id,
       s.geom, s.distance_m,
       CASE WHEN d.forward THEN s.bearing_deg
            ELSE (s.bearing_deg + 180.0) - 360.0 * floor((s.bearing_deg + 180.0) / 360.0)
       END AS bearing_deg,
       CASE WHEN d.forward THEN s.start_elevation_m ELSE s.end_elevation_m   END AS start_elevation_m,
       CASE WHEN d.forward THEN s.end_elevation_m   ELSE s.start_elevation_m END AS end_elevation_m,
       CASE WHEN d.forward THEN s.elevation_gain_m  ELSE s.elevation_loss_m  END AS elevation_gain_m,
       CASE WHEN d.forward THEN s.elevation_loss_m  ELSE s.elevation_gain_m  END AS elevation_loss_m,
       CASE WHEN d.forward THEN s.average_grade ELSE -s.average_grade END AS average_grade,
       CASE WHEN d.forward THEN s.max_grade     ELSE -s.min_grade     END AS max_grade,
       CASE WHEN d.forward THEN s.min_grade     ELSE -s.max_grade     END AS min_grade,
       s.accident_count, s.intersection_count, s.poi_signal, s.poi_crossing,
       s.poi_stop, s.poi_level_crossing, s.poi_barrier,
       s.lc_valid_pixels, s.lc_water, s.lc_trees, s.lc_flooded_veg, s.lc_crops,
       s.lc_built, s.lc_bare, s.lc_snow_ice, s.lc_rangeland,
       s.source_run_id
FROM road_segments s
JOIN source_features_osm_way w ON w.feature_id = s.osm_way_id
JOIN way_materials          m ON m.osm_way_id = s.osm_way_id
CROSS JOIN LATERAL (VALUES (true), (false)) AS d(forward)
WHERE m.allowed_orientation = 'both'
   OR (m.allowed_orientation = 'forward') = d.forward;

CREATE TABLE way_materials (
    osm_way_id bigint PRIMARY KEY REFERENCES source_features_osm_way (feature_id),
    allowed_orientation text NOT NULL,
    divided boolean,
    accident_count real, intersection_count smallint,
    poi_signal smallint, poi_crossing smallint, poi_stop smallint,
    poi_level_crossing smallint, poi_barrier smallint,
    lc_valid_pixels integer,
    lc_water real, lc_trees real, lc_flooded_veg real, lc_crops real,
    lc_built real, lc_bare real, lc_snow_ice real, lc_rangeland real,
    designation_emergency_transport double precision,
    designation_critical_logistics  double precision,
    source_run_id bigint NOT NULL REFERENCES source_run_success_osm_way (run_id),
    CHECK (allowed_orientation IN ('both', 'forward', 'backward')),
    CHECK (designation_emergency_transport IS NULL
           OR designation_emergency_transport BETWEEN 0 AND 1),
    CHECK (designation_critical_logistics IS NULL
           OR designation_critical_logistics BETWEEN 0 AND 1)
);

CREATE TABLE node_materials (
    osm_node_id bigint PRIMARY KEY REFERENCES source_features_osm_node (feature_id),
    kind text,
    branch_count        smallint NOT NULL,
    has_traffic_signals boolean  NOT NULL,
    max_highway_rank    smallint NOT NULL,
    source_run_id bigint NOT NULL REFERENCES source_run_success_osm_node (run_id),
    CHECK (branch_count     >= 0),
    CHECK (max_highway_rank >= 0)
);
```

子パーティションと、そこへ張るソースごとのCHECK・一意索引は取込の側が作る。`kind`と
`status`の値域、件数と`lc_*`の値域はdomainの宣言から生成する（`way_materials`にも
`road_segments`と同じ値域の制約を張る）。

### アプリに残すもの（混入チェック済み）

DBが守るのは「DBのデータそのものの正しさ」で、次はアプリの責務として残す。

| | 理由 |
|---|---|
| `derive_topology.py`・`derive_node_materials.py`の`raise` | 「先に取込を通せ」＝処理順序 |
| ビューが向き依存の列を全部覆っているかの検査 | コードの不変条件。書き忘れても**DBの値は正しい** |
| アダプタのレジストリ確認・ラスタ/プロファイルの設定確認 | 設定の整合 |
| 0件マッチのWARNING | 観測。値の正しさではない |
| `schema_gap.py` | ORMとDBの差分検出 |
| 道路網が連結していること | ルート生成の結果の正しさ |

境界線上が1つある。`average_grade BETWEEN -40 AND 40`は「±40%を超える勾配はデータの誤り」
というドメインの判断であり、物理法則ではない。値域制約の形だが**しきい値が動きうる**点で
他と性質が違う。DBに置くが、変えるときにDDLを触ることは自覚しておく。

## 段取り

本番を触るのは段階Dだけ。A〜Cは使い捨てDBと手元で完結する。

| 段階 | 内容 | 終わったと言える条件 |
|---|---|---|
| **A. 新しいスキーマを作れるようにする** | ORMモデルを新しい形（`source_features`/`source_runs`/`road_edges`/`node_materials`/`way_materials`/`edge_materials`）へ書き換える。`PARTITION BY LIST`がORMで表せるかをここで確定する | 使い捨てDBへ`create_tables()`だけで新スキーマが立ち、**本番の実スキーマとのGAPが出せる**（GAP測定が道具として動く） |
| **B. 取込を新フォーマットへ** | 取込プロファイルを全ソース共通へ広げる。OSM（タグ全保持）・事故（全列）・指定路線・DEM（z15）・LULC（z14）・基礎地図（z14）を`source_features`へ入れる。`source_runs`へ絞り込みを丸ごと記録する | 使い捨てDBに小bbox（東京都心）で6ソースすべてが入り、`source_runs`から絞り込みが読める |
| **C. 派生と読み出し側を追従させる** | `road_edges`（区間1本1行・複合主キー）、`node_materials`（`branch_count`へ一本化）、`way_materials`（4表統合）、`edge_materials`（3表統合）。材料の値式・タイルSQL・リポジトリ・評価・テストを新スキーマへ | 小bboxでルート生成と路面タイルが通り、**逆向きの合成・勾配の符号反転が読み出し側で正しい**ことをテストが押さえる |
| **D. 本番を作り直す** | 新スキーマで関東全域を再構築。所要は未計測（小bboxの実績から数時間〜1日規模と見込むが**実測していない**） | 本番のGAPが0。ルート生成・タイルが本番で通る。事前生成（決定6の3段目）まで終わっている |
| **E. 後片付け** | `migrations/*.sql` 47本・`migrate.py`・`schema_migrations`を撤去。`orm_migration_column_drift`検知器を撤去し、**ORM宣言↔本番のGAP検知器**へ置き換える | CIが緑。GAP検知器がわざと入れたずれで鳴る |

**捨てる前にやること（段階Eの前）**: 47本のSQLコメントに散っている判断をDB設計書へ移す。

### 所要について

**測っていない。**小bboxでの実績（`import_pbf` 547.9秒／`refresh_derived` 196.4秒、
[T573](T573.md)）と関東全域の外挿（`import_pbf`約5時間・`way_landcover`63.5分・
`edge_landcover`約1.5時間・`elevation_attributes`は6時間超でOOMした実績）はあるが、
**実装そのものの所要は見積もっていない**。段階Aを終えた時点で測り直す。

## 完了条件

- 本番のスキーマとORM宣言のGAPが0であることを、**機械で確認できる**（今は確認手段が無く、
  外部キーが8本ずれていた）
- 生データ6ソースが`source_features`に同じ形で入り、`source_runs`から「いつ・どこから・
  どの絞り込みで」が引ける
- 派生が粒度ごとに1表になり、way単位の材料を足すのに表もバッチも増えない
- 「交差点」の定義がシステムに1つだけある
- 生データを1つ差し替えると、下流の派生とキャッシュが**作り直されてから**世代が切り替わる
- 関東全域で、生データを全部DBへ持ったうえで容量が見込みの範囲に収まる
  （β約7GB。全国換算で約74GB＝147GiBの50%）

## 消すもの（測定した規模）

**新しい形を書くことと、古い形を消すことは別の作業で、後者は黙って落ちる。**本タスクで
今日見つけた化石5つ（`raw_intersection_nodes`・`road_nodes.degree`・`edge_id`の向き込み
文字列・`ALLOWED_WAY_TAGS`・migrationの仕組み）は、いずれもそうやって残った。

ラベルを分けて測った（2026-09-20）。

| | 規模 | 確度 |
|---|---|---|
| **確実に消える** | `migrations/*.sql` 47本 **1,081行**／`migrate.py` **105行**／`schema_migrations`／`tests/test_migrate.py`／`orm_migration_column_drift`検知器とそのプローブ | 消える |
| **統合されて減る** | precomputeバッチ9本 **1,132行** → node/way/edge の3本へ | 減るが**量は未測定** |
| **触れる範囲** | app 65ファイル/20,280行・tests 42/16,118・scripts 7/1,302・benchmarks 6/1,594 ＝ **120ファイル/39,294行** | **「触れる」であって「消える」ではない**。大半は書き換え |

**純減がいくらになるかは測っていないので、見込みを出さない**（[T956](T956.md)で削減行数が
4回変わった原因は、母集団を決めずに数字を言ったことだった）。段階Eの後に、母集団の定義
（このタスクのコミットだけが触ったファイル）を先に書いてから1度だけ測る。

### 消えたことを機械で確かめる（完了条件へ入れる）

約束ではなく検査にする。

1. **廃止した表の名前が、リポジトリのどこにも残っていない**
   ```
   grep -rnE "osm_raw_ways|osm_raw_nodes|osm_raw_pois|accident_points|route_designations|
   edge_attribute_counts|way_attribute_counts|elevation_attributes|way_landcover|edge_landcover|
   way_divided_carriageway|designation_attributes|raw_intersection_nodes|road_graph_tiles|
   derived_data_meta|schema_migrations|osm_import_runs|accident_import_runs|designation_import_runs"
   ```
   → **0件**（docs/tasks の経緯記述を除く）
2. **廃止したモジュールへの参照が0件**（`migrate`・撤去したバッチ・撤去したRowクラス）
3. **`pytest backend/tests -q --co` が通る**（import破損が無い＝消し残しの参照が無い）
4. **`python scripts/review_checks.py docs --since origin/master` が違反なし**
5. **撤去した検知器が`DETECTOR_ENFORCEMENT`・プローブ・外縁ケースの3か所すべてから消えている**
   （1か所でも残るとメタテストが落ちる＝機械が確かめる）

3と5は既存の仕組みがそのまま検査になる。1と2は段階Eで実行し、結果をこのタスクへ書く。

## 一本化で消える「処理」（表ではなく処理で並べる）

表がどう変わるかだけでは、**何をしなくてよくなるか**が見えない。

| 一本化するもの | 消える処理 | 実測 |
|---|---|---|
| 0次表を`source_features`へ | ソースごとのステージングDDL・MERGE SQL・COPY経路・run記録表3つ | `import_pbf` 16箇所／`import_accidents` 6箇所／`import_designations`は別方式 |
| 派生を粒度ごとに1表へ | way粒度4本・edge粒度3本・node粒度2本の**別々のUPSERT経路** | バッチ9本 1,132行 |
| 同上 | **表ごとに違う鮮度・完成度の仕様**（`NOT EXISTS`／`degree = 0`／`max_highway_rank = 0 AND EXISTS`…） | `derived_data_freshness.py` 314行・仕様9件 → NULL判定1つ |
| 同上 | 材料の値式の別名機構（`w`/`re`/`c`/`e`/`el`/`wl`/`d`のLEFT JOIN組み立て） | 5結合 → 1結合 |
| 主キーを複合自然キーへ | タイルSQLの`LEAST/GREATEST`合成、`edge_landcover`の`node_lo`/`node_hi`列と結合条件、`edge_id`文字列の生成 | 独立に発生していた回避2箇所 |
| 向きを行で持たない | fwd/bwdの行生成、逆向きペアの重複ジオメトリ、`is_loop_too_similar`の同一区間判定 | 行が48.5%減 |
| 交差点の定義を1つへ | `raw_intersection_nodes`の全再構築SQL、`road_nodes.degree`の計算、孤立ノードのdegreeリセットSQL | 表1つとSQL3本 |
| タグの許可リスト廃止 | `ALLOWED_WAY_TAGS`／`ALLOWED_NODE_TAGS`、**それを守る検知器`way_tag_allowlist`**、取込時の分類器 | 検知器1本 |
| 取込範囲をプロファイルへ | `KANTO_PREFECTURE_CODES`（NPA採番）と`KANTO_PREFECTURE_CODES_KSJ`（JIS採番） | 定数2つ |

## 段階Bの条件を書き直す

「6ソースが入る」は**6本の取込経路を書く**とも読める。それでは共通フォーマットにした意味が
無い。取込は1本にする。

```
共通の取込経路（1本）
  ├ プロファイルを読む（どのソースを、どの絞り込みで）
  ├ source_runs へ run を起こす
  ├ ソースアダプタから (natural_key, geom, attrs, payload) を1件ずつ受け取る ← ここだけがソース別
  ├ source_features の該当パーティションへ COPY
  └ run を成功で閉じる（件数・origin・profile を記録）
```

ソースアダプタが持つのは**外部の形を開いて1件ずつ返すことだけ**（PBFはpyosmium、CSVは
csv.reader、ZIP+GMLはxml.etree、ラスタはrasterio）。ステージング・MERGE・run記録・
絞り込みの適用・パーティションの選択は全部共通側。

段階Bの条件:

- **`source_features`へ書くコードが1箇所しかない**（grepで確認）
- ソース別のコードはアダプタだけで、ステージング・MERGE・run記録を持たない
- 小bboxで6ソースが同じ経路を通って入る
- `source_runs`から「いつ・どこから・どの絞り込みで」が読める

## アプリのコードが持つべきでないもの

**バックアップと、そこからのデータ投入はアプリの責務ではない**（運用の仕組み。
[T972](T972.md)）。正本は本番DBで、新規環境は本番から持ってくる。アプリが自前の
スナップショットを抱える理由が無い。

撤去する:

| | 行数 |
|---|---|
| `app/infrastructure/axis_definitions_snapshot.py` | 77 |
| `scripts/dump_axis_definitions_snapshot.py` | 42 |
| `scripts/bootstrap_fresh_db.py` | 52 |
| `scripts/bootstrap_ci_db.py` | 79 |
| `fixtures/axis_definitions_snapshot.json` | 697 |
| 参照しているテスト6ファイルの該当箇所 | — |
| CLAUDE.mdの手動ダンプ運用の記述 | — |
| **計** | **947＋** |

`test_migrate.py`（324行）も**ファイルごと消える**。8件のうち5件はmigrate.py専用、1件は
bootstrapでmigration適用が主題、残る2件は軸スナップショットの読み込みで、**いずれも
主題が消える**。

## 削減の見込み（母集団と方法を先に書く）

母集団＝再編の対象になるファイル。方法＝「現状の行数（実測）− 残る見込み」。
**左の列だけが実測で、残る見込みと追加は見込み。**

| | 現状（実測） | 残る見込み | 減 |
|---|---|---|---|
| `migrations/*.sql` 47本 | 1,081 | 0 | 1,081 |
| `migrate.py` | 105 | 0 | 105 |
| `test_migrate.py` | 324 | 0 | 324 |
| 軸スナップショット一式 | 947 | 0 | 947 |
| precomputeバッチ 9本 | 1,132 | 3本で 360 | 772 |
| 取込バッチ 3本 | 1,129 | 共通1本＋アダプタ3で 440 | 689 |
| `_landcover.py` | 427 | アダプタへ縮んで 150 | 277 |
| `derived_data_freshness.py` | 314 | 100 | 214 |
| ORMモデル 3本 | 542 | 新しい6表で 250 | 292 |
| `road_graph_repository.py` | **3,344** | 2,400〜2,600 | 750〜950 |
| `material_sql.py`＋`material_coverage.py` | 232 | 170 | 62 |
| 検知器2本＋プローブ・外縁 | — | 0 | 約 150 |
| **実装の小計** | | | **約 5,700〜5,900** |
| テスト（触れる 42ファイル/16,118行のうち） | 16,118 | — | 1,600〜2,400（10〜15%減の見込み） |
| **追加**（共通取込経路・新モデル・パーティション管理・GAP検知器・新テスト） | — | — | −800〜1,200 |
| **純減の見込み** | | | **約 6,000〜8,000行** |

**一番怪しいのは`road_graph_repository.py`**（単一ファイル最大の3,344行）。5表のLEFT JOIN
組み立て・向きの扱い・`LEAST/GREATEST`の合成・ソースごとのステージング・タイルSQLの3分岐が
ここに集まる。20〜30%減と見込んだが根拠は「何が減るか」の列挙だけで、±300行は動く。
テストの10〜15%も同様。

**この数字は段階Eの実測まで変えない。**変えるときは理由を書く（[T956](T956.md)で削減行数が
4回変わった原因は、母集団を決めずに数字を言ったことだった）。

**この取り決めを破っている。** 上の表のうちテストの行（1,600〜2,400、根拠の無い「10〜15%」）を、
段階Aの途中で実測した実装:テスト＝1:1.17へ差し替え、純減の見込みを
**約9,800〜11,100行**へ改めた（「テストの減りは実装に比例させる（実測した比率）」節）。
理由は書いたが、段階Eまで変えないと決めたものを段階Aで変えた。**現在の見積もりは
9,800〜11,100行**であり、上の表の6,000〜8,000は差し替え前の値である。

## 段階A の実施記録

### `PARTITION BY LIST` はORMで表せる（確認済み）

SQLAlchemy 2.0.36 で `__table_args__ = {"postgresql_partition_by": "LIST (source)"}` が
`CREATE TABLE ... PARTITION BY LIST (source)` を生成する。**あるべき姿の宣言はORMモデル
1か所で足り、スキーマの宣言が割れない**（決定9の前提が成立した）。

子パーティションはこの宣言に含まれない。どのソースが在るかはデータで決まるので、
**取込の側が作る**のが正しい。

### 作ったもの

| | |
|---|---|
| `app/infrastructure/source_models.py` | `source_runs`・`source_features`（`source`でリスト・パーティション） |
| `backend/scripts/schema_gap.py` | **実DBとORM宣言のGAPを出す**。表・列・NULL許容・外部キーを比べる |

### GAP測定が実測を再現した

本番に対して実行した結果:

```
GAP 12 件
  derived_data_meta / schema_migrations       実DBにあるがORMが宣言していない表
  source_features / source_runs               ORMが宣言しているが実DBに無い表（今回追加した分）
  designation_attributes                      外部キー source_osm_import_run_id->osm_import_runs.id
  edge_attribute_counts                       同 source_accident_import_run_id / source_osm_import_run_id
  edge_landcover                              同 source_osm_import_run_id
  way_attribute_counts                        同 source_accident_import_run_id / source_osm_import_run_id
  way_divided_carriageway                     同 source_osm_import_run_id
  way_landcover                               同 source_osm_import_run_id
```

**訂正**: 本文のいくつかで「外部キーが9本」と書いていたが、正しくは**8本**
（designation 1／edge_attribute_counts 2／edge_landcover 1／way_attribute_counts 2／
way_divided_carriageway 1／way_landcover 1）。最初の測定で表ごとの差分を足すときに
誤った。

### 進め方を変えた（本番停止を前提に）

本番利用が一律停止されたため、本番への影響を考える必要が無くなった。

| | 変更前 | 変更後 |
|---|---|---|
| 本番の扱い | 段階Dまで触らない | **いつでも何度でも作り直してよい** |
| 切り替えの段取り | キャッシュを温めてから世代を切る | この作業では使わない（設計としては決定6に残す） |
| 反復のループ | 使い捨てDB | **東京都心の小bbox**（取込547.9秒＋派生196.4秒で1周） |
| 関東全域 | 段階D | **最後に1回**。途中で通す必要が無い |

### 所要の見積もり（根拠: [T956](T956.md)の実績）

T956は 09-19 19:15 〜 09-20 05:29 の**10時間14分**で、触れた行 6,706行（追加2,266／
削除4,440）・53ファイル・正味−2,174行。T970で触れる見込みは約35,000行で**約5.2倍**、
同じペースなら**約53時間**。幅を持たせて **40〜60時間**。

| 段階 | 人の作業 | 機械の時間 |
|---|---|---|
| A ORMモデル・パーティション確認・GAP測定 | 4〜6時間 | — |
| B 取込の一本化とアダプタ | 7〜10時間 | 小bbox 数分 |
| C 派生と読み出し側（`road_graph_repository` 3,344行とテスト16,000行が主戦場） | 25〜35時間 | 小bbox 数分 |
| D 関東全域を作り直す | 1〜2時間 | **15〜20時間**（夜間に回す） |
| E 撤去と検査 | 4〜6時間 | — |

段階ごとの按分は触れる行の比率で割ったもので、**按分自体は実測ではない**。T956は削除が
主で目標が明確だったが、T970は新しい構造を設計しながら作るため行あたりは遅くなるはず。
T956の10時間には私の間違いと手戻りが相当含まれており、今回も同程度と見ている。

### テストの減りは実装に比例させる（実測した比率）

```
backend/app 33,844行 / backend/tests 39,705行 → 実装:テスト = 1 : 1.17
```

当初「テストは10〜15%減」としたのは根拠の無い数字だった。比率を当てると:

| | |
|---|---|
| 実装の純減 | 約 4,500〜5,100 |
| テストの純減（×1.17） | 約 5,300〜6,000 |
| **合計の純減** | **約 9,800〜11,100行**（backend 73,549行の13〜15%） |

テスト量の多くが今回消す重複（表ごとのフィクスチャ・バッチごとの足場・fwd/bwdの両方を
用意するテスト・交差点の2定義）から来ているため、比例以上に減る可能性もあるが、
主張の根拠が列挙だけなので実測した1:1.17をそのまま当てる。

### 検知器 `orm_migration_column_drift` を先に撤去した

段階Eの予定だったが、**CIを18時間赤にしていた**ので前倒しした（プローブと外縁のケースが
無いまま`DETECTOR_ENFORCEMENT`へ登録されており、メタテストが落ちていた）。

穴を宣言して緑に戻す案もあったが、**それは本質の赤を隠す**——この検知器が守るはずだった
ずれ（fresh bootstrapと本番でスキーマが違う）は実際に起きていて、列名しか比べないため
捕まえられていなかった。主題（ORM ↔ migration の2つの作り方）はmigrationの撤去で消えるので、
検知器ごと消すのが筋。

撤去したのは検知器本体と、**それだけが使っていたヘルパ**（`migration_table_columns`・
`orm_table_columns`とDDLを読む正規表現一式。いずれも「定義＋使用1箇所」で他から使われて
いないことを確認した）。`scripts/review_checks.py` 4,524 → 4,409行（−115）。

代わりは`backend/scripts/schema_gap.py`（**ORM宣言 ↔ 実DB**）。比べる相手が違う別物で、
置き換えではなく、守っていなかった側を守る。

### 大改修中に検知器を止めるかどうか

**止めない。**空振りするのは「主題ごと消える予定のもの」だけで、それは前倒しで消せる。
他（死んだ参照・重複定義・引数の食い違い・空母集団のテスト）は大改修中こそ効く。全部
止めると、段階Dで大量の違反が一度に積み上がり、いちばん対処しづらい時点で向き合うことに
なる。空振りするものが出たら、そのつど「主題が消えるのか」を見て判断する。

## 段階B の実施記録（途中）

### 作ったもの

| | 役割 |
|---|---|
| `app/batch/source_profile.yaml` | **取り込む母集団の宣言はここだけ**が持つ。全ソース共通の`target`と、ソースごとの絞り込み |
| `app/batch/source_profile.py` | その読み込み。形式不正は即座に落とす |
| `app/batch/ingest.py` | **共通の取込経路**。ステージング・差し替え・run記録・パーティションの選択を引き受ける |
| `app/batch/source_adapters/` | アダプタ。**外部の形を開いて1件ずつ返すことだけ**を行う |
| `app/batch/ingest_cli.py` | 入口。`--source`／`--all` |

アダプタが返すのは`SourceRecord(natural_key, geom_wkb, attrs, payload)`だけ。ジオメトリを
WKBで受けることで、点・線・面を同じ経路へ載せて形ごとに書き込みを分けずに済む。

### 事故の絞りを都道府県からbboxへ変えた

警察庁の採番とJIS X 0401は別物で、行政区画で絞るには対応表が要る。母集団の指定としては
bboxで足りるうえ、ラスタ系と同じ概念になる。KSJ（指定路線）は配布がJISコードの
都道府県別ファイルなので、そちらはJISのまま——**プロファイルもJISで書くので変換が要らない**。

### 端から端まで通した（開発DB、2024年）

```
108,409件 / 52.1秒 / 68列すべてattrsへ
1件目: natural_key=2024-25-059-0007  POINT(140.814 37.030)  attrsのキー68
run: succeeded {"records": 108409, "elapsed_seconds": 52.1}
```

旧`accident_points`は関東7都県で年約101,000件なので、bboxのぶん少し多いのは想定どおり。

### 見積もりとのずれ（先に報告する）

| | 見込み | 実測 |
|---|---|---|
| 1行あたり | 1,192 バイト | **1,487 バイト** |
| 関東3年（303,455行）換算 | 345 MB | **430 MB** |
| 全国換算（面積比10.4倍） | 3.6 GB | **4.5 GB** |

**25%の過小**だった。原因は、開発DBの一時表でJSONB列だけを測り、行全体のオーバーヘッド
（`source`・`natural_key`・`run_id`・`geom`と主キー索引13MB）を数え落としたこと。JSONB
自体は1,105バイトで先の測定（1,106）と一致しており、外したのは周りだった。

全国約74GBの見込みに対して+0.9GB（1.2%）なので結論は変わらないが、数字は直す。

### 標高タイルも通した（開発DB、都心の小bbox）

```
20枚 / 2.6秒（同時8接続）
payload 131,072バイト = 256×256×int16
1行あたり 58,163バイト（TOASTが56%圧縮）
attrs: {product, z, x, y, width, height, dtype: int16_le, scale: 10, nodata, missing}
```

**面が点・線と同じ骨格に乗ることを、実物で確認した。**読み手が形を推測しなくて済むよう、
どう読むか（幅・高さ・型・尺度・欠測値）は`attrs`が持つ。

#### 容量の見込みを直す

| | 前の見込み | 実測からの試算 |
|---|---|---|
| 1タイルあたり | 68.5 KB | **58.2 KB**（測定） |
| 関東の対象タイル | 29,955枚（道路が触れるもの） | **59,540枚**（bbox全体） |
| 関東の標高 | 約 2.0 GB | **最大 3.46 GB** |

増えたのは1枚あたりではなく**母集団**で、「道路が触れるタイルだけ」から「bbox全体」へ
変えたため。生データの層が派生（道路網）に依存しない形を採った結果で、これは正しい。

ただし**上限**である——bboxの内側には海が相当あり、整備区域外のタイルは配信元が404を返して
そのまま落ちる。関東bboxは約57,000km²で、陸は実測36,300km²（64%）なので2.2GB前後に
収まる見込みだが、**実際の枚数は通してみないと分からない**。

全国（面積比10.4倍）は最大36GB・見込み23GB前後で、**先に出した約74GBの見込みへ
10〜25GB上乗せになる**。147GiBに対しては収まるが、数字は段階Dで測り直す。

### 点・線・面が1本の経路を通ることを、実物で確認した

| ソース | 形 | 結果（開発DB） |
|---|---|---|
| 事故（警察庁CSV） | 点 | 108,409件 / 52.1秒 / 68列すべて保持 |
| 指定路線（KSJ ZIP+GML/GeoJSON） | 線 | 東京都で1,210本 / 1.2秒 / すべてLINESTRING（頂点2〜1,315） |
| 標高（地理院タイル） | 面 | 20枚 / 2.6秒 / payload 131,072バイト |

**3つとも同じ`ingest.py`を通り、違うのはアダプタだけ。**ソースごとのステージングDDL・
MERGE SQL・run記録は1つも書いていない。

指定路線のアダプタは、ZIPの中身の読み方（N10はJPGIS/GML、N12は素のGeoJSON）を
`import_designations.py`の既存関数から使う——同じ外部仕様を2度読み解かない。

### OSMも通した（Tokyo.osm.pbf、都心のbbox）

```
osm_way : 12,029件 / 146秒 / 1wayあたりタグ 6.77個 / 6,224 kB
osm_node: 55,564件 / 171秒 / うちタグを持つもの 14,768件 / 12 MB
```

**タグ全保持の実測値が取れた。1wayあたり6.77個**——Overpassの標本から外挿した2.9個
（`highway`・`surface`を除いた値）より多い。除いた2つを戻しても4.4個ほどで、残りは
都心の付けられ方が濃いぶん。**関東全域の実測は段階Dで取る**（ここから外挿しない）。

ノードはタグの有無で分けずに全部入れる。タグを持つのは14,768/55,564で、残りは形状の
頂点。POIかどうかの判定は派生側へ移る。

#### 分かったこと

- **pyosmiumは非ASCIIを含む絶対パスのPBFを開けない**。現在位置からの相対パスで渡す
- way・nodeで**PBFを2回読む**（共通の経路が1ソースずつ処理するため）。Tokyo抽出で
  146秒＋171秒。関東（487MB、6.2倍）なら約33分で、取込は年に数回なので許容する。
  1回の読みで2ソースぶんを作る形にすると、経路の一様さを崩すことになる

### 段階B 完了——6ソースすべてが1本の経路を通る

| ソース | 形 | 結果（開発DB、都心のbbox） | 1行あたり |
|---|---|---|---|
| 事故（警察庁CSV） | 点 | 108,409件 / 52.1秒 | 1,487 B |
| 指定路線（KSJ ZIP） | 線 | 1,210本 / 1.2秒 | — |
| OSM way | 線 | 12,029件 / 146秒 / タグ6.77個 | — |
| OSM node | 点 | 55,564件 / 171秒 / タグ持ち14,768 | — |
| 標高（地理院タイル） | 面 | 20枚 / 2.6秒 | 58,163 B |
| 土地被覆（GeoTIFF→タイル） | 面 | 30枚 / 1.1秒 | 約 5,000 B |
| 基礎地図（ベクタタイル） | 面 | 30枚 / 2.8秒 | — |

**ソースごとのステージングDDL・MERGE SQL・run記録は1つも書いていない。**

土地被覆は`landcover_raster.py`の`render_tile`を`tile_classes`（クラス配列）とPNG化へ
分け、取込は配列側を使う——同じ読み取りを2度書かない。

#### 基礎地図の見積もりを直す（母集団から一様に測り直した）

先に「関東z6〜14で0.36 GB」と出したのは、各ズーム4枚ずつの標本で**偏っていた**。
都心の1枚は624 KBあり、65倍違う。母集団（関東bboxのz14、15,065枚）から一様に60枚を
取り直した:

```
平均 40.7 KB / 中央値 10.9 KB / 最小 0.1 KB / 最大 404.5 KB
関東z14  0.58 GB      全国（面積比10.4倍）  6.1 GB
```

**分布の歪みが大きい**（中央値の3.7倍が平均）ため、60枚の標本でも幅は残る。

**訂正（2026-09-20）**: ここで「基礎地図はDBに持ってよい」と結論したのは誤り。容量ではなく
**読み手が1つも無かった**（`source = 'basemap'` を参照する実装は0件）。配信は
`/api/basemap/{path}` のプロキシが `tile_cache`（ファイルシステム）経由で行っており、DBの
複製はどこからも読まれていなかった。決定1の条件1（週次で焼き直される）と条件3（データ源は
OSMで、そのOSMを別に持っている）も満たしていない。取込は削除した（[T976](T976.md)）。

なお同日、この0.58 GBを忘れて開発DBの都心30枚（614 KB/枚）から8.8 GBと出し直した。
**この節が「都心の標本は偏っている」と既に書いていたのに、同じ偏りを繰り返した。**
正しい見積もりは0.58 GB。

土地被覆は逆に小さかった——クラス番号はよく圧縮され、1タイル65,536バイトが約5,000
バイトになる。**関東z14で約75 MB**（見込み120 MB）。

### 設計の訂正: 逆向きの方位は「+180°」ではない

区間を1本1行にする根拠として「方位は+180°で導ける」と書いたが、既存の`build_road_graph`は
**そうしていない**——「+180度の単純反転ではなく、逆順の始点・終点から都度求める。短い
区間ではほぼ等価だが、地球の丸みを近似せず素直に正しい値を使う」。

形状を保存するので導出できること自体は変わらないが、**導き方が違う**。読み出し側は
逆順の始点・終点から求め直す。

### 段階C（途中）: 生データから道路網の形を導けた

`app/batch/derive_topology.py` を作り、開発DBで通した。切る位置は「2本以上の道が通る
ノード」で、`osm_way`の参照ノード列だけから決まる——形は他の派生に依存しない。

```
入力: way 12,029件 / node 55,564件（Tokyo抽出・都心bbox）
出力: 区間 23,322本 / ノード 15,980点 / 4.7秒
```

**1.94区間/way**で、本番の実測（2,611,210区間 / 1,329,082way ＝ 1.96）とほぼ一致する。
分割の規則が合っている裏付け。

| | 実測 |
|---|---|
| `road_edges` | 23,322行 / 5,496 kB / **241バイト/行** / 平均65.6m / 3.81点 |
| `node_materials` | 15,980点 / うち枝3本以上 11,955 / 平均2.92 / 最大7 |
| `edge_materials` | 23,322行（器だけ。値は全てNULL＝未計算） |

**241バイト/行**は、旧`road_edges`の734バイト/行（3,719MB ÷ 5,065,850行）に対して67%減。
行数も半分になるので、way1本あたりでは 2,789 → 468バイト。見込み（−66%）とよく合う。
ただしこれは都心の抽出で、関東全域の値は段階Dで測る。

#### 直したもの

`node_materials`の既定値をORMの`default=`からDB側の`server_default`へ移した。ORMの
`default=`はPython経由の挿入にしか効かず、取込や導出が生SQLで書く行には適用されない
（実際にNOT NULL違反で落ちた）。**制約はDBが持つ**という決定どおりの形になった。

### 段階C（途中）: ノードに付く値を埋めた

`app/batch/derive_node_materials.py`。**種別は取込ではなくここで付ける**——取込は外部の
タグをそのまま入れるだけで、「これはPOIか」の判断は派生の側。判断が変わったら生データを
取り直さずにここを流し直せばよい。

```
タグを持つノード 14,768点 → 種別が付いた 13,585点 / 信号 5,820点 / 3.0秒
内訳: crossing 9,179 / traffic_signals 2,794 / stop 1,490 / level_crossing 62 / barrier 57 …
結果: 27,427点（うち信号が近い 10,896 / 階級が付く 15,078 / 枝3本以上 11,955）
```

#### 空間の索引で107秒→3.0秒

`ST_DWithin(a::geography, b::geography, m)`は**geometryのGiSTを使えない**。`&&`と
`ST_Expand`の前置フィルタを先に置いて矩形で絞ると、107秒が3.0秒になった（35倍）。
既存の`_RECOMPUTE_NODE_TRAFFIC_SIGNALS_SQL`が同じ形にしていたのは、これが理由。

生データのパーティションには取込時にGiSTを張るようにした（`ensure_partition`）。生データは
範囲や近さで引かれる（事故を区間へ、信号を交差点へ割り当てる）ので、索引が要る。

#### 信号の判定を domain へ置いた

信号は`highway=traffic_signals`のほか**信号付きの横断歩道**としても書かれる
（`highway=crossing`＋`crossing`の値に`signals`を含む）。片方だけ見ると大半を取りこぼす。
判断なので`domain/traffic.py: is_traffic_signal`へ置き、バッチはそれを呼ぶ。

## 要確認: 停止要因を区間へ割り当てる規則が、向きの廃止で変わる

**ユーザーの確認が要る判断**。実装は進めるが、較正に影響するので明示しておく。

旧実装の規則（`_POI_COUNTS_BODY`のコメント）:

1. **そのwayの構成ノードだけ**を対象にする（距離だけで拾うと、交差する別の道の信号まで数える）
2. **区間の始点にあるノードを除く**——交差点のノードは「入る区間」と「出る区間」の両方が
   持つため、両方が数えると1回の停止が2回になる。「止まるのは区間を走り終えるところなので、
   到着側（終点側）の区間が持つ。**有向Edgeのため逆向きの区間でも同じ規則が成り立つ**」
3. 同じ種別の点を40mでまとめてから数える（日本のOSMは1つの信号交差点を流入路ごとの
   複数ノードで描く）

**規則2は有向Edgeであることに依存している。**区間を1本1行にすると、A→Bと走るときは
Aが交差点Jを持つが、B→Aと走るときはBの終点はJではないので数えられない。

### 採る規則: 端点を半分ずつ持つ

```
区間の停止要因 = 0.5 × (始点の停止要因) + 0.5 × (終点の停止要因)
```

経路上の中間ノードは前後の区間が0.5ずつ持ち、合計1回になる。**向きに依らない**。
経路の端のノードだけ0.5になるが、経路長に対して無視できる。

### 影響

**値が旧実装と変わる。**軸の較正（`axis_definitions.shape_params`のbreakpoint）は旧実装の
分布に対して調整されているため、そのままでは効きがずれる。段階Dの後に分布を測り直して、
較正を見直す必要がある。

規則1（そのwayのノードだけ）と規則3（40mのまとめ）は変えない。

### 段階C（途中）: 数の値を埋めた——地図と評価が構造として一致する

`app/batch/derive_counts.py`。事故・停止要因・交差点を区間へ入れ、**道の値は区間の値を
足し上げて作る**。

```
区間 23,322本 / まとめ後の停止要因 5,031点 / 4.2秒
信号 2,963 / 横断歩道 454 / 一時停止 24 / 事故 4,803.0 / 交差点 22,318
（事故は死亡を重く見た重み付き合計。先に 14,265 と記録したのは死者数の判定が
`(attrs->>'死者数') <> '0'` で、値が `'000'` のゼロ詰めのため全件に一致していた誤り。
生データ108,409件のうち死亡は773件）

区間の和 = 道の和（信号 2,963.0 = 2,963.0）
```

**同じ数え方から作るので、地図と評価が食い違わない。**先に見つけた「交差点の定義が2つ
あって26,512箇所ずれる」問題は、way粒度を区間から導くことで構造的に消える。

停止要因の割り当ては端点を0.5ずつ（上の「要確認」節）。交差点も同じ。

### 段階C（途中）: 面のデータを線へ落とした

面の生データ（標高・土地被覆）は`source_features`にタイル1枚=1行で入っている。これを
読んで区間の値にする。**面のまま持つ派生表は作らない**——面を読む出口は「そのまま
見せる」か「線へ落とす」のどちらかで、面のままの中間結果を要る相手がいない。

値の出し方はdomainが持つ。`compute_elevation_attribute`から値の計算だけを
`compute_elevation_values`として切り出し、バッチはそれを呼ぶ。土地被覆は
`class_percentages`をそのまま使う。バッチ側に勾配の上限（40%）も有効画素数の下限（20）も
無い。

土地被覆の列の対応は`LandcoverPercentages`の項目名から導く（`lc_` + `*_percent`の前半）。
クラスが増えてもバッチは変わらない。

開発DB（東京都心、区間23,322本）の実測:

| | 対象 | タイル | 所要 |
|---|---|---|---|
| 標高 | 23,322本すべて | 110枚 | 2.1秒 |
| 土地被覆 | 23,322本すべて | 36枚 | 19.9秒 |

- 標高 -2.1〜42.9m、平均17.3m（都心として妥当）
- 割合の合計が100%から0.5%以上ずれる区間は0本
- 橋・トンネルの500本は`average_grade`を持たない（地表面の標高は道の勾配ではない）
- `shapely.prepare`の有無で127.3秒→19.9秒。帯1つに何百点も当てるため効く

#### 取込CLIへ`--profile`を足した

狭い範囲で試すときは別のプロファイルファイルを指す。**引数で範囲を上書きできるように
はしない**——宣言がファイルの外へ散り、「どの範囲を取り込んだのか」がプロファイルを
読んでも分からなくなる。

### 段階C（途中）: 道1本の性質を埋めた

通行方向（`oneway`・`oneway:bicycle`・`junction`からの解決）は、Pythonの1実装
（`resolve_direction`）だけが持つ。上下線分離の判定は相方の向きも見るため、いったん
一時表へ出してからSQLへ渡す——SQLの中で同じ規則を書き直さない。

しきい値は`domain/divided_carriageway.py`へ移した。`road_graph_repository.py`が
持っていたが、判定のパラメータであってリポジトリの都合ではない。

#### 指定路線の列名を決定4から変えた

`designation_emergency`／`designation_logistics`ではなく、`designation_` + 種別名
（`designation_emergency_transport`／`designation_critical_logistics`）にした。種別が
増えたときに列名が機械的に決まり、バッチに対応表が要らない。決定4のSQLは種別名の短縮形を
使っており、3種目を足すときに短縮形を人が決める必要があった。

開発DB（東京都心、道12,029本）の実測:

- 通行方向: forward 5,110 / both 6,918 / backward 1
- 上下線分離: 2,595本（一方通行5,111本の50.8%）。0.8秒。名前の付いた該当例は
  昭和通り・永代通り・内堀通り・表参道・江戸通りで、いずれも実際に上下線が分かれている
- 指定路線: 緊急輸送道路 3,719本 / 重要物流道路 1,110本。5.7秒

### 段階C: 派生の全列が埋まった。入口は1つ

`derive_cli.py`が段の順番（形→ノード→数→面→道）を持つ。飛ばせない依存があり、
覚えていないと部分的に古い派生ができる。

まっさらから端まで**55.8秒**（topology 6.5 / nodes 4.6 / counts 6.3 / raster 31.4 /
ways 7.0）。2回流して同じ値になることも確認した。`edge_materials`・`way_materials`・
`node_materials`のどの列にも未計算（NULL）は残っていない。

これで**生データ→派生の側は端まで通った**。残りは読み出し側の移行。
