# T557: T531（フロンティア方式）の`/code-review`指摘の是正（上位10件＋P3相当10件）

状態: 完了（2026-09-03起票・同日完了。上位10件のうち4件[3・6・8・17]を理由付きで見送り、
残り16件[P1×2・P2×6・P3×8]を対応。[T531](T531.md)完了直後にユーザーが起動した`/code-review`
（high effort）の結果。レビュー記録は
[.claude/commands/review/history/2026-09-03_codereview.md](../../.claude/commands/review/history/2026-09-03_codereview.md)）

## 背景

T531の一連コミット（`5b408c4`〜`7fbf04d`）を対象に8角度のレビューを行い、正しさ2件（P1）・
構造/効率/規約8件（P2）・小規模な整理10件（P3）を検出した。P1の2件はいずれも「新しい
入力の組合せ」（モード切替後のstate残り、短距離＋フロント固定の許容5km）に対する境界の
見落としで、既存テストは周回モード・20〜30kmの正常系しか固定していない。

## 対応方針

上位10件（レビュー記録のFindings節と同じ順）:

- [x] 1. **[P1] 目的地モードで未検証の`max_routes`を常時送信**（`frontend/src/app/page.tsx:1402`）:
  目的地モードでは`max_routes`を送らない（backend既定8）か、送信前に`maxRoutesInput`を検証
  する。`page.test.tsx`へ「周回で空にして目的地モードへ切替→生成が422にならない」を追加。
- [x] 2. **[P1] 許容≥目標距離でリング下限が負、短距離で極端に短い周回が上位**
  （`backend/app/services/road_graph_engine.py:736`）: `ring_lower_m`を0でクランプし、
  `ring_center_m`を`目標/中央比`（(2.0+2.3)/2）で決める。狭い許容での反転フォールバックも
  同じ中心を使う。テスト: 距離1km・許容5kmで往路の中央値が目標の半分付近になること。
- [ ] 3. **見送り（T552へ）**: **[P2] 全軸データ欠損Edgeが往路ランキングで難易度0扱い**
  （同`:765`）: [T552](T552.md)（コスト補完）と併せて、ランキング側で「木の実距離に対する
  NaN区間の距離割合」を第2キー以前に扱う（欠損割合の高い折返し点を後ろへ）。T552の実態
  計測（欠損比率）を先に行う必要があり、T552自体が今回のtask:next選定で未選択のため本
  タスクの範囲外。T552完了後に着手する。
- [x] 4. **[P2] 再split後のキャッシュ済み`lazy_graph`と`graph`のずれでKeyError**（同`:578`、
  `domain/routing.py:257`、同`:866`）: `.get()`で欠損を検知したらタイル集合の
  `search_graph_cache`エントリを破棄して再構築する。`save_graph`完了時に該当タイル集合を
  無効化する方が根本的だが、`GraphService`側の変更になるため規模を見て判断。
- [x] 5. **[P2] docs/modulesの乖離・経緯記述**（`docs/modules/backend/routing-engine.md:177,183,215`）:
  リング定義を`[(目標−許容)/2.0, (目標+許容)/2.3]`（反転時は対称化）・タイブレークを
  「リング中心に近い順」へ、215行の経緯文を削除。あわせて`select_loop_turnarounds`節の
  手順列挙（実装docstringの写し）を1〜2文へ圧縮する。
- [ ] 6. **見送り（[T568](T568.md)へ切り出し）**: **[P2] `SearchGraphStatics`のメモリ
  （最大約1.2GB）と`masks`（22MB×2/リクエスト）**（`backend/app/infrastructure/
  search_graph_cache.py:51`、`domain/routing.py:217,409`）: `entry_keys`をフィールドから
  外し`_accumulate_tree_lengths`で都度生成、`indptr`/`indices`/`entry_edge_index`をint32へ、
  staticsの上限を`lazy_graph`と分離して小さく（例: 16）。`select_diverse_by_overlap`の
  `masks`は採用済み集合をuint64ビットマスク1本（Edge数×8B）で持ち2パスで共有
  （`TURNAROUND_POOL_MAX=40 ≤ 64`）。CSRのdtype変更・キャッシュ上限の分離・
  間引きアルゴリズムのビットマスク化という3つの独立した変更を含み、いずれも本タスクで
  既に2回改修した同じホットパス（`select_loop_turnarounds`/`select_diverse_by_overlap`、
  項目10・15）に触れる。正しさより性能が目的の変更を同じコミットへ積み増すとレビュー・
  検証の焦点がぼやけるため、専用タスクとして分離することを推奨し本タスクでは見送る。
- [x] 7. **[P2] 死んだassert**（`backend/tests/test_road_graph_engine.py:369`）:
  `assert "route-180" not in [c.id …]`を`compass_label(180) not in [c.direction_label …]`へ。
- [ ] 8. **見送り（T553へ）**: **[P2] 間引きが往路のみ比較のため復路が収束するとほぼ同一の
  周回が並ぶ**（同`:803`）: [T553](T553.md)の周回単位重複率チェックを「復路同士・往路と
  他候補の復路」も含む進行方向無視のEdge集合比較へ広げる（T553側の対応方針へ追記済み）。
  T553自体が今回のtask:next選定で未選択のため本タスクの範囲外。T553完了後に着手する。
- [x] 9. **[P2] 重複実装**（`road_graph_engine.py:1196,891`）: `_origin_estimate_fn`は
  `_build_estimate_cost_fn(context.graph, node_lat, node_lon, origin_node)`の結果を
  `context.origin_estimate`へ保持する形へ。`return_edge_indices`は`domain/routing.py`へ
  `path_to_edge_indices_lazy`（index列）を切り出し、`path_to_edge_ids_lazy`をそのラッパに
  して二重走査を無くす（`retrace_ratio`のDEBUGログは維持するならこの結果から算出）。
- [x] 10. **[P2] 毎リクエストの全Edge/全Node規模の不要なオブジェクト生成**（同`:436,773,789`）
  （int32配列化のみ見送り、理由は実装メモ参照）: `cost_list`は従来の内包（参照コピー）で作り
  `cost_array`は`np.array(cost_list)`（または項目12で`cost_array`自体を廃止）、
  `far_enough`の平面座標と`difficulty_by_node`は`ranked`（≤4,000）に絞る、
  `tree_path_edge_indices`はint32配列を返し`outbound_cache`は採用/再検査対象に限定する。

P3相当10件（同じコミットにまとめてよい）:

- [x] 11. `_accumulate_tree_lengths`の到達判定を`predecessor >= 0`ではなく`np.isfinite(cost)`
  から作る（固定版scipy 1.18.1では差が無いが、infコストEdgeの先に前任者を書く版への
  契約保証）。
- [x] 12. `_SearchGraph`/`_RoadGraphContext`の`cost_array`を廃止し`cost_list`を
  `build_shortest_path_tree`へ渡す（内部で`np.asarray`済み）。
- [x] 13. 未使用フィールドの撤去（`_TurnaroundData.node_index`のみ撤去、
  `outbound_distance_km`は撤去し`outbound_difficulty`は維持。理由は実装メモ参照）:
  `_TurnaroundData.node_index`、`LoopTurnaround.outbound_distance_km`/`outbound_difficulty`
  （戦略層は読まない。テストは`select_loop_turnarounds`の戻り順序で同じ意図を検証する）。
- [x] 14. `ShortestPathTree._predecessor_list`の遅延キャッシュを`predecessor_list`の通常
  フィールド（構築時に`tolist()`）へ。
- [x] 15. `select_diverse_by_overlap`の`initial_selected`/`rejected_by_overlap`を、閾値列
  `max_overlap_ratios: Sequence[float]`を受けて関数内で緩和ループする形へ（呼び出し側の
  2回呼びを1回に）。
- [x] 16. `overlap_ratio`（DEBUGログ専用）と間引き本体の重複率定義を一本化する（ログ用途を
  やめるか、マスク版を`overlap_ratio`のbulk版として同じ定義の下に置く）
  （ドキュメント上の紐付けのみ、理由は実装メモ参照）。
- [ ] 17. **見送り（[T569](T569.md)へ切り出し）**: `preview_segment`で`SearchGraphStatics`を構築・キャッシュしない
  （`prepare`側だけで`_get_or_build_search_statics`を呼ぶ）。着手中に発見: 項目4で追加した
  キャッシュ不整合検知（`LazyGraphEdgeMismatchError`）は`build_search_graph_statics`の
  内部に埋め込まれており、`_build_search_graph`が両エンドポイント共通でこれを呼ぶことで
  `preview_segment`側も副次的にこの保護を受けている。項目17どおりpreview_segmentから
  statics構築を外すと、この保護も一緒に失われ`preview_segment`が同種のKeyError相当に
  再び晒される。安全に分離するには、lazy_graphの整合性チェック自体をstatics構築から
  独立させる別の構造変更が必要で、当初想定より規模が大きいため見送る。
- [x] 18. `search_graph_cache.py`のLRU get/set 3組を小さな内部クラスへ共通化（`clear()`・
  `*_cache_size()`も含む）。
- [x] 19. `benchmarks/bench_t531_route_generation.py`と`bench_t536_route_generation.py`の
  サービス配線を共通ヘルパーへ寄せ、splitガード（読み取り経路の確認）を両方に適用する。
  `bench_nearest_node.py`の「1リクエストあたり最近傍探索17回」の旧8方位前提コメントを現状
  （`prepare`の1回）へ。
- [x] 20. `tests/test_routing.py`の`_length_array`を`build_search_graph_statics(...).edge_length_m`
  に置き換える。

## 完了条件

- 上記20項目のチェックが埋まる（見送る項目は理由を書いて残す）。
- 新設テスト: 目的地モード切替後の生成（項目1）、距離1km・許容5kmのリング（項目2）、
  キャッシュ不整合時の再構築（項目4）。
- `pytest -q -m "not postgis"`・frontend `vitest`/`tsc`/`eslint` green。項目6・10の効果は
  `benchmarks/bench_t531_route_generation.py`（dev DB）で`select_ms`とRSSの前後を記録。
- docs/modules（項目5）の更新を同一コミットで行い、CLAUDE.md「完了扱い前の機械的チェック」を実施。

## 実装（2026-09-03）

**環境上の制約**: 本セッションのサンドボックスは実PyPI・実DB（PostGIS）に接続できるが、
`backend/requirements.txt`が指定する`numpy==2.5.2`/`scipy==1.18.1`はサンドボックスの
実行時点でまだ実在しないバージョンだったため、検証用に`numpy==2.4.6`/`scipy==1.17.1`
（同系列の最新実在版）で一時的な`.venv`を作成して使用した（`requirements.txt`自体は
変更していない）。また`ridecompass_test`のようなPostGIS実DBも本セッションには無いため、
`pytest -m "not postgis"`のみ実行し、postgis統合テスト・本番/dev DBでのベンチマーク実測
（`bench_t531_route_generation.py`の`select_ms`/RSS計測）はユーザー側での実施が必要。

**対応方針**: 上位10件のうち項目3・8はそれぞれ[T552](T552.md)・[T553](T553.md)への依存
（いずれも今回のtask:next選定でユーザーが選ばなかったタスク）のため範囲外として見送り
（両タスクのファイルには本タスクの指摘が既に追記済みのため、新規タスクの起票はしていない）、
項目6は独立した性能特化の大きめの変更（CSR dtype変更・キャッシュ上限分離・間引き
アルゴリズムのビットマスク化の3点）を含み、本タスクで既に2回改修した同じホットパスへ
さらに手を入れるためレビュー・検証の焦点を保つ目的で[T568](T568.md)として切り出して見送った。
項目17は着手中に、項目4で追加したキャッシュ不整合検知が`SearchGraphStatics`構築処理に
埋め込まれておりpreview_segment経路もその副次的保護を受けていることが判明し、安全な
分離には想定より大きい構造変更が要るため[T569](T569.md)として切り出して見送った。
残り16件（P1×2・P2×6のうち4件・P3×10のうち8件）を対応。

**検証**:
- `pytest -q -m "not postgis"`（`tests/test_import_pbf.py`・`test_pbf_source.py`は
  `osmium`未インストールのため実行不可、対象外。以下同じ）: 1336件green
  （項目1〜20対応前の同条件ベースラインも1334〜1336件greenで、対応の過程で追加した
  新規テスト分だけ件数が増えている）。
- frontend: `tsc --noEmit`エラー0（`next typegen`実行後）、`eslint .`警告・エラー0、
  `vitest run`943件green（全101ファイル）。
- `python scripts/review_checks.py docs`: 記載粒度違反0件・死んだ参照0件
  （項目5のdocs更新に加え、`LoopTurnaround.outbound_distance_km`撤去[項目13]に伴う
  routing-engine.md 82行目の死んだ参照も精読で発見・是正した）。
- 項目2・4はそれぞれ専用の回帰テストを追加し、修正前のコードへ一時的に戻すと
  実際に失敗する（項目2は誤ったノードを選ぶ、項目4は元のKeyErrorをそのまま再現する）
  ことを確認してから元に戻した。
