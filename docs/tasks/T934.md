# T934: 信号の点が、1交差点あたり2.58個のまま地図へ出ている

状態: 未着手（2026-09-18起票）

## 背景

停止密度を数える側は`ST_ClusterDBSCAN`で信号を1交差点へ畳んでいるのに、地図タイルを焼く
側（`_POI_TILE_MVT_SQL`）は`osm_raw_pois`を1ノード1点で素通ししている。本番実測で
実質的な信号ノード130,716に対し40mでまとめた塊は50,760（1塊あたり2.58）。

詳細はこのファイルへ追記する。
