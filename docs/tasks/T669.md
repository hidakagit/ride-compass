# T669. architecture.mdの現状化（削除済みRedis機構ほか）

規模S〜M。

状態: 完了（2026-09-08起票、2026-09-08完了）

## 背景

統合レビュー第5回（[2026-09-08_all.md](../../.claude/commands/review/history/2026-09-08_all.md)）
が検出した指摘のうち、下表の4件を1つの起票単位として扱う。

T652が削除した機構を性能実測の根拠まで含めて現行として記述し続けている（約64行）。
「同一コミットで更新」ルールの現状文書としては最も重い乖離。

## 対象の指摘

| # | Sev | 対象 | 内容 |
|---|---|---|---|
| 6-1 | P1 | `docs/architecture.md:425-488`（約64行） | T652（`59dbce43`）が`road_graph_tile_cache.py`・`road_edge_geometry_cache.py`を削除したが、**同コミットはdocsを一切触っていない**。architecture.mdは削除済みの機構を性能実測の根拠まで含めて現行として記述し続けている。「同一コミッ |
| 6-2 | P2 | `docs/architecture.md:535,536,623,1132` | api層の記述に実装に存在しないシンボル・誤ったパスが4点（`get_route_generator`・`get_route_generation_builder`・`_tile_validation.check_tile_rate_limit`・`/api/accidents/tiles/`）。加えて`cache_p |
| 6-3 | P2 | `docs/architecture.md` | 未記載のエンドポイント2件（`/api/jma-tile-index`・`/api/debug/db-status`）。他28件は記載あり |
| 6-12 | P3 | `docs/architecture.md:509` | 「footway/pedestrian等は取込対象外」がT99の実装（自転車通行可のfootway/pathは取込）と食い違う |

各項目のEvidence・failure_scenario・Recommendationはレビュー結果の全件表を参照する。

## 着手時に決めること

- 上記のうちどれを実施し、どれを見送るか（見送る場合は理由を本ファイルへ残す）
- 実施順序（レビューのPhase 8は「検証の自動化→境界の固定→実害のある修正→
  現状文書の是正→内部の再配置」を提案している）

## 対応方針と実装

4件すべてを実施した（見送りなし）。

### 6-1 削除済みRedis機構の64行

[T652](T652.md)が削除した`road_graph_tile_cache.py`・`road_edge_geometry_cache.py`の記述
（性能実測の根拠を含む約64行）を、現在の設計判断へ差し替えた——「ルート生成の経路に
Redisを置かない」。T652が速度ではなく構造を理由に削除したこと（split鮮度が危険側の判断を
単調でないまま保持し、その正しさをfail-openのpush無効化に依存していた）を残し、実測値の
詳細はT652.mdへ委ねた。あわせて、プロセス内に残るタイル単位キャッシュ
（`graph_material_cache`・`tile_score_matrix_cache`）との線引きを明記した。

### 6-2 実在しないシンボル・列挙漏れ

| 記述 | 実装の正 |
|---|---|
| `get_route_generator` | 実在するDI工場の例（`get_graph_service`等）＋`open_route_generation_setup` |
| `get_route_generation_builder` | `open_route_generation_setup`（[T265](T265.md)で置換済み） |
| `_tile_validation.check_tile_rate_limit` | `validate_tile_coords`のみ（レート制限は`dependencies.py: enforce_rate_limit`が全router共通で担う） |
| `GET /api/accidents/tiles/…` | `GET /api/region/accident-tiles/…` |

列挙から欠けていた`cache_policy.py`・`derived_data_freshness.py`も追加した。

### 6-3 未記載エンドポイント2件

`GET /api/jma-tile-index`（在否インデックス）・`GET /api/debug/db-status`（HTTP Basic認可要）
をそれぞれ`jma_tile.py`・`health.py`の記述へ追加した。

### 6-12 取込スコープのfootway

`shared_pedestrian_ways`ルール（`highway=footway/path`かつ自転車通行可を別途拾う、
[T99](T99.md)）を反映した。「フロント凡例にfootwayが含まれるがタイルには現れない」という
続きの記述も、自転車通行可のものは現れるという実態へ直した。

## 追加で見つけた死んだリンク（レビュー指摘外）

`architecture.md`が言及する`backend/`・`frontend/`配下のパス61件の実在を機械的に確認し、
マークダウンリンクで404になる4件を解消した（`elevation_service.py`×2・
`openrouteservice_engine.py`・`wind_service.py`）。

このファイルはStep別の節（Step4・Step5・Step7…）が当時の記述として残る構成のため、
**内容の現状化ではなくリンクの解消に留めた**——現行の外部データ源一覧（表）だけは実態
（GSI DEMタイル＋`ElevationAttributeService`＋`elevation_aggregation.py`）へ書き換え、
Step別の節は「現在は〜へ再編済み」と添えてリンクを外した。バッククォート内で撤去済みと
明示している4件（`scoring.yaml`・`route_scorer.py`・`generate_axis_migration_sql.py`・
`safetyExpression.ts`）はリンクではないためそのまま残した。

## 検証

- `python scripts/review_checks.py docs` 違反なし
- 言及パスの実在チェック: マークダウンリンクの404が0件
