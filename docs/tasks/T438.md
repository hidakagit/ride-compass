# T438. 一般ユーザー向け画面の文言から「軸スタジオ」の露出を除去する 規模S

状態: 完了（2026-08-30）

- 背景: review:ui（2026-08-30、[history/2026-08-30_ui.md](../../.claude/commands/review/history/2026-08-30_ui.md) F-4）。
  `RouteAxisProfile`のヒント文言・`evaluationAxes.ts`の`difficulty_weight`説明文（`RouteList`の
  おすすめ度説明に組み込まれる）に、Basic認証必須の管理画面限定機能名「軸スタジオ」がそのまま
  露出しており、一般ユーザーが到達・理解できない固有名詞になっている。
- 対応: 該当2箇所の文言を変更。
  - `RouteAxisProfile.tsx:43`: 「軸スタジオの重みで自動追従」→「軸の重み付け設定に応じて
    自動的に更新されます」
  - `evaluationAxes.ts:37`（`difficulty_weight`の`description`）: 「軸スタジオの重みで合成した
    総合難易度が小さいほど高評価」→「各軸の重み付け設定で合成した総合難易度が小さいほど高評価」
- 検証: `RouteList.test.tsx`の既存アサーション（文言変更に伴い更新）に加え、両コンポーネントへ
  「軸スタジオ」という文字列を含まないことを検証する回帰テストを新規追加（`RouteList.test.tsx`・
  `RouteAxisProfile.test.tsx`各1件）。`vitest run`両ファイル12件成功、`tsc --noEmit`・`eslint`とも
  問題なし。

