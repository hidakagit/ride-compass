# T465. ゼロベース網羅レビュー§3(MapView.tsx)の残指摘6件対応

状態: 起票

- 背景: [T426](T426.md)棚卸し結果の§3（frontend/src/components/Map/MapView.tsx）、有効6件。
  元は[zero-base-review-2026-08-30.md](../zero-base-review-2026-08-30.md)の§3。
- 対象6件（severity・file:line・要旨。全てP2/P3。P1の2件[windAxisPenalties/
  windPenaltyFillの再適用漏れ・windPenaltyFillのクリックガード欠如]はT425で対応済み）:
  1. `clearRoadTileFeatureState`（L1141-1144） P2: `map.removeFeatureState`がkey指定
     なしで全キー削除。T444で風/勾配間の実害は解消したが、将来別のfeature-stateキーが
     同ソースに追加されたら巻き添えで消える構造自体は残存（関数コメントも自認）。
  2. `ensureDesignationLayer`/`ensureTunnelLayer`/`ensureOnewayLayer`（L1275-1351）P2:
     3関数がコピペで増殖（windAxis/gradientAxisはT440でファクトリ化済みだが対象外）。
  3. `handleMapError`（L2665-2685） P2: 基礎地図タイル更新失敗時、初回ロード成功後は
     `styleLoadFailed`バナーが出ない。
  4. `refreshToken`エフェクト（L3128-3149） P2: 短時間の連続変化時、複数`setStyle`
     呼び出しの重複に対するガードが無い。
  5. `applyAxisFeatureStateValues`（L1120-1128） P3: `way_id`ごとに`setFeatureState`を
     個別呼び出しし負荷が線形に増える。
  6. `buildStopPoiPopupHtml`/`buildSupplyPoiPopupHtml`（L1849-1866） P3: 型定義・関数
     本体がほぼ同一の2組が別々定義。
- 対応方針: 未検討。
