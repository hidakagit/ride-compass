# T946. 較正値（T805）の後始末 規模M

状態: 未着手（2026-09-19起票、統合レビュー第11回のP2/P3）

## 背景

T805が導入した較正値の仕組みのうち、宣言から導けていない箇所と、層構造の逸脱。
対象は[history/2026-09-19_all.md](../../.claude/commands/review/history/2026-09-19_all.md)「Findings（P2）」の「較正値（T805）」節（20〜24。25は[T936](T936.md)が扱う）。

- `routeSplice.ts`が「届かなかったときの既定」を**フロント側に独自に持つ**（下限なし＝0）。
  backend側の`tuning_value`はfail-fastで、ガードが左右で逆向き
- `TuningPanel`の`EFFECT_GROUPS`がbackendの`TuningEffect`を手書きで複製（生成型が`string`のため
  綴りの型検査も効かない）
- `tuning_admin.py`だけがサービス層を経由せずルーターで`commit()`/`rollback()`する（構造仕様7）
- `tuning_overrides`のORMモデルとmigrationが食い違い、fresh bootstrapのDBに`updated_at`列が無い
- `/admin/api/tuning/`を通るテストが0本

## 完了条件

- 効き方の追加点が1本道になる（backendへ1つ足したときフロントが無言で「その他」へ落とさない）
- fresh bootstrap（`bootstrap_ci_db.py`）で作ったDBと本番のスキーマが一致する
