# T563: docsドリフトの一括是正（統合レビュー第4回P2まとめ）と、docs/tasks「状態:」行の整備

状態: 完了（2026-09-03起票・同日完了。統合レビュー第4回
[history/2026-09-03_all.md](../../.claude/commands/review/history/2026-09-03_all.md) Phase 8
起票案3（P2まとめ）に、`scripts/review_checks.py docs`初回監査[T560]の状態不一致を合流）

## 背景

いずれもdocs・コメントのみの修正で、まとめて1コミットで対応できる。

統合レビュー第4回 Phase 5のP2（詳細・行番号は同ファイルのFindings参照）:

- `docs/modules/frontend/map-axis-coloring.md`: 旧UI文言の残存、T515の粗格子機構が未記載
- `frontend/src/components/MapColorLegend/MapColorLegend.tsx`: 表示位置コメントの訂正（「地図上部中央」）
- `frontend/src/components/Map/axisLayers.ts`: docstringの`rampColorForValue`への死んだ参照を削除
- `docs/logging.md`: T536新設の4キャッシュが`log_external_call`を省略している例外規定を追記
- `docs/modules/frontend/developer-research-tools.md`: `frontend/src/app/admin/api/debug/logs/route.ts`を
  対象ファイル表へ追記（`review_checks.py docs --since cab1441`でも唯一の新規記載漏れとして検出）

`review_checks.py docs`の状態照合:

- `docs/tasks/T501.md`・`T505.md`: improvement-plan.mdでは`[x]`（2026-08-31完了）だが
  「状態:」行が「着手中（2026-08-31）」のまま → 完了へ更新
- `docs/tasks/T534.md`: 「状態:」行なし（レビューでも指摘）
- 「状態:」行が無くスクリプトで照合できないファイル17件: T492〜T500・T502〜T504・T506〜T508・
  T516・T534 → improvement-plan.mdの現状に合わせて「状態:」行を追加する

## 対応方針

- 上記を1コミットで是正する。docs/modulesへの追記は「記載粒度」節に従い現状のみ書く。
- 「状態:」行の書式は`状態: 完了（YYYY-MM-DD）`／`状態: 未着手（…）`／`状態: 着手中（…）`／
  `状態: 見送り（…）`（見送り・保留はimprovement-plan側も`[ ]`のまま）に揃える。

## 完了条件

- `python scripts/review_checks.py docs`で「状態:」の不一致0件・「状態:」行なし0件・
  `--since cab1441`の新規記載漏れ0件。
- 上記6箇所のdocs/コメント修正が反映されている。

## 実装（2026-09-03）

- **6箇所のdocs/コメント修正**: `map-axis-coloring.md`の旧UI文言「生成したルートの色分け」
  →「地図の色分け」（T518準拠）、同ファイルへT515の粗格子/詳細格子クリップ機構
  （`windPenaltyCoarseGridToClippedFeatureCollection`/`subtractRectangle`）の説明を追記。
  `MapColorLegend.tsx`のコメントを「地図の左下」→「地図上部中央」（実際のCSS配置に一致）。
  `axisLayers.ts`の`rampColorForRatio`docstringから削除済み`rampColorForValue`への言及を
  削除。`docs/logging.md`へT536系4キャッシュ（`graph_material_cache`・
  `tile_score_matrix_cache`・`search_graph_cache`・`tile_persistent_cache`）が
  `log_external_call`を使わない理由（前者3つは外部I/O自体が無い、後者は専用loggerで
  同等の方針を直接実装）を追記。`developer-research-tools.md`の対象ファイル表へ
  `app/admin/api/debug/logs/route.ts`を追記。
- **docs/tasks「状態:」行の整備**: `T501.md`/`T505.md`の「状態: 着手中」を「状態: 完了」へ
  訂正。`T534.md`（improvement-plan.mdでは`[ ]`のまま）へ「状態: 着手中（実装・テストは
  完了、本番反映・サンプリング実測が残作業）」を新規追加。「状態:」行が無く照合不能
  だった17件（T492〜T500・T502・T503・T506〜T508・T516、いずれも`## 状態`という見出し＋
  本文の旧形式だった）を、他の全タスクファイルと同じ`状態: 完了（日付）`という1行形式へ
  書き換え。T504（`## 状態`見出し自体が無かった）には新規に1行を追加。
- **検証**: `python scripts/review_checks.py docs`で「improvement-plan.md [x]/[ ]と
  docs/tasks「状態:」の不一致」「docs/tasksの「状態:」行なし」がいずれも0件になったことを
  確認（記載漏れ18→17件は`route.ts`追記1件ぶんの前進、残り17件は[T564](T564.md)の
  スコープのため対象外）。frontend `tsc --noEmit`・`eslint .`エラー0、
  `vitest run src/components/MapColorLegend src/components/Map/axisLayers`29件green。
