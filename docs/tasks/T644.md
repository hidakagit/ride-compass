# T644. サーバー側キャッシュの保持方式を精査し、重複した骨格を共通化する

規模L→S。残っていた項目2は[T636](T636.md)へ分離済み。

状態: 完了（2026-09-08）

## 前提（着手前に確認すること）

[T651](T651.md)の結論により、Redisの用途は外部連携（JMAタイル・在否インデックス・アメダス）
へ絞られ、ルート生成系の3モジュールはプロセス内へ移る（[T652](T652.md)）。その結果
**Redis cache-asideの骨格を持つモジュールは4→1（`jma_tile_redis_cache`）へ減り、
本タスクの「骨格が重複している」という論点はほぼ消える**。

**T652を先に片付けてから着手し、残る論点（プロセス内キャッシュの`cachetools`統一・
TTLの根拠整理・空振りの覚え方の整理・ファイルキャッシュの無効化）に絞ること。**
規模もLから縮む見込み。

## 背景

全エンドポイントのサーバー側キャッシュを一覧にした結果（2026-09-07）、保持方式が
**モジュールごとに独立して育っており、考え方が統一されていない**ことが分かった。

### 1. Redis cache-asideの骨格が4モジュールで重複

`jma_tile_redis_cache`・`dynamic_way_value_cache`・`road_edge_geometry_cache`・
`road_graph_tile_cache`が、いずれも次の同じ骨格を独立に書いている
（`redis_available`/`get_redis_client_or_none`/`record_redis_failure`/
`record_redis_success`の出現回数は順に16・12・16・24）。

```python
if not redis_available(): return <未キャッシュ>
client = get_redis_client_or_none()
if client is None: return <未キャッシュ>
with log_external_call(...) as fields:
    try: ...
    except Exception as exc:
        record_redis_failure(); fields["result"] = "error"; ...
        return <未キャッシュ>
    record_redis_success()
```

`simple_api_client.py: cached_fetch`が、プロセス内`TTLCache`側で同じ問題
（「TTLCache参照→ミス時のみfetch→エラー処理→書き戻し」の重複）を1箇所へまとめている。
Redis側の対応物は`redis_json_cache.py`として既にあるが、**利用者は`jma_tile_index.py`
1つだけ**で、上記4モジュールは移行されていない。

### 2. バイナリの扱いが1モジュールだけ違う

`jma_tile_redis_cache`だけがbase64+JSON包装をしている（`redis_client.py`が
`decode_responses=True`で文字列前提のため）。バイナリ用の接続を1つ用意すれば包装は不要で、
CPUとメモリ（1.33倍）を節約できる（[T636](T636.md)で個別に起票済み。本タスクへ統合するか
判断する）。

### 3. プロセス内キャッシュが2方式

`cachetools.TTLCache`（警報・WBGT・洪水予報・`targetTimes`）と、自前のLRU
（`OrderedDict`＋上限で追い出し。`elevation_client._tile_grid_cache`・
`graph_material_cache._LRUCache`・`gsi_relief_tile_client`の404記憶）が混在する。
後者は件数上限が要件でTTLを持たないため`TTLCache`とは前提が違うが、追い出し処理を
各自で書いている点は共通で、1箇所へ寄せられないかは検討されていない。

### 4. TTLの決め方に共通の基準が無い

2分・10分・15分・20分・30分・1時間・3時間・24時間が混在する。上流の更新間隔に合わせたもの
（アメダス15分＝バッチ10分＋余裕）、キャッシュ共有率で決めたもの（`targetTimes`2分）、
根拠がコメントに書かれていないものが混ざっている。「何に合わせた値か」を型か命名で
表現できると、値の妥当性をレビューできる。

### 5. 「空である」という事実の持ち方が3層に散っている

同じ「取りに行っても得るものが無い」という事実を、層ごとに別の形で持っている。

| 仕組み | 覚えるもの | 減らす経路 |
|---|---|---|
| `jma_tile_redis_cache`の`TILE_NOT_FOUND`（[T605](T605.md)） | 上流に**存在しない**パス（404） | サーバー→上流 |
| `jma_tile_redis_cache`のタイル本体 | 200で返った**空タイルの実体**（334バイト） | クライアント→サーバー（同一`basetime`内のみ） |
| `jma_tile_index`（[T635](T635.md)） | 200で返るが**中身が空**という事実 | クライアント→サーバー（要求自体を発生させない） |

[T635](T635.md)の導入により、**空タイルの実体をRedisへ持つ意味が薄れた**。クライアントが
そのタイルを要求しなくなったためである（実測では運用範囲498枚のうち374枚が空で、
実体としておよそ125KB／base64包装後166KBを20分保持している）。プリウォームがインデックス
生成のために取得する経路では引き続き通るため単純に削除はできないが、「空だと分かって
いるタイルは実体ではなくフラグで持つ」形に寄せられないかは検討の余地がある。

`TILE_NOT_FOUND`（404）と「200＋空タイル」を別々に扱っている点も、上位から見れば同じ
「得るものが無い」という1つの事実であり、統合できるかを含めて整理する。

### 6. ファイルキャッシュの無効化手段が2つ

世代番号（`ROAD_SURFACE_TILE_VERSION`等をキーへ埋める）と`tile_cache.clear_all()`
（[T639](T639.md)の「変わらないデータを更新」ボタン）が併存する。前者は世代を上げた
瞬間から新旧が別キーになり安全だが、後者は全消しのため他レイヤーのキャッシュまで巻き添えに
する。

## 進捗（2026-09-08）

| 項目 | 状態 |
|---|---|
| 1. Redis cache-asideの骨格の重複 | **解消**。[T652](T652.md)で2モジュールを削除、`dynamic_way_value_cache`を`redis_json_cache`へ移行。自前で骨格を持つのは`jma_tile_redis_cache`（バイナリのため例外）だけ |
| 2. バイナリの扱いが1モジュールだけ違う | 未着手。[T636](T636.md)として分離済み |
| 3. プロセス内キャッシュが2方式 | **解消**。自前の`OrderedDict`追い出し5箇所を`cachetools`へ統一 |
| 4. TTLの決め方に共通の基準が無い | **解消**。類型を[docs/caching.md](../caching.md)へ整理し、現存するTTL定数はすべて根拠コメントを持つことを確認した |
| 5. 「空である」という事実の持ち方が3層に散っている | **解消**。3層→2層。下記「項目5の実施内容」参照 |
| 6. ファイルキャッシュの無効化手段が2つ | **解消**。[T649](T649.md)で世代番号側に旧世代の掃除を実装。`clear_all`は運用操作としてのみ残す |

写経を止める機械チェックは[T647](T647.md)で導入済み。

## 対応方針（案）

方針の正本は[docs/caching.md](../caching.md)（本タスクの起票を受けて整備）。移行はそこへ書いた形へ寄せる。

段階的に進める。一度に全部を書き換えると、キャッシュはどれも「壊れても機能は止まらない」
fail-open設計のため**不具合が表に出ず静かに劣化する**危険がある。

1. **薄い共通ヘルパー**（可用性チェック・ログ・fail-open・JSON包装を内包したget/set）は
   `redis_json_cache.py`として既にある。新規のRedisキャッシュはこれを使う。
2. 既存4モジュールを順に移行する。移行のたびに、そのキャッシュのTTLの根拠をコメントへ
   明示する（上記4の整理を兼ねる）。
3. バイナリ接続の導入（[T636](T636.md)）と、プロセス内キャッシュの方式統一を検討する。
4. ファイルキャッシュの無効化を世代番号へ寄せられるか検討する（`clear_all`が要る場面を
   洗い出す）。
5. 「空である」という事実の持ち方（上記5）を整理する。特に、空タイルの実体をRedisへ
   保持し続ける必要があるかを[T635](T635.md)の実績を踏まえて見直す。

## [docs/caching.md](../caching.md)整備で確定した方針（2026-09-07）

正本の整備にあたり「この層立てでよいか」を検討し直した結果、本タスクの対象へ次を加える。

- **プロセス内キャッシュは`cachetools`へ統一する**（自前の`OrderedDict`＋`move_to_end`＋
  `popitem`を書かない）。現在5箇所が自前実装: `search_graph_cache`・`graph_material_cache`
  （`_LRUCache`）・`tile_score_matrix_cache`・`elevation_client`（`_tile_grid_cache`）・
  `gsi_relief_tile_client`（404記憶）。固有要件（複数の上限・統計）があるものは例外として
  理由をdocstringへ書く
- **保持層は「共有範囲×寿命」の2軸で選ぶ**。値の形式（バイト列/オブジェクト）を層を増やす
  理由にしない
- **ディスクは既定にしない**。条件（大きい・再計算が高価・世代交代時に旧世代を消す手段がある）を
  満たすときだけ。現状`tile_persistent_cache`が3つ目を満たしておらず[T649](T649.md)で是正する
- **L1+L2の2段構成を既定にしない**（現在2段のものは無い）

## 保留した場合の影響

新しいキャッシュを足すたびに同じ骨格が写経され、モジュール数が増え続ける。実際、
[T635](T635.md)の実装中に**12個目の同型モジュール（`jma_tile_index.py`）を書き始めて
いた**ことがこのタスクの発端である。fail-open設計のため個々の写経ミス（`record_redis_
failure`の呼び忘れ等）は表に出ず、Redis障害の検知だけが静かに欠ける、という壊れ方をする。

## 関連

[T632](T632.md)がクライアント側のキャッシュ方針（`Cache-Control`）を1つの対応表へ集約
したのに対し、本タスクはサーバー側の保持方式に同じことをする。入力側（外部APIの取得層）の
定型文は[T488](T488.md)が`simple_api_client.py`へ集約済みで、本タスクはその
キャッシュ層版にあたる。

[T645](T645.md)で`wind_forecast_cache`（Redis）と`weather_client`のプロセス内キャッシュが
撤去され、対象モジュール数と方式の内訳が上記のとおり変わっている。

## 項目5の実施内容（2026-09-08）

**3層のうち2層を1つへ統合した**。残る1層（在否インデックス）は役割が違うため統合しない。

### 統合したもの: 404と「200＋空タイル」

上流は、データの無いタイルを404で返すことも、200で全画素が透明なPNG（334バイト）・
0バイトのMVTで返すこともある。サーバーはこれを別々に扱っていたが、**利用者から見れば
同じ「得るものが無い」**である。決め手は、クライアント側（`jmaTileProtocol.ts`）が
既に両方を透明タイルへ倒していたこと——区別が消費されていなかった。

| | 変更前 | 変更後 |
|---|---|---|
| 上流404 | `{"not_found": true}`を保存、`TILE_NOT_FOUND`を返す | `{"empty": true}`を保存、`EMPTY_TILE`を返す |
| 200＋空タイル | 実体（base64包装後およそ450バイト）を保存、200で返す | 同上（実体を保存しない） |
| 応答 | 404 / 200＋空タイル | どちらも404 |

`jma_tile_redis_cache.set()`が書き込み時に空かどうかを判定し、空ならフラグへ倒す。
判定（`is_empty_tile`）は`jma_tile_index.py`から`jma_tile_content.py`へ出し、キャッシュと
在否インデックスが同じ1つの判定を使う——基準がずれると危険側（危険情報を表示しない）へ
倒れるため。

### 統合しないもの: 在否インデックス

インデックスは**要求そのものを発生させない**ための仕組みで、クライアントが起動時に1回
受け取る。キャッシュは**要求が来てから答える**ためのもので、上流への往復を消す。
減らす対象（ネットワーク要求 / 上流フェッチ）が違うため、同じ事実を扱っていても別の層で
持つ。両者が同じ`is_empty_tile`を使うことで、基準のずれだけを潰した。

### 付随して直したこと

`JmaTileClient.get()`が404を`None`（取得失敗）へ潰しており、統合後はプリウォームが
空タイルをすべて「エラー」として数えることになった。`get()`の戻り値を3通り
（中身のあるタイル・`EMPTY_TILE`・None）にし、プリウォームのログも`empty=`と`errors=`を
分けた。平常時は取得の大半が空のため、混ぜるとエラー件数が常に大きくなり本物の障害が
埋もれる。

### 検証

`tests/test_jma_tile_redis_cache.py`へ4件追加（空ラスタ・空ベクタはフラグで持つ、中身の
あるベクタは実体を持つ、判定できないもの[GeoJSON]は実体を持つ）。backend全体 1623 passed。
