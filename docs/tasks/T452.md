# T452. gradientFillの独自実装統一DEFERにトリガー条件が明記されていない

状態: 完了（2026-08-31）

- 背景: `統合レビュー第2回`（`.claude/commands/review/history/2026-08-30_all_2.md`P3-3）の
  指摘。`gradientFill`の独自実装（汎用化されていない）を統一する対応が`docs/tasks/
  T432.md`にDEFERとして記載されているが、principles.md（レビュー基盤の共通原則）が
  要求するトリガー条件が明記されていない。
- 対応方針: `T432.md`のDEFER記載へ、着手すべきトリガー条件（例: 3例目の同種実装が
  必要になった時点、またはgradientFillに次回手を入れるタイミング）を追記する。
  実装統一自体を今すぐ行う必要はない（DEFERの妥当性自体は否定しない）。
- Confidence: High（frontend/Mapシャードがprinciples.mdのDEFERルールとT432.mdの記載を
  突き合わせ確認）。
- 規模S。

## 実施記録（2026-08-31完了）

- **対応**: トリガー条件「gradientFillのensure/apply関数自体へ次回手を入れる判断をした
  時点」または「同種のbespoke実装（way_id依存の動的面塗り）が3例目として追加される時点」を
  `T432.md`の実装メモと`MapView.tsx`のGRADIENT_FILL_SOURCE_ID直前コメントの両方へ追記した
  （両箇所とも同じDEFER記載を参照するペアのため揃えて更新）。
- **検証**: `tsc --noEmit`・eslint通過。コメント・ドキュメントのみの変更のためテスト・
  実機確認は対象外。
