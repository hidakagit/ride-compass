# T945. 静的レイヤー追加の1本道化の残り（T888の未実施射程を含む） 規模L

状態: 着手中（2026-09-19起票・2026-09-20着手、統合レビュー第11回のP2/P3）

## 背景

T888は`show◯◯` propを記述子から導出する形へ畳んだが、**手書きの表が4つ残っている**
（`LAYER_ICONS`・`buildLayerDataSources`・`underRoadSurface`・`legendDetailsByLayerId`）。
`LAYER_ICONS`は2026-09-16のレビューで既出・未解消で、この期間もT914で実際に手で足す必要があった。

あわせて**T888本文:33-38が射程として明示した「描画順の決め方を`MapLayerDescriptor`へ持たせる」が
未実施のままT888が`[x]`化され、後続タスクも起票されていなかった**（本タスクがその行き先）。

対象は[history/2026-09-19_all.md](../../.claude/commands/review/history/2026-09-19_all.md)「Findings（P2）」の「T888（静的レイヤーの1本道化）の未完部分」節（12〜19）と、
P3のレイヤーid・描画順まわり。

## 規模の見直し（2026-09-20）

起票時は規模Mとしたが、着手前に中身を数えて**L**へ改めた。根拠は、対象が8項目あり、
フロントで最も大きい3ファイル（`MapView.tsx`・`page.tsx`・`MapOverlayControls.tsx`）に
散っていること、うち2項目（16・17/18）が目視確認の要る画面の挙動であること。

## [T938](T938.md)との違い

検知器が「レイヤー・静的」の語の重なりで参考出力した組。**重複していない**——
T938は「配信情報が届かないときに何が見えるか」（実行時の縮退）、本タスクは
「レイヤーを1枚足すのに何箇所触るか」（追加のコスト）を扱う。片方へは寄せない。

## 対応方針

### 何を記述子へ寄せ、何を寄せないか

**寄せるのは宣言で、描画コードは寄せない。** `buildStaticOverlayLayers`の各エントリが
持つ`ensure`はMapLibreを直接叩く実装そのもので、これを`mapLayers.ts`（DOM・MapLibreを
知らない純粋なカタログ）へ入れると、カタログを読むだけのテスト・`page.tsx`が地図実装を
引き込む。`ensure`は`MapView.tsx`に残し、**それ以外の宣言（アイコン・データ源・描画順）を
記述子へ移す**。

`interactive`（クリック判定の対象にするか）は本タスクの対象外とする——レビューが挙げた
4つの表に含まれず、`OverlayLayerEntry`の必須フィールドとして既に「足すときその場で
答えさせる」形が効いている。

### 描画順（T888:33-38の射程）

いまの並び順は`buildStaticOverlayLayers`が返す配列の順そのもので、**足す位置を人が選ぶ**。
これを記述子の宣言から導く。並びの根拠はファイル内のコメントが既に述べているとおり
「面 → 推定の線（下敷き）→ 観測の線 → レンズの線 → 点」で、レイヤーidではなく
**描画の性質**で決まっている。この段を`MapLayerPaintTier`として名前を持つ値にし、
記述子が自分の段を宣言する。並べ替えは段の順（`MAP_LAYER_PAINT_TIER_ORDER`）で行い、
同じ段の中はカタログ順を保つ。

`underRoadSurface`はこの宣言から導く（段が`area`のものが路面ソースより先にensureされる）。

## 完了条件

- [ ] 静的レイヤーを1枚足すのに必要な編集が、記述子1エントリで済むことを実際に数えて示す
- [ ] 面レイヤーの完全性テスト（`MapView.overlayFilters.test.ts`）の母集団を、手書きの2件から
  記述子由来へ変える
