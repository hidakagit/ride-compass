# T568: SearchGraphStatics/選定間引きのメモリ最適化（entry_keys撤去・int32化・ビットマスク化）

状態: 実装完了・本番同等ベンチマーク待ち（2026-09-03起票、[T557](T557.md)項目6の
`/code-review`指摘を専用タスクへ切り出し。CSR dtype変更・タイル集合キーキャッシュの
上限分離・間引きアルゴリズムのビットマスク化という独立した3つの性能変更を含み、T557で
既に2回改修した同じホットパスへさらに手を入れるため、正しさ修正と同じコミットへ積み増さず
レビュー・検証の焦点を保つ目的で分離した。2026-09-04実装・自動テスト完了。完了条件のうち
`benchmarks/bench_t531_route_generation.py`によるRSS・`select_ms`の本番同等条件での実測は
本番同等の実DB［東京都心規模の実道路データ］が要るため、デプロイ後に別途行う）

## 背景

`/code-review`（[history/2026-09-03_codereview.md](../../.claude/commands/review/history/2026-09-03_codereview.md)）が指摘したメモリ使用量の懸念:

- `SearchGraphStatics`（`backend/app/domain/routing.py`の`CsrGraphStructure`）が持つ
  `entry_keys`は`build_shortest_path_tree`実行のたびに`np.searchsorted`の検索対象として
  使うだけの派生値だが、フィールドとして永続保持している。タイル集合キーの
  プロセス内LRU（`infrastructure/search_graph_cache.py`、上限64件）ぶん常駐し、
  実データ規模の見積もりで最大約1.2GBに達しうる。
- `indptr`/`indices`/`entry_edge_index`が既定のint64のままで、int32で十分な値域
  （Edge数・Node数が2^31を超えない前提）に対し2倍のメモリを使っている。
- `_lazy_graph_cache`と`_search_statics_cache`が同じ上限件数（64）を共有しているが、
  後者の1エントリの方が重い（CSR構造一式）ため、上限を分離して`SearchGraphStatics`側は
  より小さく（例: 16）絞る余地がある。
- `domain/routing.py: select_diverse_by_overlap`内部の間引き判定が、採用済み集合を
  `masks`（`(max_count, edge_count)`のbool二次元配列）で持ち、リクエストごとに
  約22MB×2本（2閾値パスぶん、[T557](T557.md)項目15で1回のループへ統合済みだが配列自体の
  サイズは変わらず）確保する。`TURNAROUND_POOL_MAX=40 ≤ 64`のため、採用済み集合を
  Edgeごとのuint64ビットマスク1本（Edge数×8B）で表現できれば大幅に削減できる。

## 対応方針（`/code-review`指摘のまま、着手時に再検証すること）

1. `entry_keys`を`CsrGraphStructure`のフィールドから外し、`_accumulate_tree_lengths`が
   都度（`np.searchsorted`が必要になった時点で）`indptr`/`indices`から再構築する形へ。
2. `indptr`/`indices`/`entry_edge_index`をint32へ（Edge数・Node数の実際の上限を確認し、
   オーバーフローしないことを検証してから変更する）。
3. `infrastructure/search_graph_cache.py`の`_search_statics_cache`に、`_lazy_graph_cache`とは
   別の（より小さい）上限件数を設定する。
4. `select_diverse_by_overlap`の`masks`をuint64ビットマスク1本（Edge数ぶん）へ置き換え、
   ビット演算（AND＋popcount相当）で重複率を求める形へ再設計する。

## 完了条件（案、着手時に精査する）

- 上記4点の実装後、`pytest -q -m "not postgis"`・`benchmarks/bench_t531_route_generation.py`
  （dev DB）でRSS（常駐メモリ）と`select_ms`の前後を実測し記録する。
- 既存の`select_diverse_by_overlap`関連テスト（`tests/test_routing.py`）が、ビットマスク化後も
  同じ決定的な選定結果を返すことを確認する（境界ケース: `max_count`が64を超える呼び出しが
  無いことのassert、または64超過時のフォールバックを設計する）。
- docs/modules（`docs/modules/backend/routing-engine.md`「探索・索引構築のキャッシュ」節）を
  同一コミットで更新する。

## 実装（2026-09-04）

対応方針の4点をそのまま実装した。

1. **`entry_keys`の撤去**（`domain/routing.py`）: `CsrGraphStructure`から`entry_keys`
   フィールドを削除し、`build_csr_structure`は構築時のソートキーとして一時変数でのみ使う。
   `_accumulate_tree_lengths`が`(pred, v)`のCSRエントリ位置検索に必要な時点で、新設の
   `_reconstruct_entry_keys(structure)`（`indptr`の行境界から`np.repeat`で行indexを復元し
   `row*node_count+indices`を組む）を都度呼ぶ。整列キー自体はint64のまま
   （`node_count`の2乗がint32の値域を超えうるため、後述のdtype変更とは独立）。
2. **int32化**（`domain/routing.py`）: `CsrGraphStructure.indptr`/`indices`/
   `entry_edge_index`を`_CSR_INDEX_DTYPE = np.int32`へ変更した。`build_shortest_path_tree`
   （`scipy.sparse.csr_matrix`の構築・`entry_edge_index`でのコスト配列並べ替え）は
   int32配列をそのまま受け付けるため無変更で動く。
3. **キャッシュ上限の分離**（`infrastructure/search_graph_cache.py`）: `SEARCH_STATICS_
   MAX_ENTRIES = 16`を新設し、`_search_statics_cache`/`_reverse_search_statics_cache`の
   `set`だけこの上限（モジュール変数`_search_statics_max_entries`、テストが
   monkeypatch可能）を使うようにした。`_lazy_graph_cache`/`_routable_index_cache`は
   引き続き`DEFAULT_MAX_ENTRIES`（64）のまま。
4. **ビットマスク化**（`domain/routing.py: select_diverse_by_overlap`）: 採用済み集合を
   `(max_count, Edge数)`のbool行列から、Edgeごとのuint64ビットマスク1本
   （`edge_bits: np.ndarray[uint64]`、bit `i`＝「採用済み`i`件目がこのEdgeを含む」）へ
   置き換えた。候補のEdge index配列で`edge_bits`を抜き出し、`slot_bits`（`1 << arange
   (max_count)`）とのAND演算で採用済みスロットごとの共有判定をベクトル化する。
   `max_count > 64`は`ValueError`（実際の呼び出し元`TURNAROUND_POOL_MAX`=40・
   `MAX_ROUTES`=15はいずれも収まる）。

### 検証

- `pytest -q -m "not postgis"`（backend全件、osmium未導入の2ファイルを除く）green
  （既存の`select_diverse_by_overlap`関連テストは無変更のまま通過——決定的な選定結果は
  ビットマスク化前後で変わらない）。
- 新規テスト: `tests/test_routing.py`に`CsrGraphStructure`のdtype確認1件・
  `_reconstruct_entry_keys`の直接テスト1件・`select_diverse_by_overlap`の
  `max_count>64`エラーテスト1件。`tests/test_search_graph_cache.py`に
  `_search_statics_max_entries`が`_max_entries`と独立に効くことを確認するテスト2件
  （`search_statics`・`reverse_search_statics`各1件）。既存の`test_build_csr_structure_
  matches_node_pair_index`等（`entry_keys`を直接参照していた3件、うち2件はT551で
  新規に書いたばかりのテスト）は`indices`/`indptr`だけで同内容を検証する形へ書き換えた。
- `docs/modules/backend/routing-engine.md`「探索・索引構築のキャッシュ」節（上限分離）・
  domain/routing.py関数一覧（int32化・entry_keys撤去・ビットマスク化）を更新した。
- 未実施（デプロイ後に別途行う）: `benchmarks/bench_t531_route_generation.py`による
  本番同等条件でのRSS・`select_ms`実測（本番同等の実道路データを積んだDBが必要で、本
  セッションのサンドボックスには無い）。
