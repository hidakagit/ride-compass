# T569: preview_segmentがSearchGraphStaticsを不要に構築・キャッシュしている問題の分離

状態: 完了（2026-09-03起票・2026-09-04完了。[T557](T557.md)項目17の`/code-review`指摘に
着手して判明した相互作用を専用タスクへ切り出し）

## 背景

`RoadGraphEngine._build_search_graph`は`prepare`（周回生成、フロンティア方式）・
`preview_segment`（単発区間確認、`/api/routes/preview`）の両方から共通で呼ばれる
（`backend/app/services/road_graph_engine.py`）。この共通経路が内部で必ず
`_get_or_build_search_statics`（一対全最短経路木用の`SearchGraphStatics`をタイル集合キーで
構築・キャッシュ）を呼ぶが、`preview_segment`は2点間の直接A*探索
（`shortest_path_node_ids_lazy`）だけを行い、一対全木を要する`SearchGraphStatics`を
一切使わない。`/code-review`はこれを「`preview_segment`で不要な構築・キャッシュコストを
払っている」と指摘した（[history/2026-09-03_codereview.md](../../.claude/commands/review/history/2026-09-03_codereview.md)）。

## T557での着手時に判明した相互作用（未解決のまま残す理由）

[T557](T557.md)項目4で、`_get_or_build_search_statics`は`domain/routing.py:
build_search_graph_statics`が検出する`LazyGraphEdgeMismatchError`
（タイル再split後のキャッシュ済み`lazy_graph`と新しい`graph`のedge_id不整合）を
検知し、該当タイル集合のキャッシュ3種を破棄して`lazy_graph`ごと再構築する保護機構を
追加した。この保護は`build_search_graph_statics`の内部に実装されているため、
`_build_search_graph`を経由する`preview_segment`もこの保護を副次的に受けている——
`preview_segment`自身のコスト配列構築（`cost_by_edge_id[edge_id] for edge_id in
lazy_graph.edge_ids`）も同種のKeyErrorに理論上は脆弱なため、この副次的な保護は
偶然ではなく実質的に必要な保護になっている。

そのため、項目17の指摘どおり単純に`preview_segment`から
`_get_or_build_search_statics`の呼び出しを外すと、この保護も一緒に失われ、
`preview_segment`が再split直後にKeyError相当で失敗する経路を再び持つことになる。

## 対応方針（案、着手時に精査する）

1. lazy_graphの整合性チェック（`graph.edges`に`lazy_graph.edge_ids`が全て存在するかの
   検証と、不整合時のタイル集合キャッシュ無効化・再構築）を、`build_search_graph_statics`
   から独立した専用関数へ切り出す（例: `_ensure_lazy_graph_consistent(lazy_graph, graph,
   tile_set)`）。
2. `_build_search_graph`が、`prepare`/`preview_segment`のどちらの経路でも1に切り出した
   整合性チェックを共通で呼び、`SearchGraphStatics`自体の構築・キャッシュは`prepare`側
   （一対全木を実際に使う`select_loop_turnarounds`の手前）だけで行うよう配線を変える。
3. 実測: `preview_segment`のタイル集合キャッシュ使用量・応答時間の前後を
   `benchmarks/`配下の適切なスクリプトで比較する。

## 完了条件（案、着手時に精査する）

- `preview_segment`が`SearchGraphStatics`を構築・キャッシュしなくなったことを、
  `search_graph_cache.search_statics_cache_size()`をテストから直接確認する形で検証する。
- 再split後のキャッシュ不整合シナリオ（[T557](T557.md)項目4の回帰テストと同型）を
  `preview_segment`向けにも追加し、`prepare`側と同様に例外を出さず正しく再構築されることを
  確認する。
- `pytest -q -m "not postgis"` green。docs/modules（`docs/modules/backend/
  routing-engine.md`）を同一コミットで更新する。

## 実装（2026-09-04）

対応方針の案どおり実装した。

1. **整合性チェックの分離**（`domain/routing.py`）: `build_search_graph_statics`から
   検知ロジックだけを`find_missing_lazy_graph_edge_id(lazy_graph, graph) -> str | None`
   （CSR構築を伴わない軽量版、`lazy_graph.edge_ids`のうち`graph.edges`に無い最初の
   edge_idを返すだけ）へ切り出した。`build_search_graph_statics`自身もこの関数を呼んで
   `LazyGraphEdgeMismatchError`を送出する構造は残す（事前チェック無しで直接呼ばれても
   安全なままにする二重の安全網、実運用では後述の共通チェックが先に効くため実際に
   ここで送出されることは無い想定）。
2. **共通の整合性検証・再構築ステップ**（`services/road_graph_engine.py`）:
   `_get_or_build_search_statics`内にあった「`LazyGraphEdgeMismatchError`を捕まえて
   タイル集合キャッシュ3種を破棄しlazy_graphごと再構築する」ロジックを、新設した
   `_ensure_lazy_graph_consistent(tile_set, lazy_graph, graph) -> LazyRoadGraph`へ
   移設し、`_build_search_graph`（`prepare`・`preview_segment`共通経路）が
   `_get_or_build_lazy_graph`の直後に呼ぶよう配線した——`_build_search_graph`自身の
   `cost_by_edge_id[edge_id] for edge_id in lazy_graph.edge_ids`も同種のKeyErrorに
   脆弱なため、この位置に置くことで両メソッドが同じ保護を受ける。
3. **`SearchGraphStatics`の構築・キャッシュはprepareだけへ**: `_SearchGraph`
   データクラスから`statics`フィールドを削除し、`_build_search_graph`はもう
   `SearchGraphStatics`を一切構築しない。`prepare`が`_build_search_graph`の直後に
   （整合性検証済みの`search.lazy_graph`を使って）`_get_or_build_search_statics`を
   呼び、その結果を`_RoadGraphContext.statics`へ渡す。`preview_segment`はこの呼び出しを
   持たない。
4. **`_get_or_build_search_statics`の簡素化**: `lazy_graph`は呼び出し元
   （`prepare`）が`_ensure_lazy_graph_consistent`で検証済みという前提に変え、
   `LazyGraphEdgeMismatchError`の捕捉・再構築ロジックを削除した（単純なキャッシュ
   参照/構築のみ）。戻り値も`(statics, cached, lazy_graph)`の3要素タプルから
   `(statics, cached)`の2要素へ簡素化した（lazy_graphを差し替える経路が無くなったため）。
5. ログ: `_build_search_graph`のINFOサマリから`statics_cached`を削除し、代わりに
   `prepare`独自のINFOサマリ（`prepare search_statics build edges=... statics_ms=...
   statics_cached=...`）を追加した。

### 検証

- `pytest -q`（backend全件）green。
- 新規テスト: `tests/test_routing.py`に`find_missing_lazy_graph_edge_id`の直接テスト2件・
  `build_search_graph_statics`が引き続き例外を送出することの直接テスト1件。
  `tests/test_road_graph_engine.py`に`preview_segment`が`SearchGraphStatics`を構築・
  キャッシュしないことの確認1件、`preview_segment`向けの再split不整合シナリオ
  （`test_prepare_rebuilds_stale_lazy_graph_after_resplit_cache_desync`と対になる
  `preview_segment`版）1件を追加。既存の`prepare`向け回帰テストは無変更のまま通過。
- `docs/modules/backend/routing-engine.md`「探索・索引構築のキャッシュ」節を更新
  （`SearchGraphStatics`が`prepare`限定になったこと、`_ensure_lazy_graph_consistent`/
  `find_missing_lazy_graph_edge_id`による自己修復の仕組みを追記——後者は[T557](T557.md)
  項目4で実装されてから本ドキュメントに一度も記載されていなかった乖離だったため、
  本タスクで触れる箇所の範囲であわせて是正した）。
