# T562: docs/architecture.mdのbackendディレクトリツリーへT536〜T539/T546新設4ファイルを反映

状態: 完了（2026-09-03起票・同日完了。統合レビュー第4回
[history/2026-09-03_all.md](../../.claude/commands/review/history/2026-09-03_all.md) Phase 5 C-3、P1）

## 背景

T536〜T539・T546（いずれも規模M以上）で新設された次の4ファイルが、`docs/architecture.md`の
backendディレクトリツリー（`infrastructure/`・`batch/`の列挙）に載っていない。同ツリーは他の
全infrastructureファイルを1行ずつ✅付きで列挙する慣行のため、この4件だけが欠落している状態。
`docs/modules/backend/routing-engine.md`側には記載済み。

- `backend/app/infrastructure/search_graph_cache.py`
- `backend/app/infrastructure/tile_persistent_cache.py`
- `backend/app/infrastructure/tile_score_matrix_cache.py`
- `backend/app/batch/presplit_road_graph.py`

原因はCLAUDE.md「規模M以上でAPI・ドメイン概念・レイヤー種を新設するタスクは完了条件へ
architecture.md追従を既定で含める」の見落とし。

## 対応方針

- 4ファイルをツリーの該当位置へ、既存行と同じ書式（ファイル名・✅・1〜2文の役割）で追記する。
  役割の文言は`routing-engine.md`・各タスクファイルの記述から取り、経緯ではなく現状を書く。
- ついでにT536〜T539/T546の各タスクファイルに「architecture.md追従」の記載があるか確認し、
  無ければ本タスクで追従した旨を各ファイルの完了条件へ一言残す。

## 完了条件

- `docs/architecture.md`のツリーに4ファイルが載り、`git grep -n "search_graph_cache.py\|tile_persistent_cache.py\|tile_score_matrix_cache.py\|presplit_road_graph.py" docs/architecture.md`が4件以上ヒットする。

## 実装（2026-09-03）

- `docs/architecture.md`のbackendディレクトリツリーへ4ファイルを追記（infrastructure/へ
  `search_graph_cache.py`・`tile_persistent_cache.py`・`tile_score_matrix_cache.py`、
  batch/へ`presplit_road_graph.py`）。文言は`docs/modules/backend/routing-engine.md`の
  記述から現状のみを抽出し、経緯は書いていない。
- `git grep`確認: 4件ヒット（完了条件クリア）。
- 新設元タスク（T536: tile_score_matrix_cache.py、T537: search_graph_cache.py、
  T538: tile_persistent_cache.py、T539: presplit_road_graph.py）の各ファイルに
  architecture.md追従の記載が無かったため、それぞれへ本タスクで追従した旨を追記した。
  T546はこれら4ファイルを新設していない（既存ファイルの内部実装変更のみ）ため対象外。
