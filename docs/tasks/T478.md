# T478. 統合レビュー第3回P3級指摘の一括対応（死コード削除・docs/テストの軽微是正10件）

状態: 完了（2026-08-31）

- 背景: 統合レビュー第3回（`history/2026-08-31_all.md`）で検出されたP3級の指摘10件を、
  個別起票のオーバーヘッドを避けるため1タスクへまとめる。いずれも実害は軽微・修正は
  S未満の小さな変更。
- 対応方針（各項目）:
  1. **死コード削除（対応済み、2026-08-31）**: `backend/app/services/weather_service.py`の
     `get_conditions_many`/`prefetch`（T462でopenrouteserviceエンジン撤去後、呼び出し元が
     消滅）を削除した（Shard C1・Shard G重複発見）。`_conditions_from_data`の`at`パラメータ
     （get_conditions_many専用だった未来時刻分岐）も併せて整理し、`backend/tests/
     test_weather_service.py`の対応テスト・`weather_client.py`/`domain/weather.py`/
     `domain/wind_grid.py`の言及コメント・`docs/modules/backend/weather-dynamic-layers.md`の
     経路表・`docs/architecture.md`の天候行（`WindService.prefetch`言及、T462で既に削除済みの
     クラスへの参照が残っていた）も同時に更新した。T462.md「さらなる派生発見」から切り出した
     バックグラウンドタスクとして本項目単独で先行対応。
  2. `frontend/src/components/SystemStatusPanel/SystemStatusPanel.tsx:45-50`のコメント
     「Promise.allSettledでまとめる」を実装（`Promise.all`）と一致する記述へ修正（Shard E）。
  3. `backend/tests/test_redis_client.py:20`の`redis_client._last_failure_at`への直接代入を
     公開関数`reset_circuit_breaker()`呼び出しへ置き換える（Shard C2/C1）。
  4. `docs/modules/backend/elevation.md:32`の「`RoadGraphEngine._aggregate_elevation`」表記を
     「`road_graph_engine.py: _aggregate_elevation`（モジュールレベル関数）」へ訂正（Shard G）。
  5. `docs/tasks/T466.md`のjmaNowcastFrames.tsフォールバック除外理由「対応検討中」を
     「T425で対応済み（[T425](T425.md)参照）」へ訂正（Shard F）。
  6. `docs/tasks/T470.md`項目7（debugStatsApi.ts手書き型二重管理）の切り出し先spawn_taskの
     タイトルを追記する、または正式にTxxxとして起票してリンクする（Shard F）。
  7. `frontend/src/app/page.tsx`のT468バグ修正（`isDynamicGroupLayer`の`dataNature`単一
     ソース化）に対する回帰テストを`page.test.tsx`へ追加する（Shard F）。
  8. `frontend/src/hooks/usePolledFetch.test.ts:1`の`// @vitest-environment jsdom`上書きが
     本当に必要か確認し、必要なら理由をコメントに残す。不要ならデフォルト（happy-dom）へ
     戻す（Shard F）。
  9. `frontend/src/components/Map/MapView.tsx`の`interactiveLayerIds`から
     `GRADIENT_FILL_LAYER_ID`が正しく除外されているか再確認し、カーソルだけhoverに反応する
     不整合が残っていれば解消する（Shard D）。
  10. `docs/tasks/T426.md`のサマリ表「陳腐化」行の内訳（1→2、または§6の「棚卸し前に確認済み」
      分を独立行として追加）を訂正する（Shard H）。
- 規模: M（コード1箇所[dead code削除]・テスト2件・docs修正7件）
- 実施結果（2026-08-31、項目2〜10）:
  2. `SystemStatusPanel.tsx`のコメント「Promise.allSettledでまとめる」を実装
     （`Promise.all`）と一致する記述へ修正。
  3. `backend/tests/test_redis_client.py`の`redis_client._last_failure_at`への直接代入を
     公開関数`redis_client.reset_circuit_breaker()`呼び出しへ置き換え。
  4. `docs/modules/backend/elevation.md`の「`RoadGraphEngine._aggregate_elevation`」を
     「`road_graph_engine.py: _aggregate_elevation`（モジュールレベル関数）」へ訂正
     （実装を確認、クラスメソッドではなくモジュールレベル関数）。
  5. `docs/tasks/T466.md`のjmaNowcastFrames.tsフォールバック除外理由を
     「[T425](T425.md)項目5で対応済み」へ訂正（実装で確認済み）。
  6. `docs/tasks/T470.md`項目7のspawn_task先を、正式に起票・完了した
     [T481](T481.md)へのリンクへ更新。
  7. `frontend/src/app/page.test.tsx`へ、`isDynamicGroupLayer`（page.tsx）が
     `dataNature==="dynamic"`単一ソースを見ていることの回帰テスト2件を追加
     （gradientFillは「[設定はサイドバー]」を付けない・roadTypeは付ける）。
     既存の`overlay-layers`テストid（[id, on]の2要素固定を前提にした複数の既存
     アサーション）を壊さないよう、titleは新設の別testid`overlay-layer-titles`
     （[id, title]）へ出す形にした。
  8. `frontend/src/hooks/usePolledFetch.test.ts`の`// @vitest-environment jsdom`
     上書きを削除（renderHookのみでDOM描画を伴わないテストのため既定のhappy-domで
     問題無く動作することを確認、3回連続実行で安定）。
  9. `frontend/src/components/Map/MapView.tsx`の`buildInteractiveLayerIds`が
     `"gradientFill"`（`GRADIENT_FILL_LAYER_ID`）を除外していなかったため、
     `handleMouseMove`（同じ`interactiveLayerIds`を参照）がこのレイヤー上でpointer
     カーソルを出す一方、`handleClick`はT461の早期returnガードで何もしないという
     「カーソルだけhoverに反応する」不整合が実際に残っていた。`"elevation"`と同じ
     除外条件へ`"gradientFill"`を追加して解消。`buildInteractiveLayerIds`/
     `GRADIENT_FILL_LAYER_ID`をテスト専用にexportし、`MapView.overlayFilters.test.ts`へ
     回帰テスト2件を追加（gradientFill除外・designation等の通常レイヤーは引き続き含む）。
  10. `docs/tasks/T426.md`のサマリ表を全9セクションの見出し表記（各§の宣言件数）と
      突き合わせて再集計した結果、「陳腐化」行の1→2漏れだけでなく「要確認」
      （63に内数ではなく独立カウント）・「対象外」（2ではなく1、§6の陳腐化1件が
      誤って二重計上されていた）にも数え間違いがあったため、5行すべてを実際の
      内訳と一致するよう訂正した（63+5+2+1+1=72で合計と整合することを確認）。
- 検証: `pytest tests/test_redis_client.py -q`（green）・`npx vitest run
  src/app/page.test.tsx src/hooks/usePolledFetch.test.ts src/components/Map/
  --pool=threads`（全green）・`npx tsc --noEmit`・関連ファイルの`eslint`いずれも
  エラー無し。
