# T917. 路面タイルをedge単位にしたときの費用を測る

状態: 着手中
規模: S

## 背景

ユーザー指摘「今後、edge単位の情報をフロントが知っていたほうがいいこともあるのではないかと
思っている。地図上の区間毎の向かい風、区間毎の予想速度や時間など」。

同じ「区間ごとの値」に2つの世界がある。ルート確定前はway丸ごと（タイルが`osm_raw_ways`
由来）、確定後は区間（`RouteSegmentDetail`）。移行の可否を決めるための計測だけを先に行う。
