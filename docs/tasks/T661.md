# T661. 認可・レート制限の非対称を是正する

規模S〜M。

状態: 未着手（2026-09-08起票）

## 背景

統合レビュー第5回（[2026-09-08_all.md](../../.claude/commands/review/history/2026-09-08_all.md)）
が検出した指摘のうち、下表の2件を1つの起票単位として扱う。

同じファイルの`/coverage`が「認可なしに公開すると繰り返し呼ばれるだけでDBを圧迫できて
しまう」という理由でBasic認証を課しているのに、同じ根拠が`values`側に適用されていない。

## 対象の指摘

| # | Sev | 対象 | 内容 |
|---|---|---|---|
| 2-2 | P1 | `api/routers/material_catalog.py:133-148` | `GET /api/material-catalog/{material_id}/values`が認可なし・レート制限なし・同時実行上限なしで、索引の効かない`SELECT DISTINCT`（実質全表走査）を1リクエスト1回実行する。**同じファイルの`/coverage`は「認可なしに公開すると繰り返し呼ばれるだけ |
| 2-3 | P2 | `api/routers/routes.py:283-292` | `_generate_semaphore`をHTTPハンドラ内で`acquire()`し、解放をレスポンス送出**後**の`BackgroundTasks`に委ねている。送出側が失敗すると背景タスクが起動せずセマフォが解放されない。`generate_max_concurrent=2`のため2回でルート生成が**恒久4 |

各項目のEvidence・failure_scenario・Recommendationはレビュー結果の全件表を参照する。

## 着手時に決めること

- 上記のうちどれを実施し、どれを見送るか（見送る場合は理由を本ファイルへ残す）
- 実施順序（レビューのPhase 8は「検証の自動化→境界の固定→実害のある修正→
  現状文書の是正→内部の再配置」を提案している）
