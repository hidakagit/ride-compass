# T593: 専用way値レイヤーの配信値を難易度スケールへ統一しdisplay_thresholds_overrideの二重帰属を解消する

状態: 完了（2026-09-05起票・同日完了、分割元は[T590](T590.md)。本番の段階ラベル設定はT599へ）

## 背景

風軸の`display_thresholds_override`がルート前（材料スケールm/s、`page.tsx:
dedicatedWayValueBoundaries`→`windAxisColorExpression`）とルート後（難易度0〜100、
`routeStyleModes.ts: routeColorableModeFromAxis`）の2系統で共用されている。勾配は前後とも
符号付き%で整合するが、風は不整合。本番の`[10,20,30,40,50]`は難易度スケールとしては
妥当な値で、[T587](T587.md)で「異常値」とされた真因はこの二重帰属である可能性が高い。

## 対応方針

- `GET /api/region/dynamic-way-values/{material_id}/...`の配信値を、その軸のshapeが
  signed-abs（`breakpoint_linear`かつ`preprocess="abs"`かつterms単数）でなければ軸の難易度
  （`evaluate_axis_scalar`で軸スタジオのbreakpointsを適用した0〜100）にする。signed-absの
  軸（勾配）は符号付き材料のまま。分岐はshape属性で行い、`routeStyleModes.ts:
  isSignedAbsShape`と同じ判定をbackendへ寄せる。
- `WindWayService`は風の材料をタイル中心の風から計算した後、軸の評価まで行って返す。
  材料の計算はT591の1関数を経由し、第4の式重複を解消する。
- 軸カタログに「専用レイヤーの配信値の種類」（`difficulty` / `signed_material`）と単位を
  含め、フロントは凡例の単位・色式・しきい値のフォールバックをそこから決める。
  `windAxisLayer.ts`の`WIND_AXIS_THRESHOLDS`・"m/s"固定・風専用の色式は撤去し、
  `gradientAxisLayer.ts`と共通の汎用機構（配信値の種類で分岐）へ畳む。
- 本番DBの風軸`display_thresholds_override=[10,20,30,40,50]`は難易度スケールとして
  そのまま有効になる。段階ラベル（`display_band_labels_override`）を5段階ぶん設定する
  （axis_admin API経由、[docs/deployment-sync.md](../deployment-sync.md)の完了条件）。

## 対象ファイル

`backend/app/services/wind_way_service.py`・`services/gradient_way_service.py`・
`api/routers/region.py`・`api/routers/axis_catalog.py`・`domain/axis_display.py`・
`frontend/src/components/Map/windAxisLayer.ts`・`gradientAxisLayer.ts`・`MapView.tsx`
（`makeEnsureDedicatedWayValueLayer`）・`page.tsx`（`dedicatedWayValueBoundaries`）・
`docs/modules/backend/dynamic-way-values.md`・`docs/modules/frontend/`該当モジュール。

## 完了条件

- 風軸について、ルート前の全道路色分けとルート後のルート線色分けが同じしきい値・同じ凡例で
  描かれる（実機確認、Playwright headless）。
- `display_thresholds_override`の意味が軸ごとに1つになっている（材料スケールと難易度
  スケールの混在が無い）。
- フロントに風の名前を持つ色分けファイルが残っていない。

## 実装メモ（2026-09-05）

- backend: `domain/dynamic_way_values.py`に`map_value_kind`/`map_value_unit`/
  `transform_dedicated_way_values`を追加。`GET /api/region/dynamic-way-values`はサービスの
  生値（キャッシュ対象）を軸定義で評価した地図表示値へ変換して返す（風=難易度0〜100、
  勾配=符号付き材料のまま）。各サービスは`material_id`クラス属性で返す材料を宣言する。
  `GET /api/axis-catalog`と静的`axis-catalog.json`に`map_value_kind`/`map_value_unit`を追加。
  `MaterialSpec.unit`を新設（勾配"%"・風"m/s"、他は空文字。T592で拡張予定）。
- frontend: `windAxisLayer.ts`・`gradientAxisLayer.ts`を撤去し、`dedicatedWayValueLayer.ts`
  （表示宣言`DedicatedWayValueDisplay`だけから色式・凡例・feature-stateキーを組み立てる）と
  `valueScale.ts`（種類ごとの既定しきい値・配色、HSL補間、段階分け色式。ルート前後で共有）へ
  統合。`routeStyleModes.ts`の`isSignedAbsShape`（shapeからの判定）は撤去し、backendの
  `map_value_kind`に従う。`MapViewProps.dedicatedWayValueBoundaries`は
  `dedicatedWayValueDisplays`（種類・単位・しきい値・段階ラベル）へ置き換え。
- 風の難易度スケールの既定しきい値は`[33,66]`（ルート後の既定と同じ）。本番の
  `display_thresholds_override=[10,20,30,40,50]`は難易度スケールとしてそのまま有効。

## 検証結果

- backend: `pytest tests/test_dynamic_way_values.py tests/test_region_routes.py
  tests/test_axis_catalog_routes.py tests/test_wind_way_service.py
  tests/test_gradient_way_service.py tests/test_material_catalog_routes.py`（新規5件含む）。
- frontend: `tsc --noEmit`・`eslint`クリーン（`layout.tsx`の`LayoutProps`はworktreeに
  `.next`生成型が無いことによる環境依存で本タスクと無関係）、`vitest`該当6ファイル87件。
- backendフルスイート（`pytest -q -m "not postgis"`）終了コード0、frontendフルスイート（`vitest run`）は末尾参照。
- 実機確認（Playwright headless chromium、worktreeのbackend/frontendをローカル起動）: ルート前に「風で地図を
  色分け表示」をONにすると凡例が「風 33未満／33〜66／66以上」（難易度スケール、単位なし）で表示され、
  `GET /api/region/dynamic-way-values/wind/...`が難易度値（例: 8.8）をway_idごとに返すことを確認。
  道路線の着色そのものは、この開発環境で路面タイル配信（外部nginx経由）が届かず視認できなかった
  （本タスクの変更範囲外の環境要因）。

## 残課題

- 本番の風軸に段階ラベル（`display_band_labels_override`、5段階）を設定する運用は、同じ
  軸スタジオ操作を行う[T599](T599.md)（風軸の新材料切替・較正）へ寄せる。本タスク時点では
  凡例は数値レンジ（`10未満` 等）のみで表示される。
