# T473. windBoundaries/gradientBoundariesの軸専用propを汎用機構へ統合し、環境グループの配線漏れを解消

状態: 完了（2026-08-31）

- 背景: `docs/design-principles.md`構造仕様3は「`dedicated_way_value_layer`軸（wind/gradient等）の
  color expression（しきい値・配色）は、軸スタジオのデータから導出する汎用機構1つが持つ。
  軸ごとにfrontend側のファイル・関数・定数・propを新設してはならない」と明記しているが、
  design-principles.md新設（`2fef457`）・CLAUDE.mdへの必須点検追加（`d573fda`）の**後**の
  コミットで、T443が`gradientBoundaries`という軸専用prop、T466が`windBoundaries`という
  別名の軸専用prop（T443と「同型」と自己申告のうえ意図的に複製）を新設し、原則が違反された
  （統合レビュー第3回 `history/2026-08-31_all.md` P1、complexityレンズF-1）。
  加えて、環境グループ（`windPenalty.ts`のgridFill）側の表示しきい値は今回の配線から
  スコープ外とされたまま`WIND_AXIS_THRESHOLDS`のハードコードが残り、モジュール自身が
  謳う「評価軸グループと環境グループで色の意味を揃える」契約に反している（Shard D）。
  `docs/modules/frontend/page-composition.md`「暗黙の前提」節・
  `docs/modules/frontend/map-axis-coloring.md`もこの状態変化に未追従（Shard F・Shard G）。
- 対象ファイル: `frontend/src/app/page.tsx`（`windBoundaries`/`gradientBoundaries`の
  専用useMemo・専用prop）、`frontend/src/components/Map/MapView.tsx`（軸ごとの型宣言・
  分割代入・ensure引数）、`frontend/src/components/Map/windAxisLayer.ts`・
  `gradientAxisLayer.ts`、`frontend/src/components/Map/windPenalty.ts`
  （`WIND_AXIS_THRESHOLDS`ハードコード）。
- 対応方針: `page.tsx`の`windBoundaries`/`gradientBoundaries`という2つの専用useMemo/propを、
  `axisCatalog`（`axes`+`secondaryAxes`の`dedicated_way_value_layer=true`軸を横断的に抽出）
  から導出する`ReadonlyMap<string, readonly number[]>`1本（例: `dedicatedWayValueBoundaries`）
  へ統合し、MapView側も軸idキーで参照する形へ一般化する。あわせて`windPenalty.ts`の
  環境グループgridFill側も同じタイミングで同じ汎用機構から配線し、
  「評価軸グループ・環境グループで色の意味を揃える」契約を実際に満たす。
  `docs/modules/frontend/page-composition.md`「暗黙の前提」・
  `docs/modules/frontend/map-axis-coloring.md`を同一コミットで更新する。
- 規模: S〜M

## 実施記録（2026-08-31完了）

- `frontend/src/app/page.tsx`: `gradientBoundaries`/`windBoundaries`という2つの専用
  useMemoを、`dedicatedWayValueBoundaries`（`axisCatalog.axes`から
  `dedicatedWayValueLayer===true`の軸を横断的に抽出した`ReadonlyMap<axisId, readonly
  number[]>`）1本へ統合。`<MapView>`へも単一propとして渡す。
- `frontend/src/components/Map/MapView.tsx`: `MapViewProps`の`windBoundaries`/
  `gradientBoundaries`を`dedicatedWayValueBoundaries?: ReadonlyMap<string, readonly
  number[]>`へ統合。`buildStaticOverlayLayers`は内部で`.get("wind")`/`.get("gradient")`
  を引く形へ変更（呼び出し元の見た目はシンプルなまま、既存のwindAxisColorExpression/
  gradientAxisColorExpression/makeEnsureGradientFillLayerへの受け渡しは変更なし）。
  環境グループの風penalty gridFill（`DYNAMIC_WEATHER_RENDERERS.windVector.penaltyFill`）
  も同じMapから配色しきい値を引くよう、`dynamicWeatherRenderers`という薄いuseMemo
  （静的スペックのwindVector.penaltyFill.gridFill.colorExpressionだけを浅く上書き）を
  新設し、`redrawPropsRef`経由・専用effectの依存配列経由の両方の再適用パスへ配線した。
- `frontend/src/components/Map/windPenalty.ts`: `windPenaltyFillColorExpression()`が
  `boundaries?`引数を受け取れるようにし、`buildWindPenaltyColorExpression`（既にT466で
  boundaries引数対応済み）へ転送。
- **派生発見・同時修正**: `frontend/src/components/Map/secondaryAxes.ts`
  （`SecondaryAxisSummary.dedicatedWayValueLayer`フィールド新設）・
  `frontend/src/lib/evaluationAxes.ts`（`PREFERENCE_AXES`が`SECONDARY_AXES`由来の全軸の
  `dedicatedWayValueLayer`を「kind='ramp'軸のみのため常にfalse」という誤った前提で
  一律false固定していたが、gradientはkind="none"でありながら
  `dedicated_way_value_layer=true`という組み合わせが実在するため誤りだった。ビルド時
  静的フォールバック[`axisCatalog.axes`未取得中に使う`FALLBACK_CATALOG`]でgradientの
  しきい値配線が欠落する潜在バグだったため同時に修正）。回帰テストを
  `evaluationAxes.test.ts`へ1件追加（`axis-catalog.json`の`dedicated_way_value_layer`と
  `PREFERENCE_AXES`の一致を検証、gradientが`true`であることを直接確認）。
- `docs/modules/frontend/page-composition.md`「暗黙の前提」・
  `docs/modules/frontend/map-axis-coloring.md`を更新（現状の記述のみ、経緯は本ファイルへ）。
- **ユーザー指摘を受けた訂正**: 上記docs更新の初版は「以前は◯◯だった、T473で解消した」
  という経緯をdocs本体へ書いてしまっており、CLAUDE.mdの「docsは現状と経緯を分ける」
  （判断原則15）に反していた。現状のみの記述へ書き直し、再発防止としてCLAUDE.md
  「コミット時の同期ルール」・`.claude/commands/review/consistency.md`「設計 ↔ 実装」
  へ「現状docsへの経緯混入チェック」を毎タスク完了時の即時チェック項目として追加した。
- **横断チェック**: 同種の問題が他のdocs/modules/\*.mdにも無いか確認した結果、
  `docs/architecture.md`に同種の記述が広範に既存（T428「経緯記述のdecisions/への抽出」が
  別途追跡中、本タスクでは対応しない）。`docs/modules/backend/routing-engine.md`・
  `weather-dynamic-layers.md`にも軽微なT462言及が残っているが、T473で新規に生んだもの
  ではないためスコープ外とし、ユーザーへ状況のみ報告した。
- **派生タスクの起票**: `docs/design-principles.md`「関連する過去の逸脱」が挙げていた
  3症状のうち`WIND_AXIS_THRESHOLDS`ハードコードは本タスクで解消。残る2症状
  （`windPenalty.ts`の物理式JS移植・`windAxisPenalties`/`gradientAxisValues`という
  実データ本体の軸ごとの別名prop）は[T482](T482.md)として起票した
  （並行セッションと番号衝突しT481→T482へ振り直し）。
- 検証: `tsc --noEmit`・`eslint`（対象ファイル）ともにクリーン。関連テスト
  （`secondaryAxes.test.ts`・`windAxisLayer.test.ts`・`windPenalty.test.ts`・
  `MapView.dataStatus/overlayFilters/routes/segments.test.ts`・`evaluationAxes.test.ts`・
  `page.test.tsx`、計9ファイル・69件）すべてgreen。
