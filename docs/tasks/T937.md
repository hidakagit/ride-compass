# T937. 軸スタジオで、既存軸の折れ点が入力に触れただけで作り直される／再公開の成功が失敗と表示される 規模S

状態: 完了（2026-09-19）

## 背景

T922が「生成」ボタンを撤去して入力のたびに折れ点を作り直す形にしたが、生成フォームの3入力が
`draft.breakpoints`と同期されていない。あわせてT924の「調整する→保存で再公開」が、成功した直後に
「下書きのまま残った」という赤字の通知を必ず出す。どちらもこの保存経路を通るテストが0件である。

詳細は[history/2026-09-19_all.md](../../.claude/commands/review/history/2026-09-19_all.md)。

## 対応方針

2つは別の原因だが、同じ画面・同じ未検査の保存経路にあるため1タスクで扱う。

1. **折れ点が作り直される**: `AxisScoringSection.tsx`の生成フォーム3入力を`draft.breakpoints`から
   導出する。`useState`の初期化関数と、`editing`が変わったときの同期の両方が要る。
   同ファイルのコメント「『生成』を押すまで`draft.breakpoints`へは反映しない」は、実在しない
   安全装置を名指ししているので実態へ合わせる。
2. **成功時の誤った通知**: `AxisStudio.tsx`の`handleSave`が状態を消した直後に同一レンダーの
   `closeComposer()`を呼ぶため、クロージャが古い値を読む。通知は`listError`と同じ配色で描かれて
   いるが、内容は失敗ではなく状態の案内のため配色も分ける。
3. **「調整する」中の「公開する」チェック**が保存時に無条件で上書きされる（画面の操作結果が無言で
   反転する）。表示と送る値を揃える。

**テストを先に置く**——この保存経路を通るテストは現在0件で、両方の欠陥がそこに落ちた。

## 完了条件

- 既存軸を開いて「効き方」だけを変えたとき、折れ点の端点が保存されることをテストで固定する
- 再公開に成功したとき通知が出ないこと、中断したときだけ出ることをテストで固定する
- `updateAxisDefinition`へ渡るpayloadの`is_published`を1件ずつ検証する

## 関連

- [T922](T922.md)（段階の設計）・[T924](T924.md)（1画面化）・[T926](T926.md)（表示だけ編集）。
  本タスクはこの3つが残した未検査の分岐を扱う。

## 対応（2026-09-19）

1. **折れ点の作り直し**: `breakpointTools.ts`へ`generatorSettingsFrom`を足し、生成フォームの
   3入力をいまの折れ点から復元するようにした。生成物と総当たりで突き合わせて一致すれば
   効き方まで復元し、手で編集された折れ点なら端点だけを点数の低い側/高い側から復元する。
   実在しない「生成」ボタンを安全装置として名指ししていたコメントも実態へ直した。
2. **成功時の誤った通知**: 中断の通知を出すかを`closeComposer(republished)`の引数で決める形に
   した。`setRepublishAxisId(null)`の直後に呼んでも、その関数が読む状態はそのレンダーの
   クロージャのままで、再公開に成功した直後に必ず通知が出ていた。
3. **公開チェックの無言の反転**: 調整中は`公開する`チェックボックスを出さず、「保存すると
   公開へ戻ります」という事実だけを示す（`republishing` prop）。

## 検証

- 追加したテスト5件（`generatorSettingsFrom`の復元4件＋既存軸を開いたときに生成フォームが
  その軸の端点を出す統合1件）と、再公開の保存経路2件（`is_published: true`を送ること／
  成功時に中断の通知を出さないこと／調整中はチェックボックスを出さないこと）。
- **直した3点をそれぞれわざと戻して、対応するテストが落ちることを確認した**
  （3件戻して3件失敗／6件は通過）。
- `npx vitest run src/components/AxisStudio` → 19ファイル・189件 pass。`tsc --noEmit` → 0。

## 途中で直した自分の誤り

統合テストのフィクスチャで`shape`を`formula`と書き違えており、**テストは通らなかったが
理由が違った**（折れ点が空扱いになり既定値0/10が返っていた）。値を出力して初めて分かった。
「落ちた＝直すべき欠陥を捕まえた」とは限らない。

## 台帳にあった補足

- 2026-09-19完了。生成フォームの3入力をいまの折れ点から復元し[generatorSettingsFrom]、中断通知の判定をcloseComposerの引数へ移し、調整中は公開チェックを出さず事実だけを示す形にした。直した3点をわざと戻して対応するテストが落ちることを確認済み
