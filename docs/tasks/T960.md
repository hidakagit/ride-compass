# T960: 材料の導出SQLに、人が書いた期待値のテストが無い

状態: 未着手（2026-09-19起票）

## 背景

材料の導出は2か所にある——`material_catalog.py`のPython extractor（探索）と
`osm_way_tag_sql.py`＋タイルSQL（地図配信・材料カバレッジ集計）。

**期待値のテストはPython側にしかない**（`tests/test_material_catalog.py`）。SQL側の式を
検証するテストは無く、地図が配っている色は検証されていない導出から出ている。

本番の1タイル（42,134区間）で手作業で突き合わせたところ、両方が実装する材料は
すべて一致した（[T955](T955.md)）。ただしこれは手作業で、CIは何も見ていない。

詳細はこのファイルへ追記する。
