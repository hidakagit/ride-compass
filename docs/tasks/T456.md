# T456. AxisComposer.tsxのKeep List閾値文言「5つ目のshape種到達」がT396/T397後のモデルと不整合

状態: 完了（2026-08-31）

- 背景: `複雑度平衡性レビュー(正しい設計で全体実施)`
  （`.claude/commands/review/history/2026-08-31_complexity.md`F-2）の指摘。
  `context.md`「意図的な設計判断」（Keep List）のAxisComposer.tsxエントリは
  T355（2026-08-27）で「1,400行到達 または 5つ目のshape種到達」という閾値を登録した。
  当時の`ShapeKind`は4種（`breakpoint_linear`/`recipe_then_breakpoint_linear`/
  `categorical`/`flag_sum`）だったが、T396/T397（2026-08-30、4テンプレート→
  2プリミティブへの再設計）により現在の`ShapeKind`は3種（`flag_sum`撤去）しかない
  （`frontend/src/components/AxisStudio/AxisComposer.tsx:33`）。
- Impact: 現在の分類だと「5つ目のshape種」は本来「3種→5種で+2種」を意味するはずが、
  文言だけ見ると「4種→5種で+1種」の含意にも読める。実害は無いが、次回complexityレビュー
  等でこの閾値の発火判定をする際に読み手が混乱し誤判定するおそれがある。
- 対応方針: `context.md`のAxisComposer.tsxエントリと`docs/tasks/T355.md:27`
  （「閾値は『1,400行到達 または 5つ目のshape種到達』を提案値とする」）を、
  「現在のShapeKindは3種、閾値は『1,400行到達 または 4つ目のshape種到達』」へ更新する。
- Confidence: High（コード実測＋T355.mdの一次記録で裏付け）。
- 規模S。

## 実施記録（2026-08-31完了）

- **確認**: `context.md`自体は実数の閾値を持たず（「現在有効な閾値・発火状況は
  improvement-plan.mdのT355...を正として参照する」形式）、実際の閾値文言は
  `docs/tasks/T355.md`の「実施内容」節にのみ存在することを確認した。対応方針が
  「context.mdとT355.mdの両方を更新する」としていたが、context.md側は更新不要
  （実数値を持たない設計のため）と判断した。
- **対応**: `T355.md`の「実施内容」節末尾に訂正の追記を追加した（過去の記録本文は
  書き換えず、この場合限りの正しい読み替えを追記する既存の運用方針に従う）。
  「5つ目のshape種到達」（当時のShapeKind4種を前提にした「+1種で発火」という意図）を、
  現在のShapeKind3種（`AxisComposer.tsx:33`、`flag_sum`撤去）を前提にした同じ意図
  （+1種）である「4つ目のshape種到達」へ読み替える旨を明記した。行数閾値（1,400行）は
  変更なし。
