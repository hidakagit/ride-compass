# T448. T427.mdのFinding 1（サイドバー4分類記述）が結果的に解消済みなのに未更新のまま半陳腐化

状態: 完了（2026-08-31）

- 背景: `統合レビュー第2回`（`.claude/commands/review/history/2026-08-30_all_2.md`P2-4）の
  指摘。`docs/tasks/T427.md`のFinding 1（architecture.mdのサイドバー4分類記述）は、
  無関係なT413/T418の変更で結果的に解消済みだが、T427.md自体は未更新のまま残っている。
  Finding 2（軸スタジオテンプレート数の矛盾）は未解決のまま残る。
- 対応方針: T427.mdのFinding 1を「T413/T418で結果的に解消済み」と追記して更新する。
  Finding 2（テンプレート数の矛盾）は引き続きT427の対応スコープとして残し、実際に
  解消してからT427自体をクローズする。
- Confidence: High（docsシャードがT413/T418の変更内容とarchitecture.mdの現状を突き合わせ
  確認）。
- 規模S。

## 実施記録（2026-08-31完了）

- **確認**: `docs/architecture.md`を実際にgrepし、Finding 1が指摘していた「サイドバーは
  T406のスコープ外として観測/推定の2区分のまま」という記述は本文中に**もう存在しない**
  ことを確認した（統合レビュー2026-08-30以降、モジュール設計書整備等の並行作業で
  architecture.mdが広く書き換わった過程で、この誤った記述自体が置き換わっていた）。
  現在は「サイドバー...も改善計画T413で同じ`mapOverlayGroupFor`を単一ソースとして使うよう
  統一済み」（2347行目付近）と正しく記述されている。
- **Finding 2は未解決のまま残存**: `docs/architecture.md`の別箇所（現在の行番号で754行目
  付近「4種のshape kind...は変わらず」・1524行目以降「2プリミティブ化」「3カードへ
  再設計」）で、指摘された矛盾は依然として現存することも確認した。
- **対応**: [T427.md](T427.md)へ、Finding 1解消済み・Finding 2未解決という状況を追記した
  （行番号は現状に合わせて更新）。T427自体はFinding 2が残るため未完了のまま。
