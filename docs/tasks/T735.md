# T735. 契約不一致の一括是正（docstring・型・docsが述べる前提の不成立） 規模M

状態: 完了（2026-09-12。名前が挙がっていた4件＋導出で見つけた2件を是正。件数を追うのは打ち切った——[T752](T752.md)参照。起票は2026-09-11、統合レビュー第7回 P2束ね）

## 背景

対象9件。`strict_model.py`の適用範囲宣言（DBのJSONB・Redis永続データが例外）、`hard_filters.py`の「2箇所だけ」、`axis_templates.py`が存在しない関数を共有相手と名指し、Draft保持の契約ほか。

詳細な指摘（Evidence・Impact・Root Cause・Recommendation）は[history/2026-09-11_all.md](../../.claude/commands/review/history/2026-09-11_all.md)を参照。

## 是正した4件（2026-09-12）

| 箇所 | 述べていた前提 | 実際 |
|---|---|---|
| `strict_model.py` | 提供側のペイロードがそのままモデルへ流れ込む経路は無い | 外部APIについては正しいが、**自前で書いた過去のデータは流れ込む**（`shape_params`のJSONB・軸定義スナップショット）。`forbid`でフィールドを消すと既存の行が読めず起動に失敗する（[T396](T396.md)で本番障害の実績）。意図した挙動である旨と、本番DBの移行を先に済ませる必要を明記した |
| `hard_filters.py` | フィルタを増やすときに書き換えるのはこの2箇所だけ | highway種別のフィルタにしか当てはまらない。タグ由来は`is_edge_allowed`へ判定を1本書く必要がある（`no_bicycle`が唯一の実例）。種類ごとに書き分けた |
| `axis_templates.py` | `domain/night.py`の各`*_difficulty`関数と実装を共有する | その関数群は存在しない（`night.py`は`night_materials`のみ）。実在する共有の形（スカラー経路`evaluate_axis_scalar`とベクトル化経路`evaluate_axis_array`）へ書き換えた |
| `curveDistributionOverlay.ts` | 範囲内の棒と範囲外の割合は同じ範囲定義に従う | `visibleBars`だけが`xMax <= xMin`で早期returnし、`offRangeShare`が分岐していなかった。`overlap(-∞, xMin)`と`overlap(xMax, ∞)`が同じ区間を二重に数え、合計が1を超える |

## 残り5件: 件数を追うのをやめ、同じ型を導出した

第7回はP2/P3の個票をファイルへ残していない（[T752](T752.md)）。全体の要約に名前が
挙がったのは上の4件と「Draft保持の契約」だけで、後者は`axisDraft.ts`を読むと
`PASSTHROUGH_PAYLOAD_KEYS`と`_PayloadKeyCoverage`（型レベルの網羅検査）で守られており、
かつモジュール冒頭の「今は選ばれていないkindの入力値も保持する」も成立している
（`breakpoints`・`categoricalMaterial`・`trueScore`等がkindごとに分かれず平坦に並ぶため、
kindを切り替えても値が消えない）。破れている契約を特定できなかった。

復元できない件数を待つのをやめ、**この型が成立する条件から母集団を導出した**
（[T740](T740.md)〜[T742](T742.md)と同じやり方、[design-principles.md](../design-principles.md)構造仕様12）。

| 導出した母集団 | 件数 | 結果 |
|---|---|---|
| コメント・docstringがバッククォートで名指しする識別子の実在 | 全件 | 実在しないもの0件（`axis_templates.py`の`*_difficulty`はワイルドカード表記のためこの走査では拾えず、個別に確認して直した） |
| 「N箇所」と数を主張するコメント（N≥2で、場所を数え上げているもの） | 検証可能な主張4件 | **2件が実際と違っていた**（下記） |
| コメントが名指しする識別子を「定義の有無」で判定する案 | — | 誤検知が多すぎて使えない。DBのテーブル名・外部ライブラリ名・JSONのキー・組み込み型（`None`・`dict`）まで拾い、329出現/185トークン。見送った |

### 導出で見つけた2件

| 箇所 | 述べていた前提 | 実際 |
|---|---|---|
| `jma_tile_content.py` | 空判定が必要なのは`jma_tile_redis_cache.py`と`jma_tile_index.py`の2箇所 | `jma_tile_index.py`は`__all__`へ再exportしているだけで呼んでいない。実際の2つ目の呼び出し元は`jma_tile_prewarm_service.py`（indexを経由してimportしていた）。docstringを実際の消費者へ直し、prewarmは正準の置き場所から直接importする形にして再exportを外した |
| `axis_definitions.py: evaluate_axes_scalar` | 共有相手は`compute_edge_axis_scores`/`axis_inspector_breakdown`［domain/evaluation.py］・`evaluate_axis_difficulties`［domain/difficulty.py］ | `axis_inspector_breakdown`は`domain/axis_inspector.py`にある。ファイルの帰属が違っていた（3箇所という数自体は合っている） |

## 検証結果

- 「N箇所」の主張4件を1件ずつgrepで突き合わせた（`is_empty_tile`の呼び出し元・
  `refresh_axis_definitions`の呼び出し元・`evaluate_axes_scalar`の呼び出し元）。
  `axis_registry_service.py`の「更新タイミングは以下の2箇所のみ」は、起動時と管理API書き込み
  直後という**タイミングの種類**を2つ挙げており（呼び出し箇所の数ではない）、成立している。
- 再exportを外した影響: `is_empty_tile`のimport元を`jma_tile_content`へ揃えた
  （`jma_tile_prewarm_service.py`・`test_jma_tile_index.py`）。
- `pytest -q -m "not postgis"` → 1,828 passed。`ruff check app scripts benchmarks tests` → All checks passed。
- `python scripts/review_checks.py docs` → 違反なし。
