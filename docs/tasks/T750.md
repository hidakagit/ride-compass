# T750. 修正の原則を明文化し、完了前チェックの自己申告分を畳む

規模M。
状態: 未着手

## 背景

レビューで出た指摘を直すと、その修正自体が次のレビューで新たな指摘になる、という循環が
続いている。第7回統合レビューで見直した対象コミットの47%が前回レビュー指摘の修正分であり、
指摘密度（実装1,000行あたり）は24.6→26.2でほぼ横ばい、P0+P1密度は1.63→2.45へ上がった。

同時に、完了前の自己申告チェックと機械強制チェックの守られ方に差が出ている。
`scripts/review_checks.py`の検知器が持つ項目（新規実装ファイルのdocs記載漏れ・
ソースコードの経緯コメント）は実測0件である一方、CLAUDE.mdが文章で指示している
自己申告項目は破られている（共有パターン変更時の全消費者grep・タスク`[x]`化時の
残項目起票）。にもかかわらず再発のたびに自己申告項目を1つずつ足してきたため、
CLAUDE.mdの当該節は47行まで伸びた。

## 対応方針

（このファイルは採番を確保するためのスタブ。詳細は次のコミットで書き足す）
