# T692. Edge単位の材料をway単位でも見られるようにする 規模M

状態: 完了（2026-09-10、[T691](T691.md)より分離）

## 背景

[T691](T691.md)で足した`curvature_deg_per_km`はEdge（`road_edges`）単位の材料で、
ルート探索とルート結果には効いている。一方、**way（`osm_raw_ways`）単位で材料を組む
経路には出てこない**:

- 区間インスペクタ（クリックした道の材料一覧）に蛇行が出ない
- 軸スタジオの分布プレビューで蛇行の値域・折れ点の効きを確認できない
- 地図タイルへ焼けないため、蛇行軸は地図レイヤーを持てない（`tile_property=None`）

3つとも原因は同じ「wayに対応する値が無い」で、way単位の集計（そのwayに属するEdgeの
延長加重平均）を1つ用意すれば同時に解ける。

## 保留した場合に何が起きるか

- Edge単位の材料を今後増やすたびに同じ3つの穴が開き、軸スタジオで折れ点を較正できない
  軸が積み上がる（較正できない軸は[T686](T686.md)の分布プレビューの外側に置かれる）
- 蛇行軸は「ルートを選ぶ基準にはなるが、地図でどの道が蛇行しているか見えない」状態が続く
- 区間インスペクタの材料一覧が「軸に効いている材料の一部しか映さない」ものになり、
  なぜこの区間の得点が高いのかを画面から追えない

## 実測（本番、2026-09-10）

抽選4,031way・784kmで確認した:

- **road_edgesは本番のwayの99.5%（延長でも99.5%）を覆う**。ただし`road_edges`はルート生成時に
  遅延構築される派生データで、母集団としての保証が無い（`WayAttributeCountsRow`の
  docstringが同じ理由でway単位の事前集計を選んでいる）。way単位の値はEdgeの集計ではなく
  **`osm_raw_ways.geom`から直接測る**。
- way形状から測った値とEdge集計値の差（延長加重）は p50 +1・p75 +20・p90 +69・p99 +399 度/km。
  差が20度/km未満の延長が75.5%。wayの折れ線はEdgeを切り出す交差点の頂点も含むため、
  way単位の値は常にEdge単位の延長加重平均以上になる。

## 先に直す: 同じ列に2つの定義が残っていた

[T691](T691.md)で入れた蛇行には、頂点2点の折れ線の扱いにPythonとSQLの食い違いがある。

| | 頂点2点の折れ線 |
|---|---|
| `domain/geo.py: curvature_deg_per_km` | `None`（測れない） |
| `precompute_edge_curvature.py`のSQL | `0.0`（`COALESCE(total.deg, 0)`） |

本番のroad_edgesは**行数の46%・延長の27.6%が2頂点**で、現在は全件0.0が入っている。
再split（PBF再取込）でこの延長が「0点＝最良」から「材料欠損＝軸が評価不能」へ反転する。

2点の折れ線は幾何的に直線であり、方位変化の合計は0と測れる。「曲がりが無い」と
「測れない」を混同していたのはPython側で、`< 3`の門を`< 2`へ下げる。距離0は従来どおりNone。
[T691](T691.md)のSQL↔Python一致テストは3頂点のwayしか通していなかったため素通りしていた。

## 対応方針

### way単位の蛇行を持つ（`way_geometry`）

`way_landcover`・`way_attribute_counts`と同じ「way単位の事前集計テーブル」の流儀に揃える。
母集団は`osm_raw_ways`全域で`road_edges`に依存しない。行が無い＝未計算、
列がNULL＝算出不能（頂点1点・長さ0）。

蛇行を測るSQLはEdge版とway版で同一のため、`road_graph_repository.py`へ1つ置いて
両方から使う（テーブル・列名だけを差し替える）。既存のprecomputeバッチが
「SQLはリポジトリに置き、バッチは呼ぶだけ」という規約なのに、`precompute_edge_curvature.py`
だけが自前のSQLを持っていたのを揃える。

### 3つの消費者への配線

| 消費者 | 変更 |
|---|---|
| 区間インスペクタ | way取得SQLへ`way_geometry`をLEFT JOIN、`way_scalar_materials`へ渡す |
| 分布プレビュー | `_SAMPLE_WAY_MATERIALS_SQL`へ同上 |
| 地図タイル | `_ROAD_SURFACE_TILE_MVT_SQL`へ同上、`curvature_deg_per_km`を焼く |

タイルへ焼くと`MaterialSpec.tile_property`を設定でき、`derive_ramp_inputs`が通って
**蛇行軸は自動で地図レンズを得る**（`axis_display_for`が`kind="ramp"`を返すため、
フロントエンドの変更は不要）。0はNULLIFでキーを省く（既存のaccident_per_km等と同じ流儀、
フロントは欠損を0として読む）。

`way_scalar_materials`の引数は材料ごとに増え続ける形のまま残る（土地被覆の2列に続いて
蛇行で3つ目）。まとめて渡す形への整理はここでは行わない。

## 実施内容

### `domain/geo.py`: 頂点2点は0

`< 3`の門を`< 2`へ下げ、2点の折れ線（直線）に0を返す。距離0・実質1点はNoneのまま。
`tests/test_precompute_edge_curvature.py`へ2頂点のwayを足し、Python↔SQLの一致を
その形でも固定した（修正を外すと落ちることを確認済み）。

### 測り方のSQLを1箇所へ

`road_graph_repository.py: _curvature_total_cte_sql(source, id_column, geom_column)`が
方位変化の合計を返すCTEを組み立て、Edge単位（`_RECOMPUTE_EDGE_CURVATURE_SQL`）と
Way単位（`_RECOMPUTE_WAY_CURVATURE_SQL`）の両方が使う。`precompute_edge_curvature.py`が
自前でSQLを持っていたのを、他のprecomputeバッチと同じ「SQLはリポジトリ、バッチは呼ぶだけ」
へ揃えた。

### `way_geometry`（migration 0036）とバッチ

| | |
|---|---|
| テーブル | `way_geometry`（osm_way_id主キー、`curvature_deg_per_km`、系譜追跡3列） |
| バッチ | `app/batch/precompute_way_curvature.py`（2万件ずつUPSERT） |
| 母集団 | `osm_raw_ways`（geom非NULL全件）。`road_edges`を一切見ない |
| 再構築 | `refresh_derived.py`の⑪段として組み込み済み（PBF再取込で自動的に追随する） |

### 3つの消費者

| 消費者 | 経路 |
|---|---|
| 区間インスペクタ | `get_way_curvature` → `axis_inspector_breakdown` → `way_scalar_materials` |
| 分布プレビュー | `_SAMPLE_WAY_MATERIALS_SQL`のLEFT JOIN → 同上 |
| 地図タイル | `_ROAD_SURFACE_TILE_MVT_SQL`が`curvature_deg_per_km`を焼く |

材料の`tile_property`を`"curvature_deg_per_km"`にしたことで`derive_ramp_inputs`が通り、
**蛇行軸は地図レンズを自動で得る**（`axis_display_for`が`kind="ramp"`を返す。
フロントエンドは1行も変えていない）。`tests/test_axis_display.py`が固定する。

材料カバレッジ（`material_coverage.py`）はEdge側からWay側（`way_geometry`）へ移した。
母集団が`osm_raw_ways`全域のWay側で数える方が実態に合う（`trees_percent`が
`way_landcover`を見るのと同じ）。

## 本番反映の順序

タイルは世代番号ごとにキャッシュされるため、**バッチを流す前に焼いたタイルは
「全部まっすぐ」のまま世代22に残る**。そこで:

1. push（デプロイでmigration 0036が自動適用され、`way_geometry`は空のまま）
2. `precompute_way_curvature.py`を本番で実行
3. `ROAD_SURFACE_TILE_VERSION`を22→23へ上げて再デプロイ（空だった間のタイルを捨てる）

## 本番反映（2026-09-10）

1. デプロイ `0fe64abe`（migration 0036が自動適用され、`way_geometry`は空）
2. `precompute_way_curvature.py`を実行: **1,329,632件を75.9秒**（2万件ずつ67回）。
   充填100%（NULL 0件）、25.6%が0（頂点2点の直線）、本数ベースで中央値85・p90が962・
   p99が2367度/km
3. `ROAD_SURFACE_TILE_VERSION`を22→23（空だった間に焼いたタイルを捨てる）

## 本番での確認（3つの消費者すべて）

| 消費者 | 確認内容 |
|---|---|
| 区間インスペクタ | osm_way_id 159758713（735.7度/km）で`curvature`が`available=true`・83.5点 |
| 分布プレビュー | 抽選20,000way・3,806kmで延長加重 p50 149・p75 475・p90 934・p99 1908度/km |
| 地図タイル | z14/14557/6422で200featureのうち170が`curvature_deg_per_km`を持つ（残り30は0でキー省略）。対象wayは735.7 |
| 地図レンズ | `GET /api/axis-catalog`の蛇行が`display.kind="ramp"`、しきい値`[100, 300, 600, 1000]` |

[T691](T691.md)がEdge単位で測った延長加重分布（p50 95・p75 453・p90 919）と比べると、
way単位はp50だけ高くp75以降はほぼ一致する。同じ折れ点をそのまま当てても色分けの粒度は
保たれる（80〜100点に入る延長は約13%で、[T686](T686.md)の警告閾値50%に余裕がある）。
