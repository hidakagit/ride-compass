# T709. コード自身が述べる契約と実装の不一致を是正する 規模M

状態: 未着手（2026-09-10起票、統合レビュー第6回 P2/P3の束ねC。対象18件）

## 背景

docstring・コメント・型が述べる前提条件が、実装で強制・検証されていない箇所が18件。
consistency.mdが挙げる3パターン（①無条件アクセス ②兄弟の一部にしか無いバリデーション
③コメントが主張する挙動と実装が逆・不完全）のうち、②③が中心。

## 対象

### 実装と逆のことを述べている

- `domain/graph.py:46`（**新規追加コメント**）「Noneは『測れない』（頂点3点未満・距離0）で0ではない」
  → `domain/geo.py:73-76`は「頂点2点の折れ線は直線であり、曲がりが無いという測定結果として**0**を返す」と明記し、
  `tests/test_precompute_edge_curvature.py`が両側で0を固定している。
  **この本文を根拠にPython側かSQL側を「修正」すると、欠損と0の取り違えを実際に作り込む。**
- `road_graph_models.py:189-191,244-246`（**新規追加コメント**）「`poi_counts`の値を足し合わせると
  `stop_count`に一致する」→ `get_poi_counts_by_kind`のdocstring（`:2124-2126`）は
  「したがって両者の合計は一致しない」、`docs/modules/backend/static-road-attributes.md:148`も
  「**合計も一致しない**」と明記。T655段階2では成り立ったがT655段階3で成立しなくなった。
- `road_graph_repository.py:2336-2337` — 「`poi_counts`はNOT NULL DEFAULT '{}'のため行があれば必ず辞書」
  → migration 0034が**まさにそのNOT NULLとDEFAULTを落とし**既存の`'{}'`をNULLへ書き換えている。
  実装（`:2392`）もORM定義（`nullable=True`）もNULL前提で、docstringだけが廃止済みの制約を語る。
- `page.tsx:866`（**新規追加行として書き換えられている**）「軸スタジオ由来のレイヤーだけは
  1つだけ選べる状態を保つ」→ 排他ロジックは同じdiffで削除済み（対応テスト2件も削除）。
  `docs/modules/frontend/static-map-layers.md:157`にも同じ主張が残る。
- `page.tsx:840` — `mapLayers`の宣言位置の根拠を「`handleLayerToggle`が`mapLayers`全体を参照するため」と
  説明するが、現行の`handleLayerToggle`は`mapLayers`を一切参照せず依存配列からも外れている。
- `MapView.tsx:1064-1067` — 「`spec.*.colorExpression`等は呼び出し元が動的なしきい値から
  組み立てることがある」→ 呼び出し元2箇所とも常にモジュール定数を渡すだけで動的経路は現存しない。
- `MapLayersPanel.tsx:305-306`（**新規追加行**）「標準構成（panelHint＋OFF案内＋絞り込み軸）」→
  `renderStandardSectionBody`（`:273-281`）は`panelHint`を一切描画しない。
- `MapOverlayControls.tsx:918-921` — 「専用レイヤーを持たない軸はundefinedのまま渡す」→
  唯一の呼び出し元（`:1110-1117`）は常に`layerId: member.id`を渡し、`layerId?`のoptional性・
  `toggleHidden`のガードは**到達不能な分岐**。挙げられている軸は既に地図UIから撤去済み。
- `recipeControls.tsx:83,96-98` — `FieldLabel`が「開閉状態も自身が持つ」→ `InfoPopover.tsx:42`へ移設済み。
- `hooks/usePolledFetch.ts:13-16,45,63-66` — `hasFetched`は「`enabled: false`の間はfalseのまま」→
  一度trueになると`enabled`がfalseへ戻ってもリセットしない。
  `deriveFetchLayerStatus`がこれを「取りに行った結果、値が無かった」として扱うため、
  レイヤーを消しただけの状態が「データなし」と表示されうる。
- `MaterialCoveragePanel.tsx:42-43`（**新規追加コメント**）「Recordで全semanticsを要求する
  （…黙って消える）」→ `missing_semantics`がnullの経路は今も塞がっていない
  （`:155`のfilterがnullをどのグループにも入れない）。

### 兄弟の一部にしか無いガード・二重定義

- `RouteSettingsPanel.tsx:301,427,429` — `DEFAULT_HARD_FILTERS`を生成物から導出する形に直したのに、
  `hardFilterCustomized`と表示側・トグルは既定値を`true`固定でハードコード。
  backendが既定OFFのフィルタを追加すると、何も操作していないのに「変更あり」バッジが出る。
- `BackendLogsPanel.tsx:48-59` — 追加された`.catch()`はPromiseの拒否しか捕まえない。
  Clipboard APIは`[SecureContext]`のため非セキュアコンテキストでは`navigator.clipboard`自体が
  undefinedになり**同期TypeErrorを投げcatchに入らない**——コメントが対策したと述べる当のケースで
  ボタンが無反応になる。catchの書き込み先が`error` stateなのも誤解を招く（「取得失敗:」と表示される）。
- `dependencies.py:268-276` — 新設の`get_road_graph_repository`が`get_session_factory()`
  （command_timeout=20、タイル配信と共有）を使うが、利用者2件はいずれも全表走査寄りの管理APIで、
  同種の管理APIは明示的に180秒の`get_route_generation_session_factory()`を選んでいる。
- `axis_display.py:240` — `raw_value_unit`が「単位一致・重みが正・2項以上ならadditive」の3条件のみで
  **重みの大きさを見ない**。生値は`Σ(材料値×weight)`なのでweight≠1だと材料の単位のスケールでなくなる。
  本番の公開軸`axis_abdaded0be20`は`signal×1.0 + level_crossing×1.5 + stop×0.3 + crossing×0.0`で
  「回/km」を返すが、**実際に1kmあたりに存在する停止要因の回数ではない**
  （`tests/test_axis_display.py:737`がこの挙動を期待値として固定している）。
- `road_graph_repository.py:2607-2610` — `AttributeRepository.get_poi_counts_by_kind`の
  第3引数`on_edge_tolerance_m`をファサードが受け取らず既定値で固定して委譲している。
- `road_graph_repository.py:2185-2199` — `sample_way_rows`がSQLAlchemyの生`Row`を`list[Any]`のまま
  infrastructureの外へ返し、`axis_preview_service.py:61-83`が列別名で直接属性アクセスする。
  他の読み出しは全てドメイン型へ詰め替えており、ここだけ流儀が崩れている。
- `domain/material_catalog.py:104-107` — `tile_property_direction_dependent`の説明が
  「現行MATERIAL_CATALOGに該当する材料は無い…将来の型的な安全弁」と述べるが、
  同ファイルの`gradient_percent`（`:549`）と`wind_drag_ratio`（`:569`）が既に`True`を設定している。
