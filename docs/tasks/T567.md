# T567: 既存ソースコードの経緯コメント一掃（モジュール単位で分割）

状態: 未完了（一部着手。2026-09-03起票、T566完了により2026-09-05着手開始。
15モジュール中14モジュール完了、残り1モジュール）

## 背景

[T566](T566.md)調査時点で、backend/frontend合わせて`改善計画T[0-9]+`だけで2,844件・373ファイル
（他に「以前は」289件・「実機フィードバック等」212件・「ユーザー要望/指摘/判断/報告」185件、
重複ヒットあり）の経緯コメントが確認された。過去の「ゼロベース網羅レビュー」
（[T425](T425.md)/[T426](T426.md)→T463〜T471）と同様、`docs/modules/README.md`の15モジュール
単位で分割し、各ファイルを触る前に対応する`docs/modules/*.md`を読んでから[T566](T566.md)の
判定基準（設計書と重複＝削除／非自明な現在の理由＝現在形で残す／日記的記述のみ＝削除）で
仕分ける。

## 対応方針・分割単位（2026-09-05追記）

- `docs/modules/README.md`のbackend8・frontend7の全15モジュールを単位に、1モジュールずつ
  片付ける。T425/T426→T463〜T471のような子タスク（個別Txxx）は起票しない——各モジュールの
  作業内容はコメントの要否判定という同一パターンの繰り返しで、実際の規模もモジュールごとに
  大きくばらつく（今回のelevation.mdは対象3ファイルのみで小規模）ため、進捗はこのファイルの
  チェックリストで管理し、1モジュール完了ごとに本ファイルへ実施メモを追記する形にする。
- 着手前に`python scripts/review_checks.py docs`のフルスキャン結果（ソースコードの経緯
  コメント参考件数）を対象モジュールの所有ファイル（各`docs/modules/*.md`の「対象ファイル」表）
  で絞り込み、対象を洗い出す（NARRATIVE_PATTERNは`#`コメントのみ対象のため、docstring内の
  経緯記述は各ファイルを実際に読んで手動でも確認する）。
- 進捗チェックリスト（backend8・frontend7）。着手順はREADME.mdの掲載順ではなく、
  対象ファイルのソースコード経緯コメント推定件数が小さいモジュールから片付ける
  （小さく完結させて進捗を積み上げる方針。件数は`python scripts/review_checks.py docs`の
  フルスキャン結果を各モジュールの「対象ファイル」表で按分した概算）:
  - [x] 標高（backend/elevation.md、推定3件） — 2026-09-05完了
  - [x] 横断基盤（backend/cross-cutting-infrastructure.md、推定89件） — 2026-09-05完了
  - [x] 気象・動的レイヤー（backend/weather-dynamic-layers.md、推定127件） — 2026-09-05完了
    （動的材料・way_id値配信より先に着手した。理由: 並行セッションがdynamic-way-values.md
    領域[風/勾配road-value、T606]で作業中だったため、ファイル衝突を避けて順序を入れ替えた）
  - [x] 動的材料・way_id値配信（backend/dynamic-way-values.md） — 2026-09-05完了
  - [x] 静的道路属性・タイル配信（backend/static-road-attributes.md、推定113件） —
    2026-09-05完了
  - [x] 評価・スコアリング（backend/evaluation-scoring.md、推定152件） — 2026-09-05完了
  - [x] ルート生成エンジン・経路探索（backend/routing-engine.md、推定434件・19ファイル） —
    2026-09-05完了（着手当時、road_graph_repository.pyの動的材料配信SQL部分
    [`_WAY_IDS_IN_TILE_SQL`/`_WAY_GRADIENT_INPUTS_IN_TILE_SQL`まわり]は
    dynamic-way-values.md管轄と見て対象外・未変更のまま残置していたが、後日
    dynamic-way-values.md自体のdocs/modules記載を確認したところ
    「`get_way_gradient_inputs_in_tile`・`get_way_ids_in_tile`はrouting-engine.mdが
    主管するファイルに属する」と明記されていたため、これらの残りコメントは
    dynamic-way-values.mdモジュール着手時にまとめて一掃した。詳細は下記
    「動的材料・way_id値配信モジュール」の実施メモ参照。19ファイル中17ファイルに実際の
    ヒットがあり（`osm_way_tag_sql.py`・`errors.py`はヒット無し）、ファイル単位で
    個別コミット・push した）
  - [x] 軸スタジオ・評価軸定義（backend/axis-studio.md、11ファイル・推定257件） —
    2026-09-05完了（routing-engine.mdと同様の規模のためファイル単位で個別コミット・push
    した。11ファイル全てに実ヒットあり）
  - [x] 軸スタジオ管理画面（frontend/axis-studio.md、対象ファイル14件・実ヒット10件） —
    2026-09-05完了（機械スキャナの推定件数を大幅に超える実ヒットがJSDoc/JSX形式の
    コメントに残っていたことを`AxisComposer.tsx`で発見。詳細・機械スキャナのカバレッジ穴
    自体はT622へ分離起票した）
  - [x] ルート設定・結果パネル（frontend/route-settings-and-results.md、対象14ファイル・
    実ヒット9ファイル） — 2026-09-05完了
  - [x] 地図: 動的気象レイヤー（frontend/dynamic-weather-layers.md、対象約21ファイル・
    実ヒット20ファイル） — 2026-09-05完了。`Map/MapView.tsx`は
    map-axis-coloring.mdと同じ理由（他の複数モジュールと共有する最大級ファイル）で
    対象外とし、page-composition.mdモジュール着手時にまとめて処理する
  - [x] 地図: 静的レイヤー・道路表示（frontend/static-map-layers.md、対象14ファイル・
    実ヒット12ファイル） — 2026-09-05完了。`Map/MapView.tsx`は他モジュールと同じ理由
    （他の複数モジュールと共有する最大級ファイル）で対象外とし、page-composition.md
    モジュール着手時にまとめて処理する
  - [x] 地図: 軸・ルート色分け（frontend/map-axis-coloring.md、対象10ファイル・
    実ヒット9ファイル） — 2026-09-05完了。ただし`Map/MapView.tsx`（windAxis/
    gradientAxis/gradientFill/DETAIL_LAYER_ID関連箇所）は本モジュール以外にも
    dynamic-weather-layers.md/static-map-layers.md/page-composition.mdが同じ
    ファイルの別箇所を管轄しており、3594行・約230件のヒットを抱える最大級のファイルの
    ため断片的に触らず、page-composition.mdモジュール着手時にMapView.tsx全体を
    1回でまとめて処理する方針にした（対象外・未着手のまま残置）
  - [ ] ページ全体構成・状態管理（frontend/page-composition.md、MapView.tsx/page.tsxが
    最大級のためほぼ最後に回す）
  - [x] 開発者/研究者機能（frontend/developer-research-tools.md、対象18ファイル・
    実ヒット11ファイル） — 2026-09-05完了（`app/admin/page.tsx`が対象ファイル表に
    未記載だったため同ドキュメントへ追記した上で本モジュールの対象に含めた）

## 実施メモ（標高モジュール、2026-09-05）

- 対象ファイルは`elevation_client.py`・`elevation_attribute_service.py`・
  `precompute_elevation_attributes.py`の3件（`attributes.py`のElevationAttribute・
  compute_elevation_attribute部分にはNARRATIVE_PATTERNの一致は無かった。同ファイル内の
  他の経緯コメントはEdgeAttributeCounts/EdgeMaterialTable等[別モジュール管轄]のもの）。
- 設計書と重複＝削除: `elevation_client.py`のDEMタイル方式・キャッシュ2段・LRU上限の説明、
  `ElevationAttributeService`の同時実行制御・repository前提の説明等、`docs/modules/backend/
  elevation.md`に既に現在形で書かれている内容は削除した。
- 非自明な現在の理由＝現在形で残す: `_CoverageGap`（一時的通信エラーと404の区別が無いと
  一時障害まで恒久キャッシュされるバグになる）、GSI失敗Edgeの非永続化（再試行を続けさせる
  ため）等は経緯の言い回しだけ落として残した。
- 昇格: `elevation_client.py`のタイル仕様コメントに埋もれていた非自明な事実
  （`dem`は`dem5a/b/c`のGSI側自動フォールバック統合種別ではなく別の粗いデータセットである
  こと、非対応エリアは明示的に404を返すこと、全種別をz=14固定で扱う理由）が
  `docs/modules/backend/elevation.md`に書かれていなかったため、同ドキュメントへ現在の事実
  として追記してから削除した。
- 実害の発見: `elevation_client.py`のLRU上限コメントが「`precompute_elevation_attributes.py`
  はEdgeを地理的に並べ替えず処理する」と書いていたが、実際は`_fetch_all_edge_ids`が
  `ORDER BY geom`で地理的順序を返す実装に変わっており（`docs/modules/backend/elevation.md`
  の記載と一致）、コメントだけが古いまま食い違っていた。経緯コメントが実態に追従されず
  陳腐化する典型例のため、正しい記述に置き換えて削除した。
- 検証: `pytest tests/test_elevation_attribute_service.py tests/test_elevation_client_cache.py
  tests/test_precompute_elevation_attributes.py -q`と`pytest -q -m "not postgis"`（1521件）
  green。コメントのみの変更のため挙動に影響なし。

## 実施メモ（横断基盤モジュール、2026-09-05）

- 対象16ファイル全件（`main.py`・`config.py`・`version.py`・`admin_auth.py`・
  `dependencies.py`の横断的部分[今回は経緯コメント自体が無く未変更]・`health.py`・
  `debug_admin.py`・`database.py`・`migrate.py`・`redis_client.py`・`http_client.py`・
  `rate_limiter.py`[経緯コメント自体が無く未変更]・`request_log.py`・
  `response_compression.py`・`debug_log.py`・`debug_control.py`・`job_registry.py`）を
  精読し、docs/comments.mdの判定基準で仕分けた。
- `config.py`が最大件数（29件）。各設定値の「なぜこの数値か」という技術的根拠
  （DB接続プールの算数、外部APIのレート制限特性等）は非自明な現在の理由として残しつつ、
  「改善計画Txxxで」「以前は〜だった」という経緯の言い回しだけを落とした
  （数値そのものの妥当性は変えていない）。
- 設計書と重複＝削除（大きめの例）: `config.py: road_graph_use_repository`・
  `redis_client.py`のモジュールdocstring・`health.py: db_status`の認可境界説明は、
  `docs/modules/backend/cross-cutting-infrastructure.md`に既に現在形で書かれている
  内容と重複していたため大幅に圧縮し、詳細は同ドキュメントへのポインタへ置き換えた。
- 非自明な現在の理由＝現在形で残す（大きめの例）: `database.py`のSupabase
  Session pooler選定理由（Transaction poolerだとasyncpgのprepared statement命名方式が
  衝突しうる）・command_timeoutの数値根拠（実測値ではなく「観測された最悪値」という
  現在の事実として記述）は、この1ファイルだけが持つ非自明な制約のため、経緯の言い回し
  （「実機で確認した」「実測で」等）だけを落として残した。
- 日記的記述のみ＝削除の例: `health.py`のExternalCallStatsResponse定義理由（過去の型無し
  実装からの移行経緯）、`job_registry.py`の「以前は別の方式と書いていたが参照ミスだった」
  という自己言及的な訂正コメント、`debug_admin.py`/`debug_control.py`のT318本番調査の
  経緯等は、現在のコードを理解・変更するのに不要なため削除した。
- 検証: `pytest -q -m "not postgis"`（1525件、並行セッションのT602/T603分含む）green、
  `python -c "import app.main"`で起動時ログも正常。コメントのみの変更のため挙動に影響なし。

## 実施メモ（気象・動的レイヤーモジュール、2026-09-05）

- 着手直前に並行セッションが本モジュール隣接領域（backend/weather-dynamic-layers.mdの
  JMAタイル/色別標高図タイル404処理、T604/T605）・dynamic-way-values.md領域
  （フロントの風/勾配road-value可視化、T606）へ立て続けにコミットしていたため、
  ファイルレベルで重複していないか都度`git fetch origin master`で確認しながら進めた。
  T604/T605はbackend/weather-dynamic-layers.mdの対象ファイル
  （jma_tile_client.py・gsi_relief_tile_client.py・jma_tile_redis_cache.py）を直接
  変更していたため、着手前にmergeして最新状態を読んでから経緯コメント判定を行った。
- 対象28ファイル全件（domain 9・services 6・infrastructure 13、api routersのうち
  weather.py/jma_tile.py/basemap.py/gsi_relief_tile.pyの4件）を精読し、
  docs/comments.mdの判定基準で仕分けた。weather_client.py（21件）・api/routers/
  weather.py（17件）が特に密度が高かった（Open-Meteo 429対策の変遷が長く、
  「実機報告」「ユーザー要望」形式の経緯が多数残っていた）。
- 設計書と重複＝削除の例: `jma_tile_client.py`のモジュール冒頭コメント（キャッシュ戦略の
  説明）・`jma_tile_redis_cache.py`のモジュールdocstring（T510の移行経緯を含む）は、
  `docs/modules/backend/weather-dynamic-layers.md`「JMAタイル系の共通プロキシ」節に
  既に現在形で書かれている内容と重複していたため大幅に圧縮し、同ドキュメントへの
  ポインタへ置き換えた。
- 非自明な現在の理由＝現在形で残す（大きめの例）: `weather_client.py`の
  `get_forecast_many`が応答を位置対応ではなく座標（latitude/longitude）で対応付ける
  ロジック（Open-Meteoが地点を省略して応答すると位置ズレで誤った地点に割り当たる
  という、現在も成り立つ正しさの根拠）は、「実機報告2026-08-31」という発見の経緯を
  落とし、ロジック自体の説明として残した。`jma_amedas_client.py`のエンドポイント名
  （`amedastable.json`/`latest_time.txt`、仕様書に明記が無く実データでのみ確認できる
  事実）も同様。
- 日記的記述のみ＝削除の例: `night.py`の「旧配列版・旧互換ラッパは削除した」という
  死んだコードの撤去経緯（対象の関数は既に存在しない）、`jma_amedas_client.py`の
  「2026-08-29に2つのURL推測が誤りと判明し修正」という実装ミスの発見経緯、
  `weather_service.py`/`domain/weather.py`の「今日の見通し」パネルの複数段階の
  UI変遷（3コマ→8コマ等）は、現在の実装を理解するのに不要なため削除した。
- 検証: `pytest -q -m "not postgis"`1535件green（並行セッションのT604〜T606分の
  テスト追加を含む）。コメントのみの変更のため挙動に影響なし。

## 実施メモ（静的道路属性・タイル配信モジュール、2026-09-05）

- 対象29ファイルのうち経緯コメントが検出された20ファイルを精読・仕分けた（domain 6・
  services 1・infrastructure 6・api routers 3・batch 8のうちヒットありは各層の一部）。
  `region.py`は`region_dynamic_way_values`（[動的材料・way_id値配信](../modules/backend/dynamic-way-values.md)
  管轄、直近T599が編集）だけ対象外とし、路面/POI/区間インスペクタ部分のみ対応した。
- 設計書と重複＝削除（大きめの例）: `region_service.py`のROAD_SURFACE_TILE_VERSION/
  POI_TILE_VERSIONのv1〜v17逐次changelog（約90行）は、各世代の履歴自体がdocs/modules側の
  現状記述（「MVTプロパティを追加・削除するたびに上げる」）と機能的に重複する日記のため
  丸ごと削除し、現在の運用ルールだけを残した。同種のPOIタイル世代のv1〜v3履歴も
  `vector_tile.py`・`region.py: region_poi_tile`docstring含め同様に削除した。
- 非自明な現在の理由＝現在形で残す（大きめの例）: `match_designations.py`のJOIN条件が
  geographyキャストを避ける理由（PostGISプランナがGiST索引を認識できず長時間無応答になる
  リスク）、ST_Intersectionのgrid_Size指定が必須な理由（GEOS OverlayNGの数値ロバスト性
  問題）は、日付・実測経緯を落として現在の制約として残した。
- 実害の発見: `osm_adapter.py`の`segregated`タグに付いていた「自転車インフラ分類への
  反映はT102の実測結果を待って判断する」は、直後の`lit`タグのコメントが同じT102の
  実測完了・採用推奨をすでに記述しており、T102の実測自体は完了済みなのに`segregated`側の
  コメントだけ「判断待ち」のまま更新されていなかった（docs/static-road-attributes-plan.md
  §2.5で採用可否は決着済み、分類への反映のみ別タスク未着手と確認）。「反映は未実施
  （判断は完了、実装は別タスクで検討）」という正しい現状へ書き換えた。
- 日記的記述のみ＝削除の例: `region.py: region_axis_inspector`の「改善計画T292: 車ストレス
  専用レシピ（旧`/api/region/car-stress-breakdown`）は廃止し統合した」（対象エンドポイントは
  既に存在しない）、`_tile_validation.py`の「region.py/accidents.pyが重複実装していたのを
  切り出した」経緯、`_common.py`モジュールdocstringの「4バッチに独立実装が増殖していた」
  経緯は、現在のコードを理解するのに不要なため削除した。
- 検証: `pytest -q -m "not postgis"`1534件green（T599マージ後）。コメントのみの変更の
  ため挙動に影響なし。

## 実施メモ（評価・スコアリングモジュール、2026-09-05）

- 対象8ファイルのうち経緯コメントが検出された6ファイル（`domain/evaluation.py`65件・
  `domain/material_catalog.py`55件・`api/routers/material_catalog.py`12件・
  `domain/difficulty.py`8件・`services/evaluation_service.py`7件・`domain/recipe.py`5件、
  計152件）を精読・仕分けた。`services/material_coverage_service.py`・
  `infrastructure/material_coverage.py`はヒット無し。
- 設計書と重複＝削除（大きめの例）: `domain/material_catalog.py`のモジュールdocstring
  （材料の「登録」と「評価軸での利用」が独立している旨等、docs/modules/backend/
  evaluation-scoring.md「材料カタログ」節の`MaterialSpec`フィールド表と重複）は
  ドキュメントへのポインタへ圧縮した。
- 非自明な現在の理由＝現在形で残す（大きめの例）: `domain/evaluation.py`の
  `_neumaier_accumulate`（Python 3.12の`sum()`とビット単位で一致させる必要性）・
  `compose_costs_from_axis_matrix`の丸め誤差対策、`domain/material_catalog.py`の
  `gradient_percent`がタイルへ焼き込めない理由（進行方向依存の符号付き値のため）・
  `designation`材料の3値AND条件がCategoricalShapeで表現できない理由は、日付・
  実測経緯・ユーザー指摘等の記述を落として現在の技術的制約として残した。
- 日記的記述のみ＝削除の例: `domain/evaluation.py`の`RoutePreference.with_weight`
  docstringにあった2026-08-25の本番障害の詳細（night軸非公開化で500になった経緯）、
  `domain/material_catalog.py`の削除済み`bicycle_infra`材料（旧classify_bicycle_
  infrastructure由来）に関する経緯段落一式、`domain/recipe.py`の削除済み
  `RoadSuitabilityRecipe`等の専用Pythonレシピ撤去の経緯、`domain/difficulty.py`の
  削除済み`*_difficulty`スカラー互換ラッパの撤去経緯は、対象のコード自体が現存しない
  ため削除した。
- 実害の発見: 無し（このモジュールでは陳腐化コメントは検出されなかった）。
- 検証: `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に
  影響なし。

## 実施メモ（ルート生成エンジン・経路探索モジュール、2026-09-05）

- 本モジュールは対象19ファイル中17ファイルにヒットがあり、合計推定434件と、
  ここまでの4モジュール合計（約544件）に迫る規模と判明した（着手前のサイズ見積りが
  外れた実例）。1ファイルずつ完了するたびにコミットし、モジュール全体の完了は
  複数コミットに分割する（他モジュールとの違い、`docs/improvement-plan.md`のT567行は
  モジュール完了まで更新しない）。
- `road_graph_engine.py`（104件、対象19ファイル中最大）完了。モジュールdocstring
  冒頭の「設計上の重要な決定」節（T531/T218a/T529→T536/T537/T522等の最適化の変遷を
  実測値付きで詳述、約80行）は、docs/modules/backend/routing-engine.mdが既に現在形で
  ほぼ同内容を記載しているため、現在の設計の骨子だけを残し大幅に圧縮した
  （フロンティア方式・静的スコア行列・タイル集合キーキャッシュ等の「何を・なぜ」は
  残し、T番号付きの変遷そのものは削除）。`_build_node_pair_index`（既に撤去済みの
  関数）への言及、`旧routable_node_ids`等の削除済み識別子への言及も同様に削除した。
  検証: `pytest tests/test_road_graph_engine.py -q`91件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `road_graph_repository.py`（103件、対象19ファイル中2番目）完了。ただし
  `_WAY_IDS_IN_TILE_SQL`・`_WAY_GRADIENT_INPUTS_IN_TILE_SQL`（way_id→wind_penalty/勾配
  配信層）まわりのコメントは意図的に対象外とした（dynamic-way-values.mdが管轄、並行
  セッションが作業中のため衝突回避）。主な削除・書き換え: `_ROAD_SURFACE_TILE_MVT_SQL`
  冒頭の約70行の設計コメントからT番号・「以前は」history framingを落としつつ
  ST_AsMVT採用理由・car_stress/lit/accident_per_kmの各材料の現在の依存関係は残した
  （`bicycle_infra`・`cycleway_class`・`car_stress_breakdown`等、既に削除済みの
  プロパティ/関数への言及は削除）、`get_way_specs_with_closure`docstringの旧GIN検索
  実装との比較history（約15行）を削除し現在のST_Intersectsベース実装の説明に置き換え、
  `get_edge_materials_batch`docstringの「以前は個別5クエリだった」history削除（6.4倍の
  ベンチマーク数値自体は残す）。`get_surface_attributes`の削除済みsurface_attributes
  テーブルとの比較ベンチマーク（陳腐化した数値）は削除。
  - 陳腐化コメントの発見: `get_graph_in_bbox`のコメントが「ST_X/ST_Y最適化は未適用の
    ままだった」と書いていたが、直後のコード（`func.ST_X`/`func.ST_Y`）は既に適用済みで
    食い違っていた。elevation_client.pyに続き本モジュール2件目の陳腐化コメント。
    正しい現状に置き換えた。
  検証: `pytest tests/test_road_graph_repository.py -q`（PostGIS要のため95件skip）、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `graph_service.py`（50件、対象19ファイル中3番目）完了。主な削除・書き換え:
  モジュール冒頭の温めセマフォ/クールダウンのコメントからT番号・本番実測秒数を落とし
  現在の設計意図のみ残した、`GraphService`クラスdocstringから「地図表示で
  road_nodes/road_edgesが永遠に空のままだった問題」history部分を削除し現在の呼び出し
  関係のみ記述、`get_or_build_graph_with_attributes`のbuild_road_graph重さの説明から
  過去障害再現の経緯（T105参照）を削除しCPU専有・ヘルスチェック無応答という現在の
  技術的理由のみ残した（ベンチマーク数値4kmで1.1秒は残す）、`get_search_materials_for_bbox`
  ・`_build_search_materials_from_tile_cache`・`_get_or_build_tile_materials`等の
  docstring/コメントからT219/T222/T229/T248/T261/T262/T264/T390/T391/T469/T531/T536/
  T537/T538/T546/T59/T9等のタグと「以前は`dict.update`で即座に復元していたが」history
  framingを落とし、現在の依存関係・パフォーマンス上の理由（6.4倍のベンチマーク数値は
  残す）はそのまま残した。
  検証: `pytest tests/test_graph_service.py -q`32件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `routing.py`（32件、対象19ファイル中4番目）完了。探索アルゴリズム自体を扱う
  domain層の中核ファイルで、ベンチマーク数値・技術的トレードオフの記述が多い。
  モジュールdocstringのNetworkX→scipy→rustworkxという実装移行史（T220/T529/T536の
  変遷）を削除し、「2点間探索はrustworkxのA*・一対全探索はscipy Dijkstra」という
  現在の構成とその理由（rustworkxが前任者木を返さない制約）のみ残した。各docstring
  からT219/T256/T363/T463/T529/T531/T536/T551/T557/T568/T569/T602/T12 Stage 1等の
  タグを落としつつ、性能上の理由（合成グリッドでA*が全Edgeの2.79%しか評価しない、
  辞書コールバック8.3〜17.7秒→list.__getitem__で0.37秒、Pythonループ1.2秒/8.6秒→
  ベクトル演算0.2秒等）は数値ごと残した。`find_nearest_node_indexed`の
  「改善計画T463で訂正。訂正前のdocstringは逆の主張をしていた」という自己言及的な
  経緯は削除し、現在の正しい安全マージンの説明のみ残した。
  検証: `pytest tests/test_routing.py -q`42件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `routes.py`（`app/api/routers/routes.py`、25件、対象19ファイル中5番目）完了。
  リクエスト/レスポンスPydanticモデルのフィールドコメント中心のファイルのため、
  T218/T218a/T221 Stage B/T266/T292/T316/T364/T365/T386/T441/T531/T551/T602/T265等の
  タグを機械的に落としつつ各フィールドの現在の意味は保持した（`T12 ADR原則1`・
  `T12 ADR原則5`のADR citationは既存方針どおり残す）。`generate_routes`の
  TOCTOUレース対策コメントから「以前はlocked()確認とacquireを別タイミングで
  行っていたため」history部分を削除し、現在の対策（awaitを挟まず連続実行）の
  説明のみ残した。フィールド追加・削除は無い（コメントのみの変更のため
  OpenAPI生成物への影響なし、export_openapi.py再生成は不要）。
  検証: `pytest tests/test_routes_generate.py tests/test_routes_preview.py -q`
  42件green、`pytest -q -m "not postgis"`1534件green。
- `route_generator.py`（23件、対象19ファイル中6番目）完了。周回生成戦略層。
  T531/T551/T553/T364/T365/T441/T602/T548/T402/T550等のタグを機械的に落とし、
  各docstring・コメントの現在の設計意図・契約説明はそのまま残した。
  `TURNAROUND_RADIUS_RATIO`のコメントから死んだ「旧8方位方式」比較（削除済みの
  実装との比較）を削除し、現在の値（0.4）の理由のみ残した。
  検証: `pytest tests/test_route_generator.py -q`40件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `graph.py`（`app/domain/graph.py`、18件、対象19ファイル中7番目）完了。
  Node/DirectedEdge/LeanNode/LeanEdge/WaySpec等のdomain型定義ファイル。T248/T218/
  T262/T546等のタグを落としつつ、Pydantic→dataclass軽量化のベンチマーク数値
  （Node 68,760件で2.125秒、DirectedEdge 171,461件で8.938秒、pickle復元
  253ms→118ms/タイル等）は現在の設計選択の根拠としてそのまま残した。
  検証: `pytest tests/test_graph.py -q`19件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `route.py`（`app/domain/route.py`、15件、対象19ファイル中8番目）完了。
  RouteSegmentDetail/RouteCandidate等のレスポンスdomain型。T309/T316フォローアップ/
  T550/T402/T431/T11のタグと2026-08-25の実障害言及を落とした。`RouteCandidate`
  docstringの「T431: stop_density等5フィールドをT421で撤去した」という削除済み
  フィールドの経緯ブロックは丸ごと削除（現在のスキーマにもうそのフィールドは
  存在しないため）。`RouteSegmentDetail.geometry`の「以前は始点・終点の直線で結んで
  いた」history、`aggregate_segments_into_bins`の「T11レビュー指摘M3」タグも削除し
  現在の設計理由のみ残した。
  検証: `pytest tests/test_route.py -q`16件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `road_graph_models.py`（14件、対象19ファイル中9番目）完了。PostGIS ORMモデル定義。
  T28/T151/T218/T12 Stage 0/T144/T145b/T351等のタグを落とした。`osm_raw_nodes.geom`の
  GiST索引撤去の理由（PBF初回取込でチャンク処理時間が7秒→73秒へ単調増加する死荷重）は
  数値ごと残しつつ「以前は張っていたが撤去した」history framingを削除、`osm_raw_ways`の
  GINインデックス撤去historyも同様に現在の設計（geom列の空間検索、GINは張らない）の
  説明へ置き換えた。`EdgeAttributeCountsRow`のForeignKey非採用理由コメントから
  「実機確認済み、2026-08-30」の日付framingを削除し技術的理由のみ残した。
  検証: `pytest -q -m "not postgis" --ignore=tests/test_import_pbf.py
  --ignore=tests/test_pbf_source.py`1534件green（ORM定義のみの変更のためテーブル単位の
  専用テストは無い）。コメントのみの変更のため挙動に影響なし。
- `tile_score_matrix_cache.py`（13件、対象19ファイル中10番目）完了。T536/T538/
  T546（フォローアップ含む）/T574/T575/T576/T281等のタグを落とした。モジュール
  docstringの「旧`infrastructure/axis_score_cache.py`」への言及は削除済みファイルへの
  死んだ参照のため削除し、現在のキャッシュ設計の説明に置き換えた。
  `TILE_SCORE_MATRIX_CACHE_VERSION`直上のv1/v2/v3変更履歴コメント（各バージョンで
  何が起きたかのT番号付き changelog）は削除し、バージョンを上げるべき条件のみを
  現在形の運用ルールとして残した（module 4のROAD_SURFACE_TILE_VERSION changelog削除と
  同じ扱い）。
  検証: `pytest tests/test_tile_score_matrix_cache.py -q`22件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `graph_material_cache.py`（12件、対象19ファイル中11番目）完了。
  tile_score_matrix_cache.pyと同型のキャッシュファイルのため同じ扱い:
  T219/T12 ADR Stage 1/T538/T546/T281/T574/T575/T576のタグを落とし、
  `TILE_MATERIALS_CACHE_VERSION`直上のv1〜v4変更履歴changelogを削除して現在形の
  運用ルールのみ残した。
  検証: `pytest tests/test_graph_material_cache.py -q`27件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `search_graph_cache.py`（7件、対象19ファイル中12番目）完了。LazyRoadGraph/
  SearchGraphStatics/NodeSpatialIndexのタイル集合キーLRU。T537/T522/T568/T557/T531の
  タグとdocs/tasks/T522.md・T537.md・T568.mdへのポインタ、`/code-review`指摘への言及を
  落とし、現在の設計理由・トレードオフ説明のみ残した。
  検証: `pytest tests/test_search_graph_cache.py -q`23件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `geo.py`（6件、対象19ファイル中13番目）完了。方位角・距離計算のdomain純関数群。
  T262/T248/T228/T554/T536のタグを落とし、`LatLon`Protocol・`LatLonPoint`・
  `KM_PER_DEGREE_LATITUDE`等の現在の設計理由（Pydanticバリデーションコスト回避、
  重複定義を避けるための正準1箇所）はそのまま残した。
  検証: `pytest tests/test_geo.py -q`10件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- 残り5ファイル（`road_edge_geometry_cache.py`4件・`road_graph_tile_cache.py`3件・
  `presplit_road_graph.py`3件・`precompute_road_node_degrees.py`3件・
  `tile_persistent_cache.py`2件、対象19ファイル中14〜18番目）をまとめて完了。
  いずれも小規模なT番号タグ（T390/T218/T531/T387/T22/T539/T151/T538/T546）の
  機械的な除去で、設計理由自体の書き換えは不要だった。
  検証: `pytest tests/test_presplit_road_graph.py tests/test_road_edge_geometry_cache.py
  tests/test_road_graph_tile_cache.py tests/test_tile_persistent_cache.py -q`
  43件green・3件skip（precompute_road_node_degrees.pyには専用テストファイルが無い）、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。

**routing-engine.mdモジュール完了（19ファイル中17ファイルに実ヒット、対象19ファイル全て
処理済み）**。合計約434件の経緯コメントを一掃。ファイル単位で個別コミット・push（9回）
した理由は本モジュール冒頭の「対応方針・分割単位」節、および各ファイルの実施メモ参照。

## 実施メモ（軸スタジオ・評価軸定義モジュール、2026-09-05）

- 本モジュールは対象11ファイル全てにヒットがあり、合計推定257件とrouting-engine.mdに
  次ぐ規模と判明した。routing-engine.mdと同じ運用（ファイル単位で個別コミット・push、
  `docs/improvement-plan.md`のT567行はモジュール完了まで更新しない）にする。
- `axis_definitions.py`（55件、対象11ファイル中最大）完了。評価軸のデータモデル・
  評価パイプラインの中核ファイル。T221 Stage B/C・T292・T268・T271・T310・T316・
  T352・T396・T404・T458・T501・T513・T534・T536・T149・T311フォローアップ等の
  タグを機械的に落とした。docs/modules/backend/axis-studio.mdが既に現在形で
  データモデル・評価パイプライン・ライフサイクルを詳述しているため、モジュール
  docstringの「Stage A〜E」の開発段階史・`AXIS_DEFINITIONS`のPython literal撤去の
  経緯は大幅に圧縮した。`BreakpointLinearShape`docstringの「旧4テンプレート
  （recipe_then_breakpoint_linear/FlagSumShape等）を統合した」という削除済み
  テンプレートへの言及は削除し、現在の2プリミティブ構成の説明に置き換えた。
  `AxisInternalAxisPublishError`docstringの「car_stress内部軸が誤って公開され
  ルート設定画面へ漏れ出た実障害」という2026-08-25の実インシデント記述は削除し、
  現在の検証ルールの説明のみ残した。
  - 陳腐化コメントの発見: `AXIS_DEFINITIONS`定義直前のコメントが「新規軸の追加・
    既存軸の変更は他のスキーマ変更と同じく手書きのmigration SQLで行う」と書いて
    いたが、同じファイルのモジュールdocstring・`AxisMaterialConflictError`周辺の
    記述は「行データはaxis_admin.py経由で行い、migrationでは行わない」という正反対の
    現在の運用を示していた。本モジュール3件目の陳腐化コメント（elevation_client.py・
    road_graph_repository.pyに続く）。正しい現状に置き換えた。
  検証: `pytest tests/test_axis_definitions.py -q`21件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `axis_display.py`（44件、対象11ファイル中2番目）完了。地図表示ルール自動導出の
  中核ファイル。T278/T308/T292/T396/T404/T409/T297/T320/T423等のタグと開発段階史
  （T278調査→T308一般化→T396でFlagSumShape統合→T404で拡張→T409でdisplay_override
  撤去）を大幅に圧縮し、docs/modules/backend/axis-studio.mdの「地図表示ルールの
  自動導出」節（既に現在形の表で同内容を持つ）を参照する形にした。旧`FlagSumShape`
  への言及は削除済みクラスへの死んだ参照のため削除し、現在の
  「全termがboolean材料の場合」という条件の説明に置き換えた。
  検証: `pytest tests/test_axis_display.py -q`22件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `axis_registry_service.py`（42件、対象11ファイル中3番目）完了。DBロード・管理API
  ユースケース層。T221 Stage D/T349/T348/T294/T295/T350/T352/T459/T458/T536/T534/
  T546フォローアップ/T469/T296/T268/T311フォローアップ/T292/T271/T501/T316/T302等の
  タグを機械的に落とした。`_CODE_COUPLED_AXIS_IDS`直上の「以前は`night`/`car_stress`/
  `gradient`もここに含めていたが、各々の理由で対象から外れた」という3段落の除外履歴
  コメントを削除し、現在空集合である理由（性質ベースの宣言的フィールドへの汎用化）
  のみ残した。エラーメッセージ文字列中のT番号引用（例:「T296」）はコメントではなく
  実行時に表示される値のため対象外とした。
  検証: `pytest tests/test_axis_registry_service.py -q`40件skip（PostGIS要）、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `axis_admin.py`（`app/api/routers/axis_admin.py`、36件、対象11ファイル中4番目）完了。
  軸定義CRUD管理APIルーター。T221 Stage D/T269/T271/T292/T310/T318/T352/T404/T513/
  T458/T277/T353/T396/T425/T467/T302等のタグと「レビュー指摘の修正」「コードレビュー
  指摘の修正」という自己言及的な経緯framingを機械的に落とし、各バリデータ・
  docstringの現在の検証ロジック説明はそのまま残した。raiseされる例外メッセージ
  文字列中のT番号引用（axis_registry_service.pyと同様）はコメントではないため対象外。
  検証: `pytest tests/test_axis_admin_routes.py -q`55件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `registry.py`（20件、対象11ファイル中5番目）完了。一次属性・二次軸レジストリ
  （`AXIS_DEFINITIONS`とは別系統）の型定義。T137/T278/T292/T333/T298/T320/T145b等の
  タグを機械的に落とした。
  - 陳腐化コメントの発見: モジュールdocstringが「現時点（T137）では車ストレス・
    安全度・自転車インフラの3軸は入力が重複したままのためまだ登録しない」という
    T137時代の手動登録方式（軸ごとに`register_axis()`を個別に書く）を前提にした
    記述のままだったが、実際の`registry_defaults.py: _register_axes()`は
    `AXIS_DEFINITIONS`を走査して公開軸すべてを無条件で登録する設計へ既に一本化されて
    おり、除外リストという概念自体が現在は存在しない。本モジュール4件目の陳腐化
    コメント（elevation_client.py・road_graph_repository.py・axis_definitions.pyに
    続く）。registry_defaults.pyの正しい記述に合わせて書き換えた。
  検証: `pytest tests/test_registry.py -q`8件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `axis_catalog.py`（`app/api/routers/axis_catalog.py`、17件、対象11ファイル中6番目）
  完了。軸カタログの公開読み取りAPI。T269/T308/T271/T404/T310/T318/T440/T440 Part D/
  T352/T513等のタグを機械的に落とし、各フィールドの現在の意味・エンドポイントの
  設計理由はそのまま残した。
  検証: `pytest tests/test_axis_catalog_routes.py -q`9件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `axis_definition_models.py`（12件、対象11ファイル中7番目）完了。軸定義のPostGIS
  ORMモデル。各カラムコメントに埋め込まれていた「0015/0016/0018/0019/0020/0023/
  0025 migrationで◯◯として導入した（既存N行はbackfill済み）」という導入経緯を
  全カラム分削除し、現在のnullable/server_default・意味の説明のみ残した
  （road_graph_models.pyの同種changelog削除と同じ扱い）。
  検証: `pytest -q -m "not postgis"`1534件green（ORM定義のみの変更のためテーブル単位の
  専用テストは無い）。コメントのみの変更のため挙動に影響なし。
- `axis_definition_repository.py`（12件、対象11ファイル中8番目）完了。
  axis_definitions/axis_registry_metaのPostGIS永続化層。T221 Stage D/T292/T310/T404/
  T409/T469/T271/T361等のタグを機械的に落とした。モジュールdocstringの
  「display_override（改善計画T310...T409でフィールド・DBカラムごと削除した）は、
  以前は...で同じ規約の往復をしていたが、現在はこのリポジトリが扱うフィールドでは
  ない」という削除済みフィールドの経緯段落は丸ごと削除した。
  検証: `pytest -q -m "not postgis"`1534件green（PostGIS要のため専用テストはpostgis
  マーカー側、今回は非postgisスイートのみ実行）。コメントのみの変更のため挙動に
  影響なし。
- `registry_defaults.py`（9件、対象11ファイル中9番目）完了。T137/T142/T154/T320/
  T270フォローアップ/T269/T347等のタグと「二次軸の登録を軸id直書きの手動列挙から
  AXIS_DEFINITIONS走査へ一本化した」という開発経緯段落を圧縮し、現在の一本化された
  設計のみ残した。`_register_primary_attributes`冒頭の「死コード監査で
  source/geometry/dtype等を削除した」という経緯コメントも削除し、
  registry.py側の現在のPrimaryAttributeSpec docstringを参照する1行に置き換えた。
  検証: `pytest tests/test_registry_defaults.py -q`17件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `axis_templates.py`（6件、対象11ファイル中10番目）完了。区分線形補間・カテゴリ
  マッピングの2汎用プリミティブ。T221 Stage A/T239/T396/T292/T240等のタグを
  落とし、モジュールdocstringの「旧4テンプレート（フラグ加算・レシピ→レベル
  変換等）を統合した」という削除済みテンプレートへの言及を削除して現在の
  2プリミティブ構成のみ残した。`evaluate_categorical`の「以前はO(要素数×
  キー数)のループだった」history部分も削除しO(要素数×log(キー数))という
  現在の実装のみ残した。
  検証: `pytest tests/test_axis_templates.py -q`17件green、
  `pytest -q -m "not postgis"`1534件green。コメントのみの変更のため挙動に影響なし。
- `axis_definitions_snapshot.py`（4件、対象11ファイル中11番目・最終）完了。fresh
  bootstrap用スナップショット読み書き。モジュールdocstringのT350→T353→T360→T361という
  開発経緯の物語（「軸定義の変更経路がmigrationとAPIの2つ存在する限り同期漏れが
  構造的に再発する」という当時の根本原因分析を含む）を大幅に圧縮し、CLAUDE.mdの
  axis_definitions同期ルールと重複しない範囲で現在の設計（行データはaxis_admin API
  経由、migrationはDDLのみ、fresh bootstrapはスナップショットから復元）のみ残した。
  検証: `pytest -q -m "not postgis"`1544件green（専用テストファイルは無い。T618の
  マージによりベースラインが1534→1544件に増加）。コメントのみの変更のため挙動に
  影響なし。

**axis-studio.mdモジュール完了（11ファイル全てに実ヒット、対象11ファイル全て
処理済み）**。合計約257件の経緯コメントを一掃。ファイル単位で個別コミット・push
（11回）した理由はrouting-engine.mdと同じ（本ファイル冒頭「対応方針・分割単位」節
参照）。本モジュールで新たに1件、陳腐化コメント（`registry.py`のモジュールdocstring
がT137時代の手動軸登録方式を前提にした記述のまま残っていた）を発見・修正した
（本タスク通算4件目）。

## 実施メモ（動的材料・way_id値配信モジュール、2026-09-05）

- 着手前に`git fetch origin master`で確認したところ、dynamic-way-values.md領域
  （wind.py/wind_grid.py/gradient.py/dynamic_way_values.py・wind_way_service.py/
  gradient_way_service.py・dynamic_way_value_cache.py/wind_forecast_cache.py・
  region.py/dependencies.py）を直近で触っている並行セッションは無く、着手可能と判断した。
- 対象ファイルのうち`api/dependencies.py`は横断基盤・気象・評価・軸スタジオ・
  ルート生成エンジンの各モジュール（対象ファイル表に同名で列挙されている）でも
  「対象」扱いだったが、実際にはどのモジュール完了時にも変更されておらず（横断基盤
  モジュールの実施メモに「dependencies.pyの横断的部分[今回は経緯コメント自体が無く
  未変更]」と記録されていたが、実際にはT425/T467等のタグ付きコメントが残っていた）、
  複数モジュールにまたがる1ファイルを毎回断片的に触るより、本モジュール着手時に
  ファイル全体（22件）をまとめて一掃する方が合理的と判断した。dynamic-way-values.md
  固有部分（`_DYNAMIC_WAY_VALUE_SERVICE_FACTORIES`・`get_dynamic_way_value_service`）
  以外も含め全件処理し、`pytest tests/test_dependencies.py -q`（11件）で確認した。
- `wind.py`はNARRATIVE_PATTERNヒット0件で未変更。`wind_grid.py`（16件）・
  `wind_way_service.py`（7件）・`gradient.py`（1件）・`dynamic_way_values.py`（3件）・
  `gradient_way_service.py`（3件）・`dynamic_way_value_cache.py`（4件）・
  `wind_forecast_cache.py`（3件）・`region.py`（`region_dynamic_way_values`部分のみ2件）を
  処理した。
- 陳腐化コメント（本タスク通算5件目）: `wind_way_service.py`のモジュールdocstringが
  「way_id→wind_penalty配信層」と書いていたが、T599で本番の風軸がwind_penaltyから
  wind_drag_ratioへ切り替わり材料は撤去済みだった（`material_id = "wind_drag_ratio"`
  が実際のコード）。同じ死んだ参照が`domain/gradient.py`（「風のwind_penaltyと同じ
  考え方の応用」）・`infrastructure/road_graph_repository.py`（`_WAY_IDS_IN_TILE_SQL`
  コメント・`get_way_ids_in_tile`docstring×2箇所、計4箇所）にも残っていたため、
  合わせてwind_drag_ratio/「動的値配信層」という現在の名称へ置き換えた。
- `road_graph_repository.py`の`_WAY_IDS_IN_TILE_SQL`/`_WAY_GRADIENT_INPUTS_IN_TILE_SQL`
  まわり（4箇所）は、routing-engine.mdモジュール着手時点では「dynamic-way-values.md
  管轄のため対象外」として意図的にスキップしていたが、dynamic-way-values.md自身の
  「対象ファイル」節に「`get_way_gradient_inputs_in_tile`・`get_way_ids_in_tile`は
  routing-engine.mdが主管するファイルに属する」と明記されており、スキップの判断が
  逆だったと判明した。本モジュール着手のタイミングでまとめて一掃し
  （T405→T414/T423タグ・docs/tasks/T400.md等の参照・旧SQL実装の撤去経緯を削除し、
  上記のwind_penalty→wind_drag_ratio訂正も含める）、routing-engine.mdの実施メモへも
  参照を追記した。ついでに同ファイル内で見つかった別件のスキャナ抵触
  （`get_graph_in_bbox`近くの「実測でこの呼び出し単体13.3秒」、ベンチマーク数値は
  残し「実測で」→「計測で」の言い換えのみ）も合わせて修正した。
- 設計書と重複＝削除の例: `dependencies.py`の`_DYNAMIC_WAY_VALUE_SERVICE_FACTORIES`
  コメント（T460の設計変更経緯）・`dynamic_way_values.py`のモジュールdocstring・
  `dynamic_way_value_cache.py`のモジュールdocstringは、いずれも
  `docs/modules/backend/dynamic-way-values.md`の「材料登録と地図表示値」節・
  「キャッシュ」節に現在形で書かれている内容と重複していたため大幅に圧縮し、
  同ドキュメントへのポインタへ置き換えた。
- 非自明な現在の理由＝現在形で残す（大きめの例）: `wind_grid.py`の格子間隔0.1°の
  選定根拠（正方格子の最悪ケースがズーム13の表示半径にほぼ収まる値）・GETからPOSTへの
  切替理由（414 Request-URI Too Large回避）・詳細格子の原点固定によるキャッシュ共有の
  理由は、経緯（「変遷」「実機で判明した」「ユーザー要望」等）の言い回しだけを落として
  技術的根拠として残した。
- 日記的記述のみ＝削除の例: `wind_grid.py`モジュールdocstringの
  `@openmeteo/weather-map-layer`（GPLv2ライブラリ）からMapLibre symbolレイヤーへの
  切替経緯、`wind_grid.py`の格子間隔「変遷（2026-08-20）: 初期値0.35°→0.2°→0.1°」という
  逐次changelog、`wind_way_service.py`のT405→T414→T423という3段階の設計変更の物語は、
  現在の実装を理解するのに不要なため削除した。
- 検証: 各ファイルの単体テスト（`test_wind_grid.py`・`test_wind_way_service.py`・
  `test_dependencies.py`・`test_gradient.py`・`test_dynamic_way_values.py`・
  `test_gradient_way_service.py`・`test_dynamic_way_value_cache.py`・
  `test_wind_forecast_cache.py`・`test_region.py`・`test_region_routes.py`・
  `test_region_service.py`）と`pytest -q -m "not postgis"`1544件green。コメントのみの
  変更のため挙動に影響なし。
- ついでにdynamic-way-values.mdの「対象ファイル」節が`infrastructure/
  road_graph_repository.py: get_way_gradient_inputs_in_tile`・`get_way_ids_in_tile`は
  routing-engine.mdの主管と明記していたため、routing-engine.mdモジュール着手時に対象外と
  していた同ファイルの`_WAY_IDS_IN_TILE_SQL`/`_WAY_GRADIENT_INPUTS_IN_TILE_SQL`まわり
  （4箇所）もこのタイミングでまとめて一掃した。ここで`wind_way_service.py`・
  `domain/gradient.py`・同ファイルの3箇所に残っていた「wind_penalty」という材料名の
  死んだ参照（T599で本番の風軸がwind_drag_ratioへ切替済み）を発見・修正した
  （本タスク通算5件目の陳腐化コメント）。

## 実施メモ（軸スタジオ管理画面モジュール、2026-09-05）

- 対象14ファイル（`AxisStudio.tsx`・`AxisComposer.tsx`・`breakpointTools.ts`・
  `axisAdminApi.ts`・route handler×3・`MaterialCoveragePanel.tsx`・
  `materialCoverageApi.ts`・route handler・`DerivedDataFreshnessPanel.tsx`・
  `derivedDataFreshnessApi.ts`・route handler・`useMaterialCatalog.ts`・
  `useMaterialValues.ts`・`materialCatalogApi.ts`・`axisMaterialsCatalog.ts`・
  `axisIconPalette.tsx`）のうち実ヒットは`AxisStudio.tsx`・`AxisComposer.tsx`・
  `axisAdminApi.ts`・`axisIconPalette.tsx`・`axisMaterialsCatalog.ts`・
  `useMaterialCatalog.ts`・`useMaterialValues.ts`・`materialCatalogApi.ts`の8ファイル
  （残り6ファイルは機械スキャナ・手動grep双方でヒット0件を確認済み）。
  `recipeControls.tsx`のFieldLabel部分（本モジュールの管轄）は、実ヒット4件がすべて
  FieldLabel定義より前（RouteSettingsPanel向けの別部分）にあったため対象外とし、
  route-settings-and-results.mdモジュールへ委ねた。
- **重要な発見**: `AxisComposer.tsx`で、事前の機械スキャン（`python scripts/
  review_checks.py docs`）が示した推定8件を大幅に超える約60箇所の経緯コメントが
  実際には残っていた。原因はJSDoc(`/** */`)・JSX(`{/* */}`)形式のコメントに対する
  スキャナのカバレッジ不足（詳細調査の結果、JSXコメントは`comment_only()`の判定条件
  `stripped.startswith(("/**","/*","*/","*"))`に`{/*`が一致せず完全に検出不能、
  JSDocコメント自体は行ごとに検出できているが`NARRATIVE_PATTERN`正規表現がコロン区切りの
  T-tag（`改善計画T345:`等）・年なしのユーザー指摘（`実機フィードバックへの対応`等）・
  「コードレビュー指摘の修正」「実バグ」等の言い回しを拾えていなかった）。この穴自体は
  スキャナの改修が必要なため[T622](T622.md)へ分離起票し、本モジュールでは手動精読で
  対応した。他の対象ファイルもこの教訓を踏まえ、機械スキャナが0件を返したファイルも
  含めて手動grep（コードレビュー指摘・実バグ・UIレビュー・ゼロベース網羅等の追加
  パターンを含む広めの正規表現）で再確認した。
- 陳腐化コメント（本タスク通算6件目）: `AxisComposer.tsx`のモジュールdocstring・
  ステップ定義コメントに残っていた「内部で保持する4種の変換テンプレート」という記述。
  backend側の`AxisShape`は`BreakpointLinearShape`/`CategoricalShape`の2型のみ
  （`domain/axis_definitions.py`で確認）、フロント側の`ShapeKind`も
  `breakpoint_linear`/`recipe_then_breakpoint_linear`/`categorical`の3種であり、
  4種ではない（旧flag_sum統合前の名残と見られる）。「3種」へ訂正した。
- 設計書と重複＝削除の例: `AxisStudio.tsx`のBasic認証・モーダル表示フローの説明、
  `AxisComposer.tsx`のシェイプカード統合ロジック・折れ点補間の説明等は、
  `docs/modules/frontend/axis-studio.md`「AxisStudio.tsx」「AxisComposer.tsx」節に
  現在形で書かれている内容と重複していたため圧縮し、同ドキュメントへのポインタへ
  置き換えた。
- 非自明な現在の理由＝現在形で残す（大きめの例）: `AxisComposer.tsx`の`NumberField`の
  「"-"や"12."のような未確定入力を親stateへ伝播しない」設計・`goNext`ボタンの
  `key`切り替え（type="button"↔"submit"のDOM再利用によるフォーム誤送信を防ぐReactの
  挙動）は、経緯（「コードレビュー指摘の修正」「実機不具合の修正」等の言い回し）を
  落とし、メカニズムの説明として残した。
- 日記的記述のみ＝削除の例: `AxisComposer.tsx`の「T327時点のヒント文の向きが逆だった
  ため訂正した」という自己言及的な訂正コメント（現在の正しい記述は他所[スコア0〜100の
  向き説明]と重複していたため削除）、`AxisStudio.tsx`の「以前はユーザー名/パスワード
  入力欄を持っていた」撤去経緯等は、現在の実装を理解するのに不要なため削除した。
- 検証: 各ファイルの単体テスト（`AxisStudio.test.tsx`・`AxisComposer.test.tsx`・
  `axisIconPalette.test.ts`・`useMaterialCatalog.test.ts`・`useMaterialValues.test.ts`・
  `axisAdminApi.test.ts`・`materialCatalogApi.test.ts`）計127件green。コメントのみの
  変更のため挙動に影響なし。

## 実施メモ（開発者/研究者機能モジュール、2026-09-05）

- 対象18ファイル（`app/admin/page.tsx`含む。詳細は上記チェックリスト参照）のうち
  実ヒット11ファイル（`app/admin/page.tsx`・`HeaderMenu.tsx`・`DebugPanel.tsx`・
  `DebugConsole.tsx`・`SystemStatusPanel.tsx`・`BackendLogsPanel.tsx`・
  `ResearchPanel.tsx`・`researchMode.ts`・`debugStatsApi.ts`・`debugAdminApi.ts`・
  `app/admin/api/debug/logs/route.ts`）を処理した（残り7ファイルは機械スキャナ・
  手動grep双方でヒット0件を確認済み）。AxisComposer.tsxでの教訓を踏まえ、機械スキャナが
  0件を返したファイルも含め手動grep（コードレビュー指摘・実バグ・UIレビュー等の
  追加パターンを含む広めの正規表現）で確認したが、本モジュールではJSDoc/JSX起因の
  見逃しは無かった（軸スタジオ管理画面ほどJSDocコメントを多用していないため）。
- ドキュメント記載漏れの発見・修正: `app/admin/page.tsx`（`/admin`のタブ構成を束ねる
  コンポジションルート）が`docs/modules/frontend/developer-research-tools.md`の
  「対象ファイル」表に載っていなかった。同ページの各タブは同ドキュメントの
  「`/admin`とpage.tsx（`/`）の境界」節で図示されているのに、ページ自体は表から
  漏れていた。同コミットで表へ追記した。
- 設計書と重複＝削除の例: `HeaderMenu.tsx`・`ResearchPanel.tsx`・`researchMode.ts`の
  「研究モードのON/OFF導線を`/admin`から`page.tsx`ヘッダーへ一本化した」という
  移設経緯は3ファイルに重複して書かれていたため、経緯の言い回しを落として現在の
  役割分担の説明だけに揃えた（完全な1箇所への集約はモジュール外の設計文書の
  役割のため見送り、各ファイル内の非自明な理由付けとしては個別に残した）。
- 非自明な現在の理由＝現在形で残す（大きめの例）: `HeaderMenu.tsx`の研究モードフラグに
  サーバー側検証が無く`/`のDevToolsからも操作可能という事実（一般公開ページへの
  露出が新たなセキュリティリスクにならない根拠）、`app/admin/page.tsx`のデバッグログ
  ビューアを`/admin`ではなく`/`に置く理由（DebugConsoleが地図インスタンスに紐づく
  情報のため）、`SystemStatusPanel.tsx`のPromise.all採用理由（個別`.finally()`だと
  片方の解決でloading表示が消える）は、経緯の言い回しだけを落として技術的根拠として
  残した。
- 日記的記述のみ＝削除の例: `SystemStatusPanel.tsx`の「以前はDebugConsoleに同居させて
  いたが分離した」撤去経緯（`DebugConsole.tsx`側に同じ理由が既にあり重複）、
  `app/admin/page.tsx`の「3つのDisclosureをやめてタブへ再構成した」UI変遷、
  `researchMode.ts`・`BackendLogsPanel.tsx`・`debugAdminApi.ts`等に残っていた
  Txxxタグ（T29/T92/T379/T471/T481/T517/T519等）は現在の実装を理解するのに不要な
  経緯情報のため削除した。
- 検証: 各ファイルの単体テスト（`DebugPanel.test.tsx`・`DebugConsole.test.tsx`・
  `BackendLogsPanel.test.tsx`・`SystemStatusPanel.test.tsx`・`ResearchPanel.test.tsx`・
  `HeaderMenu.test.tsx`・`useResearchMode.test.ts`・`debugAdminApi.test.ts`・
  `debugStatsApi.test.ts`・`researchMode.test.ts`）計42件green（`app/admin/page.tsx`は
  専用テスト無し）。コメントのみの変更のため挙動に影響なし。

## 実施メモ（地図: 軸・ルート色分けモジュール、2026-09-05）

- 対象10ファイルのうち実ヒット9ファイル（`routeStyleModes.ts`・
  `dedicatedWayValueLayer.ts`・`valueScale.ts`・`gradientGridFill.ts`・
  `dynamicWayValues.ts`（Map/）・`mapColorLegend.ts`・`useDynamicWayValues.ts`・
  `axisLayers.ts`[rampColorForBand/COLOR_UNKNOWN定義部分のみ、他モジュールと共有する
  ファイルのため]・`mapLayers.ts`[isAxisStudioLayer定義部分のみ、同様]）を処理した。
  `LensControl.tsx`はヒット0件。`Map/MapView.tsx`は上記チェックリストの注記どおり
  対象外のまま残置した。
- **陳腐化コメント（本タスク通算7件目）**: `gradientGridFill.ts`の2箇所に「風のgridFill
  （windPenalty.ts）」という削除済み機能・削除済みファイルへの死んだ参照が残っていた。
  並行セッションのT623（風の評価軸を細かい風グリッドへ乗せ替え）で風のgridFill自体が
  撤去され、風は現在gridMark（矢印）のみになっている（docs/modules/frontend/
  dynamic-weather-layers.mdで確認）。同様に存在しない関数
  `windPenaltyGridToCellFeatureCollection`への参照（実際の関数名は
  `precipitationGridToCellFeatureCollection`）も見つかり修正した。
  `useDynamicWayValues.ts`にも同種の「風のgridFillは別経路のためbyTileを使わない」という
  誤った現状説明が残っていたため、「風はgridFillを持たない」という正しい記述へ訂正した。
- 設計書と重複＝削除の例: `mapColorLegend.ts`・`routeStyleModes.ts`のユーザー要望引用
  部分は、要望自体の経緯を落とし現在の仕様（体感ラベルの併記・境界値配列駆動の段階数）
  だけを残した。
- 非自明な現在の理由＝現在形で残す（大きめの例）: `gradientGridFill.ts`の「勾配は道路
  ごとの属性であり独立した空間フィールドを持たないためタイル境界を1セルとして集計する」
  という設計判断、`useDynamicWayValues.ts`の`byTile`が必要な理由（勾配のgridFillだけが
  タイル単位の生データを要求する）は、経緯の言い回しを落として技術的根拠として残した。
- 日記的記述のみ＝削除の例: `routeStyleModes.ts`の「以前は"gradient"/"road"も固定文字列
  unionの一員だった」撤去経緯、`axisLayers.ts`の「以前は4色固定配列で5段階以上の軸が
  同色に潰れる問題があった」撤去経緯、`mapLayers.ts`の「T406時点は地図上チップの
  グループへ束ねられていたがT418で撤去しルート設定パネルへ移設した」移設経緯等は、
  現在の実装を理解するのに不要なため削除した。
- 検証: 各ファイルの単体テスト（`routeStyleModes.test.ts`・
  `dedicatedWayValueLayer.test.ts`・`gradientGridFill.test.ts`・
  `dynamicWayValues.test.ts`・`useDynamicWayValues.test.ts`・`axisLayers.test.ts`・
  `mapLayers.test.ts`）計85件green（`valueScale.ts`・`mapColorLegend.ts`は専用テスト
  無し、他ファイル経由で間接的に検証済み）。コメントのみの変更のため挙動に影響なし。

## 実施メモ（地図: 動的気象レイヤーモジュール、2026-09-05）

- 対象約21ファイル（`dynamicWeather.ts`・`precipitationNowcast.ts`・`thunderNowcast.ts`・
  `jmaNowcastFrames.ts`・`lidenLayer.ts`・`lidenIcon.ts`・`windLayer.ts`・
  `windArrowIcon.ts`・`riskMap.ts`・`useDynamicWeatherLayers.ts`・`useWeatherGrid.ts`・
  `useWeatherConditions.ts`・`usePolledFetch.ts`・`WeatherPanel.tsx`・
  `amedasWeatherIcon.ts`・`weatherCode.ts`・`TodayOutlook.tsx`・`WarningBadge.tsx`・
  `weatherApi.ts`・`types/weather.ts`）のうち20ファイルに実ヒットがあった
  （`Map/MapView.tsx`のこのモジュール担当箇所は対象外、下記チェックリスト参照）。
  本モジュールは推定144件（対象外のMapView.tsx分を除く）と、これまでで最大級だった。
- **陳腐化コメント（本タスク通算8件目）**: `dynamicWeather.ts`の共通ループ関数
  `gridToFeatureCollection`のdocstringに、削除済みファイル`windPenalty.ts`への
  死んだ参照が残っていた（map-axis-coloring.mdモジュールの`gradientGridFill.ts`で
  発見したのと同じT623由来の撤去漏れ）。実在するファイル名（precipitationNowcast.ts・
  windLayer.ts）へ修正した。
- 設計書と重複＝削除の例: `dynamicWeather.ts`冒頭の「共通契約4本柱」・
  `riskMap.ts`冒頭の「キキクル3種と線状降水帯予測マップの扱いの分岐」は、
  `docs/modules/frontend/dynamic-weather-layers.md`に同じ構成で現在形で書かれている
  内容と重複していたため大幅に圧縮し、同ドキュメントへのポインタへ置き換えた
  （洪水キキクルのvectorTile実装詳細等、ドキュメント側に無い技術的詳細は残した）。
- 非自明な現在の理由＝現在形で残す（大きめの例）: `windLayer.ts`のビューフォート風力
  階級を刻み幅に採用した理由・`mergeWindGridKeepingStale`の欠損補完ロジック、
  `precipitationNowcast.ts`のrasrf(降水短時間予報)データ構造（immed/none系統の混在、
  sjfcstmapとの混在）は、経緯（「実機確認」「実機フィードバック」等の言い回し）を
  落として技術的根拠・データ構造の説明として残した。
- 日記的記述のみ＝削除の例: `windLayer.ts`/`windArrowIcon.ts`双方に重複していた
  `@openmeteo/weather-map-layer`（GPLv2ライブラリ）からMapLibre symbolレイヤーへの
  切替経緯（backend `wind_grid.py`と同じ経緯が両ファイルに残っていた）、
  `WarningBadge.tsx`の「以前は全件常時表示だったが折り返し問題でサマリーボタン方式へ
  変更した」UI変遷、各所に散在していたTxxxタグ（T170/T171/T178/T180/T183/T195/T198/
  T201/T202/T203/T204/T205/T212/T375/T385/T387/T407/T410/T414/T416/T432/T441/T470/
  T541/T608等）は現在の実装を理解するのに不要な経緯情報のため削除した。
- 検証: 各ファイルの単体テスト（`dynamicWeather.test.ts`・`precipitationNowcast.test.ts`・
  `thunderNowcast.test.ts`・`jmaNowcastFrames.test.ts`・`windLayer.test.ts`・
  `windArrowIcon.test.ts`・`lidenLayer.test.ts`・`riskMap.test.ts`・
  `useDynamicWeatherLayers.test.ts`・`useWeatherGrid.test.ts`・
  `useWeatherConditions.test.ts`・`WeatherPanel`関連・`TodayOutlook`関連・
  `WarningBadge`関連・`weatherApi.test.ts`）計200件超green。コメントのみの変更のため
  挙動に影響なし。


















## 実施メモ（ルート設定・結果パネルモジュール、2026-09-05）

対象14ファイル中9ファイルに実ヒットがあった: `RouteSettingsPanel.tsx`(25)・
`WindBearingSlider.tsx`(6)・`AxisContributionBar.tsx`(2)・`routePreferenceSync.ts`(2)・
`axisCatalogApi.ts`(1)・`evaluationAxes.ts`(15)・`useAxisCatalog.ts`(13)・
`ComparisonPanel.tsx`(10)・`recipeControls.tsx`(6、`FieldLabel`/`withAutoEnable`/
`RecipePanelSection`部分。axis-studio.mdモジュール完了時に対象外として本モジュールへ
明示的に委ねられていた4件を含む)・`TravelBearingControl.tsx`(5)。残り5ファイル
（`RouteForm.tsx`・`useRouteFormSubmit.ts`・`RouteAxisProfile.tsx`・
`types/experimentSlot.ts`）は機械スキャナ・手動grep双方でヒット0件を確認済み
（`types/experimentSlot.ts`の「研究インターフェース改善 §10-3」参照はTxxx形式の
経緯タグではなく設計レビュー文書の節番号引用のため対象外）。

削除した経緯コメントの主な内容: 改善計画Txxx番号タグの一括除去、日付付きユーザー要望/
指摘の引用（「」を含む）、撤去済み機能の履歴叙述（プリセットボタン・
`@fseehawer/react-circular-slider`版コンパス・`routeSegmentChartPopup.ts`・
`AxisCategory`型/`AXIS_CATEGORIES`/`axisCategory()`・`PREFERENCE_WEIGHT_KEY_BY_AXIS_ID`・
`CarStressRecipePanel`系専用部品の一覧）。いずれも「復元する場合はgit履歴参照」
のような差し戻し前提の記述ごと削除し、現在も成り立つ設計理由（重み配分バーが
帯グラフドラッグ・矢印キー操作のみである、`loaded`フラグで未確定状態を区別する
必要がある、`useSyncExternalStore`で2インスタンス間の食い違いが構造的に起きない、
コンパスの矢印は実機センサーと非連動の手動設定値専用である等）は現在形のまま残した。

テスト: frontend該当ファイルの`vitest run`（RouteSettingsPanel/WindBearingSlider/
AxisContributionBar/routePreferenceSync/axisCatalogApi/evaluationAxes/useAxisCatalog/
ComparisonPanel/recipeControls/RouteAxisProfile/RouteForm関連）計117件green。
コメントのみの変更のため挙動に影響なし。

## 実施メモ（地図: 静的レイヤー・道路表示モジュール、2026-09-05）

対象14ファイル中12ファイルに実ヒットがあった: `staticAttributeLayers.ts`(24)・
`roadFilterAxes.ts`(12)・`legendFilter.ts`(3)・`primaryAttributes.ts`(14)・
`routeArrowIcon.ts`(1)・`axisInspectorPopup.ts`(5)・`types/traffic.ts`(2)・
`LayerChip.tsx`(2)・`InfoPopover.tsx`(1)・`LegendCheckboxList.tsx`(1)・`icons.tsx`(23)・
`services/regionApi.ts`（本モジュールの管轄範囲＝roadSurfaceTileUrl/poiTileUrl/
accidentTileUrlとタイル世代定数のみ）。共有ファイル`Map/mapLayers.ts`
（レイヤーカタログ本体・最上位3グループ判定・軸スタジオ由来レイヤーの除外判定、
isAxisStudioLayer自体はmap-axis-coloring.mdで完了済み）・
`components/MapOverlayControls/MapOverlayControls.tsx`(75)・
`components/MapLayersPanel/MapLayersPanel.tsx`(36)も本モジュールの管轄範囲を処理した。
`Map/MapView.tsx`・`Map/routeArrowIcon.ts`の一部・`Map/icons.tsx`は他モジュールとも
共有するため、page-composition.mdモジュール着手時にMapView.tsx分をまとめて処理する
方針は他モジュールと同様。

発見した実装不具合（副産物）: `mapLayers.ts`のbuildMapLayers()が組み立てる
panelHint（サイドバー「地図の見え方」パネルにエンドユーザーへ直接表示される説明文）
の中に「（改善計画T407）」「（改善計画T414）」等の内部タスク番号がそのまま埋め込まれて
おり、ユーザー向けUI文言へ内部管理番号が漏れ出ていた。本コミットで削除した
（コメント一掃作業でユーザー向け文字列リテラルの中身まで確認したことで発見）。

削除した経緯コメントの主な内容: 改善計画Txxx番号タグの一括除去、日付付き実機
フィードバック/レビュー指摘/ユーザー要望の引用、撤去済みUI・機構の履歴叙述
（ドラッグスクロール方式useDragScroll・評価軸グループ横並び展開・旧「観測/動的」
2グループ構成・RoadFilterEditor下書き→適用方式・BICYCLE_INFRA_CATEGORIES・
recipeBreakdownPopup.ts・CarStressBreakdown型）。dead reference（存在しないファイル・
型への言及）を2件発見し修正した: `axisInspectorPopup.ts`の`recipeBreakdownPopup.ts`
参照（ファイル自体が既に削除済み）、`types/traffic.ts`の`CarStressBreakdown`型への
言及（型自体が既に削除済み）。

テスト: frontend該当ファイルの`vitest run`（staticAttributeLayers/roadFilterAxes/
legendFilter/primaryAttributes/routeArrowIcon/axisInspectorPopup/LayerChip/
InfoPopover/LegendCheckboxList/regionApi/mapLayers/MapOverlayControls/
MapLayersPanel関連、WeatherPanel/TodayOutlook/WarningBadge/HeaderMenu関連の
icons.tsx依存確認込み）計300件超green。コメント・文字列リテラルのタスク番号除去のみの
変更のため挙動に影響なし。
