# T955: 材料の冷パスが160秒かかり、バッチのたびに最初の利用者が待たされる

状態: 未着手（2026-09-19起票）

## 背景

本番でルート生成に174秒かかった（2026-09-19 17:48、`req:b7f19819cf4d`）。内訳は
`prepare_ms=165428`のうち`materials_ms=160469`で、**探索も評価も速い**（`select_ms=6659`・
`evaluate_ms=470`）。2分半後の同条件は`materials_ms=964`・`total_ms=6336`。

`docs/tasks/T747.md`が記録する材料の冷パスは**29〜45秒規模**で、いまは**108〜160秒**。
3〜4倍に伸びている。世代を上げるのは日常運用（バッチを流すたびに起きる）ため、
**バッチ後に最初にその範囲を見た利用者が3分待つ**。

詳細はこのファイルへ追記する。
