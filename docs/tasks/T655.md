# T655. 停止密度を作り直す（種別別カウント・way上カウント・信号のクラスタ化） 規模M〜L

状態: 未着手

## 背景

評価軸`stop_density`が本番で全wayに100点（最難）を返しており、
「ストップアンドゴーが少ないルート」を選べない。原因は集計層が停止要因の種別を
捨てていること・自分の道の上に無い地点まで数えていること・1つの信号交差点を
複数ノードとして数えていることの3つ。
