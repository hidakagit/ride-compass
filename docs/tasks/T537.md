# T537: 探索用グラフ・索引（LazyRoadGraph/routable集合/空間索引/Node対索引）をタイル集合キーでキャッシュする

状態: 未着手（2026-09-02起票、T522の全再点検で判明）

- 背景: これらはタイル集合と0次フィルタだけで決まる派生物だが毎リクエスト作り直しており、
  本番VM実測で温パスのprepare約2.4秒のほぼ全てを占める（T522.md「評価ロジック全体の再点検」節）。
