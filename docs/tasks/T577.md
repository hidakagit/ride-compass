# T577: 材料ごとの欠損割合を管理画面で可視化する

状態: 完了（2026-09-04起票・同日実装＋SQL断片共有化フォローアップ。本番EXPLAIN ANALYZEで
実行計画を実測済み[3.5〜4.5秒]。本番デプロイ後の「材料」タブでの実地確認は別セッションの
判断に委ねる）

## 背景

2026-09-04、[T552](T552.md)対応方針4（surface_q軸の取込側既定値補完の要否判断）の
検討中に、材料`surface`（OSMのsurfaceタグ）が本番DB全体で85.0%欠損していることが
判明した。同じ調査で`elevation_attributes`（gradient軸の材料元）も88.4%欠損して
いたが、こちらは⑦precompute_elevation_attributesの未完走という運用上の問題
（[T575](T575.md)・[T576](T576.md)参照）で、根本原因が全く異なる2つの欠損が
同時に見つかったことになる。

いずれも発見手段は手作業のSQLクエリだった。`/api/material-catalog/{material_id}/values`
（既存API）は材料が取りうる値の一覧を返すのみで、NULL/未取得の割合は一切扱っていない。
管理画面・APIのどちらにも、材料ごとの欠損割合を継続的に把握する仕組みが無い
（surfaceに限らず全材料が対象外）。

## 方針（ユーザー判断、2026-09-04）

欠損データについて、取込側で既定値を推測して埋める（対応方針4的なアプローチ）のでは
なく、**欠損の実態を可視化し、埋めるかどうかの判断は利用側（軸定義・評価ロジック）へ
委ねる**方針を採る。この可視化の仕組みが本タスクの対象。

イメージ: 管理画面に材料一覧（`material_catalog.py`のレジストリが既に持つ全材料）と、
各材料の欠損割合（該当primary_attribute_idを持つEdge/Way総数に対する、値が
NULL/未取得のEdge/Wayの割合）を表示する。

## 対応方針・設計判断（2026-09-04）

1. **「欠損」の定義は元データ（タグ・派生テーブル行）の不在とし、評価上の扱いを併記する**。
   [T552](T552.md)の計測で分かったとおり、評価パイプラインの「材料が欠損したEdge比率」は
   材料によって0%になる（タグ不在を「非該当」という確定値として扱う設計のため）。その
   数値だけ見せると「タグは無いが評価できている」材料と「タグは無く評価もできない」材料の
   区別が付かない。そこで欠損数は常にタグ/行の不在で数え、`missing_semantics`
   （`"unknown"`=不明値として評価対象外、`"definite"`=確定値として評価）を別列で示す。
   この値は`MaterialSpec.bool_default`からは導出しない（`bool_default="nan"`でも
   `bicycle_infra_flags`がタグ不在をFalseとして扱う自転車インフラ系5材料があり、
   実際の扱いはextractor実装で決まるため、宣言テーブル側に明示する）。
2. **母集団はWay（`osm_raw_ways`全行）とEdge（`road_edges`全行）の2種**。OSMタグ由来の
   材料はWay単位で数える（T552の手作業SQLと同じ母集団、走査も軽い）。Edge単位の派生
   テーブル由来（`elevation_attributes`・`edge_attribute_counts`）はEdgeで数えるが、
   派生テーブルの`edge_id`が`road_edges`へのFK（ON DELETE CASCADE）であることを使い、
   派生テーブル側の行数だけを数えて「値ありEdge数」とし、500万行規模の`road_edges`との
   JOINを省く。Way側は全材料を`count(*) FILTER`で1回の走査にまとめる。
3. **材料id→判定式は宣言テーブル（`MATERIAL_COVERAGE_SPECS`）＋対象外一覧
   （`MATERIAL_COVERAGE_EXCLUSIONS`、理由付き）**。`MATERIAL_CATALOG`の全材料がどちらか
   一方に必ず載ることをテストで強制し、材料追加時の登録漏れを検知する。判定式は
   `infrastructure/osm_way_tag_sql.py`の共有SQL断片から組み立て、
   `_ROAD_SURFACE_TILE_MVT_SQL`（地図タイル配信）・`_MATERIAL_VALUE_COLUMN_EXPR`
   （軸スタジオの値候補列挙）と文字どおり同じPython定数を参照する（フォローアップ
   「SQL断片の共有化」参照——独立した文字列を突き合わせる整合性テストではなく、
   同じ定数を使う構成自体が一致を保証する）。対象外は`wind_penalty`（動的計算）・
   `oneway`（NOT NULL列）・`designation`系4材料（行の有無がそのまま確定値）。
4. **認可はBasic認証必須（`/api/admin/`配下）**。既存の`GET /api/material-catalog`系は
   認可不要だが、本APIは`osm_raw_ways`/`road_edges`の全表走査を伴い、認可なしに公開すると
   繰り返し呼ぶだけでDBを圧迫できてしまう。管理画面`/admin`からのみ使う想定のため、
   `axis_admin.py`/`debug_admin.py`と同じ`require_admin_basic_auth`で保護し、フロントは
   同一オリジンのroute handler（`proxyToBackendAdmin`）経由で呼ぶ。DB例外は
   `axis_admin.py`と同じく503へ変換する（診断用APIのため空レポートへ倒さない）。
5. **サーバー側キャッシュは持たない**。管理画面側で「集計する」ボタン押下時のみ実行し、
   `computed_at`を表示する。バッチ実行直後に古い値が見える混乱を避けるため、TTL
   キャッシュより「毎回その場で数える」方が診断用途には素直と判断した。本番規模
   （Way約133万・Edge約500万）での所要時間は未計測のため、backendセッションはルート生成用の
   長い`command_timeout`（180秒）、フロントの転送タイムアウトは90秒にしてある。

## 実施内容（2026-09-04）

- backend
  - `app/infrastructure/material_coverage.py`（新規）: `MATERIAL_COVERAGE_SPECS`・
    `MATERIAL_COVERAGE_EXCLUSIONS`・`build_way_coverage_sql`・`MaterialCoverageQuery`。
  - `app/services/material_coverage_service.py`（新規）: `build_material_coverage_report`
    （純関数）・`MaterialCoverageService`（`log_external_call("material-coverage")`＋
    1行INFOサマリ）。
  - `app/api/routers/material_catalog.py`: `GET /api/admin/material-catalog/coverage`
    （`MaterialCoverageResponse`、Basic認証必須）を追加。`app/api/dependencies.py`:
    `get_material_coverage_service`を追加。`app/api/routers/__init__.py`のdocstring更新。
  - テスト: `tests/test_material_coverage.py`（網羅性・MVT SQLとの整合・レポート組み立て・
    サービス、DB非依存）、`tests/test_material_coverage_repository.py`（PostGIS統合、
    Way/Edge両母集団の実カウント）、`tests/test_material_catalog_routes.py`（認可・
    レスポンス形・503変換）。
  - OpenAPI生成物（`frontend/src/types/generated/openapi.json`・`api.d.ts`）を再生成。
- frontend
  - `/admin`に4つ目のタブ「材料」を追加し、`components/AxisStudio/MaterialCoveragePanel.tsx`
    （新規、`.module.css`・`.test.tsx`同梱）を表示。表は欠損割合の降順、
    `missing_semantics="definite"`の行はバーの色を落とす。対象外材料は折りたたみ一覧。
  - `services/materialCoverageApi.ts`（新規）・`app/admin/api/material-coverage/route.ts`
    （新規、`route.test.ts`同梱）。`lib/adminApiProxy.ts`へ`timeoutMs`オプションを追加
    （既定15秒のまま、本route handlerだけ90秒）。`types/route.ts`へ生成型の再エクスポート追加。
  - `app/admin/page.tsx`・`page.test.tsx`をタブ4枚構成へ更新。
- docs: `docs/modules/backend/evaluation-scoring.md`（材料カタログAPI表・「材料の欠損割合」節、
  未記載だった`api/routers/material_catalog.py`も対象ファイル表へ追記）、
  `docs/modules/frontend/axis-studio.md`・`developer-research-tools.md`、
  `docs/architecture.md`「材料カタログ」節。

## 開発DBでの実測（2026-09-04）

開発DB（東京都心南部、Way 86,642件・Edge 319,530件）に対し集計1回=1.05秒。本番
（Way約133万・Edge約500万、およそ15倍）へ単純に外挿すると15〜20秒程度で、フロント側の
転送タイムアウト90秒・backendの`command_timeout`180秒に収まる見込み。開発DBでの主な値:
surface 60.8%（`unknown`）・gradient_percent 57.8%（`unknown`）・maxspeed_kmh 84.4%・
smoothness 99.7%・no_lit 90.8%（`definite`）・stop/intersection/accident 4.0%・highway 0.0%。

## フォローアップ: SQL断片の共有化・本番実行計画の計測（2026-09-04）

初回実装（上記）では、`MATERIAL_COVERAGE_SPECS`の各判定式が`_ROAD_SURFACE_TILE_MVT_SQL`
（地図タイル配信）の対応する正規化式と**独立に書き写され**、両者が一致することを
文字列突き合わせのテストで担保する設計だった。これは「材料が増えるたびに判定ロジックを
複数箇所へ手で複製する」構造そのものは解消しておらず、ユーザー指摘（「将来材料が
追加されても容易に対応できるように、材料ごとの作り込みはなるべくやめて」）を受けて
見直した。

- **共有モジュール新設**: `backend/app/infrastructure/osm_way_tag_sql.py`に
  `osm_raw_ways`のOSMタグ分類SQL断片（`SURFACE_GOOD_CASE_SQL`・`CYCLEWAY_TAGS_ARRAY_SQL`
  等の定数、`normalized_tag_sql`/`positive_integer_tag_sql`ヘルパー）を集約。
  全断片は`osm_raw_ways`のテーブルエイリアス`w`を前提にする。
- `road_graph_repository.py: _ROAD_SURFACE_TILE_MVT_SQL`（f-string化）・
  `_MATERIAL_VALUE_COLUMN_EXPR`（軸スタジオの値候補列挙、`get_distinct_material_values`）・
  `material_coverage.py: MATERIAL_COVERAGE_SPECS`の3箇所が、この共有モジュールの
  同じPython定数を参照するよう書き換えた（`_MATERIAL_VALUE_COLUMN_EXPR`はFableの
  実装時点では対象外だったが、`_ROAD_SURFACE_TILE_MVT_SQL`の正規化式を独立に書き写す
  同型の重複だったため合わせて解消した）。`build_way_coverage_sql`の`FROM osm_raw_ways`
  へ`AS w`エイリアスを追加。
- `tests/test_material_coverage.py`の「文字列を突き合わせる整合性テスト」を、
  「両クエリが同じPython定数を実際に使っているか」を確認する形へ書き換えた
  （突き合わせ対象の2つの文字列が独立に存在しない設計になったため、ドリフト自体が
  構造的に起こらない）。
- 新材料が増えた場合の作業は「`MATERIAL_COVERAGE_SPECS`または`MATERIAL_COVERAGE_EXCLUSIONS`
  へ1件追加（既存の網羅性テストが登録漏れを検知）」のみで、既存の`_ROAD_SURFACE_TILE_MVT_SQL`
  等が同じOSMタグを既に使っていれば`osm_way_tag_sql.py`の定数をそのまま再利用できる
  （新規のタグ・材料であれば、その1箇所だけ新しい定数を足す）。

**本番実行計画の実測**（開発DBでの外挿ではなく、本番DBで直接`EXPLAIN ANALYZE`）:

| クエリ | 実測 |
|---|---|
| way集計（16材料まとめて1スキャン、`osm_raw_ways`約133万件） | 1.74秒 |
| edge_total（`road_edges`約500万件） | 1.03秒 |
| gradient_percent（`elevation_attributes`、`average_grade IS NOT NULL`） | 0.66秒 |

いずれも2並列ワーカーが自動的に使われ、`Buffers: shared hit`のみ（ディスクI/Oなし、
オンメモリ）だった。合計しても3.5〜4.5秒程度で、開発DBからの外挿見積もり（15〜20秒）
より大幅に良好。90秒のフロント転送タイムアウトに対して十分な余裕がある。

**チューニングの判断**: インデックス追加の余地はない（全材料の判定が「全行を舐めて
タグを分類する」性質上、原理的にフルスキャンが必要で、既に効率的[並列・オンメモリ]に
実行されている）。残る改善余地は「wayクエリとedge系クエリを別コネクションで並行実行し
体感を半分程度に縮める」ことだが、現状のシーケンシャル実行でも実測3.5〜4.5秒は管理画面
として十分実用的なため、**追加のチューニングは見送り**（ユーザー判断、2026-09-04）。
将来、体感速度が問題になった場合の選択肢として記録だけ残す。

## 残課題・引き継ぎ

- 本番での実測（所要時間・実際の欠損割合）は未実施。T552の手作業SQL（surface 85.0%、
  elevation_attributes 88.4%）と同じ数値が管理画面で再現できることを、本番デプロイ後に
  「材料」タブで確認する。所要時間が90秒（フロント側転送タイムアウト）に近づく場合は、
  route handler（`COVERAGE_PROXY_TIMEOUT_MS`）とbackendの`command_timeout`の両方を見直す。
- 欠損割合は件数ベース（距離加重ではない）。T552の`missing_axis_distance_ratio`（距離加重）
  とは母集団も物差しも異なるため、両者の数値を直接比較しないこと。距離加重版が必要になった
  場合は`WayMaterialCoverageSpec`へ`ST_Length`ベースの集計列を足す形で拡張できる。

