# T822. ドキュメントと実装のずれを是正する（第8回ぶん）

規模M。
状態: 未着手（統合レビュー第8回のP2/P3を束ねたもの、2026-09-13）

## 背景

[T810](T810.md)（T790の前提を語る記述）・[T811](T811.md)（MapLayersPanel）・
[T809](T809.md)（map-axis-coloring.mdの不変条件）・[T813](T813.md)（caching.mdの断言）・
[T817](T817.md)（architecture.mdのpenalty_strength）が扱う分を除いた、残りのずれ。

**`review_checks.py docs`が検知しているのは3件だけ**（`contribution_arrays`・
`stackBarLegendTrigger`・`destinationState`）で、下記の大半は現在どの経路でも検知されない。

## 対象

- `docs/architecture.md:135`: サイドバー構成を「WeatherPanel・旧LocationControl・
  MapLayersPanel・RouteForm・BackendStatus等」と書くが、現在の`<aside>`は
  「ルート設定／ルート結果／ルート編集」の3Disclosureのみ（T808でさらに変わる）
- `docs/architecture.md:565-566`: `route_preference`のリクエスト例が7キーで、`stop_density`は
  本番スナップショットに存在しない軸id（停止密度は`axis_abdaded0be20`）。
  `RoutePreferenceWeights._check_axis_keys`は公開軸の完全一致を要求するため、
  **この例をそのまま投げると必ず422になる**。軸はDBで増減するので全件を写す限り再発する
- `docs/architecture.md:21`: 「`RoadGraphEngine`（…`EvaluationService`…を使う）」と書くが、
  `road_graph_engine.py:700-702`が「evaluate_graphは本エンジンから呼ばない」と明記している
- `docs/modules/frontend/page-composition.md:27`: 「`NEXT_PUBLIC_API_BASE_URL`」。実装・
  `.env.example`・`docker-compose.yml`・`README.md`はすべて`NEXT_PUBLIC_API_URL`で、
  この1行にしか存在しない。**この名前で設定すると既定の`http://localhost:8000`のままになり
  ブラウザからのAPI呼び出しが全滅する**
- `docs/modules/frontend/page-composition.md:37`: 「全`services/*Api.ts`クライアントが共有する
  fetch骨格」（`healthApi.ts`は共有していない。[T821](T821.md)と対）＋骨格を「7段」と数えている
- `docs/modules/frontend/page-composition.md:225-228`: `headerAction`がタブ列を含むと書くが、
  実装は`headerLead`（タブ列）と`headerAction`（生成ボタン）に分かれている
- `docs/modules/frontend/page-composition.md:265-266`: 「タブは順位番号と距離だけを表示する」と
  書くが実装はタブ内に総合難易度も描く（同文書212-213行は正しい）。「方位はRouteAxisProfileに
  出る」も事実でない（`direction_label`の描画は1箇所のみ）。同じ誤りが`page.tsx:1881-1884`・
  `page.module.css:290-293`にもある
- `docs/modules/frontend/route-settings-and-results.md:218, 305`: `stackBarLegendTrigger`・
  `destinationState`（実体は`destinationSet`）。**検知器が既に違反として出しており
  pre-commit/CIをブロックする状態**
- `docs/modules/frontend/route-settings-and-results.md:235-240`: [T816](T816.md)と同じ
  「薄いチップとして残る」の記述（T816で扱う分を除いた残り）
- `docs/modules/backend/routing-engine.md:111`: 「`LoopRoutingEngine`という**7メソッド**の契約」。
  Protocolは8件、`route_generator.py`のdocstringは9件を契約として書く。別エンジンを実装する人が
  7件だけ満たして「契約を満たした」と判断する
- `docs/modules/backend/routing-engine.md:98, 102, 430-437`: 迂回率の適用先の誤り／
  `compose_leg_costs`ログの存在しない項目名／via-node選定の候補打ち切りに触れていない
- `docs/modules/backend/static-road-attributes.md:369`: T790で`traffic.py`へ入った
  `STOP_SECONDS`（停止1回あたりの秒）と`HIGHWAY_RANK`がファイル表に無く、所要時間への
  効き方の記述がどちらのモジュール文書にも無い（現在の説明は`docs/tasks/`にしかない）
- `docs/modules/frontend/static-map-layers.md:32, 35`: 共通部品の消費者一覧が実態と違う
  （`LayerChip`の消費者は`HardFilterPanel`のみ、`LegendCheckboxList`の消費者は`LensControl`と
  `MapOverlayControls`で`RouteAxisProfile`ではない）
- `docs/modules/frontend/developer-research-tools.md:27`: 「`GET /api/health`」。実装は
  `/health`（`/api`接頭辞なし）。この形でヘルスチェック監視を書くと常に404になる
- `docs/frontend-design-system.md:93, 103, 171`: `Button`のvariantに実在しない`danger`／
  `ui/`のCSS全件列挙が`unusedBadge.module.css`を落としている／44pxタップ領域の唯一の実例が
  撤去済み`MapLayersPanel.module.css`
- `docs/tasks/T790.md:32`: 完了サマリの本番レスポンスが最適化2回目**前**の数字
  （5,483ms）のままで、同ファイルの計測表と`improvement-plan.md`の最終値4,675msと食い違う
- `backend/app/batch/precompute_edge_attribute_counts.py:72-74`: T765の書き換え時に
  参照先を取り違え、かつ「**本バッチの実行前に`precompute_road_node_degrees.py`の実行が必須**」
  という警告が同時に消えている（docstring:13-15には残存）。飛ばすと全Edgeの
  `intersection_count`が無警告で0になる
- `backend/app/batch/import_pbf.py:416-417`: Supabase時代の容量予算（300MB）を現行の
  監視指標として書いているが、参照先の`docs/osm-pbf-import.md:9`は撤廃を明記している
- `backend/app/services/jma_amedas_service.py:16`・`frontend/src/lib/apiBaseUrl.ts:3`:
  CLAUDE.mdの存在しない節を参照している（正本はそれぞれ`docs/tasks/T387.md`・
  `docs/design-principles.md`）
- `backend/app/api/dependencies.py:211-212`・`tests/test_routes_generate.py:121`:
  T661で廃止した`BackgroundTasks`を現行の実行文脈として書いている（実装は`create_task`）
- `backend/app/services/road_graph_engine.py:1043-1045`: `preview_segment`のコメント
  「road_graphエンジンは実測所要時間モデルを持たない」が走行モデル導入後は成立しない。
  実際に`/api/routes/preview`だけが勾配・風・路面を無視した所要時間を返し続けている
- `backend/app/domain/traffic.py:1-11`・`material_catalog.py:416-417`: ファイルの責務を語る
  docstringが現在の中身を覆っておらず（所要時間モデルのパラメータを持つ）、自転車インフラ材料の
  消費者も誤記（実際は`material_catalog.py`）
- `frontend/src/hooks/useWeatherGrid.ts:25-27`・`services/materialCatalogApi.ts:22-25`・
  `services/regionApi.ts:14, 20, 74-75, 87`・`hooks/useAxisCatalog.ts:34`・
  `Map/riskMap.ts:11-20`・`Map/dynamicWeather.ts:35, 117`・`Map/routeStyleModes.ts:163-166`・
  `Map/secondaryAxes.ts:5-8`・`AxisStudio/axisDraft.ts:210`・`AxisStudio/AxisStudio.tsx:299`・
  `AxisStudio/breakpointTools.ts:50`・`MapOverlayControls.tsx:638`・`ui/Card/Card.tsx:5`・
  `BottomSheet.tsx:192`・`useRouteFormSubmit.ts:33-34`・`app/page.tsx:712-713, 2384-2385`・
  `page.module.css:356-357`: いずれも成立しなくなった前提・実在しない識別子・削除済み機能の
  名指し（個票に1件ずつの failure_scenario がある）

## 進め方の注記

**件数を消すのではなく、型を検知器にできないかを先に見る。** 上記のうち
「実在しない識別子の名指し」は[T810](T810.md)が`dead_identifier_refs`の射程を
コード内コメントへ広げれば機械的に拾える。**T810を先に入れてから残りを手で直す**のが
効率が良く、取り残しもその場で分かる。

## 共通の進め方

- **対象は下の一覧が正本**。個票は [history/2026-09-13_all_shards.md](../.claude/commands/review/history/2026-09-13_all_shards.md) にあり、そこから対象を復元できる
  （第7回で[T735](T735.md)がP2/P3の個票を復元できず対象を確定できないまま残った失敗を
  繰り返さないため、一覧をこのエントリに固定する）。
- **範囲は性質から導く**。一覧はレビューが見つけた分であり、同じ性質に当てはまる箇所を
  機械的に列挙し直してから直す（一覧の件数だけを消して終わりにしない）。
- **一覧に無い箇所を足したら、その件数もここへ書く。** 逆に「一覧にあるが直さない」と
  判断したものは理由を残す。
- 由来: 統合レビュー第8回。全体像は [history/2026-09-13_all.md](../.claude/commands/review/history/2026-09-13_all.md)。
