# T601: T595〜T597の完了条件乖離是正（出発時刻ドラッグタイムライン化・デッドコード/ドキュメント整合）

状態: 起票（2026-09-05、ユーザー指摘の総点検から）

## 背景

ユーザー報告「時刻スライダーが出なくなった」を受けT596を調査したところ、`DynamicLayerTimeSlider`
撤去の宣言と実態（未削除）の乖離、および出発時刻UIがドラッグ式タイムラインという期待と
異なる（クイックボタン+datetime-local）ことが判明。これを機にT595・T597・T593も含め
直近UI改修シリーズを監査した結果、同種の「完了条件・ドキュメントの宣言と実装の乖離」が
複数見つかった。詳細・監査結果は追って本ファイルへ追記する。

## 対応方針（実装しながら追記）

- T596: 出発時刻ピッカーをドラッグ式タイムライン化（`DynamicLayerTimeSlider`を再利用）。
- T597: モバイルの候補0件時の空状態表示欠落を修正、モバイル分岐のテストカバレッジ追加。
- T595: `docs/modules/frontend/*.md`の複数の陳腐化記述、`page.tsx`/`routeStyleModes.ts`の
  削除済み関数を参照するコメント、未使用CSSクラスを是正。
- T593: backendのdocstringが参照する削除済みフロントファイル名を是正。
