# T594: 動的way値配信へ走行速度を伝播する（dynamic_way_value_needs_speed）

状態: 未着手（2026-09-05起票、分割元は[T590](T590.md)）

## 背景

二乗則材料は走行速度に依存するため、`GET /api/region/dynamic-way-values`・`WindWayService`・キャッシュキー・フロントのフェッチへ速度を通す必要がある。
