# T678. backendの残骸を撤去しruffを導入する

規模S。

状態: 未着手（2026-09-08起票）

## 背景

統合レビュー第5回（[2026-09-08_all.md](../../.claude/commands/review/history/2026-09-08_all.md)）
が検出した指摘のうち、下表の5件を1つの起票単位として扱う。

未使用import 13件はruff F401なら共通化のコミット時点で止まっていた。
backendにmypyも無く、Protocol引数順の不一致・型注釈の食い違いも検知できない。

## 対象の指摘

| # | Sev | 対象 | 内容 |
|---|---|---|---|
| 8-4 | P3 | `evaluation_service.py:32-33` | `self._preference`が一度も読まれない（必須引数だけ残存） |
| 8-8 | P3 | `batch/` 6ファイル13シンボル | `_common.py`共通化後の未使用import。backendにPython lintが無く機械検知できない |
| 8-11 | P3 | `road_graph_engine.py:1567` | 理由コメントの無い関数内import（循環回避ではない） |
| 8-12 | P3 | `match_designations.py:114` | 無意味なローカル再束縛 |
| 11-6 | P3 | CI | backendにruff等のPython lintがCI・pre-commitに無い（`metrics.md:82`でDEFER記録済み）。8-8の13件はruff F401なら共通化のコミット時点で止まっていた。backendにmypyも無く3-6・3-7が検知できない |

各項目のEvidence・failure_scenario・Recommendationはレビュー結果の全件表を参照する。

## 着手時に決めること

- 上記のうちどれを実施し、どれを見送るか（見送る場合は理由を本ファイルへ残す）
- 実施順序（レビューのPhase 8は「検証の自動化→境界の固定→実害のある修正→
  現状文書の是正→内部の再配置」を提案している）
