# T467. ゼロベース網羅レビュー§5(backend/api+batch)の残指摘12件対応

状態: 起票

- 背景: [T426](T426.md)棚卸し結果の§5（backend/app/api/ + backend/app/batch/）、有効11件・
  要確認1件。元は[zero-base-review-2026-08-30.md](../zero-base-review-2026-08-30.md)の§5。
- 注意: 要確認1件（項目8、`api/dependencies.py: client_id`）はT462（openrouteservice
  エンジン削除、並行実施中）が同ファイルの別関数（エンジン分岐）を大きく書き換え中の
  ため、着手前にT462の完了・push状況を確認し、必要ならfetchしてから該当箇所を
  再確認すること。
- 対象12件（severity・file:line・要旨。P1の2件[weather.py等の429重複、axis_admin.pyの
  priority_overrides未検証]は別タスクで対応検討中のため対象外）:
  1. `axis_admin.py`の`to_definition()`(L276-299)/`_to_response()`(L317-336) P2:
     `_to_response()`が16フィールドを手動コピーし続け、同期ペアの片側更新漏れリスク。
  2. `import_accidents.py`(L107)・`import_pbf.py`(L177)の`_status_count`、
     `precompute_edge_attribute_counts.py`(L75)・`precompute_elevation_attributes.py`
     (L55)・`precompute_way_attribute_counts.py`(L60)の`_chunked`が
     `_common.py`新設後も重複したまま P2。
  3. `match_designations.py`(L143/L163) P2: マッチング実行後に系譜追跡用run idを
     別クエリ取得、並行実行時に不整合しうる。
  4. `basemap.py: basemap_refresh`(L26-34) P2: 基礎地図キャッシュ削除APIが無認証。
  5. `health.py: db_status`(L101-137) P2: `GET /api/debug/db-status`が無認証。
  6. `axis_admin.py: unpublish_axis_definition`(L417) P2: `assert definition is not
     None`（`-O`実行時に消える）。
  7. `import_pbf.py`(L381-456)・`import_accidents.py`(L213-244)・`import_designations.py`
     (L287-313) P3: `*_import_runs`のrunning状態がプロセスクラッシュ時に永久残留。
  8. `api/dependencies.py: client_id`(L53-60) P3: `request.client`がNoneのとき
     レート制限キーが固定文字列"unknown"。【要確認、T462完了待ち】
  9. `region.py: region_axis_inspector`(L155-175) P3: タイル向け
     `check_tile_rate_limit`を座標なしAPIが流用。
  10. `debug_admin.py: read_recent_logs`(L43-50) P3: `limit`パラメータに上限
      チェックが無い。
  11. `import_accidents.py`(L178-181)・`import_designations.py`(L255-257) P3:
      ファイルダウンロードがforループ内で直列await。
  12. `precompute_edge_attribute_counts.py`(L106/L131-159) P3: run id取得が
      チャンク処理開始前の1回のみ。
- 対応方針: 未検討。
