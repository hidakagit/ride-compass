# T659. 評価結果が誤る3件を直す（openness欠損値・逆回りレグ・AxisComposerの素通し）

規模M。

状態: 未着手（2026-09-08起票）

## 背景

統合レビュー第5回（[2026-09-08_all.md](../../.claude/commands/review/history/2026-09-08_all.md)）
が検出した指摘のうち、下表の3件を1つの起票単位として扱う。

いずれも**ユーザーが見る値が誤る**。1-1と1-2はレビュー実施者がコードを動かして再現済み。
1-1はスカラー版と配列版のどちらの意味論を正とするかの判断を含む（`material_coverage.py`が
`missing_semantics="unknown"`と宣言している以上、評価不能へ倒すのが一貫する）。

## 対象の指摘

| # | Sev | 対象 | 内容 |
|---|---|---|---|
| 1-1 | P1 | `domain/axis_definitions.py:631-645,693-704` | 全term`required=false`かつ全材料欠損のとき、スカラー版は`None`、配列版は0代入で評価し値を返す。**公開軸`openness`が該当**し、`way_landcover`未計算のwayが難易度**100＝最悪帯「吹きさらし」**として地図・結果パネルに出る。本番経路は配列版。実測: `eval |
| 1-2 | P1 | `services/road_graph_engine.py:1377-1380` | 逆回り候補のレグ割当てが、ソースコメントと`routing-engine.md:62`の契約（先に走る側が往路配列）と**正反対**。`list(reversed(leg_of_edge))`は元のレグ添字をそのまま運ぶため、走り始めを「帰着時刻の風」、走り終わりを「出発時刻の風」で評価する。`_pick_better |
| 1-4 | P1 | `AxisComposer.tsx:413-441,472-479,497-504,823-832` | 「編集欄を持たないフィールドの素通し」が同一ファイル内4箇所への手作業リスト。backendの`AxisDefinitionPayload`が全フィールドoptional/既定値付きのため、追加漏れはtscもbackend検証も通り、**そのフィールドが既定値で静かに上書きされる**（414行のコメント自身が「静かなデ |

各項目のEvidence・failure_scenario・Recommendationはレビュー結果の全件表を参照する。

## 着手時に決めること

- 上記のうちどれを実施し、どれを見送るか（見送る場合は理由を本ファイルへ残す）
- 実施順序（レビューのPhase 8は「検証の自動化→境界の固定→実害のある修正→
  現状文書の是正→内部の再配置」を提案している）
