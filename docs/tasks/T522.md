# T522: ルート生成が異常に遅くなる事象の調査（prepare段階＝探索用グラフ構築が支配的）

状態: 調査完了・改善タスクへ分割済み（2026-09-02。評価ロジック全体を本番VMで再計測し、
改善候補を[T536](T536.md)〜[T540](T540.md)として起票した。本ファイルは調査記録として残す。
2026-09-01起票時点からの経緯: 本番環境[Oracle VM]へ直接ルート生成リクエストを送り
再現・実測ログで内訳を特定、T529[別タスク]でscipy→rustworkx lazy A*化、T534で軸別スコア
キャッシュ、標高データ二重取得の解消を実施済み）

## 評価ロジック全体の再点検（2026-09-02、本番VM実測）

T534完了後もルート生成が遅いという指摘を受け、開始（prepare）から完了（RouteScorer）までを
全て読み直し、本番VM（2vCPU）上でbackendイメージの使い捨てコンテナ（`--network=host`、
稼働中コンテナとは別プロセス）からステージ別に計測した。条件は東京駅
`(35.681,139.767)`・周回30km・既定重み・風は固定値（4m/s・風向200度）。対象bboxは
split済みで読み取り経路のみを通り、本番DBへの書き込みが無いことを
`max(osm_raw_ways.split_at)=2026-09-01 05:18 UTC`（計測より前）・`elevation_attributes`の
直近2時間の新規行0件で確認した（1本目の計測は`is_split_up_to_date`の事前ガードを
入れ忘れて実行したため、事後にこの確認を行った。2本目はガード付き）。

**現状の内訳**（Edge 477,320件・Node 181,224件、z12タイル16枚）:

| ステージ | 温パス | 冷パス |
|---|---|---|
| 材料取得（タイルキャッシュ結合／DB読み出し） | 336ms | 29,300ms |
| `build_lazy_road_graph` | 957ms | 同左 |
| `compute_routable_node_ids` | 726ms | 同左 |
| `build_node_spatial_index`＋`_build_node_pair_index` | 201ms＋165ms | 同左 |
| `engine.prepare`合計 | 2,321〜2,467ms | 約31,700ms |
| A* 24本（軸スコアキャッシュ温／冷） | 8,269ms／17,726ms | 同左 |
| `trace_loop`×8を`asyncio.gather`＋`to_thread`で実行 | 10,376ms | |
| `trace_loop`×8を直列実行 | 8,471ms | |
| `evaluate_loops`（8候補、GSI問い合わせ無し） | 432ms | |

- A*のコスト関数は474,153回呼ばれ、ユニークEdgeは176,736件（bbox全体の37%）。
  推定関数は469,996回。
- 全477,320 Edgeのコストをスカラー版`cost_fn`で一括計算すると温キャッシュ28,885ms・
  冷キャッシュ37,163ms（＝温でもEdge1本あたり約60µs）。
- 8方位の`to_thread`並列は直列より約2秒遅い（GIL競合。rustworkxはEdgeごとにPython
  コールバックへ戻るためGILを手放せない）。
- 8方位のうち2方位（38.9km・47.8km）が距離フィルタ±5kmで棄却された。

**Edge1本あたりのコスト計算の内訳**（cProfile、温キャッシュ、30,000件）: 累積2,474msのうち
`_topological_axis_order_cache_key`が1,059ms（43%。`AxisDefinition.materials`プロパティを
Edgeごとに全軸ぶん再構築している）、`compute_cost_from_axis_scores`289ms、
`evaluate_axis_scalar`（風軸）285ms、`EdgeCostResult.model_construct`250ms。
`_build_estimate_cost_fn`のPydantic`Coordinates`生成を素のhaversineへ置き換えると
A* 24本は8,269ms→7,045ms。

**ベクトル化の試作**（同じコンテナ内）: 軸スコアキャッシュから「Edge×公開軸8」のnumpy行列を
作り、リクエスト時に風の列と重みだけを掛けてコスト配列を作る方式を試した。

| 項目 | 現行 | 試作 |
|---|---|---|
| 477,320 Edgeのリクエスト時コスト計算 | 28,885ms | 110ms |
| A* 24本（`edge_cost_fn=list.__getitem__`、推定も配列の`__getitem__`） | 8,269〜17,726ms | 372ms（推定配列の計算237ms） |
| A* 24本（同じグラフで自明なPython lambda） | | 412ms |

行列を辞書キャッシュから組み立てる部分は19,585msかかったが、これは試作の都合であり、
実設計では`evaluate_axis_array`（`compute_edge_costs_bulk`の計算フェーズ）でタイル
読込時に直接作る。試作は丸めを簡略化したためスカラー版と1,331件（0.28%）のコスト
不一致があり、実装では既存の`round1_array`・Neumaier加算を使う。周回距離は
現行と同一（34.3/34.4/38.9/47.8/33.7/33.2/34.0/34.3km）。

**メモリ試算**: `axis_score_cache`の1エントリは約1,265B（`tracemalloc`実測）。上限
1,000,000件で約1.27GB、この30km 1回で約60万件・約600MBが積まれる（コンテナ上限6GB）。

**効果が薄いと判断したもの**: 材料取得のPydantic構築（10万行で検証0.42秒、
`model_construct`は0.75秒でむしろ遅い。冷パス29秒の主因はDB問い合わせと行走査）、
レグ1・レグ3の多目標Dijkstra化（ベクトル化後のA*が24本で0.4秒のため不要）、
Contraction Hierarchies等の前処理系（リクエスト単位の重みと両立しない）、SQL・索引
（上記「SQL/DB側の調査」節のとおり余地無し）。

**起票した改善タスク**: [T536](T536.md)（静的スコア行列＋ベクトル化コスト＋C実装
コールバックのA*。局所改善B群・メモリ対策Cもここへ集約）、[T537](T537.md)（探索用
グラフ・索引のタイル集合キャッシュ）、[T538](T538.md)（冷パスの永続化・予熱）、
[T539](T539.md)（分割の事前バッチ化）、[T540](T540.md)（距離超過の早期打ち切り）。
重み付きA*（ヒューリスティック1.2〜1.5倍）は最適性を捨てる変更で仕様書34章に触れるため
起票せず、採否はユーザー判断として記録のみ残す。開発機で観測した「2回目が1回目より
悪化」は本番で再現せず、開発機固有のリソース競合と判断して追跡を打ち切る。

## 材料辞書統合＋EdgeCostResult.model_construct化（2026-09-01、T533派生）

T529実装（rustworkxのlazy A*、探索が実際に訪れたEdgeのみコスト計算）後も、Edge単位の
コスト計算自体（`compute_edge_cost`）がなお1リクエストあたり最大24回×訪問Edge数ぶん
呼ばれるホットパスであるため、その内部を実データ（渋谷相当bbox、24.7万Edge、
ローカルdev DB）で検証した。

1. **材料辞書の統合（EdgeMaterialBundle）**: `get_edge_materials_batch`（T248で
   クエリ自体は1回のJOINへ統合済み）が戻り値だけ4つの辞書（surface_attributes/
   edge_attribute_counts/way_tags/elevation_attributes）+designated_edge_idsの集合へ
   再分割していた技術的負債を発見し、Edge ID→統合済み1オブジェクト
   （`@dataclass(frozen=True, slots=True)`のEdgeMaterialBundle、`domain/attributes.py`）
   へ書き換えた。**実測: 辞書アクセスのみを切り出すと3.5倍速いが、`compute_edge_cost`
   全体を通した公平な比較では速度差はほぼ無かった（1.02倍、誤差の範囲）**——
   `compute_edge_cost`自体の内部処理（軸別スコア算出）が支配的で、辞書アクセスの差は
   誤差に埋もれるため。ルーティングの速度改善への寄与は見込めないが、T248の技術的
   負債是正・コードの一貫性向上として実施価値ありと判断し実施した（同種の負債が他にも
   無いかの横断監査は[T533](T533.md)として別途起票済み）。
2. **EdgeCostResult.model_construct化**: cProfileで内部プロファイリングした結果、
   bulk版`compute_edge_costs_bulk`は既に`EdgeCostResult.model_construct(...)`で
   Pydanticバリデーションをスキップしていたが、実際の探索ホットパスで使われる
   スカラー版`compute_edge_cost`だけが通常のフルバリデーション付きコンストラクタを
   使っていた不整合を発見。同じ`model_construct`パターンへ揃えた（渡す値はすべて
   内部計算済みの正しい型で、numpy値の混入が無いことも`evaluate_breakpoint_linear`の
   `float(...)`明示キャスト等で確認済み）。**実測: 24.7万Edge全件のcompute_edge_cost
   実行がmedian 6026ms→5564ms（約1.08倍、8%改善）**。

いずれもバックエンド全体1229件green（`pytest -q -m "not postgis"`）。本番未反映
（次項で反映・サンプリング実測予定）。

`compute_edge_axis_scores`自体（軸別スコア算出、タグ正規化）が`compute_edge_cost`の
累積時間の6割超を占め、これは評価ロジックそのものであり軸スタジオの自由度を保ったまま
安全に削れる部分ではない。副次的に`topological_axis_order`のキャッシュキー計算
（`AXIS_DEFINITIONS`全軸を毎回舐めてタプルを組み立て直す処理、呼び出しごとに約4us）も
見つかったが、`AXIS_DEFINITIONS`が複数のテストファイル・本番の`refresh_axis_definitions`
から直接`clear()+update()`で書き換えられる構造のため、安全な高速化には軸定義変更の
検知機構自体の設計変更が必要で、効果（全体の5%程度）に見合わないと判断し見送った。

## 軸別スコアの事前計算キャッシュ（T534、2026-09-01完了・本番反映済み）

`compute_edge_cost`内部（風以外の軸別スコア）をEdge単位でプロセス内キャッシュする
設計を実装・本番反映した。詳細・実測結果は[T534](T534.md)参照。本番サンプリング
（同時実行の干渉を避けた単独実行）でtrace_msが1.60〜1.90倍に改善したことを確認済み。

## SQL/DB側の調査（2026-09-01、T534完了後の追加調査）

`get_edge_materials_batch`のクエリを`EXPLAIN (ANALYZE, BUFFERS)`で分析した結果、
**クエリ自体・インデックス構成にチューニングの余地は無いと判断した**。対象bbox
（半径13km、中心部東京）がroad_edges等の対象テーブルのほぼ全件（road_edgesで約78%、
他テーブルはほぼ100%）を要求しており、PostgreSQLが選んだ並列Seq Scan+Hash Joinは
この規模では妥当な計画（インデックスを使うより速い）。SQL実行時間そのものは
247,191Edgeで数秒台（asyncpg直接fetch 3,465ms、SQLAlchemy ORM行構築込み5,110ms、
オーバーヘッド1.47倍）に収まり、過去に観測した「materials_ms=数十秒」はDBキャッシュ
未温状態でのディスクI/O、または複数リクエスト同時実行時のCPU競合が主因と考えられる。

本番のPostgreSQL設定（`shared_buffers=3GB`、対象テーブル合計約1.7GB）・実測キャッシュ
ヒット率（`pg_statio_user_tables`で99.7%）を確認したところ、**本番側は既に十分
チューニングされておりshared_buffersを上げる余地も無い**と判明した。開発機
（`shared_buffers=128MB`＝PostgreSQL初期値のまま、対象データ504MBに対しヒット率89.0%）
は未チューニングだが、これは開発機のみのローカル計測精度の話であり本番へは無関係
（開発機のshared_buffers引き上げはユーザー確認待ち、未実施）。

## 評価ロジック入口〜出口の再点検（2026-09-01〜02、T534完了後の追加調査）

軸別スコアキャッシュ導入後、評価ロジック全体（cost_fn→軸評価→候補確定後の表示構築→
RouteScorer）を通しで再点検し、2件の改善候補を発見。うち安全度の高い1件を実装した。

1. **`_build_segment_details`（候補確定後の区間表示）の軸スコア再計算**（未実装、見送り）:
   探索中に`axis_score_cache`で計算済みのはずの軸別スコアを、表示構築時は非キャッシュ版
   `compute_edge_axis_scores`で再計算している。ただし表示側の標高（`_fetch_elevation_
   attributes`、次項参照）は探索時より新しいデータを持ちうるため、単純にキャッシュを
   再利用すると探索後に新しく判明した標高（gradient軸）を取りこぼす懸念があり、安全な
   実装にはもう一段の設計判断（差分検知）が必要。`evaluate_ms`は3フェーズ中最小のため
   優先度を下げ、今回は見送った。
2. **`_fetch_elevation_attributes`の標高データ二重取得**（実装済み、2026-09-02）:
   候補確定後、`ElevationAttributeService.get_attributes_for_graph`が
   `context.materials`（探索フェーズで既にDB取得・タイル単位キャッシュ済みの
   `EdgeMaterialBundle`）に既にある標高データを、同じ`elevation_attributes`テーブルへ
   もう一度問い合わせて取り直していた。`context.materials[edge_id].elevation_attribute`
   が既にあるEdgeはそれをそのまま使い、無い（事前計算バッチ未実行の）Edgeだけを
   従来どおりサービス経由で取得するよう`road_graph_engine.py:
   _fetch_elevation_attributes`を変更した。`ElevationAttributeService._repository_lock`が
   候補評価（`asyncio.gather`、最大8件）を直列化する構造のため、削減したDB往復の分だけ
   候補数倍のレイテンシが積み上がっていた分を解消する。正しさへのリスクは無い
   （同じテーブルの同じデータを二重に読んでいただけ）。回帰テスト2件
   （`test_elevation_attribute_service_is_not_queried_when_materials_already_has_
   precomputed_data`・`test_elevation_attribute_service_is_queried_only_for_edges_
   missing_from_materials`）で「全Edge事前計算済みなら問い合わせ0回」「一部欠けている
   場合は欠けている分だけ問い合わせる」を確認。バックエンド全体1478件
   （postgis含む）green。

なお、「重み0の軸に関する材料計算を丸ごと省く」という追加最適化案も検討したが、
`axis_score_cache`（T534）はEdge単位・重み非依存で複数リクエスト間キャッシュを
共有する設計のため、特定リクエストの重み0を理由に軸の計算自体を省くと、他の
重み設定を持つリクエストが同じキャッシュを再利用した際に不完全なデータ
（本来は値があるはずの軸がデータ無し扱いになる）を返してしまう。加えて軸別スコアは
コスト計算だけでなく`RouteSegmentDetail.axis_difficulties`という区間表示にも
使われるため、重み0でも計算自体は必要になるケースがある。効果もキャッシュ共有性の
毀損に見合わないと判断し不採用とした。

## 本番環境での再現・内訳調査（2026-09-01）

開発機での251秒・355秒という実測が開発機固有のリソース競合（並行Claude Codeセッション等、
[[parallel-worktree-resource-contention]]参照）によるものか切り分けるため、本番
（Oracle VM、`https://193.123.166.150/`、backend/.env.oracle.localのSSH鍵で接続）へ
直接`POST /api/routes/generate`を2回投げ、`GET /api/admin/debug/logs`
（Basic認証、`ridecompass-backend.env`に`ADMIN_BASIC_AUTH_USERNAME=admin`/
`ADMIN_BASIC_AUTH_PASSWORD=admin`——**この認証情報が極めて弱い値である点は本タスクの
スコープ外の別問題として後述**）で実測ログを取得した。起点・距離は開発機と同一
（`(35.6817502, 139.7634149)`・周回30km）。

**1回目（未split・冷パス、`req:c2e14abc7655`）**: `total_ms=103590`
（`prepare_ms=77686 trace_ms=15382 evaluate_ms=10521`）。開発機の251〜355秒より
明確に速いが、依然として支配的にprepareが遅い。ログのタイムスタンプから、
`prepare_ms=77686`の内訳は次のとおりほぼ全て説明できた（`GraphService.
get_or_build_graph_with_attributes`の既存ログ+ログ間のタイムスタンプ差分で算出）:

| ステージ | 所要時間 | 内容 |
|---|---|---|
| `_ensure_tiles_cached`+`_ensure_split_up_to_date`（2回、後述） | 約63ms | Redis cache-aside+PostGIS `LIMIT 1`判定。無視できる規模 |
| `get_way_specs_with_closure`（closure_ms） | 9,918ms | bbox内Way＋近傍Wayの取得（このbboxはway=107,328件） |
| `build_road_graph`（build_ms、`asyncio.to_thread`保護済み） | 8,790ms | 交差点分割（CPU） |
| `save_graph`（save_ms） | 26,946ms | 分割結果の永続化。primary_edges=333,529・primary_ways=82,840・nodes=128,524。**単独最大のステージ** |
| `get_edge_materials_batch`（materials_ms、uncachedフォールバック経路） | 17,428ms | 333,529 Edge分のsurface/attribute_counts/way_tags/elevation/designated一括取得 |
| **無計測の残差**（wind取得＋evaluate_graph＋build_sparse_graph＋prepareの索引構築） | **約13,821ms** | ログのタイムスタンプ差分（materials_ms終了18:27:19.566〜prepare終了推定18:27:33.387）から逆算。後述の追加計測で正式に内訳化した |

（`get_way_specs_with_closure`〜`save_graph`は`get_or_build_graph_with_attributes`の
`rebuild`ログ1行に`closure_ms=9918 build_ms=8790 save_ms=26946 total_ms=46200`として
まとまっている。`_build_search_materials_uncached`の`materials_ms=17428`と合わせて
63,628ms、`prepare_ms`全体77,686msとの差13,821msが上表の残差）

**この無計測の残差＝evaluate_graph（探索コスト計算、333,529 Edge対象）・
build_sparse_graph・`prepare()`内の索引構築（`build_node_spatial_index`・
`_build_node_pair_index`）は、いずれも同期関数で`asyncio.to_thread`による保護が
無い**——`get_or_build_graph_with_attributes`の`build_road_graph`が同種の理由
（改善計画T261、T105のCPU専有→ヘルスチェック無応答→強制再起動と同型の障害を受けた対応）で
`asyncio.to_thread`化済みなのと対照的。つまりこの約13.8秒間、実行中のuvicornワーカーの
イベントループが丸ごと塞がれ、他の同時接続リクエスト（`/health`含む）も応答できない
状態になっている可能性が高い。

**2回目（split済み・タイルキャッシュ経由、`req:e82585369dc4`、1回目の約5分後）**:
`total_ms=24744`（`prepare_ms=23510 trace_ms=820 evaluate_ms=414`）。ログに
`get_or_build_graph_with_attributes rebuild`も`_build_search_materials_uncached`も
出現せず、想定どおり高速経路（`_build_search_materials_from_tile_cache`）を通った。
**それでも`prepare_ms=23510`と、`graph_service.py`のdocstringが述べる「真の温パス
4.9秒」（改善計画T248、別の小規模エリアでの実測）よりはるかに遅い**——これは、
上記「無計測の残差」約13.8秒（evaluate_graph等のCPUバウンド処理）がキャッシュの有無に
関わらず発生する固定コストであり、この規模（Edge数十万件級）のbboxでは温パスでも
下限として乗ってくることを示唆する（DB/タイルキャッシュがどれだけ効いても、
このCPU計算自体を高速化・並列化しない限り数秒〜十数秒を切れない）。

**開発機の「2回目(355秒)が1回目(251秒)より悪化」という異常は本番では再現しなかった**
（本番2回目は1回目の1/4以下に短縮）。これは開発機固有のリソース競合が2回目悪化の
主因であり、コード自体の欠陥ではない可能性を支持する（引き続き確定はできないが、
本番の一貫した挙動がより信頼できるシグナルと考える）。

Redis経由キャッシュの失敗（`[cache:edge-geometry-redis] failed after 605ms/2742ms`、
`タイル材料キャッシュのバックグラウンド温めに失敗 error=TimeoutError()`×2）も本番で
確認できたが、いずれも`prepare_ms`ではなく`trace_ms`区間・レスポンス送出後の
バックグラウンド処理で発生しており、影響は数秒未満に留まる——Redisが主因でないという
既存の結論（下記）を追認するのみ。

## 計測用ログの追加（2026-09-01、コミット済み・本番未反映）

上記「無計測の残差」を今後の調査で毎回タイムスタンプ差分から逆算せずに済むよう、
`RoadGraphEngine._build_search_graph`（`materials_ms`/`wind_ms`/`cost_ms`
[evaluate_graph]/`sparse_ms`[build_sparse_graph]/`total_ms`）と`prepare()`
（`index_ms`＝`build_node_spatial_index`+`_build_node_pair_index`）へステージ別
所要時間ログを追加した（`road_graph_engine.py`、docs/logging.md「処理ステージの
サマリ」方針に準拠）。関連テスト（`test_road_graph_engine.py`・`test_route_generator.py`）
はpass、ログ追加のみで挙動変更なし。**本番へは未push**——次にこのbbox（または別の
未splitな大規模エリア）で冷パスを再現した際、この計測で`cost_ms`/`sparse_ms`/
`index_ms`の内訳が直接ログへ出るようになる。

## 元の背景（起票時点の記録、変更なし）

- 背景: T518の実機確認中（開発機、中心部東京30kmループ生成）、通常想定より大幅に遅い
  （3〜6分）ルート生成に遭遇した。当初`GET /api/debug/stats`でRedisキャッシュ呼び出しが
  軒並みTimeoutErrorだったことから「Redis疎通不能→高コストな直接計算のfallback多発」を
  主因と推測したが、`GET /api/admin/debug/logs`（T517、Basic認証必須。認証情報は
  backend/.env: ADMIN_BASIC_AUTH_USERNAME/PASSWORD）で`route_generator.py`の
  ステージ別所要時間サマリログ（`generate engine=... prepare_ms=... trace_ms=...
  evaluate_ms=... total_ms=...`）を直接確認したところ、**この当初の推測は誤りだった**
  ——Redisは主因ではなく、`prepare()`（探索用グラフ構築、`road_graph_engine.py:
  _build_search_graph`／`GraphService.get_search_materials_for_bbox`）自体が支配的に
  遅いことが判明した。

- **実測ログ（2026-09-01、backend構造化ログより該当4件を全件引用）**:
  ```
  02:24:57 origin=(35.68,139.76) target_km=30.0 -> candidates=6
    prepare_ms=251140 trace_ms=20891  evaluate_ms=39188 total_ms=311219
  02:34:20 origin=(35.70,139.74) target_km=30.0 -> no context (road data unavailable)
    prepare_ms=219
  02:39:57 origin=(35.73,139.71) target_km=30.0 -> no context (road data unavailable)
    prepare_ms=16
  03:00:48 origin=(35.68,139.76) target_km=30.0 -> candidates=6
    prepare_ms=355516 trace_ms=2844   evaluate_ms=2265  total_ms=360625
  ```
  - 候補が実際に見つかった2件（同一起点で2回）は、**`prepare_ms`だけでtotal_msの
    80〜99%を占める**（251.1秒/311.2秒＝81%、355.5秒/360.6秒＝99%）。`trace_ms`
    （8方位のDijkstra探索）・`evaluate_ms`（候補評価・軸別difficulty計算、Redis
    キャッシュのfallback先である`region:gradient-way-values`/`region:wind-way-penalty`
    はこの中に含まれる）は、2回目にいたっては合計5秒程度に過ぎず、遅延の主要因ではない。
  - **「起点付近の道路データが未整備」（no context）の2件は`prepare_ms`が16ms・219ms
    と極めて高速**——ユーザー質問への回答: 道路データ未整備そのものは遅延の原因では
    なく、むしろ最速で終わる経路（グラフ構築の入口で早期return）である。今回「文京区
    付近が遅いのでは」という懸念は誤りだったと判明した。
  - 2回目（355.5秒）が1回目（251.1秒）より悪化している点は、プロセス内タイル単位
    キャッシュ（`_build_search_graph`のdocstring「同一エリアへの2回目以降のリクエストは
    DBへ一切アクセスしない」という設計意図）が実際には効いていない、または他の要因で
    上書きされていることを示唆する——本タスクの主要な未解明点。

- 分かっていること:
  - Redisサーバー自体は健全（生TCPで`127.0.0.1:6379`へ直接PINGを送ると即座に`+PONG`が
    返る、過負荷やダウンではない）。`app/infrastructure/redis_client.py`は接続・応答
    タイムアウトを意図的に0.2秒へ短縮設定済み、10秒のサーキットブレーカーも実装済みで、
    `dynamic_way_value_cache.py`のfail-open設計も仕様通り機能している——Redis周りは
    実装上の欠陥ではなく、**当初の主因という推測自体が誤りだった**（stats単体では
    「エラー件数」しか見えず、「そのエラーが遅延の何%を占めるか」を見誤った）。
  - `prepare()`＝`_build_search_graph`＝`GraphService.get_search_materials_for_bbox`
    （bbox内のトポロジ＋材料をz12タイル単位のプロセス内キャッシュ経由で取得）が支配的に
    遅い。半径計算は`radius_km = distance_km/3`・`margin_km = max(2.0, radius_km*0.3)`
    （`route_generator.py`: `RADIUS_RATIO`・`road_graph_engine.py`: `BBOX_MARGIN_RATIO`/
    `BBOX_MARGIN_MIN_KM`）で、30km指定なら半径約13kmのbbox（中心部東京の稠密な道路網を
    広範囲にカバーする規模）になる。
  - 背景で`jma_tile_prewarm_service`（T509系、約10分間隔で1983タイルを毎回フル取得、
    1回34〜94秒）が動いている。1回目の生成（02:19:46頃〜02:24:57）とは時間的に重ならず、
    2回目（02:54:48頃〜03:00:48）は02:58:44完了の予熱ラン（開始約02:57:10）と一部
    重なるが、重なりは94秒程度に留まり355秒の遅延全体を単独では説明できない——
    寄与している可能性はあるが主因と断定はできない。
- 分かっていないこと（要調査、2026-09-01の本番調査後に更新）:
  - ~~`get_search_materials_for_bbox`のプロセス内タイルキャッシュが実際に機能しているか~~
    → **本番では機能していることを確認済み**（2回目`prepare_ms=23510`、rebuild/
    uncachedログが出ずタイルキャッシュ経路を通過）。開発機の「2回目悪化」は開発機固有の
    事情（並行セッション等）である可能性が高く、キャッシュ機構自体の欠陥ではなさそう。
  - ~~`prepare()`内部の内訳が無い~~ → タイムスタンプ差分から`evaluate_graph`＋
    `build_sparse_graph`＋索引構築の合計が約13.8秒と判明、ステージ別ログも追加済み
    （上記「計測用ログの追加」節）。ただし`evaluate_graph`・`build_sparse_graph`・
    `build_node_spatial_index`・`_build_node_pair_index`の4つを合算した値であり、
    このうちどれが支配的かは追加ログを本番へ反映して次の冷パス再現時に確認する必要がある
    （未確定）。
  - **新規判明**: この合計約13.8秒のCPUバウンド処理群は`asyncio.to_thread`保護が
    無く、実行中イベントループを塞いでいる可能性が高い（未検証——実際に他リクエストが
    詰まるかはこの13.8秒間に別リクエスト・`/health`を打って初めて確認できる）。
  - 本番環境でも同様の事象が起きることを確認済み（1回目`total_ms=103590`）。ただし
    開発機の251〜355秒より明確に速く、開発機のリソース競合が実際に遅延を増幅している
    ことも同時に示唆される——「本番でも起きる根本問題」と「開発機で増幅される要因」の
    両方が存在する。
  - ユーザー体験への影響範囲: 中心部東京・30km・周回モードでのみ確認。距離が短い場合や
    データ密度が低いエリアでも同程度の`prepare_ms`が発生するのか（bbox内の道路密度に
    比例するなら、都心以外では問題にならない可能性がある）は未確認。
- 次の調査候補（2026-09-01更新、完了分は取り消し線）:
  1. ~~`_build_search_graph`/`get_search_materials_for_bbox`内部にステージ別タイミング
     ログを追加~~ → 完了（road_graph_engine.py、本番未push）。
  2. ~~同一bboxへの2回目呼び出しがプロセス内キャッシュを実際にヒットしているか~~ →
     本番で確認済み（ヒットしている）。
  3. ステージ別ログを本番へ反映（push）し、未splitな別エリアで冷パスを再現して
     `cost_ms`（evaluate_graph）・`sparse_ms`（build_sparse_graph）・`index_ms`
     （索引構築）のうちどれが約13.8秒の大半を占めるかを特定する。
  4. 3で支配的と判明した処理を`asyncio.to_thread`で保護し、大規模bboxのルート生成中に
     イベントループが塞がれないようにする（レイテンシ自体は改善しないが、同時接続の
     他リクエスト・ヘルスチェックへの影響を無くす。T105/`build_road_graph`と同じ対応）。
  5. 他セッションが稼働していない静かなタイミングで開発機の同じ生成を再実行し、
     `prepare_ms`が本番同等（〜100秒程度）まで改善するか（＝開発機リソース競合の
     寄与度）を確認する。
  6. 距離・エリアを変えた複数条件で`prepare_ms`を比較し、道路密度との相関を確認する。
  7. （スコープ外・別件として起票検討）本番`ADMIN_BASIC_AUTH_PASSWORD`が`admin`という
     弱い値になっている。T272で導入した/admin保護の実効性が損なわれている。

- **再現条件（2026-09-01実測、開発機）**: 起点座標`(35.6817502, 139.7634149)`
  （千代田区・両国寄り、周回モード・距離30km・既定重み）で2回generateを実行し、
  backendログの`prepare_ms`実測値で**251秒・355秒**（いずれも候補は6件生成に成功）。
  起点座標`(35.70,139.74)`・`(35.73,139.71)`（いずれも文京区付近、同条件）は
  「起点付近の道路データが未整備」で`prepare_ms=219ms`・`16ms`という対照的な速さで
  早期終了した——両者の差は「候補探索に入れたかどうか」であり、道路データの疎密自体は
  低速要因ではない。
