# T846. 「鮮度」タブの名前が、中身の一部しか指していない

規模S。
状態: 着手（2026-09-14起票。ユーザー指摘「鮮度台帳や本番DB含めて『鮮度』タブに入っているけれど、
タブ名が実態に合っていない」から）

## 背景

`/admin`の「鮮度」タブは派生データ鮮度台帳・本番DBの状態・タイルキャッシュ消去を持つ。
鮮度はそのうち1枚が見るものでしかなく、DBの状態（取込run・容量・統計・接続）も
キャッシュを消す操作もこの名前からは読めない。
