# T953. MapOverlayControlsの汎用フック2本を切り出す 規模S

状態: 未着手（2026-09-19起票、統合レビュー第11回のP2/P3）

## 背景

`MapOverlayControls.tsx`（1,188行）に7つの独立した変更理由が同居しており、うち
`usePagedOverflow`（はみ出しのページ送り）と`useHoldRepeat`（長押しリピート入力）の約140行は
**`MapLayerId`・凡例・グループのいずれも知らない汎用フック**で、他の地図UIからも使える。

一方(a)チップのグルーピング・描画、(e)凡例内訳の描画、(f)表示する項目の設定は
「チップ列の見せ方」という1つの理由で同時に変わるため、そこを割る理由は無い（理由つきKEEP）。

## トリガー

**[T945](T945.md)（記述子への集約）完了後も1,100行を超えているとき。**
先に`LAYER_ICONS`を記述子へ移すと変更理由が1つ減るため、それを見てから判断する
——先に分割すると「分割後のどのファイルへ表を置くか」という新しい判断が要る。

## 完了条件

- トリガー成立時に、切り出す／しないの判断とその根拠（実測行数）を記録する
