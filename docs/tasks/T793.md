# T793. ベンチマークの変更で本番backendがデプロイされる

規模S。
状態: 完了（2026-09-15。2026-09-13起票）

## 背景

`.github/workflows/deploy-backend.yml`の`paths`は`backend/**`で、`backend/benchmarks/`の
変更もデプロイ（VM上での`docker build`＋コンテナ入れ替え）を起こす。[T790](T790.md)段階1で
ベンチマークスクリプトを追加したとき、実際に本番のビルドとコンテナ再起動が走った。

`backend/Dockerfile`がイメージへ入れるのは`app`・`fixtures`・`migrations`・`scripts`・
`requirements.txt`だけで、**`benchmarks`はイメージに含まれない**（本番計測はホスト側へ置いて
マウントする）。つまりこのデプロイは本番の動作を何も変えないまま、ビルド時間とコンテナ
入れ替え（`docker stop -t 90`のグレース待ちを含む）だけを消費する。

## 対応と結果

**除外する範囲は「`backend/Dockerfile`のCOPY対象外か」の一点で決めた。** デプロイが変えるのは
イメージの中身と起動時のmigrationだけで、イメージに入らないものを変えてビルドとコンテナ
入れ替え（`docker stop -t 90`のグレース待ちを含む）を走らせても本番の動作は何も変わらない。

```yaml
paths:
  - "backend/**"
  - "!backend/benchmarks/**"
  - "!backend/tests/**"
  - ".github/workflows/deploy-backend.yml"
```

`backend/tests/`も外した。起票時は「テストの変更でデプロイが走ること自体にCIの整合確認
としての意味があるかもしれない」と保留していたが、**テストを走らせるのは`ci.yml`で、
デプロイのワークフローはテストに一切触れない**（`git reset --hard`→`docker build`→
migration適用→コンテナ入れ替えのみ）。デプロイが走っても確認されるものが増えないため、
意味は無いと判断した。

### VM側の副作用（記録しておく）

ベンチマーク・テストのファイルは、このデプロイの`git reset --hard`でVMの
`~/ridecompass-repo`へ届いていた。除外したぶんは届かなくなるため、**VM上で新しい
ベンチマークを回すときは先に手で`git pull`する**（または`workflow_dispatch`で回す）。
ワークフローのコメントにも同じことを書いてある。

`CLAUDE.md`の「`backend/**`パスの変更を検知すると自動デプロイする」も、パスを書き写した
記述だったためワークフローの`paths`を正本として指す形へ変えた（写した側だけが古くなる）。

## 検証結果

**除外が効いていること**（2026-09-15、masterへの連続した2つのpushで観測）:

| push | 変更したもの | 起動したワークフロー |
|---|---|---|
| `3c7ccaa2` | `deploy-backend.yml`ほか | **Deploy Backend to Oracle VM**（success）・CI・Docs Consistency |
| `0f052d4e` | `backend/benchmarks/`の1ファイルのみ | CI・Docs Consistency（**Deployは起動せず**） |

変更前は、[T802](T802.md)でベンチマークを1本足しただけで
`Deploy Backend to Oracle VM 390`が走っていた（2026-09-14、本番の動作は何も変わらず
ビルドとコンテナ入れ替えだけを消費）。

## 関連

- 由来: [T790](T790.md)段階1でベンチマークを追加したときに実際にデプロイが走った。
