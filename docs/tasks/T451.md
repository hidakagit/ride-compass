# T451. MapView.tsxにT432で削除済みのensureWindPenaltyFillLayerを参照する古いコメントが残る

状態: 完了（2026-08-31）

- 背景: `統合レビュー第2回`（`.claude/commands/review/history/2026-08-30_all_2.md`P3-2）の
  指摘。`frontend/src/components/Map/MapView.tsx:1138`付近、`ensureGradientFillLayer`の
  近傍に、T432で既に削除済みの関数`ensureWindPenaltyFillLayer`を参照する古いコメントが
  残っている。
- 対応方針: 該当コメントを削除または実態に合わせて訂正する。
- Confidence: High（frontend/Mapシャードがgrepで削除済み関数への参照を確認）。
- 規模S。

## 実施記録（2026-08-31完了）

- **対応**: `makeEnsureGradientFillLayer`（旧`ensureGradientFillLayer`、T443でファクトリ化）
  近傍のコメントを、実態（比較対象の`ensureWindPenaltyFillLayer`はT432で撤去され、
  風penalty gridFillはDYNAMIC_WEATHER_RENDERERS汎用機構へ移った。勾配gridFillは独立した
  空間フィールドを持たないためこの汎用機構には乗せず専用関数のまま）へ書き換えた。
- grep確認により、`ensureWindPenaltyFillLayer`への参照は他ファイルに無いことを確認。
- **検証**: `tsc --noEmit`・eslint通過。コメントのみの変更のためテスト・実機確認は対象外。
