# T666. lazy_graphの整合ガードを実消費コレクションへ広げる

規模S。

状態: 未着手（2026-09-08起票）

## 背景

統合レビュー第5回（[2026-09-08_all.md](../../.claude/commands/review/history/2026-09-08_all.md)）
が検出した指摘のうち、下表の1件を1つの起票単位として扱う。

ガードが検証している集合と、直後にKeyErrorを起こす集合が違う。2つのキャッシュ世代定数は
どちらも手動運用で機械同期が無く、片方だけ上げるとbbox単位でルート生成が全滅し、
ディスクキャッシュを手で消すまで復旧しない。

## 対象の指摘

| # | Sev | 対象 | 内容 |
|---|---|---|---|
| 2-4 | P2 | `services/road_graph_engine.py:536,548,553` | `_ensure_lazy_graph_consistent`が`lazy_graph⊆graph`しか検証せず、直後にKeyErrorを起こす`full_edge_row`（`score_matrix.edge_ids`由来、**別キャッシュ・別世代定数**）を検証対象外にしている。2つの世代定数はどちらも手動運用で |

各項目のEvidence・failure_scenario・Recommendationはレビュー結果の全件表を参照する。

## 着手時に決めること

- 上記のうちどれを実施し、どれを見送るか（見送る場合は理由を本ファイルへ残す）
- 実施順序（レビューのPhase 8は「検証の自動化→境界の固定→実害のある修正→
  現状文書の是正→内部の再配置」を提案している）
