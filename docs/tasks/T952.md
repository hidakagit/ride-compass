# T952. 規模ウォッチの発火6件に、次の閾値または分割を決める 規模S

状態: 完了（2026-09-19。発火6件を分類し、4件へ個別閾値を設定して書き戻した。書き戻し後に`review_checks.py size`を回して**発火0件**になることを確認済み）

## 背景

第11回の規模ウォッチで**6件が発火した**（`scripts/review_checks.py` 3,952行・
`backend/app/infrastructure/road_graph_repository.py` 3,236行・
`scripts/tests/test_review_checks.py` 1,642行・
`frontend/src/components/AxisStudio/AxisScoringSection.tsx` 682行・
`scripts/literal_scan.py`・`scripts/check_master_ci.py`）が、
**次の閾値を`history/size_watch.json`へ書き戻していない**。

`complexity.md`は「次閾値の提案を結果ファイルへ書いただけで終わらせないこと——書き戻さない限り
次回も同じファイルが発火し続け、安全弁が『鳴りっぱなし』になって機能しなくなる」と定めており、
**現状はその状態**。とくに`road_graph_repository.py`は個別閾値3,100を超えたまま据え置かれている。

## 対応方針

第11回の判断材料（[history/2026-09-19_all.md](../../.claude/commands/review/history/2026-09-19_all.md)「Phase 7: 矛盾と判断」2、および「Keep List」）を踏まえ、
各ファイルを(a)理由つきKEEP・(b)抽出/分割・(c)閾値付きKEEPの個別設定のいずれかへ分類する。
**閾値はユーザー承認のうえ`size_watch.json`の`thresholds`へ書き戻す**。

`review_checks.py`は、分割するなら計測群（`size`/`metrics`/`duplication`/`trigger`、約340行）を
出し検知器群は据え置くのが構造に沿う（検知器群とmutateは同じ理由で一緒に動くため、
分けると`DETECTOR_ENFORCEMENT`と`guard_probe_*`が離れ、前回P0の「表と配線のずれ」を作りやすい）。

## 完了条件

- 発火6件すべてが3分類のいずれかに割り当てられている
- 閾値付きKEEPにしたものは`history/size_watch.json`の`thresholds`へ書き戻されている

## 対応（2026-09-19）

発火6件を3分類へ割り当て、**(c)閾値付きKEEP**とした4件を`history/size_watch.json`の
`thresholds`へ書き戻した（ユーザー承認済み）。

| ファイル | 今回 | 分類 | 閾値 | 根拠 |
|---|---:|---|---:|---|
| `scripts/review_checks.py` | 3,952 | (c) | 4,500 | 検知器群とmutateは同じ理由（母集団・プローブ・強制表）で一緒に動くため分けない。独立して切れるのは計測群（約340行）だけで、行数で切ると検知器群が割れる方向へ倒れる |
| `backend/app/infrastructure/road_graph_repository.py` | 3,236 | (c) | 3,400 | way単位の照会・再計算が積み上がっている点（2026-09-13で既出）の整理が済むまで監視 |
| `scripts/tests/test_review_checks.py` | 1,642 | (c) | 2,000 | 検知器1つにつきテストが増える構造で、検知器群と同じ理由で伸びる |
| `frontend/src/components/AxisStudio/AxisScoringSection.tsx` | 682 | (c) | 900 | 変更理由が3クラスタ同居。分割案（②折れ点の較正・③categorical入力）は[T947](T947.md)の範囲を見てから判断する |
| `scripts/literal_scan.py` | 140 | (a) | — | 「上位に新規登場」は分母が小さいだけで、成長していない |
| `scripts/check_master_ci.py` | 98 | (a) | — | 同上 |

## 検証

書き戻し後に`python scripts/review_checks.py size`を実行し、**発火0件**を確認した
（安全弁が「鳴りっぱなし」の状態を解消した）。

## 残っている判断（本タスクの範囲外）

`AxisComposer.tsx`の閾値1,750は、[T924](T924.md)でファイルが1,424行→327行になった後も
据え置かれている。**327行のファイルに1,750の閾値では、5倍に育つまで安全弁が鳴らない。**
下げるかどうかは次の規模ウォッチで判断する。
