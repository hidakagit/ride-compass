# T675. 未定義CSSトークンを是正しトークン実在チェックを追加する

規模S。

状態: 未着手（2026-09-08起票）

## 背景

統合レビュー第5回（[2026-09-08_all.md](../../.claude/commands/review/history/2026-09-08_all.md)）
が検出した指摘のうち、下表の1件を1つの起票単位として扱う。

`--color-text`/`--color-text-muted`が参照5箇所・定義0件。SVGの`<text>`で`fill`が
継承値＝黒に固定され、**ダークモードで軸スタジオの値ラベルが読めない**。
規約（`var(--color-*)`を使う）に従った書き方に見える形で通るため、実在検証が要る。

## 対象の指摘

| # | Sev | 対象 | 内容 |
|---|---|---|---|
| 7-1 | P2 | `AxisStudio.module.css:397,435` / `RideConditionBar.module.css:44,72,81` | **未定義のCSSトークン**`var(--color-text)`/`var(--color-text-muted)`を参照（**参照5箇所・定義0件**、正しくは`--foreground`/`--color-muted`）。`.curveDragLabel`はSVGの`<text>`で`fill`が継承値＝黒に固 |

各項目のEvidence・failure_scenario・Recommendationはレビュー結果の全件表を参照する。

## 着手時に決めること

- 上記のうちどれを実施し、どれを見送るか（見送る場合は理由を本ファイルへ残す）
- 実施順序（レビューのPhase 8は「検証の自動化→境界の固定→実害のある修正→
  現状文書の是正→内部の再配置」を提案している）
