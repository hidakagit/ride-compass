# T663. 軸スタジオのGUI操作で500になる構成を書き込み時に拒否する

規模S〜M。

状態: 未着手（2026-09-08起票）

## 背景

統合レビュー第5回（[2026-09-08_all.md](../../.claude/commands/review/history/2026-09-08_all.md)）
が検出した指摘のうち、下表の2件を1つの起票単位として扱う。

どちらも**コードデプロイなしのGUI操作だけで本番が500になる**導線。1-8は動的材料と
静的材料の混在（全ルート生成が500）、1-9はサービス実装が未登録の軸
（当該レイヤーの全タイルが500。404でないためフロントの「データなし」フォールバックも効かない）。

## 対象の指摘

| # | Sev | 対象 | 内容 |
|---|---|---|---|
| 1-8 | P2 | `domain/evaluation.py:1251-1255` / `axis_admin.py:201` | 動的材料と静的材料を混在させた軸は`KeyError`で`/api/routes/generate`ごと500になるが、`_check_materials_are_known`はこの組み合わせを拒否しない。bulk経路側は同型事故を明示的に潰しており非対称。GUI操作だけで全ルート生成が落ちる導線 |
| 1-9 | P2 | `domain/dynamic_way_values.py:53-73` / `dependencies.py:325` | docstring「軸スタジオで宣言するだけで自動反映される」に対し、`_DYNAMIC_WAY_VALUE_SERVICE_FACTORIES[material_id]`がKeyError未捕捉。GUI操作で公開軸を作ると当該レイヤーの**全タイルが500**（404でないためフロントの「データなし」フォールバックも |

各項目のEvidence・failure_scenario・Recommendationはレビュー結果の全件表を参照する。

## 着手時に決めること

- 上記のうちどれを実施し、どれを見送るか（見送る場合は理由を本ファイルへ残す）
- 実施順序（レビューのPhase 8は「検証の自動化→境界の固定→実害のある修正→
  現状文書の是正→内部の再配置」を提案している）
