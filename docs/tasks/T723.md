# T723. architecture.mdの「コードの写し」を機械で検知して削る 規模M

状態: 着手（2026-09-11起票）

## 背景（起票時のスタブ。実装中に詳細を書き足す）

[T707](T707.md)が是正したdocs乖離26件は、すべて「コードを見れば分かることを
ドキュメントが写していた」箇所だった（実装に存在しない識別子21件・個数表現12件、一部重複）。
一方で[architecture.md](../architecture.md)のmaplibre-gl制約のように**コードから導けない
事実**は1か月後も正しいままだった。

古くなるのは写しの側で、写しは更新し続ける対象ではなく削る対象。
`review_checks.py`の検知器は`docs/modules/`だけが対象で`architecture.md`を見ていない。
