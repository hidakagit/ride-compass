# T1047. タスク番号の確保を1コマンドにする（手書きの衝突判定をなくす）

状態: 未完了（2026-09-23起票）

## 背景

CLAUDE.md「作業ツリーの安全」節の採番手順（fetch→スタブ＋台帳1行→即push、衝突したら
自分の側を振り直す）が手作業で、手書きの衝突判定を誤る実例が出た。並行セッション・並行
エージェントが1日に何度も採番するため、手順そのものを1コマンドにする。
