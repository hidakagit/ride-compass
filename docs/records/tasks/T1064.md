# T1064. 本番とCIのGEOS・PROJをPGDGの版へ上げ、土地被覆を作り直す

状態: 未完了（2026-09-23起票）

## 背景

ユーザー決定（2026-09-23、判断D-019）: 本番DBのGEOS・PROJを、Ubuntu標準の版（GEOS 3.12.1・PROJ 9.4.0）からPostgreSQL公式リポジトリ（PGDG）の版（GEOS 3.14.1・PROJ 9.8.1）へ上げる。本番の一時停止は許容（D-015と同じ扱い。ユーザー「本番一時的に止まっても問題ない」）。

事前の調査（公式のNEWSとpostgis.net/docsを突き合わせ、2026-09-23）: 変わりうる経路は backend/app の derive_raster_materials.py の土地被覆の帯（ST_Buffer(geography) 100m−10m と ST_Difference）だけで、GEOS 3.12.2〜3.13.1 の buffer 修正（GH-995・1056・978・1200・1233）と交点計算（GH-937）が届く。区間の長さ・距離・方位（geographyの測地計算）・タイルの線と点・ST_ClusterDBSCAN・4326→3857変換には届かない。3.13.0 の「長さ0の線を点として扱う」は、本番の road_edges 2,582,177件に長さ0が0件（run_probe.py で実測）のため届かない。T1061 で CI は本番と同じ版（GEOS 3.12.1・PROJ 9.4.0）を /noble 指定で入れている。

手順（順序を守る）:
1. 変更前の記録（本番、読み取りのみ）: SELECT postgis_full_version()、edge_materials の lc_* 各列について件数・平均・分位点、比較用に固定した区間のサンプル（例: 区間idで1万件）の lc_* を取って残す。
2. CI を新しい版へ: ci.yml の GEOS・PROJ の /noble 指定を外し、PGDG の版が入ることを作業ブランチ（orch/**）の CI の注釈「DB」で確かめる。全件が通ること。docs/architecture/tech-stack.md の「DBの版（本番が正本）」節の版を更新する。master へはまだ入れない。
3. 本番を上げる: apt で libgeos・proj（PostGIS が依存するパッケージ）だけを PGDG の版へ上げる。PostgreSQL を再起動し、SELECT postgis_full_version() で GEOS 3.14.1・PROJ 9.8.1 が読み込まれていることを確かめる。backend の /health と、ルート生成・タイル1枚が通ることを確かめる。
4. 2 の CI 変更を master へ入れる（本番と CI の版が揃った状態にする）。
5. 土地被覆を作り直す: 本番で derive_raster_materials を回し直し、1 のサンプルで lc_* の新旧を比べる（変わった区間の数・差の最大と分布）。見込みを大きく超える差（例: 1区間で数%以上）があれば、その区間の帯の形を ST_HausdorffDistance・ST_Area(ST_SymDifference) で調べて報告する。
6. 開発機（Windows、PostgreSQL 18.6・PostGIS 3.6.2）の版は本タスクの範囲外。差を tech-stack.md に書く。

完了条件: 本番と CI の postgis_full_version の GEOS・PROJ が一致（3.14.1・9.8.1）、本番の /health・ルート生成・タイルが通る、lc_* の新旧比較が記録にある、tech-stack.md が新しい版を正本として書いている。

## 性能の新旧比較（ユーザーの関心の中心、2026-09-23 追記）

ユーザー「どちらかというと、レスポンス改善があったか？が知りたい」。版を上げる前と後で、
**本番で、同じ条件で**次を測り、表にする（CLAUDE.md「性能は利用者から見た単位で測る」。
開発DBでは測らない）。

| 測るもの | 利用者から見た単位 | 測り方 |
|---|---|---|
| ルート生成 | 応答時間 | 出発点・距離を固定した代表の要求（周回と目的地の両モード、短・長の距離）を各5回以上。キャッシュの効き方を揃える（1回目を捨てるか、冷たい状態を揃える）。backendの1行INFOサマリのステージ別所要も添える |
| 地図タイル（道路・事故・POI） | 1枚の生成時間 | 代表のz/x/yを数十枚、タイルのキャッシュを通らない経路で生成し、所要を比べる。バイト列のハッシュも新旧で比べ、同じか違うかを記録する（違えば件数） |
| 土地被覆のバッチ（derive_raster_materials） | バッチ全体の所要 | 上の手順5の作り直しの所要を、変更前に同じ範囲で1回測ったものと比べる |

呼び出しの経路から見た事前の見立て（未計測）: ルート生成の重い部分はメモリ上の探索で、DBの
距離・近傍計算はgeographyの測地計算なのでGEOSを通らない。タイルでGEOSを通るのは、枠から
はみ出す線のクリップ（GEOSClipByRect）だけ。GEOSを最も使うのは土地被覆のバッチ。したがって
利用者の応答への効果は小さく、差が出るならバッチと見ている。**この見立てと実測がずれたら、
結論より先にずれを報告する**（CLAUDE.md「見積もりとずれたら」）。

## ユーザー操作（済 2026-09-23）: 本番VMのGEOS・PROJを上げる（2026-09-23 依頼）

**なぜユーザーか**: 本番VMへの書き込み（パッケージの更新とPostgreSQLの再起動）は、担当の権限の判定で
拒否された。変わるのはGEOS・PROJのパッケージ5件（削除0件）で、止まるのはPostgreSQLの再起動の数秒の
見込み。DBの行は消えない。

**実行する場所**: 本番VMにSSHで入って、次の2行。

```
sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --only-upgrade libgeos-c1t64 libproj25 proj-data
sudo systemctl restart postgresql@18-main
```

**済んだら**: 司令塔へ一言。担当（L-env）が版の確認・変更後の計測・新旧の比較・CIの変更
（e1335ee2、本番を上げる前にmasterへ入れない）のmasterへの反映を行う。

**結果（2026-09-23 18:1x、司令塔が読み取りで確認）**: `postgis_full_version()`は`GEOS="3.14.1-CAPI-1.20.5"`・
`PROJ="9.8.1"`（PostGIS 3.6.4、compiled against GEOS 3.12.1・PROJ 9.4.0）。PostgreSQLは確認の約40秒前に
再起動済み。backendの`/health`は200（commit 5f46ba3d）。

## 実施（2026-09-23、L-env）

### 手順1: 変更前の記録（本番、読み取りのみ、17:15〜17:25 JST）

計測は本番コンテナの中で走らせた（`backend/scripts/run_probe.py --in-container`。ルート生成は
コンテナの中から`http://127.0.0.1:8000`のAPIへ投げたので、開発機のネットワークを含まない）。

- 版: PostgreSQL 18.6（pgdg24.04+2）・PostGIS 3.6.4・**GEOS 3.12.1・PROJ 9.4.0**・GDAL 3.8.4。
  road_edges 2,582,177件、lulcタイル（source_features）あり。
- `edge_materials`の`lc_*`（2,582,177件すべて値あり）: 平均と10/50/90%点
  | 列 | 平均 | p10 / p50 / p90 |
  |---|---|---|
  | lc_built | 84.699 | 29.66 / 100 / 100 |
  | lc_crops | 9.848 | 0 / 0 / 44.56 |
  | lc_trees | 3.923 | 0 / 0 / 4.93 |
  | lc_rangeland | 1.105 | 0 / 0 / 0 |
  | lc_water | 0.416 | 0 / 0 / 0 |
  | lc_bare | 0.0064 | 0 / 0 / 0 |
  | lc_flooded_veg | 0.0028 | 0 / 0 / 0 |
  | lc_snow_ice | 0.00013 | 0 / 0 / 0 |
  | lc_valid_pixels | 809.13 | 568 / 718 / 1115 |
- 固定サンプル: `lc_valid_pixels IS NOT NULL`の区間を`md5(osm_way_id || '/' || segment_index)`の順に
  1万件。保存値（`lc_*`と`lc_valid_pixels`）を控えた。
- **サンプルへの再計算（読み取りのみ）**: バッチと同じ式（`_BUILD_RINGS`の帯をサンプルの区間だけで
  一時テーブルに作り、`class_percentages_sql(_COUNT_CLASSES)`をSELECT）で計算し直すと、保存値を
  再現した——`lc_valid_pixels`は1万件すべて一致、割合の差は最大3.8e-6（保存列がREALのための丸め）。
  **変更後も同じ手順で計算し直せば、書き込まずに新旧の値を比べられる**。
  - 所要（GEOSを通る段）: 帯の作成7.96秒・画素の数え上げ8.50秒（1万区間）。全258万区間なら
    土地被覆だけで約70分の見込み（未計測、比例で見積もった値）。
  - 帯の形の指紋: 面積の合計483,808,651.7m²・頂点の合計835,031。
- タイル（皇居の周り、z13を2×2・z14を5×4・z15を3×2の30枚。リポジトリの生成メソッドを直に呼び、
  タイルのキャッシュを通らない。2周して2周目を比べる）: 中央値で道路66.1ms・POI 116.2ms・事故4.3ms
  （1周目は道路198ms・POI 124.5ms・事故6.0ms。POIの1枚が13.7秒）。各タイルのSHA-256を控えた。
- ルート生成（受付からジョブ完了まで。各6回、1回目はキャッシュを温める分として別、2〜6回目の中央値）:
  | 要求 | 1回目 | 2〜6回目の中央値（最小〜最大） |
  |---|---|---|
  | 周回20km（皇居） | 15.34秒 | 2.88秒（2.80〜2.94） |
  | 周回50km（皇居） | 119.04秒 | 12.65秒（12.58〜12.75） |
  | 目的地・渋谷（10km指定） | 1.74秒 | 1.01秒（0.99〜1.01） |
  | 目的地・二子玉川（25km指定） | 4.51秒 | 2.70秒（2.63〜3.80） |

  ステージ別（backendの1行サマリ、2〜6回目）: 周回20kmはprepare 1.49〜1.62秒・select 0.61秒・
  trace 0.23〜0.26秒・evaluate 0.24〜0.26秒、周回50kmはprepare 5.80〜6.00秒・select 4.46〜4.66秒・
  trace 1.57〜1.68秒・evaluate 0.25〜0.27秒。1回目の遅さはprepare（周回50kmで112秒）。

### 手順2: CIを新しい版へ（作業ブランチ）

`ci.yml`の`libgeos-c1t64/noble libproj25/noble proj-data/noble`を外した。
run https://github.com/hidakagit/ride-compass/actions/runs/35837064102 （21e17a33）は全ジョブ成功、注釈`DB`は
`PostgreSQL 18.6 … / PostGIS 3.6.4 / GEOS 3.14.1-CAPI-1.20.5 / PROJ 9.8.1 / GDAL 3.8.4`。
docs/architecture/tech-stack.md「DBの版」を、本番・CIともPGDGのGEOS・PROJという記述へ直した
（手順6の開発機の版もここへ書いた: PostgreSQL 18.6・PostGIS 3.6.2・GEOS 3.14.1dev・PROJ 8.2.1）。

### 手順3の準備（本番、読み取りのみ）

- VM: Ubuntu 24.04.4・arm64。aptの配布元にPGDG（`noble-pgdg`）が既に入っている。
- 入っている版: `libgeos-c1t64` 3.12.1-3build1、`libproj25`・`proj-data` 9.4.0-1build2
  （どれもUbuntu本体の版）。候補はPGDGの`libgeos-c1t64` 3.14.1-2.pgdg24.04+1・
  `libproj25`/`proj-data` 9.8.1-1.pgdg24.04+1。
- `sudo apt-get -s install --only-upgrade libgeos-c1t64 libproj25 proj-data`（予行、何も変えない）:
  **4件の更新と1件の新規、削除0件**——`libgeos-c1t64`・`proj-data`・`libproj25`・`proj-bin`を
  更新し、`libgeos3.14.1`を新たに入れる。libgeos・libprojに依存して入っているのは
  `postgresql-18-postgis-3`・`libgdal34t64`・`libgeotiff5`・`librttopo1`・`libspatialite8t64`・`proj-bin`。
- 止まる時間の見込み: PostgreSQLの再起動（数秒。未計測）。backendのコンテナは触らない（Pythonの
  shapely・pyprojはwheel同梱のGEOS・PROJで、VMのパッケージと無関係）。再起動の瞬間にDB接続を
  持っていた要求は失敗しうる。**DBの行を消す操作は含まない**。戻すときは
  `apt-get install libgeos-c1t64=3.12.1-3build1 libproj25=9.4.0-1build2 proj-data=9.4.0-1build2
  proj-bin=9.4.0-1build2`（Ubuntu本体のアーカイブにある）と再起動。
