# T1011. 撤去で消えた判断を、全履歴から洗い出して戻す（frontend）

状態: 完了（区画A・B判定済み・要相談37件すべて行き先が決まった。採番衝突のためT1007から振り直した）

（採番の振り直し: 起票時はT1007だったが、origin/masterへ先に別タスクのT1007が入っていたため、本ブランチ側をT1011へ振り直した。）

## 背景

[T1001](T1001.md)の段階Aで、ファイルを丸ごと撤去した際にそこにしか無かった判断
（「1次の分類は中立色、2次の評価は緑〜赤」）を一緒に消した。標準側の穴は
[T1018](T1018.md)で塞いだ。本タスクは**既に消えたものを戻す**側を受け持つ。

## 母集団（再現できる形で）

```
git log -p -M --format="@@@%H|%ad|%s" --date=short -- frontend/src
```
の削除行のうち日本語を含むもの＝**25,764行**。そこから、現在の `frontend/src`・`docs`・
`CLAUDE.md`・`.claude` の本文に**同じ文字列が残っているもの**（コメント記号と空白を除いて
突き合わせ）を除くと**21,460行**が候補。長さによる足切りはしない。

候補は2つの区画に割る（判定の重みが違う）。

| 区画 | 対象 | 候補行 | 状態 |
|---|---|---|---|
| **A** | 今は存在しないファイル 101件（後継が無い） | 5,009 | 完了 |
| **B** | 今も存在するファイル 267件（その場で書き換え） | 15,454 | 完了 |
| 除外 | `types/generated/`（backendのdocstringの写し。判定対象はbackend側の原文） | 975 | — |

## 判定の基準

`docs/conventions/comments.md`「判定基準」の判定木のみを使う（①経緯の言い回しか →
②その制約が他に別の言い回しで存在するか）。判定の根拠が**現在のリポジトリの状態に依存する
もの**（「消えた機構の説明だから破棄」「他に記録があるから破棄」「どこにも無いから昇格」）は
Grepで確認する。文面だけで決まるもの（経緯・実装を読めば分かる）は読みで判定する。

## 区画Aの結果（2026-09-22、8分割で並行判定）

| 区画 | ファイル | 候補行 | 昇格 | 要相談 |
|---|---|---|---|---|
| 1 | 6 | 626 | 3 | 3 |
| 2 | 10 | 626 | 1 | 3 |
| 3 | 11 | 626 | 3 | 3 |
| 4 | 14 | 626 | 3 | 3 |
| 5 | 15 | 627 | 1 | 3 |
| 6 | 15 | 626 | 4 | 2 |
| 7 | 15 | 626 | 3 | 3 |
| 8 | 15 | 626 | 2 | 1 |
| 計 | 101 | 5,009 | 20 | 21 |

破棄の内訳は概ね次の4つで尽きた: **そもそもコメントでないコード行**（テスト名・JSX・
`expect`。母集団を落とさない側に倒した結果として混ざる）、**既に消えた機構の説明**、
**経緯の言い回し**、**他に記録あり**（後継コードかdocsに現在形で存在）。

### 反映したもの（重複を畳んで12件）

| 行き先 | 判断 |
|---|---|
| `static-map-layers.md` | 分類は網羅でなく走りやすさの単位へ束ねる／束ね方から軸スタジオの値ラベルを導出しない／寄与していても順序が運用で変わるものに評価配色を使わない／「対象外」に緑を割り当てない／重大度は大きさと別に絞り込み軸としても持つ |
| 同（新設「MapLibreの式を組むときの前提」） | 式の誤りは例外にならない／配列は`["literal"]`で包む／比較の前に欠損を落とす／`beforeId`は今のスタイルに在るものだけ |
| `frontend-design-system.md` | 出没させず常時マウントして`disabled`（3区画から出た）／flex行のSVGは`flex-shrink:0`／`Disclosure`の本文は閉じると隠れる（2区画から出た） |
| `map-axis-coloring.md` | タイルへ焼くのは材料タグまで／動的値だけの軸は`primary_attribute_ids`が空で正しい |
| `axis-studio.md` | 往復の検査は実在軸のスナップショットを母集団にする |
| コード | `roadLines.ts`（石畳・敷石の中立グループ）・`points.ts`（順序が運用で変わる）・`mapStyleOps.ts`（`beforeId`で例外）・`sceneBuilders.ts`（同じURLで`setTiles`を呼ばない・拡大曲線が要る理由） |

反映後、追記した文言が他と二重になっていないことを確認した（`石畳・敷石`1・`flex-shrink`1・
`常にマウント`1・`beforeId`1・`["literal"`1。`primary_attribute_ids`の2件目はbackend側の
別の記述）。

### 撤去の後始末（同時に直した）

- 撤去済みファイルを名指ししていた**死んだ参照21箇所**（`page.tsx`3・`axisLayers.ts`5・
  `mapLayers.ts`4・`MapView.tsx`2・`legendFilter.ts`・`roadFacts.ts`・`routeStyleModes.ts`・
  `regionApi.ts`・`dedicatedWayValueLayer`2・`mapStyleOps.ts`、docs 5箇所）
- 参照0になっていた旧アイコンサイズ式を撤去し、その理由を現行の拡大曲線へ移した
- **消えていた不変条件テストを復活**（`scene/groups/declarations.test.ts`）。`surface-tags.json`・
  `poi-kinds.json`の全値が宣言に現れること、線/点の幾何が`primary-attributes.json`と一致する
  こと、鍵と色が重複しないこと。`chipseal`を1つ落として落ちることを確認済み

## 区画Bの結果（2026-09-22、10分割で並行判定）

| 区画 | ファイル | 候補行 | 昇格 | 要相談 |
|---|---|---|---|---|
| B-1 | 1（page.tsx） | 2,086 | 0 | 3 |
| B-2 | 1（MapView.tsx） | 2,072 | 1 | 2 |
| B-3 | 30 | 1,412 | 0 | 2 |
| B-4 | 31 | 1,412 | 0 | 1 |
| B-5 | 33 | 1,412 | 0 | 3 |
| B-6 | 34 | 1,412 | 0 | 1 |
| B-7 | 34 | 1,412 | 0 | 4 |
| B-8 | 34 | 1,412 | 0 | 1 |
| B-9 | 34 | 1,412 | 1 | 2 |
| B-10 | 35 | 1,412 | 0 | 1 |
| 計 | 267 | 15,454 | 2 | 20 |

**区画Aと対照的に昇格がほとんど出ない**のは、対象ファイルが存在し続けている＝消えた行の
大半が「同じ箇所のコメントが書き換えられた旧版」だからである。判定木①を通った件
（各区画10〜17件）は、ほぼすべて②で後継を確認できた。

### 反映したもの

| 行き先 | 判断 |
|---|---|
| `static-map-layers.md`「MapLibreの式を組むときの前提」 | `filter`は値が無いならキーごと省く（`undefined`を持たせると「filterには配列が必要」で弾かれる。代役地図はstyle検証を持たないためテストでは捕まらない） |
| `AxisStudio.test.tsx` | happy-domは`window.confirm`を定義しないため自分で差し込む（`vi.spyOn`では対象が無い） |

### あわせて直したもの（判定の副産物）

- **現行の記述が実態とずれていた6件**: `AxisStudio.test.tsx`の環境名（jsdom→happy-dom）2件、
  撤去済み「ウィザード」への参照2件、`static-map-layers.md`の3節が撤去済みの記号名
  （`applyRoadMaterialTrackOffsets`・`buildAxisOverlayLayers`・`setStaticOverlayFilters`・
  `ensureLayerFromSpec`・`makeEnsureAxisRampLayer`）を現行として説明していた箇所
- **死んだ参照4件**: 存在しない`docs/static-road-attributes-plan.md`への参照2件、
  撤去済み`mapOverlayExclusiveDomainFor`、存在しない`app-debug-console`
- **コードの不整合1件**: `page.tsx`の`hasHiddenFilters`が保存済みキーの生の配列長を見ており、
  `static-map-layers.md`が明記する「実在するキーだけを数える」に反していた。段の綴りが
  変わった版の値がlocalStorageに残ると、隠れている段が無いのに一括クリアだけ有効になる。
  いま描いている凡例（道路の線・点と評価軸・災害・レンズ）に実在する鍵だけを見る形へ直した

### 判断済み（2026-09-22）

- **重み0の軸をレンズに出すか**: 現在の「公開軸を無条件で対象にする」が意図どおり
  （2026-09-03の「重み0は選択肢から消す」はレンズ化で方針変更済み）。コード変更なし

### 要相談の行き先（37件）

**件数の訂正**: 作業中に「32件」と報告したが、区画別の内訳を足し直すと**37件**
（区画A 18件＋区画B 19件。区画Bを一度14件と報告したのは数え落とし）。

判断はユーザーが性質ごとに与えた。**規則そのものは
[`docs/conventions/comments.md`「残すと決めたものの行き先」](../../conventions/comments.md)
が持つ**——本ファイルは記録であって標準ではなく、維持されないため、ここに規則を置くと
次に同じ判断をする人へ届かない。以下は、その規則をこの母集団へ当てた結果である。

| 性質 | 行き先 | 件数 |
|---|---|---|
| (a) 罠の性質が機構より長生きするもの | コードのコメント（安全側に） | 5 |
| (b) 根拠・実測値の所在 | 設計書へ設計根拠として | 5 |
| (c) 現行の記述から落ちた含み | 残す | 7 |
| (d) 規約へ足すかの判断 | 設計書へ移動 | 10 |
| (e) 実装の判断が要るもの | 別途起票 | 10 |

### (a) コメントへ残した（5件）

| 行き先 | 残した罠 |
|---|---|
| `Map/axisLayers.ts` | 欠損は`null`のままにせず同じ型の番兵へ倒す（出力の型が混ざる`case`/`match`を作らない） |
| `AxisStudio/AxisComposer.tsx` | 同じ`<button>`の`type`を押された直後に書き換えるとフォームが暗黙に送信される（happy-domでは再現しない） |
| `scene/groups/weather.ts` | 中身が届く前のプレースホルダURLは実際に要求されうる |
| `hooks/useWeatherGrid.ts` | 解像度の違う格子を重ねるなら、落とす判定は中心1点ではなく4隅すべて。半透明を2枚重ねると`1-(1-X)^2`で濃くなる |
| `frontend-design-system.md`「5-5」 | Reactの`onWheel`は`passive: true`で委譲されるため`preventDefault`が効かない（**ホストになるコードが現在1行も無いため、例外的に設計書側へ**） |

### (b) 設計書へ設計根拠として書いた（5件）

| 行き先 | 書いた根拠 |
|---|---|
| `static-map-layers.md`「面の濃さ」 | 下限はΔE（CIE76）15以上。`docs/records/tasks/T912.md`にしか無かった |
| `route-settings-and-results.md`「生成を待つ時間・投げる回数の根拠」 | 打ち切りは本番（都心30km・未split 約316秒）と開発機（約361秒）の**両方**を上回ること。コード側には開発機の値しか残っていなかった |
| 同 | 差分プレビューを明示操作にするのは、生成APIに1分10回の上限があるため |
| `deployment-sync.md`「backendを先に出した窓」 | 材料を増やした直後の窓では、その材料を使う軸の編集画面が別物として開く |
| `axis-studio.md` | 点数の形（`shapeKind`）は列挙されたものだけを持つ。`docs/records/decisions/`にしか無かった |

**規約の穴も1つ塞がった**: 判定木②「他に存在するか」に`docs/records/`を数えてよいかが
書かれていなかった。**数えない**と決め、`comments.md`の判定木②と`CLAUDE.md`のコメント方針
へ書いた。上の(b)5件はこの判断で動かしている。

### (c) 含みを残した（7件）

| 行き先 | 残した含み |
|---|---|
| `hooks/useLocation.ts` | 固定時間のデバウンスは代わりにならない（許可ダイアログの応答はどんな長さも超えうる） |
| `hooks/useDebouncedValue.ts` | 初回の値は遅れない |
| `AxisStudio/TuningPanel.tsx` | 行ごとに説明を敷くと一覧として読めなくなる（弊害の形） |
| `hooks/useWeatherConditions.ts` | 安全側でないことを承知で選んでいる（根拠は`api-design.md`にあるため、事実と所在だけ） |
| `RouteSplicePanel.tsx` | 位置の取り違えは、地図の帯と差し替わる区間のずれとしてしか現れない |
| `AxisStudio.test.tsx` | Radix Checkboxが`ResizeObserver`を呼ぶのは`<form>`直下のときだけ |
| （破棄1件） | チップ幅が文字数に連動する機構は`.iconChip`の`width: max-content`に現在形であり、`mapLayers.ts`のポインタも中身を持つ。二重にしない |

### (d) 設計書へ移した（10件）

| 行き先 | 移した一般則 |
|---|---|
| `frontend-design-system.md`「5-3. 状態の伝え方」 | 色だけで伝える状態は`aria-label`/`title`にも載せる／同じ行に並ぶ操作は文言を変える／UI文言に開発用語を出さない |
| 同「5-4. `composes`が安全な理由」 | `composes`は取り込み側に新しいローカルクラスを発行するため、足した状態セレクタが元クラスへ波及しない |
| `testing.md`「パターン9」 | 2言語で同じ計算を持つなら期待値を書き写さない／素のテキストノードは正規表現で引く／XYZ座標は入れ替えたら落ちる形で見る |
| `route-settings-and-results.md`「AxisContributionBar」 | 軸の並び順に連続的な意味は無い（値どうしをつながない） |
| `static-map-layers.md`「面で出せるもの・出せないもの」 | 面になるのは道と無関係に値が決まるものだけ。勾配は道の属性で、格子へ均すと潰れる |
| （破棄1件） | 「スタイルJSONとタイルの配信元を揃える」は`static-map-layers.md`「タイルの配信元」の暗黙の前提に現在形で存在 |

### (e) 起票した（10件→2タスク）

| タスク | 中身 |
|---|---|
| [T1012](T1012.md) | 点レイヤー間の色の使い回し／レンズ軸を1カテゴリまで絞れること／全キーがフィルタ述語を持つ検査の再設置／同じ判定を2か所へ出すときの入口一本化 |
| [T1013](T1013.md) | `evaluationAxes`のキー照合ガード／上書きOFF時の送信契約／`mapStyleOps`の呼ぶタイミング／線を先に積めない理由／`Disclosure`のクリック方法 |

残り1件（`static-road-attributes.md`の「突き合わせテストが検証する」の主語）は、
検査を`declarations.test.ts`へ復活させた時点で真に戻っているため、主語だけ直した。

## 完了条件

- 区画A・Bの候補が、すべて「昇格済み」か「破棄（理由付き）」に分類され、**未判定が0**
- 昇格した文が二重になっていないことを、追記のたびに確認している
- 要相談の一覧が空（判断されたものは昇格・破棄・別タスクのいずれかへ移っている）
