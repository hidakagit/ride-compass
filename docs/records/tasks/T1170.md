# T1170. どこまで源泉に従わせて導出するか（同じ事実か・機械的に引けるか・目的が違うか）の判断基準を設計原則に定める

状態: 未完了（2026-09-26起票）

## 背景

2026-09-26、T1140（画面が持つ値をbackendの宣言へ移す）で、地図のレイヤーの説明文を材料カタログの説明から引くか（ユーザーの提案「各材料の源泉の1属性にしたらだめ？」）が論点になった。担当の調べでは、道路タイルのレイヤーidは一次属性idと一致し材料カタログのtile_propertyで属性→材料が引けるが、材料の説明文（何を測るか）と今のⓘの文（地図で何が見えるか・どの評価に使われるか）は中身が違う。設計原則（design-principles.md）は原則1（frontendは描画の都合だけ）・4（正準は1か所）・8（この表の各行はどこかから導けないか）・12（母集団は導出）で「導けるなら導く」までを持つが、同じ語でも言っていることが違う場合の線引きが無い。ユーザーの指示「どこまで従属的な導出にするか、プロジェクトとして判断基準を作りたい」「起票して進めて」。司令塔のたたき台: 導出する条件は①同じ事実（源泉を書き換えたらこちらも必ず変わるべきか）かつ②機械的に引ける（手の対応表を要さない）。独立に宣言する条件は③目的が違う（ただし置き場はbackendの宣言、原則1）④対応表が要るなら表を置かず要素の宣言に属性を持たせる（原則8の具体化）。1つの文の中で同じ事実の部分だけを導出して差し込んでよい。完了条件: design-principles.mdに基準を足し、既存の原則1・4・8・12との関係を整理（重複させない）、今回の例（レイヤーの説明）への当てはめを1つ書く。文面はユーザーの確認を経てからmasterへ入れる。

## 範囲（2026-09-26 20:00〜20:06のユーザーの指示）

- 段階A: 判断基準を設計原則へ定める。文面はユーザーの確認を経てからmasterへ入れる。
- 段階B: 全体への横展開の母集団を数える（直さない）。件数と性質を見て、司令塔が次の段階・別タスクへ分ける。
- 20:06の追加指示「説明文に限らず対象にして」: 基準も母集団も、文言だけでなく値・しきい値・対応表・一覧・名前・型・
  設定・生成物のすべてを対象にする。当てはめの例は文言以外からも挙げる。
- T1140（地図のレイヤーの説明）は答え(1)（評価の名前を評価の一覧から差し込む）で進めており、この結果を待たない。

## 段階A: 判断基準（design-principles.md 構造仕様17）

新しい番号にした。原則8へ足すと「表を置くか」の文脈に閉じ、値・名前・設定へ当てるときに読む人が8を探しに行かない。
原則8には「導ける」の判定は17が持つ、という1句だけを足した（重複させない）。17は「同じ事実か」「機械的に引けるか」の
2問、既定は同じ事実（目的が違うと言うには、源泉を変えたときにこちらを変えたくない理由を1文で言う）、1つの値の中で
混ざるときは部分ごとに、既存の原則1・4・8・12との関係、当てはめの例3つ（文言: レイヤーの説明／値: 雨と雪を分ける
気温／対応表: 道の詳細に「あり」と出す項目）を持つ。

## 段階B: 横展開の母集団（2026-09-26、origin/master ee1200fc時点。状態: 数えただけで、直していない）

### 母集団と数え方

- **F**: `frontend/src`の`.ts`・`.tsx`のうち、テスト（`*.test.*`・`*.spec.*`）・生成物（`types/generated/`）・
  テスト用の固定データ（`testing/`・`__fixtures__/`・`__mocks__/`）・`.d.ts`を除いたもの（195ファイル）。TypeScriptの
  コンパイラAPI（`frontend/node_modules/typescript`）で構文木を歩く。
- **B**: `backend/app`の`.py`（Pythonの`ast`）。
- **G**: `frontend/src/types/generated/`の生成物（backendの宣言から書き出されたもの）。`.ts`は先頭の`{`/`[`から末尾までを
  JSONとして読む。`openapi.json`は`components.schemas`の`enum`の値だけを語彙に数え、`api.d.ts`は読まない。
- **名前の語**: 定数名をスネーク・キャメルで語に割り、単位の語（c・mm・km・px・percent・deg・meters等）と
  default・threshold・value(s)を外した集合。2語以上が共通なら「名前で結合」する。
- **識別子らしい文字列**: `^[a-z][a-z0-9_\-]{2,}$`。**表示名**: 日本語を含む20文字以下の文字列。
- 数える道具は使い捨て（リポジトリに置かない）。上の定義と下の表の「何を数えたか」で同じ結果を出し直せる。

| 種類 | 何を数えたか（機械） | 候補 | 一次判定（人が1件ずつ） | 検算した既知の1件 |
|---|---|---|---|---|
| 値・しきい値（名前の付いた定数） | Fのモジュール直下の`const X = <数値>`（60件）× Bのモジュール直下の大文字の数値定数（178件）とGの数値の葉（161件）を名前の語で結合 | frontendの定数12件 | 同じ事実の写し4（雨と雪の境の気温・Webメルカトルの緯度の上限・緯度1度の距離・地球半径）／源泉の性質を画面だけが持つ1（風の詳細格子を出す最小ズーム。構造仕様1）／要判断1（ルートの矢印の画素の大きさとbackendのズーム別の倍率）／無関係6 | 雨と雪の境（frontend 2・backend 0で値は違うが語で結合できた）、緯度1度の距離 |
| 値・しきい値（式の中のリテラル） | F・Bの式の中の数値リテラル（モジュール直下の代入の外）× 上の名前の付いた宣言を値の一致（×1・×1000・×1/1000）で結合。有効数字3桁以上のものだけ | backend 13か所・frontend 19か所 | 同じ事実の写し1（信号の近傍の度換算に書いた111,000）／要判断6（frontendのラスタのタイルの画素数256。外部タイルの仕様で、源泉の性質を画面が持つ形）／無関係25（バイト換算1024・色の255・微小値1e-6） | 111,000（T1115の「経度1度あたりの距離」） |
| 対応表・一覧（配っている語彙） | Fの式の中の文字列がGの識別子と一致。同じ最上位の宣言に2種以上並ぶものを「表・一覧」、1種だけを「分岐」 | 表・一覧17宣言／分岐81宣言（一致したリテラルは計247個） | 表・一覧: 写し候補6（道の詳細の「あり」の項目・レイヤーidの一覧・地図の出典の種類→タイルの対応・天気の分類・塗り分けの種類の判定・軸スタジオの採点の選択肢）／要判断4／描画の都合（重なり順・MapLibreの語）3／生成された型の値での分岐4。分岐81は未判定（多くは`none`・`primary`等のCSS・部品の語との偶然の一致） | 道の詳細の「あり」の項目（`has_tunnel`等） |
| 対応表・一覧（backendが持つが配っていない語彙） | Fの文字列がGに無く、Bのソースに文字列として現れるもの。うち`_`か`-`を含み、`backend/app/domain`に現れるものに絞る | 絞る前132宣言・223語／絞った後9宣言・21語 | 絞った後の9宣言: 写し候補7（経由地ルートのid・形の種類`breakpoint_linear`を書く4宣言・塗り分けの種類の判定・方位の問い合わせの項目名）／目的が違う1（軸スタジオで編集欄を持つpayloadの項目。生成された型に対して網羅を型で検査済み）／偽陽性1（生成された応答の型への`Pick`）。絞る前の132は未判定 | 経由地ルートのid（`route-waypoints`）。生成物との結合では出ず、この結合で初めて出た |
| 名前・文言 | ①Fの日本語の文字列の中の「内部名＋軸／材料／レイヤー／属性」②Fの文字列がGの表示名と完全一致 ③含む（部分一致） | ①1件 ②6宣言・8語 ③22宣言 | ①は内部名の露出（night軸）。②は写し候補2（レイヤーの説明の中の「道路種別」「降水」「風」、区間の風）＋偶然の一致4（「成功」「その他」）。③は「道路」「注意」等の短い語の偶然の一致が大半で未判定 | 地図のレイヤーの説明の「night軸」 |
| 型 | Fの型の位置（リテラル型）の文字列がGの識別子と一致 | 22宣言 | 手書きの合併型が生成された列挙と同じ値を並べる写し候補（天気の分類・生成ジョブの状態等）。7宣言は`Omit<…, "kind">`等のキー名の一致（偽陽性）。残りは未判定 | 天気の分類（`AmedasWeatherCategory`） |
| backend内の二重宣言 | Bのモジュール直下の定数どうし（別ファイル）を名前の語で結合／文字列の並びの定数どうしで2要素以上が共通 | 名前22組・並び1組 | 要判断3組（タイルの画素数256を3ファイルが持つ）／要判断1組（8方位と16方位の表示名。16方位側に、気象庁の番号の割当が特有だという理由が書いてある）／目的が違う1組（前置フィルタの1度あたり80,000m。変えたくない理由がコメントに書いてある）／無関係17組 | 前置フィルタの80,000m（目的が違う側が、理由付きで正しく独立していることの検算） |
| 設定 | `backend/app/config.py`の`Settings`の項目（38件）の環境変数名が、追跡中の`*.yml`・`docker-compose*`・`*.env.example`・`*.bat`・`*.sh`・`*.ps1`で値を持つか | 7項目 | コードの既定値と同じ値を別ファイルにも書いている4項目（基礎地図の公開URL・CORSの許可元・DBのURL・デバッグの切り替え。`.env.example`とcompose）／環境ごとに違う値（目的が違う）3項目 | なし（既知の例が無い） |
| 生成物 | 生成物の置き場の外にある、追跡中のデータファイル（`frontend/src`・`backend/app`・`frontend/public`の`.json`・`.yaml`、テストを除く） | 1件 | 0件（`source_profile.yaml`は取込の源泉そのもので、写しではない） | なし |
| 逆向き（目的が違うのに従わせている） | Gを直接importするFのファイルが、その生成物のキーを`.key`か`["key"]`で読む。同じ（生成物, キー）を2つ以上の領域（`features/<名>`・`app`・`lib`・`components`・`services`等の1段目）が読む | 17組 | 目的が違うと1文で言えたもの0（色の宣言・生成の設定・タイルの設定・注意報の語彙を、同じ目的で別の画面が読んでいる） | なし（今のコードに既知の例が無い。材料の説明をレイヤーの説明へ流用する案はT1140で採らなかった） |

### 数え方の穴（0件・少数に見えても、見ていないもの）

- **軸の名前は数えられない**: 軸の正本は本番DBで、リポジトリに写しが無い。画面の文言に軸の名前を書いた箇所
  （例: ルートのレイヤーの説明の「風・勾配・路面・総合難易度」）は表示名の結合から漏れる。拾うなら実行時の
  `GET /api/axis-catalog`の応答を語彙に足す。
- **有効数字2桁以下の式の中のリテラル**（0.1・0.3・2等）は、値で結合すると偶然の一致ばかりになるため外した。外れた割合は
  backend 1,309個中1,229個（94%）・frontend 1,355個中1,312個（97%）。しきい値の多くはここにあり、frontendの側は
  T1140が母集団（`features`・`lib`・`app`の非テストの大文字の定数と式の中の比較）を人が1件ずつ見る形で持っている。
  backendの側は見ていない。
- **名前の結合は2語の共通が条件**で、語が1つしか重ならない同じ事実（別名で書いた同じしきい値）は漏れる。
- **backendの逆向き**（backendの宣言を別の目的のbackendのコードが読む）は数えていない。
- **逆向きはGを直接importするファイルだけ**を見た。`lib/`の包みを経由して読む箇所は包みの側の領域に数えられる。
- **設定はfrontendの環境変数（`NEXT_PUBLIC_*`）と、配備の設定どうし（composeとワークフロー）の重複を見ていない**。

### 件数から見えること（次の段階の分け方の材料）

- 写しの確度が高い候補（一次判定で「同じ事実の写し」「写し候補」としたもの）: 値5（定数4・式の中1）、対応表・一覧13宣言
  （配っている語彙6・配っていない語彙7）、名前・文言3、型は数宣言、設定4項目。多くはfrontendにあり、T1140の範囲
  （画面が持つ値をbackendの宣言へ）と重なる。重ならないのは、backend内の二重宣言（タイルの画素数256・方位の表示名）、
  式の中の111,000（T1115の範囲）、設定の既定値の写し、配っていない語彙のうちT1140に挙がっていないもの（形の種類・
  方位の問い合わせの項目名）。
- 「目的が違うのに従わせている」向きは、今の数え方では0件だった。基準の既定（同じ事実）どおり、写しの側が多い。
- 未判定の塊は3つ: 分岐81宣言・配っていない語彙の絞る前132宣言・表示名の部分一致22宣言。CSS・部品・MapLibreの語や
  短い日本語との偶然の一致が大半に見えるが、判定はしていない。

所要（並行実行）段階A・B: 依頼 2026-09-26 20:04 → 完了 20:17（13分）、うち枠待ち0分（枠を使っていない）・CI待ち0分、実績 実装+0/−0・テスト+0/−0
