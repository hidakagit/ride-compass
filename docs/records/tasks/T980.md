# T980. 一次属性の語彙が材料カタログと別に宣言されている 規模M

状態: 完了

未着手（2026-09-20起票）。

## 背景

評価軸（DB）が参照するのは**材料だけ**である。一次属性idは軸が宣言せず、
`domain/axis_display.py: primary_attribute_ids_for()`が軸の材料を`MATERIAL_CATALOG`で
引いて`MaterialSpec.primary_attribute_id`を集めることで導出される。

にもかかわらず、一次属性の語彙そのものは`domain/registry_defaults.py:
_register_primary_attributes()`に18個のリテラルとして別に並んでいる。

実測（2026-09-20）:

| | 件数 |
|---|---|
| `registry_defaults`が宣言 | 18 |
| `MATERIAL_CATALOG`の`primary_attribute_id`から導ける | 15 |
| どの材料も指さない | 3（`bicycle_access`・`geometry`・`supply_poi`） |
| 宣言に無いのに材料が使う | 0 |

さらにフロントは同じ18個を**手書きで2つの集合へ分けて持つ**
（`primaryAttributes.ts: PRIMARY_ATTRIBUTE_LAYER_IDS` 10件 ＋
`PRIMARY_ATTRIBUTES_WITHOUT_LAYER` 8件）。ドリフト検知テストはあるが、語彙は3箇所に散る。

## 何が問題か

**材料を1つ足すときに、その材料が指す一次属性が語彙に無ければ登録が失敗する**
（`register_axis`が`inputs`を語彙で検証する）。材料の宣言だけで完結せず、別ファイルへ
1行足す手順が要る。手順を知らないと、材料を足した時点では通り、軸が公開された瞬間に落ちる。

3つの孤児（どの材料も指さない一次属性）は、その手順が片方向にしか守られていないことの
表れである——語彙からは消されない。

## あるべき形

一次属性は材料の性質なので、**`material_catalog.py`が唯一の正本**にする。

- ラベルは`MaterialSpec.primary_attribute_id`の隣で宣言する（`registry_defaults`の
  リテラル列は消える）
- 語彙は`MATERIAL_CATALOG`から導出する（`register_primary_attribute`の検証は、
  導出した集合に対して行う）
- 孤児3件は、対応する材料が無いことを確認して消すか、材料側へ移す。
  **`supply_poi`はフロントの地図レイヤー（補給・休憩POI）が使っているため、
  消す前に読み手を確認すること**

## 完了条件

- `_register_primary_attributes()`が無くなり、語彙が`MATERIAL_CATALOG`から導出される
- 材料を1つ足したとき、その材料の宣言だけで一次属性が揃う（別ファイルへの追記が要らない）
- フロントの手書き2集合が、生成物との突き合わせで完全性を検査されている
  （現状のドリフト検知テストが何を保証しているかを先に読むこと）

## 段階A（2026-09-24、担当 W-vocab）: 着手時点で既に済んでいた

### 方針

振り出しの指示は「一次属性のラベルを`MaterialSpec.primary_attribute_id`の隣で宣言し、語彙を
`MATERIAL_CATALOG`から導き、`_register_primary_attributes()`を消す。孤児3件は読み手を数えてから
処置する。frontendには触らない」だった。着手前に今のコードを数えたところ、前提が変わっていた。

- 起票の翌日のコミット ddc18297b「一次属性の語彙を材料カタログから導く（T980）」で、backend側は
  既に直っていた（この記録だけが「未着手」のまま残っていた）。`_register_primary_attributes()`は
  消え、語彙は`material_catalog.py`へ移り、`register_defaults()`が材料の指す一次属性の欠落を
  登録時に落とす。孤児のうち`bicycle_access`・`geometry`はこのとき削除された。
- その後 T1001 段階A（c2e73ce0）で、語彙はラベルだけの表から`PRIMARY_ATTRIBUTES`
  （`PrimaryAttributeSpec`の並び。名前・幾何・地図の束ね方・タイルの系統を持つ）になり、
  frontendの手書き2集合（`PRIMARY_ATTRIBUTE_LAYER_IDS`・`PRIMARY_ATTRIBUTES_WITHOUT_LAYER`）は
  消えて、地図レイヤーは生成物`mapDisplay.layerIds`から導かれるようになった。
- 「ラベルを`MaterialSpec`の隣で宣言する」形は、今の形より悪い。一次属性は複数の材料に共有され
  （`landcover`は8材料、`cycleway`は5材料が指す）、材料を持たない属性（`supply_poi`）もあるため、
  材料側に書くと同じ宣言を材料の数だけ写すことになる。一次属性の側に1回だけ書き、材料はidで指す
  今の形を正とする。

したがってこの段階ではコードを変えず、設計書の抜け（語彙の置き場と、欠落がいつ落ちるかが
`docs/modules/backend/axis-studio.md`に書かれていなかった）だけを埋めた。

### 実測（2026-09-24、origin/master b06c44a1）

母集団: `material_catalog.PRIMARY_ATTRIBUTES`の`attr_id`（宣言）と、`MATERIAL_CATALOG`の全材料の
`primary_attribute_id`のうちNoneでないもの（材料が指す）。scratchpadのPythonで両方を読んで比べた。

| | 件数 |
|---|---|
| 宣言（重複なし） | 15 |
| 材料が指す | 14 |
| どの材料も指さない | 1（`supply_poi`） |
| 宣言に無いのに材料が指す | 0 |

効いていることの確認: `PRIMARY_ATTRIBUTES`から`tunnel`を抜いて`register_defaults()`を呼ぶと
`ValueError: 材料が指す一次属性がPRIMARY_ATTRIBUTESにありません: ['tunnel']`で落ちた。実宣言では15件が登録された。

孤児3件の読み手（`git grep`でリポジトリ全体。`docs/records/`を除く。backend・frontend/src・
frontend/e2e・frontend/e2e-live・生成物を含む）:

- `bicycle_access`: 0件（語「bicycle_access」・ラベル「自転車通行可否」とも）。削除済み
- `geometry`: 0件（`attr_id`としての出現・ラベル「区間形状」とも。語「geometry」単独は
  `PrimaryAttributeSpec.geometry`フィールド等と区別できないためこの2形で数えた）。削除済み
- `supply_poi`: 本番コード1（`frontend/src/features/map/layers/mapLayers.ts`が名前と分類の一覧を読む）・
  生成物2（`mapDisplay.ts`・`primaryAttributes.ts`）・テスト4（backend `test_primary_attribute_display.py`、
  frontend `mapLayers.test.ts`・`MapOverlayControls.test.tsx`・`MapView.state.contract.test.ts`）。
  `test_tag_classification.py`の一致は語「supply_point」の部分一致で読み手ではない。地図の分類として
  使われているため消せない。材料を持たない＝評価に効かない一次属性として、宣言に残すのが今の形

### 完了条件に対する状態

1. `_register_primary_attributes()`は無く、語彙の正本は`material_catalog.py`にある。「導出」ではなく
   同じファイルの宣言と、材料が指す集合の包含の検査である（上の理由で導出にはしない）
2. 既存の一次属性を指す材料は材料の宣言だけで揃う。新しい一次属性は、名前・幾何・地図の束ね方を
   持つため宣言が要るが、同じファイルで、忘れればビルドとテストで落ちる
3. frontendの手書き2集合は既に無く、検査する対象が無い

T980を閉じてよいかをダッシュボードに問いとして置いた（`T980-close`）。答えが出るまで`状態: 未完了`のまま。

### 問いと答え（`T980-close`、2026-09-24）

- 問い: T980（一次属性の語彙を材料カタログへ一本化）は、既に済んでいたので閉じてよいか。選択肢は
  (1) 閉じる（コードは変えない）、(2) 閉じる前に、材料が一次属性を文字列のidではなく宣言そのもので指す形へ
  変える（実装 ±40行ほど。得られるのは打ち間違いが型で起きなくなり、登録時の検査1本が要らなくなること。
  一次属性は複数の材料に共有され、材料を持たない`supply_poi`もあるので宣言の一覧は残る）、(3) 閉じずに残す。
  推奨は(1)。
- 答え: **(2)**（2026-09-24）。段階Bとして行い、T980を閉じる。

## 段階B（2026-09-24、担当 W-vocab）: 材料が一次属性を宣言そのもので指す

### 方針

- `MaterialSpec.primary_attribute_id: str | None`を`primary_attribute: PrimaryAttributeSpec | None`へ変える。
  外へ出る形（`GET /api/axis-catalog`の`primary_attribute_ids`の文字列の配列、生成物）は変えない——
  `axis_definitions.primary_attribute_ids_for()`が`.attr_id`を取り出して返す。
- 材料が宣言を指す手段は、`PRIMARY_ATTRIBUTES`の表の中で`:=`により要素へ名前を付けること
  （`_ATTR_LANDCOVER := PrimaryAttributeSpec(...)`）。表の外にモジュール定数として宣言して表へ並べ直す形は
  採らなかった——その形では「宣言したが表に並べ忘れた一次属性」を材料が指せてしまい、消したい検査が
  そのまま要る。名前が表の要素にしか付かなければ、材料が表に無い一次属性を指す形そのものが書けない。
  表を並べ替えずに済むため差分も1要素1行で済む。
- 名前を付けるのは材料が指す一次属性だけ（材料を持たない`supply_poi`には付けない）。
- 形を変えたことで要らなくなった`register_defaults()`の検査（「材料が指す一次属性がPRIMARY_ATTRIBUTESに
  ありません」）と、そのテスト2本（未宣言の一次属性を指す材料を拒む・Noneを欠落として扱わない）を消した。
  二重登録を拒むテストと実宣言で通るテストは残る。

### 参照元の列挙

`git grep -n "primary_attribute_id" -- . ':!docs/records'`（着手時、origin/master 7ce7bfde）: 行単位で
backend/app 31行（`material_catalog.py` 28・`axis_definitions.py` 2・`registry_defaults.py` 1。ほかに
`primary_attribute_ids_for`の語の部分一致が`axis_catalog.py`に4行）、backend/tests 7行
（`test_axis_definitions.py` 5・`test_registry_defaults.py` 1・`test_axis_catalog_routes.py`のコメント1。
同ファイルの残りは`primary_attribute_ids`の部分一致）、docs/modules 2行。scripts・frontend・生成物の一致は
すべて`primary_attribute_ids`（APIの項目名）の部分一致で、書き換えの対象ではない。
`getattr`等の文字列での属性参照は0件（`git grep -n "\"primary_attribute_id\""`）。
書き換え後に残る`primary_attribute_id`は、`test_axis_definitions.py`の架空の材料を作る補助関数の引数だけ
（idを受けて宣言を組み立てる）。`test_axis_catalog_routes.py`のコメントは旧フィールド名を書いていたので直した。

### 検証

- 書き換え前後の対応: origin/masterの`material_catalog.py`と書き換え後をそれぞれ読み込み、全材料34件の
  「材料→一次属性id」を比べて一致（一次属性を指す材料30件・一次属性14種）。指す宣言はすべて
  `PRIMARY_ATTRIBUTES`の要素そのもの（同一性で比較、表の外の宣言0件）。
- 効いていることの確認: 材料`tunnel`の`primary_attribute=_ATTR_TUNNEL`を`_ATTR_TUNEL`（打ち間違い）へ変えると、
  `ruff check`が`F821 Undefined name`、importが`NameError: name '_ATTR_TUNEL' is not defined`で止まった。
  表にはあるが材料を持たない`supply_poi`を指そうと`_ATTR_SUPPLY_POI`と書いても同じ2か所で止まった
  （名前が付いていないため。材料に使うときは表の中で名前を付ける）。以前は`register_defaults()`の呼び出し
  （ビルド時とテスト）まで気づけなかった。
- `ruff check app scripts benchmarks tests`: 違反なし。`mypy`: 195ファイルで違反なし。
- `scripts/export_openapi.py`: 生成物・`openapi.json`とも差分なし。
- `pytest tests/test_axis_definitions.py tests/test_registry_defaults.py tests/test_axis_catalog_routes.py
  tests/test_material_catalog.py tests/test_axis_display.py tests/test_primary_attribute_display.py
  tests/test_dynamic_way_values.py -q`: 208 passed。
- `scripts/review_checks.py docs`: 違反なし。

### 見立てとのずれ

実装は見立て ±40行に対して +49/−57。材料26行と宣言14行の1対1の置き換え（計±40）に、検査の削除（−7）と
フィールドの説明・表の説明の書き換えが乗った。削除した説明文のうち、材料`highway_is_cycleway`の
「highway自体は…排他チェック対象を維持する」は、排他の検査が材料単位（`check_material_exclusivity`）で
一次属性を見ないため今は成り立たず、書き換えの際に落とした。

### 完了条件に対する状態

段階Aの「完了条件に対する状態」のとおり（語彙の正本は`material_catalog.py`、材料を足すときは材料の宣言だけで
済み、frontendの手書き2集合は既に無い）。加えて、材料が宣言に無い一次属性を指すことは型と名前で書けなくなった。
残りは無い。

## 経緯

T970段階Eで`axis-catalog.json`（DB状態の写し）を撤去する際、そこに同居していた
`primary_attributes`の由来を調べて判明した。段階Eでは語彙の構造に触れず、フロントが
読んでいたラベルの取り出し先を移すだけに留めた。

所要（並行実行）: 段階A 依頼 2026-09-24 11:12 → 区切り 11:19（7分）、うち枠待ち0分・CI待ち0分、実績 +0/−0（実装・テストの変更なし、文書のみ）
所要（並行実行）: 段階B 依頼 2026-09-24 17:40 → 完了 17:48（8分）、うち枠待ち0分・CI待ち0分、実績 +61/−87 → S
