# T1188. 経路探索の自前部品（優先度キュー・最寄りの交差点の索引）を標準の道具へ置き換えられるか実測し、置き換えるか残す理由を書く

状態: 未完了（2026-09-27起票）

## 背景

起票案 proposal-std-routing-heap-nearest（2026-09-27承認。標準の道具の棚卸し・T1152の担当から）。規模S（根拠: ①実測の段階を先に切り、その段階はS。②置き換える場合も、変わるのは routing.py・test_routing.py・requirements-dev.txt の3本）

■ 背景（画面に出ない、経路探索の内部の話。置き換えても、経路の結果が変わるのはコストが同点のときだけの見込み）
- 優先度キュー: domain/routing.py は、numba でコンパイルする探索のために、配列のバイナリヒープと「満杯なら倍に伸ばす」処理を自前で持つ（約90行と伸長のテスト）。理由として docs/modules/backend/routing-engine.md に「heapq は使えない」とあるが、これは誤り。入っている numba 0.67 は numba/cpython/heapq.py に heappush／heappop を実装している（条件は、要素の型がそろった list であること）。
- 最寄りの交差点の索引: 同じファイルの格子索引（約160行。探す範囲を輪状に広げ、打ち切りも自前）。作った当時の理由（総当たりの線形探索が遅い）は記録にしか無い。今は距離の一括計算を numpy で書けるので、総当たり＋argmin で同じ約束を満たせる可能性がある。自前の打ち切りには「条件に合う点が1つも無いと止まらず16.9分」という過去の不具合があった。
- 開発用の依存の scipy は「ベンチマーク bench_t790_turn_expanded.py が比べるため」と書かれているが、そのファイルは存在せず、scipy を import するコードも無い。

■ やること
1. 実測: benchmarks/bench_route_generation.py で、ヒープを heapq（型付き list のタプル）にした場合と、最寄りの交差点を numpy の総当たりにした場合の所要を今と比べる（本番DBの規模で。1リクエストあたりの呼び出し回数は数回〜約20回）。
2. 速度が落ちないものは置き換え、落ちるものは残す理由（実測値）を routing-engine.md へ書く。どちらにしても「heapq は使えない」の誤りは直す。
3. scipy は根拠が無ければ依存から外す。

■ なぜ既存のタスクに入らないか
経路探索の部品を扱う未完了のタスクは無い。
