# T1190. 開発・運用スクリプトの小さな自前処理（カバレッジの直読み・デプロイ対象のパス判定・画面の撮影・クラウドの状態確認・道具の書き出し・本番調査の受け渡し）を標準の道具へ置き換える

状態: 未完了（2026-09-27起票）

## 背景

起票案 proposal-std-scripts-small（2026-09-27承認。標準の道具の棚卸し・T1152の担当から）。規模M（根拠: 変わると見込むファイルは audit_test_rewrite.py・deploy_backend_gate.py・test_deploy_backend_gate.py・smoke-check.mjs・knip.json・check-oracle-cloud.sh・launch.py・run_probe.py の8本。7を超える）

■ 背景（画面に出ない、開発・運用の道具の話）
どれも小さく、置き換え先の標準の道具があり、自前にした理由が書かれていないもの:
- backend/scripts/audit_test_rewrite.py: カバレッジの記録（.coverage）を SQLite として直接読み、内部の表を結合している（分岐の記録が別の表へ入る差をコメントで説明しているのは、内部の形に依存している印）。coverage.py の公開の読み取り口（CoverageData の contexts_by_lineno）へ置き換える。約−20行。
- scripts/deploy_backend_gate.py: GitHub の paths と同じ規則の glob を正規表現へ直す処理を自前で持つ（約18行とテスト2本）。git の pathspec（git diff --name-only 起点 対象 -- … ':(exclude,glob)…'）に答えさせる。差分の起点を本番で動いているコミットに取る判定（dorny/paths-filter を使えない理由）は残す。
- frontend/scripts/smoke-check.mjs（17行）: Playwright 標準の CLI の1行（playwright screenshot --viewport-size=390,844 URL 出力先）と同じ。スクリプトを消し、README と knip.json を直す。
- scripts/check-oracle-cloud.sh: クラウドの CLI が返す JSON を数え・整形するのに node -e を6か所で使う。CLI 標準の --query（JMESPath）と --raw-output で書ける。ただし 2026-08-20 以降変更が無く、生きた文書から参照されていないので、まず使われているかを確かめ、使われていなければ置き換えずに消す。
- scripts/orchestration/launch.py の export_tools: 道具のファイルを1本ずつ git show で書き出す（約15回プロセスを起こす）。git archive の1回にする。
- backend/scripts/run_probe.py: .env を自前で読む部分は python-dotenv（既に入っている）の dotenv_values に、コンテナへの受け渡し（base64 にして docker cp）は標準入力へ流す形（docker exec -i … python -）にできる。

■ なぜ既存のタスクに入らないか
T1148 は launch.py の道具の一覧（TOOL_PATHS）を扱うが、書き出しの方法は範囲外。ほかは該当する未完了のタスクは無い。
