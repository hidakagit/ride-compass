# T968. デッドコードと不要なテストを総整理し、残すものをあるべき場所へ置く 規模L

状態: 完了（2026-09-24、ユーザー決定で範囲を縮小して閉じる。末尾「縮小して閉じる」節）

2026-09-20起票、2026-09-21にユーザー指示で枠を広げた

## このタスクが何をするか

3つを1つの作業として行う。**分けない**——同じコードとテストを3回読むことになるため。

1. **消す**: 本番の入力から到達しない実装と、それを生かしているだけのテスト。
2. **移す**: 残すと決めたテストを、あるべき場所へ置き直す。必要なテストでも、層に合わない
   場所・合わない粒度にあると、実装を直すたびに関係ない場所が落ちる。
3. **点検する**: 層ごとの責務（DB=データ／app=ふるまい／テスト=今のふるまい）が守られて
   いるか。**現行追認ではない**——守られていない箇所は、制約を正しい層へ移して分岐を消す。

起票時（2026-09-20）は3の点検だけの枠だった。2026-09-21のユーザー指示で1と2を含め、
[T963](T963.md)の「本番の入口からの到達可能性グラフ」「デッドコードの機械的な検知」
「同じ値を2か所で持っている検知」を段階1〜2として取り込んでいる。T963には`metrics`の
増減傾向を見せる話だけが残る。

## 守るべき線

- **DB**: データの制約（NOT NULL・FK・UNIQUE・CHECK）。派生表は親への参照を持つ。
- **app**: ふるまいの制約。DBが保証している前提を再確認しない。
- **テスト**: 「今」のふるまいの網羅。appにテスト専用の口を作らせない。

崩れると、DBが緩いぶんをappが吸収し、appの吸収をテストが固定し、直すと全部が同時に
落ちるため誰も直せなくなる。

きっかけは[T956](T956.md)の2つの実測。`road_edges.osm_way_id`にFKもNOT NULLも無く、
app側の材料の値式10本が`CASE WHEN w.osm_way_id IS NULL THEN NULL ELSE ...`で毎回吸収して
いた（本番の違反行は0件、兄弟の派生表5本は同じFKを既に持っていた）。同じ点検で、
`app/domain/material_sql.py`にテストからしか呼ばれない関数を1本足していたことも分かった
（`folds_tag_absence_to_false`、同タスク内で撤去）。

## 進め方（2026-09-21ユーザー指示）

**機械的に洗い出せる範囲は機械で洗い出す。そこで出ないものは、泥臭く読むしかない。**
両方やる。機械の側だけで終えると、動的参照やSQL文字列の向こう側に残ったものがそのまま
生き残る。読む側だけでやると、backend 165＋frontend 181ファイルを人の集中力に賭ける
ことになる。

境界をはっきりさせる。

- **機械で出る**: 本番の入口から辿れない定義（段階1）、同名フィールドを多く共有する
  dataclass（段階2）、FK/NOT NULLの無い派生列とそれを吸収する分岐（段階3）、テストから
  しか参照されない口・テスト同士の直接依存・そのテストだけが通す行が0行のテスト（段階4）、
  テストと実装の対応が取れないファイル・環境が過剰なテスト（段階5）。
- **機械では出ない**: 生きている経路の中にある無駄（通っているが結果を変えない分岐・
  二重の正規化・同じことを2度やる段）、写経された骨格、テストが検査している「つもり」の
  ずれ。これは段階8でモジュール単位に読む。

**読む側は必ず記録を残す**——どのモジュールをいつ読み、何を見つけ、何を見つけなかったかを
書く。書かないと、途中で止まったときに次に読む人が最初から読み直すことになる。

## 段階

**段階ごとにコミットする。** 段階1の出方を確かめてから段階6（frontend）へ進む——
到達可能性の作り方はbackendとfrontendで別物になるため、先に出方の当たりを付ける。

### 段階1: backendの到達可能性グラフを作る

**「怪しそうな書き方」をgrepして母集団を作らない。** 症状や少数の事例の特徴から母集団を
決めると、そこに無いものは定義上見つからない。本番の入力の源流から呼び出しをたどり、
たどり着かないものを孤立点として出す。

源流（エントリポイント）はこのリポジトリでは有限に列挙できる。

- FastAPIのルーター（`app/api/routers/`のエンドポイント）
- `main.py`のlifespanが起動時に走らせるもの
- バッチの`main()`（`app/batch/*.py`）
- `scripts/`・`benchmarks/`の実行可能スクリプト
- スケジュール実行（cron・ワークフロー）

**テストは源流に含めない。** 「誰からも呼ばれていない」ではなく「本番の入力から到達しない」
を出すのが目的で、テストを源流に入れるとテストが延命している実装が見えなくなる
（T956で`EdgeMaterialTable`が実際にそうなった）。

完全性は**インスタンスを全部見たこと**ではなく、次の2つに依る。どちらも短い表なので、
人が読んで「これが抜けている」と言える。

- (a) エントリポイントの列挙が閉じていること
- (b) 呼び出し辺の作り方が動的参照を取りこぼさないこと

(b)で取りこぼしやすいのは、このリポジトリが多用する**宣言テーブル経由の呼び出し**。
`MaterialSpec.extractor`のように関数をテーブルへ載せる形は静的解析から辺が見えない。
**宣言テーブル自体を列挙して辺として与える**（`MATERIAL_CATALOG`・`AXIS_DEFINITIONS`・
`MATERIAL_COVERAGE_SPECS`・`HARD_FILTER_HIGHWAY_TYPES`・`POI_COUNT_KINDS`等）。SQL文字列へ
埋め込まれた参照も辺として扱う。

**偽陽性は列挙の時点で除く**——FastAPIのルートハンドラ（デコレータ登録でコードからは
参照されない）とSQLAlchemyの宣言モデル（メタクラス経由で`Base.metadata`へ入る）。除かないと
出力の大半がこの2つで埋まる。

既知の実測（2026-09-20、T956の削除作業で判明）:

- テキストのgrepではdocstring・コメント内の言及を「使用」と数える
  （`compute_cost_from_axis_scores`が生きていると判定された）。ASTで名前の参照
  （`ast.Name`/`ast.Attribute`/`ast.alias`）だけを数えると同じ母集団から8件多く出た。
- ASTでも「app内から参照されるが本番の入口から到達しない」は出ない
  （`compute_edge_costs_bulk`）。**到達可能性の閉包が要る**のがこの段階の核心。
- app配下の公開定義685件に対し、テキストgrepで12件、ASTで23件（偽陽性を除くと15件）。

**検証**: T956で本番から外した実装を、作った仕組みが孤立点として挙げられるか。挙げられ
なければ辺の作り方（とくに宣言テーブル経由）が甘い。

### 段階2: 出た孤立点を1件ずつ処分する

出力を眺めて終わりにしない。1件ずつ**消す**か、**残す理由を観測できる形で書く**。

残してよいのは次を全部満たすときだけ（T963で決めた残す側の条件）。

1. いつ通るのかを言える
2. 通った回数が観測できる
3. 測って0件だったものは、その場で消す（別タスクへ送らない）

**同じ値を2か所で持っている形も併せて出す**。`EdgeMaterialArrays`と
`StaticEdgeScoreMatrix`が10個以上の同名フィールドを共有し、どちらもタイル単位で
キャッシュへ載るため同じ値をディスクに2組持っていた（T956）。「2つのdataclassが一定数
以上の同名フィールドを持つ」は`ast`で数えられる。

### 段階3: DBが持つべき制約をappが肩代わりしていないか

母集団の導出:

1. 派生表の親子関係を列挙する。`app/infrastructure/*_models.py`の`ForeignKey`宣言と、
   本番の`pg_constraint`（`contype='f'`）を突き合わせ、**モデル上は親を持つが
   DBに制約が無い／そもそもどちらにも無い**列を出す。
2. 1で出た列について、app側が「無いかもしれない」前提のコードを持っていないかを探す
   （`IS NULL THEN NULL`を含むSQL式、`or {}`、`is None`分岐、`COALESCE`の既定値）。
3. 制約を張れるかを本番データで測る（違反行数）。違反が0でなくても、**取込・正規化側で
   制約を守らせてから**張る——「本番データがこうだから張れない」で終えない。

本番DBへの適用まで終えて完了とする（[docs/conventions/deployment-sync.md]
(../../conventions/deployment-sync.md)に従い、開発DBのみの反映で完了扱いにしない）。

### 段階4: テストを消す（backend）

3つを別々に扱う。

1. **appにあるテスト専用の口**。母集団: `app/**`のモジュール直下の公開定義（先頭が`_`で
   ないもの）のうち、定義元以外の`app/`・`scripts/`・`benchmarks/`・`migrations/`から
   参照されず、`tests/`からのみ参照されるもの。2026-09-20時点で12件（再計測15件）。
   `reset_*`・`*_cache_size`のようにテストが状態を初期化・観測するための口は正当。
   `OsmRawPoiRow`・`decode_terrain_rgb_meters`・`osm_ways_to_way_specs`・
   `read_persisted_axis_revision`は実装に取り残された可能性がある。
2. **テストファイル同士の直接依存**。母集団:
   `grep -l "from tests\.test_\|import tests\.test_" backend/tests/test_*.py`。
   2026-09-20時点で2件（`test_wind_time_varying.py`→`test_road_graph_engine`、
   `test_gsi_tile_routes.py`→`test_terrain_rgb`）。共有ヘルパー（`tests/`直下の
   `test_`で始まらないモジュール）経由は正当で対象外。片方の都合でもう片方が壊れる
   （T956で`make_generator`のシグネチャを変えたとき実際に壊しかけた）。共有ヘルパーへ出す。
3. **何も教えないテスト**。母集団の作り方を「読んで怪しいもの」にしない。
   `pytest --cov --cov-context=test`でテストごとの実行行を取り、**そのテストだけが
   通す行が0行のテスト**を出す（他のテストが既に覆っている）。そこから人が見て、
   (a) 同じ性質の重複、(b) 実装と同じ式を再計算して比べているだけ（壊れても何も
   教えない）、(c) 消えた実装に付いていた残骸、へ分類する。**カバレッジが重なること
   自体は悪ではない**ので、0行だったものを機械的に消さない。

### 段階5: 残すテストをあるべき場所へ移す（backend）

消さないと決めたテストも、置き場所が合っていないと実装を直すたびに関係ない場所が落ちる。
**移すだけで中身は変えない**（中身を変えるなら段階4で消す判断をしている）。

判断の軸は「何を固定しているか」。

- **層**: DBの制約が保証することをappのテストが再確認していないか（段階3で制約を張ったら、
  その分岐を固定していたテストは消えるか、DB側の検査へ移る）。純関数のふるまいを
  サービス経由で検査していないか（domainのテストへ）。
- **重さ**: DBを使わずに書けるものが`postgis`マーカー付きになっていないか。母集団は機械で
  出せる——`pytest -m postgis`のテストのうち、`road_graph_session`等のDB fixtureを
  1つも使っていないものを列挙する。逆（DBを使うのにマーカーが無い）も同じ方法で出す。
- **対応**: テストファイルと実装モジュールの対応が取れているか。母集団: `tests/test_*.py`
  のうち、`app/**`に同名のモジュールが無いものを列挙し、1件ずつ「複数モジュールに
  またがる正当なもの」か「置き場所を見失ったもの」かを判断する。

### 段階6: frontendの到達可能性

**T1001段階Eへ移した（2026-09-23）**。段階6・7の中身は[T1001](T1001.md)「段階Eの方針」E-1にあり、
そちらで行う。本タスクに残るのはbackendの段階（1〜5・8）。

backendの出方を見てから作る。源流はbackendと別物になる。

- App Routerのページ・レイアウト・APIルート（`src/app/**`のファイル規約で決まる）
- クライアントの入口（`page.tsx`が読むコンポーネント）
- 生成物（`src/types/generated/`）を読む側

TypeScriptは既存ツール（`knip`・`ts-prune`）が使えるかを先に測る——**入れるかどうかは
出力の質を見てから決める**。このリポジトリはbackend生成物の型・MapLibreのレイヤー宣言・
軸カタログ由来の動的なレイヤー生成を多用するため、素朴な未使用検知は誤検知が出やすい。

### 段階7: frontendのテストを消す・移す

**T1001段階Eへ移した（2026-09-23）**（段階6と同じ）。

段階4・5と同じことを`vitest --coverage`で行う。テスト149ファイル・1,432件が対象。
frontend固有の「あるべき場所」の軸が1つある——**DOMを使わないテストがjsdom環境のまま
になっていないか**（[docs/conventions/testing.md](../../conventions/testing.md)の
`// @vitest-environment node`）。母集団は機械で出せる（docblockが無く、
`render`/`renderHook`/`document`/`window`を1つも使っていないファイル）。

### 段階8: 機械で出ない範囲を読む

段階1〜7が出せるのは「参照が無い」「到達しない」「重なっている」「場所が合わない」まで
で、**生きている経路の中の無駄**は出ない。ここはモジュール単位に読む。

- **単位**: `docs/modules/`の区切りをそのまま使う（backend・frontendの各モジュール文書が
  対象ファイル表を持っており、読む範囲が既に閉じている）。1コミット1モジュール。
- **読むときに探すもの**: 通っているが結果を変えない分岐、同じ正規化を2度かける経路、
  写経された骨格（T956の`EdgeMaterialArrays`/`StaticEdgeScoreMatrix`が実例）、実装と同じ
  式を再計算しているだけのテスト、記載と実装がずれたモジュール文書。
- **記録**: このエントリへモジュールごとに1行——読んだ日・見つけたもの・**見つからな
  かったこと**。「何も無かった」も結果として書く（書かないと読んだかどうかが分からない）。
- **見つけたものの扱い**: その場で消せるものは消す。設計判断が要るものは別Txxxへ起票し、
  このエントリには番号だけ残す。


## 段階1の結果（2026-09-21）

`backend/scripts/dead_code_survey.py`。ASTで定義と参照を集め、源流から辿れない定義を出す。

### 源流（`--show-entrypoints`で出る、167件）

| 種類 | 件数 |
|---|---|
| 実行可能スクリプト（`scripts/`・`benchmarks/`） | 107 |
| APIのエンドポイント（デコレータ登録） | 44 |
| バッチの入口（`main`/`run`） | 14 |
| アプリの起動（`app.main`） | 2 |

加えて、**参照が無くても実行されるもの**（取込アダプタ・ORM宣言モデル・Pydanticモデル）と、
源流のモジュール直下が参照する名前も源流に含める。

### 作りながら見つけた自分の穴（3つとも出力を大きく動かした）

1. **偽陽性として「報告から除いた」ものを、源流にも辿り元にもしていなかった**。取込
   アダプタは`register_adapter`で登録されコードからは参照されないが、バッチの入口から
   実行される。除くだけでは、アダプタが使う`latitude_from_raw`等が丸ごと孤立点に見える。
   → 205件から190件へ。
2. **非公開の定義をノードにしていなかった**。`_PATH_POLICIES`のような非公開テーブルが
   公開定数（`PERMANENT`等）を参照しており、ノードが無いと辺が切れる。→ 190件から27件へ。
3. **docstringを辺の材料にしていた**。SQL文字列に埋まる名前を拾うため文字列定数から
   識別子を集めていたが、docstringが名前を説明のために並べる——`evaluation.py`は
   「`compute_edge_costs_bulk`はテストだけ」と自分で書いており、**その一文が当の関数を
   生かしていた**。[T963](T963.md)が「テキストgrepはdocstringを拾う」と警告した穴は、
   ASTを使っても文字列を辿れば同じように開く。`_strip_docstrings`で落とした。

### 検証（T956直前のツリー `3e312fbb6~1` で実行）

| 名前 | 判定 | 答え合わせ |
|---|---|---|
| `compute_edge_costs_bulk` | 到達しない | ✓ T963が「1か月のあいだテストのオラクルとして残っていた」と書いたもの。docstringを落とす前は「到達する」と誤判定していた |
| `compute_cost_from_axis_scores` | 到達しない | ✓ T956で撤去 |
| `EdgeMaterialTable` | 到達する | ✓ **正しい**。当時は`TILE_MATERIALS_CACHE_VERSION = shape_digest(EdgeMaterialTable, ...)`と実コードから参照されていた |

「なぜ生きていると判定したか」を答えられないと使えないため、経路を源流まで遡って出す
`--why`を持たせた（上のEdgeMaterialTableの答え合わせはこれで行った）。

### 動的参照をどこまで洗えるか（2026-09-21の実測）

わざと動的参照だけで使う定義を作り、生きている関数（`get_accident_years`）の中から呼んで
測った。

| 形 | 判定 | 意味 |
|---|---|---|
| `getattr(traffic, 'probe_literal_ref')()` | 到達する | **名前が文字列リテラルなら辿れる**。レジストリのキー・SQLの識別子も同じ |
| `getattr(traffic, 'probe_' + kind + '_name')()` | 到達しない | **名前を組み立てる形は辿れない**。孤立点として出てしまい、消すと壊れる |

危険なのは後者なので、`--show-dynamic`で機械的に列挙できるようにした（`getattr`/`setattr`/
`hasattr`の名前が非リテラル、`globals()`/`locals()`/`vars()`の添字アクセス）。

**2026-09-21時点で4件**——`domain.axis_definitions:372`・`infrastructure.
road_graph_repository:927,1043`・`services.region_service:88`。**孤立点27件のモジュールとは
1つも重ならない**ため、段階2の処分でこの穴に当たる心配は無い。件数が増えたら、孤立点との
重なりを毎回見る。

なお**宣言テーブルへ関数を載せる形は特別扱いが要らなかった**。
`value_sql=landcover_value_sql("trees")`のように実コードの呼び出しとして書かれるため、
テーブル自体が到達すれば普通に辺が張られる（起票時は「宣言テーブルを列挙して辺として
与える」必要があると書いていたが、実装してみると不要だった）。

### 孤立点（2026-09-21時点、27件／app配下の公開定義 約1,497件）

段階2で1件ずつ処分する。ぱっと見で2種類に分かれる——テストが状態を初期化・観測する口
（正当な可能性が高い）と、実装の取り残し候補。**この分類はまだ判断ではない**。

| モジュール | 定義 |
|---|---|
| `batch.source_profile` | `FROM_TARGET` |
| `domain.accident` | `KANTO_PREFECTURE_CODES` |
| `domain.attributes` | `EdgeKeyedMetrics`・`METRIC_GROUP_COUNTS`・`METRIC_GROUP_LANDCOVER`・`METRIC_GROUP_POI`・`METRIC_KEY_ACCIDENT`・`METRIC_KEY_INTERSECTION` |
| `domain.material_sql` | `LANDCOVER_SQL_KEYS` |
| `domain.registry` | `all_axes` |
| `domain.route` | `BIN_DROPPED_DICT_FIELDS` |
| `domain.traffic` | `POI_ON_EDGE_TOLERANCE_M` |
| `domain.tuning` | `FixedValueKind`・`FIXED_VALUES` |
| `domain.warning_levels` | `WARNING_BADGE_LEVELS` |
| `infrastructure.cache_generation` | `read_persisted_revision` |
| `infrastructure.debug_log` | `reset_stats` |
| `infrastructure.graph_material_cache` | `read_persisted_revision` |
| `infrastructure.landcover_raster` | `reset_sources_for_testing` |
| `infrastructure.msm_client` | `warn_if_stale`・`refresh` |
| `infrastructure.redis_client` | `reset_circuit_breaker` |
| `infrastructure.search_graph_cache` | `lazy_graph_cache_size`・`search_statics_cache_size`・`routable_index_cache_size` |
| `infrastructure.tile_persistent_cache` | `use_directory` |
| `services.derived_data_revision_service` | `reset_for_tests` |

### 置き場所（未決）

一度きりの調査として`backend/scripts/`へ置いた。**常設の検知器にするかは動かしてから
決める**（2026-09-21ユーザー判断）。候補は`review_checks.py`の検知器、あるいはテストとして
常設する形。[T966](T966.md)（29本の検知器が合わせて何を覆っているか）の結論とも突き合わせる。


## 段階2の結果（2026-09-21、第1回）

孤立点27件を「テストからも参照されないか」で分けた（`dead_code_survey.py`が出す）。
**処分の仕方が変わる**——テストからも参照されないものはその場で消せる。テストからのみ
参照されるものは、正当な口か取り残しかの判断が要る（段階4）。

### 消した7件（本番からもテストからも参照されない）

| 定義 | モジュール | 消した理由 |
|---|---|---|
| `KANTO_PREFECTURE_CODES` | `domain.accident` | 事故の絞り込みはbboxで行う形に移っており、都道府県コード表を誰も読まない |
| `EdgeKeyedMetrics` | `domain.attributes` | 型エイリアス。使う側が無い（`Mapping`のimportも道連れで未使用になった） |
| `POI_ON_EDGE_TOLERANCE_M` | `domain.traffic` | 定数 |
| `search_statics_cache_size` | `infrastructure.search_graph_cache` | 「# テストの検証用」と書いてあるのに、そのテストが無い |
| `FROM_TARGET` | `batch.source_profile` | `"from_target"`という値を表す定数。**YAMLには`bbox: from_target`と書いてあるが、読む側が無い**（アダプタは無条件に`target.bbox`を使う）。死んだ宣言のコード側 |
| `FixedValueKind` | `domain.tuning` | 下の`FIXED_VALUES`の型。道連れ |
| `FIXED_VALUES` | `domain.tuning` | 検知器`undeclared_fixed_values`の対象範囲を宣言する表だったが、**その検知器が既に無い**。持っていた情報は[T988](T988.md)へ引き継いだ |

正味 **−105行**（10ファイル、27挿入132削除のうち調査スクリプトの機能追加17行を除く）。
`pytest -q -m "not postgis"` → 1362 passed。`ruff check` クリア。孤立点は27件→20件へ。

### 残した論点

- **`source_profile.yaml`の`bbox: from_target`は残っている**。コード側の定数は消したが、
  YAMLの行は「読む側の無い宣言」のまま。消すか、読むようにするかは取込の設計判断なので
  段階8で扱う。
- 残る20件はすべてテストから参照される。段階4で「テストが状態を初期化・観測するための
  正当な口」か「実装の取り残し」かを判断する。


## 段階8の記録（読んだモジュール）

読む順序はユーザー指示（2026-09-21）——**周期レビューで閾値超過を繰り返しているモジュール
と、段階2で7件が出たモジュールから**。両方に当たるのがrouting-engine系だった
（`road_graph_engine.py` 2,626/2,700＝97%、`routing.py` 1,347/1,400＝96%、
`search_graph_cache.py`から`search_statics_cache_size`が出ている）。

| 日付 | モジュール | 読んだ範囲 | 見つけたもの | 見つからなかったこと |
|---|---|---|---|---|
| 2026-09-21 | [ルート生成エンジン・経路探索](../../modules/backend/routing-engine.md) | `route_generator.py`の4つの生成経路（`generate_loops`・`generate_via_waypoints`・`generate_spliced_route`・`_generate_destination_routes`） | **「道路データが未整備」の失敗処理が4箇所に写経されていた**（同じ文言・同じ構造）。`_explain_missing_context`へ集約し、ログのラベル形式も`generate(loops)`等へ揃えた。行数は+6行（ヘルパーのぶん）だが、文言を直す場所が4→1になった | 4つの生成経路そのものは統合できない——周回はフロンティア方式で折返し点を選び、経由地は順にA*で結び、目的地はvia-node方式で候補を選び、継ぎ合わせは既存のedge_id列から組む。**探索の仕方が本当に違う**ので、共通なのは準備と後片付けだけだった |
| 2026-09-21 | 同上 | `routing.py`の`_turn_expanded_dijkstra`(123行)・`_turn_expanded_astar`(143行)、`search_graph_cache.py`の全関数一覧、`road_graph_engine.py`の関数一覧（中身は未読） | **ヒープの骨格の写経**（両関数で77行が完全一致、dijkstraの62%）→[T992](T992.md)へ起票。性能に直結するため実測つきで直す | `search_graph_cache.py`は2行前後の薄い関数が並ぶが、`set_turn_structure`/`set_search_statics`/`set_routable_index`はそれぞれ別のキャッシュを指しており、まとめる先が無い（同型だが同じ物ではない）。ここは無駄ではないと判断した |

**やって戻したこと**: 最初は準備の手順ごと（半径の算出・時間の測り始め・`prepare`呼び出し・
失敗処理）を1つのヘルパーへ出そうとしたが、**+23行になり、必要なコメントを巻き込んで
壊した**ので戻した。共通化の範囲を「失敗したときの文言と警告」だけに絞ると+6行に収まり、
壊れる箇所も無くなった。**共通化は範囲を欲張ると、減らすはずが増える。**

**まだ読んでいない**（このモジュールの続き）: `road_graph_engine.py`の`select_via_nodes`
(249行)・`select_loop_turnarounds`(202行)・`_build_search_graph`(133行)・
`_build_segment_details`(111行)、`route_generator.py`、`graph_service.py`、
`road_graph_repository.py`。次はここから。


## 責務の逸脱を測った（2026-09-21、ユーザー指摘で基準を切り替えた）

**行数削減を基準にしていたのを、責務の逸脱へ切り替えた。** このタスクの起源は
「DB設計をやり直した結果システムの責務が明確でなく、複雑度がapp側に溜まっていた」ことで、
判断の基準は減った行数ではなく「設計書・テストから逸脱した責務を持っていないか」である
（ユーザー指摘）。

設計書（[docs/architecture/directory-layout.md](../../architecture/directory-layout.md)）が
定めるのは、依存が`api → services → domain` / `api → services → infrastructure`の一方向で、
`domain`はどの層にも依存せず外部I/Oを持たないこと。

| 測ったこと | 結果 |
|---|---|
| 層の依存が設計と逆向き | **0件** |
| `domain`が外部I/O（httpx・asyncpg・sqlalchemy・redis等）を読んでいる | **0件** |
| SQL式での「親が無いかもしれない」吸収（`IS NULL THEN NULL`） | **0件**（T956で解消済み） |
| 本番DBのFK | **13本**（`edge_materials → road_edges`・`road_edges → node_materials`等、T970で整備済み） |
| postgisマーカー付きだがDB fixtureを使わないテスト | **0件** |
| **DB fixtureを使うのにpostgisマーカーが無いテスト** | **11件** |
| **appの非公開（`_`始まり）をimportしているテスト** | **15件** |

上2つの「0件」は、わざと`domain/geo.py`へ`import httpx`と`from app.services import ...`を
足して**2件とも検出されることを確認してから**の0件（検知0件を「直っている」の証拠にしない）。

### 分かったこと

**app→DBの責務の移動は、T956とT970で既に済んでいる。** 段階3（DBが持つべき制約をappが
肩代わりしていないか）の母集団はほぼ空で、これが段階8の削減率が小さかった理由でもある
（`route_generator.py`で−3.4%）——**減らないのは、既に片付いているから**。

**残っている逸脱はテスト層にある。** 「DBを使うのにマーカーが無い」11件は
`pytest -m "not postgis"`で除外されず、「実装の非公開に触る」15件は**今のふるまいではなく
実装の形を固定している**。どちらも段階4・5の対象で、T968の「守るべき線」のうち
テストの行だけが守られていない。

なお上の11件・15件は文字列とimportからの粗い母集団で、正当なもの（共有ヘルパの
`app.batch._common`、非公開SQL定数の契約テスト等）を含む。段階4・5で1件ずつ判断する。

### frontendも測った（2026-09-21）

設計書が名指しで禁じているのは「**backendが持つ値の一覧・既定値をfrontendが手書きで
複製しない**」こと。あわせて「`services/`はbackend APIを叩く薄い層」。

| 測ったこと | 結果 |
|---|---|
| `services/`を通さず直接`fetch`する`components`/`hooks` | 2件（`jmaTileProtocol.ts`はMapLibreのプロトコルハンドラ内でタイル1枚ごとに叩くもので正当。`jmaNowcastFrames.ts`はグレー——時刻一覧の取得だが、JMAのタイムスタンプ形式とトリミング方針という地図固有の知識と一体） |
| `services/`の規模 | 948行（薄い） |
| backend `domain`の宣言が持つ語彙159語のうち、frontendに文字列リテラルとして現れるもの | 73語。**大半はOSMのタグ値**（`asphalt`・`crossing`・`amenity`等）で、正本は外部 |
| そのうち**値の一覧を複製している**もの | `roadFilterAxes.ts: SURFACE_GROUPS`（路面種別の全タグ） |

**唯一の複製（`SURFACE_GROUPS`）は逸脱ではなかった。** frontendが持っているのは表示の
グルーピング（色・日本語ラベル）で、タグ値の集合は生成物`types/generated/surface-tags.json`
と一致することを`roadFilterAxes.test.ts`が検査している——`import surfaceTags from
"@/types/generated/surface-tags.json"`し、
`expect(new Set(allGroupValues)).toEqual(new Set([...surfaceTags.good, ...surfaceTags.bad]))`。
設計書が複製を禁じる理由（「片側だけ変えても全テストが緑のまま通る」）はここでは成立しない。

### このタスクの結論

**責務の逸脱は、backendにもfrontendにも実質的に無い。** 起票時の前提（DB設計をやり直した
結果、責務が明確でなく複雑度がapp側に溜まっている）は、[T956](T956.md)と[T970](T970.md)の
作業で既に解消されていた。段階3・4・5は母集団がほぼ空である。

段階8（モジュールを読む）の削減率が`route_generator.py`で−3.4%に留まったのも同じ理由で、
**減らないのは既に片付いているから**だった。この段階を全モジュールへ広げても、得られるのは
全体の1〜2%の削減で、労力に見合わない。

**残っている価値は機械の段階にある**——段階1〜2（到達可能性、7件削除・20件が判断待ち）と、
まだ測っていない段階6〜7（frontendの到達可能性・テスト整理）。

## 他のタスクとの関係

- **[T966](T966.md)（29本の検知器が合わせて何を覆っているか）との順序**: 到達可能性の
  仕組みを`review_checks.py`の検知器として足すなら、T966の結論を見てから決める。
  **ただしこのタスクは検知器を足すことが目的ではない**——一度きりの調査スクリプトで
  母集団を作り、処分し切ることが目的。常設するかは段階2が終わってから判断する。
- **[T965](T965.md)（「一本化した」と書いた改修の棚卸し）**: 段階1の出力をあちらが
  1件ずつ判断する形だったが、このタスクが段階2で処分まで行う。重複するぶんはT965側を
  狭める（段階2の完了時に見直す）。
- **[T963](T963.md)**: 増減の傾向を見せる話だけが残る。
- **[T952](T952.md)のファイル単位の閾値とは別物**。あちらは「1ファイルが太った」。
- **[T826](T826.md)・[T967](T967.md)（経緯コメントの一掃）**: 段階8で同じファイルを読む。
  読むついでに直せるものは直し、量が多いモジュールはあちらへ残す。

## 完了条件

- 段階1: エントリポイントの表と、宣言テーブル由来の辺の表が書かれている。T956で外した
  実装を孤立点として挙げられることを確認している。
- 段階2: 孤立点が1件ずつ「消した」か「残す理由＋いつ通るか＋観測方法」のどちらかに
  なっている。測って0件だったものが残っていない。
- 段階3: FK/NOT NULLの無い派生列が1件ずつ「制約を張った」か「張れない理由を実測つきで
  書いた」かになっており、app側の肩代わりの分岐が0件。本番DBへ適用済み。
- 段階4・7: テスト専用の口が「正当」「取り残し（削除済み）」に分類され、テスト間の直接
  依存が0件、「そのテストだけが通す行が0行」のテストが1件ずつ判断されている。
- 段階5・7: 層・重さ・対応の3軸それぞれの母集団が列挙され、移したものと「移さない理由」
  が1件ずつ書かれている。
- 段階6: T1001段階Eへ移した（2026-09-23）。段階4・5・7の条件のうちfrontendの分も同じくT1001側で満たす。
- 段階8: `docs/modules/`の全モジュールについて、読んだ記録が1行ずつある（「何も無かった」
  を含む）。読んでいないモジュールが残っていない。
- 削除・移動の前後で`pytest -q -m "not postgis"`・`pytest -m postgis`・`npx vitest run`・
  `tsc --noEmit`・`ruff check`が通る。**減った行数を段階ごとに記録する**
  （T963の問いへ答えるため）。

## 縮小して閉じる（2026-09-24、ユーザー決定）

**問い**（仕掛中のダッシュボード`T968-shrink`、司令塔が改善系の精査から置いた）: デッドコード総整理
（T968）を縮小して閉じ、T965（一本化の棚卸し）をどうするか。選択肢は「T968は縮小して閉じ、T965は
見送りで閉じる」「T968は縮小して閉じ、T965は20件試して当たり率で決める」「両方とも元の範囲でやる」。

**答え**: (2) T968は縮小して閉じ、T965は20件試して当たり率で決める（2026-09-24）。

縮小の中身——**上の「完了条件」をこう読み替えて閉じる**:

| 段階 | 縮小後の扱い |
|---|---|
| 1 | 済み（2026-09-21） |
| 2 | 残りを測り直して1件ずつ処分する（下） |
| 3 | 母集団がほぼ空。測り直した結果を書くだけ（下） |
| 4・5 | [T998](T998.md)へ寄せる（何を引き取ったかはT998の「T968から引き取った段階4・5」節） |
| 6・7 | [T1001](T1001.md)段階Eへ移し済み（2026-09-23） |
| 8 | [T996](T996.md)がbackendを全件読んだことで代える（T996は完了。この記録の「責務の逸脱を測った」節も、全モジュールへ広げても労力に見合わないと結論していた） |

T965はこの担当の範囲外（別の担当が20件試す）。

### 段階2の結果（第2回、2026-09-24）

`backend/scripts/dead_code_survey.py`をorigin/master（`c73fdf9a`）で測り直した。

**20件→11件→9件→5件**の順に減った。

1. **20件→11件: 9件は別タスクが既に消していた**（本番・テスト・文書のどこにも参照が無いことを
   `git grep`で確かめ、`git log -S`で消したコミットを引いた）。
   | 定義 | 消したコミット |
   |---|---|
   | `METRIC_GROUP_COUNTS`・`METRIC_GROUP_LANDCOVER`・`METRIC_GROUP_POI`・`METRIC_KEY_ACCIDENT`・`METRIC_KEY_INTERSECTION` | `e1b7499b6`（T998段階1やり直し・グループ5） |
   | `LANDCOVER_SQL_KEYS` | `e41d372cf`（T998段階1やり直し・グループ2） |
   | `all_axes` | `2a675d55`（T1034） |
   | `BIN_DROPPED_DICT_FIELDS` | `0018d027`（T1005） |
   | `WARNING_BADGE_LEVELS` | `2fcbbb797`（T998段階1） |
2. **11件→9件: 列挙器の穴で2件が偽陽性だった**。`msm_client.refresh`は`main.py`が
   `from app.infrastructure.msm_client import refresh as refresh_msm`で読み、lifespanから呼んでいる。
   調査スクリプトは`import`の**別名だけ**を名前として拾っていたため、元の定義への辺が切れていた。
   `warn_if_stale`は`refresh`の中から呼ばれており、道連れで孤立して見えた。**参照0に見えたものを
   そのまま消していれば、本番の予報の同期が止まっていた。** 別名と元の名前の両方を拾うよう直した
   （`_referenced_names`）。
   - 検算（別の入力）: T956直前のツリー（`3e312fbb6~1`）で、直した調査スクリプトが
     `compute_edge_costs_bulk`・`compute_cost_from_axis_scores`を「到達しない」、`EdgeMaterialTable`を
     「到達する」と、段階1の答え合わせと同じ判定を出すことを確かめた。
3. **9件→5件: 4件を消した**（テストの観測のためだけにある口で、同じことを公開のふるまいで確かめられる）。
   | 定義 | 処分 |
   |---|---|
   | `cache_generation.read_persisted_revision`・`graph_material_cache.read_persisted_revision` | 消した。docstringは「テスト・診断用」だが、診断で読む側は無い（T851で非公開名を置き換えたとき「診断用の公開関数」として残したもの）。テストの「記録が世代Nである」は「同じ世代Nで確かめ直すと捨てない（`False`）」で書ける——それが記録の果たす役目そのもの。`test_cache_generation.py`の`TestReadingBackWhatWasRecorded`（この関数だけを見ていた2件）は消した。記録の有無は同ファイルの`TestTheFirstLookAtADiskCache`・`TestWhenNothingHasChanged`が既に見ている |
   | `search_graph_cache.lazy_graph_cache_size`・`routable_index_cache_size` | 消した。使っていたアサーションはすべて、同じテストの中で`get_*`による確認と重なっていた。この関数だけを見ていた`test_size_tracks_entry_count`は消した |
4. **残す5件**（テストの隔離のための口）。
   | 定義 | 残す理由 | いつ通るか |
   |---|---|---|
   | `debug_log.reset_stats` | 集計・WARNING抑制窓はプロセス全体の状態で、テスト間で持ち越すと件数の検査が順序に依存する | `test_debug_log.py`・`test_debug_stats_route.py`のfixtureが毎テストの前後 |
   | `landcover_raster.reset_sources_for_testing` | 開いたラスタをプロセス内に保持し、設定を差し替えても開き直さない | `test_landcover_tile.py`が設定を差し替えるたび |
   | `redis_client.reset_circuit_breaker` | サーキットブレーカーの状態がプロセス全体で共有される | `conftest.py`のfixture |
   | `tile_persistent_cache.use_directory` | ディスクキャッシュの置き場を本番のディレクトリから一時ディレクトリへ移す | `conftest.py`のfixture（全テスト） |
   | `derived_data_revision_service.reset_for_tests` | 最後に読んだ世代とTTLの期限をプロセス内に持つ | `test_derived_data_revision_service.py`・`test_axis_catalog_routes.py` |

   どれも代わりにテストがモジュールの非公開変数を直接書き換えると、実装の形を固定することになる
   （この記録の「責務の逸脱を測った」節が逸脱として数えた形）。**残す条件の「通った回数が観測
   できる」はテストの実行そのもの**で、本番の経路ではない。

**動的参照（`--show-dynamic`）は4件のまま**（`axis_definitions`・`road_graph_repository`2件・
`region_service`）。孤立点5件のモジュールとは重ならない。

**列挙器にもう1つ穴が残っている**——同名の定義が複数あると全部を生きている側へ倒すため、
テスト専用の口でも名前がありふれていれば孤立点に出ない。実例: `tile_score_matrix_cache.size`
（「テストの検証用」）・`search_graph_cache.clear`（「テスト用。本番コードパスからは呼ばない」）。
この形は段階4-1（テスト専用の口）の母集団で、[T998](T998.md)へ寄せた。

#### 同じ値を2か所で持つ形（段階2の後半）

`backend/app`のクラスのうち注釈付きのフィールドを持つもの162件を`ast`で集め、同名フィールドを
8個以上共有する組を出した（スクラッチの`W-dead_shared_fields.py`）。13組。

- **T956の性質（同じ値をディスク・キャッシュへ二重に載せる）に当たる組は0件**。
- 出た組は層の境界の写しだった: ORMの行とdomainの型（`AxisDefinitionRow`↔`AxisDefinition`）、
  serviceのdataclassとAPIの応答モデル（`db_status`・`derived_data_freshness`・`material_catalog`の
  各`*Entry`）、同じルーターのリクエストと条件（`RouteGenerateRequest`↔`GenerationConditions`）、
  edge単位とway単位の材料表（`EdgeMaterialRow`↔`WayMaterialRow`）。前の3つは値が2か所に保存される
  のではなく、フィールドの宣言が層ごとにある形で、この段階の対象の性質とは別物として処分の対象に
  しない。最後の1組は同じ材料をedge単位とway単位の2表に持つ形で性質は近いが、材料のSQLとその生データは
  並行して動いている担当（T1044）の範囲のため、ここでは判断せず報告に回した。

#### 調査スクリプトの置き場

`backend/scripts/dead_code_survey.py`は**検知器にしない**。CLAUDE.md「検知器を足す条件」の
2つに当たる——偽陽性を除く表（FastAPIのルート・ORMの宣言モデル等）を持つことが許可リストに
相当し、検査対象が実装そのもの（参照の形）である。一度きりの調査道具として`backend/scripts/`に
残す。T965が20件を試すときの入力に使える（T965「コード側の網羅は、入力の源流からの到達可能性で
出す」）。

**増減（段階2第2回）**: 実装 +4/−19（app −18・調査スクリプト +4/−1）・テスト +5/−24。

### 段階3の結果（2026-09-24）

1. **モデルと本番の制約の突き合わせ**: `backend/scripts/schema_gap.py`を本番DBへ読み取りで
   流した（`run_probe.py`経由）。**外部キー・NULL許容のGAPは0件**。出たGAP5件は
   `source_features_accident`等の「実DBにあるがORMが宣言していない表」で、取込が実行時に作る
   `source_features`のパーティション（`batch/ingest.py`が`PARTITION OF source_features`で作る）
   だった。親表はORMが宣言しており、制約の欠けではない。
2. **appの肩代わりの分岐**: `IS NULL THEN NULL`は`backend/app`で0件（`grep -rn`）。
3. 張るべき制約が無いので、本番DBへの適用は発生しない。

段階3の完了条件（制約を張った／張れない理由を実測つきで書いた、肩代わりの分岐0件）は、
母集団が空であることで満たす。

### 段階4・5（T998へ寄せた）

段階4-2（テストファイル同士の直接依存）だけはここで測った: `from tests\.test_|import tests\.test_`
で`backend/tests`を探して**0件**（2026-09-20時点の2件は既に無い）。`test_`を外した
`^\s*from tests\.|^import tests\.`では共有ヘルパーのimport（28ファイル・34件）が拾えることで、
パターンの形が効いていることを確かめた。
残り（4-1・4-3・5）はT998へ寄せた。

### 閉じたときに残ったもの

- 第1回の「残した論点」の`source_profile.yaml`の`bbox: from_target`（3か所）は、今も読む側が無い
  （アダプタは`target.bbox`を無条件に使い、`source_runs.profile`を読む側も`target.bbox`と
  `rows.years`しか見ない）。同じ性質（プロファイルの効かない欄）を[T997](T997.md)段階A2が
  `columns`・`tags`について片付けたため、T997の件として仕掛中のダッシュボードへ改善案
  （`T997-from-target`）を置いた。

所要（並行実行）: 依頼 2026-09-24 04:02 → 完了 04:15（13分）、うち枠待ち0分・CI待ち0分、規模札L

## T1001からの返し（2026-09-25、T1001を閉じた）

段階6・7（frontendの到達可能性とテストの整理）はT1001段階Eで済んだ: 本番から辿れない部品・定数とテスト用の道具を消し、「そのテストだけが通す行が0行」のテストを1本ずつカバレッジで流して判断した（it 25本を削除）。frontendの未使用の検出はknipをCIへ入れて固定した（`docs/conventions/testing.md`「使われないコード（knip）」）。記録は[T1001](T1001.md)。
