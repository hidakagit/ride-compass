# T1130. データベースなし構成の切り替えが前提を失ったまま残り、ルート直下の.env.exampleで起動すると地図の路面・POI・事故が空になる

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 旧スキーマ・旧インフラを指したまま残った経路（再発）。T462以降ルート生成は常にDB必須（backend/.env.example:30-41も「DBなしプロファイルは選べない」）だが、config.py:17 road_graph_use_repositoryの分岐がdependencies.pyに5か所、その先のrepository is None・| Noneの分岐がregion_service.py 96/169/216/239・accident_service.py:31・wind_way_service.py:79・gradient_way_service.py:52・axis_admin.py:386・material_catalog.py:183・tile_version_service.py:51に残る。ルート直下の.env.example:14-16はfalseで「既定false」「read-throughキャッシュ」という消えた概念のまま、docker-compose.ymlがこれを読む（composeでの実起動は未確認、推論）。母集団: grep road_graph_use_repository と、そのフラグがNoneを注入する型（RoadGraphRepository | None・AccidentTileQuery | None）を受ける箇所。フラグとNone分岐を撤去し、2本の.env.exampleを直す。
