# T1070. 判断待ちの一覧が、保留の選択肢を別々の保留として数え、節の後ろのユーザー決定を認識しない

状態: 完了（2026-09-23）

## 背景

判断待ちの一覧（python scripts/orchestrate.py decision list、scripts/orchestration/decisions.py）がタスク記録の保留を集めるとき、2つの穴がある。(1) 保留の節の中の選択肢（番号付きの項目）を別々の保留として数える——1件の保留が選択肢の数だけ判断待ちに見える。(2) 節の後ろ（同じ見出しの下の末尾や直後）に書いた「ユーザー決定（日付）」を、その節の決定として認識しない——決着済みの保留が判断待ちに残る。実例（2026-09-23）: T958 は「T955の判定……着手の可否はユーザー判断で保留」の節1件が3件に見え、D-007のユーザー決定をファイル末尾へ書いても3件とも消えなかった。規約「ユーザーの判断待ち」は、判断待ちかどうかを記録の保留から導く（状態の表に写さない、T1065）ので、集め方の誤りはそのまま判断待ちの誤表示になる。

対応方針: 保留の単位を「保留を宣言した段落（見出しに保留を含む節なら節全体、『保留:』で始まる段落ならその段落）」とし、中の選択肢は1件の保留の選択肢として扱う。決定の書き方を規約に1つ決める（例: 保留の節の中に『**ユーザー決定（日付）**:』の段落を置く）——書き方を決めずに位置の推測で拾わない。既存の記録で今日決まったもの（T958）をその書き方へ直す。

完了条件: T958 が判断待ちに1件として出て、ユーザー決定を決めた書き方で書くと0件になることを、壊した入力（選択肢3つの保留を持つ一時の記録）で確かめる。decision list の全体の件数が変わる場合は、変わった分の内訳を記録する。

## 実施内容

- `scripts/orchestration/decisions.py`: 保留の単位を「2段目以下の見出しに『保留』を含む節（次の同じか
  上の見出しまで）」か「保留を宣言する段落（行頭が`**保留（`・`**保留:`・`**保留1`・`保留:`等。次の
  見出しか次の保留の段落まで）」にし、中の番号付きの項目は1件の保留の選択肢として扱う
  （`hold_units`）。決定は、単位の中の行頭が`**ユーザー決定（`の段落だけを数える（位置からの推測や、
  文中の「ユーザー決定」の語では外さない）。1段目の見出し（タスクの題名）と「**保留し続けた場合に
  何が起きるか**」のように保留が続く語の一部であるものは、宣言として数えない。一覧は1件ごとに先頭
  12行までを出す（単位が節全体なので表まで含むことがある）。
- 規約`orchestration.md`「ユーザーの判断待ち」に「保留と決定の書き方」を置き、「判断に迷ったら」と
  `/orchestrate:decisions`から指した。
- T958: 司令塔がファイル末尾（「台帳にあった補足」節）に書いた D-007 の決定を、保留の節の中へ移した。

## 検証

壊した入力（使い捨てのスクリプト。一時の記録へ直す前と後の`record_holds`を当てる）:

| 一時の記録 | 直す前 | 直した後 |
|---|---|---|
| 選択肢3つの保留、決定なし | 3件 | **1件** |
| 決定をファイル末尾（別の節）に書く | 3件 | 1件（単位の外の決定は数えない） |
| 決定を保留の節の中に決めた書き方で書く | 3件 | **0件** |
| 選択肢の行の途中に「ユーザー決定」の語だけ書く | 2件 | 1件（位置の推測で拾わない） |
| 題名に「保留」の語、本文に「**保留し続けた場合に…**」だけ | 1件 | 0件 |

**decision list の全体の件数**（origin/master の台帳の未完了87タスクの記録）: 直す前16件 → 直した後
（T958を直す前の記録で）13件 → T958を直した後12件。変わった分の内訳:

- T958: 3件→1件（選択肢3つを1件に）→0件（決定を節の中へ移した）
- T307: 1件→0件。題名（1段目の見出し）の「トリガー未到達・保留」を保留の宣言として数えていた。
  本文はトリガー待ちで、ユーザー判断の保留ではない。
- 途中の版で、T826・T533（「**保留し続けた場合に何が起きるか**」「**保留・T543へ分割**」）と
  T1070 自身（題名）が新たに数えられたため、宣言の書き出しを`**保留`の後が`（`・`:`・`：`・数字の
  ものに絞り、1段目の見出しを外した。直した後の版ではこの3件は0件で、上の全体の件数はその版のもの。

- `python -m ruff check scripts/orchestrate.py scripts/orchestration/`・`python scripts/review_checks.py docs`。

所要（並行実行）: 依頼 2026-09-23 17:02 → 完了 17:08（6分）、うち枠待ち0分・CI待ちはこのコミットの後（不明）、規模札S
