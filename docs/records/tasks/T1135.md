# T1135. 取込・派生データの作り直しの間、生データと派生が食い違った道路網と値の無いタイルが本番で配られる

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 途中の状態の露出（新しい語）。batch/_common.py:42-70のrun_batch_cliはどのバッチでも最後に世代を進め、road_network_store.ensure_currentで道路網の配列を作る。取込（ingest_cli.py:57）もこれを通り、取込の直後に作る配列は新しい生データの道と古いroad_edges/way_materialsを結合したもの（road_graph_repository.py:684-693）で、本番のルート生成がそれを読む。派生の作り直しは実測1時間05分（T970段階D）。_common.py:83-84の「余分に進めてもキャッシュが1度作り直されるだけ」はT1112で配列の作り直し（数分・1.6GB）が入った時点で崩れた。派生は段ごとに別トランザクション（derive_cli.py:57-60）で、最初の段derive_topology.py:171のTRUNCATE road_edges, node_materials CASCADEでedge_materialsが全行空になり、raster・countsの段がコミットするまで区間単位のタイル（z14以上）・勾配の配信・区間インスペクタは値の無い状態を読み、しかもその間に焼いたタイルは古い世代の鍵でディスクへ残る。対照: ingest.py:11は同一トランザクションで途中状態を隠している。方針: (a)取込の入口では配列を作らない（世代は進めてよい） (b)派生は全段を1トランザクションで流すか影の表に作って入れ替える——どちらにするかはダッシュボードの問い。本番DBへは書かない。
