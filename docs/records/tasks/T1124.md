# T1124. ルート結果に、道のりに沿った難易度のグラフ（横軸が距離、縦が区間ごとの難易度、塗った面積が負荷）を出す

状態: 完了（2026-09-25起票・2026-09-26完了）

## 背景

ユーザー要望（2026-09-26）: ルート結果の負荷と難易度の横グラフで、もう少し視覚的に情報量を増やしたい。今は1本の帯（長さ＝総合難易度、高さ＝距離、面積＝負荷、色＝軸）で、1軸だけ効くルートでは1色の短い帯にしか見えない。ユーザー決定: 横軸を距離（0〜全長km）、縦を区間ごとの難易度にした山形のグラフにする。どこが難所かが読め、塗った面積がそのまま負荷になる（今の「面積＝負荷」の読み方と同じ）。色は区間で効いている軸。区間ごとの値は既にAPIが返している（segmentsのaxis_contributions等）。まず区間の値と負荷の定義（負荷＝総合難易度×距離）がグラフの面積と一致するかを調べる段階を切る（①）。規模の根拠: 部品の新設・RouteAxisProfile・候補一覧の行・テスト・設計書で、変わるファイルは8〜12（③20以下）。

ユーザーの追加（2026-09-26）: グラフ上にバーを出し、動かすと地図の対応する場所を出したい。場所の単位を問われ、「場所は点単位、
値はedge単位」で合意した（区間の値はedgeごとに一定で、それより細かい値は無い。地図の印は区間の道なりの形の上を距離の割合で動かす）。

## 段階①の調べ

- 区間（`RouteCandidate.segments`）はルートが通るedge 1本につき1つ（`road_graph_engine.py`が`RouteSegmentDetail`を作る）。
  区間ごとに`difficulty`（0〜100、値が無ければnull）と、合計がdifficultyになる軸別の`axis_contributions`、道なりの形・累積距離を持つ。
- 負荷は「値のある区間の距離加重平均（`overall_difficulty`）× 値の無い区間も含めた全長」（`domain/difficulty.py: difficulty_load`）。
  値の無い区間を飛ばして面積を足すと負荷より小さくなる。**値の無い区間を平均の高さで描けば、面積は負荷に一致する**。
- 1ルートあたりの区間数・edge1本の長さは未計測（クラウドの開発DBに道路網が無い）。描画は軸ごとに1本のpathへまとめるので、
  区間数が増えても要素数は軸の数で決まる。

## 実施

- `features/route/DifficultyProfile/`（新設）: 形の計算（区間の柱・軸ごとに積んだ長方形・距離→区間と割合・区間の形の上の点）と
  グラフの部品（SVG。押したまま動かす／矢印・Home・Endで地点を選ぶ）。横軸の右端は一覧の中で最も長い候補の距離。
- 選択は地図で区間を押したときと同じ`selectedRouteSegment`へ入れるので、地図の印と下の区間の詳細は既存の経路で出る。
  グラフは候補の中身の先頭に置き、区間を選んでいる間も残す。
- パネルの内訳バー（`AxisContributionBar`）の高さの倍率（距離）は外した——面積＝負荷はグラフが持つ。一覧の行の小さな帯は
  候補を見比べるために残した。負荷の(i)の説明と設計書（route-settings-and-results.md・page-composition.md）を直した。

## 検証

- e2eの模擬（60区間・値の無い区間を含む・2候補・390×812・ダークモード）で撮り、グラフが描かれ、押したまま動かすと縦線が付いてきて
  下にその区間の詳細（km地点・内訳）が出ることを見た（aria-valuetext「13.3 km地点」。一時のspec、コミットしていない）。
- `vitest run src/features/route src/components/AxisContributionBar src/app/page.test.tsx` 23 files 323 passed。tsc・eslint・
  prettier・knip・`review_checks.py docs`は指摘0件。
- わざと壊した3通りはどれも落ちた: 値の無い区間を描かない→面積が負荷と一致するテスト、道なりの長さを見ない→地点のテスト、
  押していない移動でも選ぶ→操作のテスト（いずれも1 failed）。
- 地図の印が本物の地図で出ることは、e2eの地図が空のスタイルのため見ていない（選択の経路は区間を押したときと同じ）。

規模の札: 実装＋テストの変更行550（実装+305/−22・テスト+190/−33）で**規模M**（起票時の予想どおり）。

## 後始末（2026-09-26）: 2本指の操作で区間が選ばれ、masterのCI（スマホの全状態の走査）が落ちた

- 症状: masterへ入れた8b0c96eのCIで、`e2e/all-states.spec.ts`（全状態の走査: mobile）が「検査の操作で状態が変わった」
  「元のタブへ戻せない」で落ちた。走査は部品ごとに2本指のピンチを始めてページが拡大しないかを見る。グラフは押した瞬間に
  区間を選ぶ作りだったため、ピンチで区間が選ばれ、表示が区間の詳細へ切り替わって(i)のボタンが消えていた。実際の利用者でも、
  グラフの上で拡大しようとすると区間が選ばれてしまう。
- 直し方: 押しただけでは選ばず、1本の指（マウス）でなぞるか押して離したときに選ぶ。**画面のどこかに2本目の指が触れた操作は
  選択にしない**。2本目はグラフの外に触れることがある（グラフの端から始めたピンチでは、グラフが受け取るポインターは1本だけ
  だった）ため、画面全体で触れている指をwindowのpointerdown/pointerupで数える。タッチイベントの指の数（`touches.length`）では
  間に合わない——グラフのpointermoveがtouchmoveより先に届き、2本目を知る前に選んでいた（手元で記録して確かめた）。
- 検証: 手元で同じ走査を再現して落ちることを見てから直し、`playwright test -g "全状態の走査"`（スマホ・デスクトップ）2 passed。
  グラフの端にかけたピンチ（1本目がグラフの中・2本目が外）で区間が選ばれないことを見た（一時のspec）。単体テストへ
  「2本指の操作は選ばない」「2本目がグラフの外でも選ばない」を足し、画面全体の判定を外すと後者が落ちる（1 failed）。
  `vitest run src/features/route src/app/page.test.tsx` 309 passed。tsc・eslint・prettier・knipは指摘0件。

## 訂正（2026-09-26）: 区間はEdge単位ではなく約500mのビン、面積と負荷の一致は「ほぼ」

段階①で「区間（`RouteCandidate.segments`）はルートが通るedge 1本につき1つ」と書いたのは誤り。`road_graph_engine.py: _build_candidate`は
Edge単位で組み立てた区間を`aggregate_segments_into_bins`で約500mのビンへ畳んでから返す（T1117の実装中に`SEGMENT_AGGREGATES`の
コメントから気づいた）。調べたのは`_build_segment_details`だけで、その後の畳みを見ていなかった。

これによる違い: ①グラフの階段と、動かしたときに選ばれる区間・下に出る詳細は、約500mのビン単位（地図の印は、ビンの道なりの形の上を
距離の割合で点単位に動くので変わらない）。②ビンの難易度は値のあるEdgeの距離加重平均（`distance_weighted_difficulty`）で、ビンの中の
値の無いEdgeはそのビンの平均で数えられる。負荷は値の無いEdgeをルート全体の平均で数えるため、**塗った面積と負荷の一致は厳密ではなく
「ほぼ」**。ビンの全部が値の無いときだけ、グラフは総合難易度の高さの灰色で描く。単体テストの「面積が負荷に一致する」は、区間を
渡したときの計算（値の無い区間を平均で描く）を確かめるもので、成り立つ。

設計書route-settings-and-results.mdと`difficultyProfile.ts`のコメントを直した（ユーザーへは会話でも伝えた）。

## 後始末（Windowsでの大文字小文字の衝突）

- 症状: `features/route/DifficultyProfile/`に`DifficultyProfile.tsx`と`difficultyProfile.ts`（テストも`.test.tsx`・`.test.ts`）が
  大文字小文字だけ違うモジュール名で並び、大文字小文字を区別しない開発機（Windows）では`./DifficultyProfile`と`./difficultyProfile`が
  同じファイルへ解決されて`tsc --noEmit`が落ちた（TS1149・TS1192）。CI（Linux）は別のファイルとして解決するため通り、masterでは見えなかった。
- 直したもの: 純ロジックを`profileGeometry.ts`（テストは`profileGeometry.test.ts`）へ改名し、importと設計書の対象ファイル表を追従した。
  同じ形の有無は`git ls-files`の全1,811件（ファイル・そこから導いたディレクトリ117件・拡張子を外したモジュール名370件）を小文字化して
  数え、この実装とテストの2組だけだった。
- 再発の止め方: `frontend/src/structure/caseOnlyPaths.test.ts`を足した。`frontend/src`配下の全ファイル（拡張子を外したモジュール名）と
  全ディレクトリを小文字化して比べ、綴りが2通り以上あれば落とす。CIはLinuxで動き、型検査もビルドもこれを見られないため構造では守れず、
  テストに置く。許可リストは持たない。
- 検証: 直す前に`lockrun … tsc --noEmit`がexit 2（TS1149・TS1192）、直した後にexit 0。改名前の名前の`difficultyProfile.ts`を
  わざと置き直して`vitest run src/structure/caseOnlyPaths.test.ts`が衝突の1組を挙げて落ちることを見てから消し、
  `vitest run src/structure/caseOnlyPaths.test.ts src/features/route/DifficultyProfile/`で3 files・16 tests passed。
- 所要（並行実行）: 依頼 2026-09-26 18:45 → 完了 18:56（11分）、うち枠待ち0分・CI待ち0分、規模札S、実績 +72/−2 → S
