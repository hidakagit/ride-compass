# T1156. 管理・運用の表示や記録に小さな食い違いと、閉じたタスクで見つけて放置された取り残しが残っている

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 写経・「機械が見ている」という嘘・未起票フォローアップ。(1)管理APIの「DB障害→503」がDBAPIErrorしか見ず（axis_admin.py:60、material_catalog.py:238、db_status.py:85、derived_data_freshness.py:68）、servicesが使うDB_UNAVAILABLE_ERRORS（infrastructure/database.py:13、13か所）と揃っていない（TimeoutError等が素の500になりうる。包まれるかは未確認） (2)勾配の配信のキャッシュのヒット率が集計に載らない（gradient_way_service.py:60-64がfields["cache_status"]、debug_log._recordはfields["cache"]だけを数える。logging.md:52-57の説明が成り立たない） (3)較正値の上書きのupdated_atが値を動かしても変わらない（tuning_overrides.py:244のon_conflict_do_update、読み手0）、source_models.py:7の「DBの起動時初期化（create_tables）」は事実と違う (4)閉じたタスクの範囲外で見つけたもの: msm_client.update_interval_seconds()の呼び手0（T1099）、axis_definitions.py:16の存在しない参照（T1096）、dynamic_way_value_needs_speedの往復テスト無し（T984）、source = 'accident'の手書き3か所（T965）、backend/appのassert 9件（T985）。1件ずつ直すか見送る理由を書く。
