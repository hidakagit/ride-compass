# T1144. データが取れないとき、画面とbackendが黙って既定値へ倒れる箇所がまだ残っている（T1117の残り）

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 黙った既定値（再発）。T1117（完了）はルートの所要時間の風・勾配・信号の欠けを知らせる形にした。残り: (1)軸カタログの取得失敗が既定の画面（条件タブ・地図・ヘッダー）に出ず、そのまま生成すると成功に見える（告知は重みタブの中だけ。route-settings-and-results.md:52の「失敗時は既存軸の静的フォールバック」は実装（EMPTY_CATALOG、hooks/useAxisCatalog.ts:22,42）と違う） (2)位置が取れないとき既定の地点を「現在地」と表示してそこで生成する（RouteForm.tsx:237がlocationSource==="default"を見ない。違いは印の色だけ） (3)軸カタログが取れないと乗り換えの区間の長さが管理画面で較正した値でなくビルド時の生成物の値になる（lib/axisCatalog.ts:90 EMPTY_CATALOG.clientTuning） (4)材料の係数が届かないとその材料の寄与を黙って0にする（axisLayers.ts:82） (5)backend: 範囲の境界で端点の座標が無い区間を道路網の配列の作成が件数も出さずに落とす（road_network_store.py:265-268。件数は未計測）。母集団の導き方: frontendはfailedを持つ取得フックと?? 既定値、backendは配列の組み立てで行を落とすマスク・結合。1件ずつ「知らせる・断る・そのまま（理由）」を決める。
