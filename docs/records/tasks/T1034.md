# T1034. 周期レビュー第12回の小さな指摘をまとめて直す

状態: 未完了

## 背景

第12回の4観点から出た、それぞれ単独では1タスクにならない指摘をまとめる。
**互いに独立なので、1件ずつ直して差し支えない。**

### 利用者に届いている誤り

1. **存在しないボタンへの案内。** レンズの説明文が「ルート設定パネルの『風』の
   『地図で色分け』ボタンから確認できます」と案内するが、**その可視ラベルは
   `frontend/src`に存在しない**。実際の入口は地図上部中央の「レンズ」。
   同じ幽霊がCSSのコメントにも書かれている。
2. **ヘッダーメニューの「研究モード」が、制御していないものを名乗る。** ラベルが
   重み調整を含むが、実際のフラグの全参照に重み配分は無く、重み配分UIは無条件に
   マウントされる。**ラベルの直上のコメント自身が、実スコープを別のものとして
   書いている。**
3. **生成条件のうち「想定速度」だけが保存されない。** 距離・候補数・モード・除外・
   重み配分は保存され、想定速度だけが再読込で既定へ戻る。出発時刻と走行方位は
   「行くたびに変わる値」として設計原則の但し書きの対象と読めるが、**巡航速度は
   利用者固有の安定した値**。毎回既定に戻ることに気づかないまま、到達予想時刻を
   自分の速度で計算されたものとして読む。
4. **走行条件バーの現在値が`aria-label`にしかない**（`title`も無い）。
   読み上げ利用者には値が届き、視覚利用者には届かないという逆転。
   値を常時表示するかは「地図の視界を削らない」とのトレードオフだが、
   **`aria-label`と`title`の非対称だけは片方の追加で解消する**。
5. **`page.tsx`のコメントが「天候・アプリ名は常設ヘッダにある」と書くが、
   ヘッダーにアプリ名は無い**（アプリ名を出すかどうかの判断は[T1038](T1038.md)で
   見送り済み。ここで直すのは嘘のコメントだけ）。

### 用語のゆれ

**対象を列挙しない**（列挙した瞬間に古くなる）。導出方法:
JSX中のテキスト・`title`・`aria-label`・`placeholder`に現れる日本語を機械的に抽出し、
①同じ対象を指すのに名前が違うもの、②同じ名前で指す先が違うもの、を洗い出す。
第12回で確認できた実例として、同一入力欄でラベルと検証エラーの語が違うもの、
無修飾の「難易度」が画面によって総合と軸別のどちらも指すもの、
**同じボタンの`aria-label`と`title`で語が違うもの**がある。

### 道具・規約の綻び

6. `.claude/commands/review.md`が指示する`metrics`のオプションが、実行すると
   `unrecognized arguments`で失敗する。
7. **ダウンロードの耐障害手順が3本に独立して書かれている**（一時ファイルへ書く→
   置換→検証→失敗なら退避）。うち2本は本範囲での新規追加で、定数名・値・コメント
   文面までほぼ同一。共通化するか、共通化しない理由を書く。

### 参照0・冗長

8. レジストリの全軸を返す関数が、`backend/app`・`backend/scripts`から参照0
   （テストからのみ7回）。**本範囲で新たに死んだものではない**（前回レビュー時点で
   既に0）。private化するか、消すか、テストの側を直すかを決める。
9. DI工場の型注釈が実装クラスをメンバー列挙しているため、3本目を足すと登録と型注釈の
   2箇所を直すことになる（登録自体は属性駆動で1本道になっている）。
10. 材料の欠損率APIで、way側は1クエリで全材料を集計するのに、区間側は判定式ごとに
    1本ずつクエリを回す。同じ目的に2つのやり方が並んでいる。管理API専用。
11. 材料カタログのコメントが「値が一覧に無いのは見落としではなく、束ねないと決めた
    結果である」と述べるが、**実際は不変条件テストが落とすことを許さない**
    （正準分類・対訳・表示行の3集合が完全一致を強制されている）。コメントは
    「落としてよい」と読めるが落とせない。

## 完了条件

各項目について「直した」か「直さないと決めた理由」のどちらかがある。
7・8・9・10は「直さない」も正当な結論なので、**結論を書くことを完了条件にする**
（直すことではなく）。

用語のゆれは、上の導出を1回回した結果と、そこから直したものをこのエントリへ残す。

## 実施記録

### 段階A: 利用者に届いている誤り（項目1〜5）

1. **直した。** 風（矢印）レイヤーの`panelHint`の案内先を、実在する入口「地図上部中央の
   『レンズ』で風の評価軸を選ぶ」へ直した。CSSコメント（`RouteSettingsPanel.module.css`の
   凡例チップ）からも幽霊の「地図で色分けアイコン」を外した。`grep -rn "地図で色分け"`
   （`docs/records`除く）は2件→0件。
2. **直した。** `researchEnabled`の全参照（`grep -rn "researchEnabled\|isResearchEnabled"`で
   `page.tsx`の4箇所）は、実験スロットへの記録・比較タブ・地図の重ね描き・選んだ区間の
   材料値の表示で、重み配分は含まない（重み配分UIは`RouteSettingsPanel`が無条件に持つ）。
   ラベルを「研究モード[実験スロット・比較・材料値]」へ直し、`HeaderMenu.tsx`・
   `researchMode.ts`のコメントと`docs/modules/frontend/developer-research-tools.md`を
   同じ範囲へ揃えた。
3. **直した。** 設計原則「利用者が意図して決めた値は覚える」の但し書き（行くたびに変わる
   ものは対象外）は、場所・出発時刻・走行方位に当たり、巡航速度には当たらない。
   `assumedSpeedKmh`を`useStoredState`（キー`ridecompass:assumed-speed-kmh`、範囲外・非整数は
   既定値）へ移し、`page.test.tsx`の保存テスト2本へ想定速度を加えた（保存値27が送信され、
   999は既定20になる）。`page-composition.md`の永続化表へも反映。
4. **直した。** `RideConditionBar`の出発時刻・想定速度の2ボタンへ、`aria-label`と同じ語の
   `title`を足した。母集団は「値をテンプレートで埋め込む`aria-label`を持ち、画面に値の
   文字が無いボタン」（`grep -rn 'aria-label={\`'`の19件を1件ずつ見た）で、該当はこの2件
   だけ（`LensControl`・`WarningBadge`は値を画面に描いている）。
5. **直した。** `page.tsx`のコメントから「アプリ名」を外した。

### 段階B: 用語のゆれ

**導出（1回）**: 母集団は`frontend/src`の`*.tsx`（`*.test.tsx`・`__fixtures__`を除く）。
正規表現で①属性`title`/`aria-label`/`placeholder`/`label`/`summary`/`triggerAriaLabel`/
`ariaLabel`/`chipLabel`/`loadingLabel`の値（`"…"`・`{"…"}`・``{`…`}``）、②JSXテキスト
（`>…<`の間）のうち日本語を含むものを抽出し、あわせて③同じ開始タグに`aria-label`と
`title`が両方あるものを組で出した。抽出414件、コード片の誤検出を除いて354件、組16件。
354件を値で整列して目で見た。JSX外の文言（検証エラー等の`.ts`の文字列）は抽出器が
見ないため、①で見つけた語（候補数・難易度・条件）をgrepで`.ts`まで追った。

**直したもの**:

- 同じ入力欄でラベル「候補数」と検証エラー「候補件数は…」→エラー文言を「候補数」へ
  （`useRouteFormSubmit.ts`）。
- 無修飾の「難易度」が区間の乗り換えパネルでは総合難易度（`overall_difficulty`）を指し、
  地図の区間インスペクタ・内訳では「軸別難易度」と修飾されている→乗り換えパネルの指標名を
  「総合難易度」へ（`RouteSplicePanel.tsx`）。モバイル幅（390px）でplaywright headless確認、
  指標グリッドの`dl/dt/dd`にはみ出し0件。
- 同じボタンの`aria-label`と`title`が食い違う（開いている間も`title`が「表示」のまま）
  →凡例の展開ボタン・説明ボタン（`MapOverlayControls.tsx`）の`title`を開閉に追従させた。
  組16件のうち語が食い違うのはこの2件。他は同じ語（`title`が`aria-label`の省略形か、
  補足を括弧で足した形）。
- 隣り合う2行の同じ✕ボタンが「経由地をクリア」「目的地を解除」→「目的地をクリア」へ。
- ②同じ名前で指す先が違うもの: 「ルート設定」の「条件」タブ（モード・距離・候補数・地点）と、
  重み・除外・想定速度の変更でも点く「条件が変更されています」→ヒントを
  「生成条件が変更されています」へ（`page.tsx`・`generationRequest.ts`のコメント）。

**見て直さないと決めたもの**:

- 軸スタジオ（管理画面）の「スコア」「点数」「得点」「0点/100点」: 「点」はスコアの単位の
  言い方で、別の対象を指していない。
- 「現在地に移動」（地図を動かす）と「現在地に戻す」（出発地を現在地へ戻す）: 別の操作で、
  動詞の違いがそのまま対象の違い。
- 「このルートを編集」（`aria-label`）と「このルートを編集（区間の乗り換え）」（`title`）:
  語は同じで`title`が補足を持つだけ。

### 段階C: 道具・規約の綻び（項目6・7）

6. **直した。** `scripts/review_checks.py`の`metrics`サブコマンドは引数を持たない（常に全体）。
   `review.md`の`metrics --full`を`metrics`へ。`metrics --full`は
   `error: unrecognized arguments: --full`、`metrics`は表を出して終わることを実行して確認。
7. **共通化した。** 3本（`fetch_osm_pbf.py`・`fetch_accident_csv.py`・`fetch_lulc_raster.py`）は
   同じ目的（配布元のファイルを手元の所定の名前へ、半端・壊れたものを取得済みに見せずに写す）
   で、違いは「開けるか」の判定と配布元のURLだけだった。一方で**3本目（土地被覆）だけ
   手順が食い違っていた**——既にあるものは大きさ>0だけで「取得済み」とし（開けないラスタも
   取り直さない）、開けなかったものは退けずに消していた。
   手順を`app/batch/_common.py`の`fetch_verified`（読めるものは落とし直さない・一時ファイル
   経由・落とし終えたら開いてみて、開けなければ`.broken`へ退けてFalse）へ1本にし、3本は
   判定関数とURLだけを持つ。土地被覆も「開けるものには触らない」へ揃えた（デプロイが毎回
   呼ぶが、開けるかの判定はヘッダを読むだけ）。置き場は`_common.py`
   （`static-road-attributes.md`は元から`_common.py`が「ダウンロード骨格」を持つと書いて
   いたが、実体は既に無かった。その記述を`fetch_verified`の説明へ直した）。
   テストは既存の2本（本票CSV・土地被覆）の偽応答へ`headers`を足し、土地被覆の
   「既にあるものは触らない」の既存ファイルを実際に開けるラスタへ替えた（開けない中身を
   「既にある」とみなさない振る舞いへ変えたため）。
