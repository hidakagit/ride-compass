# T1149. 運用の道具（scripts/のPython約4,500行）がCIのlint・型検査・テストのどれにも入っていない

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 検知器の母集団の穴（再発）。ci.yml:83の`python -m ruff check app scripts benchmarks tests`はworking-directoryがbackend/なのでbackend/scriptsしか見ず、backend/mypy.ini:6もfiles = app, scripts, benchmarks。git ls-files '*.py'でbackend/の外は16本・4,494行（すべてscripts/）で、テストがあるのはcheck_master_ci・deploy_backend_gateの2本だけ。期間中にscriptsを変えたコミットの本文には手元のruffが27件自己申告されている。運用に出てから見つかった欠陥がある（T1076の--helpがcp932で落ちる、T1081の非ASCII出力、T1057の枠の中の段の誤判定）。逆に、scripts/.claude/.githooksだけの変更（期間中48件）でもci.ymlの重いジョブ（pytest全件・E2E）が走る。母集団: リポジトリのPython・shのうちci.ymlのどの段の入力にもならないもの。CIの静的検査の対象をリポジトリの実在から導き（scripts/へruff・py_compile、shへbash -n）、入力にならない変更では重いジョブを走らせない。既存の違反件数は未計測（ruffの結果でS〜M）。
