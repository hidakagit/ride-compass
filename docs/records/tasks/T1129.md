# T1129. 撤去したタイル単位のキャッシュを前提にした設定・説明・型・読み直しの経路が残っている

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 旧スキーマ・旧インフラを指したまま残った経路（再発）。T1112（完了）でタイル単位の材料キャッシュを撤去した後の残り。設定: config.pyのtile_cache_load_max_concurrent（読み手0、82ed5ac8で喪失）・graph_build_max_concurrent（読み手0、T970段階C 91bc3832で喪失し第12回も素通り）・tile_persistent_cache_size_limit_mbの根拠「タイル材料1世代310MB」。説明: requirements.txt:17、deploy-backend.yml:102、road_graph_repository.py:773、tile_persistent_cache.py:5-6、route_generator.py:44、directory-layout.md:29-31、setup.md:105、api-design.md:50、e2e-live/s2-route.spec.ts:25、region.py:12-15のROAD_GRAPH_TILE_ZOOM。型: graph.py LeanEdgeのosm_way_id・segment_indexの|None既定（本番の組み立て4か所は全部整数）、highway・bearing_degは読まれない、routing.py:898のdocstringと実装が逆でedge_seconds|None=Noneは渡すと必ず落ちる、evaluation.pyのBulkAxisEvaluationとStaticEdgeScoreMatrixが6フィールド重複、road_network_store.py:295-315が偽のLeanEdgeを20万件ずつ作ってget_edge_material_arraysへ渡す、search_graph_cacheの名前が中身（迂回率1つ）と合わない。挙動: 地図タイルの世代の読み直しがGET /api/axis-catalogの副作用だけに乗り、起動後に誰かがカタログを取るまで路面・POI・事故タイルがディスクへ残らない（判断原則16の暗黙のタイミング制約。tile_version_service.py:18-19）。あわせて backend/tests/structure に「Settingsの各項目がconfig.py以外の本番コードで読まれている」構造テストを置く（母集団はSettingsのフィールドの導出。cors_allowed_originsのようなプロパティ経由も読みとして数える）。

## 方針（2026-09-26、担当 C-cache）

段階を2つに切る。段階Aは読み手の無い設定・古い説明・型の整理（挙動を変えない）、段階Bは地図タイルの世代の読み直しの
経路（挙動を変える）。engineの分割（T1147）には手を広げず、engineは名前の付け替えと`LeanEdge`の組み立ての行だけを触る。

**母集団の導き方**: 性質は「T1112で撤去したタイル単位の材料・スコア行列のキャッシュ（とその世代の突き合わせ・範囲ごとの
探索用キャッシュ）を前提にして書かれ、今も残っているもの」。`git ls-files`（`docs/records/`と生成物を除く）の全行を、撤去した
仕組みに結び付く語で引いた（`タイル材料|スコア行列|タイル単位|タイルごと|材料キャッシュ|ROAD_GRAPH_TILE_ZOOM|graph_build|
tile_cache_load|search_graph_cache|StaticEdgeScoreMatrix|BulkAxisEvaluation|グラフ構築`等、scratchpadのPythonで一括）。
110行が当たり、1行ずつ「撤去した仕組みを前提にしているか」で判定した。起票時の列挙に無く当たったもの:
`evaluation-scoring.md`の材料の粒度「区間群（タイル1枚ぶん）」・「（タイル単位でキャッシュ）」・並行Edgeの解消理由
「タイル集合キーでキャッシュするため」、`axis-studio.md`の「タイル単位でキャッシュ済みの公開軸スコア」、
`road_network_store._read_materials`の「範囲指定の読み出しと同じ」、`tile_cache.prune_to_size_limit`の
「`tile_persistent_cache`のように世代単位では消せる」という対比、`cache_identity.shape_digest`の署名の根拠（pickleの列の位置。
今の読み手は列名でファイルを読む道路網の置き場だけ）と、それを確かめる`test_cache_identity.py`（撤去した2つの型で確かめていた）、
`test_road_graph_repository_contracts.py`の「同じ文字列を1つのオブジェクトにしてpickleを縮める」確認。
残りの当たりは今の仕組み（探索範囲の静的スコア行列・動的値のタイル単位の配信・DEM等のタイル）を指していて対象外。
起票時の列挙のうち`api-design.md:50`は、T1051で既に今の所要（数秒〜数十秒）へ直っていた。

**設定の読み手**: `backend/tests/structure/test_settings_readers.py`を先に書いて撤去前のコードへ当てると、
`graph_build_max_concurrent`・`tile_cache_load_max_concurrent`の2件だけが落ちた（他の項目はすべて読み手あり）。
リポジトリ内に3項目の環境変数を書く場所は無い（`git grep -i "GRAPH_BUILD|TILE_CACHE_LOAD|TILE_PERSISTENT_CACHE_SIZE"`で0件、
`deploy-backend.yml`が環境変数ファイルへ書くのは`CORS_ALLOWED_ORIGINS`と`LULC_RASTER_PATHS`だけ）。本番の環境変数ファイルに
残っていても`Settings`は`extra="ignore"`のため起動に影響しない。

**`tile_persistent_cache`のタグは残す**: 書き込みのタグと`tag_index`は読み手が無いが、材料単位の失効をタグで実装する
未完了の[T1007](T1007.md)（要件としてユーザー判断済み）が読み手を作るため、ここでは触らない。

## 段階A: 読み手の無い設定・古い説明・型（2026-09-26）

- 設定: `graph_build_max_concurrent`・`tile_cache_load_max_concurrent`を消した。`tile_persistent_cache_size_limit_mb`の根拠を
  今の中身（way_id別の動的値、鍵ごとの失効）へ直した。読み手を確かめる構造テストを置いた（読みの形は属性・文字列・プロパティ経由の
  3つを数え、テストの中で1つずつ外して効くことを確かめる）。
- 説明: `requirements.txt`・`deploy-backend.yml`（経緯の「改善計画T538」も外した）・`tile_persistent_cache.py`・`tile_cache.py`・
  `route_generator.py`・`road_graph_repository.get_derived_data_revision`・`road_network_store._read_materials`・`directory-layout.md`・
  `setup.md`（冷えたキャッシュではなく、道路網の置き場が無いと生成が失敗することと作り方）・`e2e-live/s2-route.spec.ts`・
  `axis-studio.md`・`evaluation-scoring.md`・`cache_identity.shape_digest`。
- 型: `LeanEdge`の`osm_way_id`・`segment_index`・`forward`を必須にし、読まれない`highway`・`bearing_deg`を消した（形を取り直すSQLも
  方位の列を読まなくなった。逆向きの枝は`dataclasses.replace`で作る）。`get_edge_material_arrays`は区間の3本の列を受け取り、
  道路網の置き場が偽の`LeanEdge`を作らなくなった。探索の入口（`build_turn_expanded_tree`・`turn_expanded_shortest_path`）の
  `edge_seconds`を必須にし、渡さないと落ちる`None`の経路とそのテストを消した（説明も実装どおりへ）。`BulkAxisEvaluation`と
  それを行列へ束ねる関数を`build_static_edge_score_matrix`1本へ畳み、`StaticEdgeScoreMatrix`の使われない既定値を消した。
- 名前: `search_graph_cache`→`detour_ratio_cache`。`ROAD_GRAPH_TILE_ZOOM`（永続化キャッシュの単位という説明）を消し、
  迂回率の鍵の粒度として`graph_service._RANGE_KEY_ZOOM`へ移した。
- 検証: ruff 0件・mypy（176ファイル）0件・`review_checks.py docs` 違反なし・`pytest tests --co` 2,121件収集。
  pytest（settings_readers・cache_identity・graph_service・road_graph_engine・road_graph_repository_contracts・road_network_store・
  routes_generate・routing・wind・route_generation_behavior・axis_hierarchy・region）353 passed。
