# T1062. 検査の置き場を手元・作業ブランチCI・デプロイ前CIの3層に分け、pre-pushから重複を外す

状態: 完了（2026-09-23）

## 背景

ユーザーと合意した検査の置き場（2026-09-23）:「直接触った影響範囲だけローカルではテストして、前提テストは作業ブランチCIでやる。preフックからは同じテストは除く」。T1058 で作業ブランチ（orch/**）のCIとそこへのpushでのpre-push省略は入ったが、次の2つの穴のため、masterへのpushのpre-push（ruff・prettier・tsc・OpenAPIドリフト・review_checks docs）は外せない状態にある。
(1) CI（ci.yml）は prettier を見ていない。2026-09-23の並行実行で prettier の崩れによる差し戻しが3回あり、止めたのは監査とmasterへのpre-pushだけだった。
(2) .github/workflows/deploy-backend.yml は on: push: branches: [master] で起動し、CIの結果を待たずに本番へデプロイする。masterへ入るのは、作業ブランチで個別にCIを通ったコミットを司令塔が組み合わせたもので、組み合わせた木は作業ブランチのCIを通っていない。したがって今はmasterへのpre-pushが「本番に出る前の唯一の検査」になっている。

対応方針:
1. ci.yml の frontend ジョブに prettier --check を足す（手元の pre-push と同じ対象・同じ設定）。
2. backend のデプロイを master の CI 成功後に起動する形へ変える。書き方（workflow_run の conditions・head_sha の扱い・paths の絞り込みをどう保つか）は GitHub 公式文書（docs.github.com の Events that trigger workflows / workflow_run）を読んでから決める。frontend のデプロイ経路（どこへ・何をきっかけに）も確かめ、同じ扱いにできるか・できないならなぜかを書く。影響: CI が赤の間はデプロイされない。急ぎの修正も CI の完了（現状1分半〜3分）を待つ。workflow_dispatch による手動デプロイは残す。
3. 1・2 が master に入ってから、.githooks/pre-push から CI と重複する検査（ruff・prettier・tsc・OpenAPIドリフト・review_checks docs）を外す。残すのは停止ファイルの確認と check_master_ci.py の警告だけ。外す前後で master への push の所要時間を測って記録する。
4. CLAUDE.md（「完了扱いにする前に検査器を通す」の push 直前の記述・テスト方針のCIの記述）、docs/conventions/orchestration.md（監査の機械項目・まとめpushの手順）、docs/conventions/testing.md の記述を新しい置き場に合わせる。

完了条件: CI が prettier の崩れで落ちること（壊した入力1件、作業ブランチで run の URL を記録）、master の CI を失敗させた場合に backend のデプロイが起動しないこと（試験の方法は本番へ出さない形で設計し記録する。例: 試験用ワークフローで workflow_run の条件だけを確かめる）、pre-push が停止ファイルの確認と警告だけになっていること、文書が新しい置き場と一致していること。

## ユーザーの判断（D-015、2026-09-23）

「本番が一時的に止まっても問題ない」。デプロイの起動条件の切り替えと、その試験で本番が一時的に
止まることは許容されている。本番DBの行を消すような取り返しのつかない操作は、この判断の範囲外。
（実際の試験は、下のとおり本番に触れない形で行った。）

## 段階A: CIにprettierを足し、backendのデプロイをmasterのCI成功後に起動する（2026-09-23、L-env）

### 書き方の選択（公式文書で確かめたこと）

GitHub公式「Events that trigger workflows」の`workflow_run`節:

- 起動元の結論に関係なく起動する（成功だけに絞るには`github.event.workflow_run.conclusion`で条件を書く）。
- `GITHUB_SHA`は「Last commit on default branch」で、CIを通ったコミットではない（`head_sha`を別に読む必要がある）。
- 「This event will only trigger a workflow run if the workflow file exists on the default branch」
  ——作業ブランチでは起動しないので、masterへ入れる前に試せない。
- `paths`で絞れない。

GitHub公式「Reusable workflows」（reference）:

- 同じリポジトリの`./.github/workflows/<file>`は、呼び出し元と同じコミットのファイルが使われる。
- 呼ばれた側の`github`コンテキストは呼び出し元のもの（`github.sha`がpushされたコミット）。
- 呼び出すジョブで`needs`・`if`・`concurrency`・`secrets: inherit`が使える。
- ジョブの`needs`が失敗するとそのジョブはskippedになる（既定の`success()`）。

**採用: `ci.yml`の最後のジョブが`needs: [backend, api-contract, frontend, e2e]`で`deploy-backend.yml`を呼ぶ
（`on: workflow_call`）。** `workflow_run`より、(1) CIを通ったコミットがそのまま`github.sha`になり、
(2) 作業ブランチで同じファイルのまま試せる。`workflow_dispatch`（手動）は残した。

**`paths`の絞り込みは`scripts/deploy_backend_gate.py`へ移した。** `workflow_call`は`paths`を持てない
（`workflow_run`も同じ）。起点は「直前のpush」ではなく「本番で動いているコミット」（VMのコンテナの
`GIT_COMMIT`をSSHで読む）にした。直前のpushとの差分では、CIが赤で出せなかったコードの変更を、
テストだけを直した次のコミットが運ばず、次にbackendを変えるまで本番へ出ない穴ができる。
判定は: 本番のコミットが分からない→出す／同じ→出さない／CIを通ったコミットの方が古い（後から終わった
古い実行）→出さない／新しい→差分に`DEPLOY_PATHS`（GitHubの`paths`と同じ規則。上から当て最後に
当たったものが勝つ、`*`は`/`を跨がず`**`は跨ぐ）に当たるファイルがあれば出す／履歴が分かれている→出す。
出すのはCIを通ったそのコミット（VMで`git fetch --depth 1 origin <sha>`）で、masterの先端ではない。
判定とデプロイは1つのジョブにし、そのジョブに`concurrency: deploy-backend`を置いた（呼び出し側と
呼ばれた側の両方に同じ組を置くと、公式の注意のとおり取り合いになりうるため片側だけ）。
`backend/tests/structure/test_deploy_exclusions.py`は、除外の一覧をワークフローではなく
`DEPLOY_PATHS`から読むように変えた（移設後も同じ10件・173ファイルを受け取ることを確かめた）。

**frontend（Render）は同じ扱いにできない。** RenderはGitHubのpushで直接デプロイし、リポジトリに
設定が無い（`render.yaml`も無い）。Render公式（render.com/docs/deploys）に自動デプロイの設定
「After CI Checks Pass」があり、そのコミットのチェックが全て成功（skipped・neutralを含む）したときだけ
デプロイする（チェックが0件のコミットは出さない）。切り替えはRenderのダッシュボードの操作なので、
ユーザー確認事項にした。

### prettierをCIへ

`frontend`ジョブの`npm ci`の直後に`./node_modules/.bin/prettier --check "src/**/*.{ts,tsx,css}"`
（pre-pushと同じ対象。設定は`frontend/.prettierrc.json`・`.prettierignore`をそのまま読む）。

**足した時点でmasterが31ファイルで落ちた**（試験の緑のはずの実行 run 35829187212 がprettierで赤）。
pre-pushは変更したファイルしか見ないため、崩れたまま入ったファイルが残っていた。31ファイルを
`prettier --write`で整えた（`weatherApi.test.ts`はprettierのメソッドチェーンの折り返しが1回で
収束せず、2回目で安定した）。差分は31ファイル +161/−152。空白を全て除いて比べると24ファイルで
差が残り、その内訳は末尾カンマの追加53・削除14、型リテラルの末尾`;`の削除2、JSXを包む括弧・
アロー関数の本体を包む括弧の削除3と置換1——どれも構文上の記号で、挙動は変わらない。

### 試験（本番に触れない形）

作業ブランチ`orch/L-env`で、`ci.yml`の呼び出し条件に`orch/L-env`を足し、`deploy-backend.yml`の
本番に触れる2ステップ（SSHで本番のコミットを読む・SSHでデプロイする）を空打ちに置き換えた
試験用のコミットを積んだ（secretsを1つも参照しない形。concurrencyの組も試験用に分けた）。
判定のスクリプトはそのまま走らせた。試験用のコミットはmasterへ出していない。

- 緑（3a5ff836）: https://github.com/hidakagit/ride-compass/actions/runs/35830184071 ——全ジョブ成功ののち
  `deploy-backend / deploy`が起動し、注釈に`would deploy 3a5ff836…(event=push ref=refs/heads/orch/L-env)`
  と判定`出す: 本番のイメージに届く変更が2件ある（例: .github/workflows/deploy-backend.yml）`。
  呼ばれた側の`github.sha`がpushしたコミットであることを確かめた。
- 赤（34fa01e4、上の緑に`export const t1062PrettierProbe = {a:1};`を1行足しただけ）:
  https://github.com/hidakagit/ride-compass/actions/runs/35830200017 ——`frontend`がprettierの段で失敗し、
  **`deploy-backend`はskipped**（起動しない）。backend・api-contract・e2eは成功していた。

試験の途中で1回、試験用のステップ名に「試験: 空打ち」と`: `を入れてYAMLが壊れ、両ワークフローが
0秒で失敗した（run 35828035188・35828063236）。本物のファイルには無い形で、名前を直して撮り直した。

判定のスクリプトは手元でも実リポジトリの履歴に当てた: `de113fa4~1 → de113fa4`（backend/appの変更）は
「出す」、`origin/master~6 → origin/master`（docsだけ）は「出さない」、逆向きは「古いので出さない」。
`backend/tests/test_deploy_backend_gate.py`（照合の規則・判定表・一時リポジトリでの祖先関係、17件）と
`test_deploy_exclusions.py`（2件）は手元で通過（`pytest <2ファイル> -q` 19 passed、2.66秒）。

## 段階B: pre-pushから検査を外し、監査の静的検査も外す（2026-09-23、L-env）

方針3は「1・2がmasterに入ってから外す」。段階Aと段階Bは別のコミットにし、Bを後ろに積んだ。
**同じpushでmasterへ入っても安全**: そのpushのコミットの`ci.yml`が既にデプロイをCIの後ろへ
置いており、`deploy-backend.yml`は`on: push`を持たないため、pre-pushの門が無くなった最初の
push自体もmasterのCIを通るまで本番へ出ない。

### pre-push

`.githooks/pre-push`に残したのは、停止ファイルがある間のpushの拒否と、`orch/**`以外へのpushで
masterのCIが赤いときの警告（`check_master_ci.py`）だけ。ruff・prettier・tsc・OpenAPI生成物の
ずれ・`review_checks.py docs`と、それを支えていた範囲の計算・通過記録（`pre-push-passed`）・
`find_venv`を消した（フックは+15/−164）。いずれもCIの同じジョブが見る:
ruff→`backend`、prettier・tsc→`frontend`、OpenAPI→`api-contract`、docs→`docs-consistency.yml`。
tscの段が自分で枠を取る形（04c26ebc）もフックごと消えた。

停止ファイルの拒否が残っていることは、停止ファイルを置いた使い捨てのbareリポジトリを`GIT_DIR`に
指して確かめた（共有の`.git`へ置くと全員が止まるため）: 「停止ファイル（orchestration/STOP）が
あるためpushを拒否します」、rc=1。

**所要（masterへのpush1回あたりのフック）**:

| | 計り方 | 値 |
|---|---|---|
| 外す前 | origin/masterの旧フックを、gitと同じ形の標準入力（`refs/heads/master`宛て、この作業ツリーの範囲）で3回。通過記録の読み書きだけ外した写しで計測。**この作業ツリーには当時`node_modules`が無く、prettier・tsc・OpenAPIの段は飛んでいる** | 中央値4.92秒（3.59・4.92・12.13秒） |
| 外す前（飛んだtscの段） | `scripts/lockrun.py`の記録（`log.jsonl`）のうち、2026-09-23のフックのtscの段（typegen＋tsc） | 3件、保持41・74・131秒 |
| 外す前（pushを含む保持全体） | 同じ記録のうち、2026-09-23のmasterへのpushを含む保持（fetch・rebase・cherry-pick・pushの一続き。フックはこの中で走る） | 28件、中央値59秒・最大476秒 |
| 外した後 | 新フックを同じ形で3回 | master宛て中央値1.21秒（2.24・1.09・1.21秒。ほぼ`check_master_ci.py`のAPI1回）、`orch/**`宛て0.20秒 |

外した後の値は、masterへのpushを含む保持の全体ではない（fetch・rebase・cherry-pick・push自体は
残る）。全体の新しい値は、この変更がmasterへ入った後の`log.jsonl`で測れる。

### 監査の静的検査（`scripts/orchestration/core.py`）

`orchestrate.py audit`の「静的検査（pre-pushの門と同じ）」の段と、それだけが使っていた
`checks_place`・`blob_checks`・`static_checks`・`find_venv_python`・`run_tool`・
`PRETTIER_TARGET_RE`・`PRETTIER_BIN`・`TOOL_CONFIGS`・`--checks-in`・`--no-checks`・`shutil`の
importを消した（+5/−145）。この段の理由は「まとめてmasterへpushする時に初めてpre-pushの門で落ちると
全体が止まる」で、門が無くなると成り立たない。同じ検査は作業ブランチのCIが走らせ、監査はその結論を
「CI」の項（止め）で既に読んでいる。消したコメント行は8行で、どれも消した検査かpre-pushの門の
説明だった（他に記録の要る横断的な判断は無い）。`SCRATCH_RE`からフックの一時ファイル名
（`.git-pre-push-`）も外した（フックがもう作らない）。

### 文書

- CLAUDE.md: 「完了扱いにする前に検査器を通す」（門はCIに1つ、フックは検査をしない）、テスト方針の
  CIの記述（prettier・masterではデプロイの前）、検知器の実行タイミング（pre-pushを外す）。段階Aで
  同期ルールのデプロイの記述。
- docs/conventions/testing.md: 「検査の置き場（手元・作業ブランチのCI・masterのCI）」を新設し、
  フックの説明をそこへ寄せた。
- docs/conventions/orchestration.md: 枠の節（フックの重い段の記述）、完了とpush（監査の機械項目・
  prettierがCIに無いという記述を外す）、監査の項目の表から「静的検査」の行、まとめpushの理由と手順
  （push後のmasterのCIが組み合わせた木の最初の検査）。
- docs/architecture/tech-stack.md: CIの実行枠の節（pre-push、privateにしたときの見直し項目）。
- `scripts/new_task.py`・`scripts/lockrun.py`のpre-pushに触れた説明。

### 残るもの

- `<gitの共通ディレクトリ>/pre-push-passed`（旧フックの通過記録）はもう誰も読まない。共有の`.git`の
  中なので消していない（害は無い。消すなら`rm`1回）。
- T1045（pre-pushの門を速くする）は、門から検査が無くなったため対象が無くなる。閉じるかは司令塔の判断。
- frontend（Render）の自動デプロイを「After CI Checks Pass」へ切り替えるのはユーザーの操作
  （Renderのダッシュボード）。切り替えるまで、frontendはCIを待たずにデプロイされる（設定が
  「On Commit」の場合）。

### 差し戻し（2026-09-23 16:50）: T1065との衝突の解消

段階Aはmasterへ入った（78b1276d）。段階Bは、先にmasterへ入ったT1065（ea9e7c78、状態の表から
写しを外す）と`docs/conventions/orchestration.md`「監査の結果」の2か所で衝突した。両方を残した:
T1065の「監査済み・未pushは監査の記録とgitから導く」「push後に表を書き換える手順は無い」
「`board unpushed`」をそのまま使い、pre-pushの門に触れた語句だけを段階Bの記述（pushのたびに
CIとデプロイが走る・組み合わせた木を初めて検査するのはmasterのCI）へ置き換えた。

`scripts/orchestration/core.py`は自動で合流した。新しいcoreの上でも撤去が効いていることを確かめた:
消した名前（`static_checks`・`checks_place`・`blob_checks`・`find_venv_python`・`run_tool`・
`PRETTIER_*`・`TOOL_CONFIGS`・`checks_in`・`no_checks`・`shutil`）の参照はorigin/masterのcore.pyに
15件、合流後に0件。`STATIC_CHECK_TIMEOUT`は定期確認のサブプロセスが使うため残る（以前から）。
`orchestrate.py audit --help`は`--base`・`--no-ci`だけ。`ruff check --select F scripts/`は0件、
`review_checks.py docs`は違反なし。T1065が足した部分に静的検査を前提にしたものは無い（`静的検査`・
`pre-push`・`prettier`の語はcoreに0件）。

所要（並行実行）: 依頼 2026-09-23 15:32 → 完了 16:27（55分）、うち枠待ち0分・CI待ち7分、規模札M
所要（並行実行、差し戻し）: 依頼 2026-09-23 16:50 → 完了 16:54（4分）、うち枠待ち0分・CI待ちはこのコミットのpush後、規模札M

## ユーザー操作（待ち）: Renderのfrontendの自動デプロイを「After CI Checks Pass」へ（2026-09-23 依頼）

**なぜユーザーか**: Renderのダッシュボードの設定で、リポジトリからは変えられない。
**実行する場所**: Renderのダッシュボードでfrontendのサービス（ride-compass-frontend）の設定を開き、
自動デプロイ（Auto-Deploy）を「After CI Checks Pass」にして保存（項目名はRender公式文書の名称、
画面の中の位置は未確認）。今が「On Commit」なら、切り替えるまでfrontendはCIを待たずにデプロイされる。
**済んだら**: 司令塔へ一言。
