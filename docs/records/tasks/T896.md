# T896. T886で足した土地被覆レイヤーが、3つの別々の形で整合を欠いている

規模S。
状態: 完了（2026-09-16）

## 背景

新しい面レイヤーの追加が1本道でない（[T888](T888.md)）ため、手で追従すべき箇所が3つ別々に
取りこぼされた。3つとも本番に出ている。

1. **ズーム不足の案内が一度も表示されない**。`page.tsx`が`summaryByLayerId.landcover`へ
   「ズームインすると表示されます」を入れるが、同じ`useMemo`が`legendDetailsByLayerId.landcover`へ
   常に非空の凡例を入れ、`MapOverlayControls.tsx`は`hasLegend ? 凡例 : summary`で描く。
   道路側は`regionZoomTooWide`のとき`legendDetails = []`と空にしており、landcoverにはこの
   片割れが無い。
2. **初回描画だけラスタが路面線の上に乗る**。`MapView.tsx`が`"elevation"`というハードコードのkeyで
   標高だけを路面ソースより前へホイストしている。土地被覆と路面の種類を同時にONにすると、
   再描画を押すまで路面の色分けが濁る。
3. **一次属性カタログが矛盾している**。`primaryAttributes.ts`の`PRIMARY_ATTRIBUTES_WITHOUT_LAYER`に
   `"landcover"`が残り、その根拠コメントが撤去済みの開放度軸を挙げている。ドリフト検知テストは
   「対応表に無い」「両方にある」しか見ないため通る。

統合レビュー第10回のP1 F-5（[2026-09-16_all.md](../../../.claude/commands/review/history/2026-09-16_all.md)）。

## 完了条件

- ズームを引くと案内が出る／初回描画でもラスタが路面線の下／
  `primaryAttributeIdsToLayerIds(["landcover"])`がレイヤーを返す。
- [T888](T888.md)の本文へ、この3つを動機として書き足し、**射程へ「描画順の決め方」と
  「最小ズームの宣言」を含める**（どちらも`MapLayerDescriptor`へ持たせれば配列から導ける）。

## 実装メモ

1. **ズーム不足の案内**: 凡例を空にしてから案内文を出す（道路系と同じ扱い）。
   `MapOverlayControls`側を「summaryがあれば凡例の上に併記」へ寄せる案は採らなかった
   ——道路系のsummaryは凡例が図で示している内容を文でも書いたもので、常に併記すると
   今まで出ていなかった重複した文が全レイヤーの▶へ増える。**同じ規則が2本あること自体**は
   [T888](T888.md)の射程（最小ズームの宣言）へ送り、そこで1本にする。
2. **描画順**: `OverlayLayerEntry.underRoadSurface`という宣言を足し、`layersUnderRoadSurface`
   が配列から選ぶ形にした（keyの名指しをやめた）。
3. **一次属性カタログ**: `landcover`を「レイヤー無し」の一覧から外し、対応表へ入れた。
   撤去済みの軸を根拠に挙げていたコメントは[T892](T892.md)で外してある。

## 検証結果

- 3つそれぞれにテストを足した（ズーム案内＝`page.test.tsx`、描画順＝
  `MapView.overlayFilters.test.ts`、対応表＝`primaryAttributes.test.ts`）。
- **効いていることの確認**: 3つの修正をまとめて外すと、追加した3件に加えて既存の
  ドリフト検知テスト（対応表にも「レイヤー無し」にも無い状態を拾う）が落ち、計4件が赤。
  戻すと74件緑。
- ズーム案内のテストのために、`MapOverlayControls`のモックがsummaryと凡例の件数を読める
  ようにし、ビューポート通知を出せるMapViewスタブを足した——**この組み合わせが画面から
  読めなかったこと自体**が、案内文が一度も出ないまま本番へ出た理由である。
- `tsc --noEmit`緑。

## T888への追記

射程へ「描画順の決め方」と「最小ズームの宣言」を含めた。動機（T886で3箇所を別々に
取りこぼしたこと）も書き足してある。
