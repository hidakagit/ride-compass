# T1157. 路面の対訳と土地被覆の材料が、正本の表から導かれず手で写されている

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 写経（再発）。road.py:11-40の_SURFACE_IS_GOOD（24キー）とmaterial_catalog.py:386-411の_SURFACE_VALUE_LABELS（24キー）はキー集合が完全一致するが、対訳表は導かれておらず整合を確かめるものも無い。static-road-attributes.md:75-79の「路面は正準が1箇所で、他はすべてそこから導く」は事実と違う。土地被覆の材料8件（material_catalog.py:690-777）はlandcover.py:66-79 LANDCOVER_CLASSESから生成されず説明文も8回写している（停止要因POIの材料はPOI_COUNT_KINDSから生成する形が既にある、material_catalog.py:1089-1110）。路面の対訳は正準の表の1行（タグ→良否・名前）へ入れ、土地被覆の材料はクラスの宣言から生成する。

前提の変化（2026-09-26、T1175 段階1）: `road.py`の`_SURFACE_IS_GOOD`は撤去され、正準は路面の区分の表`SURFACE_CLASSES`（区分→名前・舗装か・属するタグ）になった。舗装良否・地図の表示行・区分の値の呼び名はそこから導く。タグ1つずつの呼び名（`_SURFACE_VALUE_LABELS`）は写しのまま残っているので、この件の路面の側は「区分の表の各タグへ呼び名を持たせる」形で当てる。
