# T1157. 路面の対訳と土地被覆の材料が、正本の表から導かれず手で写されている

状態: 完了（2026-09-27）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 写経（再発）。road.py:11-40の_SURFACE_IS_GOOD（24キー）とmaterial_catalog.py:386-411の_SURFACE_VALUE_LABELS（24キー）はキー集合が完全一致するが、対訳表は導かれておらず整合を確かめるものも無い。static-road-attributes.md:75-79の「路面は正準が1箇所で、他はすべてそこから導く」は事実と違う。土地被覆の材料8件（material_catalog.py:690-777）はlandcover.py:66-79 LANDCOVER_CLASSESから生成されず説明文も8回写している（停止要因POIの材料はPOI_COUNT_KINDSから生成する形が既にある、material_catalog.py:1089-1110）。路面の対訳は正準の表の1行（タグ→良否・名前）へ入れ、土地被覆の材料はクラスの宣言から生成する。

前提の変化（2026-09-26、T1175 段階1）: `road.py`の`_SURFACE_IS_GOOD`は撤去され、正準は路面の区分の表`SURFACE_CLASSES`（区分→名前・舗装か・属するタグ）になった。舗装良否・地図の表示行・区分の値の呼び名はそこから導く。タグ1つずつの呼び名（`_SURFACE_VALUE_LABELS`）は写しのまま残っているので、この件の路面の側は「区分の表の各タグへ呼び名を持たせる」形で当てる。

## 実施内容（2026-09-27）

- 路面: `road.py: SurfaceClass.tags`を「タグの値→呼び名」の対応にし、`_SURFACE_VALUE_LABELS`（24件の写し）を消した。
  材料「路面種別」（`surface`）の`value_labels`は`SURFACE_CLASSES`の各タグから導く。区分の値式（`surface_class_sql`・
  `surface_good_sql`）は対応のキーを読むだけなので、焼き込みSQLは1字も変わらない（`region-tile-config.json`の再生成で差分0）。
- 土地被覆: 材料8件の手書きをやめ、`LANDCOVER_CLASSES`から1クラス1材料で生成する（手本は停止要因POIの材料）。
  材料id＝割合列の名前、表示名＝クラスの表示名＋「被覆率」。列・焼き込み列の名前の規則を`landcover.py`の
  `landcover_key`・`landcover_tile_property`へ寄せ、同じ`removesuffix("_percent")`・`_pct`を書いていた箇所
  （集計SQL・派生バッチ・鮮度台帳の列・路面タイルの焼き込み列・区間インスペクタ・テスト2本）をそれへ向けた。
- 説明文の補足（裸地「河川敷・造成地等」・湿地「冠水植生」）はクラスの宣言へ入れず、材料カタログの
  `_LANDCOVER_CLASS_NOTES`に置いた。`LANDCOVER_TILE_VERSION`は`LANDCOVER_CLASSES`の中身から署名を作るため、
  クラスへ項目を足すと塗りが同じでもラスタタイルの世代が変わり、ディスクの旧世代が残る。
- 文書: static-road-attributes.md（路面の正準がタグの呼び名も持つこと・土地被覆のレジストリが材料も生む
  こと）と、evaluation-scoring.md（土地被覆の材料の生成元。あわせて「区間の行が無ければway単位へ落とす／
  `el`・`wl`を`COALESCE`で繋ぐ」という記述が、T970段階Cで`em.lc_*`だけを読む形に変わった実装と食い違って
  いたので直した）。

## 検証

- 生成物の比較（再生成前の`material-catalog.json`と材料idごとに全項目を突き合わせる）: 材料44件・idの集合は一致。
  値が変わったのは`trees_percent`・`built_percent`の説明文だけ（「樹木被覆の割合」→「樹木の割合」、建物も同じ）。
  `surface`の対訳は24件で内容一致（並びは区分の順になった）。土地被覆の材料の並びは`LANDCOVER_CLASSES`の表示順
  （建物→樹木…、湿地→裸地）になった。`tile_property`・`value_sql`・`coverage`は全材料で不変。
- `export_openapi.py`の再生成で、変わった生成物は`material-catalog.json`だけ（`region-tile-config.json`・
  `landcover-classes.json`・`openapi.json`は差分0＝タイル世代は動かない）。
- `ruff check app tests scripts`: 指摘0。`mypy`: 201ファイルで指摘0。`review_checks.py docs`: 違反なし。
- `pytest tests/test_material_catalog.py tests/test_landcover.py tests/test_road_graph_repository_contracts.py
  tests/test_material_values.py tests/test_derive_landcover.py tests/test_material_catalog_routes.py
  tests/test_region_routes.py tests/test_landcover_raster.py`: 182 passed。

所要（並行実行）: 依頼 2026-09-26 23:58 → 完了 2026-09-27 00:27（29分）、うち枠待ち0分・CI待ち0分、規模札S、実績 +115/−151 → M（削除の写し121行と、路面の対訳を区分の表へ移した行が大半）
