# T1118. 本番のコードに残るテスト専用の関数・引数を撤去する

状態: 未完了（2026-09-26起票）

## 背景

設計原則 構造仕様15（アプリはテストの都合の口を持たない）に反し、本番から呼ばれずテストからだけ呼ばれる関数約10件・テストだけが渡す引数4件が残る（例: 派生データの世代を読み直す関数のforce引数・use_directory・リセット用の関数）。母集団を機械で出し、1件ずつ撤去か規約の例外かを決める。T1114で初期化の口を例外にするかが決まってから着手。起票案 proposal-test-only-hooks（2026-09-26承認）
