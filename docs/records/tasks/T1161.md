# T1161. ルート生成の「起動後の初回」と「広い範囲」の所要を測って内訳を出し、短くできるものを短くする

状態: 未完了（2026-09-26起票）

## 背景

T1112の作り直し後、都心40kmの初回は11.9秒・2回目は5.4秒（本番と同じ条件の使い捨てコンテナで実測）。起動後の初回だけ約6秒長く（numbaのコンパイルが主とみるが未計測）、都心60・80kmでは時刻ごとのコストの合成に5〜8秒かかる（2件同時で29秒）。まず本番と同じ条件で起動直後の初回と2回目の段階別所要（コンパイル・切り出し・スコア行列・合成・探索・評価）を測り、併せて探索範囲をz12タイルの外接矩形で切った区間数と要求のbboxで切った区間数を比べる（外接矩形は学習した迂回率の鍵のためだけ。graph_service.py、routing-engine.md）。内訳で、コンパイルを起動時・ディスクへ逃がせるか、合成の計算を減らせるか、切り出しを要求の範囲にできるかを決める。起票案 proposal-route-generation-first-and-wide-speed（2026-09-26承認）

**決定（ユーザー 2026-09-26、チャット）**: 計測は本番VM上の使い捨てコンテナ（本番と同じイメージ・メモリ上限）で行ってよい。
本番のDB・稼働中のコンテナ・設定へは書き込まない。終わったら使い捨てコンテナを消す。

## 段階1（途中）: 手元で分かること（2026-09-26、担当M-measure、origin/master 84896911）

**本番VMでの計測は行えていない**。担当のセッションから本番VMへのSSH（読み取りだけの状態確認を含む）が、実行環境の
自動判定（本番への読み取りとして拒否）で止まった。上の決定は司令塔経由で届いたもので、実行環境はそれを許可として扱わない。
本番での計測の手順は下の「本番での計測の手順」に用意し、手元で分かることだけを先に置く。

**前提の確認（今のコード）**:
- JITする関数は`domain/routing.py`の2つ（一対全木のDijkstra`_turn_expanded_dijkstra`と2点間のA*`_turn_expanded_astar`）で、
  どちらも`@njit(cache=True)`。イメージの組み立て（`backend/Dockerfile`）にもアプリの起動にも、事前にコンパイルする処理は無い。
- `cache=True`の書き先は、コンテナの中の`/app/app/domain/__pycache__`（コンテナの書き込み層。rootで動くので書ける）。
  デプロイは`docker rm`して新しいコンテナを作る（`deploy-backend.yml`）ので**デプロイのたびにキャッシュが消え、最初の生成が
  コンパイルを払う**。OOM等での再起動（`--restart unless-stopped`）は同じコンテナなのでキャッシュが残る。
- 道路網の置き場の形の署名は84896911でも`94e68b900648`で、本番の置き場`94e68b900648-rx`をそのまま読める（T1115・T1137・T1138は形を変えていない）。

**numbaのコンパイルとディスクのキャッシュ（開発機、`M-measure_numba.py`: 本番と同じ型の2区間の配列で呼ぶ。コンパイルの時間は
配列の大きさに依らない）**:

| 別プロセスで順に | 木（Dijkstra）の1回目 | A*の1回目 | 2回目以降 |
|---|---|---|---|
| 空のキャッシュ（コンパイルして書く） | 26.8秒 | 9.1秒 | 0秒 |
| 同じキャッシュから読む（2回） | 1.7秒・1.5秒 | 0.17秒・0.07秒 | 0秒 |
| キャッシュを使わない | 15.7秒 | 5.6秒 | 0秒 |

- 開発機は並行する担当の処理で負荷が高く（numpyのimportだけで0.5〜5.9秒に振れた）、絶対値は本番の目安にならない。
  本番では1回目の往路の木が5.3秒・2回目1.4秒（T1112段階3の前の計測）で、差の約4秒がコンパイルとみられる（本番では未確認）。
- 言えること: **1ビン・2ビン・逆向きの木は同じ1つの型で済み、関数ごとにコンパイルは1回**（`signatures=1`）。キャッシュから読むと
  コンパイルの約1/10〜1/16になる。キャッシュの大きさは4ファイル359KB。

**合成（`_LegCostComposer._compose_at`）の回数（コードから。仮定速度20km/h・風の時別予報がある場合）**:
周回は`prepare`が往路を見込み時間なしで1本合成し、`select_loop_turnarounds`が往路・復路を見込み時間（目標距離÷2÷速度）の
時刻ビン（1時間ごと、上限4本）で合成する。鍵が同じなら使い回す。

| 周回 | 往路のビン | 復路のビン | `prepare`の1本 | 合成の回数 |
|---|---|---|---|---|
| 40km | 1（`prepare`の1本を使い回す） | 1 | 使い回される | 2 |
| 60km | 2 | 2 | **使われない**（鍵が違う。中身は往路のビン0と同じ） | 5 |
| 80km | 2 | 2 | **使われない**（同上） | 5 |

1回の合成は切り出した区間の全体を走る（区間数は40km 97万・60km 193万・80km 273万、T1112）。60・80kmは40kmより区間が2〜3倍・
回数が2.5倍で、合成の時間（40km約1秒、60・80km 5〜8秒）の開きはこの積で説明がつく。**`prepare`の1本（5回のうち1回）は同じ中身を
2度作っている**。1回の合成の中で時刻に依らない部分（勾配・転がり抵抗・停止の待ち・時刻で変わらない軸の和）と風で変わる部分
（風の成分・走行モデルの解・風に依存する軸）の比は、本番の計測で出す（プローブが`c_*`として分けて測る）。

**切り出しの範囲（幾何の比。区間数の比は本番の計測で出す）**: 周回の要求の範囲（起点を中心に半径＝距離×0.4＋余白）と、
それを覆うz12タイルの外接矩形の面積比は、都心35.68,139.76で40・60・80kmとも1.31、35.70,139.70で1.52・1.31・1.10
（`M-measure_envelope.py`）。道の密度が一様なら、要求の範囲で切ると区間は1〜3割減る。東京湾側は道が無いので実際の比は
これより小さいとみる。

**見立て（本番の計測で確かめる前の仮説）**:
- コンパイル: デプロイで消えない置き場へ逃がせる見込みが高い——イメージの組み立てで1回呼んでキャッシュをイメージに焼く
  （組み立てたVMと同じCPUで動くので、numbaのキャッシュの条件に合う）か、`NUMBA_CACHE_DIR`を永続のボリューム
  （`/app/data`）へ向ける。どちらも初回のコンパイル分（本番で約4秒とみる）がキャッシュの読み込み（その1/10程度）に縮む見込み。
- 合成: `prepare`の1本の二重計算をやめると60・80kmで合成の2割が消える。残りは時刻に依らない部分をビンの外へ出せるかで、
  本番の内訳しだい。
- 切り出し: 面積では1〜3割の差。区間数の差が同じ程度なら、切り出し以降の全段（合成・構造・探索）が同じ割合で縮む。

**本番での計測の手順**（担当の scratchpad に用意済み。司令塔の scratchpad `M-measure_*`）:
`python M-measure_ssh.py M-measure_vm.sh M-measure_probe.py M-measure_app.tar.gz`（接続情報は`backend/.env.oracle.local`から
読み、表示しない）。VM上で、配信中と同じイメージ・上限6GBの使い捨てコンテナへ84896911の`backend/app`を読み取り専用で
差し込み、`/app/data`・ラスタも読み取り専用でマウントして、都心40・60・80kmを「起動直後の初回と2回目」で1コンテナずつ流す。
続けて`NUMBA_CACHE_DIR`を使い捨てのディレクトリにした40kmを2コンテナ（書く・読む）。段階別の所要（道路網の読み込み・切り出し・
スコア行列・天気・探索用グラフ・合成とその内訳・索引・構造・ターン・木・A*・評価・形の取り直し）、numbaのコンパイルの回数と時間、
外接矩形と要求の範囲で切った区間数、メモリを出す。計測中は15秒ごとに配信中のコンテナのCPU・メモリとVMの空きを出し、
空きが1GBを切ったら使い捨てコンテナを止める。最後に使い捨ての置き場を消す。

所要（並行実行）: 段階1（途中） 依頼 2026-09-26 21:03 → 21:24（21分）、うち枠待ち0分・CI待ち1分

## 本番VMでの計測の結果（2026-09-26 21:40、司令塔が上の手順を流した）

ユーザーの「再実行して」を受け、司令塔のセッションから`M-measure_ssh.py`で流した（ダッシュボードの操作の件
`T1161-prod-measure-run`）。配信中のコミットは84896911、差し込んだコードも84896911の`backend/app`。
都心35.68,139.76、仮定速度20km/h、風の時別予報あり。単位はミリ秒、括弧は呼ばれた回数。

| 要求 | 全体 | コンパイル | prepare | 合成 | 木（Dijkstra） | A*（復路） | 評価 |
|---|---|---|---|---|---|---|---|
| 40km 起動直後の初回 | 11,856 | 52回 | 2,457 | 1,185(2) | 4,678 | 2,477(9) | 882 |
| 40km 2回目 | 5,070 | 0 | 2,126 | 1,187(2) | 367 | 681(9) | 684 |
| 60km 起動直後の初回 | 21,398 | 52回 | 6,004 | 5,738(5) | 5,091 | 3,448(10) | 1,299 |
| 60km 2回目 | 14,209 | 0 | 5,073 | 5,848(5) | 796 | 1,726(10) | 921 |
| 80km 起動直後の初回 | 24,407 | 52回 | 7,097 | 7,947(5) | 5,352 | 3,780(6) | 855 |
| 80km 2回目 | 18,038 | 0 | 6,844 | 8,254(5) | 1,122 | 2,108(6) | 373 |
| 40km `NUMBA_CACHE_DIR`を書く（別コンテナ） | 11,230 | 52回 | 2,077 | 1,148(2) | 4,573 | 2,439(9) | 804 |
| 40km 同じキャッシュから読む（別コンテナ） | 5,314 | 0（hits=1） | 2,063 | 1,136(2) | 602 | 669(9) | 791 |

- **コンパイル**: 初回と2回目の差（40km 6.8秒・60km 7.2秒・80km 6.4秒）はコンパイル（木で約4.3秒、A*で約1.8秒）。
  キャッシュを別のプロセスから読むと、初回は2回目と0.2秒差まで縮む（5,314対5,070）。見立て（約4秒）より大きかった。
- **合成**: 60・80kmは合成が5回で、見立てどおり`prepare`の1本（ビン1本、時刻0）と往路の先頭のビン（時刻0）が同じ中身。
  1回の合成の内訳（60km 2回目、37回呼ばれた部品の合計）は走行時間2,976・軸の合成1,005・動的軸748・風の成分347で、
  時刻に依らない和（`c_fixed_sums`）は465を1回だけ。走行モデルが最大。
- **切り出しの範囲**: 外接矩形で切った区間数÷要求の範囲で切った区間数は、40km 973,862/768,473=1.27、
  60km 1,933,913/1,540,944=1.26、80km 2,727,110/2,440,538=1.12（面積比の見立て1.31より小さい）。
- メモリの最大（VmHWM）は40km 2.4GB・60km 3.5GB・80km 4.5GB。計測中、配信中のコンテナは0.1% CPU・123MBで変わらず、
  VMの空きは最小5.5GB。使い捨ての置き場は消えている。

## 段階2: 直す（2026-09-26、担当F-fast、origin/master 552de602）

- **① デプロイ直後の初回のコンパイルを消す**: イメージの組み立て（`backend/Dockerfile`）で`compile_search_kernels`を
  呼び、JITのコンパイル結果（`app/domain/__pycache__`）をイメージに焼く。`NUMBA_CACHE_DIR`を永続の置き場（`/app/data`）へ
  向ける案は採らなかった——`routing.py`を変えたデプロイでは結局コンパイルを払い、ディスクの古い版は誰も消さない
  （キャッシュ方針の「世代番号には削除の導線」と同じ問題）。焼く案は、VMの作業・ディレクトリの新設が要らない。
  numbaのキャッシュが読まれる条件は公式のソース（`numba/core/caching.py`）で確かめた: 索引の鍵がCPUの型
  （`codegen.magic_tuple()`＝LLVMのtriple・CPU名・機能）と関数のバイトコードのハッシュを含み、索引の印が元のファイルの
  `st_mtime`・`st_size`。組み立てと実行が同じVM・同じイメージのファイルなので一致する（組み立てを別の機械へ移すと黙って
  効かなくなることを`docs/architecture/tech-stack.md`へ書いた）。
  焼いたものが本番の呼び出しに当たるには型（dtype・並び・読み取り専用か）が同じである必要があるため、探索の入口が
  JITへ渡す配列を`_kernel_array`で揃える。本番の呼び出しは段階1の計測で関数ごとに1つの型（`signatures=1`）だったが、
  揃えておけば呼び出し側の変更で黙って型が分かれることが無い。
- **② 60・80kmの合成の二重計算をやめる**: ビンを刻むレグの各ビンは、同じ開始時刻のビン1本のレグが合成済みなら
  それを使う（`_LegCostComposer.compose`、鍵は`_timed_leg_key`に1本化）。周回・目的地ルートの往路の先頭のビンが該当する。
  使い回すのは既にキャッシュに残っている1本なので、メモリは増えない。ログ`compose_leg_costs`に`reused_bins`を足した。
  逆向き（刻んだレグの途中のビンをビン1本のレグへ回す）は、代表以外のビンの表示用配列を持ち続けることになるため採らない。
- **③ 切り出しを要求の範囲にするか**: 挙動（探索範囲の端の迂回路・学習した迂回率が表す範囲）が変わる選択のため直さず、
  上の区間数の比を添えてユーザーの判断を待つ（ダッシュボード）。

**開発機での確かめ**（開発機の値どうしで比べる。本番の値の目安にはならない）:
- `F-fast_jit_check.py`（scratchpad）: 作業ツリーの`backend/app`を使い捨ての置き場へ写し、別プロセスで
  `compile_search_kernels()`（Dockerfileと同じ）を呼んでから、新しいプロセスで14,400ノード・57,120状態の格子を
  本番と同じ組み立て（`build_lazy_road_graph`→statics→ターン構造、コストは2ビン）で探索する。
  焼かない: 最初の木8,547ms・A*2,938ms・コンパイル50回。焼く: 組み立て19.9秒、最初の木1,688ms・A*125ms・コンパイル0回・
  キャッシュのヒット2（木・A*）。2回目以降は同じプロセスで木15〜47ms。
- `F-fast_break_check.py`（scratchpad）: `_kernel_array`をただの`np.asarray`へ差し替えて型のテストを流すと、
  Dijkstraが5本・A*が4本コンパイルされテストが落ちる（揃える部品を外すと検知する）。
- 合成: `tests/test_road_graph_engine.py`で、ビン1本の合成の後に同じ時刻から2ビンのレグを合成すると、
  合成は時刻1のビンの1回だけ増え、先頭のビンの値がビン1本のレグと同じ。

**本番での確かめの手順**（このコミットが本番へデプロイされた後に、司令塔が流す。scratchpad `F-fast_*`）:
`python F-fast_ssh.py F-fast_vm.sh M-measure_probe.py`。配信中と同じイメージの使い捨てコンテナ（コードを差し込まない
——イメージに焼いたキャッシュを使うため）で、40km（初回と2回目）・60km初回・80km初回を1コンテナずつ流す。
見るのは、各コンテナの1本目で`numba_compiles=0`・`hits=1`（段階1は初回11.9秒・52回）と、60・80kmで合成が4回・
`compose_leg_costs leg=outbound … bins=2 reused_bins=1`（段階1は5回）。見込み（未計測）: 40kmの初回は約5.3秒、
60・80kmの合成は1回ぶん（60kmで約1.1秒・80kmで約1.5秒、段階1の2ビンの合成の半分）短くなる。

所要（並行実行）: 段階2 依頼 2026-09-26 21:32 → 21:55（23分）、うち枠待ち3分・CI待ち不明、実績 +120/−16

## 段階2の本番での確かめ（2026-09-26 22:23、司令塔が上の手順を流した。配信中 274abd10）

起動直後の初回の所要（都心発の周回、使い捨てコンテナ1本につき1件目）: 40km 11.9→5.3秒・60km 21.4→12.6秒・80km 24.4→17.0秒。
40kmの2回目は4.7秒で、初回との差はほぼ消えた。探索の2本（木・A*）は各コンテナの1本目から`hits=1 misses=0`（イメージに焼いた
キャッシュを読んだ）。60・80kmの合成は4回になり、`compose_leg_costs leg=outbound … bins=2 reused_bins=1`が出た。

**見込みとのずれ**: `numba_compiles`は0の見込みが、各コンテナの1本目で2回（約100ms）残った。探索の2本ではない別の小さな関数で、
特定していない。80kmの合成の短縮は1.5秒の見込みが約1.0秒（60kmは1.1秒の見込みに約1.2秒）。

## 段階3: 切り出しを要求の範囲にする（2026-09-26、司令塔がこのセッションで実装）

**問いと答え（ダッシュボード T1161-slice-by-request、2026-09-26 21:57）**: 探索で読み込む範囲を「頼まれた範囲」（bbox）へ
狭めるか。区間数の比（z12タイルの外接矩形／bbox）は40km 1.27・60km 1.26・80km 1.12。
**答え: (1) 頼まれた範囲そのもので切る。** 学習した迂回率の鍵は今と同じタイル集合のまま。
担当F-fastの編集は実行環境の自動判定に「共有資源の変更」として拒否されたため止め、ユーザーがチャットで
「このセッションに入れていい」と許可して司令塔が入れた。

**直したもの**: `GraphService.get_search_slice`はbboxそのものを`slice_network`へ渡す。bboxを覆うz12タイルの集合は
学習した迂回率の鍵としてだけ返す。参照0になった`_tiles_envelope`と`tile_bounds_lonlat`のimportを消した。
routing-engine.md「2. 切り出し」と`test_region.py`の案内を今の挙動へ直した。

**検証**:
- `tests/test_graph_service.py`: 同じz12タイル（138.955〜139.043E）の中でも、経度138.99〜139.005の狭いbboxでは道20（139.01〜139.02）を
  取らず行[0,1]になり、タイル集合はz12で返る（外接矩形で切っていた間は行[0,1,2]になる）。
- 本番VMの使い捨てコンテナ（配信中のイメージに、この変更の`graph_service.py`だけを重ねた。scratchpad `C3_*`）で、起動直後の初回:
  40km 5.3→4.8秒（2回目 4.7→4.3秒）・60km 12.6→11.1秒・80km 17.0→16.3秒。候補数は40・60km 8件・80km 3件で段階2と同じ。
  区間数の減り（約2割・2割・1割）ほどは縮まず、所要の短縮は約1割・1割・4%。

## 残り

- 合成の時刻に依らない部分をビンの外へ出す（段階1の見立ての「残り」）。80kmの初回16.3秒のうち合成が6.2秒で、最大の段のまま。
  進めるかはユーザーの判断待ち（ダッシュボード T1161-compose-hoist）。
- 起動直後に残るnumbaのコンパイル2回（約100ms）の特定。

所要（並行実行）: 段階3 司令塔 2026-09-26 22:31 → 22:47（16分）、うち本番VMでの計測3分・枠待ち0分、実績 実装 +4/−16・テスト +7/−5

## 段階4: 時刻ごとの合成のうち、ビンに依らない計算を数える（2026-09-26、担当F-fast2、origin/master 40382341）

**問いと答え（ダッシュボード T1161-compose-hoist、2026-09-26 22:44）**: ルート生成の「時刻ごとのコストの合成」をさらに削る作業を
続けるか、T1161をここで閉じるか。80kmの初回16.3秒のうち合成が6.2秒で最大の段、時刻に依らない部分もビンごとに計算している
可能性があるが割合は未計測。選択肢は「続ける（まず割合を調べる、S）」「閉じる」。
**答え: (1) 続ける**——合成のうち時刻に依らない部分を、時刻ごとに計算し直さないようにできるかを先に調べる。
この段は調べるだけで、コードは変えていない。

**母集団**: `_LegCostComposer._compose_at`を1回（＝ビン1本）呼んだときに走る計算の全部（`_evaluate`の中の
`evaluate_dynamic_axis_arrays`・`wind_components`・`_travel_time_seconds`・`compose_costs_from_axis_matrix`と、
`_compose_at`の後段）。リクエストに1回だけの`_fixed_axis_sums`（本番の計測の`c_fixed_sums`）はビンの外で既に1回なので
母集団に入れず別に数える。`values_at_rows`（経路上の数百行を合成し直す）は所要が小さく外す。

**読み取りでの分類**（ビンごとに変わる入力は、そのビンの時刻の風〔格子点ごとの風速・風向〕だけ。方位・距離・勾配・材料・重み・
0次フィルタ・速度はリクエストの中で変わらない）:
- 依らない: 勾配の下ごしらえ・路面の転がり抵抗・走行モデルの反復の前の出力と一定の抵抗（`speed_ms`の反復の外）・停止の待ち
  （停止要因の材料×距離）・0次フィルタの除外。さらに**時刻で変わる公開軸の重みがすべて0なら**、合成の難易度・範囲の平均・
  割増の倍率も依らない（時刻で変わる軸が合成へ入らないため）。
- 依る: 風の引き当て・風の追加負荷・動的軸の評価・風の成分・走行モデルの反復（向かい風で速度が変わる）・所要×倍率・並べ替え。
- 依るが同じビンの中で2回走る: 風の引き当て（`DynamicAxisRequestContext.wind_inputs`が`evaluate_dynamic_axis_arrays`の中と
  `_evaluate`の中で1回ずつ呼ばれる）と、風向と方位の差の角度・三角関数（`wind_drag_ratio_array`と`wind_components`が
  それぞれ求める）。
- 代表以外のビンは表示用の配列（公開軸・材料・難易度）を捨てる（`compose`が`cost_lazy`と`travel_seconds_lazy`だけを
  `vstack`する）。時刻で変わる軸の重みが0なら、そのビンの動的軸と風の追加負荷は誰も読まない。代表以外のビンは
  80kmで4回のうち1回、60kmで4回のうち2回、40kmで0回（代表の添字`_representative_bin`と見込み時間から）。

**手元の計測**（scratchpad `F-fast2_compose_split.py`、出力 `F-fast2_compose_split_80km.txt`。`heavy`の枠の中で3回流した）:
開発機には道路網の置き場が無いため、区間の配列は本番80kmの区間数（要求の範囲で切った2,440,538）の合成データで作り、
軸の定義・既定の重み・調整値は開発DBから読んだ（公開軸7本、時刻で変わる公開軸は`wind`の1本で既定の重み0、重みが0でない軸は2本）。
所要は配列の長さと軸・材料の本数で決まり、値には依らない。風の格子は289点・48時刻。各項目は5回の中央値。
開発機は並行する担当の処理で負荷があり、3回の間で同じ項目が±15%動いた。以下は3回の幅。

| 何を | 1ビンの所要（開発機） | 今との差 |
|---|---|---|
| 今の`_compose_at` | 2,474〜2,853ms | — |
| 段に割ると: 依らない／依る／同じビンで2回目 | 47〜49%／48〜51%／2〜4% | — |
| C1: 依らないもの（走行モデルの反復の前・停止の待ち・除外・方位の三角関数）をリクエストに1回、引き当てを1回に | 1,880〜2,143ms | −19〜−31% |
| C4: C1＋時刻で変わる軸の重みがすべて0なら割増の倍率もリクエストに1回。風の式は今と同じ（三角関数の置き換えなし） | 1,164〜1,449ms | −41〜−59% |
| C3: 上＋代表以外のビンで動的軸と風の追加負荷を求めない | 887〜1,048ms | −58〜−69% |

- リクエストに1回払うぶん（開発機）: 依らないものの前処理 648〜821ms（1ビンの0.24〜0.33）、割増の倍率 638〜750ms（0.23〜0.30）。
- 出力: C4は今と`cost_lazy`・`travel_seconds_lazy`が全区間で完全に一致した（`np.array_equal`）。C1は方位の三角関数を
  `cos(風向−方位)=cosθcosb+sinθsinb`へ置き換える形で、1回目・2回目の計測では試作の側の誤り（横風の二乗をfloat64で取って
  から縮めていた）で5区間が最大0.04秒ずれ、直した3回目は全区間一致した。置き換えは式が違うので、一致は保証されない。
- 1ビンの中で一番重いのは走行モデルの反復（約770〜1,010ms、風に依る）。反復の外の出力・一定の抵抗、停止の待ち（約460ms）、
  合成の難易度（約620ms）が依らない側の大半。

**本番の見込み**（未計測。本番の段階3の後の値に、開発機の比を当てた）: 1ビンの所要は（合成の計−固定の和）÷合成の回数で、
80km (6,177−732)÷4≈1,361ms・60km (3,743−291)÷4≈863ms・40km (920−142)÷2≈389ms。縮む時間＝回数×1ビン×差の比−1回払うぶん。
- C4（既定の重みのように、時刻で変わる軸の重みが0のリクエスト）: 80km 1.4〜2.6秒（初回16.3秒→約13.7〜14.9秒、合成6.2→約3.6〜4.8秒）・
  60km 0.9〜1.6秒（11.1→約9.5〜10.2秒）・40km 0.1〜0.3秒。
- 風の軸に重みを付けたリクエスト（C1、三角関数の置き換えを含む形で測った。置き換えない形は未計測）: 80km 0.6〜1.4秒・60km 0.4〜0.9秒・40km 0.1秒未満。
- C3の上乗せは、代表以外のビン1本につき1ビンの約1割（80km 約0.1秒・60km 約0.2秒）。
- 本番の軸の定義（時刻で変わる軸の既定の重みが0か）は確かめていない。直す側は重みを見て分岐するので、本番の定義が違っても
  結果は変わらず、縮み方だけが変わる。

**直す段の中身の見込み**（規模S。変わると見込むファイル: `road_graph_engine.py`・`cycling_speed.py`（`speed_ms`を、反復の外の
出力・一定の抵抗を受け取れる形へ分ける）・`dynamic_materials.py`（風の引き当てと角度を1回にする）・`test_road_graph_engine.py`・
`test_cycling_speed.py`、文書は routing-engine.md。実装＋テストで約150行の見込み）。
進め方はダッシュボード T1161-hoist-fix で問う。

**直す段で一緒に直すもの**（このモジュールの記述と実装の乖離。この段はコードとモジュールの文書に手を入れないので直していない）:
- `_LegCostComposer._fixed_axis_sums`のdocstring「レグが1本のビンに収まるリクエストでは一度も要らない」は今の実装と違う——
  `_evaluate`が毎回読むため、どのリクエストでも1回は求める（本番の計測でも40kmで1回呼ばれている）。
- routing-engine.md「レグ別コスト配列」の「`compose`がレグごとに風の列だけを引き直して合成する（`estimate_passage_hours`…）」は
  今の実装と違う——`compose`はビンを刻むレグではビンの開始時刻を全区間に置いて合成し（目的地から遡る木だけは区間ごとの
  通過時刻）、`estimate_passage_hours`はその遡る木で前向き木が届かない区間の予備の時刻にだけ使う。同じ節の「`lens_axis_id`が風に依存する公開軸なら重み0でもレグごとに合成する
  （`_LegCostComposer`の`lens_axis_id`）」も、今は風の時別系列があれば常に時刻で合成し、`lens_axis_id`は表示する材料の集合
  （`displayed_material_ids`）にだけ効く。

**範囲外で見つけたもの**: リクエストに1回の`_fixed_axis_sums`が本番80kmで732ms（開発機で1,237〜1,605ms）かかる。ビンの数に
依らないためこの段の母集団の外。何が時間を使っているかは測っていない。直す段で割増の倍率を1回にするとき、同じ入力（時刻で
変わらない軸）から求めるので、一緒に見る。

## 残り（段階4の後）

- 合成の時刻に依らない部分をリクエストに1回にする直し（上の見込み）。進めるかはダッシュボード T1161-hoist-fix の答え待ち。
- 起動直後に残るnumbaのコンパイル2回（約100ms）の特定。

所要（並行実行）: 段階4 依頼 2026-09-26 23:03 → 23:23（20分）、うち枠待ち0分・CI待ち0分、実績 実装 0/0・テスト 0/0（手元の計測3回で枠を3.4分保持）

## 段階5: 時刻に依らない計算をリクエストに1回にする（2026-09-26、担当F-fast3、origin/master c44d6022）

**問いと答え（ダッシュボード T1161-hoist-fix、2026-09-26 23:25）**: ルート生成の「時刻ごとのコストの合成」から、時刻に依らない
計算を1回にまとめる直しを進めるか。選択肢は「直す（規模S見込み。出てくるルートは今と同じで、80kmの初回が約1.4〜2.6秒・60kmが
約0.9〜1.6秒短くなる見込み）」「閉じる」。見込みは時刻で変わる軸（風）の既定の重みが0であることを前提にし、司令塔が本番の
`/api/axis-catalog`で風の既定の重みが0.0であることを確かめた。**答え: (1) 直す。**

**直したもの**（段階4の試作C4の形。風の式の三角関数の置き換えはしない）:
- 走行モデル（`cycling_speed.py`）を`SegmentSpeedModel`へ分けた。出力・速度に依らない抵抗・挟み込みの初期区間は作るときに1回だけ求め、
  風の成分を渡すたびに速度を解く。関数の`speed_ms`・`travel_seconds`は本番から呼ばれなくなるので消し、テストはモデルを通す。
- 合成器（`_LegCostComposer`）は、走行モデルと停止の待ちをリクエストに1回だけ作る（`_travel_inputs`）。停止の待ち・路面は静的材料
  だけから読む（動的材料は`wind_drag_ratio`だけで、停止・路面の材料と重ならない）。
- 時刻で変わる公開軸の重みがすべて0なら（`_composition_is_time_invariant`）、合成の難易度・割増の倍率を時刻で変わらない軸だけで1回
  求め（`_time_invariant_composition`。下地を1にして合成し、`cost`を倍率として使う）、ビンのコストは所要時間×倍率にする。重みが
  0の軸は合成に何も足さない（得点は区分線形・カテゴリの有限値かNaNで、0を掛けて項が0になる）ので、値は今と同じ。重みがあれば
  今と同じくビンごとに時刻で変わる軸を足す。このとき、時刻で変わらない軸の重み付き和（`_fixed_axis_sums`）は全区間ぶん1回だけ。
  経路上の行の合成し直し（`values_at_rows`）は、その行だけで和を求める（全区間の和を強いない）。
- 1ビンの中で2回だった予報の引き当てと風の分解（方位との差の三角関数）を1回にした: `DynamicAxisRequestContext.wind_components_ms`
  （contextごとに1回）を風の材料と走行モデルが読む。風の材料は`wind_drag_ratio_from_components`で成分から求める
  （`wind_drag_ratio_array`も同じ関数を通す）。`wind_inputs()`は読み手がこのプロパティだけになったので中へ畳んだ。
- 段階4で見つけた乖離: `_fixed_axis_sums`のdocstring、routing-engine.md「レグ別コスト配列」節の2か所（`estimate_passage_hours`で
  風の列を引き直す記述・`lens_axis_id`で重み0でも合成する記述）、`RoadGraphEngine.__init__`の`lens_axis_id`のコメント（同じ誤り）を
  今の挙動へ直した。evaluation-scoring.mdの`DynamicAxisRequestContext`の項で、同じ段落にあった乖離（中点座標を「タイル単位で
  キャッシュ」・並行Edgeを「edge_idの昇順」「タイル集合キーでキャッシュ」）も直した。

**検証**:
- 期待値のテスト（`test_road_graph_engine.py`、走行モデルと動的軸の評価だけを代役にし、軸の合成と風の分解は本物）: 時刻ごとに風が
  強まる2区間で、重み0ならビンの所要時間が128・248秒→129・249秒と風だけで変わり、コストは所要時間×（1＋0.5×得点/100）＝×1.2・×1.4。
  重みがあれば各ビンの合成にそのビンの時刻の値が入る（×1.1・×1.2→×1.1025・×1.2025）、経路上の行の合成し直しも同じ（40.5）。
  走行モデルは1つのモデルを続けて解いても前の風が残らない（`test_cycling_speed.py`）。
- わざと壊して検知すること（scratchpad `F_fast3_break.py`、テストの中で合成器を差し替える）: 最初のビンの所要時間を全ビンで使い回すと
  2件とも落ち、重みを見ずに倍率を1回にすると重みのある側の1件が落ちる。
- 今の版との一致（使い捨て、scratchpad `F-fast3_equiv.py`。テストには残さない）: origin/masterの`backend/app`と作業ツリーの版で、
  30万区間の合成データ（公開軸4本・時刻で変わる軸1本、停止の材料・路面・勾配・方位に欠損あり、0次フィルタの除外5%、探索用グラフに
  載らない区間1%）を、周回と同じ順（ビン1本→往路3.5時間→復路3.5時間→区間ごとの通過時刻の1本）で合成し、経路上の400行を合成し直した。
  風の重み0・重みあり・時別系列なしの3通りで、配列255本が全部`np.array_equal`で一致した。同じ実行の合成の所要（開発機、2回）:
  重み0 3.3・2.7秒→1.3・1.4秒、重みあり 3.3・3.5秒→2.6・2.5秒、時別系列なし 0.4秒で変わらず。
- `ruff check`（変えた6ファイル）・`mypy`（変えた実装4ファイル）指摘0、`review_checks.py docs`違反なし、
  `pytest tests/test_cycling_speed.py tests/test_road_graph_engine.py tests/test_wind.py tests/test_wind_way_service.py
  tests/test_route_generation_behavior.py tests/test_route_generator.py tests/test_weather_route.py tests/test_region_routes.py` 289 passed。

**メモリ（見積もり、未計測）**: 80km（区間2,440,538）で、既定（風の重み0）のリクエストは増えず、差し引き約60MB減る見込み。
リクエストの間持ち続けるものが、走行モデルの2列（float32×2＝8B/区間）・停止の待ち（8B）・倍率（8B）で+24B/区間増える一方、
代表ビンの難易度と重みの和（16B）をレグごとに持っていたもの（ビン1本・往路・復路の3本）が1本の共有になり（−32B）、時刻で変わらない
軸の重み付き和（16B）を求めなくなる（−16B）。風に重みを付けたリクエストだけ、走行モデルと停止の待ちの+16B/区間（約39MB）が増える。
T1112の1本あたり約1.0KB/区間に対して1.6%で、探索範囲の上限の導出は変えない。

**本番での確かめ**（司令塔が、段階3と同じ道具で流す）: 配信中のイメージへ、この段で変えた次のファイルを重ねる——
`backend/app/domain/cycling_speed.py`・`backend/app/domain/dynamic_materials.py`・`backend/app/domain/wind.py`・
`backend/app/services/road_graph_engine.py`。JITする`routing.py`は変えていないので、イメージに焼いたコンパイル結果はそのまま使える。
見るのは40・60・80kmの起動直後の初回の所要と合成（`compose_leg_costs`の`compose_ms`）、候補数が段階3と同じこと。

## 残り（段階5の後）

- 本番での確かめ（上の手順。司令塔）。
- 起動直後に残るnumbaのコンパイル2回（約100ms）の特定。

所要（並行実行）: 段階5 依頼 2026-09-26 23:29 → 23:58（29分）、うち枠待ち0分・CI待ち0分、実績 実装 +201/−153・テスト +79/−50（うち`speed_ms`の本体をメソッドへ移した字下げが約90行）。見込みの約150行を超えた——字下げのほか、テストの代役を
軸の合成の本物へ切り替えた分と、`DynamicAxisRequestContext`の風の成分の共有

## 段階5の本番での確かめ（2026-09-27 0:1x、司令塔）

配信中 79624328 のイメージに、この段で変えた4ファイル（cycling_speed.py・dynamic_materials.py・wind.py・road_graph_engine.py）を
重ねた使い捨てコンテナで、起動直後の初回を測った（scratchpad → 司令塔の置き場 `.git/orchestration/coordinator/` の C6_vm.sh・C3_probe.py）。
計測の道具は、T1129 で移した名前（ROAD_GRAPH_TILE_ZOOM）の import と、この段で無くなった計測点（wind_components）で2回落ち、道具の側を直した。

| 距離 | 段階4の後（段階3の実測） | 段階5 | 見込み |
|---|---|---|---|
| 40km 初回 | 4.8秒（2回目4.3） | 4.4秒（2回目3.9） | ほぼ変わらない |
| 60km 初回 | 11.1秒 | 9.4秒（合成 2.3秒） | 9.5〜10.2秒 |
| 80km 初回 | 16.3秒（合成 6.2秒） | 13.1秒（合成 4.0秒） | 13.7〜14.9秒 |

候補数は段階3と同じ（8・8・3件）。1回目の計測では40kmの初回だけ準備の段が8.1秒（内訳の合計は約2.3秒で、約5.8秒がどの計測点にも
計上されない）だったが、流し直した2回目は1.6秒で再現しなかった（原因は特定していない。1回目の結果は C6_vm_result_run1.txt）。

## 段階6: 起動直後に残るnumbaのコンパイル2回を特定して消す（2026-09-27、担当F-fast4、origin/master febd6c85）

**問いと答え（ダッシュボード T1161-close、2026-09-27 0:21）**: ルート生成の高速化（T1161）をここで閉じるか。ここまでで起動直後の初回は
40km 11.9→4.4秒・60km 21.4→9.4秒・80km 24.4→13.1秒。残りは各コンテナの最初のリクエストで走る、探索とは別の小さな関数のコンパイル2回
（計約0.1秒、所要の約2%）。選択肢は「閉じる（推奨）」「続ける: 関数を特定し、イメージに焼けるなら焼く（調べる段は規模S）」。
**答え: (2) 続ける。**

**特定**（scratchpad `F-fast4_jit_probe.py`）: 作業ツリーで`compile_search_kernels()`（Dockerfileと同じ）を別プロセスで呼んだ後、
新しいプロセスで`numba.core.event`の`numba:compile`に関数名と引数の型を出す聞き手を付け、公開の入口から生成を流すテスト
（`tests/test_route_generation_behavior.py`）を走らせた。コンパイルは2回で、どちらも**区間の表示がPythonから呼ぶ`time_bin_of`**だった:
`app.domain.routing.time_bin_of (float64, float64, int64)`（234ms）と、その中の`int()`の実装
`numba.cpython.builtins.ol_int.<locals>.impl (float64,)`（63ms、前者の中で入れ子に走る）。`time_bin_of`は探索の中へ展開する
`@njit(inline="always")`の部品で、探索（焼いた2本）の中ではコンパイル済みの本体に展開済みのため読まれないが、
`RoadGraphEngine._route_passages`が経路をたどるときにPythonから直接呼ぶ。Pythonからの呼び出しは`compile_search_kernels`が
通らないので焼かれず、各コンテナの最初の生成でコンパイルされていた。

**直したもの**: 焼く（`compile_search_kernels`でPythonから1回呼ぶ）のではなく、Pythonから呼ぶ側をJITしない形にした。
`time_bin_of`を素の関数にし、探索の中では同じ本体をJITした`_time_bin_of_kernel`を展開する。焼く案を採らなかった理由:
スカラー1つの計算をPythonから呼ぶのにJITの利点は無く（1回の呼び出しは素の関数197ns・コンパイル済みのJIT 232ns、
開発機、`F-fast4_call_cost.py`）、焼くとPython側の引数の型（`bin_seconds`がPythonのfloatであること等）が焼いたものと
揃い続けることに頼ることになる。表示と探索が同じ本体を読むことは変わらない。
`compile_search_kernels`のdocstringと routing-engine.md「JITのコンパイル」に「生成の経路がPythonから呼ぶJITは探索の2本だけにする」を書いた。
`routing.py`を変えたので、焼いたキャッシュの前提（ファイルの更新時刻・大きさ）が変わる。デプロイは`backend/**`の変更でイメージを
組み立て直し（`scripts/deploy_backend_gate.py`の`DEPLOY_PATHS`→`deploy-backend.yml`の`docker build`）、Dockerfileは`COPY app`の
直後の段で`compile_search_kernels()`を走らせるため、`app`のどのファイルが変わってもその段が走り直して焼き直される（読んで確かめた）。

**検証**:
- 直した後、同じ手順で`numba:compile`は0回（`F-fast4 total=0`、生成の振る舞いのテスト27件）。直す前は上の2回。
- 見張るテスト（`test_routing.py: test_route_generation_calls_from_python_only_the_jit_that_the_image_bakes`）: `app`の全モジュールの
  JITの関数（`CPUDispatcher`）をPythonからの呼び出しを記録する包みへ差し替え、`compile_search_kernels()`が呼んだ集合と、周回・目的地
  ルートの生成（風の時別系列あり）が呼んだ集合を比べ、後者が前者に含まれることを見る。母集団はモジュールから導き、関数名を手で
  挙げない。包みは呼び出しの順序に依らず働く（先に別のテストが同じ関数をコンパイルしていても、Pythonからの呼び出しは包みを通る）。
  わざと壊す（scratchpad `F_fast4_break.py`、pytestのプラグインで`road_graph_engine.time_bin_of`をJITの版へ戻す）と、
  `{'_turn_expanded_astar', '_turn_expanded_dijkstra', 'time_bin_of'} <= {…2本}`で落ちる。
- `ruff check`（変えた3ファイル）・`mypy app/domain/routing.py` 指摘0、`review_checks.py docs`違反なし、
  `pytest tests/test_routing.py tests/test_road_graph_engine.py tests/test_route_generation_behavior.py tests/test_routes_generate.py` 186 passed。

**本番での確かめ**（このコミットが本番へデプロイされた後に、司令塔が段階2と同じ道具で流す。コードを重ねず、配信中のイメージそのままの
使い捨てコンテナで）: 各コンテナの1本目で`numba_compiles=0`。関数名を出すには、計測の道具（`C3_probe.py`）の`_install_compile_listener`の
`on_end`で`ev.data["dispatcher"].py_func`の`__module__`・`__qualname__`と`ev.data["args"]`を出す（numba 0.67の`dispatcher.py`が
`numba:compile`へ渡す`data`はこの2つと`return_type`）。

## 残り（段階6の後）

- 本番での確かめ（上の手順。司令塔）。`numba_compiles=0`なら、このタスクに残る作業は無い。

所要（並行実行）: 段階6 依頼 2026-09-27 00:28 → 完了 00:45（17分）、うち枠待ち0分・CI待ち不明、実績 実装 +12/−5・テスト +58/−1
