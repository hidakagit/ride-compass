# T1093. 道路タイルに焼く真偽の材料（照明・トンネル・自転車レーン等）の条件を、材料の宣言から組み立てる

状態: 完了（2026-09-24）

## 背景

背景（T965 段階Bの点検で見つけた崩れ。T955・T956の主張「材料の導出はSQL1本へ」に反する）: 真偽の材料9件（照明あり・トンネル・橋・自動車通行不可・自転車道・自転車レーン等3種・歩行者と共用の道）の「どのタグの値なら真か」を、材料カタログの value_sql（ルートの評価が読む）と、道路タイルのSQL（road_graph_repository.py の _ROAD_SURFACE_TILE_MVT_SQL、地図が塗る）が別々に書いている。例: 歩行者共用の道の bicycle が ('yes','designated') という条件が両方にある。

起きうること: 片方だけ条件を直すと、地図の色とルートの評価が同じ道で食い違う（地図では自転車レーンありなのに評価ではなし、等）。材料を1つ足すときも2か所を触る。
起きない場面: 今は条件が一致している（点検時に照合）。

直し方: タイル側を CASE WHEN (value_sql) THEN true END の形で材料カタログから組み立てる。値は変わらない見込み（COALESCE(x,false) が真になるのは x が真のときだけ）。ただしタイルのSQLの文字列が変わるので、形の署名（ROAD_SURFACE_TILE_SHAPE）が変わり、デプロイ後に路面タイルが全部作り直しになる（最初の表示が一時的に遅くなる）。生成物 region-tile-config.json の再生成も同じコミットで要る。

迷い: T956の記録は「材料の導出はタイルの符号化と分ける」と書いている。分けたのは真偽をNULLで持つかfalseで持つかの符号化で、条件式まで二重にする意図ではないと読んだ。

なぜ既存のタスクに入らないか: T955・T956は完了済み。T965（点検）の中で直すには、タイルの作り直しという利用者から見える副作用があり、規模Sの整理の範囲を超える。

（ダッシュボードの起票案 kihyoan-tile-bool-from-catalog を承認（2026-09-24）。起票案の全文は次のとおり）

規模S（変わると見込むファイル: infrastructure/road_graph_repository.py・domain/material_catalog.py と、その生成物 region-tile-config.json・テスト1〜2本）。本番DBへは書かないが、タイルの作り直しを伴う（下）。

背景（T965 段階Bの点検で見つけた崩れ。T955・T956の主張「材料の導出はSQL1本へ」に反する）: 真偽の材料9件（照明あり・トンネル・橋・自動車通行不可・自転車道・自転車レーン等3種・歩行者と共用の道）の「どのタグの値なら真か」を、材料カタログの value_sql（ルートの評価が読む）と、道路タイルのSQL（road_graph_repository.py の _ROAD_SURFACE_TILE_MVT_SQL、地図が塗る）が別々に書いている。例: 歩行者共用の道の bicycle が ('yes','designated') という条件が両方にある。

起きうること: 片方だけ条件を直すと、地図の色とルートの評価が同じ道で食い違う（地図では自転車レーンありなのに評価ではなし、等）。材料を1つ足すときも2か所を触る。
起きない場面: 今は条件が一致している（点検時に照合）。

直し方: タイル側を CASE WHEN (value_sql) THEN true END の形で材料カタログから組み立てる。値は変わらない見込み（COALESCE(x,false) が真になるのは x が真のときだけ）。ただしタイルのSQLの文字列が変わるので、形の署名（ROAD_SURFACE_TILE_SHAPE）が変わり、デプロイ後に路面タイルが全部作り直しになる（最初の表示が一時的に遅くなる）。生成物 region-tile-config.json の再生成も同じコミットで要る。

迷い: T956の記録は「材料の導出はタイルの符号化と分ける」と書いている。分けたのは真偽をNULLで持つかfalseで持つかの符号化で、条件式まで二重にする意図ではないと読んだ。

なぜ既存のタスクに入らないか: T955・T956は完了済み。T965（点検）の中で直すには、タイルの作り直しという利用者から見える副作用があり、規模Sの整理の範囲を超える。

## 実施（2026-09-24、担当 W-tile）

- 母集団: 材料カタログのうち`material_array_group(spec) == "boolean"`（真偽で、欠損を非該当として持つ）かつ
  `tile_property`と`value_sql`を持つもの。数えると起票時の9件と一致した（`oneway`は`value_sql`が無く、
  `surface_good`は欠損を不明として持つため外れる。どちらもタイル側の式は従来どおり）。
- `road_graph_repository.py`に`_BOOLEAN_TILE_COLUMNS_SQL`を置き、上の母集団から
  `CASE WHEN (value_sql) THEN true END AS <tile_property>`を組み立てて`_ROAD_SURFACE_TILE_MVT_SQL`へ差し込んだ。
  手書きの9列と、それだけが使っていたimport（`TUNNEL_NORMALIZED_SQL`等6件）を消した。
  `CYCLEWAY_TAGS_ARRAY_SQL`は`material_sql.py`の中でしか使われなくなったため非公開名にした。
- タイルのSQLは、どのテストも実DBへ通していなかった（契約テストは結果の行を差し替えている）。
  `test_material_values.py`の実テーブルへ通す検査に、路面タイルのSQLを空のDBへ通す1件を足した
  （タイルのFROM句に無い別名を読む値式が混ざると落ちる）。
- 文書: `docs/modules/backend/evaluation-scoring.md`の「タイルへ焼く式だけは符号化が違う」に、条件は同じ式から
  組み立てること・成り立つ条件（値式が`w`だけを読む）を足した。
- 迷いへの答え: T956の「材料の導出はタイルの符号化と分ける」は符号化（NULLかfalseか）の話と読み、条件式は
  値式を共有した。符号化は`CASE WHEN … THEN true END`のまま変わらない。

## 検証

- 旧新の真偽列を開発DBの道の全行（12,029行）へ当てた比較（`IS DISTINCT FROM`で数えた）: 9列すべて差0件。
  同じ行で真になる件数は bridge 331・cycleway_has_lane 151・cycleway_has_shared 786・cycleway_has_track 3・
  highway_is_cycleway 134・lit 2,909・motor_vehicle_no 21・shared_pedestrian_path 578・tunnel 96
  （どの列も真が1件以上あり、NULL同士の一致だけで0件になったのではない）。
- 旧（origin/masterの`_ROAD_SURFACE_TILE_MVT_SQL`）と新のSQLで、開発DBの同じタイルを焼いて
  `mapbox_vector_tile`で解き、フィーチャーごとのプロパティを突き合わせた: z12/3637/1612（4,757件）・
  z14/14551/6451（852件）・z15/29102/12902（238件）のすべてで差0件。
- 形の署名`ROAD_SURFACE_TILE_SHAPE`: `c2b77731e123` → `11869beea9d5`（POIは`e8e91a55d1f5`のまま）。
  旧い署名の文字列はリポジトリのどこにも書かれていない。デプロイ後、路面タイルは作り直しになる。
- `backend/scripts/export_openapi.py`で生成物を再生成: 差分なし（`region-tile-config.json`は形の署名を持たない）。
- 足した検査が効くこと: 生成する列へわざと` AND re.distance_m > 0`を足すと、新しい1件が
  `テーブル"re"用のFROM句エントリがありません`で落ちた（戻して通ることを確認）。
- `ruff check`（変更4ファイル）: 指摘なし。`mypy`: 195ファイルで指摘なし。`review_checks.py docs`: 違反なし。
- `pytest tests/test_material_values.py tests/test_material_coverage.py tests/test_cache_identity.py
  tests/test_road_graph_repository_contracts.py tests/test_material_catalog.py`: 136 passed。
- e2e-liveは回していない。タイルへ焼く値が変わらないことを、上のとおり本物のSQLと開発DBでフィーチャー単位に確かめたため。

所要（並行実行）: 依頼 2026-09-24 10:38 → 完了 10:57（19分）、うち枠待ち0分・CI待ち0分、規模札S、実績 +28/−22 → S
