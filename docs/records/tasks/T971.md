# T971. ソース内へベタ宣言した「外部が決めた対応表」を振り分ける 規模M

状態: 調査済み・未着手（2026-09-20起票）。[T970](T970.md)の検討中に洗い出したものを
分離した。

## 背景

`backend/app/**.py` のモジュール直下に、大文字名でデータの表が **83件** ある
（母集団: 値が dict/list/tuple/set のリテラルで要素2個以上。分布は domain 54 /
infrastructure 15 / batch 7 / api 5 / services 2）。

このうち上位30件を見た範囲で3つに分かれた。

- **(a) 自分の判断**（式・しきい値・分類の規則）→ コードで正しい。触らない
- **(b) 外部が決めた対応表を写経したもの** → [T970](T970.md)決定3（値はDB／式はコード）に
  この箱が無い
- **(c) βの範囲に属するもの** → 取込プロファイルへ

残り53件は未分類。本タスクで分類しきる。

## 母集団の出し方（再現手順）

`backend/app/**.py` をASTで走査し、モジュール直下の代入のうち、代入先が大文字名で、
値が dict/list/tuple/set のリテラル（または`frozenset(...)`等のリテラル引数）で、
要素が2個以上のものを拾う。2026-09-20 時点で83件。

## 調査結果: 「取得できるか」は表ごとに違う（実測、2026-09-20）

配信元を実際に叩いて確かめた。**一律に「取得へ寄せる」は成立しない。**

| 表 | 件数 | 配信元 | 判定 |
|---|---|---|---|
| `KANTO_PREFECTURE_CODES`（NPA採番） | 7 | `.../opendata/koudohyou/2_koudohyou_todouhukenkoudo.csv` — **HTTP 200 / 1,288バイト / 56行** | **取得できる** |
| 当事者種別コード（`BICYCLE_PARTY_TYPE_CODES`の母集団） | — | `.../31_koudohyou_toujisyasyuetu.csv` — **HTTP 200 / 6,558バイト / 85行** | **取得できる** |
| `WARNING_CODE_NAMES` | 37 | `bosai/common/const/warningcode.json`・`bosai/forecast/const/warning.json` とも **404**。在処が不明 | **要調査**（最大の表） |
| `JMA_TILE_SPECS` | 8 | 配信元は `*.properties__<hash>.xml`。**ファイル名にハッシュが入り安定して取れない** | **取得は非現実的** |
| `LANDCOVER_CLASSES` | 8 | GeoTIFFのカラーマップ／Esriの公開仕様 | 要確認 |
| `FLOOD_CODE_LEVELS` | 9 | 未確認 | 要確認 |
| `_SIXTEEN_POINT_LABELS` | 16 | 16方位は世界共通の定義 | **分類が違う**。外部の対応表ではない |

参考: JMAの地域マスタ `bosai/common/const/area.json` は HTTP 200 で取得でき、
既に`jma_warning_client.py`が取得している（写経していない側の実例）。

## 対応方針

### 取得できるもの → 外部ソースとして取り込む

実行時に取りに行くのではなく、[T970](T970.md)決定1・2の仕組みへそのまま乗せる。

```
source_features(source='npa_codebook')   ← 都道府県コード・当事者種別コード
```

生データをDBへ置く3条件を満たす（手動で取り直す／母集団が確定して小さい／二重に持たない）。
**フォールバックは持たない**——取込バッチが失敗したら失敗するだけで、実行時経路には影響
しない。新しい仕組みも要らず、`source_runs`の世代管理に乗る。

### 取得できないもの → 宣言のまま。ただし出典を書く

`JMA_TILE_SPECS`は取得できない。[T633](T633.md)が「手で書き写して7要素中6要素を誤った」
実績を残し、その対策として1箇所へ集約したのが今の形であり、**取得できない以上これが正解**。
ただし**どこから写したかを宣言へ書き、後から突き合わせられるようにする**。
取れないと分かっているものに、取れるふりのフォールバックを付けない。

### βの範囲に属するもの → 取込プロファイルへ

`KANTO_PREFECTURE_CODES`（NPA採番）と`KANTO_PREFECTURE_CODES_KSJ`（JIS X 0401）は、
同じ7都県を2つの採番で二重に持っている。採番は本当に別物:

| | 東京 | 茨城 | 栃木 | 群馬 | 埼玉 | 千葉 | 神奈川 |
|---|---|---|---|---|---|---|---|
| NPA（警察庁独自） | 30 | 40 | 41 | 42 | 43 | 44 | 45 |
| JIS X 0401 | 13 | 08 | 09 | 10 | 11 | 12 | 14 |

対応表自体は必要だが、7都県ぶんを2回書く必要はない。

```yaml
# import_profile.yaml
target:
  prefectures: ["08","09","10","11","12","13","14"]   # JIS X 0401（国の標準）で書く
  # 正式版は prefectures: all
```

**選択（どこを取り込むか）はプロファイルへ国の標準コードで1回だけ書き、採番の変換は各
ソースのアダプタが持つ**（NPA採番への変換は、取り込んだコード表CSVを都道府県名で
突き合わせて得る）。全国化は`all`に変えるだけになり、両方の定数が消える。

`ALLOWED_WAY_TAGS`(22)・`ALLOWED_NODE_TAGS`(10)は[T970](T970.md)決定1で廃止が決まっている。

## 完了条件

- 83件すべてを (a) 自分の判断 / (b) 外部の対応表 / (c) 取込の範囲 のどれかへ分類し、
  分類の根拠を1件ずつ書く（上位30件は本文に着手済み、残り53件が未分類）
- (b) のうち取得できるものを外部ソースとして取り込み、写経を消す
- (b) のうち取得できないものは、宣言へ出典を書く
- (c) をプロファイルへ寄せ、定数を消す
- `WARNING_CODE_NAMES`（37件、最大）の在処を調べて振り分ける

## 関連

- [T970](T970.md): データの置き場所と形。本タスクはその決定3（管理データ: 値はDB／式は
  コード）に**「外部が決めた対応表」の箱が無い**ことが分かって分離したもの
- [T633](T633.md): JMAタイル仕様を手で書き写して誤った実績と、1箇所への集約
