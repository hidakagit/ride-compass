# T1147. ルート探索のエンジンのファイルに、外部とやり取りしない評価の合成が同居し、行数の上限の91%に達している

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 変更理由の同居（前提の消えたタスクも含む）。services/road_graph_engine.py（9a234615で2,448行、規模ウォッチの閾値2,700）の203〜678行（LegCostArrays・RowValues・_EdgePassage・_Evaluated・_LegCostComposer、約470行）とモジュール直下の関数（約360行）は外部I/Oが0で、静的スコア行列・重み・0次フィルタ・風から区間のコストを時刻ビンごとに合成する純粋な計算。同じファイルのRoadGraphEngineはGraphService・WeatherServiceを待ち合わせる段取りを持つ。設計原則は評価合成をdomainに集約するとしている。T1112の記録（T1112.md:216-221）は、途中の状態を手で組まないと入力を作れないため公開の入口から確かめられない性質7種（迂回率の学習・夜間軸・折返し点の選び方等）を挙げ「関数を配列だけで呼べる形へ切り出してから」と書く（判断原則17の実例）。分割を見送ったT1037の再評価条件（閾値到達・routing-engine.mdの分割）はT1043が分割せずに閉じたため発火しえない。ファイル内でしか使わない公開定数19本（RETRACE_PENALTY_MULTIPLIER等）もprivateにする。T958（評価が要求する形の区間テーブル）と着手順を調整する。
