# T1088. 回の目的と母集団を状態の表で持ち、進め方の指示を記録へ書かない

状態: 完了

## 背景

ユーザー指摘（2026-09-23）: 回の進め方の指示（T1078を待つ・改善系を続ける等）を、司令塔がT1049の記録へ「ユーザー決定」として書きmasterへコミットしていた（7c943494・40617cba）。回の目的と母集団は「今どう動いているか」という状態で、記録（維持しない）ではなく状態の表に置くべきもの。指示のたびにコミットとCIが走り、記録が会話の写しで膨らみ、決定と進め方の指示の見分けが付かなくなる。やること: (1) 状態の表（board.json）の run に、回の目的（文）と母集団（タスク番号の並び、派生元つき）を持たせ、orchestrate.py に設定・追加・一覧のコマンドを足す（例: board run goal / board run add Txxx from=Tyyy）。表は他が源泉の事実を写さない原則（規約「状態の表」）に従い、母集団の各タスクの状態は記録と台帳から導いて出す。(2) status と check が、回の目的・母集団の残り（未完了のもの）・終わりの条件（規約「回の始まりと終わり」）に当たっているかを出す。(3) 規約「回の始まりと終わり」を、目的と母集団は表に置き、回の記録には終わりにまとめを1回だけ書く、に直す。進め方の指示は記録にもコミットにもしない、と書く。完了条件: 第1回の目的（T1001の前提を0にする）と母集団を表に入れ、status に残り（T1014・T1078・T1079・T1077・T1086・T1087・T1057等）が出ることを見る。

## 対応方針

- **回（`run`）の形**: `{"name", "started", "goal", "population": [{"task", "from", "added"}]}`。持つのは表にしか
  無い事実（回の名前・目的の文・母集団のタスク番号と派生元）だけで、各タスクの状態・残り・終わりの条件は
  読むたびにorigin/masterの記録の`状態:`と台帳の行から導く（`population_view`）。名前だけを文字列で持つ今の
  表（`"run": "2026-09-23 第1回"`）は名前だけの回として読み、最初の`board run`の書き込みで形を直す（移行の
  手順は要らない）。
- **コマンド**: `board run start <名前>`（新しい回。前の回を表示してから置き換える）・`goal <文>`・
  `add <Txxx>... [from=<Tyyy>]`・`remove <Txxx>...`・`list`（引数無しも同じ）。従来の`board run k=v`
  （`limits.concurrent`等）はそのまま。
- **状態の導き方**: `完了`=記録が完了、`トリガー待ち`=未完了で台帳の行に`— トリガー:`があり、その条件が
  母集団のタスクを名指ししていない（ユーザーの判断で先送り）、`残り`=それ以外の未完了、`記録なし`=
  origin/masterに記録が無い。条件が母集団のタスクを名指しするもの（T1014・T1079の「T1078の完了」）は回の
  中の順番なので残りに数える。母集団の外のタスクを名指す条件は、完了を待つのか利用実績を待つのか
  （T207「T204だけでは見通しが不足するという利用実績」・T636）を文から決められないので先送りのまま——
  はじめは「名指し先が全部完了なら条件が満たされた」とも数えたが、T207・T636を誤って残りにするので外した。
  終わりの条件1は「母集団が空でなく、残りと記録なしが0件」。
- **写しのキーの拒否**: `board run k=v`で`run`を丸ごと書く・`run.population`を書く・`run.`の下に
  `RUN_KEYS`（name・started・goal・population）以外を書く、を拒否する（`nested_key_reason`）。回の下は
  拒否の一覧ではなく許す一覧にした——状態・残り・メモ・指示の文のどれを書かれても写しになり、名前を
  予想して並べると漏れる。手で書かれた写し（表を直接編集したもの）は`check`が`board_copy_keys`で出す
  （既存の`FORBIDDEN_KEYS`の全区分と、回・母集団の1件の許す一覧の外）。
- **status**: 先頭に回の名前・目的・母集団の件数（完了・トリガー待ち・残り）・終わりの条件1に当たって
  いるか・残りの一覧（担当・振り出し待ち・「T1078待ち」の注記つき）を出す。担当の現在のタスクが母集団の
  外なら`?`の印。
- **check**: 同じ要約の行を出し、異常として①担当が稼働しているのに目的・母集団が無い、②母集団のタスクの
  記録が無い、③終わりの条件1に当たった（新しいタスクは振り出さない）、④表に写しのキーがある、を出す。
  門が開いたときの「振り出し待ちを拾う」は、母集団があればその中だけにし、終わりに入ったら拾わない
  （母集団の外は次の回の候補、規約どおり）。門（`gate`）は変えていない（指示の範囲は run・status・check）。
- **規約「回の始まりと終わり」**: 目的と母集団は表に置く（コマンドつき）・各タスクの状態は導く・
  **進め方の指示は記録にもコミットにもしない**（待つ=振り出し待ちの前提、順番=優先度、足す外す=
  `board run add/remove`、目的の言い直し=`board run goal`で表す）・回の記録に書くのは終わりのまとめ1回だけ・
  終わりの条件1の機械の導き方、を書いた。CLAUDE.md（b657f236）の「進め方の指示は記録にもコミットにも
  しない（並行実行では状態の表が持つ）」と同じ向き。「状態の表」節の持つものの一覧と場面の表にも回の
  目的と母集団を足した。

## 検証

使い捨ての表（scratchpadの`--dir`）で。本物の表（`<gitの共通ディレクトリ>/orchestration/board.json`）には
触っていない（司令塔が入れる）。origin/masterは2bbcf831時点。

- **第1回の目的と母集団**: 名前だけの旧い形の表（`"run": "2026-09-23 第1回"`、稼働の担当2本＝T1088と
  母集団の外のT1043、振り出し待ちT1082）へ、`board run goal T1001の前提を0にする`と13件を
  `board run add <Txxx> from=<派生元>`で入れた。派生元はT1049の記録（「母集団の残り」「摩擦」「ユーザー決定」）と
  各記録の背景から: T1014←T1001、T1078←T1014、T1079←T1078、T1077←T1014、T1069←T1014、T1083←T1049、
  T1084←T957、T1085←T957、T1082←T1080、T1086・T1087・T1057・T1088←T1049。
  `status`の出力: 「母集団13件: 完了5件・残り8件」「終わりの条件1には当たっていない。残り: T1014（T1078は
  完了）、T1079（T1078は完了）、T1077、T1085、T1082（振り出し待ち）、T1087、T1057、T1088（担当 G-round（稼働））」。
  T1043の担当に「? T1043は回の母集団の外」。完了5件はT1078・T1069・T1083・T1084・T1086（検証中に
  T1078とT1086がmasterへ入り、表を触らずに残り→完了へ動いた）。
- **拒否**（どれも終了コード1で正本の場所を出す）: `board run run.population+={"task":"T1","state":"完了"}`・
  `board run run.notes=T1078を待つ`・`board run run=第2回`・同じタスクの2回目の`add`。
  `board run run.name=...`と`board run limits.concurrent=3`は従来どおり通る。
- **手で書いた写し**（壊した入力）: 表を直接編集して母集団の1件に`state`、回に`instructions`を足すと、
  `check`が「表に写しのキーがある: run.instructions（…）」「… run.population T1014.state（…）」の2件を出した。
- **終わりの条件1**: 新しい回（`board run start 試験の回`）に完了2件（T1069・T1084）とトリガー待ち1件（T206、
  条件は冬季前）だけを入れると、`check`が「要対応: 回の終わりの条件1に当たっている」を出した。
- **条件が母集団を名指すとき**（`population_view`を直接呼んだ）: T1041だけ→トリガー待ち・終わり、
  T1041＋T1021（T1041の条件がT1021を名指す）→T1041は「残り（T1021待ち）」・終わらない、T1014だけ→
  トリガー待ち、T1014＋T1078→T1014は「残り（T1078は完了）」、T207だけ→トリガー待ち。
- 静的検査: `ruff check scripts/orchestration/core.py` 通過、`python scripts/review_checks.py docs` 違反なし。

## 実装の増減

- 実装（`scripts/orchestration/core.py`）: +229/−8行（`git diff --numstat`、docstringの使い方の行を含む）。
- 文書（`docs/conventions/orchestration.md`）: +26/−10行。記録（本ファイル）は別。
- テスト: なし（この道具にテストは無く、使い捨ての表で確かめた）。

## 気づき（本タスクでは入れていない）

- 終わりの条件1に当たっても、`gate`は振り出しを止めない（`check`が異常として出すだけ）。規約は「終わりに
  入ったら新しいタスクは振り出さない」。門で止めるかは、門の理由に回の状態を足すかどうかの判断で、
  指示の範囲（run・status・check）の外なので入れていない。

所要（並行実行）: 依頼 2026-09-23 21:44 → 完了 21:58（14分）、うち枠待ち0分・CI待ち0分、規模札S。完了のコミットのCI 2分（監査で追記: 作成 21:57 → 結論 22:00）

## 保留（ユーザー判断）: 回の終わりの条件に当たったら、振り出しの門も閉じるか（2026-09-23 依頼）

いま: 母集団がすべて済んで終わりの条件1に当たっても、`check`が知らせるだけで、`gate`（振り出してよいかの門）は開いたまま。
規約「回の始まりと終わり」は「終わりに入ったら新しいタスクは振り出さない」。

1. **（推奨）門でも閉じる。** 終わりの条件に当たったら`gate`が振り出しを止め、理由を出す。規約と道具が揃う。
2. **知らせるだけのまま。** 司令塔が`check`を読んで止める（今日のように読み落とすと振り出してしまう）。

**ユーザー決定（2026-09-23）**: 1（門でも閉じる）。このタスクを開け直して入れる（新しい番号は振らない）。

## 残り: 終わりの条件に当たったら門を閉じる（2026-09-23、上のユーザー決定）

`orchestrate.py gate`（と`board dispatch pop`）が、回の終わりの条件（規約「回の始まりと終わり」の1〜3のうち機械で判定できる1）に
当たっているとき振り出しを止め、理由（回の名前・目的・母集団がすべて済んだこと）を出す。回が始まっていない（目的・母集団が無い）
ときの扱いも決めて書く。完了条件: 使い捨ての表で、母集団がすべて完了の回では`gate`が閉じ、残りがある回では開くことを見る。

### 対応方針（門）

- **回の側の閉じる理由を1つの関数にした**（`run_closed_reason`）。`gate_reasons`と`board dispatch pop`が
  同じ関数を読む。`Facts`が回の値（`population_view`）を1回導いて持ち、`gate`・`status`・`check`はそれを使う。
- **終わりの条件1に当たっている**: 門が閉じ、理由に回の名前・目的・「母集団N件がすべて完了かトリガー待ち」を
  出す。`board dispatch pop`は同じ理由で取り出さない（終了コード1）。条件2・3は機械が判定できないので門には
  入れていない（規約にそう書いた）。
- **回が始まっていない（母集団が表に無い）ときは閉じる**。規約は「母集団の外のタスクは振り出さない」ので、
  母集団が無ければ振り出せるタスクが無い。開けたままにすると、目的と母集団を置かないまま振り出す今日の
  形がそのまま通る。目的の文だけが無い（母集団はある）回は閉じない——振り出す対象を決めるのは母集団で、
  目的の未設定は`status`・`check`が「未設定」と出す。本物の表は母集団13件を持っているので、この変更で
  今の回の門は閉じない（`board run list`で確かめた）。
- **`board dispatch pop`は母集団の外を取り出さない**。規約は「母集団の外は次の回の候補として列に残す」で、
  `check`は既にそう拾っていたが、`pop`は外のものも先頭なら取り出していた。番号を指定した取り出しも、外なら
  理由を出して拒む。`board dispatch list`は外のものに「回の母集団の外」の印を付ける。
- `check`の「振り出し待ちがあり門が開いている」は、終わりの判定を自前で持たず門の結果に任せた（母集団の中だけを数える）。
- 規約: 「上限と門」節の門が見るものに回の状態を足し、「回の始まりと終わり」節に、母集団が無い間と終わりの
  条件1に当たっている間は`gate`が閉じ`pop`も取り出さないことを書いた。`.claude/commands/orchestrate/priority.md`の
  取り出しの説明にも母集団の外を取り出さないことを足した。

### 検証（門）

使い捨ての表（scratchpadの`--dir`、`limits.concurrent=99`・`--cpu-max 101`で他の理由を外した）で。本物の表は
読んだだけ（`board run list`）。origin/masterは5c127812時点。

- **母集団がすべて済んだ回**（T1014・T1078が完了、T1057がトリガー待ち）: `gate`がNG・終了コード1、理由は
  「回「全部完了の回」（目的: 試験）は終わりの条件1に当たっている——母集団3件がすべて完了かトリガー待ち。…」の
  1件だけ。`board dispatch pop`も同じ理由で「取り出さない」・終了コード1。
- **残りがある回**（T1014完了・T1077残り、振り出し待ちにT9999（母集団の外）とT1077）: `gate`がOK・終了コード0。
  `board dispatch list`はT9999に「回の母集団の外」。`board dispatch pop 1`（T9999を指定）は「T9999は回の母集団の外」で
  拒否、`board dispatch pop`は先頭のT9999を飛ばしてT1077を取り出した。
- **回が無い表**: `gate`がNG「回が始まっていない（母集団が表に無い。…）」、`pop`も取り出さない。
- 静的検査: `ruff check scripts/orchestration/core.py` 通過、`python scripts/review_checks.py docs` 違反なし。

### 実装の増減（門）

- 実装（`scripts/orchestration/core.py`）: +35/−9行（`git diff --numstat`）。
- 文書: `docs/conventions/orchestration.md` +8/−2行、`.claude/commands/orchestrate/priority.md` +2/−1行。記録（本ファイル）は別。
- テスト: なし（この道具にテストは無く、使い捨ての表で確かめた）。

所要（並行実行）: 依頼 2026-09-23 23:49（開け直しの分。依頼の時刻は未記録なので着手の時刻）→ 完了 23:54（5分）、うち枠待ち0分・CI待ち0分、規模札S。完了のコミットのCI 2分（監査で追記: 作成 23:54 → 結論 23:56）
