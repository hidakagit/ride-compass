# T592: 区間・候補の物理量表示を材料カタログ駆動へ汎用化する

状態: 完了（第1段[backend・UI]・第2段[レガシーフィールド撤去]とも完了、
2026-09-05起票・完了、分割元は[T590](T590.md)）

## 背景

`wind_score`・`wind_penalty`・`gradient_percent`・`road_surface_good`等の軸固有レガシー
フィールドと比較パネルの固定行を、「重み>0の軸が参照する材料id→値」の汎用辞書へ
置き換える。比較パネルの「風スコア m/s」は風軸の重みに関係なく常に出る固定行で、
「評価に使っていない軸は出てこない」というユーザーの期待と食い違っている。

## 対応方針

- `RouteSegmentDetail`に`material_values: dict[str, float]`、`RouteCandidate`に同名の
  距離加重平均を追加する。中身は「重みが0より大きい公開軸が参照している材料」だけ
  （`AXIS_DEFINITIONS`の`shape.terms`から導出、軸名のハードコード無し）。
  `LegCostArrays.wind_penalty`（風専用フィールド）は「材料id→配列」の辞書へ置き換え、
  `_wind_penalty_at`・`_aggregate_wind_score`は材料非依存の関数へ一般化する。
- 比較パネル（`ComparisonPanel.tsx`）の`PHYSICAL_METRIC_ROWS`から風スコア・舗装率を外し、
  `material_values`と材料カタログのラベル・単位から行を生成する。距離・獲得標高は
  材料ではないルート属性として残す。
- 区間クリック詳細（`page.tsx: selectedRouteSegment`）に材料値を出すかは要相談
  （ユーザー指示2026-09-05: 走行中のスマホ利用が主で情報量を増やしたくない。
  [T590](T590.md)「UI設計の基準」6）。既定は出さず、出す場合も研究モード限定か折りたたみの
  奥とし、実装前に詳細案をユーザーへ提示して決める。データとしての`material_values`は
  比較パネル（研究モード）向けに持つ。倍率材料の参考換算（[T598](T598.md)の参考点）も
  同じ判断に従う。
- 材料カタログへ`unit`フィールドを追加し配信する（凡例・詳細の単位表記の唯一の正）。
- レガシーフィールドの撤去は2段階。第1段で汎用辞書を追加し比較パネル・区間詳細を
  切り替える。第2段で`wind_score`・`RouteSegmentDetail.wind_penalty`・`gradient_percent`・
  `road_surface_good`・`road_score`・`max_gradient_percent`を撤去する。勾配のルート後
  色分け（`routeStyleModes.ts: isSignedAbsShape`経路が`gradient_percent`を直接読む）は
  第2段で`material_values`から読むよう合わせる。

## 対象ファイル

`backend/app/domain/route.py`・`services/road_graph_engine.py`・`domain/material_catalog.py`・
`api/routers/material_catalog.py`・`frontend/src/components/ComparisonPanel/ComparisonPanel.tsx`・
`frontend/src/app/page.tsx`（区間詳細）・`frontend/src/components/Map/routeStyleModes.ts`（第2段）・
`docs/modules/backend/routing-engine.md`・`docs/modules/frontend/`該当モジュール。

## 完了条件

- 風・勾配・路面の名前を知るフィールドがルート結果のAPIモデルから消えている（第2段）。
- 風軸の重み0で生成したとき、比較パネル・区間詳細に風の行が出ない。重み>0では軸が参照する
  材料の値が単位付きで出る。
- 軸スタジオで軸の材料を変えると表示も追従する（コード変更なし）。
- 設計原則5（同名フィールドの意味を変えない）に照らし、旧フィールドは意味を変えずに撤去
  する（改名・再利用しない）。

## 実装メモ（第1段backend、2026-09-05）

- `RouteSegmentDetail.material_values`/`RouteCandidate.material_values`を追加
  （`app/domain/route.py`）。集約は`merge_material_values`（`_merge_axis_value_dict`共有
  実装、`merge_axis_difficulties`と同じ距離加重平均）。`aggregate_segments_into_bins`の
  ビン単位集約と、`route_generator.py: RouteGenerator._with_material_values`（候補全区間へ
  の集約）の両方から呼ぶ。
- `LegCostArrays.wind_penalty`（風専用フィールド）を`material_arrays: dict[str, np.ndarray]`
  （動的材料id→`full_edge_row`順配列、`evaluate_dynamic_material_arrays`が返す全材料のうち
  値があるものだけ）へ置き換えた（`services/road_graph_engine.py`）。
  `_wind_penalty_at`は`_material_value_at(leg, material_id, row)`へ一般化し、
  `_aggregate_wind_score`は`_aggregate_material_average(edges, context, leg_of_edge,
  material_id)`を`material_id="wind_penalty"`で呼ぶ薄いラッパーにした。
  `RouteSegmentDetail.wind_penalty`・`RouteCandidate.wind_score`は値・意味を変えず
  `material_arrays["wind_penalty"]`から読む（第2段で撤去予定、レガシーフィールドの
  一時的な二重配線）。
- `material_values`に入れる材料idの集合は`_active_material_ids(weights)`が
  `AXIS_DEFINITIONS[axis_id].materials`（`is_known_material`で軸id参照を除外）から
  重み>0の公開軸ぶんだけ導出する（軸名のハードコード無し）。`_build_segment_details`が
  区間ループの前に1回だけ計算し、各区間で`material_arrays`に値がある材料だけを拾う
  （値が無い材料はキーを持たない）。
- 判断点: `evaluate_dynamic_axis_arrays`が返す`resolved`辞書は、動的材料（現状
  `wind_drag_ratio`・`wind_penalty`）と軸id（静的軸含む）が混在しているが、静的軸
  （勾配・路面等）は個々の材料id単位の配列としては保持されておらず軸別difficultyの形でしか
  持っていない。そのため今回の`material_arrays`・`material_values`は実質的に動的材料
  （風系）のみが対象になる。静的材料（例: `gradient_percent`・`surface_good`）を
  `material_values`へ含めるには`StaticEdgeScoreMatrix`側に材料単位の生値を保持する
  改修が別途要る。第1段の対応方針では「重み>0の公開軸が参照する材料id→値」を求めよと
  あり、風軸以外（勾配・路面）の軸は現状材料名がそのまま軸スコアと一致しない/生値を
  持たないため、本タスクの完了条件検証は風軸で行う（比較パネル側でどう見えるかはUI側
  [第2段]の実装時に再検証が必要）。
- テスト: `tests/test_wind_time_varying.py`（`outbound.material_arrays["wind_penalty"]`への
  追従、風軸重み>0で区間`material_values`に`wind_penalty`が出る/重み0で出ないの2件を追加）、
  `tests/test_route.py`（`merge_material_values`の距離加重平均・データ無しキー省略）、
  `tests/test_route_generator.py`（`RouteCandidate.material_values`の候補全体集約・
  segments欠損時空dict）。
- 生成物同期: `export_openapi.py`はDB接続が要るため実行不可（環境制約）。
  `frontend/src/types/generated/openapi.json`・`api.d.ts`の`RouteSegmentDetail`/
  `RouteCandidate`スキーマへ`material_values`を`axis_contributions`と同じ書式（
  `additionalProperties: {type: number}`のobject）で手動追記した。CIの`api-contract`
  ジョブが実際のエクスポート結果との差分を検証する。
- 検証: `pytest tests/test_road_graph_engine.py tests/test_wind_time_varying.py
  tests/test_route.py tests/test_route_generator.py -q` 全件green。バックエンド全体
  `pytest -q -m "not postgis" -n 4`は1510 passed（`test_pbf_source.py`・
  `test_import_pbf.py`の収集エラー2件は`osmium`未導入による環境起因で本タスクと無関係、
  T591実装メモと同じ既知事象）。

## 実装メモ（第1段UI、2026-09-05）

- backend: `GET /api/material-catalog`のレスポンス（`MaterialCatalogEntry`）へ`unit`
  （`MaterialSpec.unit`が単一ソース）を追加。frontendは単位を持たない設計を維持する。
- frontend: `lib/axisMaterialsCatalog.ts`の`AxisMaterialOption`へ`unit: string`を追加し
  （静的フォールバックにも値を補完）、新規`materialCatalogLabel`/`formatMaterialValue`を
  共用ヘルパーとして追加（ComparisonPanel.tsx・page.tsxの両方から使う）。
- `ComparisonPanel.tsx`: `PHYSICAL_METRIC_ROWS`から風スコア・舗装率の固定行を撤去し、
  `material_values`から動的生成する`buildMaterialValueRows`へ置き換えた（いずれかの
  スロットが値を持つ材料だけ行にする、`buildAxisDifficultyRows`と同じ規約）。新規
  `materials`propを追加、`page.tsx`が`useMaterialCatalog()`を渡す。
- 区間クリック詳細（`page.tsx: selectedRouteSegment`）はユーザー判断（2026-09-05）により
  「研究モード限定で出す」。`AxisContributionBar`の下へ`material_values`一覧を追加し、
  `researchEnabled && Object.keys(...).length > 0`でのみ表示する。
- 生成物同期: `frontend/src/types/generated/openapi.json`・`api.d.ts`の
  `MaterialCatalogEntry`スキーマへ`unit: string`（必須）を手動追記した
  （backend実行環境がこの環境に無く`export_openapi.py`は実行不可、環境制約）。
- 検証: frontend `npx vitest run`（973件green、`ComparisonPanel.test.tsx`・
  `useMaterialCatalog.test.ts`・`page.test.tsx`に新規テストを追加）、`eslint`・`tsc`
  （`layout.tsx`の既知エラーのみ）green。backend側の`test_material_catalog_routes.py`への
  `unit`アサーション追加は実行未確認（同じ環境制約）。

## 実装メモ（第2段、2026-09-05）

- `RouteSegmentDetail`から`gradient_percent`・`wind_penalty`・`road_surface_good`、
  `RouteCandidate`から`max_gradient_percent`・`wind_score`・`road_score`を撤去
  （`app/domain/route.py`）。`_merge_segment_bin`・`aggregate_segments_into_bins`
  docstringも追従。撤去に伴い専用の集約関数だった`_weighted_mode`
  （`road_surface_good`の距離加重多数決）・`_aggregate_road_score`・
  `_aggregate_material_average`・`_aggregate_wind_score`（`services/road_graph_engine.py`）は
  死んだコードとして削除した。`domain/road.py: distance_weighted_road_score`
  （`road_score`計算の実体）も同様に削除（`classify_osm_surface`は他所で使うため残す）。
- 判断点の解消: 第1段実装メモに残した「静的材料（勾配等）は`StaticEdgeScoreMatrix`に
  材料単位の生値を保持しないため`material_values`に含められない」という制約は、
  `StaticEdgeScoreMatrix`の改修なしで解消した——`_build_segment_details`が区間ループ内で
  既にEdgeごとに`gradient_percent`（`ElevationAttribute.average_grade`）を計算済みのため、
  この既存のローカル変数を`static_material_values`として`material_values`へ混ぜるだけで
  足りる（新たな計算コストは無い）。
- `_active_material_ids(weights, lens_axis_id=None)`にlens_axis_id引数を追加。
  `lens_axis_id`が符号付き材料の軸（`map_value_kind(definition)=="signed_material"`、
  `domain/dynamic_way_values.py`）を指す場合、その軸の材料も重みに関わらず
  `material_values`へ含める。地図の勾配レンズ（`routeStyleModes.ts`）は
  重み0の軸でも塗る必要があり、`material_values`が重み>0だけに限定される規約のままだと
  レンズ表示が壊れるため（設計レビュー時にユーザーへ確認し、`material_values`拡張＋
  この既存計算再利用の方針で合意）。
- `routeStyleModes.ts: routeColorableModeFromAxis`の`signed_material`分岐を
  `["get", axis.shape.terms[0].material]`（フィーチャ直下）から
  `["get", axis.shape.terms[0].material, ["get", "material_values"]]`
  （`material_values`経由）へ変更。`MapView.tsx`の関連コメントも追従。
- OpenAPI生成物: ローカルにPostgreSQL 16+PostGISをセットアップし
  （`backend/scripts/bootstrap_ci_db.py`でmigration適用＋`axis_definitions`スナップショット
  読み込み）、`export_openapi.py`→`npm run generate:api`を実際に実行して
  `openapi.json`/`api.d.ts`を再生成した（第1段時点の手動追記と異なり、今回は環境制約なしで
  正規の手順を踏めた）。
- バックエンド・フロントエンド双方のテストフィクスチャ（`RouteSegmentDetail`/
  `RouteCandidate`を直接構築する箇所）を撤去済み6フィールドの参照無しに更新し、
  `road_score`/`wind_score`で検証していたテストは同じ内容を検証する
  `axis_difficulties`ベースのアサーションへ置き換えた
  （`test_candidate_aggregates_surface_axis_from_path_edges`・
  `test_candidate_wind_axis_is_present_when_weather_available`等）。
  新規に`_active_material_ids`のlens_axis_id拡張の回帰テストを2件追加
  （`test_material_values_includes_gradient_when_it_is_the_lens_even_at_zero_weight`・
  `test_material_values_omits_gradient_at_zero_weight_when_not_the_lens`、
  `tests/test_road_graph_engine.py`）。
- 検証: バックエンド`pytest -q -m "not postgis"`1521 passed
  （`test_import_pbf.py`・`test_pbf_source.py`の収集エラーは`osmium`未導入の環境起因で
  無関係、T591と同じ既知事象）。ローカルPostGIS環境で`pytest -m postgis`・
  `tests/test_migrate.py`も実行し green を確認。フロントエンド`npx vitest run`995 passed、
  `tsc --noEmit`（`layout.tsx`の既知エラーのみ）・`eslint`green。
