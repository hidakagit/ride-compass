# T998. テストを実装から起こし直し、既存とdiffを取る

状態: 未完了（2026-09-22起票）

## 背景

[T996](T996.md)の走査中に3ファイルを「既存テストを一切見ずに、実装だけを読んで書き起こす」
やり方で作り直したところ、3件とも**既存テストの半分以上が実装と対応していなかった**。

| ファイル | 既存 | 起こし直し | 内訳 |
|---|---|---|---|
| `backend/tests/test_axis_display.py` | 960行 / 43件 | 429行 / 47件 | 網羅率 84%→98% |
| `frontend/.../AxisComposer.test.tsx` | 978行 / 46件 | 249行 / 16件 | 30件が別コンポーネントの責務 |
| `frontend/.../AxisStudio.test.tsx` | 17件 | 17件 | 10件が実質的に別物 |

3件目の内訳が型を示している。**実装が持つ分岐のうち5つ（取得失敗の表示・下書きが空・
最後の1軸は削除できない・削除失敗・公開取り消し失敗）を既存テストは1つも見ていなかった**一方、
既存にしか無い4件はすべて別コンポーネントの責務で、1件は既に撤去された機構
（ログイン欄）が無いことの見張りだった。

同じセッションで`AxisComposer.materialValues.test.tsx`も、真偽材料には値欄自体が
描かれないため「候補セレクトが出ない」の検査が成立していなかった（常に通る）。

これは特定の改修の後始末ではない。テストが**実装から再導出されず、変更のたびに
継ぎ足されてきた**ことの結果で、その書き方はリポジトリ全体に及んでいる。

## なぜ「全部書き直す」ではないか

実装を読んで書き起こすと、**過去のバグを刺しているテストが復元できない**。実装は
「いま何をするか」しか語らないので、「この入力で落ちた」という知識は失われる。
だから作業は書き直しではなく、**起こし直して既存とdiffを取り、両方向を判定する**。

## 対象の出し方

母集団は`backend/tests/**/test_*.py`と`frontend/src/**/*.test.ts(x)`の全件。
1ファイルずつ、次の順で進める（テストファイル単位。複数の実装をまたぐテストは、
diffの結果として分割されることがある）。

### テスト行÷実装行では並べられない（2026-09-22実測）

起票時は「比が4を超えるものから」と書いたが、backendで測ると**効かなかった**。
母集団の定義は「`ast`でdocstringを除き、コメント専用行と空行を落とした行数。
対応付けは`test_<stem>.py`↔`backend/app/**/<stem>.py`」。結果、比が4を超えたのは
`test_strict_model.py`（29行/3行）の1件だけで、それは実装が3行だから大きいだけだった。
上位は3.5前後に固まっており、順序の根拠にならない。

### 触っている層で割る（これを使う）

各テストが`from app.X`で何を読んでいるかを数えると、backendの128ファイルはこう割れる。

| 層 | 件数 |
|---|---:|
| またがり（2層以上） | 46 |
| app/domain 単独 | 35 |
| app/infrastructure 単独 | 27 |
| appを読まない | 9 |
| app/batch 単独 | 5 |
| app/services 単独 | 4 |
| app/config 単独 | 2 |

またがり46件のうち14件がapi経由で4〜6層にまたがる（`test_routes_generate.py`は18モジュール）。

### 順番は、下の層から上へ

下を先に固めると、上の層で見つかる他責務のテストは「**移す**」ではなく「**消す**」で
片付く——責務の行き先が既に完成しているため。逆順だと、apiから抜いた検査をdomainの
テストへ移し、そのdomainテストを後で起こし直すときに同じものをもう一度判定することになる。

| 段階 | 対象 | 件数 |
|---|---|---:|
| 0 | `tests/`の共有部品 | 11 |
| 1 | app/domain 単独 | 35 |
| 2 | app/infrastructure 単独 | 27 |
| 3 | services（単独＋またがり） | 14 |
| 4 | api経由 | 14 |
| 5 | batch・appを読まない・config | 16 |

**行数はapi層に集中している**（14件で約2,900行）ため、この順だと削減量の実測は遅れる。
そのぶん段階1で「無検査だった分岐が何件出るか」を先に出し、価値判断はそこで行う。

### 段階0は、既に起票済みの2件である

`conftest.py`が`autouse=True, scope="session"`で**全1480テストへ本番相当の14軸を注入**
している（`tests/realistic_axis_fixtures.py`、219行）。この上で段階1を進めると、
起こし直した35件が全部この注入に乗り、下の2件がlandingした時点で作り直しになる。

- [T984](T984.md) テストが本番の軸データを写経している 規模M
- [T981](T981.md) テストの共通部品が、そのテストに関係ない属性を渡している 規模M

**この2件を段階0として先に片付ける。**

## 手順

[docs/conventions/testing.md](../../conventions/testing.md)「既存テストを直さず、実装から
起こし直す」に従う。取り込みの判定は同ファイル「残すべきテストの型」、責務の宣言は
「テストは、テストだけで成立させる」節。**ここへ手順を書き写さない**——このタスクが
終わると記録は維持されなくなり、写した側だけが残る。

## 進み（どれが手順を通っていて、どれが通っていないか）

**作り直すかどうかは、この表で判断する。** 口頭の記憶では別セッションへ届かない。
手順を通ったファイルは、冒頭のdocstringで「対象モジュール」と「ここでは見ないもの・
どのファイルが持つか」を宣言しており、コミットも`git log --grep="実装から起こし直す"`で
辿れる。

### 手順を通した（作り直さない）

| ファイル | 件数 | 分岐カバレッジ |
|---|---|---|
| `test_axis_display.py` | 43→47 | 84%→98% |
| `test_difficulty.py` | 41→22 | — |
| `test_dynamic_way_values.py` | 14→20 | 98%→100% |
| `test_strict_model.py` | 4→3 | 100%→100% |
| `test_gradient.py` | 9→12 | 100%→100% |
| `test_terrain_rgb.py` | 12→7 | 100%→100% |
| `test_jma_tile_specs.py` | 9→18 | 49%→100% |
| `test_twilight.py` | 5→17 | 74%→100% |
| `test_traffic.py` | 6→18 | 93%→100% |
| `test_geo.py` | 11→26 | 82%→100% |
| `test_cycling_speed.py` | 13→28 | 90%→100% |
| `test_jma_area.py` | 8→12 | 96%→100% |
| `test_axis_templates.py` | 16→28 | 95%→100% |
| `test_axis_definitions.py` | 28→60 | 84%→98%（残りは`test_axis_hierarchy.py`の責務） |
| `test_accident.py` | 6→10 | 100%→100% |
| `test_wbgt_points.py` | 3→5 | 100%→100% |
| `test_landcover.py` | 5→22 | 100%→100% |
| `test_registry_defaults.py` | 6→10 | 87%→100% |
| `test_region.py` | 14→24 | 100%→100% |
| `test_jma_amedas.py` | 5→11 | 100%→100% |
| `test_wind.py` | 13→27 | 100%→100% |
| `test_msm.py` | 12→25 | 100%→100% |
| `test_wind_grid.py` | 10→22 | 94%→100% |
| `test_registry.py` | 7→14 | 97%→100% |
| `test_route.py` | 23→37 | 93%→100% |
| `test_wbgt_domain.py` | 7→13 | 100%→100% |
| `test_jma_warning_domain.py` | 7→20 | 100%→100% |
| `test_flood_forecast_domain.py` | 10→22 | 100%→100% |
| `test_resolve_direction.py` | 15→10 | 93%→100%（`traffic`。`test_tag_classification`と共同） |
| `test_tag_classification.py` | 43→21 | 同上 |
| `test_elevation_values.py` | 8→19 | 76%→99%（`attributes`。下の新設と共同） |
| `test_edge_material_arrays.py` | 新設14 | 同上 |
| `test_material_catalog.py` | 7→23 | 100%（母集団10ファイル） |
| `test_material_values.py` | 11→52 | `material_sql` 100%・`road` 100% |
| `test_axis_hierarchy.py` | 30→36 | 98%→100%（`axis_definitions`。母集団13ファイル） |
| `test_canonical_constants.py` | 6→3 | `tests/structure/test_canonical_definitions.py`へ移設 |

### 段階1は完了した（2026-09-22）

domain単独の35ファイルすべてが手順を通った（`test_edge_material_arrays.py`を新設して36）。
`test_canonical_constants.py`は中身が全てソースを読む検査だったため
`tests/structure/test_canonical_definitions.py`へ移し、宣言の書き写し2件を消した。

**段階2は減る工程ではない。** `tests/**/test_*.py`をASTで分類し、各ファイルがimportする
`app.*`モジュール数を数えた結果:

| 段階 | 層 | 件数 | 行数 | appを4モジュール以上読む |
|---|---|---:|---:|---:|
| 1 | domain単独 | 35 | 6,157 | 4 |
| 2 | infrastructure単独 | 26 | 2,977 | **0** |
| 3 | services・またがり | 24 | 5,283 | **11** |
| 4 | api経由 | 25 | 5,640 | **14** |
| 5 | batch・config・appを読まない | 16 | 1,374 | 0 |

段階2の26件は最も多いものでも2モジュールしか読んでおらず、他責務を落とす余地が依存の形
として無い。落とせる本体は段階3〜4（4モジュール以上を読むファイルが25件・10,923行で
全体の約半分）。ただしこれは依存の形からの予測で未実測のため、段階3に入った時点で
同じ母集団定義で測り直す。段階2には別の減り方の余地がある（共有の骨格——cache-aside・
`log_external_call`・レート制限・fail-open——を各クライアントで再検査している分。
`import`では見えないため未測）。

なお当初の段階表はinfrastructure 27件としていたが、機械分類では`test_cache_policy.py`が
`config`と`infrastructure`の両方を読むため段階5側へ落ちて26件になる。

### 段階1の途中経過（2026-09-22、21モジュール）

テスト÷実装は**上がった**——実コードのみで1.16→1.58、説明を含めて0.84→1.18。
比が下がったのは`difficulty`（3.02→1.77、25件が他責務）・`strict_model`（9.67→5.67、
構造テストと重複）・`terrain_rgb`の3つだけで、残り18は上がった。

**下層は「減らす」対象ではなく「足りていなかった」。** 起こし直す前の比は`traffic`が0.20
（実装171行にテスト35行）・`axis_definitions`が0.60（379行に226行）で、純関数3本や公開関数の
大半が無検査だった。分岐カバレッジも`jma_tile_specs` 49%・`twilight` 74%・`geo` 82%から
始まっている。

行数は2,811→3,953（+41%）。**比を下げる対象は上位の層に残っている**（api経由の14ファイルで
約2,900行）。段階3〜4で測り直す。

### docstringの写しの一掃（2026-09-22）

起こし直した30ファイルのdocstringが、**実装のコメント・docstringを裏返した写し**に
なっていた。`comments.md`は対象を「ソースコード」と書いてテストを除外しておらず、削除の
判定基準（「同じ内容が他所にあるなら二重管理」）は実装とテストの間でも成り立つ。

測り方と結果: docstringを句点で切った599文を、そのテストがimportするapp配下モジュールの
コメント＋docstringの全文と突き合わせた（difflibの比率と文字bigramのJaccardの最大値）。
0.55以上が106文。**分布に切れ目が無く（0.2〜1.0が連続）しきい値では判定できない**ため、
順序付けにだけ使い、30ファイルを1本ずつ目で見て落とした。599→371文。

判定基準は[comments.md](../../conventions/comments.md)「テストのdocstringも対象」へ移した。

同時に直した実装の歪み: `traffic.TAG_KIND_RULES`・`DIRECTION_RULES`の優先順位を、リテラルの
整数から群の並びから振る形へ（`_TAG_KIND_GROUPS`・`_DIRECTION_GROUPS`）。生成される表が
旧と完全一致（41件・18件）であることを確認し、優先順位を検査していた`TestRuleTables`
3件を削除した。

規模（母集団＝この30ファイル、基準は`8ddd761a^`）: 全行 3,541→5,100（+44%）、
説明行を除いた実行行 2,262→3,061（+35%）。写しを落とす前は全行+48%だったため、
差の4ポイントが二重管理だった分。

### 段階2は完了した（2026-09-22）

infrastructure単独の26ファイルを、7名へ主題ごとに配って並列に起こし直した。担当は
`backend/app/`を変更せず、実装の欠陥は報告のみとした。

| | 前 | 後 | 差 |
|---|---|---|---|
| 全行 | 2,977 | 3,500 | +523（+18%） |
| 実行行 | 1,729 | 1,962 | +233（+13%） |
| テスト関数 | 222 | 276 | +54（+24%） |

**見立てを外した。** 「段階2からはテストが減る（他の契約を利用するため）」と書いていたが
増えた。増分の6割以上は`test_road_graph_repository_contracts.py`1本で、旧版がリポジトリの
公開メソッドを1本も通していなかった（分岐カバレッジ50%→96%）。**「infrastructure単独＝
他の契約に寄りかかっている」という前提が誤りで、実際は覆えていなかっただけ**である。
減る形が出たのは気象・防災クライアントだけで、4本が揃って`simple_api_client`の責務を
写経していた責務外15件を落とした（31→24件）。

段階3以降の「減るはず」も同じ前提に立っている。**外したばかりなので、予測として扱う。**

監査は成果物の側から行い、担当の自己申告は採らなかった。

- 旧版と同名のテスト **0件 / 26ファイル**（段階1の較正値: 34ファイル中33ファイルで0件。
  唯一の例外は起こし直していなかった1本で40/40が同名だった）
- 対象モジュールの行を1つも実行しない検査 **0件 / 313件**
- 分岐カバレッジは前後同一の母集団で測り直し、**下がったモジュールなし**。100%到達 12→20本
- 母集団49ファイルを一括実行して602 passed。モジュール大域を通じたファイル間の漏れなし

**並列化の代償を1つ測った。** 担当が報告したカバレッジのうち9件は無効だった——母集団に、
他の担当が**同時に書き換えていたファイル**が入っていたためである。誰も嘘をついていないが、
測定中に母集団の中身が変わった。**並列で配るなら、カバレッジはまとめ役が合流後に測り直す。**

### 構造仕様16は、この作業から出た（2026-09-22）

7名が独立に報告した実装の欠陥を分類したところ、約35件のうち**7割超が3つの姿**に落ちた
——黙ったフォールバック（相手が何を送るか決まっていない）、多重扉（誰が入ってよいか
決まっていない）、取り違え（何が境界を渡るか決まっていない）。別々の指摘として挙がるが、
**境界が「破られたら落ちるもの」として表現されていない**という1つのことである。

同じ時点で分岐カバレッジは26本中20本が100%だった。**網羅率はこの欠陥を検出しない。**

[design-principles.md](../../architecture/design-principles.md)の構造仕様16として立てた。
構造仕様15（データの制約はDB、ふるまいの制約はapp）は、この原則をDB↔appの境界へ当てた
ものにあたる。

実装側の未修正は44件あり、モジュール別に分類して持っている。段階1のやり直し（domain層へ
構造仕様16を当てる作業）で同じモジュールへの指摘が重なるため、**重複を排してから直す**。

### 昨日のコメント削除の監査（2026-09-22）

T967（経緯コメントの一掃）とT968（デッドコードの整理）で、backendから**119件のコメント塊**が
消えていた。2つの欠陥が報告されている——削除の可否を「参照0か」で機械的に決めたこと
（**参照カウントはコードの参照しか数えず、文は参照0でも価値がある**）と、一括削除が
**関数の直前のコメントを巻き込む設計**だったこと。

119件を2つに分けた。**A（44件）**は直後のコードが今も残っているもので、説明だけが失われた
可能性が高い。**B（75件）**はコードごと消えたものだが、巻き込みの設計上、**消えた関数では
なく残っている塊全体を説明していた文**が混ざる。両方を段階1やり直しの担当へ配った。

戻す条件は[comments.md](../../conventions/comments.md)に従う。**元の文をそのまま戻さない**
——経緯を落とし、今も成り立つ制約だけを現在形の一文で書き直す。

### 触ったが手順を通していない（作り直しの負債）

軸フィクスチャの差し替え（[T984](T984.md)）で落ちたものを通しただけ、あるいは追記だけ。
**成果物として数えない。**

- `test_axis_catalog_routes.py`・`test_routes_generate.py`・`test_region_routes.py`
  （api層。段階4で起こす）
- `conftest.py`・`test_axis_registry_service.py`（フィクスチャ名の置換のみ）

## frontendで見つけたが、こちらでは直さないもの（2026-09-22）

frontendは[T1001](T1001.md)が作り直し中のため触らない。点検で見つけた責務のはみ出しを、
そちらで拾えるようここへ残す。

**2026-09-23、下の`AxisComposer.materialValues.test.tsx`・`AxisStudio.test.tsx`の2件をT1001の手順4へ
移した**（[T1001](T1001.md)「手順4（テスト）へ寄せた項目」T-2）。`WarningBadge.tsx`は生成型
（`components["schemas"]["ActiveWarning"]["level"]`）を読む形に既に直っている。この節のfrontendの項目は、
本タスクには残っていない。

- `AxisComposer.materialValues.test.tsx`——値の候補セレクトは`AxisScoringSection.tsx`の
  実装なのに、`AxisComposer`越しに触っている。取得応答（`getMaterialValues`）を差し替えて
  `AxisScoringSection`を直接立ち上げれば、候補あり・空・取得失敗の3経路をその場で見られる。
- `WarningBadge.tsx`——警戒度バッジの4語彙（`WarningBadgeLevel`）を手書きで再宣言している。
  backendは`domain/warning_levels.py`で`Literal`として持ち、Pydanticモデルのフィールド型
  として生成型（`frontend/src/types/generated/`）にも出るため、そちらをimportすれば段の
  食い違いがtscで止まる。backend側にあった「frontendの順序と一致させる」というコメントは
  何も強制していなかったため落とした。
- `AxisStudio.test.tsx`——保存の検証で`AxisComposer`のフォーム（`表示名`・`axis_id`・
  `既定重み`・`作成する`）を直接操作している。`AxisStudio`の責務は「どのモードで開くか」と
  「保存の結果をどう扱うか」なので、子を差し替えて`onSave(payload, isNew)`を呼ばせれば足りる。
  `AxisComposer.test.tsx`は自分のフィールドだけを触っておりクリーンだった。

## T968から引き取った段階4・5（2026-09-24）

[T968](T968.md)（デッドコード総整理）をユーザー決定で縮小して閉じたとき、テストの側の段階4・5を
ここへ寄せた。母集団の作り方はT968の「段階4」「段階5」節にあり、起こし直しの各段階でそのファイルに
当たったときに併せて判定する。引き取ったのは次の3つ（4-2のテストファイル同士の直接依存は
T968で測って0件だったので引き取っていない）。

- **テスト専用の口（4-1）**: appのモジュール直下の公開定義のうち、テストからしか参照されないもの。
  T968の到達可能性の調査（`backend/scripts/dead_code_survey.py`）が出した分は処分済み（4件削除・5件は
  テストの隔離の口として残した）。**ただしあの調査は同名の定義を全部生きている側へ倒すため、
  ありふれた名前の口を取りこぼす**——`tile_score_matrix_cache.size`（「テストの検証用」）・
  `search_graph_cache.clear`（「テスト用。本番コードパスからは呼ばない」）が実例。母集団は名前ではなく
  `モジュール.名前`で参照を数えて出す。
- **そのテストだけが通す行が0行のテスト（4-3）**: `pytest --cov --cov-context=test`で出す。
  0行を機械的に消さない（T968の段階4の3）。
- **置き場所（5）**: 層（DB制約が保証することをappのテストが再確認していないか・純関数を
  サービス経由で見ていないか）・重さ（DB fixtureを使うのに`postgis`マーカーが無い／逆）・
  対応（`test_<stem>.py`に同名の実装が無い）。T968が2026-09-21に粗く測った値は、DB fixtureを使うのに
  マーカーが無いもの11件・appの非公開（`_`始まり）をimportするテスト15件（文字列とimportからの粗い
  母集団で、正当なものを含む）。

## 完了条件

全件について4の判定が終わり、各ファイルの冒頭docstringに**そのファイルが何の責務を
見るか・何を見ないか（どのファイルが持つか）**が書かれていること。

「消す」と判定した件数と、「新版にしか無い＝無検査だった分岐」の件数を、
ファイル群（backend/frontend、ディレクトリ単位）ごとに数えてこのエントリへ書く。
後者が**この作業の価値の実測値**で、これが小さい領域が出たらそこで止めてよい。

## 分割

規模Lのため、着手時にディレクトリ単位で子タスクへ割る。1つの子タスク=1コミット。

## 実施記録（2026-09-22）

`backend/app/services/road_graph_engine.py`（2,438行、実装の最大ファイル）を1件やった。
テストはHEADに存在しなかった（[T970](T970.md)段階Aで`domain/graph`の型3つの撤去により
2,982行がimport不能になり一掃されていた）。

| | 旧版（削除時） | 新版 |
|---|---:|---:|
| テスト | 116本 / 2,982行 | 174本 / 2,836行 |
| 依存する`app`モジュール | 17本 | **1本** |
| 行カバレッジ | 測定不能（import不可） | **901/901** |
| 分岐カバレッジ | 測定不能 | **211/212** |

4分類の内訳: 責務外22・重複38・実装変更で不要1・列挙漏れ2・言い切れない→採用2。
**「列挙漏れ」2件が導出の不足の実測値**で、どちらも両セレクタの並べ替えキーの数え落としだった。

**規模はほぼ減らなかった（−146行・−5%）。** 3問を全174本へ当てても削れたのは3本（1.7%）で、
この実装に対してこの規模が要るということ。**「起こし直すと減る」は標準のどこにも書かれて
おらず、この作業中に持ち込まれた誤った前提だった**——標準が書いているのは「既存を編集すると
既存が見ている範囲の外へ出られない」で、目的は死角を抜けることである。

得られたのは小ささではなく、**動くこと**（0%→99.9%）・**隣が変わっても死なないこと**
（依存17本→1本）・**実装の欠陥9件の発見**である。9件は同じコミット群で処置した
（到達不可コード4件の撤去・`strict=True`・`LegCostArrays.label`の撤去・docstringと実装の
食い違いの是正・意図的な上書きの理由付け・現状維持1件）。

手順の穴が4つ見つかり`docs/conventions/testing.md`へ入れた。とくに**旧版を設計図に使うと
自分の導出の穴が見えなくなる**——旧版のテスト名から18本を書いたとき、境界51件の到達が
86%→92%へ上がって見え、写しを消すと86%へ戻って欠落3件が現れた。

## T1001からの返し（2026-09-25、T1001を閉じた）

frontendで見つけた2件（手順4 T-2: 軸スタジオのテストが責務の外から触っていた件）はT1001で済んだ。記録は[T1001](T1001.md)。
