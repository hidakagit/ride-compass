# T1009. 撤去済みのsplit工程が、docs/modulesに現行仕様として残っている

状態: 完了（2026-09-23）

## 背景

`road_graph_tiles`（タイル取込済みマーカー）・`is_split_up_to_date`（split鮮度判定）・
`presplit_road_graph.py`（事前split バッチ）・`RoadGraphTileRow` は、T970段階C
（`91bc38328` 「読み出しを新しい表へ移し、遅延構築を撤去する」）で撤去済みで、
`grep -rn "road_graph_tiles\|is_split_up_to_date\|presplit\|RoadGraphTileRow" backend/`
は**0件**である。

にもかかわらず docs/modules は、これらを現行の仕組みとして現在形で記述し続けている
（14行／2ファイル、2026-09-22 実測）:

- `docs/modules/backend/routing-engine.md` 10行。うち**対象ファイル表（18行目）が
  `presplit_road_graph.py` を載せ**、**`## batch: presplit_road_graph.py` という節
  （951行目〜）が丸ごと**存在しないバッチの挙動を説明している。他はカバレッジ判定・
  split鮮度判定・キャッシュしない理由の根拠として `road_graph_tiles` を名指しする記述。
- `docs/modules/backend/batch-pipeline-dependencies.md` 4行。**バッチ依存表の段「④」
  として presplit_road_graph.py が1行を占め**、再実行条件・冪等性・「未split範囲への
  リクエストでその場で交差点分割する経路」（＝撤去された遅延構築）を説明している。

実際の現在の姿は、カバレッジ判定が MVT 生成と同じ1クエリへ畳み込まれた形
（`services/region_service.py`、「取込の宣言した範囲か」）であり、batch は
`ingest.py`・`derive_*.py` の系である。

害: docs/modules は実装作業者が着手前に精読する前提の文書で（CLAUDE.md）、存在しない
バッチの再実行を完了条件に含めた計画や、存在しないマーカーを前提にした設計が起票されうる。
とくに batch-pipeline-dependencies.md の④は「この段を再実行しないと評価軸が欠損する」
という運用上の警告の形を取っており、実在しない段に対する警告として読まれる。

frontend側に残ったsplit撤去の残骸（`DbStatusPanel.test.tsx`の存在しない
モック）を落とす作業のなかで、同じシンボルをリポジトリ全体へ grep した際に検出。

## 対応方針

「現行仕様の記述」として残っているものを、実装に合わせて落とす／書き換える。**撤去では
なく置き換えになる箇所（カバレッジ判定・バッチ依存表）は、実装を読んで現在の姿へ
書き直す**——行を消すだけだと、カバレッジ判定という機能自体が無くなったように読める。

docs/architecture/setup.md と docker-compose.yml にも `road_graph_tiles` を Redis
cache-aside 層の対象として挙げる記述があるが（各1箇所）、これは architecture 側の
「外部の制約・構成」の記述であり、同一タスクで扱うか分けるかは着手時に判断する。
docs/records/ は対象外（維持しない方針）。

## 母集団の導出

対象は次のコマンドの出力とする（記述の言い換えを拾うため、シンボル名だけでなく
「split済み」も母集団へ入れる）:

```
grep -rn "road_graph_tiles\|is_split_up_to_date\|presplit\|RoadGraphTileRow\|split済み" docs/modules/
```

## 完了条件

- 上記 grep が docs/modules に対して0件
- `presplit_road_graph.py` が routing-engine.md の対象ファイル表から消え、
  batch-pipeline-dependencies.md の段番号（④以降の繰り上げ）が整合していること
- カバレッジ判定・バッチ依存の記述が、実装（`region_service.py`・`backend/app/batch/`）
  を読んで書き直されており、単なる行削除になっていないこと
- `python scripts/review_checks.py docs` が違反なし

## 実施内容（2026-09-23）

着手時点で`batch-pipeline-dependencies.md`は[T1022](T1022.md)が文書ごと削除済みだったため、
残る対象は`routing-engine.md`だけだった（母集団のgrepは同ファイルの8行）。

`routing-engine.md`は、split工程そのものを名指す行だけでなく、**その工程を前提に組まれた節**
（遅延構築の取得経路・書き込み側のリポジトリ・旧ORM表・事前集計バッチ）がまとめて死んでいた。
行を消すだけだとカバレッジ判定・ノードの値の出どころが無くなったように読めるため、
実装（`services/graph_service.py`・`infrastructure/road_graph_repository.py`・
`services/derived_data_revision_service.py`・`batch/derive_node_materials.py`）を読んで書き直した:

- 責務: 「Road Graphの構築・永続化も担う」→ 読み出して探索用に組むまで。区間を作るのは
  取込・派生バッチ（static-road-attributes.md管轄）。
- GraphService節: 3段階の取得経路（`get_or_build_graph_with_attributes`）を、現在の入口
  `get_search_materials_for_bbox`の3段（派生データ世代の突き合わせ→`is_covered`による
  カバレッジ判定→タイル単位のキャッシュ経由の読み出し）へ置き換えた。カバレッジは
  取込の宣言（`source_runs.profile`のtarget.bbox）から決まり、マーカーの表を持たない。
- タイル集合キー（search_graph_cache）: 「split鮮度が古いとNone」→ 常に全タイルを結合した
  ものを返すので集合だけで中身が決まる。自己修復の発火条件を「タイル再split」から
  「派生バッチが区間を作り直し、材料キャッシュだけ世代で捨てられた」へ。
- infrastructure節: 4分割のリポジトリ・closure読み出し・COPY UPSERT・旧ORM表の節を、
  読み出し専用の`RoadGraphRepository`（向きを持たない区間・3つ組で向きを伝える形）へ
  書き直した。MVT系クエリのカバレッジ判定を「road_graph_tilesのz12祖先マーク」→
  `_COVERAGE_SQL`の畳み込みへ。キャッシュしない判断を「取込完了判定＋split鮮度」→
  `is_covered`へ。
- batch節: `presplit_road_graph.py`・`precompute_road_node_degrees.py`・
  `precompute_road_node_intersections.py`の3節を落とし、探索側に要る前提（信号はノード単位・
  半径で判定・最大階級は下限を上げるだけ）を「交差点の信号・最大階級
  （`derive_node_materials.py`が埋める）」へまとめ直した。枝数の事前集計は
  `derive_topology.py`（static-road-attributes.md）が説明済みのため移さなかった。
  「半径は`POI_CLUSTER_EPS_M`と同じ値」は事実でなくなっていた（バッチは
  `SIGNAL_RADIUS_M`=25m、`POI_CLUSTER_EPS_M`=40m）ので「別の定数として持つ」へ直した。
- `docs/architecture/setup.md`・`docker-compose.yml`: Redisの用途から`road_graph_tiles`を
  落とした（Redisを使うのは`jma_*`系だけ。`grep -rli redis backend/app`で確認）。setup.mdの
  「split未済みだと数十秒」は現在の冷パス（タイル材料のDB読み出し）へ。docker-compose.ymlの
  コメントの改善計画番号も同時に落とした（コメント方針）。

## 検証

- `grep -rn "road_graph_tiles\|is_split_up_to_date\|presplit\|RoadGraphTileRow\|split済み" docs/modules/`
  → 0件（着手前は`routing-engine.md`の8行）
- 書き直した節が名指す識別子（`is_covered`・`_COVERAGE_SQL`・`get_graph_topology_in_bbox`・
  `get_edge_material_arrays`・`ensure_caches_match_db`・`reversed_material_expression`・
  `_ensure_lazy_graph_consistent`・`SIGNAL_RADIUS_M`）は`backend/app`でそれぞれ1件以上ヒット
- `python scripts/review_checks.py docs` → 違反なし

## 派生（起票）

- 書き直しの途中で、較正値の宣言`signal.match_radius_m`（既定40m、管理画面から変えられる）を
  信号判定のバッチが読んでいないことが分かった → [T1050](T1050.md)
- コード側にも「未split冷パス」を根拠にしたタイムアウト値・説明が5箇所残る。値を下げてよいかは
  本番の実測が要るため文書の書き換えで済まない → [T1051](T1051.md)
  （`docs/modules/frontend/route-settings-and-results.md`の根拠表もこちらで扱う）
- `docs/conventions/caching.md`の「タイル取得済みマーカー」・`get_or_build_graph_with_attributes`
  は[T990](T990.md)の拡張後の母集団（`docs/conventions`）に入っているため、そちらで扱う。
