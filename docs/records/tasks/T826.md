# T826. axisLayers.ts・secondaryAxes.tsの経緯コメントを一掃する

規模S。
状態: 完了（2026-09-23、T1001段階Dへ統合）

未着手・DEFER（統合レビュー第8回のP3、2026-09-13）

## トリガー

**`frontend/src/components/Map/axisLayers.ts`または`secondaryAxes.ts`を実装目的で次に編集するとき。**

**発火（2026-09-23、[T1028](T1028.md)で判定）**: 起票後、2ファイルを実装目的で編集した
コミットが複数ある（`git log --since=2026-09-13 -- axisLayers.ts secondaryAxes.ts`で18件、
書式・移動を含む。実装目的の例: `d8325af6`[T968]・`f58aaf65`[T1001段階A]・`bfb793d6`[T1015]）。
一掃は済んでいない（`改善計画T|T\d{3,4}|実機|以前は|ユーザー指摘`を含む行が
axisLayers.ts 25行・secondaryAxes.ts 10行。検知器の分類とは別の粗い目安）。[T1001](T1001.md)の段階Cが本タスクを充当先に挙げている。

単独タスクとして先に回さない理由: 45件が この2ファイルに集中しており、そのファイルを触る
ついでなら追加コストがほぼ無い。一方、いま単独で回すと[T809](T809.md)〜[T825](T825.md)より
優先度が低い作業に時間を使うことになる。

**保留し続けた場合に何が起きるか**: `docs/conventions/comments.md`が禁じる書き方の**最大の実例**が
リポジトリに残り続け、新規コードの参考例として複製される。実際`Map/WidthSwatch.tsx:8-11`・
`windArrowIcon.ts:68-70,92`・`riskMap.ts:145`・`precipitationNowcast.ts:47`・
`components/BackendStatus.tsx:11`・`TodayOutlook.tsx:168`・`AxisStudio/breakpointTools.ts:77`・
`domain/axis_definitions.py:674-675`に同型が新しく足されている。

## 背景

`scripts/review_checks.py docs`が検知する経緯コメントは全件で64件。そのうち**45件が
`axisLayers.ts`（31件）と`secondaryAxes.ts`（14件）に集中**している
（`改善計画T145b/T278/T292/T297/T308/T310/T318/T334/T352/T404/T440/T443/T466/T473/T513`、
「以前は〜だった」「実機フィードバック」「ユーザー指摘」等）。

**[T567](T567.md)はこの2ファイルを所有する「静的地図レイヤーモジュール」を2026-09-05に
完了と記載している**が、実際には一掃されていない。`NARRATIVE_PATTERN`は既存分をフル実行の
参考件数としてしか出さない（新規混入だけを`--staged`/`--since`でブロックする）ため、この
取り残しは誰にも通知されない。

**T567のエントリにこの事実を注記する必要がある**——完了判定の根拠が実態と合っていない。
→ **2026-09-16、[T898](T898.md)で注記済み**（T567末尾「完了判定の取り残し」節）。この段階は
DEFERの対象ではなかったため、本タスクとは別の`- [ ]`行として切り出して実施した。

## [T810](T810.md)との境界

同じ「コメントを一掃する」でも対象が違うので寄せない。

- **T810**: 書いてある**内容が間違っている**もの（実在しない識別子・成立しなくなった前提）。放置すると読んだ人が誤った判断をするため**即時**。検知器は`dead_identifier_refs`（射程拡大がT810の中身）。
- **T826**: 内容は**正しいが書き方が方針違反**なもの（経緯＝いつ・誰の指摘で・以前は〜だった）。誤った判断は生まないため**DEFER**。検知器は`source_narrative`（新規混入は既にブロック済みで、既存分だけが残っている）。

T810を進めるときにこの2ファイルを開くことになるなら、**それがT826のトリガーにあたる**ので同時に片付けてよい。

## 対応方針

1. **実施済み（2026-09-16、[T898](T898.md)）**: [T567](T567.md)へ「静的地図レイヤー
   モジュールの完了時に45件が残っていた」ことを注記する（これはトリガーを待たず先に行う。
   完了判定の誤りを放置しない）。
2. トリガーが来たら、`docs/conventions/comments.md`の方針どおり経緯を落とす。残す価値のある背景は
   対応する`docs/records/tasks/Txxx.md`へ寄せる（**新規に書き足す前に、既にそこにある背景と
   重複していないか確認する**）。
3. `secondaryAxes.ts:5-8`と`:114-119`の自己矛盾（ヘッダーが「代役へのポインタだけの行として
   表示する」と書き、本文が「その案内文は撤去した」と書く）も同時に直す。

## 関連

- 由来: 統合レビュー第8回のP3（経緯コメント64件の分布）。全体像は [history/2026-09-13_all.md](../../../.claude/commands/review/history/2026-09-13_all.md)。
- 数え上げの一掃は[T824](T824.md)、記述のずれは[T822](T822.md)。

## 台帳にあった補足

- **DEFER**、トリガー: この2ファイルを実装目的で次に編集するとき。経緯コメント64件中45件がここに集中しており、T567は完了と記載しているが一掃されていない——T567への注記はトリガーを待たず行う

## T1001へ統合（2026-09-23）

中身を[T1001](T1001.md)「段階Dの方針」D-2へ写した。台帳・T1001の段階表では段階Cに充当されていたが、
2ファイルは段階Dで`lib/axisCatalog.ts`側へ移す対象（T1060の基準3）で、移すときに全行を読むため
段階Dへ置き直した。写すときの現況（master `7efba1b5`）: 粗い目安のgrepで`axisLayers.ts` 20行・
`secondaryAxes.ts` 2行。対応方針3の自己矛盾（「代役へのポインタ」と「その案内文は撤去した」）は
`grep -n "代役\|案内文" secondaryAxes.ts`が0件で、本文から既に無くなっている。
