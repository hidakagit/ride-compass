# T1162. 並行実行 第3回の改善（規約・道具の直し）と回の記録を持つ

状態: 未完了（2026-09-26起票）

## 背景

orchestration.md「回ごとに残す記録」により、回の始めにその回の改善と記録を持つタスクを起票し、回の終わりに閉じる（前の回まではT1049へ積み足していたが、閉じる時機が来ない形になったため回ごとに分けた）。第3回の目的は周期レビュー第13回の指摘（T1127〜T1160）とT1115・T1118・T1161を片付けること。回の間に担当の「摩擦」欄・司令塔の観測から出た改善のうち、実測の裏付けのある規約・道具の直しをこのタスクのコミットで入れ、回の終わりに測るもの（振り出し・完了・保留の数、ロックの待ち、監査の待ち、判断の待ち、CIの赤）をまとめて閉じる。規約・道具の変更が1つも無ければ記録を残さず見送りで閉じる。起票案 proposal-round3-improvement（2026-09-26承認）

## 回の間に出た決定（ダッシュボードから移した。2026-09-26）

- **司令塔の手が空いた間の準備**（T1162-coordinator-prep、決定。ユーザーがチャットで指示、19:20〜19:25）: 司令塔は実装しないが、
  手が空いている間に振り出しを止めないための準備を先回りして行う。①判断の先回り（振り出し待ちで判断が要ると今分かるものを、
  到達を待たずにダッシュボードへ）②前提と重なりの下調べ（読み取りのみ。記録を書いた後にmasterへ入った変更で崩れた前提・
  触るファイル→振り出し待ちの前提を組み直す）③依頼文の準備（回で分かった詰まりどころを定型へ）。順位は「6 定期確認」と
  「7 規約の改善」の間。根拠: T1142が表示案の答えを待った時間、第3回で依頼文に毎回手で足した注意（完全なsha・ビルドしない
  壊し方・大文字小文字の衝突）、下調べで見つかった崩れた前提（T1128の2件・T1147の着手順）。
- **ダッシュボードのタブと優先度**（T1162-dashboard-tabs、決定。19:44）: ダッシュボードをステータス別のタブ（回答待ち・取り込み待ち・
  記録待ち・すべて）にし、回答待ちに優先度を付ける。ページは担当K-boardが作り直した（Version 14）。項目に priority（高＝稼働中の
  担当が待つ／中＝この回で振り出す予定／低＝回の外。無ければ中）と priority_reason を足す。優先度は置いた側か司令塔が付ける。
- **摩擦を控えに溜めない**（T1162-kaizen-to-dashboard、決定。20:00）: これまで司令塔のキュー（board todo）に「T1162の材料」として
  溜めていたが、ユーザーから見えなかった。摩擦・改善の材料は、定期確認のたびに改善案（task はその回の改善タスク）として置く。

## 承認された改善案（ダッシュボードから移した。2026-09-26承認）

- **検証の重い処理**（T1162-heavy-verify）: T1143は検知器を「壊して確かめる」ためにソースを書き換えて本番ビルドし直し、1回約5分を
  数回（予算19分に対し48分）。T1166は本物のnpm ciを検証に使い、出力の文字コード（cp932）の食い違いで待ちが一致せず10分空待ちした
  （予算20分に対し37分）。案: testing.md・orchestration.mdに既定として書く（画面の検知器はテストの中で壊す。数分かかる本物の処理は
  最終確認の1回まで、仕組みの確認は一時的なリポジトリ・入力の差し替えで行う）。T1168（画面系の手元のビルドをCIの成果物で
  代えられるか）とは別で、こちらは確かめ方の既定だけを持つ。
- **EPOCHSECONDSの記述**（T1162-doc-epochseconds）: T1165の担当が観測。開発機のGit Bashは4.4.23で EPOCHSECONDS（bash 5.0から）が
  無く、hook.sh は司令塔のセッションで道具を使うたびに date を1本起こしている。案: 文を事実へ直す（挙動は変えない）。

## 段階A（文書）で入れたもの

- orchestration.md「司令塔の役割」「司令塔の作業の優先順位」: 手が空いた間の準備の3つを書き、順位7に置いた（規約の改善は8へ）。
  `board todo push`の既定の順位（7）は段階Bで8へ直す（道具の変更なので段階Bに含める）。
- orchestration.md「ユーザーへのお願いはダッシュボードに置く」の摩擦の項: 控えを作らず、定期確認のたびに改善案として置く。
- orchestration.md「重い処理を減らす」・testing.md「わざと壊すのは、テストの中で」（新設）: 検知器はテストの中で壊す・数分かかる
  本物の処理は最終確認の1回まで・道具は一時的なリポジトリと入力の差し替えで確かめる。
- orchestration.md「管理のループ」と hook.sh のコメント: EPOCHSECONDS は bash 5.0からで、開発機のGit Bash 4.4.23にも無い
  （開発機で `$BASH_VERSION`→4.4.23(2)-release、`/bin/sh -c 'echo ${EPOCHSECONDS:-なし}'`→なし）。挙動は変えていない。
- orchestration.md「エージェント共通ルール」に「コマンドの書き方（隔離の検査に拒否されにくく）」を新設（改善案 T1162-agent-tooling の③）。
  依頼文の定型（よく抜ける規則）に、ディレクトリ単位のeslint・数分かかる本物の処理は最終確認の1回まで・問いを置くとき sent_at は
  書かない、を足した（10行のまま）。
- asking-user.md「仕掛中のダッシュボード」: タブを4つ（回答待ち・取り込み待ち・記録待ち・すべて）の表にし（ページ Version 14 の
  定義をページのソースから読んで写した）、項目に priority・priority_reason を足し、sent_at はページだけが書く（問いを置く側は
  書かない）とした（改善案 T1162-dashboard-sent-at の文書側）。
- asking-user.md「起票は承認制」: 承認されるまでの起票案は正本として扱わない・承認されたときにその時のmasterの最大＋1で番号を振る
  （番号を予約しないので後から承認されてもぶつからない。ユーザーの方針「承認時に付番する。それまでは正として扱わない」、20:14）・
  チャットの承認は聞いた側が answer へ書いてから道具を呼ぶ・台帳と記録を手で書く経路は残り機械では止められない
  （改善案 T1162-new-task-gate の規約側）。

所要（並行実行）: 段階A 依頼 2026-09-26 20:13 → 完了 20:20（7分）、うち枠待ち0分・CI待ち0分、実績 +1/−1
