# T1154. 規模ウォッチの閾値が今回の発火と縮んだファイルに合っておらず、総量の「実装」に生成物と道具が混ざっている

状態: 完了（2026-09-27）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 閾値の緩み（T1030の規則「現在値+15%を100行単位へ切り上げ」どおりの定期作業）・総量の内訳の欠け。scripts/size_thresholds.jsonを次のとおり直す（周期レビュー第13回でユーザーが明確なものは判断不要と包括承認）: 追加 frontend/src/types/generated/api.d.ts 3,800・frontend/src/app/page.test.tsx 2,000・docs/conventions/testing.md 1,400（次の発火では運用の節を別文書へ出すのを既定の対応にする）・scripts/orchestration/core.py 2,300（R22の後に残った最大のファイルへ置き直す）。変更 frontend/src/features/map/MapOverlayControls/MapOverlayControls.tsx 1,400→800、frontend/src/app/page.tsx 2,600→1,700。削除 frontend/src/features/map/MapView/MapView.tsx（1,000行の常時発火の線のほうが早く鳴る）。material_catalog.py（85%）は1,400到達時に一次属性の表を別モジュールへ出す、と分割先を決めておく。あわせて`review_checks.py metrics`の総量に「うち生成物（types/generated/）」「うち道具（scripts/・backend/scripts・frontend/e2e*）」の列を足す（第13回は実装+1,930が製品−3,670・生成物+720・道具+5,021で、手で分けないと読めなかった）。報告なので検知器の条件には当たらない。

## 実施（2026-09-27）

- `scripts/size_thresholds.json`: 第13回の決定どおりに追加（api.d.ts 3,800・page.test.tsx 2,000・testing.md 1,400・orchestration/core.py 2,300）・変更（MapOverlayControls.tsx 1,400→800・page.tsx 2,600→1,700）・削除（MapView.tsx）。
- 決定: `backend/app/domain/evaluation.py` 1,150→500。第13回の時点は607行（53%）で見直しに出ていなかったが、起票後に403行へ縮み35%で「閾値の見直し」に出た。T1030の規則（現在値403の+15%を100行単位へ切り上げ）で下げた。1,000行の常時発火の線より早く鳴るので外さない（MapView.tsxを外した理由の裏返し）。
- 発火したときの既定の対応を`size_thresholds.json`の`on_fire`へ置き、`review_checks.py size`が発火の一覧の下に出す: material_catalog.py（一次属性の表を別モジュールへ）・testing.md（運用の節を別文書へ）。core.pyの「残った最大のファイルへ置き直す」は発火で鳴る話ではないので、仕掛中のダッシュボードにT1148の件（kind 決定、T1148-core-threshold）として置いた。
- `review_checks.py metrics`の総量に実装の内訳（うち製品・うち生成物・うち道具）の行を足した（記録の「列」は行にした——テスト・文書の行に空欄の列が並ばないため）。道具は記録の`scripts/`・`backend/scripts/`・`frontend/e2e*`に、性質が同じ`backend/benchmarks/`とテストランナーの設定（`frontend/playwright*`・`frontend/vitest*`）を加えた。加えないと道具が第13回の手計算より40行少なく出た。
- 第13回のタグ注釈の内訳「製品−3,670」は足し算が合っていない（−3,670+720+5,021=+2,071≠+1,930）。この実装で12→13を測ると製品−3,811・生成物+720・道具+5,021で、生成物と道具は注釈と一致し、製品は+1,930からの差し引きと一致する。

## 検証

- `python scripts/review_checks.py size`: 発火7件→3件（残りはslots.py +55%・pending.py +42%・orchestrate.md +15%。第13回のタグ以降の成長で次の周期レビューの分類対象）、閾値の見直し2件→0件。core.pyは2,176行で到達率95%（第13回以降にT1162で+234行）。
- scratchpadの検証スクリプトで`periodic-review/012`→`013`の総量を内訳付きで算出: 実装+1,930＝製品−3,811・生成物+720・道具+5,021、実装＝3つの和。
- 同じスクリプトでmaterial_catalog.pyの閾値を1,000へ下げた一時JSONを読ませると、発火の一覧の下に「既定の対応: 一次属性の表…」が出る。
- `ruff check scripts/review_checks.py`・`review_checks.py docs`: 指摘0・違反なし。

所要（並行実行）: 依頼 2026-09-27 00:12 → 完了 00:58（46分）、うち枠待ち0分・CI待ち0分（完了のコミットの後に1回）、規模札S、実績 +38/−9 → S
