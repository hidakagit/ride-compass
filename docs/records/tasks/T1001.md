# T1001. frontendを仕様から起こし直し、既存とdiffを取って再構成する 規模L

状態: 未完了（2026-09-22起票・着手）

[T996](T996.md)（backendを全件読む）の対になるfrontend側。ユーザー指示は
「責務に対して実装量が明らかに過大」「数件ピックアップしてその実装のテストを確認したところ、
運用に耐えられるレベルのものではなかった」「初期描画・状態遷移時の導線、類似部品の共通性のなさ、
階層構造がなく一様に動いている部分、地図制御の状態整理」。

## 起票時の測定値

母集団: `frontend/src/**/*.ts(x)` から `.test.`・`types/generated/`・`.bench.` を除いた186ファイル
（2026-09-22、対象 59c1439a）。CSS Moduleとテストは別枠で数えた。

| 測ったもの | 値 |
|---|---|
| 実コード / 説明 / テスト | 19,053行 / 7,221行（比0.38） / 23,561行（対実コード1.24） |
| export | 793本。うち**実装から参照されないもの213本**（どこからも参照なし87・テストからのみ参照126） |
| `app/page.tsx` | 実コード1,761行・useState 34・`localStorage`直接参照12・子コンポーネント58種・export 0 |
| `MapViewProps` | 46項目（次点は`RouteFormProps`の17） |
| `components/Map/MapView.tsx` | 3,446行（実コード2,226）・useEffect 36・useRef 19・`map.*`呼び出し120箇所24種・export 46（うち34はテストからのみ参照） |
| CSS Module | 45件5,365行・クラス673個（異なり418）・`composes` 76箇所。同名クラスが別モジュールに重複（`.table` 22・`.panel` 12・`.error` 11）。button/chip/toggle系クラスが23ファイルに62個 |
| ディレクトリ | `components/`直下に30ディレクトリが横並び。`components/Map` 10,692行40件と`components/ErrorText` 10行1件が同列。`components/ui`は5ファイル169行 |
| hooks / services | hooks 21本（実際にfetchを持つのは4本）・services 16ファイル43関数 |

測り方: `frontend/src`を走査し、行を「docstring/コメント」「空行」「実コード」のどれか1つに数える。
exportは宣言（`export function|const|class|type|interface|enum`・`export {}`）を正規表現で集め、
同名識別子が自ファイル以外に出現するかで参照の有無を判定する
（型の内部利用は参照に数えない。この判定は過大に「未参照」を出しうるため、消す前に1件ずつ確認する）。

## 進め方（両面から確認する）

**「今あるものから無駄を省く」形にしない**——省くかどうかの判断基準が既存実装の形に引きずられ、
揺らぐ。[T998](T998.md)がテストで採った形を実装にも採る。単位は下記「段階」の1つ。
T998側で手順の穴が4つ見つかり`docs/conventions/testing.md`へ入っている（旧版を物差しにしない・分岐網羅を目標にしない・埋める前に入力が作れるか確かめる・残すと決めたら漏れた理由を源へ戻す）。着手前にそちらを読むこと。

1. 対象の責務を**仕様から起こす**（`docs/modules/frontend/*.md`・
   [design-principles.md](../../architecture/design-principles.md)・画面の実物）。**既存実装は開かない**。
2. 起こした形で実装を書く。
3. 既存とdiffを取り、**両方向を判定する**:
   - **新版にしか無い**: 既存が持っていなかった。入れる。
   - **旧版にしか無い**: 3つに分ける。
     - **今も要る**（仕様の読み落とし・docsに書かれていない挙動）→ 残し、**なぜ仕様から起こせなかったかを
       docs側へ1行足す**。これを足さないと次に起こし直す人も同じ落とし方をする。
     - **過去の不具合を刺している**（実装から復元できない知識）→ 残し、理由を1行書く。
     - **到達しない・写経・撤去済み機構の見張り** → 消す。
4. 同じ単位でテストも起こし直す（T998の手順）。**実装を内部化すると「テストからのみ参照」の126本は
   必ず触ることになるため、テストと分けない**。

## 目標構成

1. **地図は「望ましい状態を純関数で組み立て、1本の適用器が差分を当てる」**。設計原則 構造仕様8が
   「一覧を1か所へ宣言して各経路がそれを参照する形は1本道ではない」と定めているのがこの形。
2. **`page.tsx`の状態を意味の単位へ分ける**。`localStorage`は`useStoredState`1本だけを通す。
3. **UI基盤を実体化する**（Button/Chip/Panel/Field）。CSSは`composes`前提へ。
4. **層をディレクトリに出す**: `app/`（コンポジション）→ `features/`（機能）→
   `components/ui/`（プリミティブ）→ `lib`・`services`（I/O）。
5. **到達しない公開ドアを畳む**。

## 段階

着手順。1段階=1コミット以上とし、段階の終わりにCLAUDE.md「テスト方針」の①静的検査→②該当テストを通す。
既存タスクを充当する段階は、その段階の完了をもって元タスクを`[x]`にする。

**2026-09-23に寄せたもの**（下の「保留（ユーザー判断）」節の決定）: 充当欄の「統合」は元タスクを
閉じて中身を本ファイルの各段階の節へ写したもの（元記録は読まなくてよい）、「一部」は元タスクが
他の範囲を持って残り、寄せた項目だけを本ファイルへ写したもの。どちらも**段階の完了条件に
含める**——段階を閉じる時点で、写した項目がそれぞれの完了条件を満たしていること。

| 段階 | 内容 | 充当する既存タスク |
|---|---|---|
| A | 地図の状態適用を1経路へ | [T994](T994.md) |
| B | `page.tsx`の状態を意味の単位へ分ける | [T1013](T1013.md)の項目1・2（統合）・[T1010](T1010.md)の項目2・4（一部） |
| C | UI基盤とCSSの共通化 | [T629](T629.md)・[T953](T953.md)・[T715](T715.md)（いずれも統合） |
| D | ディレクトリを層・機能単位へ再配置 | [T1060](T1060.md)・[T826](T826.md)・[T1013](T1013.md)の項目3（いずれも統合） |
| E | 到達しない公開ドアの内部化・削除 | [T968](T968.md)の段階6・7（一部）・[T1055](T1055.md)の残り4（一部） |
| 手順4 | 各段階のテストの起こし直し | [T981](T981.md)のfrontendの共通部品（一部）・[T998](T998.md)の「frontendで見つけたが直さないもの」の残り2件（一部） |

段階Dはimportが全ファイルへ波及するため最後に置く。並行セッションの変更と衝突しやすいので、
着手前に`git fetch`して他セッションの作業中ファイルを確認する。

## 完了条件

- 段階ごとに、3の判定件数（新版のみ／旧版のみ①今も要る②回帰③消した）をこのエントリへ書く。
  **①の件数がdocsの不足の実測値**、**③の件数が削減の実測値**。
- 段階ごとに実コード・CSS・テストの行数を測り、増減をこのエントリへ書く
  （`python scripts/review_checks.py metrics`と上記「測り方」）。
- 変更が届いた`docs/modules/frontend/*.md`を同一コミットで追従させる。
- 他のタスクから寄せた項目（段階表の充当欄。中身は「段階Bの進め方」の「寄せた項目」・
  「段階Cの方針」の「寄せた項目」・「段階Dの方針」・「段階Eの方針」・「手順4（テスト）へ寄せた項目」）が、
  それぞれの段階を閉じる時点で各項目の「完了」を満たしている。一部だけ寄せた元タスク（T1010・T968・
  T1055・T981・T998）には、段階を閉じたときにその旨を1行返す。
- 画面を開いたときに、Reactのハイドレーションの不一致（#418）と、地図の`Source "area-elevation" already exists.`の例外が出ないこと（T1077のe2e-liveで開発DB・実backendの画面を開くと毎回出ていた。ユーザー決定 2026-09-23でこのタスクへ寄せた。確かめ方は`frontend/e2e-live/`のS1〜S3がページの例外0件で通ること）

## 起票時の概算（未計測。段階Aの実測で置き換える）

実コード19,053行 → 12,000〜14,000行、CSS 5,365行 → 3,500〜4,000行、
テスト23,561行 → 11,000〜14,000行。テストの比率だけはT998の実測3件
（`axis_display` 960→429行、`AxisComposer` 978→249行）からの外挿で、実装側は根拠が無い。

## 段階B以降の進め方（2026-09-22、段階Aでたどり着いた形）

段階Aは「地図の状態適用を1経路へ」から始めたが、**実際に効いたのは別の軸**だった。
同じ軸を段階B以降にも当てる。会話で決めた判断は次のセッションへ届かないのでここへ残す。

### 1. フロントが「定義・宣言・源泉」を持っている時点で疑う

画面は配信されたものを出すだけ。**残ってよいのはbackendが知りえない、描画媒体の事実
だけ**——MapLibreの式の書き方、指で押せる幅、ライト/ダークで2値を持つ色、アイコンの
識別限界（WMOコードを6種へ丸める等）。それ以外（分類・束ね方・行の名前・色・閾値・
タイルの範囲・出典・レイヤーの一覧）は源泉側。

判定は「全体で唯一か」ではなく**「源泉であり、どこからも導出できないか」**。唯一でも
写しなら消える。

### 2. 照合テストがあること自体が、写しがある証拠

「生成物と一致するか」「backendと同じか」を見る検査は、**一致させたい2つがあるから**
存在する。写しを消せば検査ごと消える。逆に、写しを消したのに検査だけ残ると
`x === x` になる（実際に2件あった）。**写しを消したら、その写しを見張っていた検査も
同時に消す。**

### 3. テストが実装の内部を覗いているのは、置き場が違う印

「テストからのみ参照されるexport」は、口を閉じる前に**そこにあるべきかを見る**。

- 画面部品の中の純関数（整形・丸め・並べ替え）→ `lib/`へ移す
- 状態を地図へ当てる一連 → `features/map/scene/`へ移す
- 移せば口は正当になり、テストはそのまま使える

**他ファイルへ実行時に依存する道具を、テストから直接叩かない**——自分は何も変えて
いないのに、他人の変更で落ちるテストになる。型だけの依存・同一モジュール内は
これに当たらない（型が変われば先にコンパイルが落ちる）。

### 4. 機械の出力をそのまま処置へ繋げない

grep・参照カウントが出すのは候補。CLAUDE.md「修正の原則」に立てた（実例: 参照0の
一覧にNext.jsが読む`metadata`が入り、`viewport`とroute handlerは語の偶然で残った）。
**構文や名前で性質を決めない**——「値をimportするテストは写経だろう」と見込んだ4件は
4件とも正当だった（CSSの`@media`との突き合わせ、境界の振る舞い）。

### 5. 段階Aの実測（段階B以降の基準）

| | 着手前 | 段階A後 |
|---|---:|---:|
| frontend 実装 | 27,953行 | 26,900行台 |
| frontend テスト | 24,225行 | 22,500行台 |
| `MapView.tsx` | 3,382行 | 1,728行 |
| export 合計 | 793本 | 640本台 |
| どこからも参照なし | 87本 | 3本 |
| テストからのみ参照 | 126本 | 50本台 |

**人が書く分は−4.4%にとどまる。** 家族へ分けたぶんが相殺しており、純減は源泉への移設
から出ている。段階B以降も同じで、**分けるだけでは減らない**。

## 段階Aの設計方針（2026-09-22、ユーザー承認）

仕様（`docs/modules/frontend/static-map-layers.md`・`map-axis-coloring.md`・
`dynamic-weather-layers.md`）から起こした。**挙動を変えないもの**:

1. **地図の中身を状態の純関数にする**。`buildMapScene(state)`（純関数）→
   `applyMapScene(map, scene, prev)`（唯一の書き手）。`setStyle`後は`prev`を空にして同じ
   関数を呼ぶため、再描画専用の経路を持たない。source/layerの追加・filter・feature-state・
   visibilityはすべてsceneが持つ——docsが「redrawから辿れない描画は消えたまま戻らない」
   「明示的に再適用しないと無色のまま残る」と暗黙の前提として警告している条件が、
   構造として発生しなくなる。
2. **`MapView`の受け口はscene 1つ＋イベント**。組み立てが純関数になるため、jsdomのcanvas
   制約で今テストが無いレイヤー登録（`static-map-layers.md`明記）を単体テストで書ける。
3. **重なり順を1つのリストへ**。段（`paintTier`）が「基礎地図の下／上」も持ち、面かどうかを
   レイヤーの`type`から推測しない。
4. **データ取得状態は記述子の1契約へ**（`mapViewLayerDataStatus`と
   `dynamicWeatherDataStatus`のマージを消す）。
5. **派生だけのフックを純関数へ**（hooks 21本のうち実際にfetchするのは4本）。

**挙動が変わるもの**（4件ともユーザーが採用を選択）:

- **A. 段（band）キーの綴りを統一する**。いまramp軸は`${axisId}-${index}`、専用way値配信軸は
  `step-N`で、ルート生成をまたいで隠した段が保たれるかが軸の種類で変わる。統一すると
  どの軸でも保たれる。**保存済みの旧キーは1回だけ無効になる**（隠していた段が一度戻る）。
- **B. 「段を隠す」を透明色へ一本化する**。`filter`（ramp軸）と透明色（専用配信軸。
  feature-stateは`filter`から読めない）の2実装を1つにする。**透明な線のクリック判定が残る**
  ため、当たり判定側も同じ非表示キーを見るよう揃える（揃えれば利用者から見た挙動は同じ）。
- **C. 初期表示の覆いを`idle`待ちからやめる**。基礎地図が描けた時点で外す
  （`INITIAL_TILES_OVERLAY_MAX_MS`は保険として短く残す）。覆いが外れるのが早くなる。
- **D. レイヤーidを規約から導出する**。id定数のexportを廃止し、記述子から導く
  （動的気象の`region-dynamic-weather-${id}-${source}-${sub}`と同じ形へ揃える）。
  テストからのみ参照されている34本のexportはここで消える。

**新しいコードの置き場**: `frontend/src/features/map/`（段階Dの目標構成の位置）。新規ファイルは
importが増えるだけで既存の参照を動かさないため、先に目標位置へ置いて段階Dの移動量を減らす。

## 遷移表: 旧実装が地図へ出す呼び出し

差分判定で使う。新実装と旧実装へ同じ筋書きを通し、地図への呼び出し列を
突き合わせるための、**旧実装側の一覧**。

母集団の出し方（2026-09-22、対象 16942979）:

```
grep -rn "\.\(addSource\|addLayer\|removeLayer\|removeSource\|setPaintProperty\|setLayoutProperty\|setFilter\|setFeatureState\|removeFeatureState\|moveLayer\|addImage\|setStyle\)(" --include=*.ts --include=*.tsx frontend/src | grep -v "\.test\."
```

→ `MapView.tsx` 37・`MapView.routes.ts` 26・`mapStyleOps.ts` 3。きっかけ側は
`MapView.tsx`の`useEffect`36本（依存配列は36/36が機械で取れる）と`map.on|once` 20箇所
（click, error, idle, load, mousemove, moveend, resize, sourcedata, sourcedataloading,
style.load, styledata, zoom, zoomend）。

`setLayerVisibility`（`mapStyleOps.ts`）は`setLayoutProperty("visibility")`の薄い包みで、
**レイヤーが無ければ何もしない**。下表では「表示」と書く。

---

### `MapView.routes.ts`（26箇所）

全入口が`runWhenStyleReady`で包まれる。**スタイル読み込み前に呼ぶと実行が遅延する**ため、
呼び出し側の順序と実際の適用順は一致しない。

| 入口 | 前提 | 地図への呼び出し（この順） | 順序・時機の制約 |
|---|---|---|---|
| `drawBaseRoutes(routes, selectedId, excludeSelected)` | — | ソース有: `setData`／ソース無: `addSource(route-candidates)` → `addLayer(候補線)` → `addLayer(当たり判定)`。以後どちらでも 候補線・当たり判定を表示 | **毎回表示を明示する**（`hideBaseRoutes`が`none`にした後、`setData`だけでは戻らない）。`excludeSelected`は選択中候補を除く（色分け線の下から単色線が透ける） |
| `hideBaseRoutes()` | — | 候補線・当たり判定を非表示 | — |
| `drawSelectedOutline(routes, selectedId)` | — | ソース有: `setData`／ソース無: `addSource(route-selected-outline)` → `addLayer(ハロー, beforeId=候補線があればその直前)` → `ensureRouteArrowLayer`。以後 ハロー・矢印ハロー・矢印を表示 | **矢印の生成はソース作成時の1回だけ**。`beforeId`は**その瞬間に候補線があるか**で変わる（無ければ最前面） |
| `hideSelectedOutline()` | — | ハロー・矢印ハロー・矢印を非表示 | — |
| `applyRouteLayerVisibility(on, routes, selectedId, hasDetail)` | — | on: `drawBaseRoutes`→`drawSelectedOutline`／off: `hideBaseRoutes`→`hideSelectedOutline` | 呼び出し元3箇所（effect2つと再描画）が個別に判定を書かないための集約点 |
| `drawSpliceStretches(stretches)` | — | ソース有: `setData`／無: `addSource(route-splice-stretches)` → `addLayer(帯)` → `addLayer(当たり判定)`。以後 両方を表示 | 帯は候補線より**上**（下に敷くと差し替え先が隠れる）。当たり判定は帯と別レイヤーで、**クリックは当たり判定だけへ登録**（両方だと1タップで2回発火する） |
| `hideSpliceStretches()` | — | 帯・当たり判定を非表示 | — |
| `drawSplicedRoute(coordinates)` | — | ソース有: `setData`／無: `addSource(route-spliced-current)` → `addLayer(縁取り)` → `addLayer(線)`。以後 両方を表示 | **縁取りを先に積む**（同じ`beforeId`なら先が下） |
| `hideSplicedRoute()` | — | 縁取り・線を非表示 | — |
| `ensureRouteArrowLayer()` | 矢印レイヤーが無いとき | `addImage(矢印, sdf)`（未登録なら） → `addLayer(矢印ハロー)` → `addLayer(矢印)` → `keepRouteArrowsAboveDetailSegments` | ソースは`route-selected-outline`を流用（専用ソースを持たない）。衝突判定を無効にする（有効だと下のハロー層が全部落ちる） |
| `keepRouteArrowsAboveDetailSegments()` | 矢印ハローがあるとき | `moveLayer(縁取り)`・`moveLayer(色分け線)`・`moveLayer(当たり判定)` をいずれも矢印ハローの直前へ | **作成順が2通りある**（矢印が先／色分け線が先）ため、両方から「色分け線→当たり判定→矢印ハロー→矢印」へ収束させる |
| `drawExperimentSlots(slots)` | — | ソース有: `setData`で終了／無: `addSource(experiment-slots)` → `addLayer(縁取り, beforeId=詳細の縁取り→色分け線→無し の順で存在する方)` → `addLayer(線, 同じbeforeId)` | `beforeId`が**その瞬間の詳細レイヤーの有無**で3通りに変わる |
| `drawDetailSegments(segments, mode, hiddenKeys)` | — | ソース有: `setData`／無: `addSource(route-detail-segments)` → `addLayer(縁取り, beforeId=矢印ハローがあればそれ)` → `addLayer(色分け線, 同)` → `addLayer(当たり判定, 同)`。**以後は毎回**: `setPaintProperty(色分け線, line-color)` → `setFilter(色分け線)` → `setFilter(縁取り)` → `setFilter(当たり判定)` → 3枚を表示 | 色式と絞り込みは**ソースの有無に関わらず毎回当てる**（モード切替・凡例の絞り込みがこの経路で反映される）。縁取り・当たり判定にも同じ絞り込みを当てる（隠した段の縁だけが残らない／クリックできない） |
| `hideDetailSegments()` | — | 縁取り・色分け線・当たり判定を非表示 | — |

**地図を触らない純関数**（差分の対象外）: `routesToFeatureCollection`・`segmentsToFeatureCollection`・
`spliceStretchesToFeatureCollection`・`restoreRouteSegmentProperties`・`computeRouteBounds`。

### この表から出てくる筋書き（新旧へ同じ順で通すもの）

1. 候補線を出す → 隠す → もう一度出す（**表示の明示が効いているか**）
2. ルート未確定で選択中ハローを出す（矢印が生成される）→ その後で詳細を出す（**矢印が上に残るか**）
3. 先に詳細を出す → 後からハローを出す（**逆順でも同じ重なりへ収束するか**）
4. 比較スロットを、詳細がある状態／無い状態それぞれで出す（**beforeIdの3通り**）
5. 詳細のモードを切り替える・凡例の段を隠す（**毎回の色式と3枚のfilter**）
6. 帯を出してから当たり判定をタップする（**二重発火しないか**）
7. スタイル差し替え後に全部を作り直す（**runWhenStyleReadyの遅延をまたいでも同じ列か**）

### 仕様に書かれていないもの（新実装は穴として返す想定）

レイヤーid・ソースidの綴り、色（`#2563eb`・`#64748b`・`#1e3a8a`・`rgba(15,23,42,0.8)`・
`#c2612b`）、太さ（候補線5/2.5・ハロー10・帯3〜5・当たり判定18/24・詳細6・縁取り10・
合成7+4・スロット4/7）、不透明度、矢印の間隔80px・基準倍率0.8・ハロー1.5倍。

---

### `MapView.tsx`（書き換え37箇所・effect 36本・購読20箇所）

抽出は機械で行う（`useEffect`の依存配列と本体の呼び出し、`map.on|once`の購読）。
コマンドは本ファイル末尾。

### きっかけ（effect 36本の内訳）

| 種別 | 本数 | 中身 |
|---|---|---|
| propsをrefへ写すだけ | 16 | 地図を触らない。**筋書きの入力**であって、きっかけではない |
| マーカー・ポップアップ（DOM） | 4 | 経由地・目的地・区間選択。地図レイヤーは触らない |
| カメラ | 2 | 現在地の変化で`flyTo`、候補一覧の変化でフィット |
| **地図の中身を変える** | **14** | 下表 |

| # | 依存（きっかけ） | 出る呼び出し |
|---|---|---|
| 23 | routes, selectedRouteId, routeLayerOn, selectedCandidate | `applyRouteLayerVisibility` |
| 24 | spliceStretches, splicedRoute, routeLayerOn | `applySpliceLayerVisibility` |
| 26 | experimentSlots | `drawExperimentSlots` |
| 27 | routes, selectedRouteId, routeLayerOn, routeStyleModes, routeStyleModeId, hiddenRouteLegendKeys | `drawDetailSegments` / `hideDetailSegments` |
| 28 | staticLayerVisibility, dedicatedWayValueVisibility, axisVisibility, staticOverlayLayers | `setStaticOverlayVisibility`（各レイヤーの`ensure`＋表示） |
| 29 | dedicatedWayValues | `applyAxisFeatureStateValues` |
| 30 | dedicatedWayValueVisibility | `clearRoadTileFeatureState` |
| 31 | dynamicWeather | `applyDynamicWeatherState` |
| 32 | staticLegendHiddenKeysByAxis, staticOverlayLayers, staticFilterAxes | `setStaticOverlayFilters`（`ensure`＋`setFilter`） |
| 33 | staticLayerVisibility, roadHiddenKeysByMode | `applyRoadLayerState` → `applyRoadMaterialTrackOffsets` |
| 34 | refreshToken | `resetBasemapAreaLayerPreparation` → `map.setStyle` → `once("style.load")`で再描画 |
| 35 | tileVersionsReady | `runWhenStyleReady`で再描画 |
| 36 | roadPopup | `ensureRoadSurfaceTileLayer` → `applyInspectedWay`（片付けで`null`へ戻す） |
| 17 | （マウント、依存なし） | `addControl` → `map.on`各種 → `prepareBasemapForAreaLayers` → `ensureRoadSurfaceTileLayer` → `ensureAllStaticOverlayLayers` |

### 地図から来るきっかけ（購読20箇所・13種）

`styledata`/`sourcedata`（出典表記を畳み直す）・`style.load`（面の差し込み位置を解決）・
`click`（全体＋区間/候補/帯の3レイヤー限定）・`mousemove`（カーソル）・`zoom`（ズーム不足の案内）・
`load`・`error`・`moveend`・`zoomend`・`resize`・`sourcedataloading`/`sourcedata`（取得状態）・
`idle`（取得状態の再計算）・`once("idle")`（初期表示の覆いを外す）・`once("style.load")`（再描画）。

### 適用関数が出す呼び出し

| 関数 | 前提 | 呼び出し（この順） |
|---|---|---|
| `applyRoadLayerState` | — | `ensureRoadSurfaceTileLayer` → 2レイヤーそれぞれ: 表示 → 非表示ならそこで次へ → `setPaintProperty`(色・太さ・不透明度) → `setFilter` |
| `applyRoadMaterialTrackOffsets` | レイヤーがある分だけ | `setPaintProperty(line-offset)`（ON中の本数から対称に割り付け） |
| `setStaticOverlayVisibility` | — | 各レイヤーで `ensure` → 表示 |
| `setStaticOverlayFilters` | 絞り込み軸を持つレイヤーのみ | `ensure` → `setFilter` |
| `applyInspectedWay` | **レイヤーが無ければ何もしない** | 表示 → `setFilter(osm_way_id)` |
| `applyAxisFeatureStateValues` | **ソースが無ければ戻る** | way1件ごとに `setFeatureState` |
| `clearRoadTileFeatureState` | ソースがあるとき | `removeFeatureState`（**ソース単位で全キー**） |
| `redrawAllLayers` | — | 表示の一括適用 → 動的気象 → feature-stateの再適用 → 絞り込み → 路面の状態 → トラックの割り付け（カメラは動かさない） |

### 時機の制約（差分に出ない／出にくいもの）

1. **再描画は連打ガードを持つ**。`style.load`が確定するまで次の`setStyle`を出さない。1回目が来ないと以後の再描画が出ない
2. **世代の到着は`isStyleLoaded()`で早期returnしない**。準備中に届くと二度と走らずタイルが永久に出ないため`runWhenStyleReady`を通す
3. **`clearRoadTileFeatureState`はソース単位で全キーを消す**。1軸だけOFFで呼ぶと、まだONの軸の色まで巻き添えになる
4. **`applyInspectedWay`はレイヤーがある間だけ効く**。`setStyle`後は再描画が復元しないと強調だけが消える
5. **refへ写す16本のeffectは、effectの宣言順に依存する**。地図を触るeffectより前に最新値が入っていることが前提

### 抽出コマンド

```
python scripts/... （scratchpadのextract_effects.py。useEffectの依存配列と本体の呼び出し、map.on|onceの購読を抜く）
```

### 面（色別標高図・土地被覆・起伏の陰影）

| 入口 | 前提 | 呼び出し | 制約 |
|---|---|---|---|
| 各`ensure`（3本、`buildStaticOverlayLayers`のエントリから呼ばれる） | `runWhenStyleReady` | ソース無: `addSource` → `ensureLayerFromSpec`（`layout.visibility: "none"`で作る） | **3本とも同型**（ラスタ2本・raster-dem 1本）。タイル世代に依存しない（世代ゲートの対象外） |
| `setStaticOverlayVisibility` | — | 各レイヤーで`ensure` → 表示 | 表示の切替は他の静的レイヤーと同じ1本の経路 |

- **筋書き**: ①3つを個別にON/OFF ②スタイル差し替え後に元の表示へ戻る ③面が基礎地図の道路網より下に入る
- **仕様に無いもの**: タイルのパス・ズーム上限・出典文言・陰影の色/方向/倍率・面の濃さ
- **backendに正本がある値**: 起伏のズーム上限は`services/terrain_tile_service.py: TERRAIN_TILE_MAX_ZOOM`、Terrain-RGBの原点は`domain/terrain_rgb.py`。**フロントに写しがある**（差分判定で扱う）

### 路面の線（路面の種類・道路の種類・詳細の強調・トンネル・一方通行・指定路線）

| 入口 | 前提 | 呼び出し | 制約 |
|---|---|---|---|
| `ensureRoadSurfaceTileLayer` | **タイル世代が届くまで何もしない**。`runWhenStyleReady` | `addSource`（`promoteId: feature_key`） → 路面・道路種別の2本を`addLayer` → 詳細強調を`addLayer`（`filter`は`osm_way_id == -1`） | `promoteId`が無いと`setFeatureState`が効かない（**例外も警告も出ず、色が付かないだけ**）。`osm_way_id`では代用できない |
| `makeEnsureAttributeLineLayer`（トンネル・一方通行・指定路線） | — | **自分で`ensureRoadSurfaceTileLayer`を呼ぶ** → `ensureLayerFromSpec`（`specOwnsFilter: false`） | 作り直しはレイヤーごとの`ensure`を順に呼ぶだけで、ソース作成が後に来ることがある。**順序に頼らず自分で用意する**。絞り込みは外側が持つ（`ensure`が利用者の絞り込みを巻き戻さないため） |
| `applyRoadLayerState` | — | `ensureRoadSurfaceTileLayer` → 2レイヤーごとに 表示 →（非表示なら次へ）→ `setPaintProperty`×3 → `setFilter` | 色の意味は**もう一方のON/OFFで変わらない**（1本の線へ2つの意味を載せない） |
| `applyRoadMaterialTrackOffsets` | レイヤーがある分だけ | `setPaintProperty(line-offset)` | ON中の本数から**対称に割り付ける**（1本→0、2本→±、3本→−/0/＋） |
| `applyInspectedWay` | **レイヤーが無ければ何もしない** | 表示 → `setFilter(osm_way_id)` | スタイル差し替えで消える。再描画が復元しないと強調だけが戻らない |

- **筋書き**: ①世代が届く前に表示ONにし、**後から世代が届く** ②路面と道路種別を同時ON（トラックが分かれる）→片方OFF（残りが中央へ寄る） ③凡例で段を隠す→表示ON/OFFを往復（**絞り込みが巻き戻らない**） ④詳細を開いたままスタイル差し替え
- **仕様に無いもの**: ソース/レイヤーidの綴り、線の太さ3px、ズーム範囲、中立色

### 点データ（事故・停止要因POI・補給休憩POI）

| 入口 | 前提 | 呼び出し | 制約 |
|---|---|---|---|
| `ensureAccidentTileLayer` | **世代が届くまで何もしない**（判定は遅延実行の中） | `addSource` → `addLayer`（circle） | 判定を外に出すと、待っている間に世代が届いても待ち始めた時点の答えで戻る |
| `ensureStopPoiLayer` / `ensureSupplyPoiLayer` | 同上 | 共有ソースを`ensurePoiTileSource`で用意 → それぞれ`addLayer` | **同じsource-layerを`kind`の集合で分ける**。`addLayer`時点では絞り込みを付けず、`setStaticOverlayFilters`の`baseFilter`が常時ANDで効かせる |

- **筋書き**: ①世代が後から届く ②停止要因と補給を同時ON（**互いの種別が混ざらない**） ③凡例で種別を隠す
- **仕様に無いもの**: 円の半径・縁の太さ・不透明度・ソースidの綴り

### 軸レイヤー（ramp軸・専用way値配信軸）

| 入口 | 前提 | 呼び出し | 制約 |
|---|---|---|---|
| `makeEnsureDedicatedWayValueLayer`（軸ごと） | — | `ensureRoadSurfaceTileLayer` → `ensureLayerFromSpec`（`specOwnsFilter: true`、色式・不透明度式はspecが持つ） | **段を隠すのは色式（透明）**で、filterではない（feature-stateの値はfilterから読めない） |
| `applyAxisFeatureStateValues` | **ソースが無ければ戻る** | way1件ごとに`setFeatureState` | パン・ズームで鍵が減っても**古い値を消さない**（穴が開くより良い、という判断） |
| `clearRoadTileFeatureState` | ソースがあるとき | `removeFeatureState`（**ソース単位で全キー**） | **1軸だけOFFで呼ぶと他の軸の色まで消える**。`shouldClearDedicatedWayValueFeatureState`が「1つも表示されていない」を全称で判定してから呼ぶ |

- **筋書き**: ①軸Aの値を入れる→軸Bの値を入れる→**Aだけ**OFF（Bの色が残るか） ②両方OFF（値が消えるか） ③スタイル差し替え後に値が戻るか ④凡例で段を隠す（**透明になるだけで、線は残る**）
- **仕様に無いもの**: レイヤーid・feature-stateキーの綴り規則、線の太さ

### 動的気象（raster / gridFill / gridMark / vector）

| 入口 | 前提 | 呼び出し | 制約 |
|---|---|---|---|
| `ensureDynamicWeatherLayer` | `runWhenStyleReady` | グループの各ソースについて、宣言された表現ごとに`addSource` → `addLayer` | **id は規約で決まる**（`region-dynamic-weather-<グループ>-<ソース>-<表現>`）。要素が増えても定数を足さない |
| `applyDynamicWeatherState` | — | `ensureDynamicWeatherLayer` → 各ソース: 中身が来ていれば`setTiles`/`setData`（**変わったときだけ**） → 表示（`visible` かつ **中身の種類が宣言と一致するときだけ**） | 種類が合わないpayloadでは表示しない。**辞書のキー順が重なり順**（面を下・線と点を上） |

- **筋書き**: ①グループ内の一部ソースだけを隠す ②時刻を動かして中身が入れ替わる ③中身が来ていない状態で表示ON ④スタイル差し替え後に戻る
- **仕様に無いもの**: 各要素のタイルURL・ズーム範囲・出典・色の段

## 段階Cの方針（2026-09-22、ユーザー指示）

**実績のある既成部品を既定にする。自前で作るのは、仕様が理由を持って要求している見た目だけ。**
いまの見た目には主観で決まった部分が多く混じっており、**主観を独自部品で再現するコストは
見合わない**（ユーザー判断）。

### 主観と要求の切り分け

- **要求**（そのまま再現する）: `docs/architecture/design-principles.md`「UI仕様」と
  `docs/modules/frontend/frontend-design-system.md`に**理由が書かれているもの**。例:
  地図の視界を削らない・走行中に見るかで情報量を絞る・状態ドットの3表現・
  ドラッグ移動や高さドラッグのように既成品で表現できない振る舞い。
- **主観**（既成部品の既定へ寄せてよい＝見た目が変わることを許容する）: 理由が書かれていない
  色味・角丸・余白・境界線・影・アイコンボタンの寸法。

### 使う部品

**既に依存に入っているものを優先する**（2026-09-22時点: Radix の accordion/checkbox/dialog/
popover/tabs/toggle/toggle-group、`class-variance-authority`・`clsx`・`tailwind-merge`、
`embla-carousel-react`）。**新しい依存を足すのは、これらで賄えないと示せたときだけ。**

### 対象の出し方

`button`・`chip`・`toggle`を名前に含むCSSクラス（2026-09-22時点で23ファイル・62クラス）から。
一覧は機械で出す:

```
grep -rn "^\.[A-Za-z-]*\([Bb]utton\|[Cc]hip\|[Tt]oggle\)" frontend/src --include=*.module.css
```

### 完了条件

- 自前で残した部品ごとに、**なぜ既成品で賄えないか**を1行書く。書けないものは既成品へ寄せる
- `frontend-design-system.md`「意図的に作らない・統合しないもの」を、この作業の結果で更新する
  （既に「汎用Chipの選択状態トグルが3系統に重複している」が将来の統合候補として記録済み）
- 下の「寄せた項目」3件がそれぞれの完了条件を満たしている

### 寄せた項目（2026-09-23統合。元タスクは閉じた）

現況の値は2026-09-23・master `7efba1b5`で測ったもの。着手時に同じコマンドで測り直す。

**C-1. ボタン・チップ系要素の共有部品化**（元[T629](T629.md)）

- **何を**: 下部2区分（ルート設定・ルート結果）と地図上のチップ・ピル（`MapOverlayControls`・
  `LensControl`）が使うボタン・チップ系要素を洗い出し、どこまで共有部品へ寄せるかを決めて寄せる。
- **なぜ**: 同じ役割の要素が複数のCSS Moduleへ重複している。いま揃っているのはトークン値
  （`var(--color-border)`・`var(--radius-sm)`）だけで、`page.module.css`・`RouteForm.module.css`・
  `RouteSettingsPanel.module.css`に同じ値の組が並ぶ。1ファイルだけ値を変えると、同じ役割なのに
  見た目がずれる不統一が再発する。
- **構造が違うもの**（1つのクラスへ寄せられない理由）: 正方形アイコンボタン（`.outcomeHeaderIcon`）は
  寄せやすい。幅可変＋テキスト（`.stepperButton`）・ピル型の複合要素（`.legendChip`・`.destinationChip`）・
  セグメントコントロール（`.modeToggle`）は構造そのものが違い、寄せるなら多バリアントの部品になる。
- **現況**: `components/ui/Button`（CVA）は「ルート生成」ボタン専用ではなくなっており、`page.tsx`・
  軸スタジオの各パネル・`HeaderMenu`等が使う（`grep -rln "ui/Button" frontend/src --include=*.tsx`）。
  `components/ui/`には`stepperButton.module.css`・`roundIconButton.module.css`・`mapCtrlButton.module.css`
  等の共有CSSが既にある。**寄せる先はこれらを起点に数える**（新設より先に、既にある共有部品の
  使われ方を出す）。
- **対象の出し方**: 上の「対象の出し方」のgrep。
- **決め方**: 未決だった「`ui/Button`を広げるか、軽い共有CSSにとどめるか」は、この節の方針
  （実績のある既成部品を既定にする・自前で残すものは既成品で賄えない理由を1行書く）で決める。
- **完了**: 対象の各クラスが「共有部品へ寄せた」か「自前で残す理由（1行）」のどちらかになっている。

**C-2. `MapOverlayControls.tsx`の汎用フック2本**（元[T953](T953.md)）

- **何を**: `components/MapOverlayControls/MapOverlayControls.tsx`（1,178行）にある`usePagedOverflow`
  （はみ出しのページ送り）と`useHoldRepeat`（長押しリピート入力）の約140行を、切り出すか決める。
  どちらも`MapLayerId`・凡例・グループを知らない汎用フックで、他の地図UIからも使える。
- **割らないもの**（理由つきで残す）: チップのグルーピング・描画、凡例内訳の描画、表示する項目の
  設定。「チップ列の見せ方」という1つの理由で同時に変わるため。
- **対象の出し方**: `wc -l`と`grep -n "function usePagedOverflow\|function useHoldRepeat"`。
- **決め方**: この節の方針に従い、切り出す前に既に依存にある部品で賄えるかを見る（ページ送りなら
  `embla-carousel-react`が候補）。賄えれば置き換え、賄えなければ`components/ui/`か`lib/`へ切り出す
  （置き場は段階Dの層に合わせる）。
- **完了**: 切り出す／置き換える／残すの判断と、その根拠の実測行数が本ファイルにある。

**C-3. 制御された数値入力の共有部品**（元[T715](T715.md)）

- **何を**: Reactの制御された数値入力が入力途中の状態（空・`-`・`1.`等）を食う問題を、3箇所が
  別々の方法で解いている。1つの部品へ寄せるかを決める。
  - `components/AxisStudio/AxisFormFields.tsx`の`NumberField`・`SliderNumberField`: 打鍵ごとに反映
  - `components/RideConditionBar/RideConditionBar.tsx`の想定速度: 下書き（`speedDraft`）を持ちblurで確定
  - `components/AxisStudio/TuningPanel.tsx`: パラメータごとの下書き文字列（`drafts`）を持ち保存時に数値へ
- **未決の問い**: どの確定タイミングを標準にするか。片方へ機械的に揃えるともう片方の操作感が
  変わる（折れ点の調整は打鍵ごとの反映が前提、走行速度は確定のタイミングが明示的な方が扱いやすい）。
- **決め方**: 「入力途中の文字列を部品の中に持つ」ところは共通の解き方として1つにする。確定の
  タイミングは、この節の「主観と要求の切り分け」で扱う——仕様（`design-principles.md`・
  `frontend-design-system.md`）に理由があるものは部品の引数として残し、無いものは1つへ寄せる。
  寄せた結果、利用者から見た操作感が変わる箇所はユーザーへ確認してから変える。
- **完了**: 3箇所が1つの部品を使うか、使わない箇所に理由が1行ある。

**寄せなかったもの**: 調査時に段階Cへ寄せると判定した[T1013](T1013.md)の項目5（`Disclosure`の
クリック方法のdocstring）は、`0b4be87f`（2026-09-22）で既に直っていた（docstringは`fireEvent.click`／
`userEvent.click`を勧め、実在しないテストへの参照は消えている）。T1013側に観測値を書いて閉じた。

## 差分の突き合わせ表: ルート家族

新実装が返ってきたら、この表の1行ずつに判定を入れる。判定は3つ——**新版のみ**（既存が
持っていなかった）／**旧版のみ・今も要る**（仕様の読み落とし。docsへ1行足す）／
**旧版のみ・消す**（到達しない・写経・手当て）。

母集団の出し方: `MapView.routes.ts` の `const` 宣言と `map.addLayer` の spec を機械抽出
（ソース6・レイヤー14・画像1）。

### ソース（6）

| id | 中身 | 判定 |
|---|---|---|
| `route-candidates` | 候補の線（選択中を除くかは呼び出し側の指定） | |
| `route-selected-outline` | 選択中候補の形。**矢印もこれを流用**（専用ソースを持たない） | |
| `route-splice-stretches` | 乗り換えられる区間の帯 | |
| `route-spliced-current` | 編集中の合成ルート | |
| `route-detail-segments` | 選択中候補の区間ごとの詳細 | |
| `experiment-slots` | 比較スロットの代表候補 | |

### レイヤー（14）と、仕様に無い値

| id | 種類 | 仕様に無い値 | 判定 |
|---|---|---|---|
| `route-selected-outline-line` | line | 色 `#1e3a8a`・幅10・不透明度0.25 | |
| `route-candidates-line` | line | 選択中 `#2563eb`/幅5/不透明1、未選択 `#64748b`/幅2.5/不透明0.65 | |
| `route-candidates-hit` | line | 幅18・不透明0 | |
| `experiment-slots-casing` | line | 縁取り色 `rgba(15,23,42,0.8)`・幅7・不透明0.85 | |
| `experiment-slots-line` | line | 色はスロットの指定・幅4・不透明0.85 | |
| `route-splice-stretches-line` | line | 色 `#c2612b`、幅 選択5/未選択3、不透明 1/0.75、破線 実線/`[2,1.5]` | |
| `route-splice-stretches-hit` | line | 幅24・不透明0 | |
| `route-spliced-current-casing` | line | 縁取り色・幅11（本体+4）・端と継ぎ目は丸 | |
| `route-spliced-current-line` | line | 色 `#c2612b`・幅7・端と継ぎ目は丸 | |
| `route-detail-segments-casing` | line | 縁取り色・幅10・不透明1 | |
| `route-detail-segments-line` | line | 色はレンズのモード・幅6・不透明1 | |
| `route-detail-segments-hit` | line | 幅24・不透明0 | |
| `route-arrow-halo` | symbol | アイコン倍率 0.8×1.5・色 `#111827`・不透明0.95・間隔80px | |
| `route-arrow` | symbol | アイコン倍率0.8・色 `#ffffff`・不透明1 | |

画像1: `route-arrow-icon`（sdf、矢印の形はコードが描く）。

### 重なり（段の中の順。背面→前面）

仕様と現行から導けるもの: ハロー → 候補線 → 候補の当たり判定 → 比較スロットの縁取り →
比較スロットの線 → 詳細の縁取り → 詳細の色分け線 → 詳細の当たり判定 → 矢印ハロー → 矢印。

**未確定**: 帯（`route-splice-stretches-*`）と合成ルート（`route-spliced-current-*`）が、
詳細の色分け線に対して前か後か。仕様は「帯は候補線より上」「合成は帯より上・候補線より上」
としか言わない。**新実装がどう置くかを見てから、実機で確かめて決める**。

### 当たり判定（押せるレイヤー）

`route-candidates-hit`（候補を選ぶ）・`route-detail-segments-hit`（区間の詳細）・
`route-splice-stretches-hit`（乗り換え）。**帯は当たり判定だけに登録する**——見た目の線にも
登録すると1タップで2回発火し、「同じ道をもう一度選ぶと戻す」挙動と噛み合って何も起きなくなる。

### 挙動として確かめること（契約テスト7ケースで担保済み）

隠しても最後は見えている／順序2通りでも同じ重なり／ハローは候補線の下／スロットは詳細の下／
凡例で3枚のfilterが揃う／モード切替で色式が入れ替わる。

### 判定（2026-09-22）

**ソース6・レイヤー14は1対1で対応した。落ちたものも、増えたものも無い。** 綴りだけが
役割からの導出（`route-<役割>`）へ変わる——採用した方針D（idを規約から導出）どおり。

| 判定 | 件数 | 中身 |
|---|---:|---|
| 新版のみ（採用） | 1 | 帯の当たり判定を区間の当たり判定より前面へ。区間の当たり判定は幅24pxでルート全体を覆うため、後ろだと**帯が一度も押せない** |
| 旧版のみ・今も要る | 2 | 下記 |
| 旧版のみ・消す | 0 | — |
| 呼び出し側で埋める（穴） | 色・太さ・不透明度・破線・矢印の間隔 | 新実装はすべて引数。配線時に現行の値を渡す |

**旧版にしか無く、今も要るもの（どちらも仕様に書かれていなかった）**:

1. **選択中候補のハローは、候補線より下にある。** 上にあると、薄い暗色の帯が選択中の線の上へ
   かぶる。**契約テスト「選択中候補のハローは、候補線より下にある」がこれを検出した。**
2. **比較スロットは、区間の色分けより下・候補の参考線より上にある。** 参考線より下だと、
   8本の候補線に埋もれて比較にならない。

どちらも実装のコメントにしか無く、`docs/modules/frontend/` には書かれていなかった。
**同じ落とし方を次の人もするので、docsへ1行ずつ足す**（この2件がこの家族での
「仕様の不足」の実測値）。

**仕様が決めていなかった重なり**（書き手が決め、こちらで追認したもの）: 帯と合成ルートは
区間の色分けより下、当たり判定どうしの前後は「押したときにどちらが勝つか」で決める。

## 分割して書いた後に、全体を最適化する工程（2026-09-22追加）

**並列で書かせると、書き手はそれぞれ局所最適を出す。誰も全体を見ないので、同じ形が人数分
生まれる。** これは書き手の失敗ではなく、分割した側が束ね直す工程を置かなかった失敗である。

実測（地図の6家族2,040行、2026-09-22）: 型定義702行（34%）・補助関数254行が家族へ散在。
`〜Style`型・`hitTargets`・`tier`が6/6家族、ソースの中身を差し替える口（`content`＋`replace`）が
5/6家族、`LayerSpecification`へのキャストが4/6家族、idの導出が4/6家族。

### 不変条件: 階層ごとにマージし、その階層以下は全体最適であること

**ある階層をマージする時点で、その階層の兄弟どうしに重複が無く、下の階層が既に括り出されて
いること。** 上へ積む前にこれを満たす——満たさないまま積むと、重複は上の階層の形として
固定され、以後は「そういう作りだから」になる。

階層は下から: ①sceneの土台 → ②家族（面・路面の線・点・軸・気象・ルート） →
③地図の合成（`MapView`の配線） → ④画面の状態 → ⑤UI基盤 → ⑥配置。

マージの直前に、その階層の兄弟すべてに対して下の判定を回す。

### 括り出すかの判定

**3つ以上の担当に同じ形が現れたら、土台へ引き上げる。** 判定は機械で出す:

```
# 同じ形が何家族に出るかを数える（形は grep で表せるものに限る）
for pat in "declareLayer" "replace: (source" "as unknown as LayerSpecification" "hitTargets"; do
  n=0; for f in frontend/src/features/map/scene/*Scene.ts; do grep -q -- "$pat" "$f" && n=$((n+1)); done
  echo "$pat: $n"
done
```

引き上げた後も「**1つ増やすときに触る場所が1箇所**」が保たれることを、各担当の報告と同じ
基準で確かめる——共通化のために足す場所が増えたら、それは別の悪化である。

### 完了条件

- 3家族以上に現れる形が残っていないこと（上のコマンドの出力で確認する）
- 引き上げの前後で、実装・テストの行数を測って記録すること（**減っていなければ、
  引き上げが失敗している**）

## 段階Aの品質（2026-09-23・周期レビュー第12回で4観点から測った結果）

段階Aを独立した4観点（全体最適・整合性・複雑度・UI/UX）がそれぞれ別の作業ツリーで
見た。**構造の質は高く、後始末の質が低い**というのが総合の判定で、後始末の失敗は
設計判断の誤りではなく**測り方の穴**に由来していた。

### 達成できたこと（実測）

- **一本道が成立している。** 評価軸を1本足すのに編集するリポジトリ内ファイルは**0**
  （frontendの実装ファイルに軸idの直書きが0件であることを検算済み）。道路属性を足すとき
  **scene側は0ファイル**（宣言から導出される）。
- **写しが実際に消えた。** ビルド時の軸カタログの写しが削除され、構造仕様9が成立。
  frontendの非テストコードに残る色リテラルは4件まで。スマホ幅判定の二重定義も消えた。
- **凡例が地図と同じ宣言から作られる**ため、原理的に食い違わない構造になっている。
- `MapView.tsx`は3,382→1,608行で、**残りが単一の変更理由**（MapLibreの命令的世界との
  境界）に収まった。複雑度の観点が「今周期で最大の改善」と独立に評価した。

### 達成できなかったこと（4観点すべてが同じ型の症状を挙げた）

**「置き換えたら、置き換えられた側とそれを説明する文を同じコミットで消す」が守れていない。**
症状は[T1023](T1023.md)・[T1024](T1024.md)・[T1026](T1026.md)・[T1029](T1029.md)へ起票した。
根は3つあり、いずれも段階Aで私が置いた判断である。

1. **列挙器がコメント本文の識別子を「消費者」と数えた**（[T1015](T1015.md)）。
   その結果、本番から到達しない一群が「消費者あり」として残った。CLAUDE.mdが
   [T1001](T1001.md)の教訓として載せている「語が他所に出た偶然だけで残った」の、
   **別の向きでの再発**である。
2. **コードの撤去と文の撤去を同じ操作でやった。** 消した色表の中にしか無かった判断
   （赤＝危険）が表ごと落ち、`docs/modules/*.md`へ昇格されなかった。CLAUDE.mdに明文が
   あるのに守れていない。
3. **移し先の性質を確かめずに移した。** 表示値を`backend/domain`へ移してデプロイ境界を
   跨ぎ（[T1025](T1025.md)）、sceneを`features/map`へ移して値だけ旧居に残した
   （[T1029](T1029.md)）。2件とも同じ形。

### 必要十分性について（着手時の論点への答え）

段階Aは「既存を読まずに仕様から起こす」で始めたが、旧側の削除行と全数突合して不足を
戻したため、必要十分性を artifact から証明できなくなっていた。**周期レビューはこれに
部分的な答えを出している。**

- **不足側（要るのに無い）は、4観点いずれも0件。** UI/UXの指摘はすべて
  「出ているが意味が違う／届かない」型で、「機能が欠けている」は1件も無かった。
- **過剰側（要らないのに在る）は見つかった。** 本番から到達しないシンボル群、
  16フィールド中14が書くだけのref、説明だけが残ったJSDoc。

つまり**段階Aは「削りすぎ」ではなく「戻しすぎ」で着地していた**。これは全数突合で
不足を戻したことの予想どおりの結果で、削減の下限がどこかは依然として分からないが、
**いま動いているものが不足している証拠は無い**。過剰側は[T1023](T1023.md)の導出で
機械的に出せる。

## 段階Bの進め方（2026-09-23、周期レビュー第12回の実測を完了条件に取り込む）

**上の「段階B以降の進め方」の4項目に、段階Aの失敗から2つ足す。**

5. **移す前に、移し先の性質を確かめる。** 少なくとも(a)デプロイ境界（その置き場の変更が
   何を再ビルド・再配置するか）、(b)依存の向き（移した先が旧居へ依存し返していないか）を
   見る。段階Aはどちらも確かめずに移し、2件とも欠陥になった。
6. **撤去するときは、コードの撤去と文の撤去を別の操作にする。** 撤去する差分の
   コメント行（`git diff`の削除行のうち`//`・`*`で始まるもの）を
   `docs/conventions/comments.md`の判定木へ1件ずつ通し、他に記録の無い横断的な判断は
   `docs/modules/*.md`へ昇格してから消す。**同じスクリプトでまとめて落とさない。**

### 対象（周期レビュー第12回の実測。着手時に測り直す）

`app/page.tsx`は2,533行で、状態フックは45件（`useState` 32・`useStored*` 13）。
宣言を全件読んで分類すると、**変更理由が5つ同居**している。

| クラスタ（変更理由） | 状態 |
|---|---:|
| A ルート生成の入力と結果 | 17 |
| B 区間の乗り換え | 6 |
| C 地図の見え方（レイヤー／凡例／レンズ／専用配信） | 9 |
| D 画面レイアウト（デスクトップ／モバイル） | 8 |
| E 結果の読み方（区間詳細／比較／研究／デバッグ） | 5 |

`MapView`へ渡すpropは46本で、出どころは **C が24本**・A/B/E が18本・位置3本・D 1本。

**A・B・Eは同居してよい**（互いに読み合っており、「ルートという画面の主題」で1つ）。
**Cが違う**——CからAへの読み出しは2本だけで、残りは軸カタログと自分の状態で閉じている。
そして**評価軸・レイヤー・外部データ源が増えるたびに膨らむのはCだけ**。いまCは
「ルートの画面」の中に置かれている。

### 完了条件（数値）

- `page.tsx`の状態フックが **45 → 36以下**。
- `<MapView>`へ渡す属性が **46 → 23以下**。
- **新しいramp軸・専用配信軸・地図チップを足したときに`page.tsx`を触らずに済む**
  （これが本命。上の2つはその結果として動く数字）。

**Dの切り出しは同じ段階に入れない。** DはA/Eをゲートしている（点の編集可否・ルートの
点検可否）ため、Cを抜いた後に改めて測る。

### 進め方（着手時の約束）

- 実装は**独立したAgentが仕様（`docs/modules/frontend/page-composition.md`）だけを
  入力に起こす**。私はこのセッションで既存実装を読んでおり、仕様からの独立性を
  持てない。
- **実験と混入を別コミットにする。** 段階Aは両方が1コミット（1,788行）に入ったため、
  どの差分が仕様由来でどれが逆輸入かを後から分離できなくなった。
- **基準は仕様であって旧コードではない。** 旧テストを基準に据えると、置き換えられる側が
  再び基準になる。
- 源泉へ移す候補が出たら、移す前に**「そもそも要るのか」**を先に聞く（段階Aで、
  何も決めていない既定値をbackendへ移してしまった実例がある）。

### 寄せた項目（2026-09-23）

現況の値は2026-09-23・master `7efba1b5`で測ったもの。4件とも`page.tsx`の状態と生成リクエストの
組み立てに属し、段階Bで状態を分けるときに同じ箇所を読む。

**B-1. 重みの辞書のキー照合**（元[T1013](T1013.md)の項目1。T1013は閉じた）

- **何を**: `lib/evaluationAxes.ts`冒頭のコメントは「`RoutePreferenceWeights`はindex signature型
  （axis_idキーの辞書）のため、キーの綴り違いは型検査で落ちない。軸idを増減したときに気づけるのは、
  カタログとキー集合を突き合わせる検査だけ」と書く。その検査（ビルド時の軸カタログの写しの
  `preference_defaults`とキー集合を突き合わせるテスト）は写しごと撤去済みで、**制約（型で照合
  できない）だけが真のまま残っている**。
- **決めること**: ガードを置き直すか、照合の要らない形へ変えるか。
- **決め方**: 上の「段階B以降の進め方」1・2に従う。まずフロントに軸idのキーの写しが残っているかを
  列挙する（`grep -rn "RoutePreferenceWeights" frontend/src --include=*.ts --include=*.tsx | grep -v "\.test\."`
  で辞書を作る・読む箇所を出し、キーの出どころが実行時の`axisCatalog.defaultWeights`と
  `lib/routePreferenceSync.ts: syncRoutePreferenceKeys`だけかを見る）。写しが無ければ照合すべき
  2つが存在しないので、コメントの「検査だけである」を外し、今も成り立つことだけを現在形1文にする。
  写しが残っていれば、検査を置く前にその写しを消す。
- **完了**: 列挙したコマンドと観測値が本ファイルにあり、コメントが現在形か消えている。

**B-2. 重みの上書きがOFFの間の送信契約**（元[T1013](T1013.md)の項目2）

- **何を**: `weightOverrideEnabled`がfalse、または軸カタログが未取得の間は、生成リクエストから
  `route_preference`を省く（`page.tsx`の`routePreference: weightOverrideEnabled && axisCatalog.loaded ? … : null`）。
  省いたときはbackendが`services/evaluation_service.py: load_route_preference`の既定を使う
  （`api/dependencies.py`の`preference_override or load_route_preference()`）。フロント側にあった
  既定の写し（ミラー定数）は消えたが、**契約は生きている**。
- **現況の仕様**: `docs/modules/frontend/page-composition.md`「page.tsxが橋渡しする主なデータフロー」は
  「カタログ未取得なら送らない」を書いているが、「上書きOFFなら送らない」は書いていない。
- **決めること**: 契約をフロントにどこまで残すか（仕様に書く／テストで固定する／何もしない）。
- **決め方**: 本タスクの差分判定そのもので決まる。段階Bで仕様から生成リクエストの組み立てを
  起こし、旧版にしか無い振る舞いとしてこの省略が出たら「今も要る」に当たるので、仕様へ1行足す。
  組み立てが純関数として切り出せたなら、その入出力（上書きOFF→`route_preference`無し、
  カタログ未取得→無し）を期待値テストで固定する。
- **完了**: 仕様に上書きOFFの扱いが書かれ、固定したならそのテストがある。

**B-3. レンズの軸の凡例を1段まで絞れることを許すか**（元[T1010](T1010.md)の項目2。T1010は残る）

- **何を**: レンズに選んだ軸の凡例の絞り込み（`page.tsx`の`hiddenLegendKeysByMode[lens]`）は、
  同じ軸に対して最後の1段まで絞れる。撤去前のコードにだけ「1段まで絞った軸を色分けに選べると
  単色になり情報量が無くなる、という混乱があった」という根拠があった。今の実装は許しているが、
  許す根拠がどこにも無い。
- **決めること**: 許すか、許さないか（許さないなら、レンズに選ばれている軸の凡例は最後の1段を
  落とせないようにする）。
- **決め方**: 利用者に見える操作の可否なので、段階Bでこのクラスタ（地図の見え方）を切り出すときに、
  現状と2択を添えてユーザーへ諮る（`docs/conventions/asking-user.md`）。推奨案を自動で採用しない。
- **完了**: 許すなら`docs/modules/frontend/static-map-layers.md`へ「許す」と理由を現在形で書く
  （書かないと次に同じ議論が起きる）。許さないなら実装とテストへ反映する。

**B-4. 同じ判定を2つの出し先へ出すときの入口**（元[T1010](T1010.md)の項目4）

- **何を**: 撤去前は「案内文とヘッダーのドットが同じ『ズームが広すぎる』判定を別々に見ていた」ため、
  判定の入口を1つにして片方だけ抑制し忘れる食い違いを防いでいた。「宣言は1つ」の原則は
  `static-map-layers.md`にあるが、「出し先が2つあるときに入口を1つにする」はどこにも無い。
- **現況**: ズーム不足の判定は`components/Map/mapLayers.ts: tileZoomTooWideLayerIds`が1本で持ち、
  `MapView.tsx`がズームのたびに計算して呼び出し側へ伝え、`page.tsx`が状態
  （`tileZoomTooWideLayerIds`）として受けてチップの案内へ使う。`page.tsx`には「世代が無い
  レイヤーもズーム不足と同じ扱いにする」という別の合成がある。
- **対象の出し方**: 同じ判定を複数の出し先が別々に評価している形を列挙する
  （`grep -rn "tileZoomTooWide\|ズーム不足" frontend/src --include=*.ts --include=*.tsx | grep -v "\.test\."`
  から始め、判定関数の呼び出し元が2つ以上あるものを拾う）。
- **決め方**: 同じ形が残っていれば段階Bで入口を1つにする（状態を分ける作業と同じ箇所）。
  残っていなければ、原則として採るかを決め、採らないなら理由を`static-map-layers.md`へ現在形で書く。
- **完了**: 列挙の観測値が本ファイルにあり、上のどちらかになっている。

## 段階Bの結果（2026-09-24、地図の見え方の切り出し）

### 進め方の実際

1. 既存実装を読まない担当が、`docs/modules/frontend/*.md`と隣接部品の契約だけから
   `features/map/view/`を書き起こした（`2f0f2b9a`、814行・テスト489行）。担当は検索の除外指定を
   誤り`page.tsx`の約30行（凡例のデバウンス時間・保存キー・災害凡例の定義）を見たと自己申告した
   ——この3点に当たる判定は「仕様から独立に決めた」と数えない。
2. 旧版（`page.tsx`）と突き合わせ、判定をユーザーへ見せてから統合した（下表）。
3. 統合の前に、新版そのものの冗長を削った（`52000717`、実装814→422行）。ユーザー指示で
   「**仕様で決まっていない部分は、特別な不都合が無ければ単純な側**」を判定基準に加えた。
4. 責務の外と、他の源泉から導けるものを点検した（下記「点検」）。**ここが複雑度の下限を決める**
   （ユーザー指摘）。段階Aの成果にも同じ点検を当て、入口の話（下記の段階A見直し1・2）は統合で消した。

### 判定（旧版と新版の突き合わせ）

| 判定 | 件数 | 中身 |
|---|---:|---|
| 旧版のみ・今も要る（仕様の穴） | 5 | レンズを選ぶと「ルート」チップをONにする／世代の案内は取得が終わってから出す／地図の再描画ボタン／「全レイヤー非表示」は既定へ戻すのではなくONを全部OFF／絞り込み中かは実在する行だけで数える（5件目は`static-map-layers.md`に書いてあったのを新版が落としたもので、仕様の穴ではなく読み落とし） |
| 旧版のみ・消した | 7 | 古い保存値の読み替え2つ（道路情報の分割・ルートチップの意味変更、ユーザー決定）／存在しないレンズを総合難易度へ戻す効果（軸カタログは1回しか取得しないため通らない）／チップのホバー文言の付け足し2つ（スマホで出ない）／絞り込みを経路ごとに遅らせ方を変える分岐（一律に遅らせる）／`useStableMap` |
| 新版のみ・採用 | 4 | レンズの「未使用」は生成後だけ（ユーザー決定。使う軸は生成時に決まる）／空になった鍵を保存値から消す／「ルート」チップの押せる条件をidではなく宣言（`kind: "dynamic"`）から導く／ズーム不足は表示範囲の`zoom`から導く |
| 新版のみ・不採用 | 2 | 「既定へ戻す」ボタン／ONのチップだけで絞り込み中を数える |

仕様の穴4件（上表1行目の1〜4件目）は、それぞれ`docs/modules/frontend/`へ現在形で書いた。

### 点検（責務の外・源泉から導けるもの）

- **出発時刻**は気象レイヤーの持ち物ではなく走行条件 → `hooks/useDepartureTime.ts`へ。
  `useDynamicWeatherLayers`は時刻を受け取るだけにした。
- 地図へ渡す値（`MapLook`）を**状態そのもの**に絞った: 22項目→9項目。表示の辞書3つ・隠した行の
  辞書4つ・取得結果の2分割・`routeLayerOn`・下敷きは、`MapView`が中で合成し直していた
  （段階Aの調査で判明）ので、地図側（`scene/applyToMap.ts`）が導く。軸カタログ・タイル世代は
  `MapView`が共有ストアから読む。
- 世代が届いたときの全体の当て直し効果は、sceneの差分適用と同じ変化で二重に走っていたため消した。
- 災害: 要素の呼び名を backend `domain/map_display.py: WEATHER_ELEMENTS.label`へ（ユーザー決定）、
  要素ごとの表示切替を`scene/legends.ts`へ、危険度の凡例を災害レイヤーの`readOnlyLegend`へ。
  **見た目の変化1件**: ▶パネルの行の並びが要素の宣言順になり「洪水キキクル」が「竜巻」の後ろへ。
- 軸の識別色: `page.tsx`と`RouteSettingsPanel`が別々に計算していた → 軸カタログで1回だけ導く
  （`AxisCatalog.axisColors`）。

### 寄せた項目の完了

- **B-1**: `grep -rn "RoutePreferenceWeights" frontend/src --include=*.ts --include=*.tsx | grep -v "\.test\."`
  で辞書を作る箇所を列挙。キーの出どころは`axisCatalog.defaultWeights`と`syncRoutePreferenceKeys`
  だけで、フロントに軸idの写しは無い（軸idを`キー:`で書いた箇所も0件）。`lib/evaluationAxes.ts`の
  「気づけるのは検査だけ」を現在形の1文へ直した。
- **B-2**: 送る判定を`lib/routePreferenceSync.ts: routePreferenceToSend`へ切り出し、期待値テスト3件
  （上書きOFF→送らない／カタログ未取得→送らない／両方あり→揃えて送る）。`page-composition.md`へ
  上書きOFFの扱いを書き、理由の「静的フォールバック」（存在しない）を「取得前は軸が0件のため全部消す」
  へ直した（`route-settings-and-results.md`も同じ）。
- **B-3**: ユーザー決定「今のまま（0段まで絞れる）」。`static-map-layers.md`へ理由付きで書いた。
- **B-4**: `grep -rn "tileZoomTooWide\|ズーム不足" frontend/src`で判定関数の呼び出し元は1か所
  （`MapView`）、出し先2つ（案内文・状態ドット）はどちらも`page.tsx`の同じ箇所で作っていた＝残って
  いる同じ形は0件。原則として採り、`static-map-layers.md`へ現在形で書いた（統合後は`overlayChips.ts`
  の1か所）。

### 実測（着手前 master `5c127812` → 段階B統合後。測り方は「起票時の測定値」と同じ、行数は全行）

| | 着手前 | 統合後 | 増減 |
|---|---:|---:|---:|
| frontend 実装 | 27,282 | 26,886 | −396（−1.5%） |
| frontend テスト | 22,730 | 22,917 | +187 |
| CSS | 5,991 | 5,991 | 0 |
| `app/page.tsx` | 2,541 | 1,750 | −791 |
| `components/Map/MapView.tsx` | 1,476 | 1,279 | −197 |
| `page.tsx`の状態フック | 45 | 37 | −8（**目標36以下に1届かず**） |
| `<MapView>`へ渡す属性 | 45 | 23 | −22（目標23以下を達成） |

**見積もりとのずれ**: 実装はほぼ減っていない。`page.tsx`の−791行の大半は`features/map/view/`
（実装約420行）と`hooks/useDepartureTime.ts`へ移っただけで、純減は重複（辞書の分割と合成し直し・
軸の色の二重計算・災害の定義）の削除分にとどまる。テストが増えたのは、新しい純関数の期待値テストと
型付きの共通部品（`scene/__fixtures__/sceneState.ts`）を足したため——契約テストは状態を
`as unknown as`で型から外して組み立てており、入口の形が変わっても型検査が気づけなかった。

**本命の条件**（評価軸・専用配信軸・地図チップを足しても`page.tsx`を触らない）: `useMapView`の入力は
ルートの文脈・走行条件・生成に使われた重みだけで、軸やレイヤーの種類を知らない。`page.tsx`に
軸id・レイヤーidの直書きは0件（`grep -nE '"(route|disaster|surface|highway)"' frontend/src/app/page.tsx`）。

### 残り（段階Bの中で続けて行う）

1. ~~**状態フック36以下**~~ → 済み（37→33）。生成（実行中・進み具合・直近の案内）と区間の乗り換え
   （評価中・適用中・失敗の文言）を、それぞれ1つの値（`Generation`・`SpliceTask`）へまとめた——1つの
   処理の状態は同時に1つにしかならない（乗り換えパネルは評価中も適用中もボタンを押せない）。
   **見つけた死んだ書き込み**: 乗り換えの評価は生成の進み具合の状態へ書いていたが、その値を画面へ
   出すのは生成中だけで、乗り換え中の進み具合は一度も表示されていなかった。読み手がいないので渡さない。
   **最小かの点検**（ユーザー依頼 2026-09-24）: 33個を「他から導けるか／同時に成り立たないものを
   分けて持っていないか／読み手がいるか」で1つずつ見た（読み書きの回数は`grep -cE "name"`で数え、
   読み手0の状態は無かった）。まとめられた3つで33→28:
   - 区間の乗り換えの編集（編集の元・適用した乗り換え・評価結果の控え・処理状態）→ `SpliceSession | null`
     1つ。4つはいつも一緒に始まって終わるため、「編集していないのに乗り換えが残る」組み合わせが
     作れなくなり、片付けのまとめ書き6か所が消えた。
   - 生成に使われた重み → 生成条件の1項目（書く・消すのが常に生成条件と同時）。
   - 画面の寸法の実測 → 地図がフィットする瞬間に測る関数（`measureRouteFitObscuredPx`）。地図は
     候補が変わった瞬間しか読まないのに、リサイズのたびに状態を更新して描き直していた。
   残す28個の理由: ルート生成の入力と結果・結果の読み方は、利用者の操作か通信の結果でしか決まらない。
   デスクトップの開閉とモバイルのシートは同時に開ける数が違うため1つにできない。「シートの高さを
   決めたか」は自動で合わせている間も動く高さとは別の事実。「重みを上書き中か」は、保存された重みが
   軸の増減に合わせて自動で埋まるため保存値から区別できず、置き換えには保存データの読み替えが要る。
2. ~~**段階Aの見直しのうち3・4**~~ → 済み（2026-09-24、段階Bの直後。ユーザー決定）。
   問いは段階Bと同じ3つ（入口で分けて合成し直していないか／共有の源泉から読めるものをpropで受けて
   いないか／同じ値を別の場所で宣言していないか）。母集団は読み取り専用の調査で出し、1件ずつ判定した。
   - **消した（到達しない）**: 帯の「選択中」（入力の`taken`が常にfalse。表示のレイヤー・判定の
     プロパティ・backendの`ROUTE_LINE_WIDTHS_PX["spliceSelected"]`まで）・使われない段`estimatedLine`・
     使われない`idPrefix`・矢印の絵の`null`分岐。
   - **1か所にした**: ソースのレイヤー名（`applyToMap.ts`と`MapView.tsx`が同じ3行を宣言）→
     `applyToMap.ts`だけ。一次属性を描くチップ9つの名前（画面の記述子とbackendの一次属性で
     6つ同じ・3つ違い）→ 記述子はbackendの宣言（`material_catalog.py`の`PrimaryAttributeSpec.label`）
     から読む。違った3つは画面に出ている名前へbackendをそろえた——backend側の3つの名前は画面の
     どこにも出ていなかった（道の詳細が一次属性の名前を出すのはOSMのタグだけ）ため、見た目は変わらない。
     backendの名前を書き写して照合していたテスト（`primaryAttributes.test.ts`）は消した。
   - **残した（理由）**: 選択中の候補を探す計算（各層が自分の入力から1行の`find`で引く。引き上げる
     共通の形が無い）／点のレイヤーidの組み立て（`MapView`も`sceneLayerId`の規則関数を呼ぶだけで写し
     ではない）／同じ値の色の別名（`COLOR_UNKNOWN`・`COLOR_NO_DATA`）と土地被覆のズームの別名（どれも
     生成物を直接読み、値の写しではない）／「データなし」の行名が2通り（道路の線の「不明・他」は分類に
     当てはまらない道、評価軸の「データなし」は値の無い道で、意味が違う）。
   - **起票案**: APIのパスは、タイル以外も含めてサービス層がすべて手で書いていて（17か所）、OpenAPIの
     `paths`と型で照合する仕組みが無い。タイルだけ照合を足すと新しい種類の仕組みになるため、ここでは
     入れない（全体に当てるかを別に決める）。
   - `material_catalog.py`は、masterの時点で`ruff format --check`を通らない（今回の変更と無関係の行）。

## 段階Dの方針（2026-09-23、寄せた項目）

段階Dはディレクトリの再配置で、**どのファイルがどこへ移るか**を決める段階である。次の3件は
いずれも「移すときに同じファイルを開く」ため、移設と同じコミットで片付ける。現況の値は
2026-09-23・master `7efba1b5`で測ったもの。

**D-1. sceneが旧居（`components/Map`）から借りている宣言を持ち主の置き場へ移す**（元[T1060](T1060.md)。閉じた）

- **何を**: `features/map/`（scene）が`components/Map/`からimportしている宣言を、持ち主の置き場へ
  移す。sceneの入力型はMapViewのpropsから切り離され、import循環は0件になっている
  （[T1029](T1029.md)）が、`features/map → components/Map`の依存は残っている。
- **対象の出し方**: 母集団は`frontend/src/features/map/**`の非テストファイル（`*.test.*`を除く）が、
  `frontend/src/components/Map/**`から**import文で名指ししている名前**。TypeScriptのASTで
  `import`・`import type`の束縛を取り、名前ごとに「`features/map`の外の本番コードからも
  importされているか」を添える（コメント・文字列の一致は数えない）。2026-09-23（master `f98d5dd6`＋T1029）
  の実測は37名で、うち**`features/map`の外に本番の消費者が無いもの**16名（例: 評価軸の値の式
  `buildAxisRampValueExpression`・「不明」の色`COLOR_UNKNOWN`・降水と風のカラーストップ・ルートの
  矢印の絵・JMAタイルのプロトコル・面の差し込み位置`areaLayerAnchor`）、**両側に消費者がいるもの**
  21名（例: 凡例の段の鍵`legendBandKey`・`LEGEND_NO_DATA_KEY`・段の配色`bandColorsFor`・軸カタログの
  ビュー型`RampAxis`・`DedicatedWayValueAxis`・レンズの配色`LENS_NEUTRAL_COLOR`）。
- **基準（どの値がsceneの持ち物か）**:
  1. sceneの外に本番の消費者が無い宣言は、sceneの持ち物。地図に何をどう描くか（色・しきい値・
     記号の絵・MapLibreへの当て方）の宣言で、画面部品は読まない。`features/map/scene/`（家族の宣言
     なら`groups/<家族>.ts`、家族をまたぐなら`sceneBuilders.ts`等）へ移し、元の置き場から消す。
  2. sceneと画面部品（凡例・チップ・軸スタジオ）の両方が読むものは、どちらの持ち物でもない。
     sceneへ入れると画面部品がsceneへ依存し返し、`components/Map`に残すとsceneが旧居へ依存し
     続ける。**両方より下の第三の置き場**へ移す。
  3. 軸カタログから導くビュー型（`RampAxis`・`DedicatedWayValueAxis`と、それを作る変換）は地図の
     描画ではなく「軸カタログをフロントの形へ移す」ものなので、`lib/axisCatalog.ts`の側へ寄せる候補。
  4. MapLibreのインスタンスを触る低水準の操作（`runWhenStyleReady`・`areaLayerAnchor`）は、
     どのレイヤーかを知らないものだけを置く`mapStyleOps.ts`の性質を保ったまま、置き場だけを
     基準2と同じく両側より下へ。
- **未決の問い**: 基準2・4の「第三の置き場」には既存の慣習が無い。CLAUDE.md「新しい種類の成果物は、
  置き場と命名を先に決める」に従い、**段階Dの全体構成（目標構成4の`app/`→`features/`→
  `components/ui/`→`lib`・`services`）と一緒に、書き始める前にユーザーの合意を取る**。材料の候補:
  (A) `features/map/display/`（地図の表示語彙: 段の鍵・配色・凡例の型）を新設し、scene と
  `components/Map/`の両方がそこを読む、(B) `lib/mapDisplay/`（`lib/`は画面部品に依存しない層という
  既存の慣習に乗せる）。段階D以外で先に決めない——全体構成と別に決めると二度移すことになる。
- **完了**: 上の導出で「`features/map`の外に本番の消費者が無い」名前が0件。両側に消費者がいる名前は
  合意した第三の置き場へ移り、`components/Map`にも`features/map/scene`にも定義が無い。本番コードの
  import循環が0件のまま（`import type`も辺に数える）。移した宣言を説明している
  `docs/modules/frontend/*.md`の対象ファイル表・本文が新しい置き場を指している。

**D-2. `axisLayers.ts`・`secondaryAxes.ts`の経緯コメントを落とす**（元[T826](T826.md)。閉じた）

- **何を**: `components/Map/axisLayers.ts`・`secondaryAxes.ts`に、`docs/conventions/comments.md`が
  禁じる経緯の書き方（`改善計画Txxx:`・「以前は〜だった」「実機フィードバック」「ユーザー指摘」）が
  集中して残っている。内容は正しいが書き方が方針違反のもので、読み手を誤らせはしないが、
  新しいコードの参考例として複製される。
- **段階Dで扱う理由**: 2ファイルはD-1の基準3（軸カタログのビュー型は`lib/axisCatalog.ts`側へ）で
  移す対象そのもので、移すときに全行を読む。段階Cで先に磨くと、移設で同じ行をもう一度読む。
- **対象の出し方**: `grep -nE "改善計画T|T[0-9]{3,4}|実機|以前は|ユーザー指摘" <2ファイル>`（粗い目安。
  2026-09-23は`axisLayers.ts` 20行・`secondaryAxes.ts` 2行）。移した先のファイル名で数える。
- **決め方**: 1件ずつ`comments.md`の判定木へ通す。今も成り立つ制約は現在形1文へ、経緯は消す。
  残す価値のある背景は該当する`docs/records/tasks/Txxx.md`へ寄せるが、**書き足す前にそこへ既に
  同じ背景が無いかを確かめる**。同じ内容が`docs/modules/frontend/*.md`にあるものはコード側を消す。
  起票時に書かれていた`secondaryAxes.ts`冒頭と本文の自己矛盾（冒頭が「代役へのポインタだけの行と
  して表示する」、本文が「その案内文は撤去した」）は、2026-09-23の本文には無い
  （`grep -n "代役\|案内文" secondaryAxes.ts`が0件）。移すときに再確認するだけでよい。
- **完了**: 移した先の2ファイルで上のgrepの該当が、判定木で「残す」と決めた現在形の文だけになっている。

**D-3. `mapStyleOps.ts`の「アプリのレイヤーを1枚も足していない時点で呼ぶ」制約**（元[T1013](T1013.md)の項目3。閉じた）

- **何を**: `components/Map/mapStyleOps.ts: prepareBasemapForAreaLayers`のdocstringは「このアプリの
  レイヤーを1枚も足していない時点で呼ぶこと（`style.load`）。後から呼ぶと、自分で足した線レイヤーが
  最長の連なりを伸ばし、差し込み位置が地名側へずれる」と書く。しかし今の差し込み位置は並び順では
  なく`source-layer === "transportation"`を描き始める最初のレイヤーから求めており、同じファイルが
  「並び順から導いてはいけない」と明記している。アプリのレイヤーは`transportation`を名乗らない。
  **理由が旧方式（最長の連なり）の残骸に見える。**
- **段階Dで扱う理由**: `mapStyleOps.ts`はD-1の基準4で置き場を移す対象で、移すときにdocstringを読む。
- **決め方**: ①呼び出し元を列挙する（`grep -rn "prepareBasemapForAreaLayers\|areaLayerAnchor" frontend/src`。
  2026-09-23は`MapView.tsx`・`features/map/scene/applyToMap.ts`と自身のテスト）→ ②アプリのレイヤーが
  `transportation`を名乗らないこと（sceneの宣言の`source-layer`）と、アプリのレイヤーを足した後に
  呼んだときに差し込み位置と面の移動が変わるかを、`mapStyleOps.test.ts`の形で確かめる →
  ③変わるなら今の理由を現在形1文へ、変わらないならその文と、それに従うためだけにある形
  （呼ぶ時機の縛り）の両方を消す。
- **完了**: ①②のコマンドと観測値が本ファイルにあり、③のどちらかになっている。

## 段階Eの方針（2026-09-23、寄せた項目）

段階Eは「到達しない公開ドアの内部化・削除」。次の2件は同じ母集団（本番の入口から辿れないもの）を
frontendで出す作業で、別々に回すと同じ走査を2回することになる。

**E-1. frontendの到達可能性とテストの整理**（元[T968](T968.md)の段階6・7。T968はbackendの段階を持って残る）

- **何を（到達可能性）**: 本番の入口から辿れない定義を出し、1件ずつ処分する。入口はbackendと
  別物で、App Routerのファイル規約で決まるページ・レイアウト・APIルート（`src/app/**`）、
  クライアントの入口（`page.tsx`が読むコンポーネント）、生成物（`src/types/generated/`）を読む側。
- **道具の決め方**: 既存ツール（`knip`・`ts-prune`）が使えるかを先に測り、**入れるかは出力の質を
  見てから決める**。このリポジトリは生成物の型・MapLibreのレイヤー宣言・軸カタログ由来の動的な
  レイヤー生成を多用するため、素朴な未使用検知は誤検知が出やすい。CLAUDE.md「修正の原則」の
  「機械の出力をそのまま処置へ繋げない」（フレームワークが規約として読む口、文字列で解決される参照、
  走査範囲外の`frontend/e2e/`は参照0に見える）をそのまま当てる。段階Aの実測（参照なし3本・
  テストからのみ参照50本台）が出発点。
- **処分の基準**: 孤立点1件ごとに「消した」か「残す理由＋いつ通るか＋観測方法」のどちらかにする。
  測って0件だったものが残っていないこと。
- **何を（テスト）**: `vitest --coverage`で、テスト専用の口を「正当」「取り残し（削除）」に分け、
  テスト同士の直接依存を0件にし、「そのテストだけが通す行が0行」のテストを1件ずつ判断する。
  置き場は層・重さ・実装との対応の3軸で見て、移したものと移さない理由を1件ずつ書く。frontend固有の
  軸が1つある——**DOMを使わないテストがjsdom環境のままになっていないか**
  （`docs/conventions/testing.md`の`// @vitest-environment node`）。母集団はdocblockが無く、
  `render`/`renderHook`/`document`/`window`を1つも使っていないテストファイル。
- **完了**: 上の各項目の母集団と処分が本ファイルにあり、減った行数（実装・テスト）が記録されている。

**E-2. `MapView.tsx`の宣言に結び付かない段落**（元[T1055](T1055.md)の残り4。T1055は他の残りを持って残る）

- **何を**: `components/Map/MapView.tsx`の`// payload（page.tsx側が各要素のデータ層関数から計算した値）を
  反映する。…`で始まる段落（2026-09-23は161〜168行）。動的気象のソースごとの表示判定（visibleと
  payloadの両方が揃うまで非表示、payloadの種類に合うサブレイヤーだけ表示）を説明しているが、
  説明している「このループ」はMapViewに無い（sceneへ移った）。名前が実在するため、撤去済みの名前を
  拾う列挙には出ない。直後の`// interactive:`で始まる段落も、次の宣言（`LayerDataSource`）と
  結び付いていないかを同時に確かめる。
- **決め方**: `comments.md`の判定木へ1件ずつ通す。同じ内容が`docs/modules/frontend/dynamic-weather-layers.md`
  かsceneの宣言にあればMapView側を消す。無く、今も成り立つ横断的な判断なら`docs/modules/`へ昇格して
  から消す。
- **完了**: 段落がMapViewから消え、中身が正しい置き場にあるか、不要と判定した理由がコミットメッセージにある。

**寄せなかったもの**: 調査時に段階Eへ寄せると判定した[T1013](T1013.md)の項目4（「線のレイヤーは
路面ソースを共有するため、先に積むと`source not found`になる」）は、段階Aで成立しなくなっていた。
`features/map/scene/applyMapScene.ts`は1回の適用で全ソースを足してから全レイヤーを足すため、
順序で`source not found`になる経路が無く、その文も残っていない（`grep -rn "source not found\|ソースを共有\|順序に頼" frontend/src`
の本番コードの該当は、路面と無関係の`lib/backendInternalUrl.ts`の1件だけ）。T1013側に観測値を書いて閉じた。

## 手順4（テスト）へ寄せた項目（2026-09-23）

「進め方」の手順4（同じ単位でテストも起こし直す）で、対象のテストに届いたときに当てる。
**どの段階も届かなかった項目は、段階Eの完了前にまとめて当てる**（段階Eの完了条件に含める）。

**T-1. テストの共通部品が、そのテストに関係ない値を既定で持っている**（元[T981](T981.md)のfrontend分。
backendと`frontend/e2e/fixtures.ts`はT981に残る——E2Eは[T1014](T1014.md)が作り直している）

- **何を**: 共通部品（フィクスチャ・ビルダー・ファクトリ）が、型を満たすため以外の「もっともらしい値」を
  既定で持つと、その値に関心のないテストまでそこに依存し、部品を1つ直すと無関係なテストが落ちる。
  実例: 軸カタログの共通フィクスチャが`unit: "点"`・`thresholds: [20,40,60,80]`を焼き込んでいたため、
  単位にもしきい値にも関心のない凡例ラベルのテストが`'0未満'`と`'0%未満'`の食い違いで落ちた
  （[T970](T970.md)段階E）。あるべき形は**透明な部品**——既定値は型を満たすための空（`[]`・`""`・`null`）
  だけにし、見たい性質は呼び出し側のテストが上書きで書く。
- **対象の出し方**: ①共通部品は`git ls-files "frontend/src/**/__fixtures__/*" "frontend/src/testing/*"`
  （2026-09-23は`components/Map/__fixtures__/catalogAxes.ts`と`testing/`配下）→ ②各部品について
  既定で値を持つフィールドを列挙する（空・`null`・0件は除く）→ ③その部品を使うテストを列挙し、
  **そのフィールドを読んでいないテストが1本でもあれば違反**。
- **直し方**: 既定値を空へ落とし、必要な性質は各テストが上書きで書く。よく使う組み合わせは部品の中に
  隠さずテスト側のローカル定数にする。複数のテストが同じ組み合わせを要するなら、その組み合わせ
  自体が検証対象の性質かを先に問う（性質ならその名前で部品にしてよい）。
- **完了**: 部品ごとに既定で持つ値と、それを読まない利用テストの件数が実測で本ファイルにある。
  違反を直した後、**部品の既定値を1つ変えて関係ないテストが落ちないこと**を確かめている。直さないと
  決めたものは理由がある（例: その値がその部品の定義そのもの）。

**T-2. 責務の外から触っている軸スタジオのテスト2件**（元[T998](T998.md)の「frontendで見つけたが、
こちらでは直さないもの」の残り。3件目の`WarningBadge`は生成型を読む形に既に直っている）

- `components/AxisStudio/AxisComposer.materialValues.test.tsx`: 値の候補セレクトは
  `AxisScoringSection.tsx`の実装なのに、`AxisComposer`越しに触っている。取得応答（`getMaterialValues`）を
  差し替えて`AxisScoringSection`を直接立ち上げれば、候補あり・空・取得失敗の3経路をその場で見られる。
  同じファイルには、真偽材料には値欄そのものが描かれないため「候補セレクトが出ない」の検査が
  成立していない（常に通る）ものがあった——起こし直すときにこの形を作らない。
- `components/AxisStudio/AxisStudio.test.tsx`: 保存の検証で`AxisComposer`のフォーム（`表示名`・`axis_id`・
  `既定重み`・`作成する`）を直接操作している。`AxisStudio`の責務は「どのモードで開くか」と「保存の結果を
  どう扱うか」なので、子を差し替えて`onSave(payload, isNew)`を呼ばせれば足りる
  （`AxisComposer.test.tsx`は自分のフィールドだけを触っておりクリーン）。
- **完了**: 2ファイルが自分の責務だけを見る形になり、冒頭に何を見て何を見ないか（どのファイルが
  持つか）が書かれている（T998の完了条件の書き方）。

## 保留（ユーザー判断）: 残っているフロント系の改善をT1001の段階へ寄せるか（2026-09-23 依頼）

ユーザー依頼「フロント系の残っている改善はT1001にまとめられるか、それぞれ見てほしい」。読み取りの調査
（origin/master f02977e6、未完了85件のうちフロント系30件）で、統合できると判定したもの:

- 段階B: T1013の項目1・2、T1010の項目2・4
- 段階C: T629（統合済み、T629.mdに決定）・T953・T715・T1013の項目5
- 段階D: T1060（ユーザー決定で吸収済み。段階表の充当欄に無い）・T826（台帳・段階表は段階Cだが中身は段階D）・T1013の項目3
- 段階E: T968の段階6・7、T1013の項目4、T1055の残り4
- 手順4（テスト）: T981のfrontendフィクスチャ分、T998の「frontendで見つけたが直さないもの」の残り2件

統合しないと判定したもの（主にbackend・新しい設計・判断待ち・トリガー未発火）: T1014・T1069・T1017・T640・T1052・
T542・T808・T779・T854・T925・T957・T396・T631・T307・T273・T206。

**決めること**: 上の「統合できる」を段階表と各段階の節へ書き足すか。

**ユーザー決定（2026-09-23）**: 寄せる。寄せた側のうち全部を寄せたものは閉じる。
段階表の充当欄を直し（T826は段階Cから段階Dへ）、中身を各段階の「寄せた項目」節と「段階Dの方針」・
「段階Eの方針」・「手順4（テスト）へ寄せた項目」へ写した。閉じたのはT629・T953・T715・T826・T1060・T1013、
一部だけ寄せたのはT1010・T968・T1055・T981・T998。着手前の確認で、T1013の項目4・5は既に解消していたため
寄せずにT1013側で閉じた（段階C・Eの「寄せなかったもの」）。


所要（並行実行）: I-merge（他タスクの統合、文書のみ） 依頼 2026-09-23 18:49 → 完了 19:09（20分）、うち枠待ち0分・CI待ち不明、規模札M
