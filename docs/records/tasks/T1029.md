# T1029. sceneの入力語彙を、上位のReactコンポーネントのpropsから切り離す

状態: 完了（2026-09-23）

## 背景

T1001段階Aで`features/map/scene/**`を新設し、`MapView.tsx`は3,382行から1,608行へ減った。
残った1,608行は「MapLibreの命令的な世界との境界（マーカー・ポップアップ・イベント）」
という単一の変更理由に収まっており、**この分割自体は今周期で最大の改善**である。

一方で、新しい層が旧居へ依存し返している。

- `features/map/scene/applyToMap.ts`が`MapView.tsx`から`MapViewProps`型をimportし、
  `MapView.tsx`が`applyToMap.ts`をimportする——**本番コードのimport循環**（型のみ）。
  直接の原因は、sceneの入力型を`Pick<MapViewProps, …>`で組み立てていること。
  **sceneの入力語彙を、上位のReactコンポーネントのpropsが定義している。**
- `features/map/**` → `@/components/**` のimportは26本、逆向きは11本。
- `mapSceneGroups.ts`は「見た目の値はこのファイルが持つ（呼び出し側は埋めない）」と
  宣言しているが、実際の値（分類外の色・降水と風のカラーストップ・危険度の色・
  凡例の欠損キー）は旧居の`components/Map/*`にあり、そこから借りている。
  **適用の機構だけが移り、宣言は移っていない。**
- `components/Map/axisLayers.ts`には「循環import回避のため値を複製している」という
  コメントが残っている（直下は生成物を読んでおり、実際には複製ではない）。

結果として`features/map/scene`は`components/Map`の下でも上でもなく横に並び、
**次に地図の部品を足す人が、どちらへ置くかの基準を得られない**。

## 対応方針

**2段に分ける。第2段を同じタスクに含めない**（含めると、循環を切るだけの小さな修正が
26本の設計判断に引きずられて止まる）。

1. **第1段（このタスク）**: `applyToMap.ts`の入力型を`MapViewProps`から切り離す。
   sceneが必要とする入力を、sceneの側で宣言する。これで循環が消える。
   `MapView.tsx`の単一責務は壊れない（型の向きが変わるだけ）。
2. **第2段（別タスクへ切り出す。切り出す先の中身を、第1段の完了時に具体化する）**:
   26本の依存について「どの値がsceneの持ち物か」の基準を決めてから移す。
   CLAUDE.md「段階を別タスクへ切り出すときは、切り出す先の中身を具体化してから
   切り出す」に従い、**基準を決めずに『あとは移すだけ』として切り出さない**。

## 完了条件

- 本番コード（`*.test.*`を除く`frontend/src/**`）のimport循環が、
  `features/map` ⇄ `components/Map` の間で0件。
  導出: `import`/`export … from`の辺をDFSで走査する（`import type`も辺に数える——
  実行時には消えるが、読む側と道具から見た循環は残る）。
  **既知の1件（`applyToMap.ts` ⇄ `MapView.tsx`）が直す前に検出されることを確認してから
  使う。**
- `axisLayers.ts`の「循環import回避のため複製している」というコメントが、現状に
  照らして正しいかを判定し、正しくなければ落とす。
- 第2段の中身（基準の決め方と対象の導出方法）が、このエントリか新しいエントリに
  具体化されている。

## 範囲外

`RouteForm.tsx` ⇄ `useRouteFormSubmit.ts` の循環は本範囲の変更で生じたものではない。
第1段の導出で検出されるが、このタスクでは扱わない（扱うなら別途判断する）。

## 結果（2026-09-23）

### 循環の導出

TypeScriptのASTで本番コード（`*.test.*`・`__fixtures__/`・`testing/`・`structure/`を除く
`frontend/src/**`）の`import`・`import type`・`export … from`・動的`import()`を辺にし、
強連結成分を取った。直す前は2成分: `MapView.tsx` ⇄ `applyToMap.ts`（既知の1件、検出を確認）と
範囲外の`RouteForm.tsx` ⇄ `useRouteFormSubmit.ts`。直した後は範囲外の1成分だけになった
（`features/map` ⇄ `components/Map`をまたぐ循環0件）。

### 第1段

- `applyToMap.ts`の入力型（`SceneWiringProps`）を`Pick<MapViewProps, …>`から、sceneの側の
  型宣言へ置き換えた。各フィールドの型は下位の宣言（`RouteCandidate`・`RampAxis`・
  `DynamicWeatherGroupState`等）から直接引く。
- 乗り換え区間の入力型は`MapView.tsx`のローカル型だったため、sceneの側
  （`applyToMap.ts: SpliceStretchInput`）へ移し、`MapView.tsx`がそれをimportする向きにした。
- `axisLayers.ts: COLOR_UNKNOWN`の「循環import回避のため値を複製している」は**誤り**と判定して
  落とした。値は生成物の`palette.semantic.no_data`を読むだけで、`COLOR_UNKNOWN`の定義は
  リポジトリに1つしか無い（`grep -rn "COLOR_UNKNOWN =" frontend/src`で1件）。複製ではない。

### 第2段

[T1060](T1060.md)へ切り出した。対象の導出（sceneが`components/Map`からimport文で名指しする
名前を、外の本番消費者の有無つきで列挙）・どの値がsceneの持ち物かの基準・着手前に決める
置き場の候補・完了条件を、そのエントリに具体化してある。起票時点の見立ての「26本」は
import文の本数で、名前で数えると37名（sceneの入力型を下位の宣言から直接引いたため、型の名前が
増えた）。

所要（並行実行）: 依頼 2026-09-23 不明 → 完了 不明（約10分。担当の報告の時刻から見た振り出し〜報告。T1067.mdの背景）、うち枠待ち不明・CI待ち不明、規模札S
