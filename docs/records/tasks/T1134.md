# T1134. 軸の0次条件（当たる道を通らせない条件）が、区間の内訳・地図の色・ルート選びで効き方が違う

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 片側だけの修正・同じ計算の重複（再発）。0次条件の一致判定がスカラー版（axis_definitions.py:761-770、真偽を"true"/"false"文字列にして比べる）と配列版（:922-928、dtype==boolのときだけ真偽と比べる）の2本。(1)「不明」を持つ真偽の材料（surface_good）は配列経路でfloatの行列に載り文字列比較が全要素偽→区間の内訳（スカラー）では条件が効くのにルート選び（配列）では一度も効かない (2)equalsが"yes"・"1"等だと配列版は偽の行すべてを一致とみなし、スカラー版はどれも一致させない（numpy 1.26.4で確認、本番は2.5.2）。スカラー版を使う: 区間インスペクタ（axis_inspector.py:65）・ルート前の専用way値レイヤー（dynamic_way_values.py:171）。配列版: ルート選び・ルート線（evaluation.py:260、dynamic_materials.py:109）。(3)地図のramp描画はタイル入力をshape.termsだけで作り（axis_display.py:194-218、TileInputSpecに0次条件の形が無い）、タイルに載る材料を0次条件にした軸では条件が地図に効かない。本番に該当する軸があるかは未確認（管理APIに認証が要る）。直す方針: 一致判定は配列版1本にしスカラー版は長さ1の配列で呼ぶ（wind_drag_ratioの形）、0次条件を持つ軸の地図は条件を反映するかkind=noneにそろえる。残りのスカラー版・配列版の対の一本化はT1115。
