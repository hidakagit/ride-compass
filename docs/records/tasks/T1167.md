# T1167. 重い処理の枠が、重くない段（gitの操作・自作スクリプトの包み）に長く持たれている

状態: 未完了（2026-09-26起票）

## 背景

2026-09-23〜26の枠の保持798分のうち、規約の対象の段の語（npm ci・next build・tsc・vitest・pytest・playwright・mypy等）を含まない呼び出しが約348分（44%）あり、その中に `git fetch origin master && git rebase ...` を枠に入れたもの（22回・35分）や、scratchpad の自作スクリプトを丸ごと包んだもの（1回22分等）がある。規約（orchestration.md「何を枠の下に置くか」）は「重い段と軽い段が混ざった一続きを丸ごと包まない」「git は包まない」と書くが、守られるかは担当の注意に任されている。直す方向: lockrun.py 自身が、受け取ったコマンドが対象の段か（規約の対象の項目のコード書式の語と同じ一覧）を見て、対象外は枠を取らずに走らせる・包みは拒否する等、枠に入るものを機械で決める。緩めたり除外したりする場合は、外れる件数と割合を上の記録で実測して書く。第3回の改善（派生元 T1162）。
