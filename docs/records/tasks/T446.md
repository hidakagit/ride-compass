# T446. buildRoadSurfaceSharedLayerIdsがgradientAxisを含まない非対称＋古いコメントの訂正

状態: 完了（2026-08-31）

- 背景: `統合レビュー第2回`（`.claude/commands/review/history/2026-08-30_all_2.md`P2-2）の
  指摘。`frontend/src/components/Map/mapLayers.ts: buildRoadSurfaceSharedLayerIds`
  （647-666行）が`"windAxis"`のみ含み`"gradientAxis"`を含まない——T440で風・勾配は
  同じ`dedicated_way_value_layer`機構で対称に扱われるようになったはずだが、この
  リストだけ非対称のまま残っている。
- 加えて、`windAxis`の`MapLayerDescriptor`近傍のコメント（507-516行）が
  「`RouteSettingsPanel.tsx`がこのリストをズームアウト警告に参照する」と主張するが、
  grep確認の結果`RouteSettingsPanel.tsx`にそのような参照は存在しない（事実誤り）。
- 対応方針: `gradientAxis`をリストへ追加する妥当性を確認（意図的な除外でないか、着手時に
  再確認）した上で追加。誤ったコメントは実態（誰が実際にこのリストを参照しているか）へ
  訂正する。
- Confidence: High（frontend/Mapシャードがgrepで参照元不在を確認済み）。
- 規模S。

## 実施記録（2026-08-31完了）

- **非対称の解消**: `buildRoadSurfaceSharedLayerIds`へ`"gradientAxis"`を追加
  （windAxisの直後）。省略は意図的なものではなく、T423でgradientAxisを追加した際に
  この対称化が漏れていただけと確認した（着手時の再確認で意図的な除外理由は見当たらず）。
  regionZoomTooWide判定（`MapView.tsx: isRoadSurfaceGroupVisible`→`updateRoadZoomHint`、
  「表示範囲が広すぎます」バナー）の対象に含まれるようになる。
- **コメントの訂正**: `RouteSettingsPanel.tsx`が`buildRoadSurfaceSharedLayerIds`経由の
  ズーム範囲外判定・`panelHint`のツールチップ文言を再利用しているという記述は事実誤り
  （grep・実装読解で確認: `RouteSettingsPanel.tsx: mapColorLayerIdFor`は
  `layerVisibility[layerId]`のON/OFFのみを扱い、tooltip文言も自前のハードコード文字列）。
  windAxisの旧コメントを、実際の消費経路（MapLayerId型定義＋
  buildRoadSurfaceSharedLayerIds経由のregionZoomTooWide判定の2点のみ）を正しく記す内容へ
  書き換えた。gradientAxis側にも簡潔な参照コメントを追加。
- **検証**: 回帰テスト1件追加（`buildRoadSurfaceSharedLayerIds`がwindAxis/gradientAxisを
  対称に含むことを確認）。`tsc --noEmit`・eslint・vitest（mapLayers/axisLayers/
  MapView.dataStatus/MapLayersPanel、計81件+新規1件）すべて通過。開発サーバー実機で
  コンソールエラー増加無し（既知の`/api/weather/amedas` 502のみ）を確認。
