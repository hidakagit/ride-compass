# T955: 材料の冷パスが160秒かかり、バッチのたびに最初の利用者が待たされる

状態: 完了

2026-09-19起票、2026-09-23完了（段階A＝[T956](T956.md)の後の冷パスを本番で測り直し、
[T958](T958.md)の発火を判定した。末尾「段階A後の冷パス」「T958（段階B）の判定」）

## 背景

本番でルート生成に174秒かかった（2026-09-19 17:48、`req:b7f19819cf4d`）。

```
generate(destination) prepare_ms=165428 select_ms=6659 evaluate_ms=470 total_ms=173898
_build_search_graph edges=542999 materials_ms=160469 lazy_graph_cached=False
  missing_axis_edges=542999 missing_axis_distance_ratio=1.000
```

**探索も評価も速い**。2分半後の同条件は`materials_ms=964`・`total_ms=6336`で、
冷えているかどうかで**174秒 ↔ 6.3秒**。

`docs/records/tasks/T747.md`が記録する材料の冷パスは29〜45秒規模で、いまは108〜160秒。
世代を上げるのは日常運用（バッチを流すたびに起きる）ため、**バッチ後に最初にその範囲を
見た利用者が3分待つ**。

**周回でも同じ**。材料は探索方式より手前の段階で、周回30kmの生成も116.6秒だった
（`via-node候補を打ち切りました`のWARNINGは目的地ルート特有だが、遅さとは無関係）。

## 冷パス1タイルの内訳（本番コンテナ内で実測、2026-09-19）

`_get_or_build_tile_materials`と`_get_or_build_tile_score_matrix`が呼ぶ関数を、
本番DBと同じVMのbackendコンテナ内でそのまま呼んで測った。

| 段 | 何をしているか | 42,134区間 | 37,073区間 | 11,777区間 |
|---|---|---|---|---|
| A | `get_graph_topology_in_bbox` | 1,566ms | 1,388ms | 423ms |
| B | `get_edge_materials_batch` | **4,170ms** | **4,305ms** | 1,069ms |
| C | `EdgeMaterialTable.from_bundles` | 198ms | 202ms | 83ms |
| D | `build_static_edge_score_matrix` | 1,897ms | 1,654ms | 483ms |
| | 合計 | **7,830ms** | 7,550ms | 2,058ms |

**合計がログの「1タイル8〜10秒」と一致する**。区間数にほぼ比例し、Bが約半分を占める。

さらにA・B・DをSQLとPythonへ割った（同じタイル、同じコンテナ）:

| | 所要 | 性質 |
|---|---|---|
| A Edge行のSQL＋転送 | 344ms | SQL |
| A Node行のSQL＋転送 | 258ms | SQL |
| A 行→`LeanRoadGraph`（Node・Edgeオブジェクト） | 557ms | 区間ごとのPython |
| B SQL＋転送（行が返るまで） | 2,575ms | SQL |
| B 行→`EdgeMaterialBundle` | 1,432ms | 区間ごとのPython |
| C bundle→列 | 198ms | Python |
| D `to_legacy_dicts`（列→区間ごとの辞書へ戻す） | 580ms | 区間ごとのPython |
| D 抽出ループ＋軸の計算 | 1,358ms | 区間ごとのPython |

**SQLが約4割、区間ごとのPythonが約5割。** numpyのベクトル計算はどこにも現れない。

### Dは全部が区間ごとのPython

`build_static_edge_score_matrix`をcProfileで見ると（計測負荷込みで4.77秒、比率で読む）:

- `dict.get`が**329万回**（区間あたり78回）。extractorが`elevation_attributes[edge_id]`等を
  区間ごとに引いている
- `to_legacy_dicts`の中で`ElevationAttribute`（Pydantic）を**区間数ぶん作り直している**
  ——列指向の表を、区間ごとの辞書へ戻すために
- `bicycle_infra_flags`が区間あたり5回。同じwayのタグ文字列を毎回parseし直している

### 表現が4回変わる

```
SQLの行 → EdgeMaterialBundle（Pydantic） → EdgeMaterialTable（numpy列）
        → to_legacy_dicts（Pydanticへ戻す） → 抽出ループ → numpy配列 → 軸の式
```

列指向の表を作った直後に、区間ごとの辞書へ戻している。

## 「評価に必要な列を1行に持つテーブル」があったら

同じ結果を一時テーブルへ落とし（読み出し用テーブルの代役）、本番で比べた。

| | 所要 |
|---|---|
| 現行（`road_edges`へ属性テーブル群をLEFT JOIN＋指定路線は相関EXISTS） | 2,052〜2,146ms |
| 1テーブルをbboxで走査 | 749〜848ms |
| 1テーブルから列ごとに`array_agg` | 915〜929ms |
| 現行のJOINのまま`array_agg` | 2,208ms |

**JOINをやめるだけでDB側が2.5倍**（約1,250ms/タイル）。`array_agg`はDB側では効かず
（JOINのまま掛けても変わらない）、効くのは受け取るPython側である（[T956](T956.md)）。

代役テーブルは1タイル（42,134区間）で19MB。全区間5,065,850に対して約2.3GBの見当。

## 最初の見立てが外れた理由

当初「行→bundleの構築が18.8秒で主犯」と書き、行から直接列を組む実装を入れたが、
本番では24,465ms→25,912msで**まったく速くならなかった**（実装は破棄済み）。

18.8秒は**開発機のPythonで測った値**で、本番の同じ処理は1,432ms——**13倍違う**。
開発機で部品を測り、本番の全体へ一般化したのが誤りだった
（CLAUDE.md「性能は利用者から見た単位で測る」、memory`feedback-perf-measure-on-prod-not-dev-db`）。

## 材料の導出は、SQLとPythonに二重実装されている

地図タイル（`_ROAD_SURFACE_TILE_MVT_SQL`）は探索と**同じテーブル群**を引き、同じ材料を
**SQLの中で**導出している。探索は同じ導出をPythonの区間ごとのループでやっている。
`osm_way_tag_sql.py`のdocstring自身が「Python版extractorとは別に、SQL側で同じOSMタグ
分類ロジックを必要とする」と書いている。

本番の1タイル（42,134区間）で、両方が実装する材料を突き合わせた。

| | 結果 |
|---|---|
| `surface_good` `surface` `highway` `smoothness` `has_tunnel` `bridge` `maxspeed_kmh` `lanes_count` `motor_vehicle_no` `lit` `highway_is_cycleway` `cycleway_has_track` `cycleway_has_lane` `cycleway_has_shared` `shared_pedestrian_path` `tracktype` | 不一致**0件** |
| `is_emergency_transport` `is_critical_logistics` | Python側に実装が無い（`extractor=None`）。参照する軸も無い |

**SQL側はPython側の超集合で、重なる部分は完全に一致する。** SQLにできない材料は
`wind_drag_ratio`だけ（リクエスト時の天候・方位・速度から決まる）。

## 導出をSQLへ寄せても、SQLは重くならない

| | 所要 |
|---|---|
| 生の列を行で返す（現行） | 1,403ms |
| 導出込みを行で返す | 1,483ms（+80ms） |
| 導出込み＋`array_agg` | 1,436ms（−47ms） |

一方、材料が列で届いたときに残る「軸の式だけ」は**全13軸で22.9ms**（現行Dは1,150〜1,900ms）。
**評価の本体は23msで、残りは全部「材料を区間ごとのPythonで作り直す」費用だった。**

## 対応方針

段階Aだけをこのタスクの範囲とし、段階Bは条件付きで残す。**両者は性質が違う**——Aは
既存の仕組みを効率化するが、Bは新しい派生データを1つ増やす（ユーザー判断）。

### 段階A（[T956](T956.md)）: 導出をSQLへ寄せ、列で受ける

`EdgeMaterialBundle`・`from_bundles`・`to_legacy_dicts`・`MaterialExtractionContext`・
区間ごとの抽出ループが消え、表現の変換が4回から1回になる。**増える永続状態は無い。**
見込み: 1タイル7,830ms → 約3,100ms。

前提として[T960](T960.md)（SQL側の導出に期待値テストを付ける）を先に済ませる。
`tests/test_material_catalog.py`の期待値はPython側にしか当たっておらず、**Pythonの
extractorを消すなら、その期待値がSQL側に当たっている必要がある**。

### 段階B（[T958](T958.md)）: 導出結果をテーブルへ

さらに約800ms/タイル。ただし新しい派生テーブルの鮮度・世代の判定が増える。
**段階Aの後に残りを測ってから判断する。**

### 残る

ウォームアップ（[T959](T959.md)）は段階A・Bとは独立に要る。

## 完了条件（2026-09-23、[T1028](T1028.md)で追加）

- 段階A（[T956](T956.md)、完了済み）の後の冷パスを、上の「冷パス1タイルの内訳」と同じ
  測り方（本番のbackendコンテナ内で、同じ3タイルに対して段ごとに所要ms）で測り直し、
  この表の隣へ書く。T956が残したのは同一実行内の比（3.6倍／5.0倍）だけで、絶対値が無い。
- その値で[T958](T958.md)（段階B）の要否を判定し、T958へ書く。**T958の発火を判定できるのは
  この計測だけ**で、ここで測らないとT958の条件は永久に判定されない。

## 段階A後の冷パス（2026-09-23、本番で測り直した）

### 測り方

`backend/scripts/run_probe.py --in-container`で本番backendコンテナ内から、
`_get_or_build_tile_materials`・`_get_or_build_tile_score_matrix`がキャッシュを外したときに
呼ぶ関数（A `get_graph_topology_in_bbox`・B `get_edge_material_arrays`・
D `build_static_edge_score_matrix`）をそのまま順に呼んだ。軸定義は
`refresh_axis_definitions`で本番DBから読み込んだ（読まないと公開軸が0本で行列が空になる）。
キャッシュへは書かない。各タイル3巡し中央値を取った。

**タイルの選び方**: 42,134区間は`z12/3638/1612`、11,777区間は`z12/3638/1613`
（コミット680799d6e・T956の記録）。37,073区間のタイルはどの記録にも座標が無く特定
できなかったため、隣の`z12/3637/1612`（41,439区間）で代えた。区間数が上の表と少し違うのは
その後のデータの作り直し（T1033等）による。

### 1タイル（中央値、3巡）

| 段 | 何をしているか | 3638/1612（40,599区間） | 3637/1612（41,439区間） | 3638/1613（11,748区間） |
|---|---|---|---|---|
| A | `get_graph_topology_in_bbox` | 1,895ms | 1,225ms | 444ms |
| B | `get_edge_material_arrays` | 1,567ms | 1,548ms | 467ms |
| C | （段階Aで消えた） | — | — | — |
| D | `build_static_edge_score_matrix` | **14ms** | **15ms** | **6ms** |
| | 合計 | **3,401ms** | **2,751ms** | **916ms** |
| | 段階A前（上の表） | 7,830ms（42,134区間） | 7,550ms（37,073区間） | 2,058ms（11,777区間） |

**見積もり（約3,100ms）どおり。**Dは1,897ms→14msで、評価の本体（23ms）だけが残った。
ディスクキャッシュへ書く費用（pickle）は材料19ms・行列10ms（40,599区間で11.7MB・9.9MB）。

AとBをさらに割った（同じ3タイル、3巡の中央値）:

| | 3638/1612 | 3637/1612 | 3638/1613 |
|---|---|---|---|
| A Edge行のSQL＋転送 | 934ms（巡回で244〜1,037ms） | 244ms | 116ms |
| A Node行のSQL＋転送 | 238ms | 259ms | 59ms |
| A 行→`LeanRoadGraph` | 614ms | 611ms | 167ms |
| B SQL＋転送 | 1,391ms | 1,376ms | 408ms |
| B 行→配列（全体からSQLを引いた概算） | 141ms | 148ms | 45ms |

**いま残っているのは、SQL（AのEdge・Node、B）が約3分の2、Aの行→グラフが約5分の1。**
区間ごとのPythonで材料を作り直す費用（段階A前の約5割）は消えた。

**DBのバッファが冷えていると1タイルで12,287ms**（1巡目の3638/1612、うちA 7,565ms・
B 4,708ms）。バッチの直後はこちらに近い状態になりうる。

### 利用者から見た単位（1リクエスト＝z12を4×4枚）

同じくコンテナ内で、`GraphService.get_search_materials_for_bbox`をキャッシュの読み書きと
世代の突き合わせ（`ensure_caches_match_db`）だけ差し替えて呼んだ（本番のキャッシュには
触れない）。bboxは`z12/3636〜3639 × 1610〜1613`。

| | 区間数 | `materials_ms` |
|---|---|---|
| 段階A前（背景のログ、2026-09-19） | 542,999 | **160,469** |
| 段階A後・1回目 | 555,710 | **52,892** |
| 段階A後・2回目（DBのバッファが温まった状態） | 555,710 | **42,099** |

**材料段は3.0〜3.8倍速くなったが、冷えた範囲を最初に見る利用者はまだ42〜53秒待つ**
（ルート生成全体では、温まったときの残り約6秒がこれに乗る）。

## T958（段階B）の判定

**残りはまだ問題である**（42〜53秒）。したがってT958の発火条件「残りがまだ問題」は成立した。

ただし**T958だけでは解消しない**。T958が削るのはBのSQL（1タイル約1,400ms）で、上の
「評価に必要な列を1行に持つテーブル」節の代役テーブル（列ごとの`array_agg`で915〜929ms）
から見込める稼ぎは1タイル約450〜500ms、16枚で約7〜8秒——42秒の2割に満たない
（見込みであり未計測）。AはT958の対象外で、同じ規模で残る。
一方、[T959](T959.md)（世代を上げた直後に主要エリアを先に焼く）は「バッチのたびに最初の
利用者が待たされる」を、冷パスの長さに関係なく主要エリアで消す。

T958は新しい派生テーブルを1つ増やす提案で、起票時に「段階Aの後に実測してから判断する
（ユーザー判断）」とされている。着手するか・どの範囲で着手するかの判断は、値と選択肢を
添えて[T958](T958.md)へ保留として書いた。

## 最初に外した理由

当初「行→bundleの構築が18.8秒で主犯」と書き、行から直接列を組む実装を入れたが、
本番では24,465ms→25,912msで**まったく速くならなかった**（実装は破棄済み）。

18.8秒は**開発機のPythonで測った値**で、本番の同じ処理は1,432ms——**13倍違う**。
開発機で部品を測り、本番の全体へ一般化したのが誤りだった
（CLAUDE.md「性能は利用者から見た単位で測る」、memory`feedback-perf-measure-on-prod-not-dev-db`）。
