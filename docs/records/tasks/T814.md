# T814. 鮮度台帳がbatchをトップレベルimportする依存を断ち、台帳の網羅性の穴を塞ぐ

規模S〜M。
状態: 完了（2026-09-13。統合レビュー第8回のP1。対応方針3は[T832](T832.md)へ分離）

## 背景

### 1. 依存の向きが逆で、本番webイメージが起動失敗しうる

`app/infrastructure/derived_data_freshness.py:23-26`がinfrastructureから
`app/batch/precompute_*.py`を4本**モジュールトップでimport**しており、batch側は
`road_graph_repository`（infrastructure）をimportし返す循環になっている。

かつwebイメージは`requirements.txt`しか入れない（`backend/Dockerfile:5-6`、rasterio等は
`requirements-batch.txt`のみ）。そのため`precompute_*`のどれかにbatch専用依存の
トップレベルimportを1行足すと、**テストとCIはbatch依存が入っているため緑のまま通り、
本番webイメージだけが**`api/routers/derived_data_freshness.py`→
`infrastructure/derived_data_freshness.py`のimport連鎖で`ModuleNotFoundError`を起こして
backendが起動失敗する。

現在この規約を守らせているのは`precompute_way_landcover.py:88-90`の「ここでのみ読み込む」
というコメント1つだけで、検知器もテストも無い。

### 2. 台帳の網羅性テストが、版数を宣言しないバッチを素通りさせる

`tests/test_derived_data_freshness.py:140-170`の母集団は「`precompute_*.py`のうち
`ALGORITHM_VERSION`を宣言しているもの」であるため、版数を宣言しなければ台帳に載らなくても
テストを通る。`precompute_edge_curvature.py`がこれに該当し、`ALGORITHM_VERSION_NOT_IN_LEDGER`も
空のままで理由の記録が無い。

結果、`way_geometry`（地図・軸スタジオ用）は台帳に載ったが、**ルート評価が実際に読む**
`road_edges.curvature_deg_per_km`は載らず、`road_edges`に系譜列自体が無い。PBF再取込後に
`precompute_edge_curvature`の実行だけを忘れると、新規splitされたEdgeはNULL
（＝蛇行軸が重み再正規化で薄まって無警告で評価から抜ける）のまま残るが、
`GET /api/admin/derived-data/freshness`にはその陳腐化がどこにも現れない。同じ穴は
`precompute_road_node_degrees`（`road_nodes.degree`）にも当てはまる。

## 対応方針

1. 台帳が必要とするのは`ALGORITHM_VERSION`定数だけなので、**モジュールをimportせずに読む**
   （定数を依存の無いモジュールへ寄せる、またはASTで読む）。依存の向きを
   infrastructure→batchから断つ。
2. 網羅性テストの母集団を「版数を宣言しているもの」から「`precompute_*.py`のすべて」へ広げ、
   載せない場合は`ALGORITHM_VERSION_NOT_IN_LEDGER`へ理由つきで明示させる。
3. `precompute_edge_curvature`・`precompute_road_node_degrees`の扱いを決める（系譜列を足すか、
   理由つきで除外するか）。**除外を足すなら、対象から外す件数と母集団に対する割合を実測して
   このエントリへ書く**（CLAUDE.md「修正の原則」）。
4. 1は**わざと壊して確認する**——`precompute_*`の1本へweb側に無い依存のトップレベルimportを
   足し、`api/routers/derived_data_freshness.py`のimportが落ちることを確かめる。

## 実装・検証（2026-09-13）

### 1. 依存の向きを断った

版数を`domain/derived_data_versions.py`へ置き、台帳（infrastructure）もバッチもそこから読む
形にした。domainは依存の最下層なので、infrastructure→domain・batch→domainのどちらも正しい
向きになり、台帳から`app.batch`のimportが消えた。`precompute_way_landcover`が持っていた
「径から版文字列を作る規則」と既定の径も同じ場所へ寄せた（バッチ側は名前を再公開するだけ）。

**実地で確認した**（バッチへweb側に無い依存`zzz_batch_only_dependency`のトップレベルimportを
1行足した状態で）:

| 台帳の依存の向き | `import app.api.routers.derived_data_freshness` |
|---|---|
| 修正前と同じ（台帳→batch） | `ModuleNotFoundError: No module named 'zzz_batch_only_dependency'` |
| 修正後（台帳→domain） | 通る |

### 2. 再発防止は検知器にした（自己申告のコメントを増やさない）

`scripts/review_checks.py`へ`web_layer_batch_import`を新設。`backend/app/{api,services,
infrastructure,domain}`が`app.batch`をモジュールトップでimportする行を弾く（関数内の遅延
importは起動時に評価されないため対象外）。上の実験でこの向きへ戻したとき、検知器が
`derived_data_freshness.py:23`を1件として出すことを確認した。既存違反は0件のため除外リストは
持たせていない。pre-commit・CI・全件のいずれの経路でも強制する。

### 3. 網羅性テストの母集団を広げた

母集団を「`ALGORITHM_VERSION`を宣言しているもの」から`precompute_*.py`**すべて**へ。
版数を宣言しなければ素通りできる穴（`precompute_edge_curvature`が実際にそうなっていた）が
閉じた。`ALGORITHM_VERSION_NOT_IN_LEDGER`は`PRECOMPUTE_NOT_IN_LEDGER`へ改め、台帳に無い
バッチはすべて理由を書かせる。**版数を宣言しない新バッチを1本置くと検査が落ちることを確認**
（置いた状態で`test_every_precompute_batch_is_in_the_ledger_or_has_a_reason`が
`{'precompute_zzz_probe'}`で落ち、消すと通る）。

### 4. 母集団拡大で要判断になった3件の実測と扱い

`precompute_*.py`は7件。世代比較の台帳に載るのが4件、`precompute_elevation_attributes`は
完成度（行数差分）で別枠に載っているため理由つきで除外。残る
`precompute_edge_curvature`・`precompute_road_node_degrees`（**7件中2件＝29%**）は
書き込み先（`road_edges`・`road_nodes`の列）に系譜列が無く、**載せる手段そのものが無い**。
理由つきで除外したうえで[T832](T832.md)へ分離した——理由を書いて消えるのは検査であって
実害ではないため、実害（PBF再取込後の実行忘れが無警告で評価から抜ける）は別タスクとして残す。

テスト: backend 1900件超（postgis除く）緑、`ruff`指摘なし、`review_checks.py docs --staged`
違反なし。

## 関連

- 由来: 統合レビュー第8回 F-6（P1）＋台帳の穴。詳細は [history/2026-09-13_all.md](../../../.claude/commands/review/history/2026-09-13_all.md)、個票は [history/2026-09-13_all_shards.md](../../../.claude/commands/review/history/2026-09-13_all_shards.md)（S3）。
