# T1160. 地図の操作部品で、ⓘ記号が2つの意味を持ち、ルートの端が操作ボタンの下に隠れ、ズームボタンの名前が英語になっている

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 状態が画面に届かない・共有断片から外れた手書き。(1)InfoIconは説明（InfoPopover・AxisContributionBar）に使うが、MapOverlayControls.tsx:523だけが「表示する項目を選ぶ」の起点に使い、開いた一覧に見出しが無い（見出しはDetailPopoverのtitleだけ）うえ、一覧の各行にも説明のⓘがあり同じポップオーバーの中で記号の意味が割れる (2)ルート生成後の視点合わせの余白はROUTE_FIT_BASE_PADDING_PX=40（MapView.tsx:178）で、覆いの幅を渡すのはモバイルの下端だけ（page.tsx:514 measureRouteFitObscuredPx）。右の操作列（50px）・左のチップ列（94px）の下にルートの端が入る (3)MapView.tsx:484がmaplibregl.NavigationControlをlocale無しで作り、ズーム・方位ボタンのaria-labelが「Zoom in」等の英語。e2eのbackendモックで確認（スクリーンショットはレビュー時の作業場所ui013/f11・r05・r10・r21、計測はnotes4.jsonl）。
