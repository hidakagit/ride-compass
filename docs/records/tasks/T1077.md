# T1077. 実データで回すe2e（frontend/e2e-live/）の中身を作る（まず定義案をユーザーへ出す）

状態: 未完了（2026-09-25。段階B・C＝実装と実測、残りの1・2＝壊した入力の確認と全シナリオの緑を済ませた。残りは末尾「残り」節の3で、雨か警報のある日のトリガー待ち、2026-09-26）

## 背景

T1014段階Dで、実backend・開発DBへ向けて回すe2eの置き場（frontend/e2e-live/・playwright.live.config.ts、CIには載せない）を決めた（D-017、docs/conventions/testing.md パターン4）が、中身は無い。ユーザー決定（2026-09-23）: 中身を作る。定義案を考えて。段階A（定義案）: 何を守るためのe2eか（モックのe2eでは出ない壊れ方——本番に近いデータでしか出ない描画・実backendとの契約・キャッシュ・時刻や天候で変わる入力等——を母集団として列挙する）、シナリオの選び方の基準、各シナリオの期待値の書き方（実データは変わるので値そのものではなく性質で書く）、前提（開発DBのデータ範囲・backendの起動・外部API）、走らせ方と所要、誰がいつ回すか、を記録に書き、ユーザーの承認を待つ（改善提案と同じく記録に置く）。段階B以降（実装）は承認後。T1014.mdの「段階Dの後に残るもの」節と引き継ぎ節、testing.md パターン4を先に読む。

## 段階A: 定義案（2026-09-23）

土台はorigin/master `366f4709`。T1078の設計の確定（`d1a197e0`）を読んだうえで書いた。コードは書いていない。

### 何を守るか——母集団は「モックが置き換えている入力」から導く

`frontend/e2e/`（モック・CI）が見られないものは、**`e2e/fixtures.ts`の`installApiMocks`が本物の代わりに
返しているもの**そのものである。そこで母集団を手で考えず、モックが置き換えている入力を1つずつ取り、
「そこに本物が来たときにだけ起きる壊れ方」を類にまとめた。モックの置き換え方（2026-09-23時点の`installApiMocks`）:

| モックが置き換えているもの | モックでの中身 | 本物が来ると通る経路 |
|---|---|---|
| 基礎地図のスタイル（`/api/basemap/**`） | 空のスタイル（ソース・レイヤー無し） | backendが書き換えたタイル・スプライト・グリフのURL |
| 路面・POI・事故・土地被覆・気象庁・地理院のタイル（`next.config.ts`のrewritesの経路） | 204・中身無し | `features/map/scene/`の式へ実データの値が入る |
| 軸カタログ（`/api/axis-catalog`） | 1軸だけ・`tile_versions`空 | 開発DBの公開軸すべて・世代・動的値の配信（`dedicated_way_value_layer`） |
| ルート生成（`/api/routes/generate`） | 1回目から完了・固定の候補 | 実グラフでの探索・区間ごとの軸の値 |
| 気象（風・アメダス・警報・WBGT・洪水予報・気象庁の時刻一覧） | 固定値・空配列 | 実行した時刻で中身が変わる入力 |

この5つから、壊れ方の類を次の5つに分けた。**実例はすべてリポジトリの記録から取った**（下の「根拠の拾い方」）。

| 類 | 壊れ方 | 記録にある実例 |
|---|---|---|
| K1 実データでしか出ない描画 | 実際の値・型・組み合わせが式や部品に入って初めて壊れる | [T367](T367.md)（公開軸の材料がタイルへ焼かれておらず、地図に一切出ない）・[T886](T886.md)（DBから読んでいる土地被覆の内訳が画面へ渡らない）・[T485](T485.md)（MapLibreが区間のオブジェクト値を文字列にし、ルート線を押すとポップアップが例外で開かない。単体テストはオブジェクトを直接渡すので通っていた） |
| K2 実backendとの契約（形は型が守るが、意味と組み合わせは守らない） | カタログ・タイル・動的値の宣言どうしが食い違う | [T672](T672.md)（3つ目の専用レイヤー軸を公開すると地図に出ず、既存軸をOFFにすると巻き添えで消える）・[T643](T643.md)（取得の失敗が「200＋空タイル」で返り、画面は「道が無い」と区別できない） |
| K3 キャッシュ・世代 | 古い値・空の値を掴んだまま出し続ける | [T643](T643.md)（その空タイルがブラウザに1時間残る）・[T574](T574.md)（ディスクから古いタイルが復元され続け、未訪問のタイルだけ新しくなる）・[T691](T691.md)（修正前に焼いた材料キャッシュが同じ世代番号で残り、修正のデプロイ後も値が来ない） |
| K4 時刻・天候・外部配信の仕様で変わる入力 | 実行した時刻・ズームで出たり消えたりする | [T619](T619.md)（日本時間0〜8時台だけ時刻が9時間ずれ、風のレンズが全て灰色）・[T637](T637.md)（気象庁のキキクルは偶数ズームにしか実データが無く、奇数ズームで危険度が消える） |
| K5 実グラフでしか出ない探索の欠陥 | 実データの形（並行する道・取込範囲の端）で生成が落ちる | [T790](T790.md)（並行するEdgeがあると探索用グラフとスコア行列の行が食い違い、ルート生成が丸ごと落ちる。開発DBの実行で見つかった）・[T236](T236.md)（取込範囲の外周で生成が失敗する） |

**対象にしないもの**（e2e-liveに入れない）:

- 見た目・色・読みやすさ（パターン4の観点4と同じ。人が見る）。
- 性能・所要。開発DBは本番と規模が違い、計測は本番で行っている（例: [T531](T531.md)・[T538](T538.md)の本番計測）。
- 本番DBにしか無い軸・データ。e2e-liveが見るのは開発DBにある公開軸で、軸の正本は本番DB（CLAUDE.md）。
- backend内部のキャッシュが**正しい値か**（K3の原因そのもの）。データを書き換えないと観測できない。e2e-liveは
  症状（値が来ない・空で返る）だけを見る。
- 画面の全状態の走査。状態の木を辿るのはモックのe2e（T1078）の仕事で、実データでは状態の数が決まらない。

**根拠の拾い方**（再現用）: `git grep -n -E "モックでは(出|見え|通)|実データでしか|本番でしか|地図に出ない|真っ白|灰色になる|描かれない|表示されない" -- docs/records/tasks`
と`git grep -n -E "起動直後|デプロイ直後|キャッシュに効かない|取込範囲外|深夜|実データでしか" -- docs/records/tasks`
の該当から、原因が「実データ・実backend・実時刻が来たときだけ起きる」ものを選び、記録の本文で原因を確かめた。
モックのe2eでも捕まえられた形のもの（Workerの404で地図が真っ白＝[T1003](T1003.md)・[T1004](T1004.md)。
`map-runtime.spec.ts`が画素で見ている）は除いた。

### シナリオの選び方の基準

1. **類（K1〜K5）ごとに、その壊れ方が画面に出る最短の経路を選ぶ。** 各シナリオは、上の表の実例のうち少なくとも
   1件を「壊した入力」として再現し、落ちることを確かめてから入れる（CLAUDE.md「修正の原則」・パターン4の書き方6）。
2. **当てる相手（軸・レイヤー・気象の種類）は手で名指ししない。** 軸は`GET /api/axis-catalog`の公開軸すべて、
   レイヤーは`mapDisplay.layerIds`すべて（`map-runtime.spec.ts`の全レイヤーONと同じ出どころ）、気象はbackendが
   宣言する描き方（[T1017](T1017.md)）すべて。時刻を入力に取る軸は、カタログの`dedicated_way_value_layer`が真の軸。
   軸が1つ増えれば、何も書き足さずに検査の相手に入る。
3. **モックのe2eで見られるものは置かない。** 同じことを実データで遅く確かめるだけになる。
4. **離散の入力（時刻・ズーム）は、実例の欠陥の境界から代表点を導く。** 時刻は「日本時間とUTCで日付が
   異なる時間帯（日本時間0〜8時台）」と「同じ時間帯」の1点ずつ（T619）。ズームは偶数と奇数の1段ずつ（T637）。
   実行した時刻に任せない——任せると、壊れていても昼に回せば緑になる。
5. **幅はモバイルだけ。** 主用途はスマホで（[T1027](T1027.md)）、幅の分岐そのものはモックのe2eが両側で見ている。
   e2e-liveが幅で見るのは、実データの長さ（実際の軸名・道路名）で観点1が崩れないかだけ。

### シナリオの組み方（ユーザー提案「A→Bで止めてC、D、E→Fをまとめて見る。移動はショートカットする」）

**1シナリオ＝1枚のページ＝1本の幹。** 幹（A→B）で重い段取り——実backendでの起動・地点への移動・ルート生成——を
1回だけ払い、幹の終点で互いに独立な枝（C、D、E→F）をまとめて見る。枝は見終えたら幹の終点へ戻す。

- **戻す・戻せたかの確かめ**は、T1078で確定した仕組み（押したものをもう一度押す／Escape、タブは元のタブを押す、
  ARIAの開閉値・localStorage・`visualViewport.scale`の指紋が押す前と一致するか）をそのまま使う。
  戻らなければ違反として落とし、黙って別の状態で次の枝を見ない（T1078と同じ判断）。
- **枝の失敗で他の枝を止めない**（`expect.soft`）。実データの1回の段取りは重いので、1回で全部の結果を持ち帰る。
- **移動のショートカット**: 利用者の操作を真似ず、アプリが既に持っている入口で状態を与える——地点は
  ブラウザの現在地（Playwrightの`geolocation`。アプリは`navigator.geolocation`から起点を取る）、レイヤーの
  ON/OFFは保存状態（`seedStoredState`でlocalStorageへ。`map-runtime.spec.ts`と同じ）。**入口が無いもの**
  （地図の拡大率・出発時刻等）は段階Bで入口の有無を調べ、無ければ画面の操作で行う（テストのためにアプリへ
  入口を足すかは、そのとき別に諮る）。
- **T1078との関係**: T1078はモックの画面で状態の木を全部辿る。e2e-liveは木を辿らず、実例から導いた枝だけを
  見る。「押せるものだけ押す」「戻す＋指紋」「フレーム単位の待ち」はT1078の実装を共用する。共用の部品の置き場
  （`e2e/`から読むか、共通の置き場へ出すか）は、T1078がmasterへ入って形が決まってから段階Bで決める。

#### シナリオ案

| シナリオ | 幹（A→B） | 枝 | 捕まえる実例 |
|---|---|---|---|
| S1 地図の描画（生成前） | 起点を現在地として与えて開く → 全レイヤーONを保存状態で与える → 地図の描画が落ち着くまで待つ | C: 地図のエラー（`[map:error]`）が**取得の失敗も含めて**0件、ページの例外0件<br>D: 表示範囲の路面タイルに道が1件以上。公開軸がタイルから読む材料（カタログの`tile_inputs`）それぞれについて、それを持つ道が1件以上<br>E→F: 公開軸のレンズを1つずつ選ぶ → その軸の値を持つ道が表示範囲に1件以上描かれている<br>G: 道を1本押す → 区間の詳細が開き、例外・エラー表示が無い | T367・T886（D・G）、T672・T691（E→F）、T643（C・D） |
| S2 ルート生成（生成後） | S1と同じ地点で開く → 距離を指定して生成を1回 | C: 候補が1件以上、エラー表示が無い<br>D: ルート線の区間を押す → チャートのポップアップが開き例外が無い<br>E→F: 比較タブを開く → 実際の軸名の行見出しで横スクロールが出ない（観点1） | T790・T236（C）、T485（D） |
| S3 時刻で変わる入力 | 開く → 時刻を入力に取る軸のレンズを選ぶ | C: 出発時刻を日本時間0〜8時台にする → 値を持つ道が1件以上<br>D: 出発時刻を日中にする → 同上<br>E→F: 気象の各種類で、backendの時刻一覧が空でないものについて一覧の中の時刻を選ぶ → 偶数ズームで「データあり」の場所は、1段拡大（奇数ズーム）してもデータがある | T619（C・D）、T637（E→F） |

**全シナリオ共通の後始末の検査**（シナリオではなく、各シナリオの最後に1回）: backendの`/api/debug/stats`
（外部呼び出し・キャッシュのカテゴリ別の呼び出し数・エラー数・429拒否数。常時公開）をシナリオの前後で取り、
**自前の処理のエラー・429拒否が増えていない**ことを見る。外部のカテゴリ（気象庁・地理院・基礎地図の元等）の
エラーは合否に入れないが、報告に「外部要因」として必ず出す（黙って緑にしない）。カテゴリが自前か外部かの
分け方は段階Bで`log_external_call`の呼び出し元から確かめる。

### 期待値の書き方——値ではなく性質で書く

実データは取込・軸の調整・天候で変わるので、**特定の値・件数・色・画素の写しは書かない**（写しと比べるのは
CLAUDE.mdが検知器へ禁じている「保存した過去の値と比べる」と同じ形になる）。書いてよい性質は次の4種類:

| 性質 | 形 | 例 |
|---|---|---|
| 存在 | 1件以上（パターン6の「空でない」主張を**同じテストの中に**置く） | 路面タイルに道が1件以上・候補が1件以上 |
| 整合 | 独立な2つの出どころが食い違わない | カタログが読むと言う材料 ⇔ タイルに焼かれた材料、偶数ズーム ⇔ 奇数ズーム |
| 不在 | エラー・例外が0件 | `[map:error]`・ページの例外・backendの自前のエラー |
| 要求への応答 | 利用者が入れた条件と結果の関係 | 候補の距離と、指定した距離・許容幅の関係（backendが許容幅を保証しているかを段階Bで仕様から確かめてから入れる。保証していなければ書かない） |

**実世界で「無い」のが正当なもの**（警報が出ていない・危険度が無い・時刻一覧が空）は、別の出どころが「ある」と
言うときだけ判定し、判定しなかったときは「該当なし（理由）」をログへ出す。黙って空振りで緑にしない
（パターン6と同じ理由）。

**失敗を2種類に分ける**: 前提の不成立（backendが動いていない・地点が取込範囲外・基礎地図のURLのずれ・外部の停止）
は`globalSetup`で最初に確かめて「前提不成立: 何を直せばよいか」で止め、個々のテストの失敗として出さない。
テストの失敗は壊れ方の検知だけにする——そうしないと、回した人が環境の問題を欠陥として追うことになる。

### 前提

1. **backend**: 開発DBへ向いたbackendが手元で動いていること（既定`http://localhost:8000`。作業ツリーには`.venv`が
   無いので本体のチェックアウトの`backend/.venv`を使う——orchestration.md「重い処理は機械全体で1本ずつ」）。
   設定がbackendを起動することはしない（DBの向け先・`.env`は人ごとに違い、設定ファイルが向け先を決めることになる
   ため）。`globalSetup`が`/health`で確かめる。
2. **開発DBのデータ範囲**: **リポジトリに記録が無い**（`backend/app/batch/source_profile.yaml`の`bbox`はラスタ系の
   範囲で、道路の取込範囲ではない）。そこで起点を範囲から推測せず、環境変数（例: `E2E_LIVE_POINT`）で与え、
   既定はアプリの既定地点（`useLocation.ts`。`docs/architecture/setup.md`の確認用の地点と同じ）にする。
   `globalSetup`がその地点の路面タイルに道があることを確かめ、無ければ「前提不成立: 取込範囲外」で止める。
3. **基礎地図のURL**: backendは基礎地図のスタイル内のURLを`basemap_public_base_url`（既定
   `http://localhost:3000/api/basemap`）で組み立てる。E2Eは専用ポートで起動する（`frontend/e2e/`は3100）ので、
   合わせないと基礎地図のタイルが別のポートへ飛ぶ。backendを`BASEMAP_PUBLIC_BASE_URL`をE2Eのオリジンに合わせて
   起動すること。`globalSetup`がスタイルJSONのタイルURLのオリジンを確かめる。
4. **frontend**: `frontend/e2e/`と同じく本番ビルド（standalone）を起動する。`NEXT_PUBLIC_API_URL`はビルド時に
   埋め込まれるので、backendの向け先を変えたらビルドし直す。**開発機では`webServer.env`に`HOSTNAME: "localhost"`
   が要る**（[T1014](T1014.md)「開発機での実行」: Git Bashの`/etc/profile`が`HOSTNAME`へ機械名を入れてexportし、
   standaloneのサーバーがIPv6のグローバルアドレスでしか待ち受けず、`localhost`への接続が拒否される）。
   `playwright.live.config.ts`は新しいファイルなので最初から入れる（`playwright.config.ts`への同じ修正はT1078の側）。
5. **外部**: backendが気象庁・国土地理院・基礎地図の元へ取りに行くので、ネットワークが要る。予報（MSM）の
   鮮度は`/api/debug/stats`の`msm.healthy`で`globalSetup`が確かめ、古ければS3の該当の枝を「前提不成立」にする。
6. **直列**: workers=1。開発DBと手元のbackendは1つなので、ルート生成の同時実行・レート制限（429）を避ける。
   ルート生成の初回は、タイル材料のキャッシュが冷えていると数十秒以上かかる（`setup.md`の記述。実測ではない）。

### 走らせ方と所要

- 手順（開発機、並行実行中は段ごとに`heavy`の枠を通す）:
  `python scripts/lockrun.py heavy -- 'cd frontend && npm run build'` →
  `python scripts/lockrun.py heavy -- 'cd frontend && ./node_modules/.bin/playwright test -c playwright.live.config.ts <spec>'`
  をシナリオ（spec）ごとに1回。枠の上限（10分）に1シナリオが収まることを段階Bの完了条件にする。
- CIには載せない（D-017。CIのランナーには開発DBも手元のbackendも無い）。
- **所要は未計測。** 分かっている部品は、本番ビルド221秒（T1014「開発機での実行」の実測）と、冷えたキャッシュでの
  初回のルート生成が数十秒以上（`setup.md`、実測ではない）だけ。シナリオごとの所要は段階Bで測って書く。

### 誰がいつ回すか（案。決めるのはユーザー、下の保留）

- 案: **変更がe2e-liveの経路に届くとき**（地図の描き方`features/map/scene/`・タイルへ焼く値・軸カタログ・動的値の
  配信・気象の描き方・ルート生成の応答）、**その変更の担当が作業ブランチを監査へ出す前に手元で1回回し**、
  実行したコマンドと結果（通った枝・「該当なし」・「外部要因」）をコミットメッセージへ書く。門にはしない
  （CIでもpre-pushでもない。回したかは監査が見る）。

### 段階B以降（承認後）

- 段階B: `playwright.live.config.ts`・`globalSetup`（前提の確かめ）・S1。着手はT1078がmasterへ入ってから
  （ユーザーが`frontend/e2e/`と`playwright.config.ts`を作り直している最中で、共用の部品の置き場もそれで決まる）。
- 段階C: S2・S3。各シナリオは実例の壊した入力で落ちることを見てから入れる。
- 規約: `docs/conventions/testing.md`パターン4の「走らせ方」の`e2e-live`の段落を、決まった前提と回し方で書き足す。

所要（並行実行）: 段階A 依頼 2026-09-23 20:29 → 完了 2026-09-23 20:41（12分）、うち枠待ち0分・CI待ち0分（完了のコミット自身のCIは含まない）

## 保留（ユーザー判断）: 定義案を採るか（2026-09-23 依頼）

利用者への約束: 本番に近いデータで地図・ルート・気象が「出る」こと（何も描かれない・値が来ない・押すと例外で
開かない、を出さないこと）。いまのE2E（CIで毎回回るもの）は、道・軸・気象をすべて作り物の応答へ差し替えて
いるので、たとえば「軸スタジオで公開した軸が地図に一切出ない」「深夜に開くと風の地図が全部灰色」
「ルート線を押すとチャートが例外で開かない」は、過去に実際に起き、どれもCIを緑のまま通った。
起きない場面: 画面の部品が幅からはみ出す・ピンチで拡大する等の見た目の崩れは、作り物のデータでも出るので
今のE2Eが見ている（この案の対象外）。

案の中身は上の「段階A: 定義案」節——開発DBと手元のbackendへ向けて、(S1) 全レイヤーを出した地図・
(S2) 1回のルート生成の結果・(S3) 時刻で変わる風と気象、を1枚のページで幹を1回だけ辿り、その先でまとめて見る。
値そのものは見ず、「1件以上ある」「宣言と実物が食い違わない」「エラーが0件」で判定する。CIには載せない。

1. **案どおり採る（推奨）**。段階Bで設定・前提の確かめ・S1から作る（T1078がmasterへ入ってから着手）。
   過去の実例が一番多い地図の描画から守りが付く。
2. **S1だけ採る**。ルート生成（S2）と時刻（S3）は、S1を回して所要と手間を見てから改めて決める。
3. **採らない**。e2e-liveの置き場は空のまま残し、今の回し方（人が画面で確かめる）を続ける。

**ユーザー決定（2026-09-23）**: 定義案を案どおり採る（S1〜S3）。

## 保留（ユーザー判断）: e2e-liveを誰がいつ回すか（2026-09-23 依頼）

利用者への約束は上と同じ。CIに載せないので、**回す約束を決めないと誰も回さない**（置き場だけ作って中身が
使われない状態になる）。一方で、1回に本番ビルド（約4分）と実backendでのルート生成が要り、毎回の変更で
回すと作業が遅くなる。

1. **地図の描き方・タイルへ焼く値・軸カタログ・気象の描き方・ルート生成の応答に触る変更のとき、その担当が
   作業ブランチを出す前に1回（推奨）**。壊した変更がmasterへ入る前に捕まる。触らない変更では回さない。
2. **周期レビュー（2週間ごと等）のときにまとめて1回**。担当の手間は増えないが、壊れてから見つかるまで最大で
   1周期かかる（その間、本番の利用者に出うる）。
3. **ユーザーが必要と思ったときだけ**。仕組みとして回す約束はしない。

**ユーザー決定（2026-09-23）**: 定義案を案どおり採る（S1〜S3）。回す時機は、地図の描き方・タイル・カタログ・気象・ルートの応答に触る変更の担当が、作業ブランチを出す前に1回回す。段階Bの着手はT1078がmasterに入ってから。

## 段階B・C: 実装と実測（2026-09-23、担当V-live）

土台はorigin/master `bf06787b`（T1078はmasterに入っている）。

### 作ったもの

- `frontend/playwright.live.config.ts`: `testDir: ./e2e-live`・workers=1・ポート3200・`webServer.env`に
  `HOSTNAME: "localhost"`（T1014）・`actionTimeout` 15秒（見つからない部品を押そうとして幹の時間切れまで
  待ち続けないため。入れる前はS2の枝1本が8分の時間切れを払った）。backendは起動しない。
- `frontend/e2e-live/global-setup.ts`: 前提の確かめ（`/health`・カタログの路面タイルの世代・起点の路面タイルに
  道があるか・基礎地図のスタイルのURLがE2Eのオリジンを指すか・MSMの鮮度）。欠ければ「前提不成立: 直し方」で止める。
- `frontend/e2e-live/live.ts`: 共通の段取りと観測。開く（現在地・保存状態で与える）、応答の記録（路面タイルを
  `@mapbox/vector-tile`で復号・動的way値・気象庁のタイル・生成の結果・backendの失敗）、枝（`e2e/states.ts`の
  `installPageHelpers`の指紋と落ち着きの判定をそのまま使う）、外部要因の報告。
- `frontend/e2e-live/s1-map.spec.ts`・`s2-route.spec.ts`・`s3-time.spec.ts`: 定義案のS1〜S3。
- `frontend/vitest.config.mts`: `e2e-live/**`をvitestの探索から外す（`*.spec.ts`はvitestの既定の探索に
  当たり、外さないとCIのvitestがPlaywrightのspecを拾う）。
- 規約: `docs/conventions/testing.md`パターン4「走らせ方」のe2e-liveの段落（見るもの・前提・手順・誰がいつ回すか）、
  `docs/conventions/orchestration.md`「重い処理を減らす」に、e2e-liveを回す担当の条件を1行（正本はtesting.md）。

### 定義案から変えたこと・決めたこと

- **地図のインスタンスの取り方**: アプリは地図を外へ出していない。テストのためにアプリへ入口を足さず、Reactが
  要素へ付ける内部の印（`__reactFiber$`）から祖先の部品の`useRef`を辿って取る（`live.ts`の`installMapFinder`）。
  Reactの内部の形が変わると見つからず、例外で止まる（黙って空を返さない）。これで「描かれている道」
  （`queryRenderedFeatures`）・道やルート線を押す点（`project`）・ズームの切り替え（`jumpTo`）が取れる。
  入口を足すかは諮っていない（足していないので諮る対象が無い）。
- **落ち着きの判定**: MapLibreの`idle`は来なかった（全レイヤーONでは動き続けるレイヤーがあり、45秒待っても
  来ない）。全ソースの`isSourceLoaded`が連続3回（250ms間隔）真になるまで待ち、読み終わらないソースは名前を出して落とす。
- **S1のD（材料）**: 定義案の「材料それぞれについて持つ道が1件以上」は、範囲に実在しない材料（踏切
  `poi_level_crossing_per_km`）を欠陥に見せた。**軸ごとに、その軸の材料のどれかを持つ道が1件以上**へ変え、
  材料ごとの不在は「該当なし」でログへ出す。T367（軸の材料がタイルに1つも焼かれていない）はこれで捕まる。
- **自前と外部の分け方**（定義案「段階Bで確かめる」）: `log_external_call`のカテゴリは自前か外部かを宣言して
  いない（呼び出し元から機械で分けられない。名前で分けると許可リストになる）。そこで**アプリがbackendから受け取った
  HTTPの状態**で分けた: 429と、502・503・504以外の5xxは自前の失敗（合否に入れる）、502・503・504はHTTPの定義で
  上流の失敗を表すので外部要因（合否に入れずログへ）。`/api/debug/stats`の外部呼び出しのエラーの増分は、すべて
  外部要因としてログへ出す。
- **S2のD**: 区間を押したあとに出るもの（区間の詳細はボトムシート側で、地図にはマーカーだけ）の判定をやめ、
  「押す点にルート線が描かれている」＋「押したあとにページの例外が無い」にした（T485は例外で捕まる）。
  押す点は、候補1の区間の形の点のうち、地図そのものが押される（シート・部品に覆われていない）最初の点。
- **S2のE→F**: 比較タブは研究モードで生成を2回したときに出る（`e2e/comparison.spec.ts`と同じ条件）。
  幹で研究モードを保存状態で与え、生成を2回する。
- **S3のE→F**: 時刻一覧から時刻を選ぶ操作は入れていない（アプリの既定の時刻のまま）。ブラウザのキャッシュを
  切り（CDPの`Network.setCacheDisabled`）、偶数ズーム→奇数ズームで取り直した気象庁のタイルを配信系統ごとに
  まとめ、偶数でデータ（透明でない画素）のある系統が奇数でもデータを持つかを見る。奇数ズームで取り直さない
  系統は「該当なし（偶数の画像を拡大して使っている）」。

### 実測（開発機、起点 `E2E_LIVE_POINT=35.68,139.76`、2026-09-23 22:15〜23:00）

| シナリオ | 所要（テスト本体） | 結果 |
|---|---|---|
| S1 地図の描画 | 1.6分（`lockrun`の包み全体で1分50秒） | 落ちた: 「勾配」「風」の道ごとの値を1件も受け取っていない／ページの例外2件（下の「見つけたもの」）。他の軸のレンズ・C・D・Gは通った。枝の所要は5.8〜15.5秒 |
| S2 ルート生成 | 23.6秒（生成2.4秒・5.0秒。キャッシュが冷えた初回の生成は20.0秒） | 落ちた: ページの例外2件（S1と同じもの）。C・D・E→Fは通った |
| S3 時刻で変わる入力 | 1.3分 | 落ちた: 「風」のC（日本時間3時）・D（同12時）で道ごとの値を1件も受け取っていない／ページの例外2件。E→Fは「該当なし」（偶数ズームでデータのある気象庁の配信系統が無かった） |
| 前提不成立（起点を既定のまま） | 5.4秒 | `前提不成立: 起点（35.7597,139.7387）の路面タイル 15/29103/12894 に道が無い` で止まった（テストは走らない） |

**どのシナリオも`heavy`の枠の上限10分に収まる**（最長S1の1分50秒）。本番ビルドは別の段で1回（枠の中、約4分）。

### 見つけたもの（直していない。ここに記録する）

1. **勾配・風の地図の色分けが値を1件も返さない（backendの欠陥、masterで再現）**。`dynamic-way-values/{gradient,wind}`が
   200・空で返り、backendのログに `TypeError('RoadGraphRepository.get_feature_gradient_inputs_in_tile() takes 5 positional
   arguments but 6 were given')`（風は`get_feature_keys_in_tile`）。`app/services/gradient_way_service.py`・
   `wind_way_service.py`が`(ROAD_GRAPH_TILE_ZOOM, ancestor_x, ancestor_y)`を5つ目の引数として渡し続けているが、
   リポジトリ側の引数は`91bc3832`（T970段階C、2026-09-20）で4つになっている。例外は`except Exception`で空へ倒され、
   利用者には「データなし」に見える（T643と同じ形）。**これがS1のE→FとS3のC・Dの「壊した入力で落ちる」の実例**に
   なった（本物の欠陥で落ちた）。
2. **ページの例外2件（S1〜S3のすべてで、開いた直後に出る）**: `Minified React error #418`（ハイドレーションの
   テキストの不一致）と `Source "area-elevation" already exists.`。原因は調べていない。モックのe2eでは出ていない
   （`frontend/e2e/`はページの例外を集めていない）ので、実データ・実時刻でだけ出るのか、モックでも出ていて
   見ていないだけなのかは未確認。
3. **開発機のRedisが応答しない**（環境）: `cache:jma-tile-redis`・`cache:jma-amedas-redis`が`TimeoutError`、
   `/api/weather/amedas`が502。外部要因としてログに出た（合否には入れていない）。

### 検証

- `./node_modules/.bin/prettier --write`・`./node_modules/.bin/eslint e2e-live playwright.live.config.ts vitest.config.mts`（指摘0件）・
  `tsc --noEmit`（`lockrun heavy`、エラー0件）。
- 壊した入力: 上の「見つけたもの」1（本物の欠陥）でS1のE→F・S3のC・Dが落ちた。前提不成立は既定の起点で止まった。

### 実装の増減

- テスト（e2e-live）: +740行（`live.ts` 297・`s1-map.spec.ts` 124・`s2-route.spec.ts` 115・`s3-time.spec.ts` 152・`global-setup.ts` 52）
- 設定: +36行（`playwright.live.config.ts`）、`vitest.config.mts` 2行変更
- 文書: `testing.md` +25行、`orchestration.md` +2行、この記録
- 実装（`frontend/src`・`backend`）: 0行

所要（並行実行）: 段階B・C 依頼 2026-09-23 22:05 → 完了 23:05（60分）、うち枠待ち0分・CI待ち0分。完了のコミットのCI 2分（監査で追記: 作成 22:37 → 結論 22:39）

## 保留（ユーザー判断）: 画面を開くたびに出ている例外2件を、どこで調べるか（2026-09-23 依頼）

e2e-liveのS1〜S3すべてで、開発DB・実backendの画面を開くと例外が2件出る: Reactの#418（サーバーとブラウザの描画の食い違い、
ハイドレーションの不一致）と、地図の`Source "area-elevation" already exists.`（同じ名前の地図のソースを2回足そうとした）。
原因は調べていない。利用者の画面が止まるものではないが、e2e-liveはこの2件で毎回赤になる。

1. **（推奨）[T1001](T1001.md)（frontendの起こし直し）に寄せる。** どちらもfrontendの組み立ての話で、T1001の段階B・Dで
   起動の流れと地図の適用器を作り直すため、今直すと作り直しで二度手間になる。T1001の段階へ「この2件が出ないこと」を完了条件として足す。
2. **このタスク（T1077）の残りとして、先に調べて直す。** e2e-liveがすぐ緑になるが、T1001で同じ場所を触る。

**ユーザー決定（2026-09-23）**: 1（[T1001](T1001.md)に寄せる）。T1001.mdの完了条件へ「この2件が出ないこと」を足した。

## 保留（ユーザー判断）: e2e-liveが地図の中身をReactの内部から取ってよいか（2026-09-23 依頼）

e2e-liveは、地図に描かれている道の値を読むため、Reactが要素に付ける内部の印（`__reactFiber$`）から地図のインスタンスを取っている。
アプリには試験のための入口を足していない。Reactを上げたときに内部の形が変わると壊れうる（壊れたら空を返さず例外で止まる作り）。

1. **（推奨）このままにする。** アプリを試験のために変えない。壊れたら例外で気づける。
2. **アプリに試験用の入口を足す**（例: 開発時だけ`window`へ地図を出す）。Reactの内部に依らないが、試験のためにアプリを変える。

**ユーザー決定（2026-09-23）**: 1（このままにする。アプリに試験用の入口は足さない）。

## T1001からの返し（2026-09-25、T1001を閉じた）

T1001へ寄せた例外2件（hydrationの#418・`area-elevation`の二重登録）と、区間を押したときの`"[object Object]" is not valid JSON`は、T1001のe2e-live（開発DB、`E2E_LIVE_POINT=35.68,139.76`）でS1〜S3がページの例外0件で通った（`25ec868e`。ビルドと別の5分の枠でも通過）。この実行では勾配・風のレンズの枝も通った。記録は[T1001](T1001.md)「完了条件のe2e-live」。

## 残りの1・2: 壊した入力で落ちるかと、全シナリオの緑（2026-09-25、担当E-live2）

土台はorigin/master `12a2eeb2`。開発DBへ向けたbackend（作業ツリーのコード、`BASEMAP_PUBLIC_BASE_URL=http://localhost:3200/api/basemap`・
`CORS_ALLOWED_ORIGINS`に3200を足して起動）と本番ビルドで、`E2E_LIVE_POINT=35.68,139.76`で回した。

### 壊した入力（手元だけ。masterへは入れていない）

枝ごとに実例の壊れ方を1か所ずつ入れた。frontendの3か所は1回のビルドにまとめた（枝もシナリオも別で、落ちたときの
文言でどの壊し方かが分かるため）。

| 枝 | 入れた壊れ方（実例） | 結果 |
|---|---|---|
| S1 C | 停止要因POIタイルのエンドポイントが500を返す（T643: 取得の失敗） | 落ちた（`[map:error] AJAXError: Internal Server Error (500)` 8件） |
| S1 D | 路面タイルから「事故密度」の材料`accident_per_km`を抜く（T367: 軸の材料がタイルに無い） | 落ちた（D・E→Fの「事故密度」とも） |
| S1 G | 道の詳細の部品が描画中に`JSON.parse`で例外を出す（T485の形） | **初回は落ちなかった**→シナリオを直して落ちた（下） |
| S2 C | 周回生成が探索の準備の直後に例外で落ちる（T790: 生成が丸ごと落ちる） | 落ちた（生成の完了の応答を受け取っていない） |
| S2 D | ルート線の区間を押したとき、オブジェクトの値を`JSON.parse`する（T485・T1001で消した戻し方） | 落ちた（`SyntaxError: "[object Object]" is not valid JSON`） |
| S2 E→F | 比較表の`table-fixed`・折り返しを外す（観点1: 実際の軸名で表が器より広がる） | **直す前は捕まえられない形**だった→シナリオを直して落ちた（下） |
| S3 E→F | 奇数ズームの補間をやめる（`source_zoom_for_interpolation`が常にNone。T637: 奇数ズームで気象庁へ取りに行き空が返る） | **初回は判定まで届かなかった**→シナリオを直して落ちた（下） |

### シナリオの欠陥として直したもの

1. **S1 G**: 全レイヤーONでは道の上に事故・POIの点が重なり、押した点で一番上に来た点の詳細（MapLibreのPopupへ直接載せる
   別の中身）が開いていた。`.maplibregl-popup`が見えるだけで通るので、道の詳細を壊しても緑だった。押す点を「一番上の地物が
   道である点」に絞り、開いた中身に路面の行があることを見る。描画の例外は`app/error.tsx`のエラー境界が受け、ページの例外
   （`pageerror`）にならない（Reactが`console.error`へ出すだけ）ので、中身が出たかで見る。直したあと、壊した入力ではポップアップが
   出ずエラー画面になって落ちた。
2. **S2 E→F**: 表の部品（`components/ui/Table`）は外枠に`overflow-x-auto`を持つので、表が広がっても外枠の中で横に動くだけで、
   ページにもシートにも横スクロールが出ない。直す前の判定（ページとシートの`scrollWidth`）はこれを見ていなかった。シートの中の
   横にスクロールする容器（`overflow-x`がauto・scroll）も数える。
3. **S3 E→F**: 2つ重なっていた。①地図の既定のズーム（14前後）は気象庁のタイルの上限（宣言の`maxZoom`、8〜10）より細かく、
   偶数・奇数どちらの地図のズームでも上限のタイルを使い回すので、奇数のタイルを取り直すことが無かった（どの天気でも判定まで
   届かない）。②地図のズームとタイルのズームはタイルの大きさで1段ずれる（地図6→タイル7を取った。backendのログで確認）。
   地図のズームを宣言（`mapDisplay.weatherElements`のタイルのズーム範囲）から2段続きで選び、取れたタイルはURLのズームの偶奇で
   分ける。直したあと、壊した入力で落ちた——偶数ズームにだけデータを返す模擬（土砂キキクル`land`）に加え、**実データの
   降水（`hrpns`）と雷（`thns`）でも**「奇数ズームでデータが消える」で落ちた（この日は偶数ズームに実データがあった）。

### 全シナリオの緑（壊した入力を外したクリーンなビルド）

| シナリオ | 所要（テスト本体） | 結果 |
|---|---|---|
| S1 | 2.6分 | 通過。「該当なし」は踏切の材料だけ |
| S2 | 42.1秒 | 通過 |
| S3 | 3.1分（うちE→F 119秒） | 通過。E→Fは判定した（「該当なし」が出ていない） |

どのシナリオもページの例外0件。外部要因は開発機のRedisの応答なし（`cache:jma-tile-redis`・`cache:jma-tile-index`）と
`/api/weather/amedas`の502だけ（合否に入れない）。記録の「見つけたもの」1（勾配・風の引数の食い違い）は、勾配・風のレンズの枝と
S3のC・Dが通ったので直っている。2（#418・`area-elevation`）は出なかった。

### 増減

- テスト（e2e-live）: `s1-map.spec.ts`・`s2-route.spec.ts`・`s3-time.spec.ts`（行数はコミットメッセージ）
- 実装（`frontend/src`・`backend`）: 0行

## 保留（ユーザー判断）: 残りの3を雨の日を待つトリガー付きで先送りするか（2026-09-25 依頼、司令塔）

問い: T1077の残り（雨・警報のある日にS3の判定を1回見る）を、雨の日を待つトリガー付きで先送りし、並行実行第2回からは外してよいか。
残りの1・2（壊した入力で各枝が落ちること、S1〜S3が緑になること）は2026-09-25に済んでmasterに入っている。残る3は、気象庁の
配信に偶数ズームのデータがある日（雨・警報のある日）でないと判定まで届かず、いつ来るかはこちらで決められない。

1. **（推奨）トリガー「雨か警報のある日」を台帳に付けて先送りし、この回の母集団から外す。** 回を締められる。雨の日に司令塔か
   担当が1回回して記録し、T1077を閉じる。
2. **この回の中で雨の日を待つ。** 雨の日まで第2回を締められない。

**ユーザー決定（2026-09-26）**: 1（トリガー「雨か警報のある日」を台帳に付けて先送りし、この回の母集団から外す）。
台帳のT1077の行へ`— トリガー: 雨か警報のある日（気象庁の配信に偶数ズームのデータがある日）`を付けた。

## 残り（未実施。このタスクを閉じない理由）

3. **雨・警報のある日（気象庁の配信に偶数ズームのデータがある日）にS3を1回回し、E→Fで判定した系統の数を記録したら閉じる**。
   2026-09-25のクリーンな実行ではE→Fが判定まで届いた（「該当なし」が出ていない。同じ日の壊した入力の実行で`hrpns`・`thns`が
   実データで判定された）が、判定した系統の数をログへ出していないので数は記録していない。回すのは、台帳のトリガー（雨か警報の
   ある日）が来たときの司令塔か担当。判定した系統をログへ出す1行を足すかも、そのとき決める。記録したらこのタスクを
   `状態: 完了`にし、台帳の行を消す。

所要（並行実行）: 残りの1・2 依頼 2026-09-25 22:42 → 完了 23:36（54分、司令塔の中断を含む）、うち枠待ち不明（lockrun --reportが機械全体の集計しか出さず作業ツリーの行を分けられなかった）・CI待ちは完了のコミットの後、規模札M、実績 +49/−21 → S。超過: 見立てより中身が大きい（シナリオの欠陥3件の修正と、ビルド2回・シナリオ実行8回）
