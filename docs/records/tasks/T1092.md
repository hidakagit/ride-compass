# T1092. ルートの割増の強さ（P）の既定と、軸ごとの寄与度の計算を、それぞれ1つにする（地点確認のAPIもルート生成と同じPで探す）

状態: 完了（2026-09-24）

## 背景

背景（T965 段階Bの点検で見つけた崩れ2件）:
1. T805・T817は「好みの割増を時間へ換算する強さ（P）の既定は較正値1つ（今0.7）」とした。段階Aで計算関数の引数の既定1.0は外したが、エンジン（RoadGraphEngine.__init__）にも既定1.0が残っている。ルート生成は必ず較正値を渡すので影響しないが、地点確認のAPI（POST /api/routes/preview、get_preview_builder）はPを渡さないため、P=1.0で探している。docstringは「生成と同じ評価を使う」と書いているが食い違う。ベンチも1.0で測っている。frontendからこのAPIを呼ぶ箇所は0件。
2. T550は「軸ごとの寄与度はbackendで1回だけ計算する」とした。今は、全区間ぶんを作る配列版（compose_costs_from_axis_matrix の with_contributions の枝と AxisComposition.contributions）と、1区間ぶんを作る版（axis_contributions_at_row）の2つがある。本番で使うのは後者だけで、配列版を読む箇所は0件（唯一の本番の呼び出しは with_contributions=False を渡す）。

直し方: 1はエンジンの既定を外し、previewとベンチで resolve_penalty_strength(None) を渡す。2は with_contributions 引数・配列版の枝・contributions フィールドを消す。どちらもテストの偽物・組み立て（test_road_graph_engine.py の fake_compose_costs と make_engine）の引数を直す必要がある。

振る舞いの差: 1は /api/routes/preview の探索がP=1.0から較正値（今0.7）へ変わる（利用者の画面からは呼ばれていない）。2は変わらない。

なぜ既存のタスクに入らないか: T805・T817・T550は完了済み。T965では backend/tests を触らない決まり（T1021が作り直し中）のため、テストの直しを伴うこの2件は切り出した。test_road_graph_engine.py はT1021の対象24ファイルの外。

（ダッシュボードの起票案 kihyoan-penalty-contrib-single を承認（2026-09-24）。起票案の全文は次のとおり）

規模S（変わると見込むファイル5本: services/road_graph_engine.py・api/dependencies.py・benchmarks/_route_generation_service.py・domain/evaluation.py・tests/test_road_graph_engine.py）。

背景（T965 段階Bの点検で見つけた崩れ2件）:
1. T805・T817は「好みの割増を時間へ換算する強さ（P）の既定は較正値1つ（今0.7）」とした。段階Aで計算関数の引数の既定1.0は外したが、エンジン（RoadGraphEngine.__init__）にも既定1.0が残っている。ルート生成は必ず較正値を渡すので影響しないが、地点確認のAPI（POST /api/routes/preview、get_preview_builder）はPを渡さないため、P=1.0で探している。docstringは「生成と同じ評価を使う」と書いているが食い違う。ベンチも1.0で測っている。frontendからこのAPIを呼ぶ箇所は0件。
2. T550は「軸ごとの寄与度はbackendで1回だけ計算する」とした。今は、全区間ぶんを作る配列版（compose_costs_from_axis_matrix の with_contributions の枝と AxisComposition.contributions）と、1区間ぶんを作る版（axis_contributions_at_row）の2つがある。本番で使うのは後者だけで、配列版を読む箇所は0件（唯一の本番の呼び出しは with_contributions=False を渡す）。

直し方: 1はエンジンの既定を外し、previewとベンチで resolve_penalty_strength(None) を渡す。2は with_contributions 引数・配列版の枝・contributions フィールドを消す。どちらもテストの偽物・組み立て（test_road_graph_engine.py の fake_compose_costs と make_engine）の引数を直す必要がある。

振る舞いの差: 1は /api/routes/preview の探索がP=1.0から較正値（今0.7）へ変わる（利用者の画面からは呼ばれていない）。2は変わらない。

なぜ既存のタスクに入らないか: T805・T817・T550は完了済み。T965では backend/tests を触らない決まり（T1021が作り直し中）のため、テストの直しを伴うこの2件は切り出した。test_road_graph_engine.py はT1021の対象24ファイルの外。

## 実施（2026-09-24、担当 W-pen）

- P: `RoadGraphEngine.__init__`の`penalty_strength`の既定1.0を外して必須にした。地点確認の`get_preview_builder`と
  ベンチの`route_generator_session`は`resolve_penalty_strength(None)`を渡す（ベンチの`penalty_strength`引数は
  呼ぶ側が1つも渡していなかったため消した）。Pを渡し忘れた組み立ては型検査と実行時のTypeErrorで止まる。
- 寄与度: `compose_costs_from_axis_matrix`の`with_contributions`引数・配列版の枝・`static_sums`との併用を断る
  ValueError・`AxisComposition.contributions`を消した。配列版の寄与度だけが使っていた`_axis_terms`の3つ目の
  戻り値も同時に消えた。寄与度は`axis_contributions_at_row`（1区間ぶん）だけになった。
- 消した文の判定（comments.mdの判定木）: 配列版の名指し・`with_contributions`の説明・寄与度配列の戻り値の説明は
  対象が消えたため削除。「全軸を足すと丸め前の合成difficultyに一致」「区間の値は丸めない」「全区間ぶんは作らない
  理由」は1区間版にも成り立つ局所の制約として`axis_contributions_at_row`のdocstringへ移した。エンジンの
  「既定1.0は難易度100で体感2倍」はP=1の意味がevaluation-scoring.mdにあり、既定も消えたため削除。
- 文書: 寄与度の算出元を`compose_costs_from_axis_matrix`と名指ししていたfrontendのコメント2か所と
  route-settings-and-results.mdを`axis_contributions_at_row`へ直した。evaluation-scoring.mdへ区間の寄与度の
  求め方を、routing-engine.mdのAPI表へpreviewの重み・Pが生成の既定と同じことを足した。
- 振る舞いの差: `POST /api/routes/preview`の探索がP=1.0から較正値（`evaluation.penalty_strength`、宣言の既定0.7。
  DBの上書きがあればそれ）へ変わる。ベンチもP=1.0から`resolve_penalty_strength(None)`になる（ベンチは
  `tuning_overrides`を読み込まないため宣言の既定）。寄与度の撤去は振る舞いを変えない（配列版を読む箇所が0件）。

検証:
- 参照の列挙: `grep -rn "with_contributions|\.contributions\b|contributions="`（docs/records除く）→ 本番の呼び出し
  1件（`with_contributions=False`）とテストの代役1件だけ。`RoadGraphEngine(`の呼び出し → dependencies.py 2件・
  ベンチ1件・テストの`make_engine`1件で、Pを渡していなかったのはpreviewとベンチ。
- `ruff check`（変更5ファイル）→ All checks passed。`mypy`（mypy.iniの対象全体）→ no issues found in 195 source files。
  `review_checks.py docs` → 違反なし。prettier（AxisContributionBar.tsx）→ 通過。
- `pytest tests/test_road_graph_engine.py` → 173 passed。`pytest tests/test_routes_generate.py tests/test_tile_score_matrix_cache.py`
  → 59 passed。

範囲外で見つけたもの（直していない）: ベンチ（`benchmarks/`）は`tuning_overrides`を読み込まないため、Pに限らず
較正値はすべて宣言の既定で測っている。区間インスペクタの寄与度は`domain/difficulty.py: composite_contributions`
（スカラー版）が別に求めている（way1本の入力で、ルートの配列とは入力が違う）。

所要（並行実行）: 依頼 2026-09-24 10:17 → 完了 10:26（9分）、うち枠待ち0分・CI待ち0分、規模札S、実績 +31/−53 → S
