# T1140. 画面が、backendの宣言にあるべき値・対応表・名前を自前で持ち、一部はbackendと値が食い違っている

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: backendが決めるべき値をfrontendが計算し直す（再発。T1001で多くを移した後の残りと新規）。値が食い違うもの: 雨と雪の境の気温がfrontend conditions/WeatherPanel/amedasWeatherIcon.ts:15で2℃、backend domain/weather.py:8で0℃（常設ヘッダーと「今日の見通し」で判定が割れる）。直書き: 配信の更新間隔3件（useWeatherConditions.ts:37、useJmaTileIndex.ts:12、useWeatherGrid.ts:19。JMAの要素は生成物のrefreshIntervalMsから読む形と混在）、段の境界（windLayer.ts:52の0.3、precipitationNowcast.ts:27の0.1、TodayOutlook.tsx:43の0.1）、重みの上限MAX_AXIS_WEIGHT=0.6（routeWeightShare.ts:8、backendに無い）、applied_max_routesの条件の写し（useRouteFormSubmit.ts:20）、MAX_MERCATOR_LATITUDE（dynamicWayValues.ts:10）、経由地ルートのid（page.tsx:99 NON_DIRECTIONAL_ROUTE_IDS。route-splicedは生成物から引く）、目的地・経由地モードの探索半径distance_kmを画面が計算して送る（page.tsx:574-580。backendの内部パラメータ）、塗る材料をshape.terms[0].materialから読み直す（routeStyleModes.ts:88-95）、道の詳細のタイルプロパティ↔材料の対応（roadFacts.ts:36-40。backendのtile_propertyは生成物に出ていない）、鮮度パネルの「作り直しが要るか」（DerivedDataFreshnessPanel.tsx:25。backendのhas_missing_rowsはテストだけが呼ぶ）。名前: 地図レイヤーの説明文が軸名・内部名を直書き（mapLayers.ts:258-259の「night軸」、277、377、418）、レイヤー名5件（起伏・降水ナウキャスト・風（矢印）・災害・ルート）がfrontend持ち。母集団の導き方: features・lib・appの非テストで、モジュール直下の大文字リテラル定数（9a234615時点116件）とインラインの比較・条件を1件ずつ「描画の都合か／源泉の性質・閾値・分類か」で判定する。後者をbackendの宣言（生成物またはAPI）へ移し、描画の都合として残すものは理由を1行書く。検知器は足さない。

## 方針（2026-09-26、担当 B-decl）

母集団は2つの機械の出力の和を候補にし、1件ずつ判定する。

- **モジュール直下の大文字定数**: `frontend/src/{features,lib,app}`の`.ts`/`.tsx`（`.test.`・`.spec.`・`__tests__`を除く）で、
  行頭（インデント0）の`const NAME =`／`export const NAME =`、NAMEが大文字・数字・`_`だけのもの。origin/master（5b796bb1）で
  **259件**。記録の116件はリテラルの値に絞った数え方で、ここでは右辺が生成物・`mapDisplay`等の参照である行も含めて数え、
  参照である行は「既にbackendの宣言から引いている」として判定から外す。
- **インラインの比較**: 同じ範囲で`<`・`>`・`<=`・`>=`・`===`・`!==`の右に数値リテラルを置く行（0・1と`length`との比較を除く）。
  **26件**。

判定は「描画の都合か／源泉の性質・閾値・分類か」。前者（色・太さ・px・拡大率の段・画面の文言・操作の刻み・保存キー・
クライアントの待ち時間）は残し、後者をbackendの宣言（生成物または応答）へ移す。検知器は足さない。

進め方: 利用者に届いている食い違い（雨と雪の境）を段階1として先にコミットし、残りを段階に分ける。

## 段階1: 雨と雪の境を観測と予報で1つにする

- 常設ヘッダー（アメダス実測）の天気の分類を画面からbackendへ移した。`domain/weather.py: derive_observed_weather_code`が
  10分間の降水量を1時間あたりへ直して予報の`derive_weather_code`と同じ段で雨/雪を決め、`AmedasObservation.weather_code`
  （保存した実測から応答のたびに導く`computed_field`。Redisの形は変えない）で届く。画面は予報と同じ`weatherCategoryOf`で
  分類を引き、「晴れ」を昼夜で描き分けるだけになった。
- 境の気温は**backendの0℃へ揃えた**（画面の2℃を捨てた）。気象庁の公式ページは気温だけの境を公表していない（アメダスは
  気温と湿度で判別する。東京の平地の**積雪**の目安として「地上で1.5℃以下」はあり、湿度が低いと気温が高くても雪になるとも書く）ため、どちらの値にも公式の根拠が無い。
  値そのものの判断はダッシュボードに問いとして置いた（`T1140-snow-threshold`）。
  出典: 気象庁「南岸低気圧による東京都での課題と、大雪発生の仕組み」
  https://www.data.jma.go.jp/tokyo/shosai/chiiki/tokyo-kishou/ooyuki-nangan/nangan-kadai-shikumi.html ・
  同「推計気象分布」 https://www.jma.go.jp/jma/kishou/know/suikei_kishou/kaisetsu.html
- 利用者から見た差: 気温0℃を超え2℃以下で降水があるとき、ヘッダーが「雪」から「雨」になり、「今日の見通し」と揃う。

## 段階2: 源泉の値（取り直す間隔・段の境・重みの上限・メルカトルの限界）

画面が直書きしていた源泉の値を、backendの宣言から生成物で引く形にした。値はどれも変えていない
（在否インデックスの取り直しだけ5分→10分。backendが作り直す間隔そのものにした。遅れても間引きが効かないだけ）。

| 画面の値 | 正本（backend） | 生成物 |
|---|---|---|
| アメダス・警報等の取り直し10分 | `services/jma_amedas_service.py: AMEDAS_REFRESH_INTERVAL_MINUTES` | 新設`refresh-intervals.json` |
| 在否インデックスの取り直し5分 | `config.py: jma_tile_prewarm_interval_minutes`の既定値 | 同上 |
| 風格子の取り直し3時間 | `infrastructure/msm_client.py: DEFAULT_UPDATE_INTERVAL_SECONDS`（未同期時の既定値。実行時は配信元のメタが持つ） | 同上 |
| 無風の境0.3m/s | `domain/weather_display.py: WIND_CALM_BELOW_MS`（気象庁風力階級の風力0は0.0〜0.2m/s） | `weather-scales.json` |
| 降水の塗り・予想降水量の「-」の境0.1mm | `domain/weather.py: PRECIPITATION_MIN_MM`（天気コードの雨の判定と同じ値） | `weather-scales.json` |
| 重みの配分の1軸の上限0.6 | `domain/route_preference.py: MAX_AXIS_WEIGHT`（要求の検証には使わない） | `route-generate-config.json` |
| Web Mercatorの緯度の限界 | `domain/region.py: MAX_MERCATOR_LATITUDE` | `region-tile-config.json` |

MSMの更新間隔と在否インデックスの間隔は実行時に変わりうる（配信元のメタ・環境変数）。生成物はビルド時に決まるため
宣言の既定値を運ぶ。ずれても画面の鮮度が遅れるか取り直しが増えるだけで、表示は壊れない。

## 段階3: 対応表・条件の写し

| 画面が持っていたもの | 移した先 |
|---|---|
| 経由地ルートのid（`page.tsx: NON_DIRECTIONAL_ROUTE_IDS`） | `domain/loop_routing.py: WAYPOINTS_ROUTE_ID`→`route-generate-config.json`の`waypoints_route_id` |
| 目的地・経由地モードの距離の計算（最遠の点を切り上げて+1km、上限で頭打ち） | `api/routers/routes.py: RouteGenerateRequest._resolve_distance`。`distance_km`を省略可能にし、点があるときはbackendが決めて送られた値を使わない。画面は送らない。上限より遠い点は422（従来の画面の頭打ち＋backendの検査と同じ結果） |
| 生値を塗る材料を`shape.terms[0].material`から読み直す | 軸カタログの`map_value_material`（`domain/dynamic_way_values.py: map_value_material`。単位の導出も同じ関数を通す） |
| 道の詳細のタイル属性↔材料の対応（`roadFacts.ts: PRESENT_FACTS`） | 材料カタログの生成物に`tile_property`を載せ、画面は材料idから属性を引く。どの事実を並べるか（材料の選び）は表示の都合として残した |
| 鮮度パネルの「作り直しが要るか」 | `services/derived_data_freshness_service.py`のレポートの`needs_rebuild`（テストだけが呼んでいた`has_missing_rows`はここで使う） |

判定して残したもの: 候補数を決まった数にする条件（`useRouteFormSubmit.ts: fixedRouteCount`）。条件は「送る要求に経由地が
入るか」で、要求を組み立てる画面自身の事実。backendの規則（経由地があれば`routes_with_waypoints`件）は生成物の値が運ぶ。

死んだもの: `lib/geoDistance.ts: haversineKm`の外からの呼び手（画面の距離の計算）。exportを外し、単体のテストは
`cumulativeDistancesKm`のテストが同じ桁と単位を押さえるため消した。

## 段階4: 地図レイヤーの名前

- 一次属性を持たない5枚（起伏・降水ナウキャスト・風（矢印）・災害・ルート）の名前を`domain/map_display.py: MapLayerSpec.label`へ
  移し、一次属性を描くレイヤーは属性の名前（`map_layer_label`）で、どれも生成物`mapDisplay.layers[].label`で届く。画面の
  `attributeLabel`と名前の直書きを消し、`staticLayer`が名前ごと返す1本の経路にした。名前を持たないレイヤーは生成の時点で落ちる。
- 説明文（ⓘ）が評価の名前・内部の呼び名を書き込んでいる箇所（トンネルの「night軸」、停止要因の「停止密度」軸、風（矢印）の
  「風の評価軸」、ルートの「[風・勾配・路面・総合難易度]」）は、直すと利用者に見える文言が変わるため、ダッシュボードへ問いとして
  置いた（`T1140-layer-hint-axis-names`）。答えが出るまで着手しない。
- 判定して残したもの: チップの短い名前（`chipLabel`。チップの幅から縮めた描画の都合）と、読み取り専用の凡例の見出し（画面の文言）。

## 母集団の判定（段階1〜4の時点）

大文字定数259件のうち、右辺が生成物・宣言・他の定数の参照である90件は判定外。残り169件と、インラインの比較26件を1件ずつ
判定した。移したものは段階1〜4の表のとおり。残した理由は次の分類に尽きる（定数名は代表例）:

| 残した理由 | 例 |
|---|---|
| 描画の寸法・拡大率の段（px・ズーム・線の端・アイコンの大きさ） | `TICK_SPACING_PX`・`DIAL_SIZE_PX`・`ROUTE_ARROW_SIZE_PX`・`HIT_WIDTH_PX`・`WIND_DETAIL_MIN_ZOOM`・`WIND_GRID_DETAIL_SPACING_STOPS`（ズームごとの詳細格子の細かさ。backendは下限`detail_min_spacing_deg`を配る）・`LOAD_BAR_MAX_HEIGHT_RATIO` |
| 画面の文言・保存キー・DOMのid・凡例の鍵 | `TILE_ZOOM_TOO_WIDE_NOTICE`・`FILTERED_LABEL`・`*_STORAGE_KEY`・`*_TITLE_ID`・`LEGEND_NO_DATA_KEY`・`ROAD_OTHER_KEY` |
| 操作の刻み・画面の待ち時間・クライアントの取り直しの上限 | `WEIGHT_STEP`・`KEY_STEP_DEG`・`NOW_STEP_MS`・`POLL_INTERVAL_MS`・`MAX_CONSECUTIVE_POLL_FAILURES`・`lib/apiTimeouts.ts`・`LEGEND_FILTER_DEBOUNCE_MS`・`MAX_TILES_PER_FETCH` |
| 画面の中だけで意味を持つ識別子・空の既定値・実装の都合 | `ROUTE_HIT_TARGET*`・`JMA_TILE_PROTOCOL`・`PLACEHOLDER_TIME`・`JMA_POINT_VALUE_PROPERTY`（画面が地点へ入れる固定値の名前）・`EMPTY_*`・`NO_*`・`SPLICE_OPTIONS_PER_GROUP` |
| 画面が持つ機能の設定（backendに対応が無い） | `gpxExport.ts`（書き出しの点数・間引き）・`departureTimeline.ts`（出発時刻の目盛り。理由は`dynamic-weather-layers.md`）・`scoreDistribution.ts`（管理画面の分布の帯）・`RouteSplicePanel.tsx`の寄与の表示の閾値 |
| 外部の仕様・数学の定数 | `AREA_LAYER_TYPES`（MapLibreの型名）・`ROAD_NETWORK_SOURCE_LAYER`（下地の地図の配信元のスキーマ名。backendも中継するだけで持たない）・`EARTH_RADIUS_KM`・`METERS_PER_DEGREE_LATITUDE`・`JST_OFFSET_MS` |

インラインの比較26件は、HTTPの状態（204・404）・色の計算（HSL）・書式（桁・単位の切り替え）・表示の閾値（被覆の割合の注記、
内訳を並べる下限）で、源泉の閾値は`TodayOutlook.tsx`の0.1（段階2で移した）と鮮度パネルの判定（段階3）だけだった。

## 残り（このタスクの続き）

- 説明文が評価の名前・内部の呼び名を書き込んでいる箇所（トンネルの「night軸」、停止要因の「停止密度」軸、風（矢印）の
  「風の評価軸」、ルートの「[風・勾配・路面・総合難易度]」）: 下の「問いと答え: 地図レイヤーの説明文」の答え(1)どおり、
  その材料を使う公開中の評価の名前を軸カタログから差し込み（評価が無ければその一文を出さない）、「night軸」の一文を消す。
  説明文そのものを材料の源泉から引く案は[T1170](T1170.md)へ。
- 配信元の気象タイルのURLの形: 画面の`jmaDelivery.ts: jmaElementUrl`（組み立て）と`jmaTileIndex.ts: TILE_URL_PATTERN`・
  `ELEMENT_PATH_SEGMENT`（読み戻し）が`data/{系統}/{basetime}/{member}/{validtime}/surf/{要素}/`の形を持つ。backendも
  `services/jma_tile_prewarm_service.py`（組み立て）と`infrastructure/jma_tile_interpolation.py`（読み戻し）で同じ形を持つ。
  方針: `domain/jma_tile_specs.py`へパスの形を1つ宣言し、backendの2か所と、生成物`mapDisplay.weatherElements`経由で画面の
  2か所がそこから組み立てる。本番のプリウォームの経路に触るため、段階を分けて行う。
- 道路タイルの列名（`scene/groups/roadLines.ts: FEATURE_KEY_PROPERTY`・`WAY_ID_PROPERTY`）: backendの正本は焼き込みSQL
  （`infrastructure/road_graph_repository.py`）の出力の別名。方針: `infrastructure/vector_tile.py`（レイヤー名の正本）へ
  列名を置き、焼き込みSQLはf文字列でそれを差し込み、生成物`region-tile-config.json`の`road_surface`で配る。組み立てた
  SQLの文字列が変わらなければタイルの形の署名も変わらず作り直しは起きない——変更前後で焼き込みSQLのsha1と
  `TILE_SHAPES`が同じことを確かめる（変更前: SQL `a2bea26b…`、`road_surface`の形 `11869beea9d5`）。

## 後始末: e2e-liveの「道を押す」だけが落ちる（2026-09-26、担当 R-inspect）

- 起きていたこと: 開発DBでS1を回すと、枝「道を押す」で道の詳細は開くが「路面」の行がhiddenのまま落ち、
  `/api/region/axis-inspector`はbackendへ届いていなかった（T1137・T1138の担当の報告）。
- 原因は段階3・4ではなく[T1125](T1125.md)（1088db19）。道の詳細の属性（「路面」の行を含む）を、閉じた「この道の属性」の
  折りたたみへ移した——設計書（`static-map-layers.md`の`RoadInspectorPopup.tsx`の行）と単体テスト（「属性は畳んでおき…」）が
  主張する意図どおりの見た目で、利用者から見た退行ではない。T1125はe2e-liveを回しておらず（コミットに「e2eの地図は空で道を
  押せない」）、S1の期待だけが古いまま残った。評価の問い合わせは「この道の評価を見る」を押したときだけ送る設計で、
  枝はそれを押さないため届かないのが正しい。段階3の`tile_property`（`surface_good`・`tunnel`・`bridge`・`oneway`・
  `smoothness`）は置き換え前の属性名と一致し、「路面」の行は`tile_property`を通らない。
- 本番: frontend（Render）はmasterから出ており、T1125を含むmasterのCIは緑（例: 404ba4cf）なので本番にも同じ畳み方が出ている。
  意図した見た目のため急ぎの取り込みは不要。
- 直したもの: S1の枝が「この道の属性」を開いてから「路面」の行が見えることを見る。単体テストに「閉じている間は見えず、
  開くと見える」を足した（DOMの主張はvitestの持ち物。testing.mdパターン4。モックのe2eは道路タイルを204で返し道を押せない）。

## 問いと答え: 地図レイヤーの説明文

- 問い（2026-09-26、`T1140-layer-hint-axis-names`）: 地図のレイヤーの説明（ⓘ）が評価の名前を文に書き込んでいる箇所を
  どう直すか（評価の一覧から差し込む／評価に触れる部分を消す／今のまま）。
- 答え（2026-09-26 19:55）: (1) その材料を使っている公開中の評価の名前を、表示のたびに評価の一覧から差し込む（評価が
  無くなればその一文を出さない）。内部の呼び名「night軸」の一文は消す。

## 問いと答え: 雨と雪の境の値

- 問い（2026-09-26、`T1140-snow-threshold`）: 降っているとき「雨」と「雪」を分ける気温の境を何度にするか（0℃のまま／1.5℃／2℃）。
- 答え（2026-09-26 19:49）: (1) 0℃のまま（段階1の実装のまま）。

所要（並行実行）: 依頼 2026-09-26 19:04 → 段階4 20:10（66分）、うち枠待ち6分・CI待ち約5分、規模札M、実績（段階1〜4）実装 +222/−161・テスト +102/−69 → M（未完了。残りは「残り」節） 超過: 見立てより中身が大きい
所要（並行実行）: 依頼 2026-09-26 21:00 → 後始末 21:25（25分）、うち枠待ち約2分・CI待ち0分、実績 実装 +0/−0・テスト +9/−3
