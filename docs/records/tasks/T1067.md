# T1067. 並行実行の見込み超過の予算を、記録に残した所要の実績から計算する

状態: 完了（2026-09-23）

## 背景

並行実行の見込み超過の予算を、規模札の定義（CLAUDE.md: S=1時間・M=半日・L=1日以上。人が作業する前提の見立て）から流用していた（M=240分）。ユーザー指摘「Mが4時間は明らかに長すぎる。妥当な範囲に収束させていくプロセスは無い？」。2026-09-23の担当の報告の時刻から見た実時間（振り出し〜報告）: S は T1029 約10分・T1024 約25分・T999 約20分、S〜M は T1004 約45分・T993 約75分、M は T1061 約105分・T1054 約35分・T1056 約25分・T1058 約30分＋CI待ち、段階 T1014 A 約60分・B 約110分。機械の飽和中の参考値: T1023（S）約160分・T1027（M）約260分。飽和を除くと S 10〜45分・M 30〜105分で、M=240分では止まったタスクの検知に4時間かかる。

合意した置き場（2026-09-23）: 所要の実績は、担当が完了のコミットで Txxx.md の末尾へ1行残す（規約 docs/conventions/orchestration.md「完了とpush」の書式: 所要（並行実行）: 依頼 … → 完了 …（N分）、うち枠待ちN分・CI待ちN分、規模札X。超過時は理由を1つ）。状態の表には実時間・予算・差を写さない（T1065 で task_log と scale_budget_min を外す）。

対応方針:
1. 予算を読むときに計算する: docs/records/tasks/*.md から「所要（並行実行）:」の行を集め、規模札ごとに「実時間 − 枠待ち − CI待ち」（作業そのものの時間）と実時間の両方の80パーセンタイルを出す。見込み超過の検査（orchestrate.py check・gate）はこの値を使う。値はどこにも保存しない。件数が少ない規模札（例: 5件未満）の扱いを決める（例: 隣の規模札の倍率で補う）。
2. 今日の実績（上の表）を、各タスクの Txxx.md へ所要の行として書き足し、最初のデータにする（担当の報告の時刻が出典。分からない待ちの内訳は「不明」と書く）。
3. 超過の理由の集計を出す（規模札ごとに「見立てより中身が大きい／環境の待ち／進め方」の件数）。「見立てより中身が大きい」が続く規模札は、起票の書き方（CLAUDE.md の規模の目安の運用）を見直す材料としてユーザーへ示す。
4. /orchestrate:status か check の出力に、今の予算（規模札ごとの80パーセンタイルと件数）を1行出す。

前提: T1065（状態の表から写しを外す）が master に入っていること。

完了条件: 所要の行を1件足すと予算が変わること、飽和中の外れ値1件が予算を大きく動かさないこと（作業そのものの時間で見る側）を、壊した入力で確かめる。今日の実績から計算した S・M の予算が記録にあること。

## 実施内容

- **方針1（予算を読むときに計算）**: `scripts/orchestration/core.py`の`effort_records`が、origin/master の
  タスク記録から所要の行を1回の`git grep`で集め、`parse_effort`が「完了 …（N分」・`枠待ちN分`・
  `CI待ちN分`・`規模札X`・`超過: …`を読む（「不明」はNone。幅のある規模札は大きい側）。
  `effort_budgets`が規模札ごとに、作業そのものの時間（実時間 − 枠待ち − CI待ち）と実時間の
  80パーセンタイル（最近順位法）を出し、**作業そのものの時間が5件以上ならそれを、無ければ実時間を
  予算にする**。どちらも5件に満たない規模札は、実績のある隣の規模札に規模札の定義の比（S:M:L=
  1:4:8）を掛けて補い、隣も無ければ定義の値。check・gate・status の見込み超過はこの値を使い、
  値はどこにも保存しない。T1065の「台帳の規模札の定義から」を置き換えた。
- **件数が少ない規模札の扱い**: 閾値5件。最近順位法の80パーセンタイルは、5件あれば外れ値1件が
  最大値の位置に入るだけで予算の順位（4番目）は動かない——5件未満では1件で予算が決まってしまうため
  使わない。補いに定義の「比」を使うのは、定義の絶対値（人の作業前提）は並行実行の実績と合わないが、
  規模札どうしの大小関係は起票の見立てとして残っているため。
- **方針2（今日の実績）**: 背景の表の値を、各タスクの記録（T1029・T1024・T999・T1004・T993・T1061・
  T1054・T1056・T1058・T1023・T1027、T1014の段階A・B）の末尾へ所要の行として書き足した。依頼と
  完了の時刻・待ちの内訳は「不明」、出典は「担当の報告の時刻から見た振り出し〜報告」。T1023・T1027
  には「機械の飽和中」と書き、T1014の段階は規模札を書かない（段階には札が無い）。
- **方針3（超過の理由の集計）**: `/orchestrate:status`が、所要の行の「超過: …」を規模札ごとに数えて
  1行出す（今の記録にはまだ「超過:」の行が無いので出力されない）。
- **方針4**: check は毎回、規模札ごとの予算・根拠・件数を1行出す（status も同じ行を出す）。
- 規約（`orchestration.md`「完了とpush」「管理のループ」）を、予算の計算と所要の行の読まれ方に合わせた。

## 今日の実績から計算した予算

作業ツリーの記録（本タスクで書き足した後）から、核と同じ計算で出した値。masterへ入ると check が
同じ値を出す（入る前の origin/master は所要の行が少なく、S=60・M=240・L=480 の定義へ落ちる）:

| 規模札 | 予算 | 根拠 | 実時間の80パーセンタイル（件数） | 作業そのものの時間の80パーセンタイル（件数） |
|---|---|---|---|---|
| S | 25分 | 実時間 | 25分（5件: 10・20・24・25・160） | 8分（1件） |
| M | 105分 | 実時間 | 105分（9件: 18・25・30・35・45・75・84・105・260。S〜Mは大きい側のMに数える） | 82分（1件） |
| L | 210分 | Mの実績×2 | —（0件） | —（0件） |

T1042の所要の行は「完了 16:05（T1066へ畳んだ）」で分が無く、着手しないまま畳んだタスクなので、
予算の計算に入らない（読めない値は数えない）。作業そのものの時間は、待ちの内訳の分かる行がまだ
少なく（S・Mとも1件）、当面は実時間で予算が決まる。

## 検証

使い捨てのスクリプト（作業ツリーの記録を読み、核の`parse_effort`・`effort_budgets`をそのまま使う。
引数の行を架空のタスクの記録として足す）で:

| 入力 | Sの予算 | 見るところ |
|---|---|---|
| 今日の実績 | 25分（実時間、5件） | — |
| Sの所要の行を1件足す（実時間40分・待ち不明） | **40分**（実時間、6件） | 1件で予算が変わる |
| 待ちの分かるSの行を4件足す（作業20・25・20・25分） | 25分（作業そのものの時間、5件） | 作業の側が5件に届き、根拠が切り替わる |
| さらに飽和中の外れ値1件（実時間300分・枠待ち270分・CI待ち10分） | **25分**（作業そのものの時間、6件） | 外れ値の作業は20分で、予算は動かない |

- 実物の origin/master に対する`python scripts/orchestrate.py check`が予算の1行を出すこと（git grep の
  読み取りが通ること）を確かめた（入る前なので S・M・L とも定義へ落ちる: S 実時間1件・M 実時間3件）。
- `python -m ruff check scripts/orchestrate.py scripts/orchestration/`・`python scripts/review_checks.py docs`。

所要（並行実行）: 依頼 2026-09-23 16:51 → 完了 16:58（7分）、うち枠待ち0分・CI待ちはこのコミットの後（不明）、規模札S
