# T1145. 画面側に、到達しないガードと、同じ整合・取得の骨格・写しの型が複数の入口に散っている

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 嘘の戻り値型と死んだガード・多重ドア・写経・共有断片から外れた手書き（いずれも再発）。死んだガード: 生成された型が必須とするフィールドへの?? 既定値が共有層で9件（useAxisCatalog.ts:49-55、axisLayers.ts:136-141。typescript-eslintのno-unnecessary-conditionで列挙）、軸スタジオ由来レイヤーの除外を3回判定（overlayChips.ts:58、MapOverlayControls.tsx:610、mapLayers.ts:118）、useRouteFormSubmit.ts:83-98の到達しない入力検証（距離の範囲はtype=rangeが作れない。page.tsx:274の≥1と:89の>0も食い違う）。多重ドア: 重みのキーを軸カタログへ合わせる入口が2つ（RouteSettingsPanel.tsx:52-57のeffectとpage.tsx:592の送信時。ハードフィルタは復元時1回）。共有断片から外れた手書き: lib/time.tsを通さない時刻の切り出し（SegmentWind.tsx:15 slice(11,16)、TodayOutlook.tsx:36）。写経: admin/useMapBandsOfThresholds.tsとuseScoresPreview.tsが同じ骨格、軸カタログの行を手書きのcamelCaseの型4つ（PreferenceAxisDef・SecondaryAxisSummary・RampAxis・DedicatedWayValueAxis）へ写し、略名の補い方が型ごとに違う。1件ずつ消すか1か所へ寄せる。写しの型4つは次に軸カタログの項目を足すタスクと一緒でもよい。lintのCI化はnoUncheckedIndexedAccessの誤検知の扱いが決まるまで見送る。
