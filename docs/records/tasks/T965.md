# T965: 「一本化した」と書いた過去の改修が、本当にそうなっているかを棚卸しする

状態: 完了（2026-09-24）

2026-09-19起票。段階A（20件の試し）と段階B（残り74件と、派生の入口の一本化）は2026-09-24に済んだ

## 背景

複雑度を下げる改修をしてもコードが増え続けている（[T963](T963.md)）。原因の1つは、
**旧実装が残ったまま「完了」になっていること**である。

[T956](T956.md)の作業中に実例が出た。[T546](T546.md)は「`EdgeMaterialTable`で復元コストを
Edge数に依存しなくする」を達成したが、**旧`EdgeMaterialBundle`経路は残った**。当時は意図した
並存だが、いつ解消するかを誰も持っていなかった。同じ作業中に私自身も、本番から到達しなく
なった実装を「後で消す」と言って残していた。

`docs/improvement-plan.md`の`[x]`は700件、うち「一本化・共通化・統合・撤去・削除・廃止」を
題や注記に含むものが**150件**。

## 対応方針

その150件それぞれについて、**タスクが「消した」と書いているシンボル・ファイル・経路を
いまのリポジトリでgrepする**。残っていれば、その改修は主張した状態に達していない。

結果は3つに分かれるはず。

- **消えている**: 主張どおり。何もしない
- **残っているが理由がある**（テスト専用のオラクル等）: その理由をタスクエントリへ書き足す
  ——「なぜ残すか」が書かれていないものは、次に読んだ人が本番経路だと誤解する
- **残っていて理由が無い**: 削除を起票する

あわせて、**そのタスクが残す/消すの判断に使った根拠の形**も見る。「0件だった」「使われて
いない」のように**その時点の計測だけで決めているもの**は、条件が変われば通る。
「いつなら通るのか」が書かれているかを確認し、書かれていなければ足す
（[T961](T961.md)が実例——`edge_landcover`のカバレッジ100%を見て「way側フォールバックは
死んでいる」と判定したが、実際は「いまバッチが最新」という意味でしかなかった）。

有限で、結果が白黒つく。**件数が多いので、まず20件ほどで当たりの割合を見てから残りの
進め方を決める**（全件を最初から通すと、当たりが少なかったときに費用だけ払う）。

## 母集団2は、症状から決めない

上の150件は**文書に残った判断**だが、見逃しは定義上そこに現れない。今日の
[T961](T961.md)の誤判定も、指摘されなければ通っていた。**記録されたものだけを見るのは
循環した点検**である（ユーザー指摘）。

起票時にここへ「SQLの`COALESCE` 28件とPythonの`or {}` 28件の計56か所」と書いたが、
**それは今日の不具合と同じ書き方をgrepしただけ**で、母集団の選び方が症状由来だった
（CLAUDE.md「修正の原則」の「範囲は性質から導く」に反する）。`LEFT JOIN`でNULLになった列も
`MaterialSpec.bool_default`のような宣言された既定値も入っておらず、`.get(k, 既定値)` 89件を
中身を見ずに「大半は素直」と切り捨てていた。**この母集団は撤回する。**

コード側の網羅は、**入力の源流からの到達可能性**で出す（[T963](T963.md)）。到達しない
実装を孤立点として機械的に列挙してから、残すかどうかを判断する。

## 今日起票したものとの関係（包含・供給・競合）

- **[T964](T964.md)（PostGIS統合テストのDB共有）とは別物**。検知器が語の重なりを指摘するが、あちらは
  並行セッション間で同じテストDBを共有している件で、母集団も対象も重ならない。

- **[T961](T961.md)はこのタスクの母集団の1件**である。土地被覆のway側フォールバックは
  「無ければ別の源で代用する」箇所そのもので、独立の不具合ではなく**先に見つかった1例**。
  T961を単独で直しても、同じ性質の他の箇所は残る。
- **[T963](T963.md)がこのタスクの入力を作る**。到達可能性グラフが孤立点を出し、
  このタスクがその1つずつに「残す理由があるか」を判断する。**T963が先**。
- **[T956](T956.md)がいま消している実装は、T963の検知対象そのもの**。T963を実装したら、
  T956で消し残したものが出るはず——出なければT963の辺の作り方が甘い、という検証に使える。

## [T963](T963.md)との違い

T963は**今後**増え続けることを見る仕組み。こちらは**過去**の清算で、一度きりの棚卸し。

## 段階A: 20件を試す（2026-09-24、ユーザー決定「20件試して当たり率で決める」）

見込み（CLAUDE.md「規模の札」）: 調べる段階なのでS（①）。崩れが見つかって直すぶんだけ増える。

### 母集団

**定義**: 状態が`完了`のタスク記録（`docs/records/tasks/Txxx.md`、826件）のうち、記録の題（1行目）か、
origin/master上でそのタスク番号を件名の頭に持つコミットの件名に、「何かを1つの正本・1本の実装へまとめた」ことを
表す語を含むもの。単位はタスク1件。

- 語: `一本化|1本化|一元化|寄せ|統一|置き換|置換え|統合|共通化|集約|(1|一)(本|つ|か所|箇所)(へ|に)(まとめ|畳|寄せ|絞)|へ畳|に畳|1本道|一本道`
- 当てる前に文から落とす語（語は当たるが、まとめた主張ではない）: `統合レビュー`・`統合テスト`・`統合実装`・`左寄せ`・
  `寄せた側`・`寄せるかの保留`・`T\d+(の段階|段階…)?へ(統合|寄せ)`（タスクどうしの合流）
- 数えないコミット: 件名に`起票`を含むもの・マージ（結果の主張ではない）
- 記録の題は起票時の予定だが、完了したタスクの題は「そうした」という主張として読む

**件数**: 語だけで147件、落とす語・数えないコミットを除いて**99件**（起票時の見込み約150件との差は、ほぼ
`統合レビュー`・`統合テスト`とタスクどうしの合流）。**検算**: 既知の1件
[T1044](T1044.md)（題に語が無く、コミット件名「…2つの断片へ寄せ、手書きの7か所を0にする」だけで当たる）が入ることを確かめた。

### 選び方

99件をタスク番号の昇順に並べ、Pythonの`random.Random(965).shuffle`で並べ替え、先頭から順に見た。
**見た目・配置・書式を揃えた主張**（「縦並びへ統一」「同じ見た目に統一」等）は、1つの正本・1本の実装を
主張していないので対象外として飛ばし、次を取った。先頭25件のうち対象外は5件（T417・T369・T505・T545・T372）で、
残る20件を見た。

### 判定の基準

主張が崩れている＝**同じ事実の宣言・実装が、今のmaster（`97deba4d`）に2つ以上ある**（旧い実装・二重の宣言・
写しが残る、または後から戻った）。主張の対象が後続の作り直しで丸ごと消えたものは崩れに数えない。コメント・
文書に死んだ名前が残るだけのもの、テストだけに残るものは「付随」として分けた（テストは並行する
[T1021](T1021.md)が作り直し中のため直さない）。各件は、記録・コミットが名指ししたシンボルと、同じ事実を
持ちうる名前・値を`git grep`で数えて判定した。

### 結果

| # | タスク | 主張 | 判定 | 根拠（今のmaster） |
|---|---|---|---|---|
| 1 | T945 | 静的レイヤーの宣言（アイコン・データ源・描画順・凡例）を記述子へ | 保たれている | 畳んだ4つの表の名前は0件。データ源は記述子の`dataSource`から導く |
| 2 | T679 | fetch骨格・定期取得・(i)→Popover・気象庁の時刻一覧・MVTのMIME等を1つへ | 保たれている（注記） | 直接の`fetch(`は骨格と外部の中継だけ。MVTのMIMEの綴りは`response_compression.py`の圧縮対象の一覧にもあるが、「どの媒体を圧縮するか」という別の事実で、生成側の写しではない |
| 3 | T522 | 材料の読み出しを1つの束へ | 保たれている | 後続の[T956](T956.md)が列の配列へ作り直し、4辞書への再分割は無い |
| 4 | T575 | プロセス内キャッシュの追い出しを`cachetools`へ | 保たれている | 同じ列挙（`pop(next(iter`等）で0件。辞書の大域変数はレジストリ・統計・ジョブ台帳 |
| 5 | T460 | 動的way値のサービス組み立てを登録テーブルへ | 保たれている | 材料id→組み立ての表1つ。材料名の分岐は0件 |
| 6 | T530 | `wind`という名の器を`weather`へ | 保たれている | `wind=`・`.wind`は0件 |
| 7 | T483 | 軸ごとのpropを`dedicatedWayValues`1つへ | 保たれている | 旧prop名は0件 |
| 8 | T763 | 候補の後処理を1か所へ | 保たれている | 生成の4経路がすべて`_evaluate_and_aggregate`を通る |
| 9 | T350 | 軸定義の退避・復元を1つへ等 | 保たれている | 退避・復元は`axis_definitions_snapshot`だけ。CIのジョブ統合はその後の判断で分け直され、理由がci.ymlにある |
| 10 | T457 | 設計原則を1文書へ | 保たれている | 原則を定める文書は`design-principles.md`だけ |
| 11 | T281 | 派生の作り直しを単一の入口へ | 保たれている | 順番を持つのは`derive_cli.py`だけ（下の標本外の件は別） |
| 12 | T347 | 自転車インフラの旧材料・分類関数を消して新しい軸へ | 保たれている（付随） | 実装は0件。例示に消えた材料名が2か所残る（下） |
| 13 | T341 | 分類の定義場所を1つへ | 保たれている | 二重だったPython側の分類関数は後続のT347で消えた |
| 14 | T1071 | GitHubへの問い合わせを1か所へ | 保たれている | 問い合わせは`scripts/orchestration/github.py`経由だけ |
| 15 | T805 | 較正値の宣言を1つにし、読み出しをそこへ | **崩れている** | 下 |
| 16 | T608 | 動的気象の取得状態を1つの関数へ | 保たれている | 状態は`deriveFetchLayerStatus`1本、旧い集約は0件 |
| 17 | T758 | 生成のフィードバックを「ルート結果」欄1か所へ | 保たれている | 失敗・生成中の出し先は`renderRouteOutcomeEmptyState`だけ |
| 18 | T596 | 出発時刻・速度を条件バーへ、出発時刻のstateを1つへ | 保たれている | 出発時刻は`useDepartureTime`1つ。時刻スライダーは後続のT611が条件バーの中へ戻したもの |
| 19 | T884 | テストの足場を共有へ | 対象消滅 | 対象のテストは作り直され、足場ごと無い |
| 20 | T462 | ルート生成エンジンを1本へ | 保たれている（付随） | 実装は0件。テストのフェイクに多エンジン時代の`engine_name`が残る（下） |

**当たり率: 1/20（5%）**。

#### 崩れていた1件（直した）

[T805](T805.md)は「較正値の宣言を1つにする」としたが、主観と時間の換算レート（P）の既定が2つあった。
較正値`evaluation.penalty_strength`の既定は0.7、`domain/evaluation.py: compose_costs_from_axis_matrix`の引数の
既定は1.0（T805より前からある）。本番の呼び出し（`road_graph_engine.py`の1か所）は必ず値を渡すので1.0は使われて
いないが、モジュール文書`evaluation-scoring.md`は「P、既定1.0」と書いており、**文書が実際の既定（0.7）と違う値を
教えていた**。引数の既定を外し（呼び出しの無い既定は宣言の二重化でしかない）、文書を較正値の宣言へ向けた。
同じ事実を主張する[T817](T817.md)（「penalty_strengthの既定値を片側importへ寄せ」、母集団の標本外の1件）も
これで同時に崩れていたことになる。

#### 付随（数えない）

- `frontend/src/features/admin/AxisStudio/axisDraft.ts`のコメントと`docs/modules/frontend/axis-studio.md`が、
  分類材料の例に消えた`bicycle_infra`を挙げていた。文書は直した（例を落とし、挙動の記述だけ残す）。
  frontend側のコメントは並行する[T1001](T1001.md)の範囲なので直していない。
- `backend/tests/test_route_generator.py`の`FakeEngine.engine_name = "fake"`は、本番のエンジンが持たない属性。
  [T1021](T1021.md)の範囲なので直していない。

#### 標本外で見つけた1件

T281を見る途中で、[T650](T650.md)（母集団の1件、標本外）の崩れが見えた。T650はバッチの起動処理
（引数の解析→ログ設定→実行）を`_common.py`の1関数へ寄せたが、後続の作り直しでその関数ごと消え、
今は`derive_topology`・`derive_node_materials`・`derive_counts`・`derive_raster_materials`・
`derive_way_materials`の`main()`が同じ骨格を1つずつ持つ。しかも各段は単独の入口（`python -m app.batch.derive_*`）を
持ち、`derive_cli.py`の「順番はここだけが持つ」と並んで**段を単独で流す扉が別にある**。単独の入口を残すかは
取込の設計判断なので、ここでは直さず改善案（ダッシュボード`T965-derive-entrypoints`）に置いた。
**当たり率には入れない**（標本の外で、無作為に選んでいない）。

### 見積もりとのずれ

- 残りは約130件の見込みだったが、実際は**74件**（99−25）で、そのうち主張と読めるのは先頭25件の割合（20/25）から
  約59件。語だけで数えた147件は`統合レビュー`等で膨らんでいた。
- 崩れていた2件（T805と標本外のT650）は、どちらも「旧い実装が残った」形ではなかった。T805は寄せたときに
  同じ事実の既定を1つ見落とした形、T650は寄せたあとの作り直しで写しが戻った形。起票時に想定した
  「旧実装が残ったまま完了」（T546の`EdgeMaterialBundle`）の形は20件に1件も無かった——[T968](T968.md)の
  到達可能性の掃除と[T996](T996.md)の全件の読みが、その形を既に片付けていたと読める。

所要（並行実行）: 段階A 依頼 2026-09-24 06:44 → 区切り 07:01（17分）、うち枠待ち0分・CI待ちは報告時点で未計測、実績 +1/−1 → S

## 段階B: 残りを全部見る・派生の入口を1本にする（2026-09-24）

### 問いと答え

- 問い（`T965-rest`）: 点検を試した20件（崩れ1件）で打ち切るか、残りも見るか → 答え（2026-09-24）:
  (3) 残りを全部見る（74件、うち主張と読めるもの約60件）
- 改善案（`T965-derive-entrypoints`）: 派生データを作り直すバッチの「1段だけ流す入口」5本を消し、作り直しの入口を1本
  （順番を守る`derive_cli.py`の`--from`指定）にする → 承認（2026-09-24）
- 問い（`T965-B-code-review`）: 取り込みの前に`/code-review`を回すか → 答え: (3) 回す（high）。司令塔が回す

見込み（CLAUDE.md「規模の札」）: 点検は調べる段階なのでS。入口の撤去は変わると見込むファイル6本（段5本＋`derive_cli.py`）でS。

### 点検の進め方

母集団・並べ替え・判定の基準は段階Aと同じ。段階Aで保存した母集団（99件）を種965で並べ替え直し、先頭25件
（見た20件と対象外の5件）を除いた**74件**を全部見た。4つに分け（24・20・17・13件）、読み取り専用の調査の担当4本が
各件の主張を記録・コミットから取り、名指しされたシンボルと同じ事実を持ちうる名前・値を`git grep`で数えて判定した
（0件のときは寄せた先の既知の1件を拾えることを検算）。**崩れと判定したものは、担当の出力をそのまま使わず自分で
読み直して確かめた**（T550・T593・T632・T817・T821・T955/T956）。対象は`377eb606`。

### 結果

| 判定 | 件数 | タスク |
|---|---:|---|
| 対象外（見た目・配置・書式・語彙の統一、タスクやコミットの合流、テストの追加） | 18 | T250・T387・T418・T452・T453・T476・T507・T518・T583・T598・T609・T611・T613・T628・T629・T668・T761・T791 |
| 保たれている | 28 | T244・T316・T440・T472・T473・T481・T504・T592・T610・T634・T644・T653・T658・T718・T749・T766・T810・T922・T946・T949・T968・T994・T1006・T1023・T1024・T1031・T1044・T1054 |
| 保たれている（付随: コメント・文書・テストに死んだ名前） | 14 | T280・T285・T336・T525・T595・T657・T673・T681・T731・T888・T979・T986・T995・T1015 |
| 対象消滅（後続の作り直しで主張の対象ごと消えた） | 5 | T398・T413・T479・T511・T748 |
| **崩れている** | **9** | T550・T593・T632・T650・T768・T817・T821・T955・T956 |

（対象外18＋判定56＝74。）

**当たり率: 9/56（16%）**。

### 見積もりとのずれ（結論より先に）

- 段階Aの当たり率（1/20＝5%）から、残り約60件で「3件前後（0〜十数件の幅）」と見込んだが、**9件**だった。
  うち2件（T650・T817）は段階Aで標本外に見えていたもので、それを除いても7件。
- 段階Aは「旧い実装が残ったまま完了」の形は20件に1件も無いと読んだが、**1件あった**（T550: 1区間ぶんの寄与度を
  求める版を足して呼び出しを切り替え、全区間ぶんを作る配列版の枝を本番から届かないまま残した）。
- 崩れの形は3つに分かれた。**寄せたときに同じ事実を1つ見落とした**（T817のエンジン側の既定・T821の材料idの綴り・
  T955/T956のタイルSQLの条件）、**寄せた後に写しが足された・戻った**（T593・T632・T768は後のコミットで写しが入り、
  T650は作り直しで写しに戻った）、**置き換えた側を残した**（T550）。
- 利用者の結果に効いていたのは1件（T817: 地点確認のAPIだけがP=1.0で探す。画面からは呼ばれていない）。

### 崩れていた9件の処置

| タスク | 何が2つあるか | 処置 |
|---|---|---|
| T632 | タイル応答の`no-store`を`_tile_http.py`が直書き（対応表`cache_policy.NO_STORE`と二重） | **直した**（`NO_STORE.header()`を使う。出る値は同じ） |
| T821 | 停止要因の密度の材料id`poi_<種別>_per_km`の綴りが`traffic.py`・`material_catalog.py`・`road_graph_repository.py`（タイル列名）で別々 | **直した**（`traffic.py: poi_density_material_id`へ寄せ、3か所から呼ぶ。タイルSQLの文字列が変わらないことを実行して確かめた）。件数の列名`poi_<種別>`と、数の段が停止要因を数えて0で埋める列も手で並んでいた——下の「code-reviewの指摘の直し」 |
| T650 | 派生の段ごとの`main()`が起動処理を1つずつ写していた | **直した**（下の「入口の一本化」）。`scripts/measure_axis_saturation.py`が`_common.batch_session_factory`と同じ形を手で書いていたのも**直した**（`batch_session_factory`を使う）。残る入口（取込・派生・DBの立ち上げ）の`main()`の骨格の写しは起票案`kihyoan-batch-cli-skeleton`（共有する部分が1入口あたり5行前後と小さく、寄せる形に判断が要る） |
| T593 | 専用配信の軸の「符号付きの材料か」の判定を、`map_value_kind`と`map_value_thresholds`が別々に書く（後者は「項が材料を指すか」を欠く） | 改善案`T1021-dynamic-thresholds-kind`。1行の直しで、本番の軸では振る舞いが変わらないこと（本番DBを読み取りで確認: 絶対値を評価する軸は勾配の1本で項は材料、境界の上書きもある）まで確かめたが、置き換えるとT1021が群2で起こし直したテストが落ちる（テストが材料カタログの足場を付けずに符号付きの境界を期待している）。テストはT1021の持ち物なので実装も戻した |
| T550 | 軸ごとの寄与度が配列版（本番から届かない）と1区間版の2本 | 起票案`kihyoan-penalty-contrib-single`（テストの偽物の引数を直す必要があり、このタスクでは`backend/tests`を触らない） |
| T817 | Pの既定が較正値（0.7）とエンジンの引数（1.0）の2つ。地点確認のAPI・ベンチは1.0で探す | 同じ起票案（振る舞いが変わる: 地点確認のAPIのPが較正値になる） |
| T955・T956 | 真偽の材料9件の条件式が、材料の`value_sql`と道路タイルのSQLに別々に書いてある | 起票案`kihyoan-tile-bool-from-catalog`（タイルの形の署名が変わり路面タイルの作り直しを伴う） |
| T768 | e2eの`states.ts`が生成の完了待ちを`fixtures.ts`と別に書く | 改善案`T1001-unify-leftovers`（frontendはT1001の範囲） |

### 付随（数えない）の処置

- 直した（実装と逆のことを言っている文書、消えたものを指すコメント）:
  - `docs/modules/backend/evaluation-scoring.md`: `bool_default`を「材料ごとに固定する宣言」「`missing_semantics`から
    導出しない」と書いていたが、実装は`coverage.missing_semantics`から導くプロパティ（T995）。`MATERIAL_COVERAGE_SPECS`を
    「宣言テーブル」と書いていたが、カタログから導いた値（T956）。
  - `docs/modules/backend/dynamic-way-values.md`: `map_value_kind`の条件から「項が材料を指す」が抜けていた。
  - `docs/architecture/design-principles.md`: 消えた`.claude/commands/review/principles.md`・`overall.md`・`complexity.md`と、
    台帳に無い「進め方の原則」節を指していた（T979・T986）。
  - 消えた`extractor`を今もあるように語るコメント: `domain/evaluation.py`・`domain/axis_definitions.py`・
    `domain/dynamic_materials.py`（T280・T955・T956）。消えた判定の置き場を指すコメント:
    `infrastructure/tile_score_matrix_cache.py`（T731）。ハードフィルタのキーを数え上げるdocstring:
    `api/routers/routes.py`（T673）。消えた`docs/architecture.md`を指すコメント: `restart-dev.bat`（T979。同じ文の経緯の
    言い回しも落とした）。`backend/requirements-dev.txt`のコメントが消えたpre-commitと`scripts/pre-commit-ruff.sh`を
    挙げていた（T979）。
- 直していない:
  - frontend（T285・T336・T413・T525・T595・T748・T888・T1015のコメント・文書、T650のテストの`jsonResponse`の写し、
    T657の読み手の無い生成物`poi-kinds.json`）→ 改善案`T1001-unify-leftovers`
  - backend/tests（T650の`test_fetch_*`のストリーム応答の偽物の写し）→ 改善案`T1021-dynamic-thresholds-kind`に併記。
    T681のテストが保存キーの文字列を直書きするのは、キー定数が`page.tsx`の中にありテストからimportできないため避けにくい写し

### 迷い（判定が分かれるので崩れに数えない）

- T634: 気象庁タイルのパスを組み立てるのは1か所だが、読み取り側（`jmaTileIndex.ts`の`"/surf/"`）も同じ形を知っている
- T644: Redisの可用性の確認→取得→失敗の記録という骨格を`jma_amedas_service.py`も2回持つ（T644が挙げた4モジュールの外、
  ハッシュ格納で`redis_json_cache`へそのまま寄せられない）。同じ2行は`jma_tile_redis_cache.py`にもある（T472の隣）
- T653: `tile_cache.py`（基礎地図・地理院タイル）が自前のアトミック書き込みを持つ（T653の対象外、その前から存在）
- T968: 区間の辞書の合わせ方が、ビン単位（`route.py: BIN_DICT_FIELD_MERGERS`）と候補単位
  （`route_generator.py: SEGMENT_AGGREGATES`）で別々に宣言されている。粒度が違い、同じ事実かが分かれる
- T1015: 方位の丸めがbackendとfrontendの両方にある（コミットが「計算は配れない」と明記して残したもの）
- T598: 軸スタジオの折れ線の補間をfrontendでも書いている（backendと同じ補間として意図して作った写し）

範囲外の観察（どのタスクの主張にも当たらない）: 事故の生データを読む`source = 'accident'`が`derive_counts.py`と
`accident_repository.py`に手書きである（T1031・T1044がway・nodeで寄せたのと同じ形。ただし副問い合わせの形を持たない
素の絞り込みで、寄せるとタイルの形の署名が変わる）。

### 入口の一本化

消す前に、段ごとの入口を呼ぶものを全部出した（`git grep -n -E "derive_(topology|node_materials|counts|raster_materials|way_materials)"`
をリポジトリ全体と記録に、`-m app.batch.derive_`を記録に、`derive`を`.github`・`scripts`・`*.sh`・`*.yml`・`Dockerfile*`に、
自動メモリの運用メモに）。

- 単独の入口（`python -m app.batch.derive_*`・各段の`run`/`main`）を呼ぶのは、各段自身のdocstringの「実行方法」だけ。
  ワークフロー・デプロイ・スクリプトからは0件。`bootstrap_database.py`は`derive_cli.run`を、テストは各段の`derive(conn)`を呼ぶ。
- 本番で段を単独で流す運用: 記録に2件あった。[T1064](T1064.md)（本番で`derive_raster_materials`を回し直す計画）は
  ユーザー決定「作り直さない」で閉じている。[T1050](T1050.md)（未完了）は信号の半径の較正値をノードの段が読むようにする
  タスクで、段の名前をファイルとして挙げるだけで単独の入口を使う手順は書いていない。**止まる条件（単独で使う運用がある）には
  当たらない**と判断した。
- 注意（T1050の較正に効く）: 1段だけを流したいときの`--from <段>`はその段から後ろを全部流す。ノードの段（`nodes`）から
  流すと、土地被覆・標高の段（`raster`）も流れ直す。本番で`raster`は全258万区間で約70分以上の見込み（T1064、1万区間の所要
  からの比例で未計測）。`raster`は道1本の値の表（`counts`が作り直す`way_materials`）へ書くので、`counts`より前へは動かせない。

撤去したもの: 各段の`run()`・`main()`・`if __name__`と、それだけが使っていたimport・`sys.path`の差し込み。各段は`derive(conn)`
だけを持つ。撤去した文は「実行方法（backendディレクトリから）: …」の5つで、どれも消える入口の使い方なので削除した
（comments.mdの判定木の1でNo）。段の依存と、単独の入口を持たない理由を`derive_cli.py`のdocstringと
`static-road-attributes.md`「派生」節へ書いた（はじめ「後ろの段は前の段の出力を読む」と全段に成り立つように書いたのを、
code-reviewの指摘で実際の依存どおりに直した。下）。
ノードの段のdocstringの「ここを流し直せばよい」は「この段から流し直せばよい（`derive_cli --from nodes`）」にした。

### code-review（high）の指摘の直し

司令塔の`/code-review high`（`a8ca6adc...39b27019`）の4件。どれも段階Bの範囲なので同じコミットで直した。

1. **数の段が停止要因の種別を手で並べていた**（T821の崩れの残り）。`derive_counts.py`の`_EDGE_STOP_COUNTS`・`_EDGE_STOP_ZERO`・
   `_WAY_FROM_EDGES`が5種を手で書いており、`POI_COUNT_KINDS`へ種別を足すと材料・タイルの列は増えるのにこの段は数えず
   0でも埋めないため、新しい材料が未計算（NULL）のまま残る。`POI_COUNT_KINDS`から組み立てる形にした。0で埋める文は、
   どれか1列がNULLの行の**NULLの列だけ**を0にする形にした（種別を足したとき、既にある列の値を0で潰さない）。
2. **件数の列名`poi_<種別>`が4か所に手で綴られていた**（`derived_models.py: _COUNT_COLUMNS`・`material_sql.py:
   poi_density_value_sql`・`road_graph_repository.py: _POI_TILE_COLUMNS_SQL`・`derive_counts.py`）。`traffic.py:
   poi_count_column`へ寄せ、`poi_density_material_id`もそこから導く。ORMの列の宣言（`EdgeMaterialRow`・`WayMaterialRow`の
   `poi_signal`等）は手で並んだまま——種別を足して列の宣言を忘れると、`_COUNT_COLUMNS`から作る検査制約が無い列を指し、
   表の作成（`create_tables()`）で止まる（黙って欠けない）。土地被覆の列（`lc_*`）と同じ扱い。
3. **段の依存の書き方が全段には成り立たなかった**。`raster`はノードの値も数の値も読まず、`counts`が作り直す道1本の行へ
   書き込むためにその後ろにある（`derive_raster_materials.py`・`derive_way_materials.py`が読む表を`grep`で確かめた）。
   実際の依存どおりに書き直した。「ノードの段だけを直したいのに`--from nodes`で`raster`まで流れる」は、直し方を決めずに
   改善案`T965-derive-from-granularity`としてダッシュボードに置いた（選択肢: 今のまま／依存を入口に宣言／道1本の行の作成を
   最初の段へ移す／段を選んで流す指定）。
4. `routes.py`の`HardFilterOverride`のdocstringの行幅を詰めた。

検証（別の入力で）: 数の段を、旧い手書きのSQL（`origin/master`の`derive_counts.py`）と新しい組み立てで、開発DBの同じ入力へ
1つのトランザクションの中で当てて比べ、最後に巻き戻した → 区間23,326行（停止要因が1つ以上の区間3,449）・道とも差0件、
NULLの残る区間0件。

### 検証

- `ruff check backend` → All checks passed
- `python scripts/review_checks.py docs` → 違反なし
- `backend/scripts/export_openapi.py`を流し直し、`git status --short frontend/`が空（生成物のドリフト無し。タイルの形の署名も
  変わらない）
- `pytest tests -q --co` → 2061件収集（読み込みの壊れ無し）
- `python -m app.batch.derive_cli --help` → `--from {topology,nodes,counts,raster,ways}`
- 変更が届くテスト: `test_axis_catalog_routes.py`・`test_axis_display.py`・`test_axis_registry_service.py`・
  `test_dynamic_way_values.py`・`test_road_graph_engine.py`・`test_traffic.py`・`test_material_catalog.py`・
  `test_batch_common.py`・`test_region.py`・`test_derive_counts.py`・`test_derive_node_materials.py`・`test_derive_topology.py`
  → T593の直しを入れた状態で335件通過・1件落ち（上の表）。実装を戻して`test_dynamic_way_values.py`だけを流し直し17件通過
- `_POI_TILE_COLUMNS_SQL`を旧い組み立て方と比べて一致（`True`）

所要（並行実行）: 段階B 依頼 2026-09-24 08:00 → 完了 10:05（125分。うち初回の報告まで36分、code-reviewの指摘の直し約30分、その間は司令塔のcode-review待ち）、うち枠待ち0分・CI待ち約3分、実績 +91/−252 → M（削除が252行で札を超えた。追加は91行）

## 閉じたあとに移した答え

- 改善案（`T965-derive-from-granularity`、段階Bのcode-reviewの指摘3から）: 交差点の値の段だけを直したいとき、
  作り直しの入口の`--from nodes`が土地被覆・標高の段（`raster`。本番で約70分以上の見込み、T1064の1万区間の所要からの
  比例で未計測）まで流れる。`raster`は交差点・数の値を読まず、数の段が作り直す道1本の行へ書くためだけにその後ろにある。
  選択肢: (1) 今のまま（「この段から後ろを全部」の粒度を受け入れ、流れる時間を払う） (2) 段どうしの読み書きの依存を
  入口に宣言し、変えた段の値を読む段だけを流す（宣言が実際の読み書きと食い違うと、読む段を流し忘れて古い値が残る）
  (3) 道1本の行を作る仕事を最初の段へ移し、`raster`を交差点・数の段より前へ動かす（数の段が道の行を消す・足す作りを
  分ける必要があり、実現できるかは未検討） (4) 段を選んで流す指定を足す（T965で消した単独の入口と同じ性質）。
  推奨は置かなかった（どれを取るかは運用の頻度次第。[T1050](T1050.md)の較正が済むまでは交差点の段を流し直す機会は
  多くない見込み） → 答え: **見送り**（2026-09-24）。今の「この段から後ろを全部」のまま
