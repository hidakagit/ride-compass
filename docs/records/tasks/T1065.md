# T1065. 状態の表から他に正本のある写しを外し、読むときに正本から導く

状態: 完了（2026-09-23）

## 背景

並行実行の状態の表（<gitの共通ディレクトリ>/orchestration/board.json）が、他に正本のある事実を写して持っている。ユーザー指摘「管理系の仕組みは、二重持ちした瞬間に破綻する」。実例: 司令塔が判断D-016・D-017の回答をタスクの記録（T1046.md）だけに書き、表の判断は未回答のまま、表の「前提の判断待ち」の写しにも決着済みのT1046が残り、のちの移行がその古い値を正式な形へ運んだ（2026-09-23）。規約 docs/conventions/orchestration.md「状態の表」節に原則（表に持つのは表にしか無い事実だけ）を置いた。

2026-09-23 15:40 時点の表の全キーを列挙して分類した（python で board.json の最上位・agents・decisions・queue・coordinator_queue・manual のキーを出現数つきで出した）:
A. 表にしか無い（残す）: run・limits.concurrent・check_interval_min・last_check／agents の id・name・where・current_task・task_first_started・started・expected_min・監査の記録（reported・reported_sha・audit_started・audit_done・audit_result・audit_log）／queue の task・after・agent・priority・added／coordinator_queue。
B. 他に正本がある写し（外して、読むときに導く）: queue の what（題名・規模・判断番号の文。正本は台帳の行とTxxx.md）／agents の scale_budget_min（正本は台帳の規模札。手入力で当日3回誤った）／agents の queue（1件ずつの依頼にしたので current_task と二重）／manual.T1001 の prereqs・waiting_decision・note（前提の関係の正本は T1001.md、判断待ちは判断の回答から導く）／decisions の question・options・answer（正本は各タスクの記録の保留節と「ユーザー決定」）／audited_unpushed の「未push」（masterに入ったかは git。cherry-pick で sha が変わるので patch-id で比べる）／last_master_push・last_master_push_at（origin/master の履歴）／agents の state のうち稼働か否か（正本は実行環境と作業ツリーの動き。今は status が突き合わせて印を出すだけ）。
C. 規則・記録の文（表から出す）: resume_policy・decision_policy・check_rule・limits.note（規約の写し。すでに規約と食い違う）／incidents・hookspath_reverts・audit_findings・notes（回の記録 T1049.md の持ち物）／pending_cleanup（台帳の T1042）／agents の note。

対応方針:
1. B の各項目を正本から導く読み出しを scripts/orchestration/ に作り、gate・check・status・dispatch list・prereqs・decision list がそれを使う形にする。題名・規模は docs/improvement-plan.md の行、前提は手動タスクの Txxx.md の「前提」節（無ければ作る。書き方を決めて規約へ）、判断は記録の保留節、push 済みかは origin/master と patch-id、稼働かは作業ツリーの最新の変更時刻と実行環境の報告。
2. 外す前に、今の表の値と正本を突き合わせ、食い違いの件数と中身を記録する（それが今までに起きていた破綻の件数）。
3. C を表から消す。記録として残す価値のあるものは T1049.md へ移す（同じ内容が既にあれば移さない）。
4. 表の書き込みは orchestrate.py のコマンド経由だけにし、B・C のキーを書こうとしたら拒否する（写しを再び作らせない）。
5. 表の移行は1回で、変換前の写しを残す。移行後に gate・check・status・prereqs T1001・decision list が同じ答え（または正本から見て正しい答え）を返すことを確かめる。

完了条件: 表のキーが A だけになっている（キーの列挙で確かめる）。B の各項目を、正本を1か所だけ変えると表示が変わることを壊した入力で1件ずつ確かめる（例: 台帳の規模札を変えると予算が変わる、Txxx.md の保留にユーザー決定を書くと判断待ちから消える、master に入れると未push から消える）。

## 追加（2026-09-23）: task_log も写しとして外す

T1056 で入った状態の表の`task_log`（報告の受領時に実時間・規模札の予算・差を残す）は、
3つとも導ける値の写しで、しかも表はリポジトリに入らず履歴が残らない。ユーザーと合意した形:
実行中は表に「今のタスクと振り出した時刻」だけを持ち、完了時は担当が完了のコミットで
`Txxx.md`へ所要を1行書き（規約「完了とpush」）、監査を通ったら表からその行を消す——所有が
表から記録へ移るだけで、同じ事実を同時に二か所に持たない。本タスクで`task_log`と
`scale_budget_min`を表から外し、予算を記録の所要の行から読むときに計算する側（別タスク）へ渡す。

## 実施内容

- **B（正本から導く読み出し）**: `scripts/orchestration/core.py`に`ledger_rows`（台帳の未完了の行の
  題名と規模札。「S〜M」は大きい側）・`budget_of`（担当の現在のタスクの台帳の規模札から予算）・
  `task_title`（台帳の行、無ければ記録の見出し）・`audited_unpushed`（監査の記録の`通す`のうち、
  `git cherry`のpatch-idでも件名でもorigin/masterに見当たらないもの。司令塔が取り込みで衝突を
  解くと中身が変わりpatch-idだけでは入っていないように見えるため件名も見る）・`master_tip_time`
  （前回のpushの時刻）。gate・check・status・`board dispatch list`・`board unpushed`がこれを使う。
  監査待ちは監査の記録（受領`reported`が最後の`audit_done`より新しい）だけから導く。
  手動タスクの前提は、そのタスクの記録の`## 前提（並行実行）`節（書き方を規約に置き、T1001.mdに
  節を足した）。判断待ちは台帳の未完了タスクの記録の未決の保留（`decisions.py`は表を読まない）。
- **表の書き込みで写しのキーを拒否**（`FORBIDDEN_KEYS`）: 担当の`queue`・`scale`・
  `scale_budget_min`・`task_log`・`note`・`audit`、最上位の`decisions`・`audited_unpushed`・
  `last_master_push(_at)`・`push_blocked`とC（規則・記録の文）、`limits.note`、振り出し待ちの
  `what`・`note`。`state`の語彙は司令塔の指示（稼働・停止指示・停止済み・強制停止）だけにし、
  監査の段階（監査待ち・監査済み）は書けない。`board dispatch push`はタスク番号を取る。
  `board unpushed add/done`・`decision add/answer`・`prereqs --add/--remove`・`queue migrate`は撤去。
- **現在のタスク**: 振り出し（`current_task=`）で着手時刻が入り、監査を通したときに外れる。
  報告の受領では閉じない（差し戻しの間は同じタスクのまま着手時刻を保つので、T1056の`task_log`
  から着手時刻を引き継ぐ仕組みは要らなくなった）。
- **`state`の扱い（Aへ入れた判断）**: 分類Bの「agentsのstateのうち稼働か否か」は、表の`state`を
  司令塔の指示（振り出した・止めた）に限ることで扱った。実際に動いているかは表に写さず、gateが
  作業ツリーの動きを足して数え、statusが指示と動きの食い違いに印を付ける（既存）。指示そのものは
  表にしか無い事実なので残す。
- **移行（1回）**: 実物の表へ16:22にかけた。変換前の写しは
  `<gitの共通ディレクトリ>/orchestration/board.20260923-162221.before-t1065.json`。移行の本体は
  使い捨てのスクリプト（リポジトリに入れない。外したキーは上の拒否の一覧と同じ）で、R6-licenseの
  手書きの`audit`は`audit_log`へ移し、`監査済み`の3担当は`停止済み`へ、`what`だけに番号のあった
  振り出し待ち（T1064）には`task`を付けた。
- C（規則・記録の文）のうち記録に無かったものは[T1049](T1049.md)「状態の表から移した記録」へ移した。
  規則の文（`resume_policy`・`decision_policy`・`check_rule`・`limits.note`）は規約に同じ内容が
  あるので移していない。

## 外す前の突き合わせ（今までに起きていた破綻）

移行の直前（16:22）に、表の写しを正本と突き合わせた（使い捨てのスクリプト。正本の読み出しは
上の導出を使う）。機械が出した30件のうち、題名の「省略しただけ」6件（T1044・T990・T1043・T1055・
T1059・T1016。写しの文が台帳の題名を縮めただけで中身は同じ）を除く**24件が食い違い**:

| 種類 | 件数 | 中身 |
|---|---|---|
| 振り出し待ちの題名 | 3 | T1057（台帳で題名が変わり写しが古い）・T1045（題名が別物）・T1014（写しは段階Dの中身、台帳はタスク全体） |
| 振り出し待ちのタスク番号 | 1 | T1064: `task`が無く`what`の文にだけ番号 |
| 担当の予算 | 3 | C-uistate・L-ci: 現在のタスクが無いのに予算240分が残る／A-scene: T1054は閉じて台帳に無いのに240分 |
| 手動タスクの前提 | 1 | T1001の前提（T1014・T1068）が表にしか無く、正本（T1001.md）に節が無かった |
| 判断の回答 | 10 | 表だけに回答があり、記録に「ユーザー決定」が無い（下の表のD-007・D-008・D-010・D-011・D-012・D-014・D-015・D-017・D-019・D-021） |
| 最終push | 2 | 表=b330fa81・15:44 / origin/masterの先端=e39b6736・16:10（今日、司令塔が更新し忘れて誤報を出したのと同じ種類） |
| 状態 | 3 | B-display・R6-license・D-small: `監査済み`（監査の記録から導くと監査待ちではない。語彙を指示だけにしたため停止済みへ） |
| 未push | 0 | T1056（213ebaad）は取り込みで中身が変わりpatch-idでは未pushに見えたが、件名で入ったと判定（導出の修正前は1件の誤検知） |

判断の回答10件は記録へ反映が要るため、司令塔のキューへ積んだ（`board todo`、16:22）。外す前の表の
判断の全件（反映の材料。記録へ書かれたらこの表は不要）:

| ID | 出所 | 問い | 回答 | 回答時刻 |
|---|---|---|---|---|
| D-001 | T1026 | 事故の点の配色 | A 今のまま青・黄土 | 2026-09-23T11:54 |
| D-002 | T1026 | 自販機（中身不明）を薄く出すか | A 他と同じ濃さ | 2026-09-23T11:54 |
| D-003 | T1034 | 本番で動いていない軸側レジストリ | A 検査ごと消す | 2026-09-23T11:54 |
| D-004 | T1027 | 畳んだチップに絞り込み中の印 | A 絞り込み中だけ印を出す | 2026-09-23T11:54 |
| D-005 | T1027 | 走行条件バーの現在値を画面に出すか | A: 右側を幅を揃えた縦一列にし、時計と速度計のボタン内に値を入れる（今のずれ3点も直す） | 2026-09-23T13:25 |
| D-006 | T1027 | 失敗をドットより強く知らせるか | 今のまま（トーストは出さない） | 2026-09-23T12:07 |
| D-007 | T958 | 区間テーブルの範囲 | ②（区間テーブルの作り替え）の効果を先に実測して検討材料にする。①（T959）は進める | 2026-09-23T12:18 |
| D-008 | T959 | 先に焼く主要エリアの定義 | 直前まで使われていた場所 | 2026-09-23T12:18 |
| D-009 | T1020 | ワーカー2本の起動を実機で見られない件 | 完了、本番の起動確認は別途起票 | 2026-09-23T12:18 |
| D-010 | T973 | 非商用データの実体（開発・本番DBの行とディスクのzip） | 削除する（開発・本番DBの行とzip） | 2026-09-23T12:07 |
| D-011 | T1023 | 完了条件の扱いと wind:"風" の直書き | T1023は完了で良い。風の直書きは消し、どの軸も内部名で出ない形へ一本化→T1054 | 2026-09-23T12:18 |
| D-012 | T1006 | 方針変更（unit/noteを削除し凡例はカタログのraw_value_unitを読む） | 認める | 2026-09-23T12:18 |
| D-013 | T1049 | 確認のフックの導入 | 導入する | 2026-09-23T12:07 |
| D-014 | T1057 | 作業ブランチでもCIを動かし重い検査を手元から外すか | 作業ブランチでもCIを動かす（publicは無料と公式で確認。private化したらルールを変える）→T1058 | 2026-09-23T13:12 |
| D-015 | T1062 | デプロイをCI成功後に起動し、切り替えの試験で本番が一時的に止まることを許容するか | 許容する（「本番一時的に止まっても問題ない」） | 2026-09-23T13:56 |
| D-016 | T1046 | 色トークンをTailwindのテーマへ取り込むか | 今の長い形のまま（採らない） | 2026-09-23T15:22 |
| D-017 | T1014 | 実データでE2Eを回す系統の置き場 | 作る。置き場は frontend/e2e-live/ と playwright.live.config.ts、CIには載せない | 2026-09-23T15:22 |
| D-018 | T1060 | 両方が使う宣言の新しい置き場と命名 | D-020によりT1001段階Dで決める（単独では決めない） | 2026-09-23T14:58 |
| D-019 | T1061 | 本番VMのGEOS・PROJを据え置くかPGDGの版へ上げるか | 上げる（調査の結果を見て。本番の一時停止は許容）→T1064 | 2026-09-23T15:08 |
| D-020 | T1060 | T1060をT1001の前提にするか段階Dに吸収させるか | T1001に吸収（段階Dで置き場と一緒に扱う。前提にしない） | 2026-09-23T14:58 |
| D-021 | T1054 | 0次フィルタの表示名、研究モードの区間詳細を論理名のみに | このままでよい | 2026-09-23T14:58 |
| D-022 | T1066 | 案2の観測のために、試験用のフックをどう入れるか | 未回答（記録に未決の保留があり、`decision list`に出る） | - |

## 検証

- **表のキーがAだけ**（移行後の実物をキーの出現数で列挙）: 最上位 `agents`・`check_interval_min`・
  `coordinator_queue`・`coordinator_session`・`last_check`・`limits`（`concurrent`のみ）・`manual`
  （タスク番号の並び）・`queue`・`run`。担当 `id`・`name`・`where`・`state`・`started`・
  `expected_min`・`current_task`・`task_first_started`・`reported`・`reported_sha`・`audit_done`・
  `audit_result`・`audit_log`。振り出し待ち `task`・`after`・`agent`・`priority`・`added`。
- **正本を1か所だけ変えると表示が変わる**（一時リポジトリでorigin/masterの参照を手で動かし、一時の
  表で。使い捨てのスクリプト）:

| 正本 | 変えたもの | 結果 |
|---|---|---|
| 台帳の規模札 | T9001を規模S→M（90分前に着手） | check「T9001の着手から通算90分 / 規模の予算60分」→ 超過なし、status の予算240分 |
| 台帳の題名 | T9001の題名を変える | status・dispatch list が新しい題名を出す |
| 記録の前提節 | T9003の前提T9002（未決の保留あり） | prereqs「残り1件 / T9002: 判断待ち」→ T9002を閉じると「開始してよい」 |
| 記録の保留 | T9002の保留に「ユーザー決定」を書く | decision list 1件→0件、prereqs の T9002 が「振り出し待ち」へ |
| git | 監査を通した横の枝のコミット | board unpushed 1件 → cherry-pickでmasterへ入れる（shaが変わる）と0件 |
| 監査の記録 | audit_result=通す | 現在のタスク・着手時刻が表から外れる |

- **写しの書き込みの拒否**: `board set X scale=S`・`queue=[...]`・`state=監査済み`、`board run decisions=[]`・
  `limits.note=x`、`board dispatch push "T9001: 題名を写す"`・`board dispatch push T9001 what=題名` の
  7件がすべて正本の場所を添えて終了コード1。
- **移行後の同じ答え**（実物の表のコピーへ移行をかけて、実物のorigin/masterで）: gate NG（稼働3本/
  上限3本、移行前と同じ）、check「異常なし」、dispatch list は各行に台帳の題名と前提の状態、
  unpushed 0件、decision list 17件（記録の保留）、status は現在のタスクの台帳の題名と予算
  （M-orchestrate: T1065 予算240分）。prereqs T1001 は「記録に前提の節が無い」——T1001.mdへ足した
  節がmasterへ入るまでの間だけで、入れば T1014・T1068 の状態を出す。
- `python -m ruff check scripts/orchestrate.py scripts/orchestration/`・`python scripts/review_checks.py docs`。

所要（並行実行）: 依頼 2026-09-23 16:08 → 完了 16:26（18分）、うち枠待ち0分・CI待ちはこのコミットの後、規模札M
