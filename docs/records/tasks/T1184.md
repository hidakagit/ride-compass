# T1184. 土地被覆の地図タイルを作るラスタの読み出しを rio-tiler へ置き換えるか、自前を続ける理由を書く

状態: 未完了（2026-09-27起票）

## 背景

起票案 proposal-std-landcover-rio-tiler（2026-09-27承認。標準の道具の棚卸し・T1152の担当から）。規模S（根拠: 変わると見込むファイルは landcover_raster.py・requirements.txt・test_landcover_raster.py の3本）

■ 背景（画面に出ない、backend内部の話）
infrastructure/landcover_raster.py の一部（約90行）は、読む範囲の計算・間引いた読み込み・最近傍での再投影・複数のラスタの重ね合わせを自前で持つ。rio-tiler（Reader.tile と mosaic_reader）が同じことをする。自前にした理由は書かれていない（最近傍にする理由だけが書かれている）。置き換えると依存が増え（morecantile 等）、本番の機械（ARM64）で入るかの確認が要る。

■ 決めてほしいこと
①置き換える（約−60行。本番の機械で入ることを先に確かめる）／②自前を続け、理由を docs/modules へ書く。推奨は②（削減が小さく、本番の依存が増える。rasterio は既に入っていて、今の約90行はその上の薄い処理）。

■ なぜ既存のタスクに入らないか
該当する未完了のタスクは無い。
