# T1096. 廃止したmigrationを現在形で案内している文言・コメントを、今の仕組み（ORMの宣言からcreate_tables()が作る）に合わせる

状態: 完了

## 背景

T997段階Bで管理API 2本の503の文言が「migration適用状況を確認」と案内していたのを直したが、同じ性質の文が残っている: axis_admin.pyの503「migration未適用の可能性があります」（テストが文字列migrationを含むかを確かめている）、axis_registry_service.pyの例外の文、deploy-backend.ymlの「migration適用より前に置く」コメント（デプロイにmigrationの段は無い）、存在しない0014_axis_definitions.sqlを名指しするテストのコメント、axis_definitions.pyのdocstring等。担当の調べで該当13ファイル、うち「積み上げ式のmigrationは持たない」のように正しい文も含むので1件ずつ判定する。規模の根拠: 変わると見込むファイル7以下。起票案 kihyoan-migration-wording として2026-09-25承認。

## 実施（2026-09-25、W-migr）

### 母集団

`docs/records`を除く全追跡ファイル（UTF-8で読めた824本）の全行を、正規表現`migrat|マイグレ|移行スクリプト|alembic`
（大小文字を区別しない）で走査した。**17ファイル・20行**。検算: `git grep -ilE 'migrat|マイグレ' -- ':!docs/records'`も
17ファイルで一致。migrationファイル名の形（`[0-9]{4}_[a-z_]+\.sql`）・適用関数名（`apply_migration`等）でも別に探し、
新しく当たったものは無い（`0014_axis_definitions.sql`の1件は上の20行に含まれる）。

### 分け方（20行）

- **今は無い仕組みを現在形で書いている → 直した（12行・10ファイル）**
  - 運用者に出る文言: `axis_admin.py`の503の`detail`（T997段階Bの2本と同じ「DB接続と、テーブルが作られているかを
    確認してください」へ）、`axis_registry_service.py`の0行の例外（軸の行はスキーマと一緒には作られず、管理API経由で
    しか入らない、へ。`axis-studio.md`「まっさらなDBに軸の行は入らない」と同じ事実）
  - docstring・コメント: `axis_admin.py`の`_guard_db_errors`、`axis_definitions.py`（構造の変更の置き場を名指す括弧書き。
    スキーマの作られ方は`axis-studio.md`が持つので消した）、`cache_identity.py`・`test_derived_data_revision_service.py`
    （Noneになるのは`derived_data_meta`の行が無いとき）、`test_axis_definition_repository.py`（存在しない
    `0014_axis_definitions.sql`を名指していた。`create_tables()`は`axis_registry_meta`の行を作らず、行が無いと版数は
    進まない、へ）
  - `deploy-backend.yml`: デプロイの段は ビルド→`import app.main`→ラスタ取得→旧コンテナ停止→新コンテナ起動 で、
    DBを変える段は無い。ラスタ取得を前に置く理由を「落ちても旧コンテナのまま動き続ける」へ。「唯一のステップ」は
    ビルドも外部に依存するので落とした
  - 文書: `.claude/commands/review.md`の層の例「DBのスキーマ（migrationとORM）」と記載漏れの起点「migrationのDDL」、
    `docs/architecture/directory-layout.md`「コードにもmigrationにも無い」
  - テスト: `test_axis_admin_routes.py`の`"migration" in detail`。このテストは503がガード（`_guard_db_errors`）から
    出たものかを見ており、同じルーターの別の503（「DB未接続のため分布を算出できません」）と区別できる
    「軸定義DBへのアクセスに失敗しました」を見るように直した
- **今も正しい → 残した（1行）**: `road_graph_repository.py`「積み上げ式のmigrationは持たない」
- **過去の出来事として書いている（経緯）→ 残した（2行）**: `test_tuning_overrides.py`（migration 0043）、
  `design-principles.md`（migration 0046で是正）。今は無い仕組みを現在形で案内してはいない。経緯の一掃は
  [T567](T567.md)の範囲
- **別の意味（本番データの移行一般）→ 残した（2行）**: `orchestration.md`の監査項目6と、それを出す
  `scripts/orchestration/core.py`
- **無関係 → 残した（3行）**: 台帳のT1096の行、`frontend/package-lock.json`の`gl-style-migrate`

直したあとの同じ grep は8行（上の「残した」と一致）。並行する担当のファイル（`tuning_service.py`・
`jma_tile_prewarm_service.py`・`test_road_graph_engine.py`）には該当が無かった。

### 範囲外で見つけたもの

- `axis_definition_repository.py`の`set_revision`は呼び出し元が無い（docstringの「一括投入」の経路が既に無い）。
  あわせて、`create_tables()`で作ったDBには`axis_registry_meta`の行が無く、`upsert`/`delete`の版数は進まないまま
  （`get_revision()`はNoneで、起動のたびにスコア行列のディスクキャッシュを無効化する安全側）。
- `axis_definitions.py`のモジュールdocstring「材料idごとの意味は`AXIS_DEFINITIONS`の各エントリのコメント参照」は、
  コードに軸のエントリが無いので指す先が無い。

### 検証

- `ruff check`（変更したPython 7本）→ All checks passed
- `mypy`（変更したapp 4本）→ no issues found in 4 source files
- `python scripts/review_checks.py docs` → 違反なし
- `python scripts/export_openapi.py` → `frontend/src/types/generated/`に差分なし（`detail`の文字列はスキーマに出ない）
- `pytest tests/test_axis_admin_routes.py tests/test_axis_definition_repository.py tests/test_derived_data_revision_service.py tests/test_axis_registry_service.py -q`
  → 119 passed

所要（並行実行）: 依頼 2026-09-25 08:23 → 完了 08:32（9分）、うち枠待ち1分・CI待ちは報告時点で未計測、規模札S、実績 +13/−14 → S
