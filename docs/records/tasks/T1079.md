# T1079. ページのピンチを止める規則を、E2Eの全状態の走査で確かめる（T1069の残り、T1078の後）

状態: 完了（2026-09-23、T1078で済んだ）

## 背景

T1069でページ全体のピンチ拡大を止める規則（globals.cssの@layer baseに * { touch-action: pan-x pan-y }）を入れ、Androidの実機で下部シートからのピンチでページが拡大しないこと・地図の上のピンチで地図が拡大することをユーザーが確かめた（2026-09-23）。iOS Safariは未確認のまま閉じた（ユーザー決定）。E2Eでの確認は、全状態の走査を効率化するT1078（ユーザーが別セッションで実施）の後に行う: やり方はT1069.md「E2Eでの確認（T1078の後）」節（どの状態でどう当てるか・セレクタ・開き方）。全状態の311件へ部品ごとのピンチを素朴に足した版（orch/P-pinch-verify 7780aa24）はCIのe2eが30分を超えても終わらなかったので、T1078の走り方の上で所要を見積もってから足す。map-runtime.spec.tsの「タッチ端末」の先頭のコメントは新しい規則と合わなくなっているので同時に直す。トリガー: T1078の完了。

## 閉じる（2026-09-23）

[T1078](T1078.md)（0d5aa205）が、全状態の走査（`e2e-scan`、モバイル）に「ピンチでページが拡大しない」を入れ、T1069の
`touch-action`の規則を外した入力で落ちることを確かめた（T1078.md「実装と検証」「壊した入力」）。このタスクの中身はそれで
済んだ。残っていた`map-runtime.spec.ts`の「タッチ端末」の先頭のコメントの書き直しは、[T1014](T1014.md)の残り（開発機での
e2e）と一緒に片付ける。
