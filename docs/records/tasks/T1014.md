# T1014. E2Eを「実機でしか出ないもの」の場として作り直す

状態: 未完了（2026-09-22起票）

## 背景

E2E（`frontend/e2e/`、Playwright）は「地図UI変更は必ずPlaywrightで実機確認する」を
自動回帰検知へ移す目的で置かれたが、**何をE2Eの対象にするかの定義が無い**まま
テストが足されてきた。その結果、単体テストと同じ主張をしているもの・共通の作り物が
判定対象そのものを決めてしまっているものが混ざっている。

普通のテストの定義・実施標準（`docs/conventions/testing.md`）は整ったが、**E2Eだけは
まだ定義が無い**。E2Eは「実データ・実ブラウザでしか見えない意図」を確かめる場であり、
単体テストと同じ扱いにできない。

## 観測（2026-09-22）

### 1. 手元では1回も走らない

`playwright.config.ts`の`webServer`が`npm run build && npm run start:standalone`を
180秒の枠で起動しようとするが、この開発機では本番ビルドだけで枠を超える
（`Timed out waiting 180000ms from config.webServer` で全テストがスキップされた）。
ビルドを先に済ませれば通るが、**手順としてそうなっていない**。

### 2. 「実機」が効いた実績は、どこに偏っているか

`grep -rn "実機" frontend/src docs/modules docs/conventions docs/architecture CLAUDE.md`
＝115件。**うち64件がCSS**。内容を読むと4種類に分かれ、E2Eの対象になるのは前3つだけ。

| 観点 | 原文の例 | 機械で判定できるか |
|---|---|---|
| 1. 収まらない・潜り込む・押せなくなる | 「チップ列がズームボタンの下に潜り込む」「タブバー配下に完全に隠れて押せなくなっていた」「スクロールバーが出てしまう」「スマホだと見切れてさわれない」 | できる |
| 2. ブラウザ/OSの既定に踏まれる | Androidの既定タップハイライト、ピンチズームがページ全体の拡大に化ける、`<form>`の暗黙送信、`passive: true`で`preventDefault`が効かない | できる（実ブラウザにしか無い） |
| 3. カスケードの勝ち負け | 「unlayeredのままTailwindの`p-2`が`padding:0`に潰される」「自前サイズを詳細度で踏みつぶす」「ビルド順に依存」 | できる（計算後の値を読める） |
| 4. 読みやすさ・色 | 「風の背景色が凡例と合ってない」「縁取りが見にくい」「コントラストが強すぎて見づらい」 | **できない（人が見るしかない）** |

**観点4はE2Eの対象にしない。** 自動化しようとすると画面写真の突き合わせになり、
`CLAUDE.md`が検知器へ禁じている「保存した過去の値と比べる」形になる。

### 3. 今あるテストをこの物差しで見る

| テスト | 判定 |
|---|---|
| `mobile.spec.ts`「ヘッダーが幅に収まり、メニューが押せる」 | 観点1。仕事をしている |
| `mobile.spec.ts`「レンズの凡例が、段階の細かい軸でも幅に収まる」 | 観点1。仕事をしている |
| `mobile.spec.ts`「ルート結果を見ている間は地図タップでピンが置かれない」 | 観点2に近い（実ブラウザのイベント） |
| `smoke.spec.ts`「地図レイヤーのON/OFF切替」 | `aria-pressed`の反転だけ。**単体テストと同じ主張** |
| `smoke.spec.ts`「ルート生成→候補一覧の表示」 | 同上 |
| 観点2・3を正面から見るもの | **0本** |

### 4. 共通フィクスチャが、判定対象そのものを決めている

`mobile.spec.ts`が「凡例の幅」を見るために、共通の`e2e/fixtures.ts`へ
`FINE_STEP_AXIS_LABEL = "段階の細かい軸"`という**そのテスト専用の軸**を足している。
観点1は中身の実寸がすべてなのに、共通の作り物がその実寸を決めている。
**1本の都合が共通側へ漏れ、他の全テストがそれを引き連れて動く。**

加えて、地図スタイルは空（`{version: 8, sources: {}, layers: []}`）・`tile_versions`は空。
そのため路面・点・事故・気象のシーングループは**ソースごと作られず**、
`features/map/scene/`の式は実ブラウザを一度も通っていない。
代役地図はstyle検証を持たないため、単体テストでも通らない
（`docs/modules/frontend/static-map-layers.md`「MapLibreの式を組むときの前提」）。

## やること

1. **定義を`docs/conventions/testing.md`へ置く**（パターン4の書き換え）。
   - E2Eの対象は「実機でしか出ない」かつ「機械で判定できる」もの＝観点1〜3
   - 観点4は対象にしない（人が見る。`frontend-design-system.md`「実機確認の方法」の側）
   - **判定対象になる値を共通フィクスチャへ置かない**。共通に置いてよいのは「目的の画面まで
     進める導線」だけ（`openMobileApp`・`generateRoutes`等）。各テストは自分が測る値を
     自分で用意する
2. **系統を分ける**。決定的な回帰検知（モック・CI）と、実データで見る系統（実backend・
   実タイル）は目的が違うので同じ場所に混ぜない。実データ系はCIの完了条件に入れない
   （所要と外部依存が読めないため）。置き場と走らせ方をこのタスクで決める。
3. **既存6本を物差しに掛け直す**。単体と同じ主張のものは消すか観点1〜3の形へ書き直す。
4. **手元で走る形にする**（`webServer`の枠、またはビルド済み前提の手順）。
5. **観点2・3を1本ずつ足す**。足したら、わざと壊して落ちることを確認してから完了扱いにする。
6. `features/map/scene/`の式が実ブラウザのstyle検証を通る状態を作る
   （`tile_versions`を持つ系統を用意し、全レイヤーONで載ることを見る）。

## 完了条件

- `testing.md`に、E2Eの対象と対象外が観点として書かれている
- 既存の各テストが、その観点のどれを見ているかを言える（言えないものは消す）
- 共通フィクスチャに「判定対象になる値」が1つも無い
- 観点2・3のテストが、わざと壊した入力で落ちることを確認済み
- 手元での走らせ方が`testing.md`に書かれ、実際に通っている

## 関連

- この定義が無いまま進めないため、[T1001](T1001.md)段階Aの実機確認は本タスク待ちにする
  （本番ビルド自体は通っている）。

## 段階の切り方（2026-09-23）

規模Lに届く見立て（計5.5〜8時間、未計測の見積もり）のため、段階ごとに別々に振り出す。
Aの定義がB以降の物差しになるので、A→Bの順。C・DはB以降なら並行してよい。

| 段階 | 中身 | 見込み |
|---|---|---|
| A 定義と走らせ方 | 「やること」1・4（`testing.md`パターン4の書き換え、手元で走る形） | 60〜90分 |
| B 既存の掛け直し | 「やること」3と、共通フィクスチャから判定値を外す（下の表） | 90〜120分 |
| C 観点2・3を1本ずつ | 「やること」5。観点3の候補は、`globals.css`の`@layer base`のリセットにTailwindのユーティリティが勝つこと（Tailwindの余白ユーティリティを持つ要素全部を母集団にし、計算後の余白が0でないことを見る。壊し方: リセットを`@layer`の外へ出す）。観点2は実例が未確定（ピンチの`touch-action`はヘッドレスで再現できるか未確認、`<form>`の暗黙送信は`AxisComposer`の警告コメントだけで現状の実例が無い）——段階Cの冒頭で試して決める | 90〜150分 |
| D scene式のstyle検証 | 「やること」6。テスト自身が`tile_versions`を持つカタログを返し、全レイヤーをON（`mapDisplay.layerIds`とカタログの軸から導く）にして、デバッグログの`map:error`のうちsourceIdの無いもの（＝スタイル検証の失敗）が0件であることを見る。壊し方: sceneに不正な式を1つ入れる | 90〜120分 |

段階Bでの既存テストの扱い（2026-09-23時点の10本。`grep -n '^test(' frontend/e2e/*.ts`で列挙）:

| テスト | 扱い | 根拠 |
|---|---|---|
| smoke「ルート生成→候補一覧の表示」 | 消す | 同じ主張の単体テストがある（`page.test.tsx`「候補タブに順位番号を表示し…」） |
| smoke「地図レイヤーのON/OFF切替」 | 消す | 同上（`MapOverlayControls.test.tsx`の`aria-pressed`、`page.test.tsx`「レイヤーの同時ON/OFF」） |
| mobile「ルート生成→「ルート結果」シートに候補が並ぶ」 | 消す | 同上 |
| comparison「比較タブを開いたまま生成すると、新しい候補が表示される」 | 単体テストへ移してから消す | 同じ主張の単体テストが見つからない（消すと守りが無くなる） |
| comparison「…2回目以降は横スクロール無しで読める」 | 幅だけを見る形へ書き直す（観点1） | 行見出しの長さを決める軸カタログ・候補を、テスト自身が用意する。前半の案内文は`ComparisonPanel.test.tsx`が見ている |
| mobile「レンズの凡例が、段階の細かい軸でも幅に収まる」 | 残す（観点1） | 段階の細かい軸（`FINE_STEP_AXIS_LABEL`）を共通フィクスチャからテスト側へ移す |
| mobile「ヘッダーが幅に収まり、メニューが押せる」 | 残す（観点1） | — |
| smoke「タイル取得が終わらなくても地図の覆いは外れる」・mobile「ルート結果を見ている間は地図タップでピンが置かれない」 | `map-runtime.spec.ts`へ移す（観点2） | MapLibreの`idle`・実クリックが地図を通って届くこと |
| map-runtime「地図が描ける」 | そのまま（観点2） | [T1004](T1004.md) |

共通フィクスチャの判定値（段階Bで外す）: 段階の細かい軸、候補の距離（「20.3km」を3本が判定に使う）、
比較表の行見出しを決める軸カタログの中身。

## 段階A（2026-09-23）

- `docs/conventions/testing.md`パターン4を書き換えた。対象は観点1〜3（表で例と判定の手段）、
  対象外は「読みやすさ・色」（人が見る）と「DOMの状態・ロジックだけの主張」（vitest）。
  共通フィクスチャには導線だけを置き、判定値は各テストが`page.route`の上書きで用意する。
  走らせ方（下記）と、旧パターン4の書き方の項目（導線・実寸・ロケータ・画素）を1節にまとめた。
- `npm run test:e2e`を`npm run build && playwright test`にした。`webServer`は起動だけ
  （`npm run start:standalone`）、E2E専用ポート（3100。`env: { PORT }`で
  `.next/standalone/server.js`へ渡す——standaloneのserver.jsは`process.env.PORT`を読む、
  `node_modules/next/dist/build/utils.js`で確認）、`reuseExistingServer: false`。
  ビルドが`webServer`の起動待ち（旧180秒）の外に出たので、開発機でビルドが枠を超えて
  全件スキップになる経路（観測1）は無くなった。CIのe2eジョブは同じ`npm run test:e2e`を
  使うので手順の変更は無い。
- 撤去したコメントの行き先（`playwright.config.ts`）: 「`next dev`は初回コンパイルの待ち時間が
  読めないので使わない」「Chromium1種で足りる（ブラウザ間の差は目的でない）」はパターン4へ
  移した。workers=1の理由は現在形の一文にしてconfigに残した。T252等の経緯は落とした。
- 実データで見る系統（「やること」2の後半）の置き場と走らせ方は、新しい種類の成果物なので
  ユーザー判断（D-017）。パターン4には「未定（D-017）」とだけ書いた。

### 段階Aの検証

- 手元: `prettier --check`（変更したts/json）・`python scripts/review_checks.py docs`→違反なし・
  `next typegen && tsc --noEmit`（heavyの枠）→通過。
- 手元でのplaywrightは回していない（司令塔の指示。判定はCIのe2eジョブ＝Linuxで行う）。
  **開発機（Windows）で`npm run test:e2e`が通ることは未確認**——完了条件「手元での走らせ方が
  書かれ、実際に通っている」のうち「実際に通っている」は、段階B以降のどこかで開発機で
  1回通して埋める。
- CIの結果はこのコミットのrunが持つ（報告に添える）。

## 段階B（2026-09-23）

段階Aの表どおりに既存10本を掛け直した。E2Eは10本→7本（`smoke.spec.ts`は丸ごと撤去）、
単体テストは2本増えた。

### 消した・移したテストと、守りが残っている場所

行番号は段階Bのコミット時点の`frontend/src/`配下。

| E2Eのテスト（消した） | 主張 | 同じ主張の単体テスト（テスト名：アサーション行） |
|---|---|---|
| smoke「ルート生成→候補一覧の表示」 | 生成すると候補がタブで並ぶ | `app/page.test.tsx:1030`「改善計画T531: 候補タブに順位番号を表示し、同じ方位の候補を区別できる」：1045-1046行 `getByRole("tab", { name: /^1 30\.1km/ })`・`/^2 31\.4km/` が在る |
| smoke「地図レイヤーのON/OFF切替」 | チップを押すとON/OFFが反転し、`aria-pressed`に出る | `components/MapOverlayControls/MapOverlayControls.test.tsx:52`「チップのクリックでonToggleがレイヤーIDと現在値の反転で呼ばれる」：60・63行 `toHaveBeenCalledWith("elevation", true)`・`("surface", false)`／同`:43`「各チップがON状態をaria-pressedで反映する」：47-49行／`app/page.test.tsx:290`「グループをまたいでも同時にONにできる（道路・スポット）」：294行で`toggle:highway`を押し299行`after.get("highway")`が`true`（押した結果がページの状態へ届く）。グループ見出しの開閉は`MapOverlayControls.test.tsx:453`「別のグループを開くと、先に開いていたグループは畳まれる」 |
| mobile「ルート生成→「ルート結果」シートに候補が並ぶ」 | モバイルでも生成すると結果シートに候補が並ぶ | **同じ主張の単体テストは無かった**（モバイルで候補を持つものは0件時の案内だけ）ので移した：`app/page.test.tsx:1854`「モバイルでも、生成すると「ルート結果」に候補のタブが並ぶ」 |
| comparison「比較タブを開いたまま生成すると、新しい候補が表示される」 | 生成すると比較タブから候補タブへ戻る | 同じ主張の単体テストが無かったので移した：`app/page.test.tsx:1823`「比較タブを開いたまま生成すると、新しい候補のタブが選ばれる」（新しい候補のタブが`aria-selected="true"`・比較タブが`"false"`） |
| comparison前半「相手がいない間は案内を出し」 | 1件だけのとき案内文を出す | `components/ComparisonPanel/ComparisonPanel.test.tsx:95`「スロットが1件のときは、もう1回生成すれば比べられることを案内する」：105行 `getByText(/もう1回生成すると/)` |

移した単体テストのうち比較タブの1本は、壊した入力で落ちることを見た: `page.tsx`の
`handleGenerate`から`setComparisonTabActive(false)`を外して
`vitest run src/app/page.test.tsx -t "比較タブを開いたまま生成すると"`→ 1 failed
（`aria-selected`が`"false"`のまま）。戻して3本（移した2本と根拠の1本）が通った。

### 残した・書き直したE2E（観点）

| テスト | 観点 | 変えたこと |
|---|---|---|
| map-runtime「地図が描ける」 | 2 | なし |
| map-runtime「タイル取得が終わらなくても地図の覆いは外れる」 | 2（MapLibreの`idle`） | smokeから移した |
| map-runtime「モバイル: ルート結果を見ている間は地図タップでピンが置かれない」 | 2（canvasへの実クリックがMapLibreのclickとして届く） | mobileから移した |
| mobile「ヘッダーが幅に収まり、メニューが押せる」 | 1 | 失敗応答を`openMobileApp`の`routes`で先に与える（旧: 開いてから上書きして再読み込み） |
| mobile「レンズの凡例が、段階の細かい軸でも幅に収まる」 | 1 | 段階の細かい軸をテスト自身が`page.route`で返す。1行が折り返し無しではセルに収まらない長さの値へ変えた（下記） |
| comparison「比較表は、長い行見出しがあっても横スクロール無しで値の列まで読める」 | 1 | 長い行見出し（区切りの無い英字を含む）の軸・その軸の難易度を持つ候補・その軸へ重みを置いた条件を、テスト自身が返す。前半の案内文の確認は単体側にあるので外した |

### 共通フィクスチャ（`e2e/fixtures.ts`）

- 外した判定値: 段階の細かい軸（`FINE_STEP_AXIS_LABEL`とその軸）。候補の距離（「20.3km」）は
  共通側に残るが、これを判定に使うテストは無くなった（生成を完了させる導線としてだけ使う）。
- 足した導線: `axisCatalogFixture(axes)`（カタログ応答の外枠）、`openMobileApp`の`routes`
  （既定のモックの後・goto前に、テストが自分の応答を登録する口）。

### 幅を見る2本を、壊した入力で落とした（CIのe2e＝Linux）

壊した入力（masterへは入れない）: 比較表の`table-layout: fixed`・`overflow-wrap: anywhere`・
行見出しの`width: 6rem`を外し、凡例の行の`overflow-wrap: anywhere`を`white-space: nowrap`へ
戻した。

- 1回目（[run 35822687998](https://github.com/hidakagit/ride-compass/actions/runs/35822687998)）:
  **1 failed / 5 passed**。比較表は落ちた（表の右端 499.125px > 390px）が、**凡例は通って
  しまった**。凡例の軸は旧フィクスチャと同じ値（しきい値10〜80・「箇所/km」）で、1行
  （「10〜20箇所/km」）が折り返さなくてもセルに収まる——**段階B以前から、このテストは
  「凡例がはみ出す」を捕まえられない入力で回っていた**。2回目で小数3桁のしきい値へ伸ばしても
  通った（[run 35822991979](https://github.com/hidakagit/ride-compass/actions/runs/35822991979)）。
- 凡例の軸に体感ラベル（`display_band_labels_override`。1行が「とても少ない（0.125〜0.375箇所/km）」の
  形になる）を持たせた3回目
  （[run 35823264533](https://github.com/hidakagit/ride-compass/actions/runs/35823264533)）:
  **2 failed / 4 passed**。凡例の全行がセルから45〜81pxはみ出し、比較表も落ちた。
  残りの4本は緑。本コミットの凡例のテストはこの3回目の値を使う。
