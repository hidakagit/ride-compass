# T1014. E2Eを「実機でしか出ないもの」の場として作り直す

状態: 未完了（2026-09-22起票）

## 背景

E2E（`frontend/e2e/`、Playwright）は「地図UI変更は必ずPlaywrightで実機確認する」を
自動回帰検知へ移す目的で置かれたが、**何をE2Eの対象にするかの定義が無い**まま
テストが足されてきた。その結果、単体テストと同じ主張をしているもの・共通の作り物が
判定対象そのものを決めてしまっているものが混ざっている。

普通のテストの定義・実施標準（`docs/conventions/testing.md`）は整ったが、**E2Eだけは
まだ定義が無い**。E2Eは「実データ・実ブラウザでしか見えない意図」を確かめる場であり、
単体テストと同じ扱いにできない。

## 観測（2026-09-22）

### 1. 手元では1回も走らない

`playwright.config.ts`の`webServer`が`npm run build && npm run start:standalone`を
180秒の枠で起動しようとするが、この開発機では本番ビルドだけで枠を超える
（`Timed out waiting 180000ms from config.webServer` で全テストがスキップされた）。
ビルドを先に済ませれば通るが、**手順としてそうなっていない**。

### 2. 「実機」が効いた実績は、どこに偏っているか

`grep -rn "実機" frontend/src docs/modules docs/conventions docs/architecture CLAUDE.md`
＝115件。**うち64件がCSS**。内容を読むと4種類に分かれ、E2Eの対象になるのは前3つだけ。

| 観点 | 原文の例 | 機械で判定できるか |
|---|---|---|
| 1. 収まらない・潜り込む・押せなくなる | 「チップ列がズームボタンの下に潜り込む」「タブバー配下に完全に隠れて押せなくなっていた」「スクロールバーが出てしまう」「スマホだと見切れてさわれない」 | できる |
| 2. ブラウザ/OSの既定に踏まれる | Androidの既定タップハイライト、ピンチズームがページ全体の拡大に化ける、`<form>`の暗黙送信、`passive: true`で`preventDefault`が効かない | できる（実ブラウザにしか無い） |
| 3. カスケードの勝ち負け | 「unlayeredのままTailwindの`p-2`が`padding:0`に潰される」「自前サイズを詳細度で踏みつぶす」「ビルド順に依存」 | できる（計算後の値を読める） |
| 4. 読みやすさ・色 | 「風の背景色が凡例と合ってない」「縁取りが見にくい」「コントラストが強すぎて見づらい」 | **できない（人が見るしかない）** |

**観点4はE2Eの対象にしない。** 自動化しようとすると画面写真の突き合わせになり、
`CLAUDE.md`が検知器へ禁じている「保存した過去の値と比べる」形になる。

### 3. 今あるテストをこの物差しで見る

| テスト | 判定 |
|---|---|
| `mobile.spec.ts`「ヘッダーが幅に収まり、メニューが押せる」 | 観点1。仕事をしている |
| `mobile.spec.ts`「レンズの凡例が、段階の細かい軸でも幅に収まる」 | 観点1。仕事をしている |
| `mobile.spec.ts`「ルート結果を見ている間は地図タップでピンが置かれない」 | 観点2に近い（実ブラウザのイベント） |
| `smoke.spec.ts`「地図レイヤーのON/OFF切替」 | `aria-pressed`の反転だけ。**単体テストと同じ主張** |
| `smoke.spec.ts`「ルート生成→候補一覧の表示」 | 同上 |
| 観点2・3を正面から見るもの | **0本** |

### 4. 共通フィクスチャが、判定対象そのものを決めている

`mobile.spec.ts`が「凡例の幅」を見るために、共通の`e2e/fixtures.ts`へ
`FINE_STEP_AXIS_LABEL = "段階の細かい軸"`という**そのテスト専用の軸**を足している。
観点1は中身の実寸がすべてなのに、共通の作り物がその実寸を決めている。
**1本の都合が共通側へ漏れ、他の全テストがそれを引き連れて動く。**

加えて、地図スタイルは空（`{version: 8, sources: {}, layers: []}`）・`tile_versions`は空。
そのため路面・点・事故・気象のシーングループは**ソースごと作られず**、
`features/map/scene/`の式は実ブラウザを一度も通っていない。
代役地図はstyle検証を持たないため、単体テストでも通らない
（`docs/modules/frontend/static-map-layers.md`「MapLibreの式を組むときの前提」）。

## やること

1. **定義を`docs/conventions/testing.md`へ置く**（パターン4の書き換え）。
   - E2Eの対象は「実機でしか出ない」かつ「機械で判定できる」もの＝観点1〜3
   - 観点4は対象にしない（人が見る。`frontend-design-system.md`「実機確認の方法」の側）
   - **判定対象になる値を共通フィクスチャへ置かない**。共通に置いてよいのは「目的の画面まで
     進める導線」だけ（`openMobileApp`・`generateRoutes`等）。各テストは自分が測る値を
     自分で用意する
2. **系統を分ける**。決定的な回帰検知（モック・CI）と、実データで見る系統（実backend・
   実タイル）は目的が違うので同じ場所に混ぜない。実データ系はCIの完了条件に入れない
   （所要と外部依存が読めないため）。置き場と走らせ方をこのタスクで決める。
3. **既存6本を物差しに掛け直す**。単体と同じ主張のものは消すか観点1〜3の形へ書き直す。
4. **手元で走る形にする**（`webServer`の枠、またはビルド済み前提の手順）。
5. **観点2・3を1本ずつ足す**。足したら、わざと壊して落ちることを確認してから完了扱いにする。
6. `features/map/scene/`の式が実ブラウザのstyle検証を通る状態を作る
   （`tile_versions`を持つ系統を用意し、全レイヤーONで載ることを見る）。

## 完了条件

- `testing.md`に、E2Eの対象と対象外が観点として書かれている
- 既存の各テストが、その観点のどれを見ているかを言える（言えないものは消す）
- 共通フィクスチャに「判定対象になる値」が1つも無い
- 観点2・3のテストが、わざと壊した入力で落ちることを確認済み
- 手元での走らせ方が`testing.md`に書かれ、実際に通っている

## 関連

- この定義が無いまま進めないため、[T1001](T1001.md)段階Aの実機確認は本タスク待ちにする
  （本番ビルド自体は通っている）。

## 段階の切り方（2026-09-23）

規模Lに届く見立て（計5.5〜8時間、未計測の見積もり）のため、段階ごとに別々に振り出す。
Aの定義がB以降の物差しになるので、A→Bの順。C・DはB以降なら並行してよい。

| 段階 | 中身 | 見込み |
|---|---|---|
| A 定義と走らせ方 | 「やること」1・4（`testing.md`パターン4の書き換え、手元で走る形） | 60〜90分 |
| B 既存の掛け直し | 「やること」3と、共通フィクスチャから判定値を外す（下の表） | 90〜120分 |
| C 観点2・3を1本ずつ | 「やること」5。観点3の候補は、`globals.css`の`@layer base`のリセットにTailwindのユーティリティが勝つこと（Tailwindの余白ユーティリティを持つ要素全部を母集団にし、計算後の余白が0でないことを見る。壊し方: リセットを`@layer`の外へ出す）。観点2は実例が未確定（ピンチの`touch-action`はヘッドレスで再現できるか未確認、`<form>`の暗黙送信は`AxisComposer`の警告コメントだけで現状の実例が無い）——段階Cの冒頭で試して決める | 90〜150分 |
| D scene式のstyle検証 | 「やること」6。テスト自身が`tile_versions`を持つカタログを返し、全レイヤーをON（`mapDisplay.layerIds`とカタログの軸から導く）にして、デバッグログの`map:error`のうちsourceIdの無いもの（＝スタイル検証の失敗）が0件であることを見る。壊し方: sceneに不正な式を1つ入れる | 90〜120分 |

段階Bでの既存テストの扱い（2026-09-23時点の10本。`grep -n '^test(' frontend/e2e/*.ts`で列挙）:

| テスト | 扱い | 根拠 |
|---|---|---|
| smoke「ルート生成→候補一覧の表示」 | 消す | 同じ主張の単体テストがある（`page.test.tsx`「候補タブに順位番号を表示し…」） |
| smoke「地図レイヤーのON/OFF切替」 | 消す | 同上（`MapOverlayControls.test.tsx`の`aria-pressed`、`page.test.tsx`「レイヤーの同時ON/OFF」） |
| mobile「ルート生成→「ルート結果」シートに候補が並ぶ」 | 消す | 同上 |
| comparison「比較タブを開いたまま生成すると、新しい候補が表示される」 | 単体テストへ移してから消す | 同じ主張の単体テストが見つからない（消すと守りが無くなる） |
| comparison「…2回目以降は横スクロール無しで読める」 | 幅だけを見る形へ書き直す（観点1） | 行見出しの長さを決める軸カタログ・候補を、テスト自身が用意する。前半の案内文は`ComparisonPanel.test.tsx`が見ている |
| mobile「レンズの凡例が、段階の細かい軸でも幅に収まる」 | 残す（観点1） | 段階の細かい軸（`FINE_STEP_AXIS_LABEL`）を共通フィクスチャからテスト側へ移す |
| mobile「ヘッダーが幅に収まり、メニューが押せる」 | 残す（観点1） | — |
| smoke「タイル取得が終わらなくても地図の覆いは外れる」・mobile「ルート結果を見ている間は地図タップでピンが置かれない」 | `map-runtime.spec.ts`へ移す（観点2） | MapLibreの`idle`・実クリックが地図を通って届くこと |
| map-runtime「地図が描ける」 | そのまま（観点2） | [T1004](T1004.md) |

共通フィクスチャの判定値（段階Bで外す）: 段階の細かい軸、候補の距離（「20.3km」を3本が判定に使う）、
比較表の行見出しを決める軸カタログの中身。

## 段階A（2026-09-23）

- `docs/conventions/testing.md`パターン4を書き換えた。対象は観点1〜3（表で例と判定の手段）、
  対象外は「読みやすさ・色」（人が見る）と「DOMの状態・ロジックだけの主張」（vitest）。
  共通フィクスチャには導線だけを置き、判定値は各テストが`page.route`の上書きで用意する。
  走らせ方（下記）と、旧パターン4の書き方の項目（導線・実寸・ロケータ・画素）を1節にまとめた。
- `npm run test:e2e`を`npm run build && playwright test`にした。`webServer`は起動だけ
  （`npm run start:standalone`）、E2E専用ポート（3100。`env: { PORT }`で
  `.next/standalone/server.js`へ渡す——standaloneのserver.jsは`process.env.PORT`を読む、
  `node_modules/next/dist/build/utils.js`で確認）、`reuseExistingServer: false`。
  ビルドが`webServer`の起動待ち（旧180秒）の外に出たので、開発機でビルドが枠を超えて
  全件スキップになる経路（観測1）は無くなった。CIのe2eジョブは同じ`npm run test:e2e`を
  使うので手順の変更は無い。
- 撤去したコメントの行き先（`playwright.config.ts`）: 「`next dev`は初回コンパイルの待ち時間が
  読めないので使わない」「Chromium1種で足りる（ブラウザ間の差は目的でない）」はパターン4へ
  移した。workers=1の理由は現在形の一文にしてconfigに残した。T252等の経緯は落とした。
- 実データで見る系統（「やること」2の後半）の置き場と走らせ方は、新しい種類の成果物なので
  ユーザー判断（D-017）。パターン4には「未定（D-017）」とだけ書いた。

### 段階Aの検証

- 手元: `prettier --check`（変更したts/json）・`python scripts/review_checks.py docs`→違反なし・
  `next typegen && tsc --noEmit`（heavyの枠）→通過。
- 手元でのplaywrightは回していない（司令塔の指示。判定はCIのe2eジョブ＝Linuxで行う）。
  **開発機（Windows）で`npm run test:e2e`が通ることは未確認**——完了条件「手元での走らせ方が
  書かれ、実際に通っている」のうち「実際に通っている」は、段階B以降のどこかで開発機で
  1回通して埋める。
- CIの結果はこのコミットのrunが持つ（報告に添える）。

## 段階B（2026-09-23）

段階Aの表どおりに既存10本を掛け直した。E2Eは10本→7本（`smoke.spec.ts`は丸ごと撤去）、
単体テストは2本増えた。

### 消した・移したテストと、守りが残っている場所

行番号は段階Bのコミット時点の`frontend/src/`配下。

| E2Eのテスト（消した） | 主張 | 同じ主張の単体テスト（テスト名：アサーション行） |
|---|---|---|
| smoke「ルート生成→候補一覧の表示」 | 生成すると候補がタブで並ぶ | `app/page.test.tsx:1030`「改善計画T531: 候補タブに順位番号を表示し、同じ方位の候補を区別できる」：1045-1046行 `getByRole("tab", { name: /^1 30\.1km/ })`・`/^2 31\.4km/` が在る |
| smoke「地図レイヤーのON/OFF切替」 | チップを押すとON/OFFが反転し、`aria-pressed`に出る | `components/MapOverlayControls/MapOverlayControls.test.tsx:52`「チップのクリックでonToggleがレイヤーIDと現在値の反転で呼ばれる」：60・63行 `toHaveBeenCalledWith("elevation", true)`・`("surface", false)`／同`:43`「各チップがON状態をaria-pressedで反映する」：47-49行／`app/page.test.tsx:290`「グループをまたいでも同時にONにできる（道路・スポット）」：294行で`toggle:highway`を押し299行`after.get("highway")`が`true`（押した結果がページの状態へ届く）。グループ見出しの開閉は`MapOverlayControls.test.tsx:453`「別のグループを開くと、先に開いていたグループは畳まれる」 |
| mobile「ルート生成→「ルート結果」シートに候補が並ぶ」 | モバイルでも生成すると結果シートに候補が並ぶ | **同じ主張の単体テストは無かった**（モバイルで候補を持つものは0件時の案内だけ）ので移した：`app/page.test.tsx:1854`「モバイルでも、生成すると「ルート結果」に候補のタブが並ぶ」 |
| comparison「比較タブを開いたまま生成すると、新しい候補が表示される」 | 生成すると比較タブから候補タブへ戻る | 同じ主張の単体テストが無かったので移した：`app/page.test.tsx:1823`「比較タブを開いたまま生成すると、新しい候補のタブが選ばれる」（新しい候補のタブが`aria-selected="true"`・比較タブが`"false"`） |
| comparison前半「相手がいない間は案内を出し」 | 1件だけのとき案内文を出す | `components/ComparisonPanel/ComparisonPanel.test.tsx:95`「スロットが1件のときは、もう1回生成すれば比べられることを案内する」：105行 `getByText(/もう1回生成すると/)` |

移した単体テストのうち比較タブの1本は、壊した入力で落ちることを見た: `page.tsx`の
`handleGenerate`から`setComparisonTabActive(false)`を外して
`vitest run src/app/page.test.tsx -t "比較タブを開いたまま生成すると"`→ 1 failed
（`aria-selected`が`"false"`のまま）。戻して3本（移した2本と根拠の1本）が通った。

### 残した・書き直したE2E（観点）

| テスト | 観点 | 変えたこと |
|---|---|---|
| map-runtime「地図が描ける」 | 2 | なし |
| map-runtime「タイル取得が終わらなくても地図の覆いは外れる」 | 2（MapLibreの`idle`） | smokeから移した |
| map-runtime「モバイル: ルート結果を見ている間は地図タップでピンが置かれない」 | 2（canvasへの実クリックがMapLibreのclickとして届く） | mobileから移した |
| mobile「ヘッダーが幅に収まり、メニューが押せる」 | 1 | 失敗応答を`openMobileApp`の`routes`で先に与える（旧: 開いてから上書きして再読み込み） |
| mobile「レンズの凡例が、段階の細かい軸でも幅に収まる」 | 1 | 段階の細かい軸をテスト自身が`page.route`で返す。1行が折り返し無しではセルに収まらない長さの値へ変えた（下記） |
| comparison「比較表は、長い行見出しがあっても横スクロール無しで値の列まで読める」 | 1 | 長い行見出し（区切りの無い英字を含む）の軸・その軸の難易度を持つ候補・その軸へ重みを置いた条件を、テスト自身が返す。前半の案内文の確認は単体側にあるので外した |

### 共通フィクスチャ（`e2e/fixtures.ts`）

- 外した判定値: 段階の細かい軸（`FINE_STEP_AXIS_LABEL`とその軸）。候補の距離（「20.3km」）は
  共通側に残るが、これを判定に使うテストは無くなった（生成を完了させる導線としてだけ使う）。
- 足した導線: `axisCatalogFixture(axes)`（カタログ応答の外枠）、`openMobileApp`の`routes`
  （既定のモックの後・goto前に、テストが自分の応答を登録する口）。

### 幅を見る2本を、壊した入力で落とした（CIのe2e＝Linux）

壊した入力（masterへは入れない）: 比較表の`table-layout: fixed`・`overflow-wrap: anywhere`・
行見出しの`width: 6rem`を外し、凡例の行の`overflow-wrap: anywhere`を`white-space: nowrap`へ
戻した。

- 1回目（[run 35822687998](https://github.com/hidakagit/ride-compass/actions/runs/35822687998)）:
  **1 failed / 5 passed**。比較表は落ちた（表の右端 499.125px > 390px）が、**凡例は通って
  しまった**。凡例の軸は旧フィクスチャと同じ値（しきい値10〜80・「箇所/km」）で、1行
  （「10〜20箇所/km」）が折り返さなくてもセルに収まる——**段階B以前から、このテストは
  「凡例がはみ出す」を捕まえられない入力で回っていた**。2回目で小数3桁のしきい値へ伸ばしても
  通った（[run 35822991979](https://github.com/hidakagit/ride-compass/actions/runs/35822991979)）。
- 凡例の軸に体感ラベル（`display_band_labels_override`。1行が「とても少ない（0.125〜0.375箇所/km）」の
  形になる）を持たせた3回目
  （[run 35823264533](https://github.com/hidakagit/ride-compass/actions/runs/35823264533)）:
  **2 failed / 4 passed**。凡例の全行がセルから45〜81pxはみ出し、比較表も落ちた。
  残りの4本は緑。本コミットの凡例のテストはこの3回目の値を使う。

## 段階C（2026-09-23）

### 観点3: Tailwindの余白ユーティリティがリセットに潰されない（`e2e/cascade.spec.ts`）— 入れた

- 母集団: デスクトップの初期画面に在る、Tailwindの余白ユーティリティ（`p-2`・`px-[0.9rem]`・
  `-mt-1`の形。`sm:`等の変種付きは除く）を持つ要素全部。期待値は、同じ親に置いた試し要素へ
  そのユーティリティの値（`calc(var(--spacing) * N)`か角括弧の中身）を`!important`で与えて
  ブラウザに計算させ、要素の計算後の余白（ユーティリティが効く辺ごと）と突き合わせる。
- 壊した入力: `globals.css`の`* { box-sizing; padding: 0; margin: 0 }`を`@layer base`の外
  （ファイル末尾）へ出した。[run 35826718509](https://github.com/hidakagit/ride-compass/actions/runs/35826718509)で
  このテストだけが落ちた（`px-2`の8pxと`py-1`の4pxが0pxに潰れた、ボタン2個×4辺）。
  壊していない側は[run 35825965232](https://github.com/hidakagit/ride-compass/actions/runs/35825965232)・
  [run 35827419550](https://github.com/hidakagit/ride-compass/actions/runs/35827419550)で緑。

### 観点2: 地図の上の部品から始めたピンチがページの拡大に化けない — 書いて検知を確かめたが、入れていない

**ヘッドレスで再現できるか**（CIのe2e＝Linuxで実験。実験のspecはmasterへ入れていない）:

| 送り方 | 結果 |
|---|---|
| `Input.synthesizePinchGesture`（CDP） | 2本の指が起点の周りに置かれる。地図のcanvasの上なら地図がズームする（`zoomend`が出る）が、部品の上から始めても、`touch-action: auto`を全要素へ強制しても**ページは拡大しない**（`visualViewport.scale`が1のまま）。再現に使えない |
| `Input.dispatchTouchEvent`で2本指を送る | **再現できた**。下部タブバーの上に2本とも置いて開くと、今のCSSでは倍率1のまま、`touch-action: auto`を全要素へ強制すると×2.71。地図のcanvasの上では地図がズームする。片方がタブバー・片方が地図だと、どちらのCSSでも地図もページも動かない（MapLibreは地図の外に乗った指を数えない） |

実験の実行: [run 35825965232](https://github.com/hidakagit/ride-compass/actions/runs/35825965232)（synthesizeのみ）・
[run 35826207041](https://github.com/hidakagit/ride-compass/actions/runs/35826207041)（片方ずつ）・
[run 35826718509](https://github.com/hidakagit/ride-compass/actions/runs/35826718509)（両指とも部品の上）。

**テスト**: 地図の範囲を8px格子で走査して最前面に当たった部品（押せる要素ごとに1点、地図のcanvasは
除く）の全部へ、2本指のピンチを`dispatchTouchEvent`で送り、`visualViewport.scale`が1のままで
あることを見る。今のコードに当てると（[run 35827419550](https://github.com/hidakagit/ride-compass/actions/runs/35827419550)、
対象28件）、**次の部品から始めたピンチがページ全体を×2.17に拡大した**:

- レンズのボタン「レンズ: 総合難易度（タップで変更）」とその中身（`LensControl`）
- MapLibreのズームボタン「Zoom in」「Zoom out」
- MapLibreの帰属表示の開閉「Toggle attribution」

チップ・タブバー等、`touch-action: none`を明示している部品は拡大しなかった。つまりこのテストは
**わざと壊さなくても、今のコードの実例で落ちる**（検知できることの確認はこれで足りている）。

**入れていない理由**: 入れると上の実例でCIが赤になる。直すのはレンズのボタンと地図のコントロールの
CSS（`touch-action: none`を足す）で、段階Cの範囲（テストを足す）の外であり、レンズ・凡例は
並行して別の担当が触っている。テストの本体は作業ツリーの手元の枝`r7-pinch-test`（コミット
`fab22e5c`）に置いてある。直した後に入れる。

## 段階Cの検証

- 手元: `lockrun heavy`で`eslint e2e && tsc --noEmit`→通過、`prettier --check`→整形済み、
  `python scripts/review_checks.py docs`→違反なし。
- E2EはCIのe2eジョブで判定（上のrun）。

所要（並行実行）: 依頼 2026-09-23 14:47 → 完了 15:42（55分）、うち枠待ち24分・CI待ち11分、規模札（段階C 見込み150分）。枠待ちのうち22分は、ロック待ちのまま取り消した手元のピンチ実験（14:52〜15:14、取得前に止めたため`lockrun --report`には出ない）。CI待ちは実験・壊した入力を含む6回のpush

## 段階Dへの追加（2026-09-23、ユーザー承認）: 走査の母集団に画面と状態を含める

段階Cの観点2（ピンチ）・観点3（カスケード）は、確かめる要素を性質で画面から集める
（格子の走査・余白ユーティリティを持つ要素の全件）が、**走査する画面と状態はスマホ幅で
起動した直後の地図画面だけ**だった。観点1の既存テスト（ヘッダー・凡例・比較表）も画面は
手で選んでいる。要素は性質で集めても、画面と状態を手で選んでいる限り、選ばなかった状態
（ルート結果を開いた・設定のシートを開いた・PC幅・研究モード等）で出る壊れ方は捕まらない。

段階Dで行うこと:
1. アプリの状態（画面 × 開いているシート・パネル × 幅 × モード）を母集団にする。**状態の一覧を
   手で書かない**——状態を宣言している場所（シートの種類・タブ・モードのstateや型）から導けるかを
   先に調べ、導けるならそこから作る。導けないなら、なぜ導けないかと、代わりに何を正本にするかを
   T1014.mdへ書いてから手で並べる（その場合も、状態が1つ増えたときに一覧の側が落ちる検査を付ける）。
2. 観点1〜3の走査（収まる・押せる／ピンチ等の既定動作／カスケード）を、その全状態へ当てる。
   各状態へ進める段取りは`e2e/fixtures.ts`のヘルパーに置く（パターン4「共通フィクスチャに置くもの」）。
3. 全状態へ当てた結果、新たに落ちたものは欠陥として起票する（テストを緩めて通さない）。
4. CIのe2eの所要が状態の数に比例して伸びるので、増分を実測して記録する。

## CIの範囲の扱い（2026-09-23、ユーザーと合意）

作業ブランチ・masterとも、CIは変更の範囲で絞らず全件を回す。範囲で絞るには各ジョブが読む
ファイルの全体を知る必要があるが、backendのテストは`parents[2] / "scripts"`等でbackendの外も
読み、grepではコメントと区別して拾いきれない——手で持つ一覧（許可リストの形）になり、新しい
テストが新しい場所を読んだ瞬間に黙って穴が開く。4ジョブは同時に走り全体は約1.5分（直近6回で
83〜105秒）なので、絞っても縮まない。段階Dでe2eの所要が状態の数に比例して伸びたら、その実測を
見てユーザーと検討する（手段は先に決めない。テストを分けて並べる案は、シナリオに依存する形に
なり読めなくなるとしてユーザーが懸念）。

## 観点2の投入（2026-09-23、T1068）

段階Cで入れずにいた「地図の上に重なる部品から始めたピンチは、ページ全体の拡大にならない」は、
欠陥の修正と一緒に[T1068](T1068.md)で`e2e/map-runtime.spec.ts`へ入れた。規則を外した入力で落ちる
ことも同タスクで確かめた。これで観点2・3は1本ずつ入った。

所要（並行実行）: 段階A 依頼 2026-09-23 不明 → 完了 不明（約60分。担当の報告の時刻から見た振り出し〜報告。T1067.mdの背景）、うち枠待ち不明・CI待ち不明、規模札（段階Aは段階のため札なし）

所要（並行実行）: 段階B 依頼 2026-09-23 不明 → 完了 不明（約110分。担当の報告の時刻から見た振り出し〜報告。T1067.mdの背景）、うち枠待ち不明・CI待ち不明、規模札（段階Bは段階のため札なし）

## 段階D（2026-09-23）

### 1. 走査の母集団に画面と状態を含めた（`e2e/all-states.spec.ts`・`e2e/states.ts`・`e2e/scans.ts`）

**状態は手で並べず、画面から導いた**。状態を宣言している場所を調べると、`page.tsx`の型
（`MobileSheet`・`SettingsTab`・`RouteMode`）はビルドで消え、実行時に読めない。一方、画面は
シート・パネルの開閉をARIAで宣言している。これを正本にした:

| 次元 | 導き方 |
|---|---|
| 幅 | 幅の分岐はCSSの`--breakpoint-mobile`だけ。その両側に1つずつ（390px・1280px）置き、両側にあることを実行時に検算する（ブレークポイントが動いて片側に寄ったら落ちる） |
| モード | ヘッダーのメニューに並ぶチェックボックス（今は研究モード）。メニューに1つ足せば母集団に入る |
| 段階 | ルートの生成前・生成後（手で2つ置いた。アプリの入口の操作が「生成」1つのため） |
| シート・パネル | `aria-expanded`を持つ部品・`role="tab"`・モバイルの「パネル切り替え」の中の`aria-pressed`の部品。基本の状態から2段まで開く |

状態が1つ増えたときに一覧の側が落ちる検査は要らない（一覧を持たない）。代わりに**列挙器の検算**
として、他のテストが前提にしている状態（モバイルの「ルート結果」シート、生成後の「比較」タブ）へ
この列挙も届くことを確かめる。

**当てる走査**（`e2e/scans.ts`、どれも画面に在るもの全部を性質で集める）:
- 観点1: ページが横にスクロールしないこと、押せる部品（ボタン・リンク・入力・タブ等）が横スクロール容器の外で
  画面の横幅からはみ出さないこと。
- 観点3: Tailwindの余白ユーティリティがリセットに潰されないこと（段階Cの`cascade.spec.ts`の中身を移した。
  同じことを初期画面だけで見ていた`cascade.spec.ts`は消した）。
- 観点2（ピンチ）は全状態へは当てていない。実験（[run 35834277290](https://github.com/hidakagit/ride-compass/actions/runs/35834277290)）で
  当てると、下部シートとポータルで開くポップオーバーの中から始めたピンチがページを拡大した。地図ペインの外に
  開くものを止めるかはUIの判断なので、測った結果を[T1069](T1069.md)へ書き、決まったら`scanPinch`を
  全状態へ足す（T1069の完了条件）。初期画面の地図の上の部品は、T1068のテストが見ている。

**新たに落ちたもの**: 観点1・3は全状態（4本で計311状態）で0件だった。欠陥として起票したものは無い
（ピンチの結果はT1069へ）。

**列挙器で直したもの**（どれもテストを緩めるものではなく、同じ部品を同じ見分けで指すための修正）:
- 部品の名前に時刻が入る（「出発時刻: 7:55」）ため、見分けの数字を`#`へ伏せた。
- 部品が後から現れる（カタログ・世代の到着）ため、基本の状態では並びが落ち着くまで待ち、押すときも
  しばらく探し直す。
- **画面の状態の一部がlocalStorageへ保存され、開き直しても残る**（チップのグループの開閉・表示項目の
  設定等。`MapOverlayControls`の仕様）。1段目でグループを開くと次の開き直しでも開いたままになり、
  折りたたみ中にだけ出る「◯◯の表示項目を設定」ボタンが見つからなくなった（本案の1つ前の
  [run 35839458223](https://github.com/hidakagit/ride-compass/actions/runs/35839458223)で4本とも落ちた原因）。
  基本の状態を作った時点の保存内容を控え、開き直すたびにそこへ戻す形にした。
- 2段目（1段目で開いたものの中の部品）で、上に重なるポップオーバーのために押せない部品は、違反では
  なく件数だけ記録する（モバイルで各6件。凡例のボタンが出発時刻のポップオーバーの下になる等）。

**e2eの所要の増え方**（CIのe2eジョブ、2並列。1本あたりは状態の走査の`console.log`の実測）:

| | 段階C時点 | 段階D |
|---|---|---|
| e2e全体 | 約40秒（10本以下） | 本案のrunを参照（報告に添える） |
| 全状態の走査1本あたり | — | モバイル 生成前 約170〜265秒（72状態）・生成後 約290〜360秒（85状態）、デスクトップ 生成前 約140〜230秒（70状態）・生成後 約220〜320秒（84状態）（[run 35836987428](https://github.com/hidakagit/ride-compass/actions/runs/35836987428)・[run 35839458223](https://github.com/hidakagit/ride-compass/actions/runs/35839458223)の幅） |

所要は状態の数（4本で計311状態）にほぼ比例し、1状態あたり約2〜4秒（大半は開き直し＝ページの読み込み）。
e2e全体は約40秒から、全状態の走査4本ぶん（直列で約14〜20分、2並列で約7〜10分）伸びる。
T1014「CIの範囲の扱い」のとおり、この実測をもとにユーザーと検討する（手段は先に決めない）。

**壊した入力**（[run 35836995148](https://github.com/hidakagit/ride-compass/actions/runs/35836995148)、masterへは入れない）:
`globals.css`の`*`リセットを`@layer base`の外へ出すと、全状態の走査4本がすべて落ちた（ボタンの`px-2`・`py-1`が
0pxに潰れる）。

### 2. sceneの式が実ブラウザのスタイル検証を通る（`e2e/map-runtime.spec.ts`）

「宣言された地図レイヤーを全部ONにしても、スタイル検証のエラーが出ない」を足した。タイル世代を配り
（無いとタイルのソース自体が作られない）、`mapDisplay.layerIds`の全レイヤーとramp軸のレンズをONにして、
取得の失敗（`sourceId`を持つ）以外の`map:error`が0件であることを見る。
**壊した入力**（同じrun）: 道路の線の`line-opacity`へ存在しない演算子を入れると、4レイヤー分の
`layers.road-tiles-*.paint.line-opacity: number expected, array found`で落ちた。

### 3. e2e中のプロキシ失敗のログを消した（`e2e/fixtures.ts`）

`next.config.ts`のrewritesの宣言から中継される経路を全部取り、中身無しで返す（気象庁の時刻一覧だけは
空の配列）。rewritesに経路を足せば自動でモックに入る。
中継の接続拒否のログ（`Failed to proxy`）: 1202件（[run 35834277290](https://github.com/hidakagit/ride-compass/actions/runs/35834277290)、jma-tileだけ）→ 29件（[run 35836987428](https://github.com/hidakagit/ride-compass/actions/runs/35836987428)、jma-tileを個別にモックしたときに残ったgsi-relief-tile）→ 0件（[run 35839458223](https://github.com/hidakagit/ride-compass/actions/runs/35839458223)、rewritesの宣言から全経路をモック）。

### 4. D-017の反映

`docs/conventions/testing.md`パターン4の「未定（D-017）」を、決定（`frontend/e2e-live/`・`playwright.live.config.ts`、
CIには載せない）へ書き換えた。**中身（設定ファイル・最初のspec）はまだ作っていない**——作るかは報告で相談する。

## 段階Dの後に残るもの（保留）

- 保留: **観点2（ピンチ）を全状態へ当てること**——下部シート・ポップオーバーの中から始めたピンチを止めるかは
  UIの判断。測った結果と選択肢は[T1069](T1069.md)へ書いた。決まったら`scanPinch`を`all-states.spec.ts`へ足す。
- 保留: **`frontend/e2e-live/`の中身を作るか**（置き場と走らせ方はD-017で決定済み）。作るなら最初の1本は
  「実backend・開発DBで、地図の上に路面タイルが描けること」（本番に近いデータでしか出ない描画の壊れ方）を
  候補にする。作らないなら、パターン4の記述だけで次に要るときへ回す。
- 保留: **CIのe2eの所要**（約40秒→約7〜10分）をどう扱うか——「CIの範囲の扱い」節のとおりユーザーと検討する。
- 未確認: 完了条件「手元での走らせ方が書かれ、実際に通っている」のうち、開発機（Windows）で`npm run test:e2e`が
  通ることはまだ見ていない（段階A以来、E2EはCIで判定している）。

所要（並行実行）: 依頼 2026-09-23 16:50 → 完了 18:20（90分）、うち枠待ち0分・CI待ち70分、規模札（段階D 見込み150分）。CI待ちは全状態の走査の実験・本案・壊した入力の3回分（1回あたり20〜26分。壊した入力は別の作業ブランチ orch/R7-e2e-verify で本案と並べて回した）で、完了のコミット自身のCIは含まない
