# T1073. ユーザーへの確認待ち（判断と操作）を1つのコマンドで記録から出す

状態: 未完了（2026-09-24、仕掛中のダッシュボードとコマンド2本のために開け直した）

## 背景

並行実行で司令塔がユーザーへ出したお願い（確認・承認・実施）が会話にしか無く、ログに流れてユーザーが全部を読み返さないと拾えない。ユーザー指摘（2026-09-23）「ユーザ確認や承認依頼、実施依頼が今はすべてログに流れていて、私が全部読み返さないといけない。それは漏れるので避けたい」「確認が要る操作やコマンドも取りまとめておいて」。規約 docs/conventions/orchestration.md「ユーザーへのお願いは記録に置く」節で、お願いは頼んだその場で該当タスクの記録へ決まった書き方で置くことにした（判断は保留の節と「**ユーザー決定（日付）**:」、操作は見出し「## ユーザー操作（待ち）: …（日付 依頼）」→済んだら「## ユーザー操作（済 日付）: …」）。当日の5件（T1064・T973・T1066×2・T1062）は記録へ移した。

対応方針: 確認待ちの一覧を1つのコマンドにする。python scripts/orchestrate.py asks（と /orchestrate:decisions の置き換え、または /orchestrate:asks）が、origin/master のタスク記録から (1) 未決の判断（decision list と同じ規則）と (2) 見出しが「## ユーザー操作（待ち）」の節（見出し・なぜユーザーか・実行する場所とコマンドの要点）を集め、タスクごとにまとめて出す。閉じたタスク（台帳に行が無い）でも操作待ちが残っていれば出す。状態の表には写さない。

完了条件: 当日の5件の操作待ちと、未決の判断が1回のコマンドで出ること。操作の見出しを「済」へ変えると一覧から消えることを、一時の記録で確かめる。書き方から外れた見出し（例: 「ユーザー操作待ち」）を拾わないこと・拾えないことを明記し、既知の1件で検算する。

## 範囲の追加（2026-09-23、ユーザー指摘）

ユーザー「改善も同じ。改善して標準にするか承認依頼するものも集めておいて。根はすべて同じ、統一して
集めて、1つのコマンドでまとめて出したい」。確認待ちは3種類——**判断**（保留の節と`**ユーザー決定
（日付）**:`）・**実施**（`## ユーザー操作（待ち）: …`）・**改善の承認**（`## 改善提案（承認待ち）: …`）——で、
どれも「ユーザーの手を待っているもの」という同じ根を持つ。1つのコマンド（`orchestrate.py asks`と
`/orchestrate:asks`。既存の`decision list`と`/orchestrate:decisions`はこれに統合し、二重に残さない）が、
origin/masterのタスク記録から3種類を集め、種類ごと・タスクごとにまとめて出す。閉じたタスクの記録に
残った待ちも出す。完了条件に「改善提案の見出しを『承認』へ変えると一覧から消える」を加える。

## 実施（2026-09-23、担当A-asks）

### 方針

- `decision list`と`/orchestrate:decisions`を`asks`と`/orchestrate:asks`へ置き換えた（改名して中身を
  広げた。旧コマンドは残さない）。モジュールは`scripts/orchestration/decisions.py`→`asks.py`。
  `queue.py`（手動タスクの前提が判断待ちか）は同じ`record_holds`を`asks`から読む。
- 読む範囲は台帳の未完了タスクから**origin/masterの全タスクの記録**へ広げた。閉じたタスクにも
  閉じた後に保留が書き足される（実例: T957は完了のあと`## 保留（ユーザー判断）: 印のほかに…`が
  足された。旧実装の範囲では出ない）。出所に「閉じたタスク: 台帳に行が無い」と付ける。
- 実施と改善の承認は、2段目以下の見出しが`ユーザー操作（待ち）`・`改善提案（承認待ち）`で始まる節
  だけを拾う（次の同じか上の見出しまでを1件）。済・承認・見送りへ変えた見出しは合わない。
- **判断の誤検出の直し**: 保留の単位を「見出しに『保留』を含む節」から「**保留を宣言する**見出し」へ
  狭めた。宣言は、見出しの文（または段落の書き出し）が「保留」で始まり、そのあとが行末・括弧・
  コロン・番号のもの（段落の側で既に使っていた判定と同じ）。「保留した場合の影響」「保留することで
  何が起きうるか」「保留理由」「保留にした判断」等、「保留」が別の語の一部であるものは拾わない。
  規約（orchestration.md「保留と決定の書き方」）に拾う形・拾わない形を並べた。
- 決定の見出し形（保留の見出しの節の中の、1段下の`### ユーザー決定（日付）`）も決定として数える
  ようにした。保留の節と**同じ段の別の見出し**に書いた決定（T1026・T1046の形）は、単位の外なので
  従来どおり数えない——「保留の直後に続く同じ段の決定見出し」を拾う案を一度入れたが、T1046は保留と
  決定の間に「## 保留した場合の影響」が挟まり効かず、挟まった節を飛ばすと位置からの推測になるため外した。

### 母集団（2026-09-23、origin/master f180e89e）

- 見出しに「保留」を含む行（`git grep -h -E "^#{2,}.*保留" f180e89e -- docs/records/tasks | wc -l`）: 72行。
  うち宣言の形（`git grep -h -P "^#{2,}\s+保留(?:$|[（(:：]|\s*\d)"`）は10行。「保留した／する／すると／
  による／し続け」で始まる影響の節（`^#{2,}\s+保留(した|する|すると|による|し続け)`）は54行で、旧実装は
  これらをすべて保留の単位にしていた（決定の段落が無ければ判断待ち）。残る8行は「保留理由」「保留にした
  判断」「保留・次に判断が必要な点」や見出しの途中の「（保留）」等。
- 宣言の形の保留（見出し＋段落）を持つ記録:
  `git grep -c -P "^(#{2,}\s+保留(?:$|[（(:：]|\s*\d)|(?:[-*]\s+)?(?:\*\*保留(?:[（(:：]|\s*\d)|保留[:：]))" origin/master -- docs/records/tasks`
  → T275・T957・T958・T973・T1001・T1014（3）・T1026（3）・T1027・T1046・T1064・T1066（2）・T1074。
  このうち単位の中に決定の段落があるもの（T958・T973・T1001・T1064・T1074、T1066の1件、T1014の1件）は
  出ない。

### 検証

- (a) 誤検出の5件: 旧実装（origin/masterの`decisions.py`）は今の記録でT636・T640の
  「## 保留した場合の影響」を判断待ちに出す（T864・T957・T1017は、その影響の節へ後から
  「ユーザー決定」が書かれたため今は出ない）。新実装の`record_asks`はT636・T640・T864・T1017で判断0件、
  T957は影響の節を拾わず、別に足された宣言の保留1件だけを出す。
- (b) 既知の本物の判断待ち: 上の`git grep -c -P`で列挙した宣言のうち決定の無いもの——T957
  （`## 保留（ユーザー判断）: 印のほかに合わなくなる2か所を起票するか`）とT1066
  （`### 保留: 司令塔の外で起動した担当の印を自動で外すか`）——が`python scripts/orchestrate.py asks`に出る。
- `python scripts/orchestrate.py asks`（約3秒）: 確認待ち9件（判断8・実施0・改善の承認1）。
  判断はT275・T957・T1014（2）・T1026・T1027・T1046・T1066、改善の承認はT1049
  （`## 改善提案（承認待ち）: 全体へ波及する変更を…`）。実施は当日の5件がすべて「済」へ変わっているので0件。
- 一時の記録で消えることを確かめた（`record_asks`へ文字列で渡す）:
  - T1049の見出しを`（承認 2026-09-23）`／`（見送り 2026-09-23）`へ変えた写し → 改善の承認 1→0。
  - T1066の「## ユーザー操作（済 2026-09-23）: 本体の設定…」を「（待ち）」へ戻した写し → 実施 0→1。
  - 一時の記録（待ち各1件・`## ユーザー操作待ち: …`・`## 承認待ち: …`・本文中の`## ユーザー操作（待ち）`
    への言及・`## 保留した場合の影響`・`**保留し続けると**:`を含む）→ 判断1・実施1・改善1。
    見出しを済・承認へ変え、保留へ`**ユーザー決定（…）**:`を書いた写し → 0・0・0。
- 書き方から外れた見出しは拾わない: 上の一時の記録の`## ユーザー操作待ち`・`## 承認待ち`は数えられて
  いない。既知の1件: T1049の237行・T582の102行は本文中で`## ユーザー操作（待ち）`に触れているが、
  見出しではないので実施に出ない（今の出力の実施0件）。
- `python scripts/orchestrate.py decision list` → `invalid choice: 'decision'`（旧コマンドは無い）。
  `python scripts/orchestrate.py prereqs T1014`は動く（`record_holds`の移動の確認）。
- `ruff check`（importの並びを2件直した後0件）、`python scripts/review_checks.py docs` → 違反なし。

### 残る出力: 記録の側の決定が書き方から外れているもの

次は決定が既にあるが、規約の書き方の外に書かれているため判断待ちに出続ける。直すなら記録の側
（決定を保留の単位の中へ`**ユーザー決定（日付）**:`で書く）で、このタスクでは触っていない。

- T1014（2件）: 3つの`- 保留:`の後ろに決定の段落が1つだけあり、最後の保留の単位にしか入らない。
- T1026・T1046（閉じた）: 決定が保留の節と同じ段の別の見出し（`### ユーザー決定（…）`・`## ユーザー決定（…）: 採らない`）。
- T1027（閉じた）: 決定が選択肢の行の途中（`1. … → **ユーザー決定（…）:`）。
- T275（閉じた）: 「**決着（2026-08-25、T299）**」という別の語で書いてある。

### 実装の増減

- 実装: `asks.py` 149行（`decisions.py` 96行を置き換え）、`orchestrate.py`・`queue.py`は差し替えのみ
  → +156／−103（純増53行）。テストは無し（このディレクトリにテストは無い）。
- 文書: `orchestration.md` +27／−14、`asking-user.md` ±1、コマンド`asks.md` +28（`decisions.md` −24を置き換え）。


所要（並行実行）: 依頼 2026-09-23 20:12 → 完了 20:23（11分）、うち枠待ち0分・CI待ち不明、規模札S

## 続き: 仕掛中のダッシュボードとコマンド2本（2026-09-24、ユーザー決定）

規約 docs/conventions/asking-user.md「仕掛中のダッシュボード」節のとおりに作る。
1. ダッシュボードのページ（https://claude.ai/artifact/E8G458My7xPA8RpbkU3RWF、collection `pending`）を、仕掛中のタスクごとに
   「誰が進めているか・待っているもの」を見せる画面にし、操作・改善案に「済」「承認」「見送り」のボタンを付ける（押すと`answer`に書く）。
2. `/dashboard`（新設）と`/asks`（`/orchestrate:asks`を置き換え、二重に残さない）。`orchestrate.py asks`は記録の保留を出す部分として残し、
   コマンドがダッシュボードの件（`ArtifactData`でしか読めない）と合わせる。誰が進めているかは状態の表から導き、写さない。
3. 1日1回のバックアップ（`.git/orchestration/pending-backup/<日付>.json`、直近14日、急に0件なら知らせる）の予約。
完了条件: 仕掛中のタスク（担当が進めている1件と、答えの出た問いの1件）がダッシュボードに出て、`/asks`が問いだけを聞き、
ページのボタンで`answer`が書かれることを1回ずつ見る。

**ユーザー決定（2026-09-24）**: 状態の表（board.json）はこの機械の外から読めないので、`/dashboard`は読めないセッション（クラウド・
別のPC）では担当の様子を出せない旨を1行書き、ダッシュボードの件と台帳だけを出す。状態の表を外へ写して見せない（重複になる）。
外から担当の様子を見るときはRemote Controlで手元のセッションにつなぐ。「外から見えなくても大丈夫」。
