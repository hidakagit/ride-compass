# T1136. 派生の段を途中から流し直すと、分類やしきい値から外れた行に前回の値が残る

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 片側だけの修正（再発）。derive_cli.py:10-11は途中から流し直す入口を「分類器を直した・しきい値を変えた」とき用と書くが、各段のUPDATE/UPSERTは値が出た行だけを更新し先に消さない: derive_node_materials.py:38-42（_UPSERT_KIND。どの分類にも当たらないノードは古いkindのまま）・67-80、derive_counts.py:76-102（停止要因。_EDGE_STOP_ZEROはNULLしか埋めない）・122-140（事故）、derive_raster_materials.py:87-93（標高）・149-155（土地被覆）・182-200（道への集約。way_materialsはtopologyからもTRUNCATEされず全段を流し直しても残りうる）。リセットがあるのは_CLEAR_SIGNALSと_WAY_ORPHANSだけで、テストもその2つだけ。母集団: app/batchのUPDATE/UPSERT文のうち、FROM副問い合わせが対象表の全行を覆わず同じ段に先行するリセットが無いもの（12文中7文）。各段の先頭で自分が書く列を未計算へ戻し、「入力を変えて同じ段を2回流すと外れた行が未計算に戻る」を段ごとに確かめる。T1050（信号の半径）はこれが直って初めて--from nodesで効く。本番DBへの書き込みは作業に含まない。
