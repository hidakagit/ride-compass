# T1132. frontendの「機能どうしは互いを読まない」が破られても何も落ちず、T1107で1件破られた

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 境界が落ちる形で表現されていない（設計原則 構造仕様16）。directory-layout.md:41-56の約束（上の層→下の層、機能どうしは読まない、共有層の中はcomponents→hooks→services→lib）に対し、eslint.config.mjsにimportの制限が無く、src/structure/にも層の検査が無い。T1001段階Dで0にした違反が、T1107（846efa4a）のfeatures/route/SegmentWind/SegmentWind.tsx:3 → @/features/conditions/cardinalLabel で3日後に1件戻った（全ソースのimport解析で、機能→別の機能1件、共有層→features 0件、共有層の中の下→上 0件）。cardinalLabelをlib/へ下ろし、3つの向きをlint（no-restricted-imports等）で落ちる形にする。わざと1件戻してlintが落ちることを確かめてから閉じる。
