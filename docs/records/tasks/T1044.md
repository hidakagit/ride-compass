# T1044. ノードの生データを読む綴りが、共有断片を持たないまま手書きで散っている

状態: 未完了

## 背景

T1031で道の生データ（`source = 'osm_way'`）の読み方を`domain/material_sql.py`の関数へ
寄せた。ノードの生データ（`source = 'osm_node'`）には同じ役の関数が無く、`source`の比較と
キーのキャストが読み手ごとに手書きされている。詳細はT1031の完了時に追記する。
