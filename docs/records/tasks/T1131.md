# T1131. backendの層（api・services・domain・batch・infrastructure）が持ってよいもの・持たないものが書かれておらず、越境が層のあちこちにある

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 契約の欠け（層ごとの全ソースで問うて初めて出た種類）。directory-layout.mdは各層を1行（api=HTTPの境界、services=ユースケースの組み立て、domain=外部I/Oを持たない）でしか書かず、次が決まっていない: apiがinfrastructureを直接呼んでよいか（routers 15本中9本が呼ぶ）、入力の不変条件をどの層が持つか（axis_admin.py:79-217のvalidator4本がapiにあり、API以外の経路（T972の復元等）で書かれた行はdtype・混在の検査を通らない）、外部の応答の形をどこで解くか（servicesの6ファイルが外部JSONのキーを読む）、応答の型を何層で書くか（serviceのdataclassとrouterのPydanticが10組72項目で重複）、domainにSQLの全文・HTTPのルート記法・生成専用の表示宣言を置いてよいか（attributes.py・landcover.py・traffic.pyのbatch専用SQL、gsi_tiles.pyのルート記法、map_display等4モジュール）、batch・infrastructureが業務判断（停止要因の畳み方・土地被覆の帯幅）を持ってよいか。dependencies.py:1-5の「組み立てはすべてここに集約」はmain.py:92,98の直接構築で事実と違う。この段階: directory-layout.mdに層ごとの「持つもの・持たないもの」を書き、tests/structure/test_layer_imports.pyの走査を「domainは他の層をimportしない」等の向きまで広げる（現在の違反0件）。越境を移す作業は種類ごとに別の段階として切る（契約を書く段階はS）。

前提の変化（2026-09-26、T1137）: 停止要因の畳み方（`COUNT_KIND_OF`）と信号の読み替えは`domain/traffic.py`へ、土地被覆の帯幅は`domain/landcover.py`へ移った。batch・infrastructureはそこから組み立てたSQL式・定数を読むだけになった。
