# T997. 旧スキーマのテーブルを指したまま残っている経路を洗う

状態: 未完了（2026-09-22起票）

## 背景

[T996](T996.md)の全件走査中に、生データのテーブルを作り直した後も**旧テーブル名を指したまま
残っている経路**が複数見つかった。現行のテーブルは`source_features`（`source`列で種別を分ける）・
`road_edges`・`edge_materials`・`way_materials`・`node_materials`等で、`osm_raw_ways`・
`osm_raw_pois`・`osm_raw_nodes`・`road_nodes`・`elevation_attributes`・`edge_attribute_counts`・
`accident_points`・`osm_import_runs`・`accident_import_runs`はORMにも開発DB（本番スナップショット
由来）にも存在しない。

同一セッションで直した分は[T996](T996.md)のコミットに含む。

- `infrastructure/material_coverage.py`が`FROM osm_raw_ways AS w`を手書きしていた。共有断片
  `domain/material_sql.py: WAYS_SOURCE_SQL`は新スキーマへ移っており、**この1本だけが取り残されて
  いた**。材料の欠損率APIは全体が`UndefinedTableError`で落ちていた（実行して確認）。
- `domain/material_catalog.py`の`gradient_percent`の`present_count_sql`が
  `elevation_attributes`を数えていた。値は`edge_materials.average_grade`にある
  （同じ材料の`value_sql`は既に`em.average_grade`を指していた）。

## 残っているもの

### 1. `/api/debug/...`の健全性表示が恒久的に null

`api/routers/health.py`の`_KEY_TABLES`（`osm_raw_ways`・`osm_raw_pois`・`road_edges`・
`accident_points`）と`_IMPORT_RUN_TABLES`（`osm_import_runs`・`accident_import_runs`）のうち、
`road_edges`以外は存在しない。`_table_row_count`は欠落を例外にせず`None`を返す設計のため
**落ちないが、「バッチが本番で一度も走っていない」という誤った信号を出し続ける**。
この表示はまさにその判断のために置かれている。

対応: 現行スキーマで同じ問いに答えるテーブル・列へ置き換える（取込の実行履歴は
`source_runs`が持つ）。置き換え先が無い項目は表示から外す。

### 2. `scripts/measure_gradient_outliers.py`（371行）が1クエリも通らない

全SQLが`elevation_attributes`・`osm_raw_ways`・`road_edges.edge_id`・`re.highway`を参照する。
`road_edges`の列は`(osm_way_id, segment_index, from/to_node_id, geom, distance_m, bearing_deg,
reverse_bearing_deg, source_run_id)`で`edge_id`も`highway`も無い。
`docs/modules/frontend/static-map-layers.md:29`がこれを運用手順として案内している。

対応: 現行スキーマで書き直すか削除するかを決める。削除するならdocsの案内も同時に消す。

### 3. `docs/modules/backend/batch-pipeline-dependencies.md`が存在しないパイプラインを説明

`import_pbf.py`・`presplit_road_graph.py`・`precompute_road_node_degrees.py`・
`precompute_edge_attribute_counts.py`・`precompute_elevation_attributes.py`・
`precompute_way_attribute_counts.py`・`match_designations.py`の7本はいずれも存在しない
（`find backend -name`で0件）。現行は`ingest_cli.py`（`source_profile.yaml`＋アダプタ）→
`derive_cli.py`の2本。テーブル名も同様に旧いまま。

**2026-09-23、[T1022](T1022.md)で実施済み**: 文書ごと削除した。現行のバッチ（取込・派生の
段・鮮度台帳・世代の繰り上げ）は`docs/modules/backend/static-road-attributes.md`が既に
説明しており、削除した文書にしか無い現行の事実は無かった。唯一のリンク元
（`routing-engine.md`）は張り替えた。

### 4. `batch/source_profile.py`の`SourceSpec.columns`/`tags`が効いていない

YAMLから読んで`source_runs.profile`へ記録するだけで、絞り込みとしてどこも適用していない
（「列の絞り込み」という名前と説明が実態と違う）。消すとYAML契約と本番の記録内容が変わるため、
どちらへ寄せるかの判断が要る。

## 完了の条件

1〜4それぞれについて、置き換え・削除・据え置きのどれかを決めて実施済み。1は
`/api/debug`を実際に叩いて値が入ることを確認する。2は削除なら参照元のdocsも同時に消す。

## 探し方

`grep -rn "osm_raw_\|road_nodes\|elevation_attributes\|edge_attribute_counts\|accident_points\|
_import_runs" backend/app backend/scripts docs --include=*.py --include=*.md`。
ORMの実テーブルは`grep -rn "__tablename__" backend/app/infrastructure`で導く。

## 母集団の拡張（2026-09-23・周期レビュー第12回で判明）

**上の「探し方」の母集団が狭く、このままでは半分が残る。** 第12回の実測では、旧テーブル名の
残存は`docs/records`を除く全追跡ファイルに広く分布しており、うち相当数が
`backend/app backend/scripts docs`という現行の走査範囲の**外**にあった。

とくに重要なのは、**利用者・運用者が画面で読む文言に混ざっているもの**である。

- 材料カタログの材料が持つ「判定根拠」の説明文に旧テーブル名が書かれており、これは
  管理APIで配られて**軸スタジオの「材料」タブでホバー時に運用者へ表示される**。
- 同じ画面のⓘポップオーバーの本文が、母集団の説明として旧テーブル名を名乗っている
  （**画面に出る嘘**）。
- 飽和度の測定スクリプトが、存在しない集計テーブルを運用者向けメッセージで案内している。

このほか、欠損率を測るモジュールの docstring が旧スキーマで母集団を説明している
（SQL は[T996](T996.md)で新スキーマへ直したが**説明文だけが旧のまま**）、
`docs/conventions/testing.md`が存在しないテスト足場を名指ししている、
frontendのコメント・テストの docstring にも残っている。

### 探し方（差し替え）

上の grep の対象を次へ広げる。**`docs/records`と`node_modules`を除く全追跡ファイル**:

```
git ls-files | grep -v '^docs/records/' | xargs grep -n \
  "osm_raw_\|road_nodes\|elevation_attributes\|edge_attribute_counts\|way_attribute_counts\|accident_points\|_import_runs"
```

### 完了条件（差し替え）

「1〜4それぞれについて決めて実施済み」を、**上の grep が0件**へ変える。項目を数え上げる
形だと、母集団の外にあるものが完了条件から漏れる（第12回で実際に漏れた）。

**着手順**: 利用者・運用者に見える文言（材料の説明文・ⓘの本文・スクリプトのメッセージ）を
先に直す。コメントや docstring より実害が直接的で、かつ直すのが安い。

## 段階A: backend・docs（2026-09-24、W-schema）

差し替え後の grep（上の「探し方（差し替え）」）で**着手時79件・18ファイル**。1件ずつ位置で判定した。

### 直したもの

- **材料の判定根拠**（`domain/material_catalog.py`の`source`・`reason`。管理画面「材料」タブでホバー時に
  運用者へ出る）: `osm_raw_ways.tags->>'lit'`等を`OSM wayのタグ lit`等へ。値は`source_features`
  （`source='osm_way'`）の`attrs`にあり、材料の式は`WAYS_SOURCE_SQL`の別名`w`経由で読むため、表の名前ではなく
  「OSM wayのタグ」と書いた。`oneway`の除外理由は`way_materials.direction`（T1055の残り3）。
  `WayMaterialCoverageSpec`のdocstringも同じ。
- **欠損率モジュールのdocstring**（`material_coverage.py`）: 母集団を`WAYS_SOURCE_SQL`で説明し直し、
  `edge_materials`のFKを実際の`(osm_way_id, segment_index)`へ（`edge_id`は無い）。
- **`/api/debug/db-status`を撤去**（残っているもの1）: 返していたのは存在しない表の行数・取込runで、恒久的に
  null だった。現行スキーマへ置き換えると、管理APIの`GET /api/admin/db-status`（`source_runs`の全ソースの
  最新run・全テーブルの実数・統計とVACUUM。軸スタジオの「鮮度」タブが表示する）と同じものを2本目として
  持つことになる。両者の住み分けは「debug側＝未適用migrationの一覧」だったが、migrationの仕組み自体が
  もう無い（`git ls-files backend | grep -i migrat`で0件）。ただ消せない理由が言えないため、置き換えずに
  削除した。呼び出し元はリポジトリ内に無い（`git grep "debug/db-status"`は文書と生成物だけ）。
  `db_status.py`の「未適用migrationの一覧は`/api/debug/db-status`が返す」も嘘になっていたので消した。
  OpenAPIの生成物（`openapi.json`・`api.d.ts`）から当該パスが落ちる。
- **`scripts/measure_gradient_outliers.py`を撤去**（残っているもの2、T1055の残り1）: 全SQLが存在しない表・列を
  引き1クエリも通らない。本番からは呼ばれず、目的の調査（[T931](T931.md)）は完了している。コメントは
  comments.mdの判定木へ1件ずつ通した——どれも計測の設計（帯の切り方・対処案ごとの件数）で、今も成り立つ
  結論（橋・トンネルと上限超えの勾配は値を持たせない、0次フィルタは値の無い区間を除外しない）は既に
  `docs/modules/backend/elevation.md`と`domain/attributes.py`にある。昇格したものは無い。
  docs側の案内（`static-map-layers.md`）は既に無かった。
- **標高属性の辞書の名前**（`road_graph_engine.py`・そのテスト）: `elevation_attributes`は区間id→
  `ElevationAttribute`の辞書で、表とは別の意味の同名。除外リストで逃げず`elevation_by_edge`へ改名した
  （完了条件の grep を例外なしに0件で読めるようにするため）。`routing-engine.md`の関数名も追従。
- 運用者向けメッセージ・docstring: `measure_axis_saturation.py`の0件時の案内、`axis_preview_service.py`の
  母集団、`region.py`のPOIタイル（補給POIも同じタイルに入る）、`traffic.py`（POIの種別は
  `node_materials.kind`）、`material_catalog`ルーター（全表走査の対象、T1055の残り2の値一覧の説明）、
  `test_road_graph_repository_contracts.py`。
- 触ったモジュールの文書の乖離: `cross-cutting-infrastructure.md`（責務の「マイグレーション」、運用
  エンドポイントの表）・`axis-studio.md`（撤去済みの`compute_edge_axis_scores`と存在しない「呼び出し元」節）・
  `evaluation-scoring.md`（値一覧のAPIのDB障害時の返し方）。

grep は**79件→10件**。残る10件は下の段階Bの範囲（frontend/src）で、担当の範囲外のため直していない。
`.claude/commands/review.md`の例示（「`FROM osm_raw_ways`を手書きし」→「旧スキーマのテーブル名を手書きし」）は、同じファイルを触っていた[T963](T963.md)がmasterへ入り終えていたため、rebase後にここで直した。

### 残り（段階B）

1. **frontend/src**（並行して[T1001](T1001.md)がfrontend全般を触っているため止めた）:
   - `components/AxisStudio/MaterialCoveragePanel.tsx`141行のⓘ本文「母集団: Way=osm_raw_ways全件、
     Edge=road_edges全件」（**画面に出る嘘**）→ 例「Way=OSMのway全件、Edge=区間全件」。同105行のコメント。
   - `app/admin/api/material-coverage/route.ts`5行のコメント（全表走査の対象）。
   - `components/Map/axisLayers.ts`9行のコメント（`way_attribute_counts`→`way_materials`等）。
   - テストの仮データ: `DbStatusPanel.test.tsx`の`table_name: "elevation_attributes"`（実在する表名へ）、
     `MaterialCoveragePanel.test.tsx`の`source`（backendの今の書き方`OSM wayのタグ surface`・
     `edge_materials.average_grade の有無`へ）。
   - grep の外: `components/Map/RoadInspectorPopup.tsx`75〜77行のJSXコメントが、この段階で撤去した
     `scripts/measure_gradient_outliers.py --way`を名指している。way idの表示そのものは残し、名指しだけ落とす。
2. 残っているもの4（`SourceSpec.columns`/`tags`）: 消すか・効かない値を拒むか・据え置くかはユーザーの判断
   （仕掛中のダッシュボード`T997-profile-filter`）。答えが出たら実施する。

### 範囲外で見つけたもの

- `db_status.py`・`material_catalog.py`（ルーター）の503の`detail`が「migration適用状況を確認してください」と
  案内するが、migrationの仕組みは無い（スキーマは`create_tables()`が作る）。管理画面に出る文言。

### 検証（段階A）

- `ruff check app scripts tests`（backend）→ All checks passed
- `python scripts/review_checks.py docs` → 違反なし
- `python scripts/export_openapi.py`→`openapi-typescript`（`npm run generate:api`と同じ）→ 差分は
  `openapi.json`・`api.d.ts`から`/api/debug/db-status`が落ちる62行だけ
- 変更が届くbackendテスト12ファイル（`test_road_graph_engine`・`test_debug_stats_route`・`test_db_status_routes`・
  `test_material_coverage`・`test_material_catalog`・`test_material_catalog_routes`・`test_measure_axis_saturation`・
  `test_region_routes`・`test_road_graph_repository_contracts`・`test_traffic`・`test_axis_preview_service`・
  `test_cache_policy`）→ 345 passed
- 上の grep → 10件（すべてfrontend/src）

所要（並行実行）: 段階A 依頼 2026-09-24 03:29 → 区切り 03:44（15分）、うち枠待ち0分・CI待ちは報告時点で未計測
