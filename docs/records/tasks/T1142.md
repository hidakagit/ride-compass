# T1142. 候補が出ている状態で再生成が失敗しても、失敗がどこにも表示されず前の候補が残る

状態: 未完了（2026-09-26起票）

## 背景

周期レビュー第13回（2026-09-26）の指摘。性質: 状態が画面に届かない（再発）。e2eのbackendモックで1回目を成功させ、2回目に429を返すと、デスクトップ・モバイルの設定シート・結果シートのどこにも「混み合っています」が出ない（スクリーンショットはレビュー時の作業場所のui013/s01・s03・s04）。失敗の文言を読むのはpage.tsxのrenderRouteOutcomeEmptyStateだけで、呼ばれるのはroutes.length===0のときだけ（masterでも1235・1442行の2箇所）。T758（2026-09-12）からある欠陥。モバイルでは下部タブの点がconditionsDirty||hasUnseenResultsの1色で、条件の変更・新しい結果・失敗の3つを区別しない（429でも成功でも同じ色）。候補がある間も結果の先頭に失敗を出し、点の意味を分ける。
