# RideCompass

サイクリング向け周回ルート生成アプリ。backend（FastAPI）+ frontend（Next.js）。
アーキテクチャ全体は docs/architecture/ 参照。

設計レビュー（2026-08-15〜16）の指摘と改善実行計画は docs/improvement-plan.md にある。
リファクタリング・機能追加の着手前に該当タスクの有無を確認し、完了したらチェックを更新すること。
improvement-plan.md自体はチェックボックス付きリンクの一覧（インデックス）のみを持ち、
各タスクの背景・対応方針・実装メモ・検証結果はdocs/records/tasks/Txxx.md（タスク番号1件=1ファイル）
にある。
設計原則（RideCompass固有の仕様）は docs/architecture/design-principles.md が唯一の正本（常に最新）。
新しい仕組みを作る前は必ず読むこと。判断原則・進め方は
.claude/commands/review.md「判断原則」節、機能単位の設計は
docs/modules/README.md（モジュール単位、実コードのみを根拠に記述）
を参照。**改善計画のタスクに限らず、既存モジュールへの修正・追加に着手する際は、
対象となるdocs/modules/*.mdを必ず先に精読し、記載と実装の乖離（未記載のファイル・
古くなった記述）を見つけたら同一コミットで修正すること**（サンプリング読みで済ませない
こと自体は本ファイル全体の運用と地続き）。
**依存ライブラリのバージョン・デプロイ・実行環境（プラットフォーム）に触る変更は、
docs/architecture/の該当箇所も着手前に読むこと**——これらの制約はコードからは導けず、
あの文書だけが持つ（「このバージョンへ上げられない理由」はその典型で、読まずに起票すると
実現不能な方針のタスクができる。[T722](docs/records/tasks/T722.md)）。領域と正本の対応は
docs/modules/README.md「このディレクトリが扱わない領域」節にある。

## ドキュメント階層（必読、新しいルールを追加する前に確認すること）

ルール・方針を書く場所は以下の基準で決める。**何でもCLAUDE.mdへ足さない**——CLAUDE.mdは
毎回のセッション開始時に無条件で全文読み込まれるため、肥大化すると本当に常時必要な内容が
埋もれる。

| 階層 | 置くもの | 判定基準 |
|---|---|---|
| **CLAUDE.md（本ファイル）** | 常時・無条件に従うべき短い方針＋詳細docsへのポインタ | 「毎回のタスクで、明示的に呼ばれなくても常に適用すべきか」がYesかつ短く書けるもの。長くなる場合は要点だけ残し詳細を下記のdocs/へ追い出す。**ルール自体の経緯（いつ・何が起きて追加したか）はここに書かず、該当するdocs/records/tasks/Txxx.mdへ任せる**——本ファイルは毎セッション無条件で全文読み込まれるため、経緯の蓄積がそのまま肥大化に直結する |
| **docs/conventions/*.md**（logging.md・testing.md等） | 規約。仕事のやり方の取り決め | CLAUDE.mdからは要点＋「詳細は◯◯.md参照」の形で参照するだけにする |
| **docs/architecture/*.md** | 構成・技術選定・外部の制約・設計原則（構造仕様） | コードから導けない事実と、コードが従うべき構造契約 |
| **docs/modules/*.md** | モジュール単位の実装粒度の記述 | 実装作業者向けの詳細。architecture より細かいものはここへ落とす |
| **.claude/commands/review.md**（`/review`） | **複数の変更を並べて初めて判断できる、周期実行向きのチェック**（複雑度のトレンド・UI一貫性・アーキテクチャ全体の一貫性等） | 1タスク単体では判断材料が無い・判断にコストがかかるもの。**注意**: grep一発で済むような安価で機械的なチェックはここへ寄せない——「次の周期レビューまで気づけない」設計になり手遅れになる。そういうものは`scripts/review_checks.py`の検知器にし、ここには二重チェックとしてのみ残す |
| **docs/records/** | 記録（タスク・決定）。**維持しない** | 記録時点で嘘がなければよく、訂正は新しい記録で行う。レビュー・検知器の対象外（`docs/records/README.md`） |

## 出力言語（必読）

**ユーザーへ見せる結果・メッセージは常にすべて日本語で表示すること。** 対話中の通常の
返信本文（要約・説明・質問・見出し等）はもちろん、バックグラウンドで動くタスク
（Bash/PowerShellのrun_in_background・Agentのdescription・ScheduleWakeupのreason・
CronCreate等）に付随する進捗・ログ・通知メッセージも例外なく日本語にする。英語の技術用語
（ファイル名・コマンド・ライブラリ名等）はそのままでよいが、地の文・説明・見出しは日本語で書く。

## ログ方針（必読）

**コードを追加・変更するときは docs/conventions/logging.md のログ方針に従うこと。** 要点:

- エラー・429拒否・候補0件はWARNING以上で**常時**出す（debug_modeはDEBUG詳細の追加スイッチであり、エラー出力の条件にしない）
- 外部API/キャッシュアクセスは `app/infrastructure/debug_log.py` の `log_external_call` で囲む（cache hit/miss・result・statusをfieldsに設定。ログと /api/debug/stats の統計が自動で付く）
- 高コスト処理はステージ別所要時間と中間結果の減り方を1行INFOサマリにする（route_generator.py参照）
- リクエストIDは request_log.py のミドルウェアが全ログへ自動付与する。個別ログに書かない
- 常時出るログの座標は小数2桁へ丸める。APIキーはどのレベルでも出さない

## コメント方針（必読）

**コードコメントに経緯（いつ・誰の指摘で・改善計画Txxxで・以前は〜だった・実機/実測で
〜だったため等）を書かないこと。** 詳細な判定基準・実例はdocs/conventions/comments.md参照。要点:

- 書いてよいのは「今も成り立つ非自明な制約・理由」を現在形の一文で書いたものだけ
  （例:「このロックはXがYから並列に呼ばれるため必要」）。実装を読めば分かることは書かない。
- 経緯は書かない。同じ内容が対応する`docs/modules/*.md`に既にあるならコード側は削除する
  （二重メンテ防止）。経緯自体を残す価値があるなら該当タスクの`docs/records/tasks/Txxx.md`へ
  （新規に書き足すより、既にそこにある背景と重複していないか先に確認する）。
- 新規に書く/変更するコメントは直ちにこの方針へ従う。既存コードに残る経緯コメントの一掃は
  個別タスク（[T567](docs/records/tasks/T567.md)、モジュール単位で分割）として計画的に実施し、
  一括では行わない（背景・規模は[T566](docs/records/tasks/T566.md)参照）。
- 機械的に検出する仕組みは無い。書く時点でこの方針に照らすこと（判定基準は
  docs/conventions/comments.md）。

## ドキュメント記載方針（必読）

**ドキュメントに要素を数え上げないこと（個数・全件の一覧）。示すべきは挙動。**
詳細・実例はdocs/conventions/documentation.md参照。要点:

- 書かない: 個数（「4本のバッチ」「現行8軸」「5値の分類」）・全件の一覧（ファイル/
  エンドポイント/state/定数/軸id）。**要素が1つ増えたときに嘘になる文は数え上げている**
- 書く: その要素群が何をするか・増えたときに何が起きるか・実装を読んだだけでは
  分からないこと（暗黙の前提・外部システムの挙動・実測値）
- **代表例は許容する**。挙動を分かりやすくするためなら「◯◯等」「例:」の形で挙げてよい
- 全件が必要なら生成物（material-catalog.json等）かそれを定義するコードを指す
- 例外はdocs/modules/*.mdの対象ファイル表だけ（新しい実装ファイルの責務を逆引きする
  ために全件を持つ。完全性は周期レビューで人が見る）。同じ一覧を二重に持たない

## テスト方針（必読）

**新しいテストを追加するときは docs/conventions/testing.md のパターンに従うこと。** 要点:

- レート制限の境界値テストは `rate_limiter.check_rate_limit` を直接呼んで上限-1件を埋め、実HTTPは境界の1〜2回に絞る（上限回数分の実HTTPループ厳禁）
- PostGIS統合テスト（road_graph_session、conftest.py）はファイル単位でエンジン・イベントループを共有する設計。新規ファイルでは `pytestmark = pytest.mark.asyncio(loop_scope="module")` が必要（自前の追加async fixtureにも `loop_scope="module"` を明示）。CIはpytest-xdistで並列化しているため `pytest.mark.xdist_group(name="postgis")` も併せて必須（詳細はdocs/conventions/testing.md参照）
- フロントエンドの新規テストがDOM（render/renderHook/window等）を使わない純ロジックなら、ファイル先頭へ `// @vitest-environment node` docblockを付ける（実装側関数の隠れたDOM依存にも注意、詳細はdocs/conventions/testing.md参照）
- **検査は作業中に回さない。書き終えてから、順番を守って1回ずつ通す**: ①秒で終わる静的検査を
  まとめて（`ruff`・`scripts/review_checks.py docs`・`tsc --noEmit`・OpenAPI生成物のドリフト）
  → 出たものを**全部**直す → ②変更が届くテストを1回（backend・frontendそれぞれの該当
  ファイル。互いに独立なので並行してよい）。静的検査を最後に回すと、シンボルを
  消した時点で既に死んでいた参照を最後に発見し、テストを回し直すことになる。
  **CIで初めて分かる事実（別タスクが終わるまでグリーンにならない等）は、早く
  知るほど計画を変えられる**——手元で抱え込まず早めにpushしてCIに出す。
  **段階（A/B/C…）を切って進めるときは、次の段階へ移る前にこの①②を通す。**段階Cで
  撤去したものの参照が段階Aの終盤で出てくる、という形の手戻りはこれで止まる。
  **1件直すたびに検査を回し直さない**——出た指摘は全部直してから、次の1回を回す。
  1つ直して回す、を繰り返すと、同じ検査を何度も待つことになる。
- **テストが落ちたら、①失敗対象を最小コストで洗い出す→②失敗の原因を取る→③同根を
  まとめて直す→④直した範囲だけを検証（テストと静的検査の両方）→⑤繰り返す、の順で進める**
  （詳細はdocs/conventions/testing.md「テストが落ちたときの直し方」参照）。**①の出力は「何が落ちたか」
  しか言わない**——「なぜ落ちたか」を別に取らないと③でまとめられず、1件直して全体を
  流し直すループに入る。列挙のためにテストを流し直さない（直前のログを読む）。
- **テスト結果はpassedの件数だけで判断しない。警告も結果の一部として読む。**
  backendは`pytest.ini`の`filterwarnings = error`で機械的に止まる。無視するものは理由付きの
  `ignore`行として足す（詳細はdocs/conventions/testing.md「警告は既定でエラー」参照）。
- **手元で回すのは、変更が届く範囲だけ**（backend例: `pytest backend/tests/test_foo.py -q`、
  frontend例: `./node_modules/.bin/vitest run <該当ファイル>`）。届く範囲は次項の手順で導出する。
  **frontendのコマンドに`npx`を付けない**——このリポジトリのツールはすべてローカルに
  入っており、`npx`は毎回パッケージ解決をやり直して13.6秒を足す（`npx tsc --version`
  だけで13.6秒。実測2026-09-22）。
  **例外は`tsc --noEmit`**——TypeScriptは型の波及を1ファイルへ絞れないため、これだけは
  プロジェクト全体で1回通す（`./node_modules/.bin/tsc --noEmit`で27秒。Next.jsの生成型が
  未作成なら`./node_modules/.bin/next typegen`を先に）。
- **フルスイート（backend全体・`-m postgis`・frontendの`vitest`全体）はCIの持ち物であり、
  手元の完了条件に含めない。** `.github/workflows/ci.yml`がpushのたびに
  backend ruff→pytest（PostGIS統合テスト込み、`-n auto --dist loadgroup`で並列）・
  frontend eslint→tsc→vitestを実行する。手元でのフル実行はこれと**同じ答えを、
  並列化できない開発機で、共有のテストDBを掴みながら**出し直すことになる
  （`-m postgis`は`ridecompass_test`を共有するため、並行セッションと実際に衝突する）。
  手元でフルを回すのは次の2つだけ: **CIが落ちた失敗の再現**と、**CIへ出せない変更**。
- **影響範囲が自分でも分からないときは、範囲を導出してから絞る**。シンボルの撤去・改名の
  ように複数ファイルへ波及する変更では、「分からないからフルスイート」ではなく、
  **`pytest backend/tests -q --co` （収集のみ、数秒）でimportが壊れたファイルを出し、
  変更したシンボルをgrepして参照元を出す**。実行するのはそこで挙がったファイルだけにする。
  フルスイートを「影響範囲の調査」に使わない——調査に使うと1回あたり数分を払いながら、
  どのファイルを直すべきかは結局出力から読み直すことになる。
- **コードの構造について何か言う前に、機械的に列挙する**。「◯◯は2か所にある」「これが
  唯一の定義だ」「この形は他に無い」は、思い出しで言わない——grepかASTで数えた結果を先に
  出す。**測った主張は押されても変わらないが、思い出しの主張は押されるたびに変わる**。
  実例: 「材料の導出は2か所」と言い、押されて3か所と言い直し、機械で数えたら6本あった
  （[T956](docs/records/tasks/T956.md)）。既に同じ目的の仕組みがあることにも、そこで初めて気づいた。
  **測る前に母集団の定義を書く**——定義を決めずに測ると、測っても押されるたびに数字が
  変わる（同じT956で削減行数を4回測り直し、−1,283→−1,688→−1,889→−2,174と動いた。
  毎回、母集団の取り方を変えていた）。**所要時間も同じで、実測の無い数字を出さない**。
  未計測なら「未計測」と言う。
- **手で操作を組み立てる前に、既に仕組み化されているもので済まないかを見る**
  ——仕組みは使われなければ無いのと同じで、しかも二重に作ることになる。規模・churnは
  `scripts/review_checks.py metrics`（3秒。git履歴を手で歩かない）、本番DBへの調査は
  `backend/scripts/run_probe.py`、docs・台帳の整合は`scripts/review_checks.py docs`。
- **同じ操作を3回やったら仕組み化を検討する**。手で組み立てる長いコマンド（環境変数の
  付け方・SSHの引用・転送と実行の連結）は、繰り返すたびに崩れる。本番DBに対する調査は
  `backend/scripts/run_probe.py`（手元から／`--in-container`で本番コンテナ内）へ寄せてある。
- **その作業に必要な最小の情報量で動く**。確認したいことが「この1ファイルが通るか」なら
  その1ファイルだけ、「この1関数の値が合うか」ならその1件だけを対象にする。手持ちで
  一番大きいコマンド（フルスイート・全タイル・全期間）を反射的に叩かない。
- **既に手元にある情報を取り直さない。テストの流し直しは重い作業だと自覚する**
  （backendのフルスイートは開発機で5〜10分）。失敗したファイル名・件数・エラー内容は
  直前の実行結果に出ている。**集計・確認のためだけに回し直さない**。回すのは
  「前回の実行時点から変えた結果」という、まだ持っていない情報を取るときだけ。
  並行して複数のテストプロセスを走らせない——共有の`ridecompass_test` DBを取り合って
  止まる（2026-09-19に実際に3組が滞留した）。

## キャッシュ方針（必読）

**キャッシュを新設・変更するときは docs/conventions/caching.md に従うこと。** 要点:

- 保持層は「共有範囲×寿命」の2軸で選ぶ。**Redisへ置くのは外部への負荷を肩代わりするものだけ**
  （失っても自前のPostGISから復元できるものはプロセス内かディスクへ置く）
- プロセス内キャッシュは`cachetools`（TTLCache/LRUCache）に統一する。追い出しを自前で書かない
- Redisへ持つときは`redis_json_cache.py`の`get_json`/`set_json`を使う（骨格を写経しない）
- 「判断」（鮮度・真偽値）をキャッシュしてよいのは、入力が全てキーにあり・単調かつ安全側で・
  TTLが許容窓から導かれているときだけ
- ディスクへ世代番号を使うなら、旧世代を削除する導線をセットで用意する（世代番号はディスクの
  古い実体を消さない）

## 修正の原則（必読）

**指摘・不具合を直すときは、症状ではなく欠陥を直す。** 直した結果が次の指摘になるのは、
次のどれかを飛ばしたときである。

- **範囲は性質から導く**: 直す対象を「症状が出た場所」で決めず、「その欠陥を成立させている
  性質」で決め、その性質に当てはまる箇所を機械的に列挙してから直す
  （導出の考え方は[design-principles.md](docs/architecture/design-principles.md)構造仕様12と同じ）。
- **検証は別の入力で行う**: 修正を書く根拠にした入力で測り直しても証拠にならない。とくに
  **検査の側を緩めて「検知0件になった」ことは、その検査が直った証拠にならない**——緩める前に
  検知できていた既知の1件が今も検知されることを、別途示す。
- **緩和は影響を測ってから入れる**: 除外・許可リスト・判定条件のゆるめを足すときは、それが
  検査対象から外す件数と母集団に対する割合を実測し、タスクエントリへ書く。測らずに入れた
  緩和は、直した件数より多くを静かに見逃す。
- **「実装した」ではなく「効いている」を確認して完了にする**: 新設した検知器・ガード・
  表示は、わざと壊した入力を1件通して落ちる/出ることを見てから完了扱いにする。
- **見積もりとずれたら、全体の結論より先にずれを報告する**: 実測が見積もりと違ったら、
  それを結論の補足へ回さない。反する内訳を落として全体を「改善」とまとめると、次に同じ
  場所を測るまで誰も気づけない。
- **性能は利用者から見た単位で測る**: 部品の計測（関数1本・木1本）は原因の切り分けにだけ
  使い、それで全体を判断しない。利用者が通る経路が複数あるなら、都合の良い側だけを測って
  結論にしない。
- **置き換えではなく削除を既定にする**: XをYへ置き換えるなら、「Xをただ消せない理由」を
  先に言えること。言えないなら消す。
- **変更のたびに「これで何が死んだか」を測り、同じコミットで消す**: 参照が0になった
  フィールド・関数・分岐・テストは、その変更が作った残骸である。次のタスクへ回さない。
- **いま消したものを見張るテスト・アサーションを足さない**: 消えたことは検査ではなく、
  変更そのものが示す。守りを足すのは、消していないものに対してだけ。
- **完了報告には実装の増減を添える**（聞かれてから出さない）。文書・テストと分けて数える。
- **新しい種類の成果物は、置き場と命名を先に決める**: 既存の慣習が無い所へ1本目を置くとき、
  書き始める前に決めて合意する。後から移せるが、決めずに進めると次に足す人も散らかす。
  分ける軸は「何を守るか」ではなく**それが何を読むか・何に属するか**
  （例: テストは docs/conventions/testing.md「ソースを読む検査は、専用ディレクトリへ置く」）。
- **段階を別タスクへ切り出すときは、切り出す先の中身を具体化してから切り出す**: 「あとは
  〜するだけ」と見えている状態で切り出さない。見えていないぶんは、切り出した瞬間に
  検討されないまま確定する。

**本番から外れたものを「残す」判断を自分でしないこと。** 1本化・置き換えの改修で、
置き換えられた側（旧実装・フォールバック・既定値）を残す理由として「回帰テストの
オラクルにする」「既存を壊さないため」「後で消す」を挙げない。**残したいと思ったら、
残す理由と、いつ通るのかを添えてユーザーへ確認する**——判断はユーザーのもので、
こちらで決めてコメントに書いて流さない。残したものは本番から到達しないぶん誰も直さず、
そのテストまで一緒に保守対象として残り続ける（実例: [T240](docs/records/tasks/T240.md)で
`compute_edge_cost`を「回帰テストのオラクル」として残すと着手時の方針に書き、
T536でそれを置き換えた`compute_edge_costs_bulk`まで同じ理由で残した。
[T956](docs/records/tasks/T956.md)で両方とそのテストを削除するまで1か月近く残骸だった）。
**置き換えた側が本番から呼ばれなくなったら、そのテストも同時に消す**——デッドコードの
テストは保守対象ではない。

**宣言をそのまま書き写すテストを作らないこと。** レジストリ・カタログ・定数に
ベタで書いてある値を、テストでもう一度書いて突き合わせるのは、同じ literal を2箇所へ
置いているだけで何も守らない（「この材料は登録されていない」「この材料の値式はNone」
「生成された集合 == 同じ内包表記」など）。宣言に対して書いてよいのは、**導出した母集団
すべてに対する不変条件**だけ（「全材料が空でない説明文を持つ」のように、要素が1つ増えた
ときに発火するもの）。手で名指しした数件に対する検査は母集団が導出されていないため
書かない（設計原則 構造仕様12）。

**「等価であること」だけを確かめるテストを残さないこと。** 新旧2実装の出力一致を見る
テストは、どちらが正しいかを誰も保証していない。残すのは**入力に対するあるべき値**を
書いた期待値テストで、それを**生きている1本の実装へ当てる**。移行が終わったら等価
テストは消す（実装ごと消えるので、等価テストだけが残ることはない）。

**検知器（`scripts/review_checks.py docs`）を足す条件は厳しい。** 検知器はプロジェクト
全体への一律チェックとして自動実行され、**実行タイミングも走査範囲もこちらで選べない**
（pre-push・CI・`--staged`・`--since`・全件）。したがって足してよいのは、
**どの文脈でも絶対に正しいと保証できるものだけ**である。次のどれかに当たるものは足さない:

- **許可リストが要る**——許可リストは誤検知を認めた印。「この綴りは外部の語彙だから除外」が
  必要なら、その検査は事実を見ていない
- **実装そのものを検査対象にする**（コードの書き方・import規則・型・レイヤーの不変条件）
  ——実装側の道具（lint・型検査・テスト）が持つ。検知器が実装の姿を知ろうとすると写し
  （スナップショット・定数表）を抱え、実装が変わった瞬間に黙って死ぬ
- **保存した過去の値と比べる**——それは検査ではなく報告

**基準を満たさないものを足した代償は大きい。** 2026-09-21の棚卸しで、32本のうち残ったのは
3本だった（[T986](docs/records/tasks/T986.md)）。撤去には文書・台帳・手順書の広範な
作り直しが伴った。**思いつきは起票して人が見る**ことにし、検知器にはしない。
自己申告のチェックを文章のルールとして足すこともしない。その場合も
**実行したコマンドと観測値をコミットメッセージへ書く**（「確認した」とだけ書かない）。
上の後半3項目は、検知器の候補を実測したうえで届かないと判定して置いたもの
（数字と判定は[T798](docs/records/tasks/T798.md)）。

## コミット時の同期ルール（必読）

コード変更と同期して更新すべきペアの漏れが繰り返し発生している。以下は**同一コミットで**
必ず実施すること。

- **backend側のAPIルーター・Pydanticモデル・レジストリ・domain定数を変更したら**、
  `backend/scripts/export_openapi.py`→`cd frontend && npm run generate:api`を実行し、
  `git diff --exit-code -- frontend/src/types/generated/`がクリーンであることを確認する。
- **規模M以上でAPI・ドメイン概念・レイヤー種を新設するタスクは、完了条件へ
  docs/architecture/追従を既定で含める**。docs（「現状」記述）はコード変更と
  同一コミットで更新する。
- **既存の仕組みと技術的に別方式の新しい配信・レンダリング機構（例: タイル焼き込み済み
  ramp軸に対する`dedicated_way_value_layer`のようなRedis経由way_id配信）を新設するときは、
  着手前に`docs/architecture/design-principles.md`の構造仕様3・8（1本道の追加点）がこの新しい機構にも
  適用されるかを点検し、適用されるなら軸ごとのファイル・関数・定数・propを新設しない
  汎用設計にする**（新しい種類の機構を作る時にだけ点検が漏れやすい）。
- **MVT焼き込み値（CASE式・材料タグ・domain純関数）を変更したら**、生成物
  （region-tile-config.json）を同一コミットで再生成する（タイル世代そのものは焼き込みSQLから
  導出されるため手で上げない。`app/infrastructure/cache_identity.py`参照）。
- **評価軸（`axis_definitions`テーブル）の新規追加・削除・既存軸の`shape_params`調整は、
  すべて`axis_admin`のAPI（軸スタジオのGUI、または直接API呼び出し。新規追加=POST、
  削除=unpublish→DELETE、公開軸の調整=unpublish→PUT→republish）経由で行う。
  **正本は本番DBだけ**で、リポジトリは軸の写しを持たない——スキーマはORMの宣言から
  `create_tables()`が作り、軸の中身は実行時の`GET /api/axis-catalog`がフロントへ配る。
  ビルド時の生成物（`frontend/src/types/generated/`）はすべてコードの宣言から決まり、
  DBを読まない。取り直せない管理データのバックアップは[T972](docs/records/tasks/T972.md)。
- **axis_admin API経由のDB行データ変更を完了扱いにする条件は[docs/conventions/deployment-sync.md]
  (docs/conventions/deployment-sync.md)参照**（開発DBのみの反映で完了扱いにしない）。
- **既存DBの行データを新しいコードが読めなくなる変更（Pydanticモデルの破壊的変更等）を
  含む`backend/**`の変更は、本番DBのデータ移行を完了させてからpushする**。
  `.github/workflows/deploy-backend.yml`はbackendの変更を検知すると本番へ自動デプロイする
  （対象と、コンテナを入れ替えるかの振り分けはそのワークフローが正本）。
  DB移行より先にpushすると、新コードが本番DBに残る旧形式データを読めず、
  `refresh_axis_definitions`等のfail-fast設計により本番backendが起動失敗する
  （本番障害の実績あり、詳細は[T396](docs/records/tasks/T396.md)参照）。
  対応順序は事前に次のいずれかを選ぶこと: 1) 本番DBのデータ移行を先に完了させてから
  push、2) 移行を即座に行えない場合は`workflow_dispatch`のみで手動デプロイへ切り替える、
  3) 新旧両方の形式を一時的に許容する後方互換コードを経由して段階的に移行する。
- **規模の目安**: S=1時間以内 / M=半日 / L=1日以上。台帳の`規模S`等はこの尺度で、
  起票時の見立てを表す（実施後に実際とずれたと分かったらタスク記録へ書く）。
- **1タスク=1コミット**（またはPR）。ただし**段階を切って進める規模のタスクは段階ごとに
  1コミット**にする（段階の境界で上記①②の検査を通すため。段階の切り方は同じ節の
  「段階（A/B/C…）を切って進めるとき」と同じ単位）。
- **規模M以上の変更は、着手前の最初のコミットで対応するタスクエントリを先に作成する**。
  作業内容が変わりうる大きめのタスクほど、途中で背景・方針判断が失われやすく、後から
  遡って記録を作ると経緯（なぜその設計にしたか）が失われるため。
  `docs/improvement-plan.md`は節見出し＋チェックボックス付きリンクの一覧のみを持ち、
  個々のタスクの背景・対応方針・実装メモは`docs/records/tasks/Txxx.md`へ分離されている:
  `docs/records/tasks/Txxx.md`を新規作成して詳細を書き、`docs/improvement-plan.md`の該当節へ
  `- [ ] [Txxx](docs/records/tasks/Txxx.md). タイトル 規模...`の1行を追記する。番号の採番・衝突時の
  振り直し手順は次の「作業ツリーの安全」節を参照。
- **判断・実行を保留する場合、「後で判断」等の一言メモで済ませず、影響範囲
  （保留することで何がブロックされるか・何が動かなくなるか）を明記した完全な
  タスクエントリとして起票する**。保留の理由だけでなく、保留し続けた場合に次に
  何が起きうるかを一緒に書く。
- **タスクエントリは「誰がやっても同じ品質になる」ように書く**。会話でしか共有して
  いない判断基準・母集団の決め方・完了の条件は、別セッションには届かない（記憶は
  リポジトリの外にあり、別環境では消える）。**そのタスクを実行する人が、対象をどう
  列挙し、何を満たせば完了で、何を根拠に残す/消すを決めるのかを、エントリだけ読んで
  再現できる状態にする**。「実測して判断する」と書いたなら、何をどう測るかまで書く。
- **完了扱いにする前に検査器を通す**: docs・タスク台帳・ソースコードの整合性（死んだ参照、
  記載漏れ、経緯記述、写経、`状態:`行との照合など）は`scripts/review_checks.py docs`が
  機械的にブロックする。push直前（`.githooks/pre-push`。`git config core.hooksPath .githooks`
  で有効化し、worktreeは共有設定を継承する。**コミットは無条件で通す**——重い検査を
  コミットのたびに払うと1タスクで何度も同じ答えを買うことになるため、門はpushに1つだけ置く）と
  CI（`.github/workflows/docs-consistency.yml`）が自動実行する。**走査範囲の指定は無く、
  常に全件**——検知器はどの走査範囲でも同じ答えになる事実だけを扱うため、差分に絞る
  必要がない。何を見ているかの正本は`scripts/review_checks.py`であり、その中身を
  このファイルへ書き写さない（写した側だけが古くなる）。
- **検査器にできず、人が見るしかないのは次の2つだけ**（どちらも上の「修正の原則」に従い、
  実行したgrepと観測値をコミットメッセージへ残す）:
  1. **共有フィールド・パターンを変更/廃止したとき**: 変更前のフィールド・パターンを
     リポジトリ全体でgrepし、①取り残された消費者がいないか、②**残る消費者が変更後の契約と
     両立するか**——従来その契約が無いことを前提に外側から別の値を与えていた箇所は、
     直した側が正しく動くぶんだけその指定が静かに巻き戻る——の両方を確認する。
  2. **タスクを`[x]`化するとき、本文に未実施の段階・派生が残っていないか**: 「見送り」
     （今後もやらない確定判断）以外は元タスクの本文へ残さず、`docs/improvement-plan.md`へ
     独立した`- [ ]`行として別Txxxへ起票する（`/task:next`等の機械抽出は`- [ ]`行しか
     見ないため、`[x]`化した時点で本文に埋もれた残項目は拾われなくなる）。検査器が
     `[x]`化の瞬間に候補を参考出力するので、その行の要否だけを判断すればよい。
- **作業に着手する際、周期レビューの閾値**（前回レビューから14日 or 実装コードの変更
  20,000行、どちらか早い方。または分割元タスク完了直後。
  `python scripts/review_checks.py trigger`で判定。前回レビューは
  `periodic-review/NNN` タグが持つ）**に該当していないか確認し、該当する場合は
  `/review`実施と`/code-review`実施提案を行う**（手順は
  [.claude/commands/review.md](.claude/commands/review.md)）。

## 作業ツリーの安全（必読）

このプロジェクトは並行セッション（複数のClaude Codeセッション・エージェント）が同じ作業ツリーを
同時に触りうる。以下を必ず守ること。

- **自分が変更したのではないファイルの変更を作業ツリーで見つけても、絶対に自動で戻さない**
  （`git checkout --`/`git reset`/`git stash`/`git clean`等）。「明らかに別エージェントの
  誤操作に見える」場合でも、まず並行セッションが作業中である可能性を疑う。対応が必要と
  思われる場合は、変更内容を報告してユーザーに確認してから行う。
  `git checkout --`等で破棄した未ステージの変更は、gitオブジェクトとして残らないため
  多くの場合復元不能である。
- **worktreeはタスク単位で作り捨てず、テーマ単位で使い回す**。frontendの依存は実体が
  32,000ファイル超あり、作り直すたびに`npm ci`の数分を払う。**`node_modules`を
  ジャンクション等で共有しないこと**——`next dev`が「プロジェクトルート外を指すリンク」
  として拒否するうえ、一方のworktreeを`git worktree remove --force`すると再帰削除が
  リンクの中へ入り共有元を壊す（実測、[T768](docs/records/tasks/T768.md)）。
- **最初から変更を目的としていない作業（調査・分析・レビュー系コマンド等）は、本体の
  作業ツリーではなく専用の `git worktree` で実施する**。読み取り専用の調査であっても、
  本体の作業ツリーを一切変更しない構成にすることで、並行セッションとの衝突・誤操作の
  リスクそのものを無くす。
- **バックグラウンドAgentへ実装タスクを委任するときは、`isolation: "worktree"`を必ず
  指定する**（isolationを指定しないと自分自身の並行作業やユーザーの手元の変更と衝突・
  混在するリスクがあり、「並行セッションが同じ作業ツリーを触りうる」という本節の前提が
  自分のAgent委任にもそのまま当てはまる。詳細は[T414](docs/records/tasks/T414.md)参照）。
- **improvement-plan.mdのタスク番号（Txxx）は、複数セッションが同時に「次の番号」を
  独立に計算するため衝突する**（複数回実績あり）。番号を決めてから実装を終えてコミットする
  までの時間が長いほど、その間に他セッションが同じ番号を使って先にpushしてしまう
  リスクが上がる。この時間差を縮めるため:
  1. 新規タスクの着手を決めたら、**`docs/records/tasks/Txxx.md`に背景1〜2行だけの最小限の
     スタブを作り、`docs/improvement-plan.md`の該当節へインデックス行を1本足した
     時点ですぐコミット・push**する（`git fetch origin master`で直前の状態を確認して
     から採番する）。対応方針・実装メモ・検証結果などの詳細は、push成功を確認した後の
     追加コミットで`docs/records/tasks/Txxx.md`へ書き足せばよい——「実装を終えてからまとめて
     起票」ではなく「番号だけ即確保してから中身を育てる」順にすることで、衝突しうる
     時間窓を実装時間全体から「fetch→2ファイルに1行ずつ書く→push」の数秒〜数十秒へ
     縮める。
  2. pushしようとして、または起票後の再fetchで、自分の番号が既に他セッションに
     使われていたと分かったら、**自分の側を次の空き番号へ振り直す**（既にmaster側へ
     載っている方はそのまま触らない）。見出しだけでなく、コード側のコメント
     （`# 改善計画Txxx`等）も含めて旧番号への参照を全てgrepし揃えて置換すること
     （置換漏れでコメントとドキュメントの番号がずれた実績がある）。振り直した経緯
     （旧番号・理由）をエントリ内に一言残す。
  3. 完了報告としてmasterへ最終pushする直前にも`git fetch origin master`をもう一度行い、
     その時点で採番が競合していないか確認する（作業完了ごとにmasterへ反映・次作業前に
     fetchする、という本ファイル全体の運用と地続き）。
